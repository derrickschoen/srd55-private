/**
 * Marks the synthetic species source created by guided lineage application.
 *
 * Kept outside the builder so portability code can recognize the row without
 * importing the builder and its UI-facing dependency graph.
 */
export const GUIDED_SPECIES_SOURCE_MARKER = 'guided:species-apply';
