# QSFOG-SVG-01: plan round cap reached with two measurement P1s open (asked 2026-09-13 23:36, supervisor)

Context: D624.1 picked the repeat arm + 5-px frame pilot. Sol wrote the plan (three rounds), astra reviewed (three rounds: 9 → 8 → 4 findings). Everything about Increment A (identity manifest, C1/C2 repeat arm, paired report with the restored brainstorm thresholds, block record, freshness/continuity attestations), the wrapper, discovery proof, staging, accounting and the capture seam is RESOLVED as of round 3 (astra's PS2 table: F1, F3-F8 resolved; supervisor verified the wrapper at its prescribed path: 186 tests / 52 files, 1/1 with the spec filter). Astra independently reproduced the darkness hatch count 2,731/16,384, Dx 37,286,343, all area-32 competitor counts, the 7/16 and 14/16 frame3 phase losses and 0/16 for frame5, and the 599-file discovery baseline.

What remains, both in Increment B's deterministic measurement (the "legibility gate" the frame must pass BEFORE any model call):

- **PS3-F1 (P1)**: the nearest-neighbour reducer still produces constant (all-zero) competitor masks in some 32-px and 64-px phases (darkness at 64: two phases; at 32: eight phases; grid at 32: four phases), which the contract must score NA = fail regardless of candidate. Astra's smallest fix: use the already-defined phase-aligned footprint reducers for the reference masks in nearest views too; keep nearest sampling for the observed RGBA and fog geometry; assert non-constant references across all 43 views. This is a bounded plan edit.
- **PS3-F2 (P1)**: astra implemented the plan's native detector equations twice (Node and Python integer arithmetic over the pinned stone floor PNG) and both give NCC = 0.109 for the frozen 5-px frame vs the 0.60 threshold. The equations are unambiguous (two implementations agree) — the frozen candidate simply fails the frozen native gate on the stone floor with no illumination/shade/obscurement. The plan's "lawful positive" (1.000000) was a hatch self-correlation that bypasses composition and edge extraction. So as written, Increment B can only end in a recorded deterministic rejection; the frame arm never runs.

Options:
A. **Split the unit (recommended).** Accept the plan for Increment A now (it is complete and reviewed) and dispatch it, so the C1/C2 repeatability arm runs tonight on 14039ca5 as D624.1 intended. Send Increment B's measurement to ONE cap-exceeding plan round limited to PS3-F1 and PS3-F2 (+ the two P2: freeze-digest ordering, block-record command templates). For PS3-F2 the round must either (i) justify the 0.60 native NCC threshold and the edge-mask definition against the actual purpose (is a frame that scores 0.109 against a busy stone-floor edge map really illegible, or is the reference/observed pairing wrong — e.g. it should be masked to the frame band, or measured on the reduced 32-px view the model actually sees at 128-px tiles), with numbers on the pinned PNGs; or (ii) state that the 5-px frame candidate fails and propose the smallest candidate change that passes, with the same numbers. No threshold lowering to reach green.
B. Accept astra's minimal corrections as-is: apply PS3-F1's reducer fix and let Increment B complete with a recorded deterministic rejection of the 5-px frame; the pilot's result would be "frame fails the legibility gate; no model arm" and the SVG-fog question returns to the brainstorm.
C. Stop QSFOG-SVG-01 here; record the plan as not implementable; return to D616 (Quietstone merge decision) with the fog regression unresolved.

Supervisor recommendation: A. The repeatability measurement has value independent of any candidate (it is what D616.1 said was missing), and PS3-F2 is a design question about the measurement, not a defect an implementer can fix in code. Nothing is dispatched on this unit until you rule.
