# DISPATCH P2 — party credential vault and token boundary (M, MINT-FREE, wt/party, PLAYWRIGHT_PORT=44514)

You are in /home/vagrant/PhpstormProjects/dnd-wt-party (branch wt/party).

THE BINDING PLAN is docs/design/2026-08-01-party-storage.md. It lives on THIS
branch (commit 9713853) and is NOT on main — if the file is absent from your
tree you were dispatched to the wrong worktree: STOP and report.

BOUND: section 6 in full (6.1 Location and lifetime, 6.2 Checkable seam, 6.3
Expiry and rejection), the unit row **P2** in section 12, the section-13 rows
PARTY-TOKEN-NEVER-TRAVELS / PARTY-FORGET-TOKEN / PARTY-CREDENTIAL-STATE, and
the section-14 file row `src/party/credentials.ts`. Sections 2.6, 2.7 and 2.8
are supervisor-proved facts — build on them, do not re-derive. Section 11's
D108 bar applies to the one control you build.

NOT YOURS: sections 3, 4, 5, 7, 8, 9, 10 — no adapter, no locator parser, no
publish/refresh orchestration, no `party_document_states` (P3), no `_headers`
edit (P6), no party route or party screen (P5).

EXIT — section 12 row P2, quoted verbatim (Scope | Exit criteria):

> sessionStorage vault, anonymous lease, forget control, source import
> boundary, redaction/error policy | `PARTY-TOKEN-NEVER-TRAVELS` and
> forget/reload/expiry tests pass; token never crosses Worker RPC

## PRECONDITION — check this before writing a line

Section 12's dependency graph makes P2 depend on P0's credential-facing
contract. `src/party/storage/contracts.ts` must exist in your merge base and
export the closed `Forge` union and the closed `CredentialState` union
(section 4). **When this brief was written it did not exist** (no `src/party`
at wt/party edb5bb2). If it still does not, STOP and report — do NOT declare
your own copy of either union. A second `Forge` is precisely the defect
section 13's PARTY-FORGE-EXHAUSTIVE exists to kill.

## AMENDMENTS — D146 (2026-08-01) postdates the doc and WINS

- **D146 closes section 15 OQ-3 to option A.** Section 6.1's sessionStorage
  vault is law, not a recommendation; OQ-3's option B (memory-only) and option
  C (durable browser-profile storage) are struck and may not be built behind a
  flag. There is NO fallback to localStorage, IndexedDB, OPFS or a cookie when
  sessionStorage is unavailable — an unavailable vault is a named refusal
  state, never a quieter store (D33).
- **D146 closes OQ-2 to option A**: v1 ships library AND characters. Section
  6.2's sentinel surface list therefore keeps BOTH item 1 (character export
  bytes) and item 2 (library export bytes). Item 2 is not constructible yet —
  section 2.2 proves no `LibraryExportDocument` exists under `src/` — so it is
  owed to DOC-L. See scope item 9; it may not be quietly dropped.
- **D146 closes OQ-1 to A and OQ-4 to A.** Consequence for you alone: the
  vault key is (forge, repository) exactly as section 6.1 says. One credential
  per repository. No per-path, per-role, per-character or per-librarian key.
- **D146 narrows sections 3 and 8's "explicit branch/ref" to default branch
  only.** Consequence for you alone: the vault key gains NO ref component. A
  locator-shaped key is a defect, not future-proofing.
- Count reconciliation, so you do not stop on it: D146 and lane-state.md say
  "10 units" while section 12 tables 12 rows — the three P1-* adapter lanes
  are one unit in that count. The TABLE is the spine and you are its P2 row.

## MINT-FREE

You mint nothing: no migration, no share-wire version, no character-backup
document version, no character-state snapshot version. Frozen with an EMPTY
diff vs your merge base: migrations 0000-0026, wire v1-v16, existing a7-v*
assertions. Every `db/schema/**` change belongs to P3. If your work appears to
need a table, a column or a version, that is a dispatch error — STOP.

FLOORS: lane-state.md in your worktree governs; never below vitest 3,162
exit 0 (194 files), Playwright 88 exit 0 (20 specs), build 0.

## Scope

1. `src/party/credentials.ts` — the ONLY module in the repo that may turn
   pasted text into an authorization header (section 6.2). It exports a
   `PartyCredentialVault` over an INJECTED storage port. Follow the exact
   existing precedent, do not invent a shape: `browserProfileStorage()` in
   `src/pwa/browser-profile-storage.ts` (try/catch around the real `Storage`,
   `null` on failure) plus the `BackupHintStorage` port in
   `src/ui/screens/guided-builder/backup-hint.ts`, whose test double is
   `MemoryStorage` in `tests/unit/ui/backup-hint.test.ts`. Your real accessor
   returns `window.sessionStorage` and nothing else.
2. `CredentialLease` is all an adapter ever receives: a capability that
   applies headers to a request. Orchestration gets neither the raw string nor
   a serializable token object — `JSON.stringify(lease)` must not contain the
   secret, and that is an assertion, not a comment. GitLab's header is
   `PRIVATE-TOKEN` (D145, supervisor-proved); the GitHub and Codeberg schemes
   are P1-* fixture-spike facts — do not guess them here.
3. The anonymous arm is a REAL arm, not an empty token: it applies NO
   authorization header — not a blank one, not `Bearer `.
4. Forget removes the session entry, **invalidates leases already handed out**
   (a lease captured before Forget must refuse afterward — that is the sharp
   assertion, not the storage delete), and returns the connection to
   anonymous. It touches no OPFS data, no remote file, no non-secret config.
5. A mountable, labelled, keyboard-operable Forget control with a status
   announcement that does not rely on colour (D108, section 11), unit-tested
   through `installInteractiveDocument()` in `tests/fixtures/interactive-dom.ts`
   (`tests/fixtures/minimal-dom.ts` if that is the closer fit). **P5 mounts
   it.** You create no route and no `src/ui/screens/party/` screen.
6. Credential-state classifier (section 6.3): exhaustive over the closed
   `CredentialState`, no `default:` arm. You own the classifier and its honest
   fallback — a response that does not distinguish invalid, expired and
   revoked maps to `invalid-expired-or-revoked`, never to a guess and never to
   `network-failed`. The per-forge signal→state fixture bindings are P1-GH /
   P1-GL / P1-CB's; do not invent a provider's expiry header or status code.
7. Redaction: the request wrapper logs no request, header, response body or
   thrown object. `src/party/**` contains zero `console.*` calls — the repo
   has exactly two console references today (`src/db/database-lifecycle.ts:264`
   and a comment at `src/ui/ai-chat/mount.ts:20`); do not become the third.
8. Source-import boundary (section 6.2's final sentence), written in the style
   of `tests/unit/source-is-greppable.test.ts`: the test fails if
   `src/party/credentials.ts` is imported by anything under `src/backup/`,
   `src/sharing/`, `src/ui/screens/sheet/`, `src/worker/`, `src/db/`, or a
   document module. Write it as a DIRECTORY rule, not a filename list — this
   guard is the mechanism that will still hold when DOC-L lands.
9. `PARTY-TOKEN-NEVER-TRAVELS` under `tests/integration/party/`: paste a
   distinctive sentinel, run fixture-backed read/write flows through P0's mock
   fetch (no live network, ever — section 13), and assert the sentinel is
   absent from each surface in section 6.2's list. Verified call sites for the
   surfaces that exist today:
   - character export bytes — `exportCharacterBackup` at
     `src/backup/character-backup.ts:1571`;
   - database backup bytes — `DatabaseBackup.sqlite`, `src/backup/database-backup.ts:11-16`;
   - share URL — `encodeShareFragment` at `src/sharing/codec.ts:1221`;
   - captured console/error/analytics arguments;
   - structured sheet JSON — `src/ui/screens/sheet/sheet-view.ts:1036` and the
     planner's `<script type="application/json">` in
     `src/ui/screens/planner/agent-reference.ts` (D4: emit DATA, never
     instructions);
   - every Worker RPC request payload built during the flow — this is the
     "token never crosses Worker RPC" half of the exit criterion, and the
     import guard over `src/worker/**` is its structural complement.
   Also assert the sentinel appears ONLY in the mock request's authorization
   header and in the injected session store before Forget, and nowhere in that
   store afterward.
10. GAP RULE for the library surface. It cannot be scanned until DOC-L lands.
    Do not fabricate a stand-in document and do not silently omit the row.
    Express the surface list as a named constant with the library row present
    and marked owed to DOC-L, plus a test asserting the row is still owed, and
    NAME the gap in your report. It must never look covered.

## Negative controls — one per load-bearing assertion, with the failing test

| Assertion | Mutation | Test that must fail |
|---|---|---|
| Token reaches no serialization | inject the sentinel into the character export document, the db backup, the share fragment, the sheet JSON, or one RPC payload — one at a time | `PARTY-TOKEN-NEVER-TRAVELS` (the per-surface case) |
| Forget is real | (a) clear the UI only and leave the storage entry; (b) leave a previously issued lease live | `PARTY-FORGET-TOKEN` |
| Session-only, never durable | point the real accessor at `window.localStorage` | `PARTY-SESSION-NOT-DURABLE` — new name, an explicit extension of section 13; say so in your report |
| Reload in-session survives | make the vault memory-only (re-reading the same store must still find the token) | `PARTY-VAULT-SURVIVES-REMOUNT` |
| Expiry is named, not generic | map a known expiry signal to `network-failed`, or add a `default:` arm to the classifier | `PARTY-CREDENTIAL-STATE` |
| Anonymous is a real arm | emit an empty `Authorization` header when anonymous | `PARTY-ANONYMOUS-NO-HEADER` |
| Lease is not serializable to its secret | return the raw string from a lease getter or a `toJSON` | `PARTY-LEASE-NOT-SERIALIZABLE` |
| Boundary holds | add `import … from '../party/credentials'` to `src/backup/character-backup.ts` | `PARTY-CREDENTIAL-IMPORT-BOUNDARY` |
| Nothing is logged | add `console.debug(request)` to the request wrapper | `PARTY-NO-CREDENTIAL-LOGGING` |
| Service worker never touches a forge | delete the cross-origin early return at `tools/pwa/service-worker.ts:92-99` | `PARTY-SW-IGNORES-FORGE` |

CHROMIUM: the service-worker assertion (section 6.2's last paragraph) is the
only P2 claim a browser can prove today — extend `tests/browser/pwa.spec.ts`.
The vault's real reload / session-end journey needs P5's route; it is OWED to
P5 and you must say so in your report rather than approximate it in Chromium.
Add no route to make it testable.
