# DISPATCH P7 — PARTY integrated honesty gate (M, MINT-FREE, wt/party, PLAYWRIGHT_PORT=44519)

THE BINDING PLAN is docs/design/2026-08-01-party-storage.md: the unit row P7
in section 12, section 9 (the failure-and-honesty table — every row is an
assertion you owe), and section 13's named negative-control table. Implement
exactly P7. DOC-C, DOC-L, P0, P1-GH, P1-GL, P1-CB, P2, P3, P4, P5 and P6 are
NOT yours: P7 is the serial integration gate over them and finishes none of
them.

EXIT (section 12 unit row P7, quoted): scope is "Cross-forge negative
controls, offline/rate/revoked/deleted/invalid/conflict/too-large flows,
build and Chromium"; exit criteria are "`npm run test`, build, and Chromium
party specs pass with all external origins intercepted; no live network
ever".

PRECONDITION — YOUR FIRST ACTION, before writing anything. Verify in THIS
worktree that section 14's implementation inventory is actually present:
`src/party/storage/{contracts,github,gitlab,codeberg,create-storage}.ts`,
`src/party/{credentials,controller,client}.ts`, `db/schema/party.ts`,
`src/worker/handlers/party.ts`, `src/ui/conflict-notice.ts`,
`src/ui/screens/party/`, `tests/fixtures/party-storage/`, and the section-10
`Content-Security-Policy: connect-src` line inside the `/*` block of
`public/_headers` (it is absent at the time this brief was written — I read
the file). If any is missing, STOP and report which — do not stub an adapter,
do not add the CSP line yourself (P6 owns it), do not write the party UI. A
gate that supplies its own subject proves nothing.

MINT-FREE in EVERY registry: zero migrations, zero share-wire versions, zero
backup document versions, zero snapshot schema versions. If a gate appears to
need one, that is an upstream defect — STOP and report it.

AMENDMENTS (rulings newer than the doc — these WIN):
- **D146** closes section 15's open questions and overrides section 3's "an
  existing repository and an explicit branch/ref" and section 8's "plus an
  optional branch/ref": **default branch only**. Assert no branch/ref picker
  and no branch-create call anywhere in the gate; the public-join parser
  takes a bare repository URL. D146 also fixes OQ-1 (a designated librarian
  writes `library/`; each player writes only their own character path), OQ-2
  (v1 ships library AND characters — all ten P-units), OQ-3 (sessionStorage +
  explicit Forget; durable at-rest storage NOT taken), OQ-4 (one repo per
  party, top-level `library/` and `characters/`).
- **OQ-6 (delete UI) and OQ-7 (multiple library subset files) are NOT closed
  by D146.** Gate what P4/P5 actually shipped. If no delete control exists,
  do not invent a delete journey; if only `party-library.json` is consumed,
  do not assert subset selection. Record which reading you found, do not
  choose one.
- **D130** overrides section 11's "no Firefox, WebKit, or mobile support is
  implied": Chromium on ANY viewport, and the ngrok phone walkthrough must
  work. The party journey gets at least one narrow-viewport (phone-width)
  pass — labels, focus and the conflict/refusal notices readable there.
- D145/D106: PARTY ships inside v1 and extends the gate, so a red P7 blocks
  publication. That is why nothing here may be softened to reach green.

SCOPE (numbered; each item is an assertion, not a suggestion):
1. **PARTY-NO-LIVE-NETWORK.** A guard that fails any unhandled request to
   `api.github.com`, `gitlab.com`, `codeberg.org` with the URL AND the test
   name. Note the constraint: `vitest.config.ts` has NO `setupFiles` and
   config edits are forbidden, so the guard is an imported helper (e.g.
   `tests/helpers/no-live-network.ts`) that every party test file imports,
   plus a Playwright `route` fallback registered for all three origins in
   every party spec. Prove the guard by making it fire, not by its absence.
2. **Cross-forge contract suite.** ONE assertion body executed against all
   three adapters from `tests/fixtures/party-storage/` recorded fixtures —
   PARTY-FORGE-EXHAUSTIVE. GitHub/Codeberg blob `sha` and GitLab
   `last_commit_id` must be indistinguishable to the suite; both arrive as
   `RepositoryRevision`.
3. **Every section-9 row, end to end through the controller and UI**, not at
   the adapter alone: `network-failed` with `writeState` distinguishing
   not-sent from unknown; `rate-limited` showing `retryAt` only when the
   response proves it, else "Retry time not supplied"; the four
   `CredentialState` arms as named states with their actions; deleted repo
   vs `not-found/unknown`; `not-found/object` never deleting local data;
   `conflict` refusing and preserving both sides; `too-large` with proved or
   explicitly unknown byte/limit; invalid hand-edited JSON; per-file partial
   refresh; and the post-write-token-unavailable row showing "Published;
   refresh required before another publish", never "up to date".
4. **PARTY-TOKEN-NEVER-TRAVELS** (section 6, the security core) across all
   six surfaces named there — character export bytes, whole and selected
   library export bytes, character and public-party share URLs, exported
   database bytes including the `party_*` tables, captured log/error
   arguments, and the structured sheet JSON — plus: sentinel present in
   sessionStorage and the mock authorization header before Forget, absent
   from storage after, and the source-boundary test failing if a credential
   module is imported by backup, sharing, sheet, Worker, database or
   document modules.
5. **The service-worker claim must not be proved vacuously.** `vite.config.ts`
   serves a NO-FETCH dev worker (its fetch handler exists only in the
   production emission), so a Playwright dev-server run cannot show that the
   worker declines forge requests — it has no handler to decline with. Pin it
   against the emitted production artifact / `tools/pwa/service-worker.ts`
   cross-origin early return (lines 92-99) instead, and say in the test's
   comment why the browser route was rejected.
6. **PARTY-NO-FALSE-FRESHNESS**, **PARTY-HAND-EDIT-ROLLBACK** (full DB image
   comparison, not a row count), **PARTY-NO-TRUNCATION** (exact original byte
   hash), **PARTY-CONFLICT-NO-LWW** (and the shared `conflict-notice` closed
   union carrying BOTH `character-revision` and `repository-revision`),
   **PARTY-PUBLIC-READ-ONLY** (write/delete controls ABSENT from the DOM, not
   disabled), **PARTY-HOSTILE-STRING-MARK** (`data-free-text` on every
   remote-derived name/path/excerpt; sheet JSON omits them),
   **PARTY-OPFS-IS-TRUTH** (zero external requests at boot),
   **PARTY-CSP-CONNECT-SRC** re-run against the BUILT output, not only source.
7. **Specs.** If P5 shipped `tests/browser/party.spec.ts`, EXTEND it (strict
   superset; never remove a case) and add the failure-flow journeys in
   `tests/browser/party-honesty.spec.ts`. Unit/integration work goes under
   `tests/unit/party/` and `tests/integration/party/`.

NEGATIVE CONTROLS — the exit bar, not paperwork. Every load-bearing assertion
above gets a named mutation in `tests/fixtures/party-mutations.mjs`, following
the apply/restore convention of `tests/fixtures/level-up-mutations.mjs`
(assert exactly one match before editing, back up the pre-mutation source,
restore cleanly). Each of section 13's named controls must exist there, be
APPLIED, and be observed killing a test — report the exact test name that
failed for each. A control whose owning unit already ships a mutation entry is
RE-RUN after integration, not re-authored. A control that does not kill
anything is a finding about the assertion, and you report it as one.
