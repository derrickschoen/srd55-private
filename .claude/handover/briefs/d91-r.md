# DISPATCH D91-R — resource computation, sheet projection, rendering, print (L, MINT-FREE, wt/pwa, PLAYWRIGHT_PORT=44477)

Requires migration 0026 (D91-M) merged to main first.

THE BINDING PLAN is docs/design/2026-07-31-d91-resource-maxima.md sections
4 (computation seam), 5 (sheet and print rendering), 6 (test strategy), and
section 7 UNIT R for scope/exit. Implement exactly Unit R — no migrations,
no db/schema/**, no src/rules/class-resources-srd.ts edits (Unit M owns
those; if the parser output shape blocks you, STOP and report).

AMENDMENTS (these WIN over the doc, everywhere they conflict):
- D122: add `size: letter;` to the existing `@page` rule in
  src/ui/screens/print/styles.css (this unit owns that line).
- D123+D134 REPLACE the 30-box threshold EVERYWHERE it appears — section
  2.5's threshold paragraph, section 5.1's `maximum <= 30` / `> 30` branch,
  section 6's large-point-pool test row, and section 7 Unit R's steps —
  with SHAPE-BY-TYPE at every level:
  * BOXES at every level (a number plus exactly that many inert boxes):
    rage, channel_divinity, wild_shape, second_wind, favored_enemy,
    bardic_inspiration, action_surge, indomitable, uncanny_metabolism,
    persistent_rage_recovery, divine_intervention,
    wild_resurgence_conversion, nature_magician_conversion, paladins_smite,
    faithful_steed, tireless, natures_veil, stroke_of_luck, innate_sorcery,
    sorcerous_restoration, magical_cunning, contact_patron.
  * `Remaining: ____ / N` at every level (no boxes ever): lay_on_hands,
    sorcery_points, focus_points (owner-ruled point pools).
  A resource NEVER changes print shape with level. Boxes always mean uses.
  Classification lives in ONE exhaustive no-default map keyed by the
  resource-kind unions from src/domain/class-resources.ts.
- Everything else in sections 4-6 stands: closed SheetResourceMaximum
  union (computed|absent, no collapse); own-class-level resolution;
  class-qualified Channel Divinity never summed; shared slots via the
  existing slots() combinator with the sole-subclass-caster guard; Pact
  separate; invalid content = absent detail, never {} or zero; visible
  row/structured-fact/box-count parity; section 6's negative-control table
  adapted to shape-by-type (the Lay-on-Hands control becomes: render boxes
  for lay_on_hands → the shape-by-type test fails; keep the Bard
  Charisma-change and Paladin 5x controls as written).
