RULES (binding, restated): You are the MERGE AUTHOR (gpt-6-astra, high, RESUMED — you wrote .tmp/runs/compact/decisions-compact-draft.md r1). Same write scope: ONLY that file (overwrite in place to r2). No other file, no git writes, no claude / other agents / consensus skills; skills under ~/.codex do NOT apply; no tests/builds/servers. Model calls WILL fail. End with the named marker.

SUPERVISOR REVIEW of r1 (Claude, after reading all 609 lines and running reference checks). Findings to fix in r2:

F1 (P1, completeness): 86 digest rows classified ACTIVE are cited nowhere in the draft. The list with their binding text is in .tmp/runs/compact/active-uncited.txt (id | date | author | kind | topic | status | binding). For EACH: either (a) add/merge it into an existing bullet with its citation, (b) add it to Part C with an explicit reason (superseded by <id> / obsolete because <what completed> / record only), or (c) if a whole program is closed (e.g. August experiment cycles D402–D483, BG3 lanes, Luna protocol reruns), one Part C line per id is still required when the digest called it ACTIVE — the archive is not a substitute for the disposition. Many are OWNER rulings; an owner ruling silently vanishing is the failure mode the owner asked us to avoid. Report the a/b/c count.

F2 (P1, staleness): the draft was written while work continued. Fold in .tmp/runs/compact/new-since-merge.md (D673–D676, 7 lines): BLIND-01 plan r4 is ACCEPTED with binding note N1 (sha 9db2a045fac70e98b6f8c647b7c06087aefe1040158d228dbaa737c678dbd9a7, 331 lines), B0 accepted (commit d71860ad), B1 verified/committed (a73cda49), B2 in flight; D674 = owner ruled the 28-blob purge runs ONCE batched with the VIS-FIELD landing (replace the "pending owner scope" bullet). Header: next free id is D677. Part B BLIND-01 section must reflect this; D672 line in Part C stays.

F3 (P2, readability): hundreds of word+number tokens lost their space ("approximately12", "all26 rooms", "native128px", "base65,536 plus semantic32,768", "load1-minute", "capture dimensions…24 boards", "brutal-pool30/pool3"). Repair every `[a-z][0-9]` and `[0-9][a-z]` join that is not an identifier, hash, path, seed id or unit (5ft, 30ft, 2d6, 1d6, 10ft, 128px are fine ONLY with the conventional spacing you would write by hand: "128 px", "5 ft", "1d6"). Do a systematic pass, not spot fixes.

F4 (P2, precision): bullets that fuse many ids into one long sentence lose the actual numbers/paths the rule depends on in a few places — e.g. line 44 Opus 5 seat/effort bullet and line 561 conflict are the same thing said twice; line 41 says astra HIGH for "BLIND-01 reduction" (an exception, now historical — drop); Part A "Verification" bullet on timeouts (81–83) is three bullets for one rule (merge into one with the named exceptions listed). Remove any remaining duplication between Part A and Open conflicts (state the conflict once, in Open conflicts, and have the Part A bullet point to it).

F5 (P3): line 7/8 "at least daily under D552" — D552's daily compaction applied to RULES.md; state simply "the supervisor re-compacts periodically (D552)". Line 10: say pre-D649 commit ids (before 2026-09-17 purge) need translation via the commit map; post-purge ids are current.

Keep everything else. Keep the 16 `(verify)` flags (the supervisor will put them to the owner as one list). Target ≤ 750 lines. Print the F1 a/b/c counts, the new line count and a per-part line count. End with `MERGE R2 DONE`.
