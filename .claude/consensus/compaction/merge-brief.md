RULES (binding): You are the MERGE AUTHOR (gpt-6-astra, high). Sandbox workspace-write on the MAIN repo, but you may WRITE ONLY .tmp/runs/compact/decisions-compact-draft.md (and may overwrite it as you iterate). No other file, no git writes, no claude / other agents / consensus skills; skills under ~/.codex do NOT apply; no tests, builds, servers. Model calls WILL fail. End with the named marker.

OWNER RULING (2026-09-18, verbatim): "compact .claude/decisions.md into a streamlined document with no repeated directions and remove the overridden decisions and the ones that are so old they are not relevant anymore. you can archive the full decision log". The full chronology will be archived at .claude/decisions-archive/2026-09-18-full-chronology.md (unchanged bytes). Your draft becomes the NEW .claude/decisions.md after supervisor verification and an independent astra review.

INPUTS (read all, in full):
- Nine digests: .tmp/runs/compact/digests/digest-01.md … digest-09.md — one table row per block of the old file (columns: id, date, author, kind, topic, status, supersedes, superseded-by, binding, why-not-active) plus per-chunk "Cross-chunk notes". Chunks 01–03 cover D495 down to D32 plus the D1–D31 tail (file order was newest-first there); chunks 03–09 cover D496 → D671 ascending. Row statuses were judged WITHOUT sight of other chunks: you must reconcile across chunks (a later id's `supersedes` marks the earlier row SUPERSEDED even if its own chunk said ACTIVE; a later restatement of the same rule collapses into one bullet citing all ids).
- .claude/RULES.md — a topic-grouped compaction of D1–D555 made 2026-09-05 (D552). Fold its rules into the new document where still active (check each against the digests; D556+ may have changed them); the new decisions.md replaces RULES.md, so nothing in it may be silently lost — if a RULES.md bullet has no digest support and is not contradicted, keep it with its citations.
- .claude/RULE-CONFLICTS.md — open conflicts; carry any still-open ones into a "Open conflicts" section.
- The original file .claude/decisions.md is readable if a digest row is ambiguous (grep the id; do not read it wholesale).
- .tmp/runs/compact/referenced-ids-code-docs.txt — 250 D-ids cited from src/tests/tools/docs. EVERY one must resolve in the new document: either it appears as a citation on an active rule, or it has an index line (see Part C).

OUTPUT STRUCTURE of .tmp/runs/compact/decisions-compact-draft.md:
- Header (≤12 lines): what this file is, that it wins over every other guidance file, the archive path, that new decisions are still APPENDED chronologically under Part D with the next free id (D673), that the supervisor re-compacts periodically, date of this compaction.
- Part A — Standing rules, grouped by topic (licensing/provenance; roles & routing; consensus protocol & dispatch mechanics; verification/gates/evidence; git/worktrees/landing; architecture & type-system invariants; rules-engine content decisions; VTT/combat/visibility/AI-DM; experiments/judging/model economics; reporting & records; anything else). One bullet per DISTINCT direction, ≤3 lines, stated so a worker who never saw the archive can obey it, ending with its citations `[D…/D…]`. No repeated directions: when several ids say the same thing, one bullet, all ids. Owner rulings and supervisor rules are both binding; mark owner-originated bullets with `(owner)`.
- Part B — Current state of in-flight units and pending owner items (VIS-FIELD landing, COHORT-01, BLIND-01 (D671: Dodge-in-place), ELEVATION-02, TEST-PERF-01/perf, purge of .tmp logs, parked findings for the owner): for each, the binding sequencing/scope decisions still in force with citations; ≤15 lines per unit. Nothing that already completed.
- Part C — Index of retired ids: one line each for every SUPERSEDED / OBSOLETE / RECORD-ONLY id that is cited anywhere in referenced-ids-code-docs.txt OR that another retained bullet names: `D### — <≤12-word gist> — superseded by D### / obsolete (why) / record only`. Ids not cited anywhere and not referenced need no line (the archive has them). Sub-ids (D635.41) count as ids.
- Part D — Chronological append zone: empty except a comment line saying appends go here.

QUALITY BAR: (1) no active ruling lost — if unsure whether something is still active, keep it and flag `(verify)`; (2) no repeated directions; (3) exact numbers, paths, names, model ids preserved where they are the rule; (4) target size 600–1000 lines total; (5) do not invent rules; (6) findings (F-notes) become rules only where they state a lesson that is a rule — cite them.

At the end print a short summary: counts of active bullets per topic, Part C line count, any digest rows you could not classify (list ids), any RULES.md bullet you dropped and why. End with the marker `MERGE DONE` on its own line.
