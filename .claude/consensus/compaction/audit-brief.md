RULES (binding): You are an independent FIDELITY AUDITOR (gpt-6-astra, high, FRESH). Sandbox read-only on the MAIN repo; no file writes, no git writes, no claude / other agents / consensus skills; skills under ~/.codex do NOT apply; no tests/builds/servers. Model calls WILL fail. Deliver your report as your final message. End with the named marker.

CONTEXT: `.claude/decisions.md` (1.78 MB, 1106 blocks, append-only chronology of owner rulings and supervisor decisions) is being compacted under the owner's instruction: "compact into a streamlined document with no repeated directions and remove the overridden decisions and the ones that are so old they are not relevant anymore; archive the full log". A merge author produced .tmp/runs/compact/decisions-compact-draft.r1.md (609 lines: Part A standing rules by topic with citations [D…], Part B in-flight units, Part C index of retired ids). The supervisor already found: 86 ACTIVE-classified ids uncited, staleness vs D673–D676, glued word+number tokens, some duplication. Do NOT re-report those.

YOUR TASK — fidelity of what IS in the draft, against the original file:
1. Sample 45 bullets from Part A and Part B: every 12th bullet starting at the 3rd, plus all bullets that state a NUMBER, a PATH, a SHA or a model id (cap 45 total; list which). For each: open the cited ids in .claude/decisions.md (grep `^## D<id>` and read that block; blocks may be long — read enough to find the cited rule), and judge: FAITHFUL / DISTORTED (the draft says something the source does not, softens/hardens it, or mis-cites the id) / MERGED-WRONG (two ids folded into one bullet that contradict each other or whose later one supersedes the earlier while the bullet keeps the earlier text). Quote the source phrase for every non-FAITHFUL verdict.
2. OWNER rulings from the last 30 days (D586.x owner entries, D600+): list every OWNER-authored block whose id is absent from the draft and whose text contains a direction still executable (not a completed one-off). Print id + one-line gist.
3. Any Part A bullet that is a REPORT (numbers of a run, a sha pin of a completed step) rather than a standing rule → list for removal.
Report: table of the 45 verdicts; the owner-absent list; the report-not-rule list; a one-paragraph overall judgement. End with `AUDIT DONE`.
