
OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
model: gpt-6-astra
provider: openai
approval: never
sandbox: read-only
reasoning effort: medium
reasoning summaries: none
session id: 01a0b236-798c-7973-b05a-1c1fc7403093
--------
user
RULES (binding): You are the REVIEWER (gpt-6-astra, MEDIUM per D627, FRESH session) for COHORT-01 Batch 1. Worktree /home/vagrant/PhpstormProjects/dnd-wt-seam-verify — DETACHED clean checkout at e92ebc93 (= 6887755f, the WALL-01 candidate on claude/vis-field, + Batch 1; node_modules symlinked). Sandbox read-only; in-memory when /tmp is read-only; no edits, no git writes, no claude / `claude -p` / other agents / consensus skills; skills under ~/.codex do NOT apply; never port 4173; no full vitest, Playwright, build. Readable: the FROZEN plan .tmp-plans/2026-09-17-cohort-01-plan.md in your worktree (sha256 4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69, 403 lines incl. amendments N1/N2 — verify), the MAIN repo /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static's .claude/decisions.md (D643, D648–D651) and .claude/consensus/cohort-01/{impl-b1-sol.md,impl-b1-fix-r1-sol.md}. Model calls WILL fail. Report exact commands and numbers; end with the named marker.

TASK: review `git diff 6887755f e92ebc93` (3 files, 191+/21−: tools/d569-second-family-manifest.ts, tests/unit/tools/{d569-second-family-manifest,d569-v5}.test.ts) against the plan's Batch 1 + N2. Supervisor verification: tsc 0/0; V-freeze 0 (v1/v5 manifests and the brutal/brutal-b/brutal-2/hard-2 fixtures byte-identical); my mutant (legacy projection also drops `round`) → 2 red; second-family 50/50; d569-v5 27/27 (was failing at collection); blind-experiment 46 + the six frozen-family reds. CHECK: (1) the projection is narrow — exactly the committed own `$.encounter.state.foggedCells`, applied to committed bytes only, never to generated bytes; raw hashes checked before projection; reproduce the 20/20 single-key structural diffs in memory with the real generator (as you did for the plan review). (2) Rejection witnesses: any other committed difference still fails; a legacy key in a v2 fixture is rejected; ignoring regeneration 2 is still caught (the plan's :179 mutant). (3) v1 semantics: which assertions were RETIRED for the frozen brutal-2 family (productivity) and which retained (integrity, static brutal checks, hashes, ledger, namespaces) — is anything lost that the plan did not authorize? Is the strict v1/v2 schema union sound (v2 fields cannot leak into v1 validation and vice versa)? (4) N2: v5 keeps cells at the manifest-declared 8,192 and pins the exact refusal of live 32,768 rows; is the pin a real enforcement witness (a synthesized live row rejected by the registered validator, not a string compare on a hand-thrown error)? Confirm no production cap value changed and that the v6 seam in tools/d569-blind-experiment.ts is correctly deferred to Batch 6 (outside Batch 1's allowlist). (5) The six blind-experiment reds: confirm they are the helper-fed-committed-bytes artefact (:294–296) the plan assigns to Batch 6, not a Batch 1 regression. (6) tsc 0/0; no fixture bytes changed; no added line > 120. OUTPUT: findings B1-Fn with severity P1/P2/P3, file:line, evidence, fix; then exactly "VERDICT: ACCEPT" or "VERDICT: REJECT (n P1, m P2)"; end with REVIEW DONE.

codex
I’ll verify the frozen plan and diff, then run the targeted checks and in-memory regeneration review within the read-only constraints.

exec
/bin/bash -lc 'pwd && git status --short && git rev-parse HEAD && sha256sum .tmp-plans/2026-09-17-cohort-01-plan.md && wc -l .tmp-plans/2026-09-17-cohort-01-plan.md && git diff --stat 6887755f e92ebc93' in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
/home/vagrant/PhpstormProjects/dnd-wt-seam-verify
e92ebc934d8e475d1e3a8d41a800b10b4d5db97e
4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69  .tmp-plans/2026-09-17-cohort-01-plan.md
403 .tmp-plans/2026-09-17-cohort-01-plan.md
 .../unit/tools/d569-second-family-manifest.test.ts | 87 ++++++++++++++++++++-
 tests/unit/tools/d569-v5.test.ts                   | 35 +++++++--
 tools/d569-second-family-manifest.ts               | 90 +++++++++++++++++++---
 3 files changed, 191 insertions(+), 21 deletions(-)

exec
/bin/bash -lc 'cat .tmp-plans/2026-09-17-cohort-01-plan.md' in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
# VIS-FIELD-01 / COHORT-01 implementation plan r3 (D643 option A1)

Date: 2026-09-17. Baseline: detached clean `807d2e3c8cbbda11cfa26d078ac7739f2954e27e`.
D643 A1 keeps historical hard-2 and adds only three solid-wall brutal v2 families.

## Outcome and hard gates

1. WALL-01 is a hard predecessor. Generation cannot start until its D642 corner seal is
   accepted on a clean checkout. Recheck `src/vtt/room-generator.ts:1227-1231` still defines
   `generatedLineIsOpen` as `tier === 'none' && !blocksSight`. Re-derive all three Batch-9
   expectations against the accepted WALL-01 tree before editing tests; if any stated result
   differs, stop and amend/review this plan rather than copying changed-code output.
2. Before edits, inventory SHA-256 for the v1/v5 manifests and every file under historical
   6203, 6206, and 6207 directories. Repeat after every batch. No byte may change in:
   `tests/fixtures/d569-second-family-manifest.json`,
   `tests/fixtures/d569-blind-experiment-manifest.json`,
   `tests/fixtures/arena-basis-brutal`, `arena-basis-brutal-b`, or `arena-basis-brutal-2`.
3. Never rewrite a historical fixture/manifest, hand-edit generated JSON, add a D466 override,
   use `--terrain-profile`, or relax membership. One failing room rejects all ten seeds; no
   nine-room cohort and no hash is retained.
4. Expectations may come only from frozen pre-change bytes, the hand calculations named here,
   or hashes made after membership and double-generation gates. Generated changed-code output
   is never an oracle for a behavioural expectation.
5. No full Vitest, Playwright, build, or port 4173 in a batch. Later landing gates are separate.
6. Fixed scope is 10 batches with Allowed-file counts `3/3/10/10/10/8/4/4/3/1`: 52 unique
   paths = 18 existing + 34 new. A batch may touch only its manifest and accepted earlier files.

Common checks after every batch:

- **T:** `npx tsc -p tsconfig.app.json --noEmit` and
  `npx tsc -p tsconfig.node.json --noEmit` -> exit 0/0.
- **V:** `git diff --check` -> empty; `git status --short` contains only allowed/earlier paths.
- **V-freeze:** `git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2` -> exit 0.

## D569 demonstrate-then-normalize ledger

1. **Historical regeneration pins:** demonstrate each of 20 committed second-family fixtures
   differs from each canonical rebuild at exactly `$.encounter.state.foggedCells`; preserve raw
   hashes, remove exactly that own key from parsed COMMITTED state, then compare both rebuilds.
2. **New fixture pins:** dated namespace proof -> decoded structural/productivity membership ->
   two independent temp generations with empty `diff -rq` -> `sha256sum` = OpenSSL -> pin.
3. **Second-family v2 pin:** byte-copy hard-2 rows/hashes from v1, add proven 6211 rows, finalize,
   hash v2 independently, then place that hash in v6.
4. **Experiment v6 pin:** byte-carry all v5 fields except version, primary brutal, and nested
   second-family path/hash; pin the final outer v6 manifest hash separately from its contents.
5. **Behaviour pins:** use only Batch 9's explicit ray/path/table calculations, rechecked after
   WALL-01. Preserve stale values only as history comments.
6. **Cap pin:** D643 fixes 32,768; measure the hand-authored oversized block, never derive a cap.

## Pre-Batch-2 namespace reservation gate

Run this after accepted WALL-01 and before adding any range to source/tests. The first command
uses the authoritative MAIN file, not the stale 22,707-line worktree copy; the second audits
tracked consumers. Copy commands, stdout, and statuses verbatim into the dated v2 ledger entry.

```text
rg -n -o -e '6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
rg -n -o -e '6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' tests/fixtures tools
rg -n -o -e '\b6209\b|\b6210\b|\b6211\b' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
```

Verified 2026-09-17 output is, verbatim: command 1 -> `23048:6209xxx`,
`23048:6210xxx`, `23048:6211xxx`, `23061:6209xxx`, `23061:6210xxx`,
`23061:6211xxx`, `23071:6209xxx`, `23071:6_209_001`, status 0; command 2 ->
empty stdout/status 1; command 3 -> `23068:6209`, `23068:6210`, `23068:6211`,
status 0. Lines 23048/23061 are costing/authorized reservations, 23068 is D645's
shorthand plan summary, and 23071 is D646's review example: none is prior seed use.
Record one reservation covering all three families before any rejection/reselection.
Any new non-reservation match is a hard stop. If a range later fails, retain it as rejected,
audit a fresh contiguous range in all three representations, and have the supervisor amend this
plan's exact Batch-2 seed table, one Batch-3/4/5 Allowed-file manifest, and all downstream
manifest/route constants before creating replacement bytes. Unamended replacement paths are
unauthorized; the other two initial reservations remain reserved.

## Ordered batches

### Batch 1 — validator normalization and historical semantics

**Allowed files (3):**

- `tools/d569-second-family-manifest.ts`
- `tests/unit/tools/d569-second-family-manifest.test.ts`
- `tests/unit/tools/d569-v5.test.ts`

**R:** add a per-fixture 20-row structural-diff table (one legacy path each, hence today's 40
regeneration violations). Add rebound-hash witnesses proving (a) any second state-field/value
difference still yields both `first_regeneration` and `independent_regeneration`, and (b) a
NEW v2 brutal fixture containing `foggedCells` is rejected even if its hash is rebound. Retain
the two-generation divergence witness. These are hand-authored mutations, not regenerated pins.

**Implementation:** make the schema a strict v1/v2 union. Hash raw bytes first. For v1 hard and
brutal, and v2's carried hard-2 only, parse COMMITTED bytes, require the legacy key, delete only
own `encounter.state.foggedCells`, canonicalize with one newline, and compare each current
canonical rebuild to that projection. Never project generated bytes. V2 brutal-2-v2 compares
raw committed/current canonical bytes and rejects the legacy key. V1 retains integrity plus
static budget/caster/terrain checks but no longer recertifies frozen brutal productivity; v2
retains full current membership/productivity. `d569-v5.test.ts` imports/asserts only the v5/v1
paths and version, uses `canonicalD569SecondFamilyRegeneration`, and becomes green because this
normalization makes v1 valid; it must never resolve v6. `seed - 6203000` may remain only there.

**M:** keep the key, drop all state, normalize generated bytes, allow a new-v2 legacy key, and
ignore regeneration 2; the 40-count, extra-diff, new-v2, and divergence witnesses kill each.

**T/V:** common checks; serially run the two listed suites -> 20/20 one-key demonstrations,
zero v1 violations, all negative witnesses green.

### Batch 2 — RED active membership and frozen-productivity retirement

**Allowed files (3):**

- `tests/unit/vtt/arena-basis-brutal-v2.test.ts` (new)
- `tests/unit/vtt/room-generator.test.ts`
- `tests/unit/vtt/arena-basis-brutal-b.test.ts`

**R:** after the reservation gate, declare exact 6209001-10, 6210001-10, 6211001-10 paths;
expect exactly 30 missing fixtures. Add decoded clones for a nonproductive living monster, a
positive-movement-only Dash monster that MUST pass, and a targeted offensive option whose
resolved origin has a blocked or total trace and MUST fail.

**Implementation:** mirror `arena-basis-brutal-b.test.ts` but order proof before hashes:
identity/reducer load; brutal budget and 90-100% spend; implemented caster; terrain density/count;
then preserve the existing productivity predicate exactly: each living monster has some valid
option with `movementCostFeet > 0` OR an attack/save/cast/world-object slot. A Dash-only monster
therefore remains productive. If the qualifying option has targeted attack/save/cast components,
each target trace from `resolution.mechanics.finalPosition` must be sight-clear and non-total;
movement-only/world-object qualification does not invent a LOS target. Cite the contract and
`room-generator.ts:1227`'s stricter generated-room predicate. Mandatory offense is out of scope.

Move precisely these active assertions: retire `room-generator.test.ts:588-601` productivity for
frozen 6203001-10 while retaining its 6203001-3 byte/digest pins; add equivalent generation and
productivity assertions for 6209. In `arena-basis-brutal-b.test.ts:292-397`, retain structural,
reducer, SHA, and round-execution pins but retire only 378-393's 6206 productivity assertion.
Batch 1 retires only the analogous 6207 productivity assertion. Each comment cites D635/D642/
D643: frozen bytes are history; 6209/6210/6211 own the live contract. No skip/todo.

**M/T/V:** force productivity true, delete the movement arm, and treat `blocksSight:true` as open;
the nonproductive, Dash-only, and blocked-target clones kill each mutant. Common checks; active
suite is RED only for 30 missing files; frozen suites have no productivity red.

### Batch 3 — generate primary brutal-v2 6209001-6209010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-v2/seed-6209001.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209002.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209003.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209004.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209005.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209006.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209007.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209008.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209009.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209010.json`

**R/M/Generation/V:** Batch-2 tests are RED first. Twice run
`npx vite-node tools/generate-arena-basis.ts -- --difficulty brutal --seed 6209001 --rooms 10 --out <fresh-temp>/arena-basis-brutal-v2`.
Assert no parsed `terrainProfile` and `d466GeneratedRoomOverride(seed) === null` for 10/10.
Require empty `diff -rq A B`, then decoded membership 10/10 and both mutants. Only then move A
unchanged to Allowed files and rerun membership/common checks. Do not compute hashes yet.

### Batch 4 — generate brutal-b-v2 6210001-6210010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json`

**R/M/T/V:** identical to Batch 3 with seed 6210001/directory `arena-basis-brutal-b-v2`:
two temps, empty diff, no profile/override, decoded membership 10/10, mutants, common checks, no hashes.

### Batch 5 — generate D569 brutal-2-v2 6211001-6211010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json`

**R/M/T/V:** identical with seed 6211001/directory `arena-basis-brutal-2-v2`. End with one
membership-only run proving exactly 30/30 fixtures and every living monster; any failure rejects
that full ten-seed namespace and invokes the pre-Batch-2 replacement procedure.

### Batch 6 — v2/v6 manifests, identity boundary, and hashes

**Allowed files (8):**

- `tools/d569-second-family-manifest.ts`
- `tools/d569-blind-experiment.ts`
- `tests/fixtures/d569-second-family-manifest-v2.json` (new)
- `tests/fixtures/d569-blind-experiment-manifest-v6.json` (new)
- `tests/unit/tools/d569-second-family-manifest.test.ts`
- `tests/unit/tools/d569-blind-experiment.test.ts`
- `tests/unit/tools/d569-v6.test.ts` (new)
- `tests/unit/vtt/arena-basis-brutal-v2.test.ts`

**R:** strict version/path/range/ledger tests, the exact v5-v6 structural diff, outer-v6 hash,
and cardinalities. Add primary-hard and second-family-hard cross-version witnesses: a v5 observed
row with the same unchanged seed/state/visual hashes must be rejected by v6 as wrong experiment
identity. A missing/wrong identity must also reject. V6 ordinals use manifest order/seed maps;
no `seed - 6203000` assumption survives in a v6 path.

**Implementation:** v2 is `d569-second-family-manifest-v2`: byte-carried hard-2 rows/hashes,
proven brutal-2-v2 rows/hashes, the dated verbatim audit/classification, and this exact ordered
ten-namespace union: 5117xxx, 5118xxx, 6203xxx, 6204xxx, 6206xxx, 6207xxx, 6208xxx,
6209xxx, 6210xxx, 6211xxx. Keep v1's five-entry historical constant/policy unchanged; add a v2
constant. V2 overlap validation exempts only the expected fixture in its own exact cohort
namespace (hard 5118xxx; brutal 6211xxx); cross-cohort and all other overlaps still fail. Removing
ANY of ten namespaces fails an exact-ledger test; preserve the precedent mutant that removes
6204xxx. V6 is
`d569-blind-experiment-v6`: unchanged hard 5117, primary brutal 6209, nested v2 path/hash, every
other v5 field byte-carried. V1/v5 constants and files remain historical; add v2/v6 constants.

Create a separate `D569ExperimentIdentity = {version, manifestSha256}` from the exact outer
manifest bytes at the load boundary. Pin/export the final v6 SHA-256 outside the manifest.
V6 dry-run cells carry both fields; include both in `sessionKey`. V6 observed rows must carry an
exact match, and row lookup/duplicate/missing keys include identity. Branch by version so v5's
cell shape, legacy session-key derivation, and observed-row matching remain byte-behaviourally
unchanged. Content/state/visual hashes remain truthful content hashes, never overloaded as the
experiment identity. Thus unchanged hard cells cannot cross v5/v6.

The active `d569-v6.test.ts` access MUST set `regenerate:
canonicalD569SecondFamilyRegeneration`; the fixture-reader helper at
`d569-blind-experiment.test.ts:294-296` may serve historical/synthetic unit tests only. Add a spy
or deliberately divergent canonical regeneration proving v6 actually calls it. Only after all
30 membership checks pass, hash each fixture with SHA/OpenSSL, finalize/hash v2, insert its hash
in v6, finalize/hash v6, and pin that outer hash in code/test.

**M:** omit experiment identity from session/row keys; substitute a v5 hard row; use committed
bytes as v6 regeneration; remove 6204 or any ledger row; over-broaden the 5118/6211 exception;
admit v1/6203, a 6207/6209 overlap, a fourth carry-forward difference, or generation 2 divergence.
Named witnesses fail each mutant.

**T/V:** common checks; both hash tools agree for 30 fixtures, v2, and outer v6. Targeted D569
suites have zero violations and exact core/with-hint counts 480/660, primary-brutal 240/330,
second-family core 480. Historical v1/v5 suites remain green and never execute v6.

### Batch 7 — versioned arena and rerun routes

**Allowed files (4):**

- `tools/ai-dm-arena.ts`
- `tests/unit/tools/ai-dm-arena.test.ts`
- `tools/ai-dm-rerun-packet.ts`
- `tests/unit/tools/ai-dm-rerun-packet-protocol.test.ts`

**R/Implementation:** first fail exact parser/path/wrong-family cases. Add arena basis labels
`brutal-v2` -> 6209 and `brutal-b-v2` -> 6210, both difficulty brutal. At rerun definitions near
lines 26/547 add closed protocols `brutal-10-v2` and `brutal-10-b-v2`, exact ten-seed arrays and
directories. Historical labels/protocols retain their old paths and behaviour.

**M/T/V:** map new labels to old paths, swap new ranges, and remove an old label; exact new and
historical route tests kill each. Common checks; arena has prior 6 + 2 labels, rerun prior 3 + 2.

### Batch 8 — LOS audit and blind fixture report

**Allowed files (4):**

- `tools/los-cover-era-audit.ts`
- `tests/unit/tools/los-cover-era-audit.test.ts`
- `tools/blind-context-fixture-report.ts`
- `tests/unit/vtt/blind-turn-context.test.ts`

**R/Implementation:** first expect 30 missing rows. Add file-backed `brutal-v2`,
`brutal-b-v2`, `brutal-2-v2`; preserve historical entries. LOS audit is 93 rooms (63+30), ten
per new family. Blind report is 63 cases (33+30), decoded from exact committed paths.

**M/T/V:** omit or alias a family; family/seed/path/hash/count assertions fail. Common checks;
focused outputs are 93 and 63, with 10/10/10 new rows and 63/63 schema-valid cap-fit cases.

### Batch 9 — hand-authored behaviour pins

**Allowed files (3):**

- `tests/unit/vtt/composite-turn-proposals.test.ts`
- `tests/unit/vtt/engine-opportunity-movement-intel.test.ts`
- `tests/unit/vtt/speculative-planning.test.ts`

**R:** preserve the three current deterministic RED titles before edits. Re-run the following
geometry calculations after WALL-01; a discrepancy stops the batch, never selects a new oracle.

**Implementation:** composite hard 5117005 labels are exactly `Dodge`, `End Turn`, with no
`Longbow`. Comment the four corner-ray derivation: Scout `(13,3)` -> Fighter `(1,2)` has two rays
enter `(7,2)` and two on shared edge `(7,2)/(7,3)`; -> Cleric `(2,4)` has two on
`(7,3)/(7,4)` and two entering `(7,4)`; -> Wizard `(1,6)` all cross column 7. D635/D642 block
every set. Room 8 pins monsters 1 `(22,1)`, 2 `(21,1)`, 4 `(21,3)` to zero-foot Dodge at current
cells; monster 5 keeps 55-ft Dash `(22,5)->(12,6)` = ten 5-ft steps + 5 difficult. Replace the
stale comment; these are hand calculations.

For real integration, replace frozen 5117001 with a 15x8 state: Scout A at `(13,4)`, actionless
monster B at `(13,6)`, unacted 30-ft player P at `(1,4)`, ids `combatant:A/B/P`, base walls
`(7,0..3)` plus `(8,5..7)`, and a typed speed-set effect with
`enginePlanningSpeedFeet(A) === 0`. Define exact extracted selector strings
`DA=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:A'}})`
and `TAP=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:P'}})`.
Production `buildHostScenarioMenu` yields labels `[Longbow + Longbow -> combatant:P, Dodge]` and
exact menu rank 1 `adjacency:${DA}:${TAP}` (P moves to `(12,4)`, 55 ft) then rank 2
`visibility:${DA}:${TAP}` (P path `(1,4)->(0,3)->(0,2)->(0,1)->(1,0)`, 20 ft); each has one
proposal reference, only influencing player P, radius 60, and score `1*60=60`. Longbow reach
cannot flip inside 15x8; Dodge contributes no movement-change fact.

The mutation adds `(8,4)`, completing `(7,3)/(8,4)`. Assert the production trace changes
sight-clear -> blocked. Because A's speed is explicitly zero it cannot use Astra's 25-ft escape
to `(8,3)`; A and B now propose `[Dodge,Dodge]`, targeted dependencies disappear, and the exact
menu is `[]`. This is a contrast, not normalization of the frozen empty scene. Recheck the full
base/mutated oracle after WALL-01; any difference stops for plan review.

Separately test the eight-entry slice through `computeHostSplitCandidates` with two Scouts A/B,
P at `(4,2)`, awaiting A's turn, speed 30/radius 60, and a controlled query table. Define
`S(x)=canonicalJson({actorId:'combatant:'+x,selector:{kind:'combatant',combatantId:'combatant:'+x}})`.
All adjacency/reach baselines are false -> true; all visibility baselines are true -> false.
The nine non-shadowed candidates and hand oracle are:

|rank|fact key|refs|legal P destination/path ft|score|result|
|---:|---|---:|---|---:|---|
|1|`adjacency:S(A):S(P)`|A+B|`(2,1)`/10|`2*60=120`|kept|
|2|`adjacency:S(B):S(P)`|B|`(7,3)`/20|60|kept|
|3|`adjacency:S(P):S(A)`|A|`(2,1)`/10|60|kept|
|4|`adjacency:S(P):S(B)`|B|`(7,3)`/20|60|kept|
|5|`reach:S(A):shortsword:S(P)`|A|`(2,1)`/10|60|kept|
|6|`reach:S(B):shortsword:S(P)`|B|`(7,3)`/20|60|kept|
|7|`visibility:S(A):S(P)`|A|`(6,1)`/15|60|kept|
|8|`visibility:S(B):S(P)`|B|`(6,3)`/15|60|kept|
|9|`visibility:S(P):S(A)`|A|`(6,1)`/15|60|excluded|

All costs are independently <=60; the controlled port rejects every unlisted destination.
Order is score, refs, then canonical fact-key bytes (`adjacency` < `reach` < `visibility`). Assert
exact keys/ranks 1-8 and excluded key 9, not merely `<=8`. This stipulated dependency/query table
tests only grouping, score, canonical tie ordering, filtering, and the eight-entry slice; it is
not evidence for D642 corner sealing, which the production integration witness above proves.

**M/T/V:** restore old Longbow/Dash values, let A move despite speed zero, disable corner sealing,
change score multiplication to 62/61, reverse the tie order, or remove `.slice(0,8)`; exact
labels/keys/paths, production mutation, and nine-row table fail. Common checks; run serially.

### Batch 10 — 32,768-byte cap rejection witness

**Allowed files (1):**

- `tests/unit/vtt/blind-turn-context.test.ts`

**R/Implementation:** clone a normal `projectEngineSemanticBoard`, set one schema-valid creature
name to 40,000 characters, prove schema parsing succeeds, then render normally. Assert measured
UTF-8 semantic block exceeds `BLIND_SEMANTIC_BOARD_MAX_BYTES === 32_768` and exact `RangeError`
states required/max byte counts. Baseline may be green because this witnesses existing policy.

**M/T/V:** disable `blind-turn-context.ts:1304-1307` (or make `>` false): the test must turn RED
because oversized context is accepted. Restore, verify source hash, run focused test/common checks.

## Final targeted verification and handoff

Run only touched suites serially: second-family, blind-experiment, v5, v6, room-generator,
brutal-v2, brutal-b, arena, rerun protocol, LOS audit, blind context, and the three behaviour
files. Expected: zero failures; D569 counts 480/660/240/330 and second-family 480; 30/30 new
fixtures; audits 93/63. Repeat T, freeze inventory, `git diff --check`, verify exact 52-path union,
and inspect all new JSON as parsed state. Supervisor later runs the untruncated full gate.

## Open questions

None. Any failed post-WALL derivation or rejected namespace invokes the stated stop/amend/review
procedure; it is not an implementation-time choice.

## Round 2 disposition (closed/superseded)

F1 remains closed at lines 211-250; F4 remains closed at lines 25-26 and 198-209.
F2's r2 work remains at 50-73 and F3 is superseded by the corrected r3 oracle at 304-348.
Supporting requirements remain at 8-12, 85-102, 236-239, and 293-348.

## Round 3 disposition

- **PLAN-F3 (P2; lines 304-348):** the production scene now makes A speed zero, uses extracted actor-relative
  selector keys, and pins both base candidates plus the exact empty post-seal menu; the verified
  nine-row table is explicitly ranking/filtering-only.
- **PLAN-F2 (P2; lines 50-73):** both absence commands now cover every 000-999 plain and underscore seed plus
  `xxx`; MAIN provenance, verbatim results, reservation classification, and replacement rules stay.
- **PLAN-F5 (P2; lines 217-246):** v2 now requires all ten historical/current namespaces, exact own-cohort
  exceptions, removal-of-any-row tests, and the 6204 mutant.
- **PLAN-F6 (P2; lines 115-138):** Batch 2 now preserves positive movement OR productive slots, keeps Dash-only
  monsters valid, and applies resolved-origin trace checks only to targeted offensive components.

COHORT-01 PLAN R3 DONE

## Binding amendment N1 (astra r3, D650 — supervisor-appended; part of the frozen plan)

Batch 9 must construct the hand-authored Scout A with `profile.rules.speed: feet(0)` before encounter creation, retain the typed zero-speed effect, and assert both declared and effective speed are zero (attack declarations take their movement allowance from `subject.profile.rules.speed`, offer-declarations.ts:431–465, so the effect alone still lets A move 25 ft to (8,3) and keep its Longbow). Preserve the exact base and mutated labels, selector keys, scores and menus at the lines the plan states (base: Longbow ×2 + Dodge, adjacency and visibility keys, scores 60/60; with (8,4) added: Dodge/Dodge, menu []). Require two mutants: restoring A's declared speed to 30 while retaining the effect, and separately disabling corner sealing, must each fail the mutation expectations. Re-verify against the ACCEPTED WALL-01 before implementation acceptance. This correction is confined to the allowed speculative-planning test file; no production movement change is authorized.

## Binding amendment N2 (supervisor ruling D651 — part of the frozen plan)

Batch 1 exposed a second historical discontinuity beside the fog key: the v5 manifest registers `caps.semanticBytes = 8192` (the engine-server `SEMANTIC_BOARD_MAX_BYTES`), while rows the live blind runner produces today carry the D643 cap `BLIND_SEMANTIC_BOARD_MAX_BYTES = 32,768`, so `validate-first-arm.ts:369` refuses them ("Registered context caps mismatch"). Ruling: (1) a cell's `semanticContextCapBytes` / `baseContextCapBytes` are the MANIFEST's declared caps (the registration), not an engine constant — `tools/d569-blind-experiment.ts:285` (schema literal) and `:712` (cell cap) become version-aware: v5 keeps 8192 as history, v6 declares `caps.semanticBytes: 32768`; (2) `tests/unit/tools/d569-v5.test.ts` stays HISTORICAL: it keeps validating the v5 manifest (fog-key normalization included), its hashes and its synthesized rows, and it PINS that a row produced by today's live runner is refused with exactly `Registered context caps mismatch` — v5 cannot be re-run as registered and the test says so; no skip, no relaxed cap; (3) the v6 active test (Batch 6) validates live rows at 32,768. Mutants: v5 accepting a 32,768 row must fail; v6 accepting an 8192 row must fail. No production cap value changes.

exec
/bin/bash -lc 'git diff 6887755f e92ebc93 -- tools/d569-second-family-manifest.ts tests/unit/tools/d569-second-family-manifest.test.ts tests/unit/tools/d569-v5.test.ts' in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089b..93df1172 100644
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@ function room(path: (typeof ALL_FIXTURE_PATHS)[number]): GeneratedRoom {
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@ function codes(
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@ describe('D569 independent second-family manifest', () => {
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@ describe('D569 independent second-family manifest', () => {
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8..b8f6042e 100644
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@ import {
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -450,7 +451,7 @@ function classifiedInvalidCatalogForConsumer(
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -471,7 +472,7 @@ function evaluateRunnerTimeoutConsumers(): {
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -558,7 +559,7 @@ function evaluateRunnerTimeoutConsumers(): {
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -573,6 +574,19 @@ function evaluateRunnerTimeoutConsumers(): {
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
@@ -650,7 +664,7 @@ describe('D569 v5 replacement validation and paired analysis tools', () => {
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -664,26 +678,31 @@ describe('D569 v5 replacement validation and paired analysis tools', () => {
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095..a26ca872 100644
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@ export const D569_USED_SEED_NAMESPACES = [
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,7 +64,18 @@ const manifestSchema = z.strictObject({
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
+
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
 
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
@@ -283,8 +293,7 @@ function everyMonsterProductive(room: GeneratedRoom): boolean {
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@ export function brutalRoomMembershipViolations(
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@ function sameNamespace(
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@ export function validateD569SecondFamilyManifest(
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@ export function validateD569SecondFamilyManifest(
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@ export function validateD569SecondFamilyManifest(
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc "rg -n '"'^##.*D(643|648|649|650|651)|''^##.*D627'"' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md; cat /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01/impl-b1{,-fix-r1}-sol.md; cat package.json; cat vitest.config.ts" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
22677:## D627 — OWNER (2026-09-15 14:09): astra effort routing — high for plan reviews and planning, medium for everything else
23060:## D643 — OWNER (2026-09-17 19:36): "Do your proposal" — the supervisor's recommendations are authorized as a whole
23077:## D648 — SUPERVISOR (2026-09-17 20:38): FINDING AGAINST MY OWN REVIEW — WALL-F1 (D645) RETRACTED; a real D642 consequence found in scene-snapshot; COHORT-01 plan r2 REJECTED by astra r2 (0 P1, 4 P2) → r3 (final round) dispatched
23083:## D649 — OWNER (2026-09-17 20:56): "Purge now with filter-repo, force-push the mirror, recreate the four worktrees from the rewritten main" — DONE in place (purge.log)
23086:## D650 — SUPERVISOR (2026-09-17 21:08): COHORT-01 plan ACCEPTED WITH AMENDMENT by astra r3 (HIGH, final round) — FROZEN at sha256 2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802 (399 lines incl. binding note N1); Batch 1 dispatched on a new worktree; WALL-01 fix r2 re-dispatched after a sol capacity outage
23091:## D651 — SUPERVISOR (2026-09-17 21:25): two correct BLOCKED stops ruled — WALL-01 fix r2's four scene subjects (test-scene edits approved) and COHORT-01 B1's v5 cap discontinuity (amendment N2; plan re-frozen at sha256 4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69, 403 lines)

OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR] (network access enabled)
reasoning effort: high
reasoning summaries: none
session id: 01a0b20e-61fb-71f3-ad42-773bb45989b9
--------
user
COMMON RULES (binding, restated in full): You are the IMPLEMENTER (gpt-5.6-sol, high, FRESH session) for VIS-FIELD-01 batch group COHORT-01, Batch 1 ONLY. Worktree /home/vagrant/PhpstormProjects/dnd-wt-cohort-01 (branch claude/cohort-01 at 6887755f = claude/vis-field's WALL-01 candidate; clean; node_modules symlinked). Sandbox workspace-write in that worktree ONLY (+ /tmp). No git write commands (the supervisor commits). No edits under .claude/** or docs/**. Do NOT invoke claude, `claude -p`, any reviewer session, any consensus workflow or any other agent; skills under ~/.codex do NOT apply. No Playwright, no full vitest, no npm run build; never port 4173. Model calls WILL fail. NEVER regenerate an expectation from our own output; never delete or weaken a test; never fresh hashes without a printed derivation; NO byte of tests/fixtures/d569-second-family-manifest.json, tests/fixtures/d569-blind-experiment-manifest.json, tests/fixtures/arena-basis-brutal*, -brutal-b, -brutal-2 or -hard-2 may change (the plan's V-freeze gate: run it before and after). Report exact commands and numbers; end with the named marker. The MAIN repo's .claude/decisions.md (D643, D648–D650) is readable.

THE PLAN (binding, FROZEN): .tmp-plans/2026-09-17-cohort-01-plan.md in your worktree — sha256 2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802, 399 lines (verify with sha256sum before starting; if it differs, STOP). Read it fully. Implement EXACTLY "### Batch 1 — validator normalization and historical v1 semantics" — its Allowed files, its RED-first witnesses (write the failing tests FIRST and show them red), its mutants, its T/V commands and expected numbers, and the D569 ledger item 1 (for all 20 second-family fixtures the structural difference between committed bytes and the current generator's rebuild must be EXACTLY $.encounter.state.foggedCells — print the per-fixture structural diff; the normalization drops only that committed key for the comparison and never touches generated bytes or fixture bytes). Include the rejection witnesses the plan requires: a committed fixture differing in anything else still fails; a legacy foggedCells key present in a NEW (v2-style) fixture is rejected. Do NOT start Batch 2 or any generation. If the plan's Batch 1 text is ambiguous or infeasible at 6887755f (a cited line moved, a helper missing), STOP and report BLOCKED with the exact gap — that is the correct outcome, not a shortcut.

VERIFICATION: the batch's T and V exactly as the plan states; plus npx tsc -p tsconfig.app.json --noEmit; npx tsc -p tsconfig.node.json --noEmit; git diff --check; no line > 120 columns; git status --short = the Allowed files only; the V-freeze gate output before/after. OUTPUT: report (files, witnesses red→green, mutants, the 20-fixture diff table, remaining reds by title with cause), then "COHORT-01 B1 DONE" (or BLOCKED with the gap).

codex
I’ll verify the frozen plan hash and clean baseline first, then read the full Batch 1 instructions and run its pre-change V-freeze gate before touching files. I’ll stop immediately if any prerequisite or cited structure does not match.
exec
/bin/bash -lc "sha256sum .tmp-plans/2026-09-17-cohort-01-plan.md && wc -l .tmp-plans/2026-09-17-cohort-01-plan.md && git status --short && sed -n '1,460p' .tmp-plans/2026-09-17-cohort-01-plan.md" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802  .tmp-plans/2026-09-17-cohort-01-plan.md
399 .tmp-plans/2026-09-17-cohort-01-plan.md
# VIS-FIELD-01 / COHORT-01 implementation plan r3 (D643 option A1)

Date: 2026-09-17. Baseline: detached clean `807d2e3c8cbbda11cfa26d078ac7739f2954e27e`.
D643 A1 keeps historical hard-2 and adds only three solid-wall brutal v2 families.

## Outcome and hard gates

1. WALL-01 is a hard predecessor. Generation cannot start until its D642 corner seal is
   accepted on a clean checkout. Recheck `src/vtt/room-generator.ts:1227-1231` still defines
   `generatedLineIsOpen` as `tier === 'none' && !blocksSight`. Re-derive all three Batch-9
   expectations against the accepted WALL-01 tree before editing tests; if any stated result
   differs, stop and amend/review this plan rather than copying changed-code output.
2. Before edits, inventory SHA-256 for the v1/v5 manifests and every file under historical
   6203, 6206, and 6207 directories. Repeat after every batch. No byte may change in:
   `tests/fixtures/d569-second-family-manifest.json`,
   `tests/fixtures/d569-blind-experiment-manifest.json`,
   `tests/fixtures/arena-basis-brutal`, `arena-basis-brutal-b`, or `arena-basis-brutal-2`.
3. Never rewrite a historical fixture/manifest, hand-edit generated JSON, add a D466 override,
   use `--terrain-profile`, or relax membership. One failing room rejects all ten seeds; no
   nine-room cohort and no hash is retained.
4. Expectations may come only from frozen pre-change bytes, the hand calculations named here,
   or hashes made after membership and double-generation gates. Generated changed-code output
   is never an oracle for a behavioural expectation.
5. No full Vitest, Playwright, build, or port 4173 in a batch. Later landing gates are separate.
6. Fixed scope is 10 batches with Allowed-file counts `3/3/10/10/10/8/4/4/3/1`: 52 unique
   paths = 18 existing + 34 new. A batch may touch only its manifest and accepted earlier files.

Common checks after every batch:

- **T:** `npx tsc -p tsconfig.app.json --noEmit` and
  `npx tsc -p tsconfig.node.json --noEmit` -> exit 0/0.
- **V:** `git diff --check` -> empty; `git status --short` contains only allowed/earlier paths.
- **V-freeze:** `git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2` -> exit 0.

## D569 demonstrate-then-normalize ledger

1. **Historical regeneration pins:** demonstrate each of 20 committed second-family fixtures
   differs from each canonical rebuild at exactly `$.encounter.state.foggedCells`; preserve raw
   hashes, remove exactly that own key from parsed COMMITTED state, then compare both rebuilds.
2. **New fixture pins:** dated namespace proof -> decoded structural/productivity membership ->
   two independent temp generations with empty `diff -rq` -> `sha256sum` = OpenSSL -> pin.
3. **Second-family v2 pin:** byte-copy hard-2 rows/hashes from v1, add proven 6211 rows, finalize,
   hash v2 independently, then place that hash in v6.
4. **Experiment v6 pin:** byte-carry all v5 fields except version, primary brutal, and nested
   second-family path/hash; pin the final outer v6 manifest hash separately from its contents.
5. **Behaviour pins:** use only Batch 9's explicit ray/path/table calculations, rechecked after
   WALL-01. Preserve stale values only as history comments.
6. **Cap pin:** D643 fixes 32,768; measure the hand-authored oversized block, never derive a cap.

## Pre-Batch-2 namespace reservation gate

Run this after accepted WALL-01 and before adding any range to source/tests. The first command
uses the authoritative MAIN file, not the stale 22,707-line worktree copy; the second audits
tracked consumers. Copy commands, stdout, and statuses verbatim into the dated v2 ledger entry.

```text
rg -n -o -e '6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
rg -n -o -e '6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' tests/fixtures tools
rg -n -o -e '\b6209\b|\b6210\b|\b6211\b' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
```

Verified 2026-09-17 output is, verbatim: command 1 -> `23048:6209xxx`,
`23048:6210xxx`, `23048:6211xxx`, `23061:6209xxx`, `23061:6210xxx`,
`23061:6211xxx`, `23071:6209xxx`, `23071:6_209_001`, status 0; command 2 ->
empty stdout/status 1; command 3 -> `23068:6209`, `23068:6210`, `23068:6211`,
status 0. Lines 23048/23061 are costing/authorized reservations, 23068 is D645's
shorthand plan summary, and 23071 is D646's review example: none is prior seed use.
Record one reservation covering all three families before any rejection/reselection.
Any new non-reservation match is a hard stop. If a range later fails, retain it as rejected,
audit a fresh contiguous range in all three representations, and have the supervisor amend this
plan's exact Batch-2 seed table, one Batch-3/4/5 Allowed-file manifest, and all downstream
manifest/route constants before creating replacement bytes. Unamended replacement paths are
unauthorized; the other two initial reservations remain reserved.

## Ordered batches

### Batch 1 — validator normalization and historical semantics

**Allowed files (3):**

- `tools/d569-second-family-manifest.ts`
- `tests/unit/tools/d569-second-family-manifest.test.ts`
- `tests/unit/tools/d569-v5.test.ts`

**R:** add a per-fixture 20-row structural-diff table (one legacy path each, hence today's 40
regeneration violations). Add rebound-hash witnesses proving (a) any second state-field/value
difference still yields both `first_regeneration` and `independent_regeneration`, and (b) a
NEW v2 brutal fixture containing `foggedCells` is rejected even if its hash is rebound. Retain
the two-generation divergence witness. These are hand-authored mutations, not regenerated pins.

**Implementation:** make the schema a strict v1/v2 union. Hash raw bytes first. For v1 hard and
brutal, and v2's carried hard-2 only, parse COMMITTED bytes, require the legacy key, delete only
own `encounter.state.foggedCells`, canonicalize with one newline, and compare each current
canonical rebuild to that projection. Never project generated bytes. V2 brutal-2-v2 compares
raw committed/current canonical bytes and rejects the legacy key. V1 retains integrity plus
static budget/caster/terrain checks but no longer recertifies frozen brutal productivity; v2
retains full current membership/productivity. `d569-v5.test.ts` imports/asserts only the v5/v1
paths and version, uses `canonicalD569SecondFamilyRegeneration`, and becomes green because this
normalization makes v1 valid; it must never resolve v6. `seed - 6203000` may remain only there.

**M:** keep the key, drop all state, normalize generated bytes, allow a new-v2 legacy key, and
ignore regeneration 2; the 40-count, extra-diff, new-v2, and divergence witnesses kill each.

**T/V:** common checks; serially run the two listed suites -> 20/20 one-key demonstrations,
zero v1 violations, all negative witnesses green.

### Batch 2 — RED active membership and frozen-productivity retirement

**Allowed files (3):**

- `tests/unit/vtt/arena-basis-brutal-v2.test.ts` (new)
- `tests/unit/vtt/room-generator.test.ts`
- `tests/unit/vtt/arena-basis-brutal-b.test.ts`

**R:** after the reservation gate, declare exact 6209001-10, 6210001-10, 6211001-10 paths;
expect exactly 30 missing fixtures. Add decoded clones for a nonproductive living monster, a
positive-movement-only Dash monster that MUST pass, and a targeted offensive option whose
resolved origin has a blocked or total trace and MUST fail.

**Implementation:** mirror `arena-basis-brutal-b.test.ts` but order proof before hashes:
identity/reducer load; brutal budget and 90-100% spend; implemented caster; terrain density/count;
then preserve the existing productivity predicate exactly: each living monster has some valid
option with `movementCostFeet > 0` OR an attack/save/cast/world-object slot. A Dash-only monster
therefore remains productive. If the qualifying option has targeted attack/save/cast components,
each target trace from `resolution.mechanics.finalPosition` must be sight-clear and non-total;
movement-only/world-object qualification does not invent a LOS target. Cite the contract and
`room-generator.ts:1227`'s stricter generated-room predicate. Mandatory offense is out of scope.

Move precisely these active assertions: retire `room-generator.test.ts:588-601` productivity for
frozen 6203001-10 while retaining its 6203001-3 byte/digest pins; add equivalent generation and
productivity assertions for 6209. In `arena-basis-brutal-b.test.ts:292-397`, retain structural,
reducer, SHA, and round-execution pins but retire only 378-393's 6206 productivity assertion.
Batch 1 retires only the analogous 6207 productivity assertion. Each comment cites D635/D642/
D643: frozen bytes are history; 6209/6210/6211 own the live contract. No skip/todo.

**M/T/V:** force productivity true, delete the movement arm, and treat `blocksSight:true` as open;
the nonproductive, Dash-only, and blocked-target clones kill each mutant. Common checks; active
suite is RED only for 30 missing files; frozen suites have no productivity red.

### Batch 3 — generate primary brutal-v2 6209001-6209010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-v2/seed-6209001.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209002.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209003.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209004.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209005.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209006.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209007.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209008.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209009.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209010.json`

**R/M/Generation/V:** Batch-2 tests are RED first. Twice run
`npx vite-node tools/generate-arena-basis.ts -- --difficulty brutal --seed 6209001 --rooms 10 --out <fresh-temp>/arena-basis-brutal-v2`.
Assert no parsed `terrainProfile` and `d466GeneratedRoomOverride(seed) === null` for 10/10.
Require empty `diff -rq A B`, then decoded membership 10/10 and both mutants. Only then move A
unchanged to Allowed files and rerun membership/common checks. Do not compute hashes yet.

### Batch 4 — generate brutal-b-v2 6210001-6210010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json`

**R/M/T/V:** identical to Batch 3 with seed 6210001/directory `arena-basis-brutal-b-v2`:
two temps, empty diff, no profile/override, decoded membership 10/10, mutants, common checks, no hashes.

### Batch 5 — generate D569 brutal-2-v2 6211001-6211010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json`

**R/M/T/V:** identical with seed 6211001/directory `arena-basis-brutal-2-v2`. End with one
membership-only run proving exactly 30/30 fixtures and every living monster; any failure rejects
that full ten-seed namespace and invokes the pre-Batch-2 replacement procedure.

### Batch 6 — v2/v6 manifests, identity boundary, and hashes

**Allowed files (8):**

- `tools/d569-second-family-manifest.ts`
- `tools/d569-blind-experiment.ts`
- `tests/fixtures/d569-second-family-manifest-v2.json` (new)
- `tests/fixtures/d569-blind-experiment-manifest-v6.json` (new)
- `tests/unit/tools/d569-second-family-manifest.test.ts`
- `tests/unit/tools/d569-blind-experiment.test.ts`
- `tests/unit/tools/d569-v6.test.ts` (new)
- `tests/unit/vtt/arena-basis-brutal-v2.test.ts`

**R:** strict version/path/range/ledger tests, the exact v5-v6 structural diff, outer-v6 hash,
and cardinalities. Add primary-hard and second-family-hard cross-version witnesses: a v5 observed
row with the same unchanged seed/state/visual hashes must be rejected by v6 as wrong experiment
identity. A missing/wrong identity must also reject. V6 ordinals use manifest order/seed maps;
no `seed - 6203000` assumption survives in a v6 path.

**Implementation:** v2 is `d569-second-family-manifest-v2`: byte-carried hard-2 rows/hashes,
proven brutal-2-v2 rows/hashes, the dated verbatim audit/classification, and this exact ordered
ten-namespace union: 5117xxx, 5118xxx, 6203xxx, 6204xxx, 6206xxx, 6207xxx, 6208xxx,
6209xxx, 6210xxx, 6211xxx. Keep v1's five-entry historical constant/policy unchanged; add a v2
constant. V2 overlap validation exempts only the expected fixture in its own exact cohort
namespace (hard 5118xxx; brutal 6211xxx); cross-cohort and all other overlaps still fail. Removing
ANY of ten namespaces fails an exact-ledger test; preserve the precedent mutant that removes
6204xxx. V6 is
`d569-blind-experiment-v6`: unchanged hard 5117, primary brutal 6209, nested v2 path/hash, every
other v5 field byte-carried. V1/v5 constants and files remain historical; add v2/v6 constants.

Create a separate `D569ExperimentIdentity = {version, manifestSha256}` from the exact outer
manifest bytes at the load boundary. Pin/export the final v6 SHA-256 outside the manifest.
V6 dry-run cells carry both fields; include both in `sessionKey`. V6 observed rows must carry an
exact match, and row lookup/duplicate/missing keys include identity. Branch by version so v5's
cell shape, legacy session-key derivation, and observed-row matching remain byte-behaviourally
unchanged. Content/state/visual hashes remain truthful content hashes, never overloaded as the
experiment identity. Thus unchanged hard cells cannot cross v5/v6.

The active `d569-v6.test.ts` access MUST set `regenerate:
canonicalD569SecondFamilyRegeneration`; the fixture-reader helper at
`d569-blind-experiment.test.ts:294-296` may serve historical/synthetic unit tests only. Add a spy
or deliberately divergent canonical regeneration proving v6 actually calls it. Only after all
30 membership checks pass, hash each fixture with SHA/OpenSSL, finalize/hash v2, insert its hash
in v6, finalize/hash v6, and pin that outer hash in code/test.

**M:** omit experiment identity from session/row keys; substitute a v5 hard row; use committed
bytes as v6 regeneration; remove 6204 or any ledger row; over-broaden the 5118/6211 exception;
admit v1/6203, a 6207/6209 overlap, a fourth carry-forward difference, or generation 2 divergence.
Named witnesses fail each mutant.

**T/V:** common checks; both hash tools agree for 30 fixtures, v2, and outer v6. Targeted D569
suites have zero violations and exact core/with-hint counts 480/660, primary-brutal 240/330,
second-family core 480. Historical v1/v5 suites remain green and never execute v6.

### Batch 7 — versioned arena and rerun routes

**Allowed files (4):**

- `tools/ai-dm-arena.ts`
- `tests/unit/tools/ai-dm-arena.test.ts`
- `tools/ai-dm-rerun-packet.ts`
- `tests/unit/tools/ai-dm-rerun-packet-protocol.test.ts`

**R/Implementation:** first fail exact parser/path/wrong-family cases. Add arena basis labels
`brutal-v2` -> 6209 and `brutal-b-v2` -> 6210, both difficulty brutal. At rerun definitions near
lines 26/547 add closed protocols `brutal-10-v2` and `brutal-10-b-v2`, exact ten-seed arrays and
directories. Historical labels/protocols retain their old paths and behaviour.

**M/T/V:** map new labels to old paths, swap new ranges, and remove an old label; exact new and
historical route tests kill each. Common checks; arena has prior 6 + 2 labels, rerun prior 3 + 2.

### Batch 8 — LOS audit and blind fixture report

**Allowed files (4):**

- `tools/los-cover-era-audit.ts`
- `tests/unit/tools/los-cover-era-audit.test.ts`
- `tools/blind-context-fixture-report.ts`
- `tests/unit/vtt/blind-turn-context.test.ts`

**R/Implementation:** first expect 30 missing rows. Add file-backed `brutal-v2`,
`brutal-b-v2`, `brutal-2-v2`; preserve historical entries. LOS audit is 93 rooms (63+30), ten
per new family. Blind report is 63 cases (33+30), decoded from exact committed paths.

**M/T/V:** omit or alias a family; family/seed/path/hash/count assertions fail. Common checks;
focused outputs are 93 and 63, with 10/10/10 new rows and 63/63 schema-valid cap-fit cases.

### Batch 9 — hand-authored behaviour pins

**Allowed files (3):**

- `tests/unit/vtt/composite-turn-proposals.test.ts`
- `tests/unit/vtt/engine-opportunity-movement-intel.test.ts`
- `tests/unit/vtt/speculative-planning.test.ts`

**R:** preserve the three current deterministic RED titles before edits. Re-run the following
geometry calculations after WALL-01; a discrepancy stops the batch, never selects a new oracle.

**Implementation:** composite hard 5117005 labels are exactly `Dodge`, `End Turn`, with no
`Longbow`. Comment the four corner-ray derivation: Scout `(13,3)` -> Fighter `(1,2)` has two rays
enter `(7,2)` and two on shared edge `(7,2)/(7,3)`; -> Cleric `(2,4)` has two on
`(7,3)/(7,4)` and two entering `(7,4)`; -> Wizard `(1,6)` all cross column 7. D635/D642 block
every set. Room 8 pins monsters 1 `(22,1)`, 2 `(21,1)`, 4 `(21,3)` to zero-foot Dodge at current
cells; monster 5 keeps 55-ft Dash `(22,5)->(12,6)` = ten 5-ft steps + 5 difficult. Replace the
stale comment; these are hand calculations.

For real integration, replace frozen 5117001 with a 15x8 state: Scout A at `(13,4)`, actionless
monster B at `(13,6)`, unacted 30-ft player P at `(1,4)`, ids `combatant:A/B/P`, base walls
`(7,0..3)` plus `(8,5..7)`, and a typed speed-set effect with
`enginePlanningSpeedFeet(A) === 0`. Define exact extracted selector strings
`DA=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:A'}})`
and `TAP=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:P'}})`.
Production `buildHostScenarioMenu` yields labels `[Longbow + Longbow -> combatant:P, Dodge]` and
exact menu rank 1 `adjacency:${DA}:${TAP}` (P moves to `(12,4)`, 55 ft) then rank 2
`visibility:${DA}:${TAP}` (P path `(1,4)->(0,3)->(0,2)->(0,1)->(1,0)`, 20 ft); each has one
proposal reference, only influencing player P, radius 60, and score `1*60=60`. Longbow reach
cannot flip inside 15x8; Dodge contributes no movement-change fact.

The mutation adds `(8,4)`, completing `(7,3)/(8,4)`. Assert the production trace changes
sight-clear -> blocked. Because A's speed is explicitly zero it cannot use Astra's 25-ft escape
to `(8,3)`; A and B now propose `[Dodge,Dodge]`, targeted dependencies disappear, and the exact
menu is `[]`. This is a contrast, not normalization of the frozen empty scene. Recheck the full
base/mutated oracle after WALL-01; any difference stops for plan review.

Separately test the eight-entry slice through `computeHostSplitCandidates` with two Scouts A/B,
P at `(4,2)`, awaiting A's turn, speed 30/radius 60, and a controlled query table. Define
`S(x)=canonicalJson({actorId:'combatant:'+x,selector:{kind:'combatant',combatantId:'combatant:'+x}})`.
All adjacency/reach baselines are false -> true; all visibility baselines are true -> false.
The nine non-shadowed candidates and hand oracle are:

|rank|fact key|refs|legal P destination/path ft|score|result|
|---:|---|---:|---|---:|---|
|1|`adjacency:S(A):S(P)`|A+B|`(2,1)`/10|`2*60=120`|kept|
|2|`adjacency:S(B):S(P)`|B|`(7,3)`/20|60|kept|
|3|`adjacency:S(P):S(A)`|A|`(2,1)`/10|60|kept|
|4|`adjacency:S(P):S(B)`|B|`(7,3)`/20|60|kept|
|5|`reach:S(A):shortsword:S(P)`|A|`(2,1)`/10|60|kept|
|6|`reach:S(B):shortsword:S(P)`|B|`(7,3)`/20|60|kept|
|7|`visibility:S(A):S(P)`|A|`(6,1)`/15|60|kept|
|8|`visibility:S(B):S(P)`|B|`(6,3)`/15|60|kept|
|9|`visibility:S(P):S(A)`|A|`(6,1)`/15|60|excluded|

All costs are independently <=60; the controlled port rejects every unlisted destination.
Order is score, refs, then canonical fact-key bytes (`adjacency` < `reach` < `visibility`). Assert
exact keys/ranks 1-8 and excluded key 9, not merely `<=8`. This stipulated dependency/query table
tests only grouping, score, canonical tie ordering, filtering, and the eight-entry slice; it is
not evidence for D642 corner sealing, which the production integration witness above proves.

**M/T/V:** restore old Longbow/Dash values, let A move despite speed zero, disable corner sealing,
change score multiplication to 62/61, reverse the tie order, or remove `.slice(0,8)`; exact
labels/keys/paths, production mutation, and nine-row table fail. Common checks; run serially.

### Batch 10 — 32,768-byte cap rejection witness

**Allowed files (1):**

- `tests/unit/vtt/blind-turn-context.test.ts`

**R/Implementation:** clone a normal `projectEngineSemanticBoard`, set one schema-valid creature
name to 40,000 characters, prove schema parsing succeeds, then render normally. Assert measured
UTF-8 semantic block exceeds `BLIND_SEMANTIC_BOARD_MAX_BYTES === 32_768` and exact `RangeError`
states required/max byte counts. Baseline may be green because this witnesses existing policy.

**M/T/V:** disable `blind-turn-context.ts:1304-1307` (or make `>` false): the test must turn RED
because oversized context is accepted. Restore, verify source hash, run focused test/common checks.

## Final targeted verification and handoff

Run only touched suites serially: second-family, blind-experiment, v5, v6, room-generator,
brutal-v2, brutal-b, arena, rerun protocol, LOS audit, blind context, and the three behaviour
files. Expected: zero failures; D569 counts 480/660/240/330 and second-family 480; 30/30 new
fixtures; audits 93/63. Repeat T, freeze inventory, `git diff --check`, verify exact 52-path union,
and inspect all new JSON as parsed state. Supervisor later runs the untruncated full gate.

## Open questions

None. Any failed post-WALL derivation or rejected namespace invokes the stated stop/amend/review
procedure; it is not an implementation-time choice.

## Round 2 disposition (closed/superseded)

F1 remains closed at lines 211-250; F4 remains closed at lines 25-26 and 198-209.
F2's r2 work remains at 50-73 and F3 is superseded by the corrected r3 oracle at 304-348.
Supporting requirements remain at 8-12, 85-102, 236-239, and 293-348.

## Round 3 disposition

- **PLAN-F3 (P2; lines 304-348):** the production scene now makes A speed zero, uses extracted actor-relative
  selector keys, and pins both base candidates plus the exact empty post-seal menu; the verified
  nine-row table is explicitly ranking/filtering-only.
- **PLAN-F2 (P2; lines 50-73):** both absence commands now cover every 000-999 plain and underscore seed plus
  `xxx`; MAIN provenance, verbatim results, reservation classification, and replacement rules stay.
- **PLAN-F5 (P2; lines 217-246):** v2 now requires all ten historical/current namespaces, exact own-cohort
  exceptions, removal-of-any-row tests, and the 6204 mutant.
- **PLAN-F6 (P2; lines 115-138):** Batch 2 now preserves positive movement OR productive slots, keeps Dash-only
  monsters valid, and applies resolved-origin trace checks only to targeted offensive components.

COHORT-01 PLAN R3 DONE

## Binding amendment N1 (astra r3, D650 — supervisor-appended; part of the frozen plan)

Batch 9 must construct the hand-authored Scout A with `profile.rules.speed: feet(0)` before encounter creation, retain the typed zero-speed effect, and assert both declared and effective speed are zero (attack declarations take their movement allowance from `subject.profile.rules.speed`, offer-declarations.ts:431–465, so the effect alone still lets A move 25 ft to (8,3) and keep its Longbow). Preserve the exact base and mutated labels, selector keys, scores and menus at the lines the plan states (base: Longbow ×2 + Dodge, adjacency and visibility keys, scores 60/60; with (8,4) added: Dodge/Dodge, menu []). Require two mutants: restoring A's declared speed to 30 while retaining the effect, and separately disabling corner sealing, must each fail the mutation expectations. Re-verify against the ACCEPTED WALL-01 before implementation acceptance. This correction is confined to the allowed speculative-planning test file; no production movement change is authorized.

codex
The plan matches exactly: SHA-256 `2b931e…e802`, 399 lines, and the worktree is clean. Batch 1 is feasible only if the accepted WALL-01 predicate and the three allowed files match the cited semantics, so I’m checking those plus the binding D643/D648–D650 decisions and recording the pre-change freeze/inventory now.
exec
/bin/bash -lc 'git branch --show-current
git rev-parse HEAD
git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2
rg -n "generatedLineIsOpen|D643|D648|D649|D650" src/vtt/room-generator.ts /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
find tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2 -type f -print0 | sort -z | xargs -0 sha256sum
sha256sum tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
claude/cohort-01
6887755fe0f204e4bf6d9a37147fa1476bdbada4
src/vtt/room-generator.ts:1227:export function generatedLineIsOpen(
src/vtt/room-generator.ts:1235:    generatedLineIsOpen(traceCombatantLine(state, source.profile.id, target.profile.id)))) return true;
src/vtt/room-generator.ts:1243:        if (!occupied.has(cellKey(target)) && generatedLineIsOpen(
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md:22969:Sol (impl-land-01-sol.md): engine-query-port.ts:1167 refuses attack/save offers whose trace blocksSight with the new exact code target_outside_line_of_sight, tracing from the proposed origin (execution's sourceAnchor semantics); the targetability audit found no other authorization gate (query-port :1234/:1547 are tactical-scoring cover inputs and :1830 reports physical cover — left alone, and changing them broke valid intel expectations); room-generator.ts:1227 generatedLineIsOpen = tier 'none' && !blocksSight for both opposing-pair and creature-to-cell predicates; the brutal-b helper materializes the first declared legal activation choice (keeps the engine-ranked option). Witnesses: the two-blocker seam fixture (rays (14,6)/(15,6)→(1,6)/(2,6) along row line 6 between (8,5)/(8,6): closed → half/blocksSight true, no attack offered, LOS code; one flank removed → offered); the AGREEMENT PROPERTY over hard 5117001-013, brutal 6203001-010, brutal-b 6206001-010: exactly 214 offered attack/save uses, every one executes through the reducer without the LOS/Total-Cover error; mutant OFFER_IGNORES_BLOCKS_SIGHT → both red (the cardinality would be 451); OPEN_RAY_IGNORES_BLOCKS_SIGHT → red. GENERATOR BYTES: in-memory regeneration of all 42 checked seeds is byte-equal before/after the fix — so the fixed generator does not cure the frozen families; restoring productivity needs a NEW cohort (reselected seeds), which sharpens question 5's option A. Suites: engine-query-port 19/19; los-cover 112/112; brutal-b 37 passed / 10 failed — the ten productivity pins only (the 19 c1 rows and the four helper rows now execute; "rejects rather than skips" is GREEN, contrary to my brief's prediction — the attended pending decision still forms); d569-blind-experiment 46 / 6 retained (frozen-family, owner's decision); OFFERS 11 files 329/329; dm-tactical-intel + challenge-room 23/23; tsc 0/0; OFFERS checks 0/0; pair 38.6 s; 5 files 260+/9−. ONE CONSEQUENCE outside the manifest: tests/unit/vtt/tactical-evaluator-r02.test.ts:262 pins a hand-authored expected-value probability 0.380769180625 that scored three attacks from origins the reducer would have REFUSED (priest m1, scout m3, bandit m4 at {half, blocksSight true}); with the corrected query the offers come from executable origins (priest 30-ft move to (13,4) none/false; scout 5 ft to (18,4) half/false; bandit 5 ft to (17,4) half/false) and the evaluator scores from each option's finalPosition, giving 0.4323649600000002. Legitimate: the old number rated offers that could not execute. RULING: manifest +1 — tests/unit/vtt/tactical-evaluator-r02.test.ts, that ONE expectation, re-derived BY HAND in its comment (each monster's new origin, line tier/sight, hit chance and damage terms, the sum; the old value kept as pre-LAND-01), never copied from the run; r2 on the resumed session; then my verification, astra medium, and LAND-02.
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md:23060:## D643 — OWNER (2026-09-17 19:36): "Do your proposal" — the supervisor's recommendations are authorized as a whole
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md:23064:Rank 6 limited to the visibility-field caches (the VIS-FIELD-PERF batch authorized in D643: explicit visibility epoch + from-scratch differential witness per revision); rank 9 (vitest worker topology / duration-balanced shards / experimental fsModuleCache — gate config only) scheduled as a small measured batch after VIS-FIELD lands, savings measured back-to-back on a quiet box per D632's invariants; rank 6 engine-wide and rank 8 stay deferred until the visibility epoch has proven the pattern. Sequence after landing: TEST-PERF-01 (ranks 1–5, 7) → rank 9 → VIS-FIELD-PERF.
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md:23071:PLAN-F1 (P2): v6 has no experiment-identity boundary — session keys {family, arm, seed, rep} (d569-blind-experiment.ts:705) and row matching (:762) omit the manifest identity, and the unchanged hard fixtures keep their hashes, so 240/330/240 cells are indistinguishable across v5/v6. PLAN-F2 (P2): the namespace audit is unsound — stale worktree decisions file, patterns miss `6209xxx` and `6_209_001` forms, and D643/D645's reservations must be classified as such. PLAN-F3 (P2): the speculative-planning oracle must be a predeclared hand-calculated scene (≥9 eligible candidates, tie order, the excluded ninth, the exact mutation result). PLAN-F4 (P3): Batch 6 has eight files. Astra independently confirmed the rest: 20/20 fog-key single-key diffs via the real generator; the scout's 12+4 blocked rays per PC (wizard 16) and labels exactly Dodge/End Turn; Room-8 monsters 1/2/4 Dodge, monster 5's 55-ft Dash (10 steps × 5 + 5 difficult); the cap guard rejects a 57,580-byte block and accepts it with the guard disabled; 52 paths (18 existing + 34 new), all existing ones present. All four findings accepted. r2 (planner resumed 01a0b1bb…; brief plan-cohort-01-r2.md; r1 archived as .tmp-plans/2026-09-17-cohort-01-plan.r1.md) must also require canonicalD569SecondFamilyRegeneration in the active v6 test and rejection witnesses for non-fog-key differences and for legacy keys in new fixtures.
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md:23077:## D648 — SUPERVISOR (2026-09-17 20:38): FINDING AGAINST MY OWN REVIEW — WALL-F1 (D645) RETRACTED; a real D642 consequence found in scene-snapshot; COHORT-01 plan r2 REJECTED by astra r2 (0 P1, 4 P2) → r3 (final round) dispatched
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md:23083:## D649 — OWNER (2026-09-17 20:56): "Purge now with filter-repo, force-push the mirror, recreate the four worktrees from the rewritten main" — DONE in place (purge.log)
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md:23086:## D650 — SUPERVISOR (2026-09-17 21:08): COHORT-01 plan ACCEPTED WITH AMENDMENT by astra r3 (HIGH, final round) — FROZEN at sha256 2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802 (399 lines incl. binding note N1); Batch 1 dispatched on a new worktree; WALL-01 fix r2 re-dispatched after a sol capacity outage
6847b5d44b2577aa31a87a2eb83220ca17f54f8a8b160741d61d47019c75e51c  tests/fixtures/arena-basis-brutal-2/seed-6207001.json
9b1b9be44245659fb1a95922c685bbb1f9082b3f0fa591bd822553573a9d5c66  tests/fixtures/arena-basis-brutal-2/seed-6207002.json
afe31b4cb79f606bd56f29d37267814319de161db745747dae1a70a528617d6f  tests/fixtures/arena-basis-brutal-2/seed-6207003.json
96701e3136318b9f4dd8859311e30bb85586d129c8ef4424fcea398f3bbc813e  tests/fixtures/arena-basis-brutal-2/seed-6207004.json
a3a6774081b01b743091115b29ccfbb81d109e2f26867ce74a1c431b9a56a63a  tests/fixtures/arena-basis-brutal-2/seed-6207005.json
28800721eac25312561d04c49389f95a05ae24e81ff13b1ed6191951a7b34082  tests/fixtures/arena-basis-brutal-2/seed-6207006.json
eb9a566eab8c314fd754d85402e280e5f75ee30e114451b9405007a536375f8f  tests/fixtures/arena-basis-brutal-2/seed-6207007.json
5926d642f77ca2cdbf8cbed29408cfbf4165d6a05b453c2203a0aace93827a2b  tests/fixtures/arena-basis-brutal-2/seed-6207008.json
d398b3ad8a24ecae7d3989a1d47be1bedf69b1b989b17d967278be2b2ca7a50c  tests/fixtures/arena-basis-brutal-2/seed-6207009.json
b7efa3c2df793fcbbdba1d453358577a1d4eb7c6b2fe8c9b575d7fe85c3adf04  tests/fixtures/arena-basis-brutal-2/seed-6207010.json
7cc25c0ab9a0e380373a306874290d0f5247e74c0efb71e43ab593b58f0d9d3b  tests/fixtures/arena-basis-brutal-b/seed-6206001.json
3fdfe05812aeb6724789b4ff6f753ce68c523c13ff024d9ee275bd1060adbcaa  tests/fixtures/arena-basis-brutal-b/seed-6206002.json
36828c26a62a8af8c9cc32db7f3a13124d897a8aacf8ce4d4df2c11d2c742d3c  tests/fixtures/arena-basis-brutal-b/seed-6206003.json
35a5980497b2184a4d684e1849117bb70e4887cb40264ea33117e3e14322c60a  tests/fixtures/arena-basis-brutal-b/seed-6206004.json
dc48b6cdd7d4e33dc3c9a0188586ebd1537deac4dec4284c7e9404cec652e2f1  tests/fixtures/arena-basis-brutal-b/seed-6206005.json
d66fc0a9f29d84b5f1bc5a46b86483f91236d05b5cb1b951519cce7db98e2269  tests/fixtures/arena-basis-brutal-b/seed-6206006.json
1c4c337148afad9aff2a86143bb3156cc446ffabd2ccc8cc29eb004d83034bd9  tests/fixtures/arena-basis-brutal-b/seed-6206007.json
11a065b490f6e68effaddb0e919b9f9f9f7ae300f19a97f3359d3d4c86658a32  tests/fixtures/arena-basis-brutal-b/seed-6206008.json
19808aca9fb3ca7e4a77b6222cfae31c4f69a16b9b74ecc0706cb1eee58d4f5e  tests/fixtures/arena-basis-brutal-b/seed-6206009.json
a9efe78460c66972d09c6efd1c7cc04409ba6fff5a0028657d2442a0ec2fbc83  tests/fixtures/arena-basis-brutal-b/seed-6206010.json
3f737f1ddf714b0381abdc0e822b3a07cda4c55287a4bc0b1a97cd4d7d71d63b  tests/fixtures/arena-basis-brutal/seed-6203001.json
8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860  tests/fixtures/arena-basis-brutal/seed-6203002.json
d54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f  tests/fixtures/arena-basis-brutal/seed-6203003.json
6f9465eccac77eb60ffab2be1832f8f1bcd3290c655130c2ac752a38e6d75a20  tests/fixtures/arena-basis-brutal/seed-6203004.json
5b3be6abbdf2fcccff3243639d582bea2fb1098099fa91cf2208a0f143a2364e  tests/fixtures/arena-basis-brutal/seed-6203005.json
3e6818fe70e276e9ee7407d76b01e3afab7254cc1c27e649d5f445a5f6a6480c  tests/fixtures/arena-basis-brutal/seed-6203006.json
e6215e5b6388a79bc850e7ff97867f2618733acbb434526a77133e382cba2d1b  tests/fixtures/arena-basis-brutal/seed-6203007.json
b067b1aa081f368859979415c468d997b6fa504cad6ad70dc6998c90eeb3207c  tests/fixtures/arena-basis-brutal/seed-6203008.json
9780276cdec0ee71c99cc13217d73376990866f5ab3ab471c7a793196665f7ce  tests/fixtures/arena-basis-brutal/seed-6203009.json
2ec7f3c1298e7b6f3519cf765bcb88f36be3426e02cf7572730a77af9e32114d  tests/fixtures/arena-basis-brutal/seed-6203010.json
83daa7ea3ca89b270e5149368d09fa97074124dfe438cf95b346e996996026c8  tests/fixtures/d569-second-family-manifest.json
8c0bcb3c0afa8358c26129efbef82d0d2882135389d8dd8040fe7e4b6451e13a  tests/fixtures/d569-blind-experiment-manifest.json

exec
/bin/bash -lc "sed -n '1220,1248p' src/vtt/room-generator.ts
sed -n '23040,23105p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
sed -n '1,280p' tools/d569-second-family-manifest.ts
sed -n '281,620p' tools/d569-second-family-manifest.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
): boolean {
  return livingOpposingPairs(state).some(({ source, target }) => {
    const trace = traceCombatantLine(state, source.profile.id, target.profile.id);
    return trace.tier === tier && trace.sourceIds.some((id) => authoredSourceIds.has(id));
  });
}

export function generatedLineIsOpen(
  trace: Pick<TerrainLineTrace, 'tier' | 'blocksSight'>,
): boolean {
  return trace.tier === 'none' && !trace.blocksSight;
}

function hasOpenOpposingRay(state: EncounterState): boolean {
  if (livingOpposingPairs(state).some(({ source, target }) =>
    generatedLineIsOpen(traceCombatantLine(state, source.profile.id, target.profile.id)))) return true;
  const occupied = occupiedCombatantCells(state);
  return state.combatants.some((source) => {
    if (source.life !== 'living' || !state.tokens.some((token) =>
      token.combatantId === source.profile.id)) return false;
    for (let row = 0; row < state.bounds.rows; row += 1) {
      for (let column = 0; column < state.bounds.columns; column += 1) {
        const target = { column, row };
        if (!occupied.has(cellKey(target)) && generatedLineIsOpen(
          traceCombatantLineToCells(state, source.profile.id, [target]),
        )) return true;
      }
    }
    return false;
## D635.53 — SUPERVISOR (2026-09-17 10:21): VIS-FIELD slowdown profiled (sol, read-only; saved .claude/consensus/test-perf/investigate-vis-field-slowdown-sol.md); three bounded fixes queued for TEST-PERF-01 / a post-landing VIS-FIELD perf batch
MEASURED (sol, both trees' production hashes identical before/after; my own numbers agree: vane 3077 → 6585 ms): vane-tpk 3180 → 6820 ms (2.14×). NOT more detection queries (0.83×) but each sight query expanding into per-target-cell traces (visibility-field.ts:431): traceSpaces 95k → 348k (3.67×), traceCornerLine 124k → 536k (4.33×); 91.7% of cell traces are pairwise sightToCreature detection (~4.8 s inclusive), only 8.3% the derived field itself (~0.55 s). The field's weak caches (visibility-field.ts:104) are keyed by EncounterState identity, so immutable reducer revisions make nearly every request cold (206 projections over 205 distinct revisions; 140 traces each). los-cover productive-option 5472 → 6139 ms (+12%): the seam scan (firstSharedEdgeBlockingCell 4.17M calls, 380 ms) plus ~0.7 s more in traceCornerLine; also 33,953 of 46,650 reach traces (72.8%) were for candidates then refused by RANGE — geometry evaluated before the cheap range check. D630 pair 33.6 → 41.5 s: replay.ts:238 projects DM + every PC per replayed revision (derived fog 3,361 calls); the replay file rebuilds the scripted gate 15 times (13 default) at ~1.9 s each.
FIXES (bounded, with required witnesses): (1) TEST-ONLY: build each parameterized replay gate once, deep-freeze, structuredClone per mutating test (~20–23 s off the replay file; witness: gate hash unchanged across tests, shuffled order twice, corruption tests still fail on their own clones) — this is TEST-PERF-01's default-gate reuse item, confirmed as the largest test-only win; (2) PRODUCTION: cache visibility outcomes by an explicit visibility epoch (bumped by movement, footprints, walls/objects, light, obscurement, relevant conditions/effects, hidden state, senses, life, placement, observer eligibility; HP/log-only revisions reuse) + observer/subject key instead of state identity (~2.5–3.5 s in vane; witness: per revision, cached == from-scratch for every eligible pair and cell, byte-identical fog/reason/sense/grade and projection hashes, incl. Large/Huge footprints, seam rays, darkness, obscurement, Invisible/Hidden/Blinded, death/placement); (3) TEST-ONLY: generate the nine versioned rooms once per suite, deep-frozen (~4.5 s per named los-cover test) + optional seam memoization by blocking-topology signature (~0.35–0.7 s); (4) small: prefilter range before LOS inside offer generation only (public reach precedence unchanged; ~0.2–0.3 s).
CORRECTNESS: no field computed for ineligible observers (eligibleVisibilityObserverIds :525 excludes dead/unplaced/adjudication-pending/unconscious); per-cell tracing for large footprints is correctness-sensitive (a single representative ray would regress); the seam scan is required work. State-identity caching = design smell, not a defect. DISPOSITION: (1)+(3) go into TEST-PERF-01 (owner's ranks 6/8/9 still pending); (2)+(4) = a VIS-FIELD-PERF batch after landing, planned and reviewed like any batch; the 5 s budgets are NOT re-pinned meanwhile.

## D635.54 — SUPERVISOR (2026-09-17 11:06): option A costed (sol, read-only; saved investigate-option-a-costing-sol.md); a refinement of the attribution — 40 of the second-family ledger's 47 violations are B2's fog key, not the seam rule
REFINEMENT (sol; consistent with the failure text I saw in D635.46): the ledger pin's 47 markers = 20 second-family fixtures (hard-2 5118xxx AND brutal-2 6207xxx) × 2 byte-regeneration checks (first_regeneration / independent_regeneration — the validator rebuilds each fixture with generateRoom twice and requires byte equality with the committed bytes, which still carry the removed foggedCells key) = 40, plus 7 brutal-2 productivity violations. So the second-family manifest is red for TWO reasons: B2's key (40, the same consequence LAND-04 normalized for the generator pins) and the seam rule (7). Neither option B nor C repairs the 40. d569-v5.test.ts fails at file level because it validates the manifest at module load.
MACHINERY: "regeneration" in D569 means a second deterministic build, not a cohort version; the sanctioned precedent for a NEW cohort is the second family itself (D569: predeclare unused ranges with a verbatim namespace audit, generate into a new directory, prove membership from decoded state, generate twice and compare bytes, pin hashes only then, nest the manifest hash) — history is never edited in place. No tracked RL row and no docs bind the brutal families.
OPTION A as costed by sol (40 fixtures: brutal-v2 6209xxx, brutal-b-v2 6210xxx, brutal-2-v2 6211xxx, PLUS hard-3 5120xxx because hard-2's bytes fail the fog-key regeneration; new d569-second-family-manifest-v2 + d569-blind-experiment-manifest-v6 with v5/v1 kept frozen; 42 new SHA-256 pins; ~62–66 paths; ~865 KB fixtures; versioned arena/rerun routes brutal-v2 / brutal-10-v2 rather than retargeting historical names; negative witnesses required; d569-v5.test.ts stays historical, a v6 test becomes active). Hard-family pins: composite-turn-proposals and Room-8 opportunity-intel are hand-derivable re-pins on the same scenes (Dodge/End Turn only; monsters 1/2/4 zero-foot Dodge, monster 5 keeps its 55-ft Dash); speculative-planning must NOT be normalized to an empty menu — it needs a small hand-authored scene with an independently computed volatility score and a seam mutation.
MY REFINEMENT TO A (A1, recommended over sol's A2): treat the 40 fog-key regeneration violations exactly as LAND-04 treated the generator pins — the validator's byte-regeneration comparison drops the obsolete legacy key from the committed bytes before comparing (a version-aware projection, demonstrated per fixture as a single-key structural diff), so hard-2 (5118xxx) stays the historical hard half and only the three BRUTAL cohorts regenerate: 30 fixtures instead of 40, no hard-3 namespace, second-family v2 = hard-2 (as is) + brutal-2-v2. Risk noted by sol applies: the normalization must be a demonstrated single-key projection, never a rewrite of manifest bytes.
B (6–10 files, relaxes everyMonsterProductive; leaves the 40 and the speculative scene broken; brutal-b has 41/42 monsters without attack offers under the seam so any threshold would be fitted to the failures) and C (reverses D635's optical ruling; restores the three behaviour pins and 214→451 offers; 10–20 files + full revalidation of cover/sight/fields/offers/browser; still leaves the 40) — both advised against.

## D641 — OWNER (2026-09-17 18:48): "I think we need to model that a wall is solid across cells. It is not just a series of cells. Look for how others have handled this." — research done by the supervisor; question put back to the owner
PROBED BY ME on 807d2e3c (traceTerrainLine, 6x6 board): (1) two blockers touching only at a corner, (2,1)&(1,2), observer (0,0) → target (3,3) through the shared vertex: tier half, blocksSight FALSE — the diagonal crack lets sight through; (2) solid 2x2 block: total, TRUE; (3) wall column with the ray along its face-line between two blockers (the D635 seam): half, TRUE; (4) a ray grazing one blocker's corner: none, FALSE. So today's tracer = closed cells whose interiors block + the D635 shared-EDGE rule; shared CORNERS of diagonally touching blockers are still open.
HOW OTHERS DO IT (sources in the reply): D&D 2014 DMG corner trace — lines from a corner to the target's corners; whether a line that merely TOUCHES an obstacle counts is explicitly the DM's call (the SRD 5.2.1 only defines cover degrees by "covers at least half / three-quarters / the whole target"); Gloomhaven — corner-to-corner lines, and "the line cannot touch or cross a wall at any point; touching the very edge of a wall still means no LOS" (walls are closed sets; touching = blocked); Foundry VTT / Roll20 — walls are SEGMENTS with endpoints (thin walls), not cells, so no cell seams exist and a long wall is one solid segment by construction; roguelikes (Red Blob "thin wall FOV", Adam Milazzo's survey, Albert Ford's symmetric shadowcasting, RogueBasin comparative study) — "thick walls" (blocking cells) vs "thin walls" (blocking edges) is the standard framing, and diagonal-gap peeking between corner-touching blockers is a known nuisance that implementations bevel or seal; Debenham & Solis-Oba, "New Algorithms for Computing Field of Vision over 2D Grids" (arXiv 2101.11002) — "adjacent vision blocking cells are first grouped together into rectilinear regions, then split into a minimal number of rectangles", and their Figure 5 is exactly our seam: two adjacent rectangles let a ray slip along the shared side, which they fix by extending one rectangle to overlap the other so the union is solid.
PROPOSAL (to the owner, one question): adopt a SOLID-WALL model — the wall is the union of sight-blocking cells as closed rectilinear regions; a ray is blocked iff it passes through the union's interior, which includes (a) any blocking cell's interior, (b) an edge shared by two blockers (D635, already in), and (c) a vertex shared by two diagonally touching blockers (seal the crack; NOT in today). Grazing an OUTER face or a single blocker's corner stays open (DMG-consistent, today's behaviour). Implementation: step 1 a local vertex rule beside firstSharedEdgeBlockingCell in cover.ts with witnesses for all four probe cases (small, rules-visible, needs its own D-ruling and astra review; lands with VIS-FIELD); step 2 the precomputed rectilinear-region/rectangle representation keyed by the blocking topology (the paper's approach) as the VIS-FIELD-PERF batch (also perf fix #3 in D635.53). Question 5 stays: regenerate the cohorts under the solid-wall geometry (A1).

## D642 — OWNER (2026-09-17 19:15): "Seal diagonal corners." — the solid-wall model adopted in full: a ray is blocked by any blocking cell interior, any edge shared by two blockers (D635) and any vertex shared by two diagonally touching blockers; outer-face grazes and single-blocker corner touches stay clear; physical cover tiers unchanged
Owner also confirmed on the annotated image: "The orange and purple lines should not work" (all four corner rays through the column-7 wall are blocked — the seam rule's result). WALL-01 dispatched to sol (resume the seam-rule session 01a0ab3f…, brief impl-vis-field-wall-01.md, log impl-wall-01.log) on the worktree at 807d2e3c: the vertex rule in cover.ts with exact lattice-point enumeration, hand-authored witnesses (crack sealed, single-blocker control, staircase wall sealed at a step vertex — the case the edge rule alone misses, outer-face graze clear, 2x2 solid unchanged), the SEAM batch's DIAGONAL_CORNER_TOUCH_UNCHANGED pin re-pinned by hand to the sealed result (renamed _SEALED_D642), three mutants, per-family consequence counts before/after, the red-set audit (a red in a non-frozen suite is a finding, not a re-pin), and proof that generation bytes are untouched. Astra medium review follows; then question 5 (A1) regenerates the cohorts under the sealed geometry.

## D643 — OWNER (2026-09-17 19:36): "Do your proposal" — the supervisor's recommendations are authorized as a whole
Authorized: (1) WALL-01 (D642, in flight) lands the solid-wall vertex rule with VIS-FIELD; (2) QUESTION 5 = A1: normalize the removed fog key in the second-family validator's regeneration comparison (demonstrated per fixture as a single-key diff, as LAND-04 did), then regenerate ONLY the three brutal cohorts as new versioned families under the sealed geometry (brutal-v2 6209xxx, brutal-b-v2 6210xxx, brutal-2-v2 6211xxx; hard-2 5118xxx kept as the historical hard half), D569 precedent in full (verbatim namespace audit, membership from decoded state, two byte-identical generations, hashes pinned only then), manifests d569-second-family-manifest-v2 + d569-blind-experiment-manifest-v6 with v1/v5 frozen, versioned arena/rerun routes (brutal-v2, brutal-10-v2, brutal-10-b-v2), hand re-pins for composite-turn-proposals and Room-8 opportunity-intel, a small hand-authored scene for speculative-planning (never an empty-menu normalization); (3) the blind semantic cap stays 32,768 and the oversized-block rejection witness (astra's LAND4-F1, P3) is added; (4) after landing: the VIS-FIELD-PERF batch — visibility-epoch cache with the from-scratch differential witness, range-before-LOS prefilter inside offer generation, and the precomputed rectilinear-region/rectangle wall representation (Debenham–Solis-Oba) — planned and reviewed like any unit, plus the two test-only items into TEST-PERF-01. ORDER: WALL-01 (verify, astra) → A1 as VIS-FIELD-01 batch COHORT-01 (planned as a brief with the file-level costing from investigate-option-a-costing-sol.md as its binding spec; astra reviews) → untruncated full gate → land → mirror push (D640) → ELEVATION-02 B0 and the perf batch.

## D644 — OWNER (2026-09-17 19:45): TEST-PERF ranks 6/8/9 = option A
Rank 6 limited to the visibility-field caches (the VIS-FIELD-PERF batch authorized in D643: explicit visibility epoch + from-scratch differential witness per revision); rank 9 (vitest worker topology / duration-balanced shards / experimental fsModuleCache — gate config only) scheduled as a small measured batch after VIS-FIELD lands, savings measured back-to-back on a quiet box per D632's invariants; rank 6 engine-wide and rank 8 stay deferred until the visibility epoch has proven the pattern. Sequence after landing: TEST-PERF-01 (ranks 1–5, 7) → rank 9 → VIS-FIELD-PERF.

## D645 — SUPERVISOR (2026-09-17 19:53): WALL-01 REJECTED by my verification (WALL-F1, P1: the vertex rule is inert for axis-aligned rays whose sealing pair straddles the line); fix r1 dispatched; COHORT-01 plan (400 lines, sha 6852051d…) harvested and sent to astra HIGH
SOL WALL-01 (saved impl-wall-01-sol.md): gcd lattice enumeration, TL+BR / TR+BL pairs, progress-ordered merge of interior/edge/vertex events, physical tier untouched; 43/43; three mutants killed; counts table "unchanged"; 42 seeds byte-identical; pair 38.6 s; two los-cover budget timeouts. VERIFIED BY ME (clean checkout + patch, traceTerrainLine, Medium tokens, 15x8): blockers (7,3)&(8,4) with the ray cells (13,4)→(1,4): blocksSight FALSE (must be true); (7,4)&(8,3): FALSE; vertical (4,2)&(5,3) with (5,0)→(5,6): FALSE; the diagonal crack cases TRUE. CAUSE (read in cover.ts): sharedVertexBlockingCells is built from `candidates`, which traceSpaces filters by the bounding box of the eight corners; for a ray along a grid line between Medium cells the far-side flank row is outside the box, so the vertex is never registered. Sol's STAIRCASE_WALL_SEALED witness passes only because its geometry keeps both cells inside the box — it does not exercise the case it is named for, and the "counts unchanged" table is not evidence about D642. FIX R1 (sol resumed; brief impl-vis-field-wall-01-fix-r1.md; log impl-wall-01-fix-r1.log): on-the-fly sightBlockingAt lookups around each interior lattice vertex (as the edge rule does), witnesses replaced with my three geometries + a split-pair control, a bbox-regression mutant, counts and generation bytes redone.
COHORT-01 PLAN (sol 01a0b1bb…, saved plan-cohort-01-sol.md; copied to .tmp-plans/2026-09-17-cohort-01-plan.md): 10 batches (validator normalization → RED membership witnesses + frozen-contract retirement → generate 6209 → 6210 → 6211 → manifests v2/v6 + dated ledger → routes → audit/report coverage → three behaviour pins → cap witness), a byte-freeze inventory of v1/v5/6203/6206/6207 checked after every batch, "reject all ten seeds if one fails", no open questions. Astra HIGH (fresh; brief review-plan-cohort-01-astra.md; log review-plan-cohort-01-astra-r1.log) reviewing now, in parallel with the WALL-01 fix.

## D646 — SUPERVISOR (2026-09-17 20:07): COHORT-01 plan r1 REJECTED by astra r1 (HIGH; 0 P1, 3 P2, 1 P3; saved review-plan-cohort-01-astra-r1.md); r2 dispatched to the planner
PLAN-F1 (P2): v6 has no experiment-identity boundary — session keys {family, arm, seed, rep} (d569-blind-experiment.ts:705) and row matching (:762) omit the manifest identity, and the unchanged hard fixtures keep their hashes, so 240/330/240 cells are indistinguishable across v5/v6. PLAN-F2 (P2): the namespace audit is unsound — stale worktree decisions file, patterns miss `6209xxx` and `6_209_001` forms, and D643/D645's reservations must be classified as such. PLAN-F3 (P2): the speculative-planning oracle must be a predeclared hand-calculated scene (≥9 eligible candidates, tie order, the excluded ninth, the exact mutation result). PLAN-F4 (P3): Batch 6 has eight files. Astra independently confirmed the rest: 20/20 fog-key single-key diffs via the real generator; the scout's 12+4 blocked rays per PC (wizard 16) and labels exactly Dodge/End Turn; Room-8 monsters 1/2/4 Dodge, monster 5's 55-ft Dash (10 steps × 5 + 5 difficult); the cap guard rejects a 57,580-byte block and accepts it with the guard disabled; 52 paths (18 existing + 34 new), all existing ones present. All four findings accepted. r2 (planner resumed 01a0b1bb…; brief plan-cohort-01-r2.md; r1 archived as .tmp-plans/2026-09-17-cohort-01-plan.r1.md) must also require canonicalD569SecondFamilyRegeneration in the active v6 test and rejection witnesses for non-fog-key differences and for legacy keys in new fixtures.

## D647 — SUPERVISOR (2026-09-17 20:22): WALL-01 fix r1 harvested (sol; saved impl-wall-01-fix-r1-sol.md) — my verification running on the throwaway checkout; COHORT-01 plan r2 harvested (sha 4f3385b4…, 373 lines; r1 archived) — astra r2 (HIGH, resumed) running
SOL fix r1: vertex lookup now queries sightBlockingAt directly around each GCD-enumerated interior lattice vertex (no candidate bbox), also on the coverTierBetweenObjects path; the three axis witnesses (my geometries: first cells (7,3) / (8,3) / (4,2)) + split control; 46/46; four mutants incl. the bbox-regression one (exactly the three axis witnesses red); counts table unchanged before/after (hard 142/35, brutal 57/34, brutal-b 15/41, brutal-2 58/36 — now with a working rule); 42 seeds byte-identical (aggregate 7b884bf9…); pair 39.1 s; the 35 frozen/behaviour reds unchanged; four los-cover 5 s budget timeouts under the combined audit (110/112 isolated with two) — perf, not re-pinned. MY VERIFICATION (verify-wall-01-r1.log; ../dnd-wt-seam-ab reset to 807d2e3c + patch): tsc, my seven-case probe (A/B/C/D must block, E split pair / F outer-face graze / G single corner must stay clear), a vertex-lookup-null mutant, the suites, los-cover alone, pair.
PLAN r2 disposition (planner): F1 → separately pinned outer-v6 identity in session keys/cells/rows/matching, v5 preserved, v5 hard rows rejected; F2 → pre-Batch-2 gate against MAIN decisions, plain/xxx/underscore forms, verbatim output, reservations classified, three families reserved together, Allowed-file amendment before replacement generation; F3 → fixed two-proposal integration scene + nine-candidate hand table (radii, paths, scores, byte tie order, excluded ninth, exact seam mutation); F4 → counts fixed; canonical regeneration required for active v6; rejection witnesses for non-fog differences and legacy keys in v2. Astra r2 told to verify the F3 hand table itself.

## D648 — SUPERVISOR (2026-09-17 20:38): FINDING AGAINST MY OWN REVIEW — WALL-F1 (D645) RETRACTED; a real D642 consequence found in scene-snapshot; COHORT-01 plan r2 REJECTED by astra r2 (0 P1, 4 P2) → r3 (final round) dispatched
RETRACTION, at full length. In D645 I rejected WALL-01 as P1 on two grounds, both wrong: (1) I claimed the vertex map was built from the bounding-box-filtered `candidates`; it was built from `candidateEntries` — the UNFILTERED union of indexed sources and blocked cells — so no cell was ever excluded; (2) I read my probe's aggregate blocksSight=false for two isolated diagonal blockers as "the rule is inert", but blocksSight is true only when EVERY corner ray is blocked, and in that geometry the ray along the far face (y=5) is legitimately clear (a graze) — the aggregate false was CORRECT. The decisive test I should have run first: a FULL stepped wall (column 7 rows 0–3, column 8 rows 4–7), Medium tokens (13,4)→(1,4): baseline 807d2e3c leaks (false); ORIGINAL WALL-01 blocks (true, first (7,3)); fix r1 blocks (true). Verified by me tonight on the throwaway checkout, all three trees. So the original WALL-01 was correct; fix r1 replaced a correct precomputed map with equivalent on-the-fly lookups and added per-ray witnesses for my three geometries (valid per-ray, and the split-pair control) — harmless, kept as the candidate, but the "bbox regression" mutant sol reports is a restriction the original never had. Sol did not push back on a false P1; the brief told it what to find. Lesson recorded in memory (aggregate vs per-ray; read the code path before naming a cause).
REAL FINDING (WALL-F2, D642 consequence, not a defect): tests/unit/vtt/scene-snapshot.test.ts "preserves full persisted request/coordinator bytes and the actual revision checksum" is red with the vertex rule (8/8 on clean 807d2e3c): the two-room scene's derived fog gains cells (8,3) and (9,3) — a sealed diagonal crack now conceals two cells the party could previously see through. Sol's suite audit did not include scene-snapshot. Handling = D569 demonstrate-then-normalize exactly as B4/B2 did for this test (D635.5–.7: show the vertex, the ray, the two cells; then re-pin the persisted expectation and the revision checksum/fingerprint with the derivation). An UNTRUNCATED full vitest on the r1 candidate is running (full-vitest-wall-r1.log) to find every other consequence before the fix-r2 brief.
MY VERIFICATION of fix r1 otherwise (verify-wall-01-r1.log): tsc 0/0; diff-check 0; vertex-lookup-null mutant → 5 sealed witnesses red; suites 2150/2186 = the 35 known frozen/behaviour reds + scene-snapshot; los-cover 110/112 (the two 5 s budget timeouts, perf); pair 36/36 in 39 s.
PLAN r2 → astra r2 REJECT (saved review-plan-cohort-01-astra-r2.md): F1, F4 closed; F3 still open (astra ran the real planner: the base menu has two candidates not one, the selector key shape is wrong, and under sealed corners A keeps its shot by moving 25 ft to (8,3) so the mutated menu has three entries, not []); F2 partial (search must cover the whole 000–999 namespaces); F5 NEW (v2's ledger drops the five historical namespaces and the own-cohort handling); F6 NEW (membership silently became mandatory offense; 6203001 monsters 3/4 are Dash-only productive). The nine-candidate table verified correct under its stipulated responses. r3 = the final round (planner resumed; brief plan-cohort-01-r3.md; r2 archived).

## D649 — OWNER (2026-09-17 20:56): "Purge now with filter-repo, force-push the mirror, recreate the four worktrees from the rewritten main" — DONE in place (purge.log)
Inventory before acting: 136 worktrees (not four; 9 dirty, mostly stale lanes), remote `mirror` = github.com/derrickschoen/srd55-private, git-filter-repo 2.47.0, the blob .tmp-lane-4b.log = 68,500,144 bytes introduced at 2ba5adba and untracked at bade7b57; 28 other .tmp-*.log blobs (~115 MB) remain in history — NOT purged (scope = the owner's named file; a second in-place rewrite is cheap). Deviation from the option text, deliberately: no worktree was deleted or recreated — filter-repo rewrote refs in place, branch-attached worktrees follow their branches (identical trees), and the 16 detached worktrees were remapped through .git/filter-repo/commit-map with update-ref --no-deref HEAD; nothing uncommitted anywhere was touched. Preflight: no codex/vitest readers; dry run 4634 commits OK; refs (174) and detached HEADs snapshotted; the WALL-01 r1 candidate committed on claude/vis-field (7acb936d → 6887755f after rewrite) and the records committed on main (0847602b → 3be26bd8) so nothing sat uncommitted. RESULT: main tree 524f110d… identical before and after; refs 174/174; blob occurrences 1 → 0; pack 67.6 MiB (one pack, 56,225 objects); `git push --force mirror main`: 9f148666...3be26bd8 (forced update); ls-remote main = local main. KEY SHA TRANSLATIONS (old → new; the full 4635-line map is committed at .claude/consensus/purge-2026-09-17-commit-map.txt): 9f148666 -> 29a7d2e2; 0847602b -> 3be26bd8; 807d2e3c -> e86a4d26; 9a220fce -> 213f9a7e; 61dd454e -> 8352aed5; ebfcacaa -> 34c564f0; 10cc9f5e -> 30403378; 5492f782 -> e143e899; 9a3235da -> 33aa146c; 78ee31b0 -> bf85fb89; dcca84fd -> a46eea99; 1d58ef72 -> 9407dfa8; 7acb936d -> 6887755f; 4a99570d -> 495d9eb2; 75c2f44a -> df694f93. Every decision written before this entry cites OLD ids; translate through the map.

## D650 — SUPERVISOR (2026-09-17 21:08): COHORT-01 plan ACCEPTED WITH AMENDMENT by astra r3 (HIGH, final round) — FROZEN at sha256 2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802 (399 lines incl. binding note N1); Batch 1 dispatched on a new worktree; WALL-01 fix r2 re-dispatched after a sol capacity outage
ASTRA r3 (saved review-plan-cohort-01-astra-r3.md): F2 closed (all three namespace commands re-run verbatim, statuses 0/1/0, 1,000/1,000 plain and underscore seeds per namespace + the xxx form); F5 closed (ten-namespace union, own-cohort exceptions, remove-any-row tests, the 6204 mutant); F6 closed (positive movement stays productive; Dash-only witness + movement-arm mutant; sight/cover only on targeted offensive components); F1/F4 closed; counts 3/3/10/10/10/8/4/4/3/1 = 52 paths. F3 resolved by AMENDMENT: with the typed zero-speed effect alone the real planner still offers A a 25-ft move to (8,3) + Longbow (attack declarations read subject.profile.rules.speed, offer-declarations.ts:431–465); with the profile's rules.speed also feet(0) the real planner + an in-memory D642 reference gives base = Longbow ×2 + Dodge with adjacency/visibility keys scoring 60/60 and, with (8,4) added, Dodge/Dodge and menu [] — and removing corner sealing restores the Longbow and the two-entry menu. N1 (appended verbatim to the plan, all copies): construct A with rules.speed feet(0), keep the effect, assert declared and effective speed zero, preserve the exact labels/keys/scores/menus, two mutants (speed 30 restored; sealing disabled), re-verify against accepted WALL-01; test-file only. Plan copies: .tmp-plans (main), ../dnd-wt-seam-verify, ../dnd-wt-cohort-01, ../dnd-wt-vis-field.
OUTAGE: the first WALL-01 fix r2 lane died with "Selected model is at capacity" + "thread … not found" before editing (worktree clean); probes SOL-OK / TERRA-OK at 21:10; re-dispatched FRESH on sol (log impl-wall-01-fix-r2b.log). Per the routing rule the next sol failure on this scope goes to Terra xhigh.
COHORT-01 B1 (validator normalization; not gated on WALL-01): new worktree ../dnd-wt-cohort-01 = branch claude/cohort-01 from 6887755f (node_modules symlinked, plan copied); fresh sol; brief .tmp/runs/cohort-01/impl-cohort-01-b1.md; log impl-b1.log; consensus dir .claude/consensus/cohort-01/. Generation batches (3–5) stay gated on WALL-01's acceptance; the cohort branch merges into claude/vis-field once both are accepted.
import { canonicalJson } from '../src/commands/canonical-json';
import { combatantSpace, reduceEncounter } from '../src/combat/encounter';
import { spacesIntersect } from '../src/combat/creature-space';
import { mulberry32 } from '../src/combat/random';
import { BUNDLED_MONSTER_ROSTER, STARTER_MONSTER_ROSTER } from '../src/combat/statblocks/roster';
import { spellDefinition } from '../src/combat/spells/definitions';
import { sha256 } from '../src/crypto/sha256';
import { d466GeneratedRoomOverride } from '../src/vtt/d466-room-overrides';
import { availableEngineActorOptions, resolveEngineActorOption } from '../src/vtt/intent-resolver';
import { decodeArenaFixtureText } from '../src/vtt/mcp/entrypoint';
import { freshMonsterPlanningState } from '../src/vtt/monster-planning-state';
import { buildOfferEnvironment } from '../src/vtt/offers/build-offer-environment';
import {
  BRUTAL_CHALLENGE_BUDGET_SCALE,
  BRUTAL_TERRAIN_FEATURE_COUNT_BAND,
  generateRoom,
  type GeneratedRoom,
  type RoomDifficultyProfile,
} from '../src/vtt/room-generator';
import { z } from 'zod';

const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });

export const D569_SECOND_FAMILY_LEDGER_ENTRY =
  'seed ranges 5118001-5118010 and 6207001-6207010 have no occurrence in .claude/decisions.md, tests/fixtures or tools as of 2026-09-07 (supervisor grep); ranges 5117xxx, 6203xxx, 6204xxx, 6206xxx and 6208xxx are used (primary hard, primary brutal, D466 override/protocol, D572 brutal-b, D572 pool)' as const;

export const D569_SECOND_FAMILY_SEEDS = {
  hard: [
    5_118_001, 5_118_002, 5_118_003, 5_118_004, 5_118_005,
    5_118_006, 5_118_007, 5_118_008, 5_118_009, 5_118_010,
  ],
  brutal: [
    6_207_001, 6_207_002, 6_207_003, 6_207_004, 6_207_005,
    6_207_006, 6_207_007, 6_207_008, 6_207_009, 6_207_010,
  ],
} as const;

export const D569_USED_SEED_NAMESPACES = [
  { label: '5117xxx', start: 5_117_000, end: 5_117_999 },
  { label: '6203xxx', start: 6_203_000, end: 6_203_999 },
  { label: '6204xxx', start: 6_204_000, end: 6_204_999 },
  { label: '6206xxx', start: 6_206_000, end: 6_206_999 },
  { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
] as const;

const manifestSchema = z.strictObject({
  version: z.literal('d569-second-family-manifest-v1'),
  generator: z.strictObject({
    tool: z.literal('tools/generate-arena-basis.ts'),
    canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
    independentRegenerations: z.literal(2),
  }),
  ledgerEntry: z.string(),
  usedSeedNamespaces: z.array(z.strictObject({
    label: z.string(),
    start: z.number().int().nonnegative(),
    end: z.number().int().nonnegative(),
  })),
  cohorts: z.array(z.strictObject({
    difficulty: z.enum(['hard', 'brutal']),
    directory: z.string(),
    fixtures: z.array(z.strictObject({
      seed: z.number().int().nonnegative(),
      path: z.string(),
      sha256: z.string().regex(/^[a-f0-9]{64}$/u),
    })),
  })),
});

export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;

export interface D569SecondFamilyValidationAccess {
  readonly readFixture: (path: string) => string;
  readonly regenerate: (
    seed: number,
    difficulty: D569SecondFamilyDifficulty,
    generation: 1 | 2,
  ) => string;
}

export interface D569SecondFamilyViolation {
  readonly code: string;
  readonly message: string;
}

function addViolation(
  violations: D569SecondFamilyViolation[],
  condition: boolean,
  code: string,
  message: string,
): void {
  if (!condition) violations.push({ code, message });
}

function cellsInBounds(
  cells: readonly { readonly column: number; readonly row: number }[],
  bounds: { readonly columns: number; readonly rows: number },
): boolean {
  return cells.every((cell) =>
    Number.isInteger(cell.column) && Number.isInteger(cell.row) &&
    cell.column >= 0 && cell.column < bounds.columns &&
    cell.row >= 0 && cell.row < bounds.rows);
}

/** Shared state/reducer invariants for the existing generator suite and D569 freeze. */
export function generatedRoomIntegrityViolations(
  room: GeneratedRoom,
  fixtureBytes: string = `${canonicalJson(room)}\n`,
): readonly D569SecondFamilyViolation[] {
  const violations: D569SecondFamilyViolation[] = [];
  const state = room.encounter.state;
  addViolation(
    violations,
    state.bounds.columns === room.spec.dimensions.columns &&
      state.bounds.rows === room.spec.dimensions.rows,
    'dimension_mismatch',
    `seed ${String(room.spec.seed)} spec and encounter bounds differ`,
  );
  addViolation(
    violations,
    cellsInBounds(room.spec.blockedCells, state.bounds),
    'blocked_cell_invalid',
    `seed ${String(room.spec.seed)} has an out-of-bounds blocked cell`,
  );

  const combatantIds = new Set(state.combatants.map((combatant) => String(combatant.profile.id)));
  const tokenIds = state.tokens.map((token) => String(token.id));
  addViolation(
    violations,
    new Set(tokenIds).size === tokenIds.length && state.tokens.length === state.combatants.length,
    'token_set_invalid',
    `seed ${String(room.spec.seed)} has missing or duplicate tokens`,
  );
  addViolation(
    violations,
    state.tokens.every((token) =>
      combatantIds.has(String(token.combatantId)) && cellsInBounds([token.position], state.bounds)),
    'token_position_invalid',
    `seed ${String(room.spec.seed)} has an invalid token position or combatant reference`,
  );
  try {
    const spaces = state.tokens.map((token) => ({
      token,
      space: combatantSpace(state, token.combatantId),
    }));
    const blocked = new Set(state.blockedCells.map((cell) => `${String(cell.column)},${String(cell.row)}`));
    addViolation(
      violations,
      spaces.every(({ space }) => cellsInBounds(space.cells, state.bounds) &&
        space.cells.every((cell) => !blocked.has(`${String(cell.column)},${String(cell.row)}`))),
      'token_footprint_invalid',
      `seed ${String(room.spec.seed)} has an out-of-bounds or blocked creature footprint`,
    );
    addViolation(
      violations,
      spaces.every((left, leftIndex) => spaces.slice(leftIndex + 1).every((right) =>
        !spacesIntersect(left.space, right.space))),
      'token_footprint_overlap',
      `seed ${String(room.spec.seed)} has overlapping creature footprints`,
    );
  } catch (error) {
    violations.push({
      code: 'token_footprint_invalid',
      message: `seed ${String(room.spec.seed)} has an undecodable creature footprint: ${error instanceof Error ? error.message : String(error)}`,
    });
  }

  const environmentRegions = [
    ...state.environment.difficultTerrainRegions,
    ...state.environment.lightRegions,
    ...state.environment.obscurementRegions,
    ...(state.environment.movementRegions ?? []),
    ...(state.environment.narrowOpeningRegions ?? []),
  ];
  const regionIds = environmentRegions.map((region) => region.id);
  addViolation(
    violations,
    new Set(regionIds).size === regionIds.length && environmentRegions.every((region) =>
      region.id.trim().length > 0 && region.cells.length > 0 && cellsInBounds(region.cells, state.bounds)),
    'environment_region_invalid',
    `seed ${String(room.spec.seed)} has an empty, duplicate, or out-of-bounds environment region`,
  );

  try {
    const loaded = decodeArenaFixtureText(fixtureBytes);
    const reduced = reduceEncounter(loaded, { type: 'roll_initiative' }, mulberry32(901));
    addViolation(
      violations,
      reduced.state.revision === loaded.revision + 1 &&
        reduced.state.initiative.length === loaded.combatants.length,
      'reducer_load_invalid',
      `seed ${String(room.spec.seed)} did not initialize through the encounter reducer`,
    );
  } catch (error) {
    violations.push({
      code: 'reducer_load_invalid',
      message: `seed ${String(room.spec.seed)} failed reducer loading: ${error instanceof Error ? error.message : String(error)}`,
    });
  }
  return violations;
}

function executableControlCaster(room: GeneratedRoom): boolean {
  return room.spec.monsterRoster.some((entry) => {
    const row = STARTER_MONSTER_ROSTER.find((candidate) => candidate.id === entry.statblockId);
    const actions = row?.statblock.sourceDetails.actions;
    return actions?.kind === 'present' && actions.value.some((action) =>
      action.kind === 'spellcasting' && action.execution?.kind !== 'absent' &&
      action.spells.some((spell) => {
        const definition = spell.manifestStatus === 'implemented' ? spellDefinition(spell.id) : undefined;
        return definition?.operation.kind === 'persistent_area' &&
          definition.operation.hooks.some((hook) => hook.effect.kind === 'save_gated') &&
          definition.operation.hooks.some((hook) =>
            hook.effect.kind === 'automatic' && hook.effect.payload.kind === 'effect' &&
            hook.effect.payload.payload.kind === 'movement_modifier');
      }));
  });
}

/** State-derived hard membership; hashes and seed names are deliberately not inputs. */
export function hardRoomMembershipViolations(
  room: GeneratedRoom,
): readonly D569SecondFamilyViolation[] {
  const violations: D569SecondFamilyViolation[] = [];
  const roster = room.spec.monsterRoster;
  const rosterIds = new Set(roster.map((entry) => entry.statblockId));
  const budgetGap = room.spec.challengeBudgetEighths - room.spec.challengeSpentEighths;
  addViolation(violations, room.spec.difficultyProfile === 'hard', 'hard_difficulty', 'room is not tagged hard');
  addViolation(
    violations,
    roster.length >= 4 && roster.length <= 7 && budgetGap >= 0 && budgetGap <= 8,
    'hard_roster_budget',
    'hard roster must contain 4-7 monsters and spend within its generated budget band',
  );
  addViolation(
    violations,
    executableControlCaster(room) && rosterIds.has('statblock:guard') && rosterIds.has('statblock:scout'),
    'hard_required_roles',
    'hard roster must contain an executable control caster plus Guard and Scout',
  );

  const difficultRegions = room.spec.terrain.filter((feature) =>
    feature.kind === 'difficult-terrain-patch');
  const hardFeatures = room.spec.hardFeatures;
  const [gate] = hardFeatures?.chokepointCells ?? [];
  const barrier = gate === undefined ? [] : room.spec.blockedCells.filter((cell) => cell.column === gate.column);
  addViolation(
    violations,
    hardFeatures?.shape === 'single-gate' && hardFeatures.chokepointCells.length === 1 &&
      gate !== undefined && barrier.length === room.spec.dimensions.rows - 1 &&
      !barrier.some((cell) => cell.row === gate.row) && difficultRegions.length >= 1 &&
      hardFeatures.likelyApproachTerrainRegionIds.length > 0 &&
      hardFeatures.likelyApproachTerrainRegionIds.every((id) =>
        difficultRegions.some((region) => region.id === id && region.cells.length > 0)),
    'hard_chokepoint_structure',
    'hard room must retain its single-gate barrier, chokepoint, and approach difficult terrain',
  );
  return violations;
}

function executableCaster(room: GeneratedRoom): boolean {
  return room.spec.monsterRoster.some((entry) => {
    const row = BUNDLED_MONSTER_ROSTER.find((candidate) => candidate.id === entry.statblockId);
    const actions = row?.statblock.sourceDetails.actions;
    return actions?.kind === 'present' && actions.value.some((action) =>
      action.kind === 'spellcasting' && action.execution?.kind !== 'absent' &&
      action.spells.some((spell) => spell.manifestStatus === 'implemented'));
  });
}

function everyMonsterProductive(room: GeneratedRoom): boolean {
  const planningState = freshMonsterPlanningState(room.encounter.state);
  return planningState.combatants
    .filter((combatant) => combatant.profile.kind === 'monster')
    .every((monster) => availableEngineActorOptions(planningState, monster.profile.id, OFFER_ENVIRONMENT)
      .some((option) => {
        const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
        return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
          resolution.mechanics.actionSlots.some((slot) =>
            slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
            slot.kind === 'use_world_object'));
      }));
}

/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
export function brutalRoomMembershipViolations(
  room: GeneratedRoom,
  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
): readonly D569SecondFamilyViolation[] {
  const violations: D569SecondFamilyViolation[] = [];
  addViolation(violations, room.spec.difficultyProfile === 'brutal', 'brutal_difficulty', 'room is not tagged brutal');
  addViolation(
    violations,
    room.spec.challengeBudgetEighths ===
      sameSeedHard.spec.challengeBudgetEighths * BRUTAL_CHALLENGE_BUDGET_SCALE &&
      room.spec.challengeSpentEighths >= room.spec.challengeBudgetEighths * 0.9 &&
      room.spec.challengeSpentEighths <= room.spec.challengeBudgetEighths,
    'brutal_budget',
    'brutal budget must be scaled from hard and spend 90-100 percent',
  );
  addViolation(
    violations,
    executableCaster(room),
    'brutal_caster',
    'brutal roster must contain an executable implemented caster',
  );

  const terrainCounts = {
    difficult: room.spec.terrain.filter((feature) => feature.kind === 'difficult-terrain-patch').length,
    hazards: room.spec.terrain.filter((feature) => feature.kind === 'hazard-object').length,
    lights: room.spec.terrain.filter((feature) => feature.kind === 'light-source').length,
    obscurement: room.spec.terrain.filter((feature) => feature.kind === 'obscurement-patch').length,
  };
  const terrainTotal = Object.values(terrainCounts).reduce((total, count) => total + count, 0);
  addViolation(
    violations,
    terrainCounts.difficult >= 3 && terrainCounts.hazards >= 3 && terrainCounts.lights >= 2 &&
      terrainCounts.obscurement >= 2 &&
      terrainTotal >= BRUTAL_TERRAIN_FEATURE_COUNT_BAND.minimum &&
      terrainTotal <= BRUTAL_TERRAIN_FEATURE_COUNT_BAND.maximum &&
      room.spec.terrain.length > sameSeedHard.spec.terrain.length,
    'brutal_terrain',
    'brutal terrain must meet every configured minimum/count band and exceed hard density',
  );
  addViolation(
    violations,
    everyMonsterProductive(room),
    'brutal_productivity',
    'every brutal monster must have a productive first-turn offer',
  );
  return violations;
}

function expectedDirectory(difficulty: D569SecondFamilyDifficulty): string {
  return `tests/fixtures/arena-basis-${difficulty}-2`;
}

function sameNamespace(
  actual: readonly { readonly label: string; readonly start: number; readonly end: number }[],
): boolean {
  return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
}

/**
 * Validates the pins only after state membership and two independent canonical
 * regenerations. It is pure/model-free; callers provide repository bytes.
 */
export function validateD569SecondFamilyManifest(
  input: unknown,
  access: D569SecondFamilyValidationAccess,
): readonly D569SecondFamilyViolation[] {
  const decoded = manifestSchema.safeParse(input);
  if (!decoded.success) {
    return [{ code: 'manifest_shape', message: decoded.error.message }];
  }
  const manifest = decoded.data;
  const violations: D569SecondFamilyViolation[] = [];
  addViolation(
    violations,
    manifest.ledgerEntry === D569_SECOND_FAMILY_LEDGER_ENTRY,
    'ledger_evidence',
    'manifest must cite the supervisor ledger entry verbatim',
  );
  addViolation(
    violations,
    sameNamespace(manifest.usedSeedNamespaces),
    'used_seed_ledger',
    'manifest must list every used seed namespace from the supervisor ledger',
  );
  addViolation(
    violations,
    manifest.cohorts.length === 2 && new Set(manifest.cohorts.map((cohort) => cohort.difficulty)).size === 2,
    'cohort_set',
    'manifest must contain exactly one hard and one brutal cohort',
  );

  const seenSeeds = new Set<number>();
  for (const difficulty of ['hard', 'brutal'] as const) {
    const cohort = manifest.cohorts.find((candidate) => candidate.difficulty === difficulty);
    if (cohort === undefined) continue;
    const expectedSeeds = D569_SECOND_FAMILY_SEEDS[difficulty];
    addViolation(
      violations,
      cohort.directory === expectedDirectory(difficulty),
      'cohort_directory',
      `${difficulty} cohort directory is not the frozen second-family directory`,
    );
    addViolation(
      violations,
      canonicalJson(cohort.fixtures.map((fixture) => fixture.seed)) === canonicalJson(expectedSeeds),
      'cohort_seed_range',
      `${difficulty} cohort must contain its exact ten contiguous second-family seeds`,
    );

    for (const [index, fixture] of cohort.fixtures.entries()) {
      const expectedSeed = expectedSeeds[index];
      addViolation(
        violations,
        !seenSeeds.has(fixture.seed),
        'duplicate_seed',
        `seed ${String(fixture.seed)} appears more than once`,
      );
      seenSeeds.add(fixture.seed);
      for (const namespace of manifest.usedSeedNamespaces) {
        addViolation(
          violations,
          fixture.seed < namespace.start || fixture.seed > namespace.end,
          'seed_overlap',
          `seed ${String(fixture.seed)} overlaps used namespace ${namespace.label}`,
        );
      }
      addViolation(
        violations,
        d466GeneratedRoomOverride(fixture.seed) === null,
        'generator_override',
        `seed ${String(fixture.seed)} is subject to a generator override`,
      );
      if (expectedSeed === undefined || fixture.seed !== expectedSeed) continue;

      const expectedPath = `${cohort.directory}/seed-${String(fixture.seed)}.json`;
      addViolation(
        violations,
        fixture.path === expectedPath,
        'fixture_path',
        `seed ${String(fixture.seed)} has a non-canonical fixture path`,
      );
      let fixtureBytes: string;
      try {
        fixtureBytes = access.readFixture(fixture.path);
      } catch (error) {
        violations.push({
          code: 'fixture_missing',
          message: `cannot read ${fixture.path}: ${error instanceof Error ? error.message : String(error)}`,
        });
        continue;
      }
      addViolation(
        violations,
        sha256(fixtureBytes) === fixture.sha256,
        'fixture_hash',
        `seed ${String(fixture.seed)} fixture hash changed`,
      );

      try {
        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
        addViolation(
          violations,
          room.spec.seed === fixture.seed,
          'fixture_seed',
          `seed ${String(fixture.seed)} fixture contains a different seed`,
        );
        violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
        violations.push(...(difficulty === 'hard'
          ? hardRoomMembershipViolations(room)
          : brutalRoomMembershipViolations(room)));
      } catch (error) {
        violations.push({
          code: 'fixture_parse',
          message: `seed ${String(fixture.seed)} fixture is invalid JSON: ${error instanceof Error ? error.message : String(error)}`,
        });
      }

      const firstGeneration = access.regenerate(fixture.seed, difficulty, 1);
      const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
      addViolation(
        violations,
        firstGeneration === fixtureBytes,
        'first_regeneration',
        `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
      );
      addViolation(
        violations,
        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
        'independent_regeneration',
        `seed ${String(fixture.seed)} independent canonical regeneration differs`,
      );
    }
  }
  return violations;
}

export function canonicalD569SecondFamilyRegeneration(
  seed: number,
  difficulty: D569SecondFamilyDifficulty,
  _generation: 1 | 2,
): string {
  return `${canonicalJson(generateRoom(seed, { difficulty: difficulty as RoomDifficultyProfile }))}\n`;
}

exec
/bin/bash -lc "wc -l tools/d569-second-family-manifest.ts tests/unit/tools/d569-second-family-manifest.test.ts tests/unit/tools/d569-v5.test.ts
sed -n '1,240p' tests/unit/tools/d569-second-family-manifest.test.ts
sed -n '241,520p' tests/unit/tools/d569-second-family-manifest.test.ts
sed -n '1,280p' tests/unit/tools/d569-v5.test.ts
sed -n '281,620p' tests/unit/tools/d569-v5.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
  489 tools/d569-second-family-manifest.ts
  188 tests/unit/tools/d569-second-family-manifest.test.ts
  829 tests/unit/tools/d569-v5.test.ts
 1506 total
import { describe, expect, it } from 'vitest';
import { canonicalJson } from '../../../src/commands/canonical-json';
import { sha256 } from '../../../src/crypto/sha256';
import type { GeneratedRoom } from '../../../src/vtt/room-generator';
import {
  D569_SECOND_FAMILY_LEDGER_ENTRY,
  D569_SECOND_FAMILY_SEEDS,
  brutalRoomMembershipViolations,
  canonicalD569SecondFamilyRegeneration,
  generatedRoomIntegrityViolations,
  hardRoomMembershipViolations,
  validateD569SecondFamilyManifest,
  type D569SecondFamilyManifest,
  type D569SecondFamilyValidationAccess,
} from '../../../tools/d569-second-family-manifest';
import { declareTestInputs } from '../../helpers/test-inputs';

const MANIFEST_PATH = 'tests/fixtures/d569-second-family-manifest.json' as const;
const HARD_FIXTURE_PATHS = [
  'tests/fixtures/arena-basis-hard-2/seed-5118001.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118002.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118003.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118004.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118005.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118006.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118007.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118008.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118009.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118010.json',
] as const;
const BRUTAL_FIXTURE_PATHS = [
  'tests/fixtures/arena-basis-brutal-2/seed-6207001.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207002.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207003.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207004.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207005.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207006.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207007.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207008.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207009.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207010.json',
] as const;
const ALL_FIXTURE_PATHS = [...HARD_FIXTURE_PATHS, ...BRUTAL_FIXTURE_PATHS] as const;
const inputs = declareTestInputs({ fixtures: [MANIFEST_PATH, ...ALL_FIXTURE_PATHS] });
const fixtureBytes = new Map<string, string>(ALL_FIXTURE_PATHS.map((path) => [
  path,
  inputs.fixtures.readText(path),
]));

function manifest(): D569SecondFamilyManifest {
  return JSON.parse(inputs.fixtures.readText(MANIFEST_PATH)) as D569SecondFamilyManifest;
}

function room(path: (typeof ALL_FIXTURE_PATHS)[number]): GeneratedRoom {
  return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
}

function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
  return {
    readFixture: (path) => {
      const bytes = fixtureBytes.get(path);
      if (bytes === undefined) throw new Error(`undeclared fixture ${path}`);
      return bytes;
    },
    regenerate: canonicalD569SecondFamilyRegeneration,
    ...overrides,
  };
}

function codes(
  candidate: unknown,
  validationAccess: D569SecondFamilyValidationAccess = access(),
): readonly string[] {
  return validateD569SecondFamilyManifest(candidate, validationAccess).map((violation) => violation.code);
}

describe('D569 independent second-family manifest', () => {
  it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
    const frozen = manifest();
    expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
      5_118_001, 5_118_002, 5_118_003, 5_118_004, 5_118_005,
      5_118_006, 5_118_007, 5_118_008, 5_118_009, 5_118_010,
    ]);
    expect(D569_SECOND_FAMILY_SEEDS.brutal).toEqual([
      6_207_001, 6_207_002, 6_207_003, 6_207_004, 6_207_005,
      6_207_006, 6_207_007, 6_207_008, 6_207_009, 6_207_010,
    ]);
    expect(frozen.ledgerEntry).toBe(D569_SECOND_FAMILY_LEDGER_ENTRY);
    expect(codes(frozen)).toEqual([]);
  });

  it.each(HARD_FIXTURE_PATHS)('%s satisfies every hard state-property invariant before its pin', (path) => {
    const frozen = room(path);
    expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
    expect(hardRoomMembershipViolations(frozen)).toEqual([]);
  });

  it.each(BRUTAL_FIXTURE_PATHS)(
    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
    (path) => {
      const frozen = room(path);
      expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
    },
  );

  it('second_family_uses_primary_seed: rejects primary or previously used seed namespaces', () => {
    const changed = structuredClone(manifest());
    const hard = changed.cohorts.find((cohort) => cohort.difficulty === 'hard');
    if (hard === undefined) throw new Error('missing hard cohort');
    hard.fixtures[0] = { ...hard.fixtures[0]!, seed: 5_117_001 };
    expect(codes(changed)).toContain('cohort_seed_range');
    expect(codes(changed)).toContain('seed_overlap');
  });

  it('brutal_membership_checks_name_only: rejects structural failures even with the roster names intact', () => {
    const brutal = structuredClone(room(BRUTAL_FIXTURE_PATHS[0]));
    const lastMonster = brutal.encounter.state.combatants.findLast((combatant) =>
      combatant.profile.kind === 'monster');
    if (lastMonster === undefined) throw new Error('missing brutal monster');
    const broken: GeneratedRoom = {
      ...brutal,
      encounter: {
        ...brutal.encounter,
        state: {
          ...brutal.encounter.state,
          combatants: brutal.encounter.state.combatants.map((combatant) =>
            combatant.profile.id === lastMonster.profile.id
              ? { ...combatant, hitPoints: 0, life: 'dead' }
              : combatant),
        },
      },
      spec: {
        ...brutal.spec,
        challengeBudgetEighths: brutal.spec.challengeBudgetEighths + 1,
        terrain: [],
      },
    };
    expect(brutalRoomMembershipViolations(broken).map((violation) => violation.code))
      .toEqual(expect.arrayContaining(['brutal_budget', 'brutal_terrain', 'brutal_productivity']));
  });

  it('rejects missing or rewritten supervisor ledger evidence', () => {
    const changed = structuredClone(manifest());
    changed.ledgerEntry = '';
    expect(codes(changed)).toContain('ledger_evidence');
  });

  it('rejects fixture bytes that no longer match the manifest hash', () => {
    const changedBytes = `${fixtureBytes.get(HARD_FIXTURE_PATHS[0]) ?? ''} `;
    expect(codes(manifest(), access({
      readFixture: (path) => path === HARD_FIXTURE_PATHS[0]
        ? changedBytes
        : access().readFixture(path),
    }))).toContain('fixture_hash');
  });

  it('seed_hash_without_properties: a matching digest cannot excuse failed membership', () => {
    const changed = structuredClone(manifest());
    const hardCohort = changed.cohorts.find((cohort) => cohort.difficulty === 'hard');
    const entry = hardCohort?.fixtures[0];
    if (entry === undefined) throw new Error('missing hard fixture entry');
    const original = room(HARD_FIXTURE_PATHS[0]);
    const { hardFeatures: _removedHardFeatures, ...specWithoutHardFeatures } = original.spec;
    const invalid: GeneratedRoom = {
      ...original,
      spec: specWithoutHardFeatures,
    };
    const invalidBytes = `${canonicalJson(invalid)}\n`;
    entry.sha256 = sha256(invalidBytes);
    expect(codes(changed, access({
      readFixture: (path) => path === entry.path ? invalidBytes : access().readFixture(path),
      regenerate: (seed, difficulty, generation) => seed === entry.seed
        ? invalidBytes
        : canonicalD569SecondFamilyRegeneration(seed, difficulty, generation),
    }))).toContain('hard_chokepoint_structure');
  });

  it('second_generation_not_compared: rejects a divergent independent regeneration', () => {
    const targetSeed = D569_SECOND_FAMILY_SEEDS.hard[0];
    expect(codes(manifest(), access({
      regenerate: (seed, difficulty, generation) => {
        const regenerated = canonicalD569SecondFamilyRegeneration(seed, difficulty, generation);
        return seed === targetSeed && generation === 2 ? `${regenerated} ` : regenerated;
      },
    }))).toContain('independent_regeneration');
  });
});
import { createHash } from 'node:crypto';
import { join } from 'node:path';
import { tmpdir } from 'node:os';
import { describe, expect, it } from 'vitest';
import { canonicalJson } from '../../../src/commands/canonical-json';
import { sha256 } from '../../../src/crypto/sha256';
import type { EncounterState } from '../../../src/combat/encounter';
import { applyRoomInitiativeProfile } from '../../../src/vtt/room-generator';
import {
  createEngineMcpRuntime,
  decodeArenaFixtureText,
  type EngineMcpLauncherManifest,
} from '../../../src/vtt/mcp/entrypoint';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
import { BlindModelIngressRecorder, type BlindIngressRecord } from '../../../src/vtt/blind-model-ingress';
import {
  agentSessionIdFromCli,
  engineDispatchId,
  type AgentInvocation,
  type AgentSessionAdapter,
  type AgentSessionBinding,
  type AgentTurnResult,
} from '../../../src/vtt/agent-session';
import {
  classifyEngineCatalogEvidence,
  type EngineReadinessRecord,
} from '../../../src/vtt/engine-dispatch-evidence';
import {
  analyzeRegisteredPrimaryPair,
  D569_PRIMARY_JUDGES,
  type D569PairedAnalysisDocuments,
} from '../../../tools/d569-v5/analyze-primary-pair';
import { mergeRepairedHard } from '../../../tools/d569-v5/merge-repaired-hard';
import {
  d569ReconciliationSidecarSchema,
  sha256Text,
  type D569ReconciliationSidecar,
} from '../../../tools/d569-v5/reconciliation';
import {
  D569_HARD_REPLACEMENT_KEYS,
  validateD569FirstArm,
  validateD569RegisteredFirstArm,
} from '../../../tools/d569-v5/validate-first-arm';
import {
  D569_EXPERIMENT_MANIFEST_PATH,
  dryRunD569Experiment,
  validateD569ObservedRows,
  type D569DryRunCell,
  type D569ExperimentManifest,
  type D569ObservedRow,
} from '../../../tools/d569-blind-experiment';
import { canonicalD569SecondFamilyRegeneration } from '../../../tools/d569-second-family-manifest';
import { BRUTAL_10_PROTOCOL, buildRerunPacket } from '../../../tools/ai-dm-rerun-packet';
import { parseArenaArgs, runArena } from '../../../tools/ai-dm-arena';
import type { ConversationBoardSnapshotService } from '../../../tools/ai-dm-conversation';
import type { BoardImageArtifact, BoardSnapshotCapture } from '../../../tools/ai-dm-board-snapshot';
import { mkdirSync, mkdtempSync, readFileSync, writeFileSync } from '../../helpers/test-filesystem';

const PATCHED_COMMIT = 'd60a0571a4588879c65992a17ab95023911d17c1';
const MANIFEST = JSON.parse(readFileSync(D569_EXPERIMENT_MANIFEST_PATH, 'utf8')) as D569ExperimentManifest;
const access = {
  readText: (path: string): string => readFileSync(path, 'utf8'),
  secondFamilyAccess: {
    readFixture: (path: string): string => readFileSync(path, 'utf8'),
    regenerate: canonicalD569SecondFamilyRegeneration,
  },
};
const CELLS = dryRunD569Experiment(MANIFEST, access).filter((cell) =>
  cell.arm === 'gpt-5.6-luna-blind');
const FIRST_BRUTAL_STATE = applyRoomInitiativeProfile(
  decodeArenaFixtureText(readFileSync('tests/fixtures/arena-basis-brutal/seed-6203001.json', 'utf8')),
  'derived_v1',
);

function historical(room: number, rep: number, sessionId: string | null) {
  return { room, round: rep, outcome: 'authorized', sessionId };
}

function current(cell: D569DryRunCell, outcome: 'authorized' | 'refused' | 'service_null' | 'infrastructure_failed') {
  const room = cell.seed - (cell.basis === 'hard' ? 5_117_000 : 6_203_000);
  const scheduledCellKey = `${String(room)}:${String(cell.rep)}`;
  const dispatchId = `engine-dispatch:grid-${String(room)}-${String(cell.rep)}-0001`;
  const infrastructure = outcome === 'infrastructure_failed';
  const delivered = outcome === 'authorized' || outcome === 'refused';
  const boardSha = sha256(`board:${cell.basis}:${scheduledCellKey}`);
  const catalog = infrastructure
    ? { status: 'absent' as const, basis: 'required_engine_initialization_failed' as const, dispatchId, corroboration: ['startup failed'] }
    : { status: 'ready' as const, basis: 'required_cli_completed_with_valid_catalog' as const, dispatchId, advertisedInvocationCount: 0, resourceOperationCount: 0 };
  const delivery = infrastructure
    ? { status: 'infrastructure_absent' as const, dispatchId, measurement: null }
    : delivered
      ? { status: 'delivered' as const, dispatchId, contextSha256: 'a'.repeat(64), measurement: { baseBytes: 100, semanticBytes: 10 } }
      : { status: 'not_requested' as const, dispatchId, reason: 'catalog_ready_model_did_not_fetch' as const, measurement: null };
  const state = applyRoomInitiativeProfile(decodeArenaFixtureText(readFileSync(cell.fixturePath, 'utf8')), 'derived_v1');
  return {
    rowContractVersion: 'arena-row-v3', arm: 'single', basis: cell.basis, seed: cell.seed,
    room, round: cell.rep, scheduledCellKey, dispatchId, outcome,
    sessionId: infrastructure ? null : `agent-session:grid-${String(room)}-${String(cell.rep)}`,
    repoCommit: PATCHED_COMMIT, cli: 'codex', model: cell.model, effort: cell.effort,
    dmMode: 'blind', blindRepairArm: 'code_only', blindMaxAttempts: 3, blindFacts: false,
    decisionTransport: 'mcp_minimal', instructionSource: 'kb', escalated: false,
    escalationModel: null, escalationEffort: null, midRoundAdjustmentsEnabled: false, fallbackReason: null, planner: 'model',
    combatModel: 'initiative_segments_v1', initiativeOrder: [], plannedBy: { model: cell.model, effort: cell.effort },
    roundNarrative: null,
    authorizedPlan: outcome === 'authorized' ? [{
      actorId: 'monster:test', resolutionSummary: { actionSlots: [{ kind: 'wait', targetIds: [] }], movementFeet: 0 },
    }] : null,
    firstDecisionAccepted: outcome === 'authorized', decisionAttempts: 1,
    decisionRejectionCodes: [], normalizationCodes: [], skillName: null, skillHash: null,
    rationale: null, overridePolicy: 'strict',
    blindContextVersion: 'blind-turn-context-v1', blindIntentVersion: 'blind-round-intent-v1',
    blindIntentText: null, blindIntents: null, blindResolverOutcome: [], blindRejectionCodes: [],
    blindAttempts: [], blindResolverLatencyMs: 0, semanticBoardEvidence: null,
    creatureFactsEvidence: { sha256: 'c'.repeat(64), utf8Bytes: 1 },
    legalMovementEvidence: { sha256: 'd'.repeat(64), utf8Bytes: 1 },
    blindPrivateAnswerKey: {},
    startingRoomDigest: sha256(canonicalJson(state)), kbHash: cell.sharedKbHash,
    sharedKbComponentHashes: MANIFEST.sharedKb.componentHashes,
    boardImage: {
      mode: 'png', sha256: boardSha, bytes: 24, width: 1, height: 1, captureMs: 0,
      relativePath: `board-images/${boardSha}.png`,
    },
    uiFeedback: null,
    visualProfile: {
      primerVersion: MANIFEST.visualPins.blind.primerVersion,
      images: [{ role: MANIFEST.visualPins.blind.imageRole, sha256: boardSha, ordinal: 1 }],
      glyphMode: MANIFEST.visualPins.blind.glyphMode,
      captureTilePx: MANIFEST.visualPins.blind.captureTilePx,
    },
    engineCatalogEvidence: catalog,
    turnContextDelivery: delivery,
    turnContextConfiguredCaps: { baseBytes: cell.baseContextCapBytes, semanticBytes: cell.semanticContextCapBytes },
    hostContextDiagnostic: null,
    ...(infrastructure ? { failingDispatch: {
      phase: 'primary' as const, exit: 'infrastructure_failed' as const, dispatchId,
      engineCatalogEvidence: catalog, turnContextDelivery: delivery, failureReason: 'startup failed',
    } } : {}),
    baseContextBytes: delivered ? 100 : null,
    semanticBoardBytes: delivered ? 10 : null,
    rawTurnContext: delivered ? '{}' : null,
    preTrimBytes: delivered ? 100 : null,
    postTrimBytes: delivered ? 100 : null,
    turnContextGranularity: delivered ? 'full' : null,
    optionsOmittedForSize: 0, optionsOmittedForSizeByActor: [], roundTotals: { contextBytes: delivered ? 2 : 0 },
    blindIngressAudit: delivered
      ? { version: 2, status: 'complete', passed: true, forbiddenContentPassed: true, delivery }
      : { version: 2, status: 'incomplete', passed: false, forbiddenContentPassed: true, delivery, missingRequiredFields: ['turn_context'] },
    ...(delivered ? { turnContextBudget: {
      configuredBaseBytes: cell.baseContextCapBytes,
      configuredSemanticBytes: cell.semanticContextCapBytes,
      actualBaseBytes: 100, actualSemanticBytes: 10, truncatedBlocks: [],
    } } : {}),
  };
}

function raw(lines: readonly Readonly<Record<string, unknown>>[]): string {
  return `${lines.map((line) => JSON.stringify(line)).join('\n')}\n`;
}

function record(value: unknown): Readonly<Record<string, unknown>> {
  if (typeof value !== 'object' || value === null || Array.isArray(value)) {
    throw new TypeError('Expected a record.');
  }
  return value as Readonly<Record<string, unknown>>;
}

function reconciliation(source: string): D569ReconciliationSidecar {
  const lines = source.trimEnd().split('\n');
  const entries = ([6, 19, 20] as const).map((rawLineNumber) => {
    const line = lines[rawLineNumber - 1];
    if (line === undefined) throw new Error('Fixture raw line is absent.');
    const row = JSON.parse(line) as Readonly<Record<string, unknown>>;
    return {
      rawLineNumber, rawLineSha256: sha256Text(line),
      scheduledCellKey: `${String(row['room'])}:${String(row['round'])}`,
      recoveredSessionId: `recovered-session-${String(rawLineNumber)}`,
      rolloutPath: `/evidence/rollout-${String(rawLineNumber)}.jsonl`,
      rolloutSha256: String(rawLineNumber).padStart(64, '0'),
      reviewerApproval: { reviewer: 'independent-reviewer', approvedAt: '2026-09-09T20:00:00.000Z', decision: 'approved' as const },
    };
  });
  return d569ReconciliationSidecarSchema.parse({
    version: 'd569-reconciliation-v1', rawFileSha256: sha256Text(source), entries,
  });
}

function mutateFirst(source: string, mutation: (row: Readonly<Record<string, unknown>>) => Readonly<Record<string, unknown>>): string {
  const rows = source.trimEnd().split('\n').map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
  const first = rows[0];
  if (first === undefined) throw new Error('Mutation fixture is empty.');
  rows[0] = mutation(first);
  return raw(rows);
}

function retainedHistorical(cell: D569DryRunCell, lineNumber: number): Readonly<Record<string, unknown>> {
  const source = current(cell, 'authorized') as Readonly<Record<string, unknown>>;
  const v3Fields = new Set([
    'rowContractVersion', 'scheduledCellKey', 'dispatchId', 'engineCatalogEvidence', 'turnContextDelivery',
    'turnContextConfiguredCaps', 'hostContextDiagnostic', 'failingDispatch',
  ]);
  return {
    ...Object.fromEntries(Object.entries(source).filter(([key]) => !v3Fields.has(key))),
    repoCommit: '90484d453b7b6d1fe63ed28c0a53570a80e158e6',
    sessionId: [6, 19, 20].includes(lineNumber) ? null : `historical-session-${String(lineNumber)}`,
  };
}

function pairedDocuments(rows: readonly object[]): D569PairedAnalysisDocuments {
  const rawRows: Readonly<Record<string, unknown>>[] = rows.flatMap((row) =>
    ['gpt-5.6-luna-blind', 'gpt-5.6-luna-advice'].map((arm) => ({ ...row, arm })));
  const constructed = buildRerunPacket(rawRows, 569_575, BRUTAL_10_PROTOCOL);
  const packet = constructed.packet.entries.map((entry) => ({ ...entry }));
  const answerKey = constructed.answerKey.entries.map((entry) => ({ ...entry }));
  const scores = Object.fromEntries(D569_PRIMARY_JUDGES.map(({ seat }) => [seat, [] as Readonly<Record<string, unknown>>[]])) as {
    [Seat in (typeof D569_PRIMARY_JUDGES)[number]['seat']]: Readonly<Record<string, unknown>>[];
  };
  for (const entry of packet) {
    for (const { seat } of D569_PRIMARY_JUDGES) {
      scores[seat].push({ blindId: entry.blindId, targetPriority: 2, actionEconomy: 2, coherence: 1, positioning: 1, total: 6 });
    }
  }
  return { packet, answerKey, scoresBySeat: scores };
}

const BRUTAL_SOURCE = raw(CELLS.filter((cell) => cell.basis === 'brutal').map((cell, index) => {
  const row = current(cell, index === 7 ? 'infrastructure_failed' : 'authorized');
  return index === 7 ? { ...row, sessionId: 'observed-before-engine-startup-failed' } : row;
}));

function validateBrutal(rawText: string, manifest = MANIFEST) {
  return validateD569RegisteredFirstArm({
    rawText, basis: 'brutal', cliVersion: 'codex-cli 0.153.4', patchedCommit: PATCHED_COMMIT, manifest,
  });
}

class D569CancellationSnapshotService implements ConversationBoardSnapshotService {
  readonly outputDirectory = mkdtempSync(join(tmpdir(), 'd569-cancelled-delivery-board-'));

  async capture(input: BoardSnapshotCapture): Promise<BoardImageArtifact> {
    const png = Buffer.alloc(96);
    Buffer.from([137, 80, 78, 71, 13, 10, 26, 10]).copy(png);
    png.writeUInt32BE(1, 16);
    png.writeUInt32BE(1, 20);
    Buffer.from(input.source.stateDigest, 'utf8').copy(png, 24, 0, 64);
    const digest = createHash('sha256').update(png).digest('hex');
    const relativePath = `board-images/${digest}.png` as const;
    mkdirSync(join(this.outputDirectory, 'board-images'), { recursive: true });
    writeFileSync(join(this.outputDirectory, relativePath), png);
    const html = Buffer.from('<!doctype html><main>D569 cancellation evidence</main>\n');
    const htmlDigest = createHash('sha256').update(html).digest('hex');
    const htmlRelativePath = `board-html/${htmlDigest}/board.html` as const;
    mkdirSync(join(this.outputDirectory, 'board-html', htmlDigest), { recursive: true });
    writeFileSync(join(this.outputDirectory, htmlRelativePath), html);
    return {
      version: 'arena-board-image-v1', audience: 'dm', mimeType: 'image/png',
      relativePath, sha256: digest, bytes: png.byteLength, width: 1, height: 1,
      capturedAtUnixMs: Date.now(), captureMs: 1, source: { ...input.source },
      chromiumVersion: 'SIMULATED Chromium',
      html: { relativePath: htmlRelativePath, sha256: htmlDigest, bytes: html.byteLength },
      blindState: {
        informationMode: 'blind_state', role: 'dm_board', ordinal: 1,
        primerVersion: MANIFEST.visualPins.blind.primerVersion,
        glyphMode: MANIFEST.visualPins.blind.glyphMode,
        captureTilePx: MANIFEST.visualPins.blind.captureTilePx,
        domEvidence: {
          optionSurfaceAbsent: true, nextEventPreviewAbsent: true,
          coordinateLabels: 1, creatureBadges: 1, rosterEntries: 1, hpBars: 1,
          legendEntries: 1, wallCells: 0, halfCoverCells: 0, threeQuartersCoverCells: 0,
          difficultCells: 0, obscuredCells: 0, illuminatedCells: 0, fogMarks: 0,
          doors: 0, objects: 0, hiddenMarks: 0, multiCellFootprints: 0,
        },
      },
    };
  }

  async close(): Promise<void> { return undefined; }
}

class D569CancellationAfterDeliveryAdapter implements AgentSessionAdapter {
  readonly kind = 'codex' as const;

  constructor(private readonly state: EncounterState) {}

  async probe() { return { present: true, version: 'D569-CANCELLATION-AFTER-DELIVERY' }; }

  async start(invocation: AgentInvocation): Promise<AgentTurnResult> {
    if (invocation.engineDispatchId === undefined) throw new Error('Cancellation fixture omitted dispatch identity.');
    const manifest = JSON.parse(readFileSync(invocation.launcherToken, 'utf8')) as EngineMcpLauncherManifest;
    if (manifest.turnContextSpoolPath === undefined || manifest.blindIngressSpoolPath === undefined ||
      manifest.dispatchPhase === undefined) {
      throw new Error('Cancellation fixture launcher omitted blind evidence spools.');
    }
    const runtime = createEngineMcpRuntime(this.state, {
      runId: manifest.runId, branchId: manifest.branchId, revision: manifest.revision,
      requestId: manifest.requestId, phase: manifest.phase, correctionNumber: manifest.correctionNumber,
      room: manifest.room, historyKind: manifest.historyKind, toolProfile: 'blind', dmMode: 'blind',
      offerEnvironment: buildOfferEnvironment({ kind: 'binding', binding: manifest.offerEnvironment }),
    });
    const context = record(runtime.toolSurface.execute('engine.get_turn_context', {
      run_id: manifest.runId, expected_revision: manifest.revision, scope: 'round', granularity: 'full',
    }));
    const deliveredContext = {
      ...context,
      semantic_board: {
        provenance: { format: 'd569-cancellation-semantic-board-v1' },
        cells: [{ badge: 1, relation: 'occupied' }],
      },
    };
    writeFileSync(manifest.turnContextSpoolPath, `${JSON.stringify({
      ...deliveredContext, dispatchId: invocation.engineDispatchId,
      dispatchProfile: 'blind', dispatchPhase: manifest.dispatchPhase,
    })}\n`, 'utf8');
    const existing = readFileSync(manifest.blindIngressSpoolPath, 'utf8').split('\n')
      .filter((line) => line.trim().length > 0)
      .map((line) => JSON.parse(line) as BlindIngressRecord);
    const ingress = new BlindModelIngressRecorder(existing);
    ingress.record('turn_context', canonicalJson(deliveredContext));
    writeFileSync(manifest.blindIngressSpoolPath,
      `${ingress.records().map((entry) => JSON.stringify(entry)).join('\n')}\n`, 'utf8');
    return {
      exit: 'cancelled', resumeSessionId: agentSessionIdFromCli('d569-cancelled-delivery-session'),
      sessionId: 'd569-cancelled-delivery-session', finalText: 'cancelled after context retrieval', usage: null,
      cancellationReason: 'operator_cancelled_after_delivery',
      processEvidence: {
        startedAtUnixMs: 1, endedAtUnixMs: 2, exitCode: null, signal: 'SIGTERM',
        stdout: 'context delivered', stderr: '', decodedEvents: [],
      },
      partialResultEvidence: {
        status: 'partial', decodedEventCount: 1, finalTextFragment: 'cancelled after context retrieval',
        observedUsage: null, stagedInvocationIds: [],
      },
      engineCatalogEvidence: {
        status: 'ready', basis: 'advertised_tool_invoked', dispatchId: invocation.engineDispatchId,
        advertisedInvocationCount: 1, resourceOperationCount: 0,
      },
    };
  }

  async resume(_binding: AgentSessionBinding, invocation: AgentInvocation): Promise<AgentTurnResult> {
    return this.start(invocation);
  }

  classifyFailure(): 'unknown' { return 'unknown'; }
}

class D569TimeoutBeforeDeliveryAdapter implements AgentSessionAdapter {
  readonly kind = 'codex' as const;

  async probe() { return { present: true, version: 'D569-TIMEOUT-BEFORE-DELIVERY' }; }

  async start(invocation: AgentInvocation): Promise<AgentTurnResult> {
    if (invocation.engineDispatchId === undefined) throw new Error('Timeout fixture omitted dispatch identity.');
    return {
      exit: 'timed_out', resumeSessionId: null, sessionId: 'd569-timeout-before-delivery-session',
      finalText: 'catalog observed before timeout', usage: null, timeoutMs: 240_000,
      processEvidence: {
        startedAtUnixMs: 1, endedAtUnixMs: 2, exitCode: null, signal: 'SIGTERM',
        stdout: 'catalog observed before timeout', stderr: '', decodedEvents: [],
      },
      partialResultEvidence: {
        status: 'partial', decodedEventCount: 1, finalTextFragment: 'catalog observed before timeout',
        observedUsage: null, stagedInvocationIds: [],
      },
      engineCatalogEvidence: {
        status: 'inconclusive', dispatchId: invocation.engineDispatchId, reason: 'no_correlated_catalog',
      },
    };
  }

  async resume(_binding: AgentSessionBinding, invocation: AgentInvocation): Promise<AgentTurnResult> {
    return this.start(invocation);
  }

  classifyFailure(): 'unknown' { return 'unknown'; }
}

async function produceCancellationAfterDeliveryRow(): Promise<Readonly<Record<string, unknown>>> {
  const directory = mkdtempSync(join(tmpdir(), 'd569-runner-cancelled-delivery-'));
  const service = new D569CancellationSnapshotService();
  const state = structuredClone(FIRST_BRUTAL_STATE);
  const rows = await runArena(parseArenaArgs([
    '--rooms', '1', '--reps', '1', '--seed', '6203001', '--basis', 'brutal',
    '--out', join(directory, 'rows.jsonl'), '--dm-mode', 'blind', '--cli', 'codex',
    '--model', 'gpt-5.6-luna', '--effort', 'high',
  ]), {
    adapter: new D569CancellationAfterDeliveryAdapter(state), repoCommit: PATCHED_COMMIT,
    heartbeat: () => undefined, boardSnapshotServiceFactory: async () => service,
    fixtureStates: [state],
  });
  const produced = rows[0];
  if (produced === undefined) throw new Error('Cancellation arena produced no row.');
  return JSON.parse(JSON.stringify(produced)) as Readonly<Record<string, unknown>>;
}

async function produceTimeoutBeforeDeliveryRow(): Promise<Readonly<Record<string, unknown>>> {
  const directory = mkdtempSync(join(tmpdir(), 'd569-runner-timeout-before-delivery-'));
  const service = new D569CancellationSnapshotService();
  const state = structuredClone(FIRST_BRUTAL_STATE);
  const rows = await runArena(parseArenaArgs([
    '--rooms', '1', '--reps', '1', '--seed', '6203001', '--basis', 'brutal',
    '--out', join(directory, 'rows.jsonl'), '--dm-mode', 'blind', '--cli', 'codex',
    '--model', 'gpt-5.6-luna', '--effort', 'high',
  ]), {
    adapter: new D569TimeoutBeforeDeliveryAdapter(), repoCommit: PATCHED_COMMIT,
    heartbeat: () => undefined, boardSnapshotServiceFactory: async () => service,
    fixtureStates: [state],
  });
  const produced = rows[0];
  if (produced === undefined) throw new Error('Timeout arena produced no row.');
  return JSON.parse(JSON.stringify(produced)) as Readonly<Record<string, unknown>>;
}

const RUNNER_CANCELLATION_AFTER_DELIVERY_ROW = await produceCancellationAfterDeliveryRow();
const RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW = await produceTimeoutBeforeDeliveryRow();

function capturedFailure(action: () => void): string {
  try {
    action();
  } catch (error) {
    return error instanceof Error ? error.message : String(error);
  }
  throw new Error('Expected consumer validation to reject contradictory evidence.');
}

type InvalidCatalogSource = 'malformed' | 'wrong_profile';

function classifiedInvalidCatalogForConsumer(
  dispatchIdValue: string,
  source: InvalidCatalogSource,
) {
  const dispatchId = engineDispatchId(dispatchIdValue);
  const readiness: readonly EngineReadinessRecord[] = source === 'wrong_profile' ? [{
    version: 1, dispatchId, phase: 'primary', profile: 'dm', requestId: 'request:d569-invalid-catalog',
    event: 'tools_list_stream_write_completed', generatedAtUnixMs: 1, writeCompletedAtUnixMs: 2,
    responseId: 'wrong-profile-response', responseSha256: 'a'.repeat(64),
    expectedToolNames: ['engine.get_turn_context'], returnedToolNames: ['engine.get_turn_context'],
    descriptorSha256: 'b'.repeat(64), validation: { status: 'valid' },
  }] : [];
  const evidence = classifyEngineCatalogEvidence({
    dispatchId, expectedProfile: 'blind', expectedPhase: 'primary',
    expectedRequestId: 'request:d569-invalid-catalog', expectedToolNames: ['engine.get_turn_context'],
    events: [], readiness, completed: false, requiredStartupFailed: false,
    ...(source === 'malformed' ? { malformedReadiness: true } : {}),
  });
  if (evidence.status !== 'inconclusive' || evidence.reason !== 'invalid_catalog_response') {
    throw new Error(`${source} consumer fixture did not classify as invalid catalog evidence.`);
  }
  return evidence;
}

function evaluateRunnerTimeoutConsumers(): {
  readonly packetRows: number;
  readonly registeredRows: number;
  readonly observedViolations: readonly string[];
  readonly contradictoryPacketError: string;
  readonly contradictoryRegisteredError: string;
  readonly contradictoryObservedViolations: readonly string[];
  readonly invalidCatalogResults: readonly {
    readonly exit: 'timed_out' | 'cancelled';
    readonly source: InvalidCatalogSource;
    readonly packetError: string;
    readonly registeredError: string;
    readonly observedViolations: readonly string[];
  }[];
} {
  const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
  const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
    .map((cell) => current(cell, 'authorized'));
  packetRows[0] = produced;
  const packetCount = pairedDocuments(packetRows).packet.length;
  const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
    .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
  registeredRows[0] = produced;
  const registeredCount = validateBrutal(raw(registeredRows)).length;
  const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
  if (cell === undefined) throw new Error('Registered timeout cell is absent.');
  const boardImage = record(produced['boardImage']);
  const observedSessionId = produced['sessionId'];
  const scheduledCellKey = produced['scheduledCellKey'];
  const dispatchId = produced['dispatchId'];
  if (typeof observedSessionId !== 'string' || observedSessionId.length === 0 ||
    typeof scheduledCellKey !== 'string' || typeof dispatchId !== 'string') {
    throw new Error('Runner timeout row omitted its observed session or dispatch identity.');
  }
  const observed: D569ObservedRow = {
    arm: cell.arm, cli: 'codex', cliVersion: 'codex-cli 0.153.4', model: cell.model, effort: cell.effort,
    family: cell.family, basis: cell.basis, seed: cell.seed, rep: cell.rep,
    sessionId: observedSessionId, outcome: 'refused', scheduledCellKey, dispatchId,
    engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
    turnContextDelivery: { status: 'timeout_before_delivery' },
    timeoutMs: cell.timeoutMs, escalationModel: null, modelDefaultFallback: false,
    stateHash: cell.stateHash, sharedKbHash: cell.sharedKbHash,
    visualSourceHash: cell.visualSourceHash, visualProfileHash: cell.visualProfileHash,
    visualArtifactHash: String(boardImage['sha256']), baseContextCapBytes: cell.baseContextCapBytes,
    semanticContextCapBytes: cell.semanticContextCapBytes, truncatedBlocks: [],
  };
  const observedViolations = validateD569ObservedRows([cell], [observed]).map((violation) => violation.code);
  const catalog = record(produced['engineCatalogEvidence']);
  const contradictory = {
    ...produced,
    engineCatalogEvidence: { ...catalog, reason: 'conflicting_success_and_failure' },
  };
  const contradictoryPacketRows = [...packetRows];
  contradictoryPacketRows[0] = contradictory;
  const contradictoryRegisteredRows = [...registeredRows];
  contradictoryRegisteredRows[0] = contradictory;
  const invalidCatalogResults = (['timed_out', 'cancelled'] as const).flatMap((exit) =>
    (['malformed', 'wrong_profile'] as const).map((source) => {
      const invalidCatalog = classifiedInvalidCatalogForConsumer(dispatchId, source);
      const delivery = exit === 'timed_out'
        ? produced['turnContextDelivery']
        : {
            status: 'not_requested' as const, dispatchId,
            reason: 'dispatch_cancelled' as const, measurement: null,
          };
      const ingress = record(produced['blindIngressAudit']);
      const invalidRow = {
        ...produced,
        ...(exit === 'cancelled'
          ? {
              outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
              turnContextDelivery: delivery,
              blindIngressAudit: { ...ingress, delivery },
              failingDispatch: {
                phase: 'primary', exit, dispatchId, engineCatalogEvidence: invalidCatalog,
                turnContextDelivery: delivery, failureReason: 'operator cancelled before delivery',
              },
            }
          : {}),
        engineCatalogEvidence: invalidCatalog,
      };
      const invalidPacketRows = [...packetRows];
      invalidPacketRows[0] = invalidRow;
      const invalidRegisteredRows = [...registeredRows];
      invalidRegisteredRows[0] = invalidRow;
      const observedDelivery: D569ObservedRow['turnContextDelivery'] = exit === 'timed_out'
        ? { status: 'timeout_before_delivery' }
        : { status: 'not_requested', reason: 'dispatch_cancelled' };
      const invalidObserved: D569ObservedRow = {
        ...observed,
        outcome: exit === 'timed_out' ? 'refused' : 'infrastructure_failed',
        engineCatalogEvidence: { status: 'inconclusive', reason: invalidCatalog.reason },
        turnContextDelivery: observedDelivery,
        ...(exit === 'cancelled' ? {
          failingDispatch: {
            dispatchId, exit, engineCatalogEvidence: { status: 'inconclusive' },
            turnContextDelivery: { status: 'not_requested' },
          },
        } : {}),
      };
      return {
        exit, source,
        packetError: capturedFailure(() => { pairedDocuments(invalidPacketRows); }),
        registeredError: capturedFailure(() => { validateBrutal(raw(invalidRegisteredRows)); }),
        observedViolations: validateD569ObservedRows([cell], [invalidObserved])
          .map((violation) => violation.code),
      };
    }));
  return {
    packetRows: packetCount,
    registeredRows: registeredCount,
    observedViolations,
    contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
    contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
    contradictoryObservedViolations: validateD569ObservedRows([cell], [{
      ...observed,
      engineCatalogEvidence: { status: 'inconclusive', reason: 'conflicting_success_and_failure' },
    }]).map((violation) => violation.code),
    invalidCatalogResults,
  };
}

const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();

describe('D569 v5 replacement validation and paired analysis tools', () => {
  it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
    const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
      roomIndex + 1, rep,
      [6, 19, 20].includes(roomIndex * 3 + rep) ? null : `historical-session-${String(roomIndex + 1)}-${String(rep)}`,
    ))).flat();
    const original = raw(originalRows);
    const sidecar = reconciliation(original);
    const hardCells = CELLS.filter((cell) => cell.basis === 'hard');
    const replacements = raw(D569_HARD_REPLACEMENT_KEYS.map((key) => {
      const cell = hardCells.find((candidate) => `${String(candidate.seed - 5_117_000)}:${String(candidate.rep)}` === key);
      if (cell === undefined) throw new Error(`Missing hard replacement cell ${key}.`);
      return current(cell, key === '2:1' ? 'infrastructure_failed' : key === '4:1' ? 'service_null' : 'authorized');
    }));
    const mixed = mergeRepairedHard(original, replacements);
    const originalLines = original.trimEnd().split('\n');
    const mixedLines = mixed.trimEnd().split('\n');
    expect(mixedLines.filter((line, index) => line === originalLines[index])).toHaveLength(27);
    expect(validateD569FirstArm(mixed, sidecar)).toHaveLength(30);
  });

  it('migrates every registered validator invariant for a complete current brutal grid', () => {
    expect(validateBrutal(BRUTAL_SOURCE)).toHaveLength(30);
  });

  it('validates registered hard replacements against retained original cells and reconciliation provenance', () => {
    const hardCells = CELLS.filter((cell) => cell.basis === 'hard');
    const original = raw(hardCells.map((cell, index) => retainedHistorical(cell, index + 1)));
    const sidecar = reconciliation(original);
    const replacements = raw(D569_HARD_REPLACEMENT_KEYS.map((key) => {
      const [roomText, repText] = key.split(':');
      const seed = 5_117_000 + Number(roomText);
      const cell = hardCells.find((candidate) => candidate.seed === seed && candidate.rep === Number(repText));
      if (cell === undefined) throw new Error(`Missing registered hard replacement ${key}.`);
      return current(cell, 'authorized');
    }));
    const merged = mergeRepairedHard(original, replacements);
    const observations = validateD569RegisteredFirstArm({
      rawText: merged, basis: 'hard', cliVersion: 'codex-cli 0.153.4',
      patchedCommit: PATCHED_COMMIT, sidecar,
    });
    expect(observations).toHaveLength(30);
    expect(observations.filter((entry) => entry.row['sessionId'] === null)
      .map((entry) => entry.effectiveSessionId)).toEqual([
        'recovered-session-6', 'recovered-session-19', 'recovered-session-20',
      ]);

exec
/bin/bash -lc "rg -n \"second.family|secondFamily|D569_EXPERIMENT_MANIFEST_PATH|version|violations|canonicalD569\" tests/unit/tools/d569-v5.test.ts tools/d569-blind-experiment.ts
sed -n '430,510p' tools/d569-second-family-manifest.ts
sed -n '760,850p' tests/unit/tools/d569-v5.test.ts
sed -n '1,180p' tools/d569-blind-experiment.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
tools/d569-blind-experiment.ts:21:} from './d569-second-family-manifest';
tools/d569-blind-experiment.ts:24:export const D569_EXPERIMENT_MANIFEST_PATH =
tools/d569-blind-experiment.ts:27:  'tests/fixtures/d569-second-family-manifest.json' as const;
tools/d569-blind-experiment.ts:235:  version: z.literal('d569-blind-experiment-v5'),
tools/d569-blind-experiment.ts:341:  secondFamily: z.strictObject({
tools/d569-blind-experiment.ts:399:    version: z.literal('d569-sealed-override-v1'),
tools/d569-blind-experiment.ts:420:  readonly secondFamilyAccess: D569SecondFamilyValidationAccess;
tools/d569-blind-experiment.ts:429:  violations: D569ExperimentViolation[],
tools/d569-blind-experiment.ts:434:  if (!condition) violations.push({ code, message });
tools/d569-blind-experiment.ts:493:  const violations: D569ExperimentViolation[] = [];
tools/d569-blind-experiment.ts:495:  addViolation(violations, canonicalEqual(manifest.coreArms, D569_CORE_ARM_IDENTITIES),
tools/d569-blind-experiment.ts:497:  addViolation(violations, canonicalEqual(manifest.hintArms, D569_HINT_ARM_IDENTITIES),
tools/d569-blind-experiment.ts:499:  addViolation(violations, canonicalEqual(manifest.analysisComparisons, D569_ANALYSIS_COMPARISONS),
tools/d569-blind-experiment.ts:501:  addViolation(violations,
tools/d569-blind-experiment.ts:504:  addViolation(violations,
tools/d569-blind-experiment.ts:507:  addViolation(violations, canonicalEqual(manifest.repairComparisons, REQUIRED_REPAIR_COMPARISONS),
tools/d569-blind-experiment.ts:509:  addViolation(violations,
tools/d569-blind-experiment.ts:512:  addViolation(violations,
tools/d569-blind-experiment.ts:515:  addViolation(violations, canonicalEqual(manifest.sealedOverride.blindSealFields, REQUIRED_BLIND_SEAL_FIELDS),
tools/d569-blind-experiment.ts:517:  addViolation(violations,
tools/d569-blind-experiment.ts:527:    addViolation(violations, left !== undefined && right !== undefined,
tools/d569-blind-experiment.ts:533:    addViolation(violations, canonicalEqual(comparison.eligibleJudgeSeats, expectedSeats),
tools/d569-blind-experiment.ts:535:    addViolation(violations, expectedSeats.length >= manifest.judgePanel.minimumConsensusSeats &&
tools/d569-blind-experiment.ts:541:  addViolation(violations,
tools/d569-blind-experiment.ts:550:      addViolation(violations, manifest.sharedKb.componentHashes[key] === sha256(bytes),
tools/d569-blind-experiment.ts:553:      violations.push({
tools/d569-blind-experiment.ts:559:  addViolation(violations,
tools/d569-blind-experiment.ts:565:  addViolation(violations, manifest.primaryCohorts.length === 2 && new Set(primaryIds).size === 2,
tools/d569-blind-experiment.ts:572:    addViolation(violations, canonicalEqual(cohort.fixtures.map((fixture) => fixture.seed), expectedSeeds),
tools/d569-blind-experiment.ts:576:      addViolation(violations, fixture.path ===
tools/d569-blind-experiment.ts:579:      addViolation(violations, !allSeeds.has(fixture.seed), 'duplicate_seed',
tools/d569-blind-experiment.ts:585:        addViolation(violations, sha256(bytes) === fixture.sha256, 'primary_fixture_hash',
tools/d569-blind-experiment.ts:590:        addViolation(violations, parsed.spec?.seed === fixture.seed &&
tools/d569-blind-experiment.ts:594:        violations.push({
tools/d569-blind-experiment.ts:603:    const secondBytes = access.readText(manifest.secondFamily.manifestPath);
tools/d569-blind-experiment.ts:604:    addViolation(violations, sha256(secondBytes) === manifest.secondFamily.manifestSha256,
tools/d569-blind-experiment.ts:605:      'second_family_manifest_hash', 'second-family manifest bytes changed');
tools/d569-blind-experiment.ts:607:    for (const violation of validateD569SecondFamilyManifest(second, access.secondFamilyAccess)) {
tools/d569-blind-experiment.ts:608:      violations.push({ code: `second_family_${violation.code}`, message: violation.message });
tools/d569-blind-experiment.ts:612:      addViolation(violations, !allSeeds.has(seed) && !manifest.trainingSeeds.includes(seed),
tools/d569-blind-experiment.ts:613:        'second_family_overlap', `second-family seed ${String(seed)} is trained or overlaps evaluation`);
tools/d569-blind-experiment.ts:615:    addViolation(violations,
tools/d569-blind-experiment.ts:620:    'second_family_seed_range', 'fluke guard must use the frozen encounter cohort, not a shuffle seed');
tools/d569-blind-experiment.ts:622:    violations.push({
tools/d569-blind-experiment.ts:623:      code: 'second_family_manifest_missing',
tools/d569-blind-experiment.ts:627:  return violations;
tools/d569-blind-experiment.ts:657:function secondFamilyCohorts(
tools/d569-blind-experiment.ts:661:  const parsed = JSON.parse(access.readText(manifest.secondFamily.manifestPath)) as D569SecondFamilyManifest;
tools/d569-blind-experiment.ts:675:  const violations = validateD569ExperimentManifest(input, access);
tools/d569-blind-experiment.ts:676:  if (violations.length > 0) {
tools/d569-blind-experiment.ts:677:    throw new TypeError(`Invalid D569 experiment manifest: ${violations[0]?.code ?? 'unknown'}`);
tools/d569-blind-experiment.ts:681:  const cohorts = family === 'primary' ? manifest.primaryCohorts : secondFamilyCohorts(manifest, access);
tools/d569-blind-experiment.ts:761:  const violations: D569ExperimentViolation[] = [];
tools/d569-blind-experiment.ts:772:    addViolation(violations, cell !== undefined, 'unexpected_row', `unexpected row ${rowKey}`);
tools/d569-blind-experiment.ts:773:    addViolation(violations, !seen.has(rowKey), 'duplicate_rep', `duplicate row ${rowKey}`);
tools/d569-blind-experiment.ts:776:    addViolation(violations, row.model === cell.model,
tools/d569-blind-experiment.ts:778:    addViolation(violations, row.effort === cell.effort,
tools/d569-blind-experiment.ts:780:    addViolation(violations, row.cli.trim().length > 0,
tools/d569-blind-experiment.ts:782:    addViolation(violations, row.cliVersion.trim().length > 0,
tools/d569-blind-experiment.ts:783:      'cli_version', `row ${rowKey} omitted the executing CLI version`);
tools/d569-blind-experiment.ts:787:    addViolation(violations, !integrityIndeterminate,
tools/d569-blind-experiment.ts:789:    addViolation(violations, diagnosedInfrastructure
tools/d569-blind-experiment.ts:807:      addViolation(violations, !dispatchIds.has(dispatchId), 'dispatch_identity',
tools/d569-blind-experiment.ts:811:    addViolation(violations, diagnosedInfrastructure || row.failingDispatch === undefined,
tools/d569-blind-experiment.ts:815:      addViolation(violations, !primaryFailure ||
tools/d569-blind-experiment.ts:820:    addViolation(violations, row.engineCatalogEvidence?.status !== 'absent' || diagnosedInfrastructure,
tools/d569-blind-experiment.ts:823:    addViolation(violations, row.timeoutMs === cell.timeoutMs, 'timeout', `row ${rowKey} changed timeout`);
tools/d569-blind-experiment.ts:824:    addViolation(violations, row.escalationModel === null, 'escalation', `row ${rowKey} escalated`);
tools/d569-blind-experiment.ts:825:    addViolation(violations, !row.modelDefaultFallback, 'model_default_fallback',
tools/d569-blind-experiment.ts:827:    addViolation(violations, row.stateHash === cell.stateHash &&
tools/d569-blind-experiment.ts:830:    addViolation(violations, row.visualProfileHash === cell.visualProfileHash,
tools/d569-blind-experiment.ts:834:    addViolation(violations, pairedVisualHash === undefined || pairedVisualHash === row.visualArtifactHash,
tools/d569-blind-experiment.ts:837:    addViolation(violations, row.baseContextCapBytes === cell.baseContextCapBytes &&
tools/d569-blind-experiment.ts:840:    addViolation(violations, row.truncatedBlocks.length === 0, 'required_block_truncated',
tools/d569-blind-experiment.ts:844:    addViolation(violations, seen.has(expectedKey), 'missing_row', `missing row ${expectedKey}`);
tools/d569-blind-experiment.ts:846:  return violations;
tools/d569-blind-experiment.ts:875:  readonly secondFamilyGainCiLower: number;
tools/d569-blind-experiment.ts:887:  const gain = result.primaryGainCiLower > 0 && result.secondFamilyGainCiLower > 0;
tools/d569-blind-experiment.ts:1317:  const violations: D569ExperimentViolation[] = [];
tools/d569-blind-experiment.ts:1318:  addViolation(violations,
tools/d569-blind-experiment.ts:1322:  addViolation(violations, evidence.blindSeal.sealedAtMs < evidence.topAdvice.revealedAtMs &&
tools/d569-blind-experiment.ts:1325:  addViolation(violations, evidence.topAdvice.answerKeyOnly, 'override_top_visibility',
tools/d569-blind-experiment.ts:1327:  addViolation(violations, sha256(evidence.blindSeal.intentText) === evidence.blindSeal.intentSha256,
tools/d569-blind-experiment.ts:1329:  addViolation(violations, evidence.blindSeal.capsuleDigest === evidence.topAdvice.capsuleDigest &&
tools/d569-blind-experiment.ts:1333:  return violations;
tests/unit/tools/d569-v5.test.ts:45:  D569_EXPERIMENT_MANIFEST_PATH,
tests/unit/tools/d569-v5.test.ts:52:import { canonicalD569SecondFamilyRegeneration } from '../../../tools/d569-second-family-manifest';
tests/unit/tools/d569-v5.test.ts:60:const MANIFEST = JSON.parse(readFileSync(D569_EXPERIMENT_MANIFEST_PATH, 'utf8')) as D569ExperimentManifest;
tests/unit/tools/d569-v5.test.ts:63:  secondFamilyAccess: {
tests/unit/tools/d569-v5.test.ts:65:    regenerate: canonicalD569SecondFamilyRegeneration,
tests/unit/tools/d569-v5.test.ts:146:      ? { version: 2, status: 'complete', passed: true, forbiddenContentPassed: true, delivery }
tests/unit/tools/d569-v5.test.ts:147:      : { version: 2, status: 'incomplete', passed: false, forbiddenContentPassed: true, delivery, missingRequiredFields: ['turn_context'] },
tests/unit/tools/d569-v5.test.ts:183:    version: 'd569-reconciliation-v1', rawFileSha256: sha256Text(source), entries,
tests/unit/tools/d569-v5.test.ts:255:      version: 'arena-board-image-v1', audience: 'dm', mimeType: 'image/png',
tests/unit/tools/d569-v5.test.ts:284:  async probe() { return { present: true, version: 'D569-CANCELLATION-AFTER-DELIVERY' }; }
tests/unit/tools/d569-v5.test.ts:349:  async probe() { return { present: true, version: 'D569-TIMEOUT-BEFORE-DELIVERY' }; }
tests/unit/tools/d569-v5.test.ts:433:    version: 1, dispatchId, phase: 'primary', profile: 'dm', requestId: 'request:d569-invalid-catalog',
        fixtureBytes = access.readFixture(fixture.path);
      } catch (error) {
        violations.push({
          code: 'fixture_missing',
          message: `cannot read ${fixture.path}: ${error instanceof Error ? error.message : String(error)}`,
        });
        continue;
      }
      addViolation(
        violations,
        sha256(fixtureBytes) === fixture.sha256,
        'fixture_hash',
        `seed ${String(fixture.seed)} fixture hash changed`,
      );

      try {
        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
        addViolation(
          violations,
          room.spec.seed === fixture.seed,
          'fixture_seed',
          `seed ${String(fixture.seed)} fixture contains a different seed`,
        );
        violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
        violations.push(...(difficulty === 'hard'
          ? hardRoomMembershipViolations(room)
          : brutalRoomMembershipViolations(room)));
      } catch (error) {
        violations.push({
          code: 'fixture_parse',
          message: `seed ${String(fixture.seed)} fixture is invalid JSON: ${error instanceof Error ? error.message : String(error)}`,
        });
      }

      const firstGeneration = access.regenerate(fixture.seed, difficulty, 1);
      const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
      addViolation(
        violations,
        firstGeneration === fixtureBytes,
        'first_regeneration',
        `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
      );
      addViolation(
        violations,
        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
        'independent_regeneration',
        `seed ${String(fixture.seed)} independent canonical regeneration differs`,
      );
    }
  }
  return violations;
}

export function canonicalD569SecondFamilyRegeneration(
  seed: number,
  difficulty: D569SecondFamilyDifficulty,
  _generation: 1 | 2,
): string {
  return `${canonicalJson(generateRoom(seed, { difficulty: difficulty as RoomDifficultyProfile }))}\n`;
}
      const { hostContextDiagnostic: _hostContextDiagnostic, ...withoutHostDiagnostic } = row;
      return withoutHostDiagnostic;
    }))).toThrow();
  });

  it('rejects partial v3 hybrids and integrity-indeterminate rows before analysis', () => {
    const historicalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) =>
      historical(roomIndex + 1, rep, `historical-${String(roomIndex + 1)}-${String(rep)}`))).flat();
    expect(() => validateD569FirstArm(raw(historicalRows.map((row, index) => index === 0
      ? { ...row, dispatchId: 'engine-dispatch:hybrid-0001' }
      : row)))).toThrow('partial arena-row-v3 hybrid');
    expect(() => validateD569FirstArm(mutateFirst(BRUTAL_SOURCE, (row) => ({
      ...row, outcome: 'integrity_indeterminate',
    })))).toThrow('integrity-indeterminate');
  });

  it('rejects registered seed mutations', () => {
    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => ({ ...row, seed: 123 })))).toThrow('Unregistered');
  });

  it('rejects registered fixture-state mutations', () => {
    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => ({ ...row, startingRoomDigest: '0'.repeat(64) }))))
      .toThrow('fixture state');
  });

  it('rejects registered code-cohort mutations', () => {
    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => ({ ...row, repoCommit: 'f'.repeat(40) }))))
      .toThrow('Code-cohort');
  });

  it.each([
    ['model', (row: Readonly<Record<string, unknown>>) => ({ ...row, model: 'gpt-5.6-sol' }), 'launch provenance'],
    ['effort', (row: Readonly<Record<string, unknown>>) => ({ ...row, effort: 'medium' }), 'launch provenance'],
    ['KB hash', (row: Readonly<Record<string, unknown>>) => ({ ...row, kbHash: '0'.repeat(64) }), 'KB provenance'],
    ['KB components', (row: Readonly<Record<string, unknown>>) => ({
      ...row, sharedKbComponentHashes: { root: '0'.repeat(64) },
    }), 'KB provenance'],
    ['visual pin', (row: Readonly<Record<string, unknown>>) => ({
      ...row,
      visualProfile: { ...row['visualProfile'] as Readonly<Record<string, unknown>>, glyphMode: 'letters' },
    }), 'visual provenance'],
  ] as const)('rejects registered %s provenance mutations', (_label, mutate, message) => {
    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, mutate))).toThrow(message);
  });

  it('rejects registered manifest mutations before accepting rows', () => {
    const changedManifest: D569ExperimentManifest = {
      ...MANIFEST,
      sharedKb: { ...MANIFEST.sharedKb, combinedStartupHash: '0'.repeat(64) },
    };
    expect(() => validateBrutal(BRUTAL_SOURCE, changedManifest)).toThrow('registered manifest is invalid');
  });

  it('runs raw outcomes through packet/key construction and the registered paired scorer', () => {
    const rawRows = CELLS.filter((cell) => cell.basis === 'brutal').map((cell, index) =>
      current(cell, index === 3 ? 'infrastructure_failed' : index === 8 ? 'service_null' : 'authorized'));
    const documents = pairedDocuments(rawRows);
    const result = analyzeRegisteredPrimaryPair({
      manifest: MANIFEST, basis: 'brutal', documents, resamples: 20, bootstrapSeed: 569_575,
    });
    expect(documents.packet).toHaveLength(60);
    expect(documents.answerKey).toHaveLength(60);
    expect(result.left.counts).toMatchObject({ infrastructureFailed: 1, serviceFailed: 1 });
    expect(result.pairedZeroInclusive.total.count).toBe(29);
    const mismatched = { ...documents, answerKey: documents.answerKey.slice(1) };
    expect(() => analyzeRegisteredPrimaryPair({
      manifest: MANIFEST, basis: 'brutal', documents: mismatched, resamples: 20,
    })).toThrow('same 60 blind IDs');
  });
});
import { canonicalJson } from '../src/commands/canonical-json';
import { sha256 } from '../src/crypto/sha256';
import { BLIND_TURN_CONTEXT_MAX_BYTES } from '../src/vtt/blind-turn-context';
import {
  D569_AI_DM_KB_COMPONENT_PATHS,
  D569_AI_DM_KB_ROOT,
} from '../src/vtt/knowledge-base-contract';
import { SEMANTIC_BOARD_MAX_BYTES, TURN_CONTEXT_MAX_BYTES } from '../src/vtt/mcp/engine-server';
import { DEFAULT_RENDERER_PROFILE } from '../src/vtt/renderer-profile';
import {
  d569PartialEvidenceHasIntegritySignal,
  type EngineCatalogIntegrityView,
  type TurnContextDeliveryIntegrityView,
} from '../src/vtt/turn-context-delivery';
import { BLIND_STATE_PRIMER_VERSION } from './ai-dm-board-snapshot';
import {
  D569_SECOND_FAMILY_SEEDS,
  validateD569SecondFamilyManifest,
  type D569SecondFamilyManifest,
  type D569SecondFamilyValidationAccess,
} from './d569-second-family-manifest';
import { z } from 'zod';

export const D569_EXPERIMENT_MANIFEST_PATH =
  'tests/fixtures/d569-blind-experiment-manifest.json' as const;
export const D569_SECOND_FAMILY_MANIFEST_PATH =
  'tests/fixtures/d569-second-family-manifest.json' as const;
export const D569_EXPERIMENT_WALL_MS = 240_000 as const;
export const D569_BOOTSTRAP_RESAMPLES = 100_000 as const;
export const D569_NONINFERIORITY_MARGIN = -0.20 as const;
export const D569_REFUSAL_RISK_MARGIN = 0.00 as const;
export const D569_REPS = 3 as const;

export const D569_PLAYER_MODELS = Object.freeze([
  'gpt-5.6-luna',
  'claude-opus-4-8',
  'gpt-5.6-sol',
  'gpt-6-astra',
] as const);

export const D569_PREREGISTRATION_AMENDMENTS = Object.freeze([
  {
    id: 'refusal-risk-upper-bound',
    timing: 'pre-results',
    reason: 'The prior lower-bound check allowed an increased refusal risk compatible with the data.',
  },
  {
    id: 'remove-fable-player-arms',
    timing: 'pre-results',
    reason: 'Fable usage exhausted; no replacement in this registration',
  },
  {
    id: 'fresh-context-judge-eligibility',
    timing: 'pre-results',
    reason: 'Owner ruling D578.3: "It is ok for the same model to judge if it starts from a fresh context".',
  },
  {
    id: 'add-astra-high-player-arms',
    timing: 'pre-astra-run-and-analysis',
    date: '2026-09-08',
    reason: 'Owner ruling D586.24 requires Astra at high effort for a fair comparison with Sol high; this amendment was recorded before any Astra player arm was run or scored.',
  },
  {
    id: 'replace-opus-and-notes-only-panel',
    timing: 'pre-opus-4-8-player-run-and-new-panel-scoring',
    date: '2026-09-08',
    reason: 'Owner rulings replace every active claude-opus-5 use with claude-opus-4-8 and set claude-fable-5-1, gpt-6-astra, and gpt-5.6-sol at high effort as the scoring panel; claude-opus-4-8 supplies labelled notes only and never participates in analysis or decisions.',
  },
] as const);

export const D569_SCORING_PANEL_IDENTITIES = Object.freeze([
  { id: 'claude-fable-5-1', model: 'claude-fable-5-1', effort: 'high', role: 'scoring' },
  { id: 'gpt-6-astra', model: 'gpt-6-astra', effort: 'high', role: 'scoring' },
  { id: 'gpt-5.6-sol', model: 'gpt-5.6-sol', effort: 'high', role: 'scoring' },
] as const);
export const D569_ADVISORY_NOTES_SOURCE = Object.freeze({
  id: 'claude-opus-4-8', model: 'claude-opus-4-8', effort: 'high', role: 'advisory_notes',
} as const);

export const D569_CORE_ARM_IDENTITIES = Object.freeze(D569_PLAYER_MODELS.flatMap((model) =>
  (['blind', 'advice'] as const).map((dmMode) => ({
    id: `${model}-${dmMode}`,
    model,
    effort: 'high' as const,
    dmMode,
    repair: dmMode === 'blind' ? 'code_only' as const : null,
    adviceAssisted: dmMode === 'advice',
  }))));

export const D569_HINT_ARM_IDENTITIES = Object.freeze([
  {
    id: 'gpt-5.6-luna-blind-minhint', model: 'gpt-5.6-luna', effort: 'high',
    dmMode: 'blind', repair: 'minimal_legal_alternative', adviceAssisted: true,
  },
  {
    id: 'gpt-5.6-sol-blind-minhint', model: 'gpt-5.6-sol', effort: 'high',
    dmMode: 'blind', repair: 'minimal_legal_alternative', adviceAssisted: true,
  },
  {
    id: 'gpt-6-astra-blind-minhint', model: 'gpt-6-astra', effort: 'high',
    dmMode: 'blind', repair: 'minimal_legal_alternative', adviceAssisted: true,
  },
] as const);

const ALL_SCORING_SEAT_IDS = Object.freeze(D569_SCORING_PANEL_IDENTITIES.map((seat) => seat.id));

export const D569_ANALYSIS_COMPARISONS = Object.freeze([
  {
    id: 'gpt-5.6-luna-blind-vs-advice',
    leftArm: 'gpt-5.6-luna-blind',
    rightArm: 'gpt-5.6-luna-advice',
    estimand: 'blind_minus_own_advice' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'claude-opus-4-8-blind-vs-advice',
    leftArm: 'claude-opus-4-8-blind',
    rightArm: 'claude-opus-4-8-advice',
    estimand: 'blind_minus_own_advice' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'gpt-5.6-sol-blind-vs-advice',
    leftArm: 'gpt-5.6-sol-blind',
    rightArm: 'gpt-5.6-sol-advice',
    estimand: 'blind_minus_own_advice' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'gpt-5.6-luna-vs-claude-opus-4-8-blind',
    leftArm: 'gpt-5.6-luna-blind',
    rightArm: 'claude-opus-4-8-blind',
    estimand: 'luna_minus_judge_within_mode' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'gpt-5.6-luna-vs-gpt-5.6-sol-blind',
    leftArm: 'gpt-5.6-luna-blind',
    rightArm: 'gpt-5.6-sol-blind',
    estimand: 'luna_minus_judge_within_mode' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'gpt-5.6-luna-vs-claude-opus-4-8-advice',
    leftArm: 'gpt-5.6-luna-advice',
    rightArm: 'claude-opus-4-8-advice',
    estimand: 'luna_minus_judge_within_mode' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'gpt-5.6-luna-vs-gpt-5.6-sol-advice',
    leftArm: 'gpt-5.6-luna-advice',
    rightArm: 'gpt-5.6-sol-advice',
    estimand: 'luna_minus_judge_within_mode' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'gpt-5.6-luna-blind-vs-gpt-5.6-sol-advice-ceiling',
    leftArm: 'gpt-5.6-luna-blind',
    rightArm: 'gpt-5.6-sol-advice',
    estimand: 'blind_minus_advice_ceiling' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'gpt-6-astra-blind-vs-advice',
    leftArm: 'gpt-6-astra-blind',
    rightArm: 'gpt-6-astra-advice',
    estimand: 'blind_minus_own_advice' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'gpt-5.6-luna-vs-gpt-6-astra-blind',
    leftArm: 'gpt-5.6-luna-blind',
    rightArm: 'gpt-6-astra-blind',
    estimand: 'luna_minus_judge_within_mode' as const,
    eligibleJudgeSeats: ALL_SCORING_SEAT_IDS,
  },
  {
    id: 'gpt-5.6-luna-vs-gpt-6-astra-advice',
    leftArm: 'gpt-5.6-luna-advice',

codex
The prerequisite matches: branch `claude/cohort-01`, HEAD `6887755f…`, and `generatedLineIsOpen` is exactly `tier === 'none' && !blocksSight`. The pre-change V-freeze exited 0; the inventory captured 32 hashes (30 historical fixture files plus the two manifests). I’m now shaping only the required RED witnesses against the existing validator before making production changes.
exec
/bin/bash -lc "rg -n \"structural diff|structuralDiff|diffPaths|foggedCells|first_regeneration|legacy_fog\" tests tools src | head -240
sed -n '1,80p' tests/fixtures/d569-second-family-manifest.json
sed -n '1,45p' tests/fixtures/arena-basis-hard-2/seed-5118001.json
rg -n '\"foggedCells\"' tests/fixtures/arena-basis-hard-2/seed-5118001.json tests/fixtures/arena-basis-brutal-2/seed-6207001.json
rg -n 'test|vitest' package.json" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
src/combat/visibility.ts:93:  readonly foggedCells: readonly GridCell[];
src/combat/visibility.ts:315:    readonly foggedCells: readonly GridCell[];
src/combat/visibility.ts:524:      foggedCells: derivedFogCells(state, visibilityObserverIds),
src/combat/visibility.ts:735:      foggedCells: state.foggedCells.map((cell) => ({ ...cell })),
tools/ai-dm-screenshot-probe.ts:242:  readonly foggedCells: readonly ProbeCell[];
tools/ai-dm-screenshot-probe.ts:313:      readonly foggedCells: readonly ProbeCell[];
tools/ai-dm-screenshot-probe.ts:575:      foggedCells: z.array(cellSchema),
tools/ai-dm-screenshot-probe.ts:968:    foggedCells: orderedCells(board.foggedCells),
tools/ai-dm-screenshot-probe.ts:1053:        foggedCells: sheet.foggedCells,
tools/ai-dm-screenshot-probe.ts:1190:        foggedCells: parsed.foggedCells.map(brandCell),
tools/ai-dm-screenshot-probe.ts:1473:        ...answer.foggedCells.map((entry) =>
tools/ai-dm-screenshot-probe.ts:1857:        foggedCells: array(jsonCellSchema),
tools/ai-dm-screenshot-probe.ts:1958:        foggedCells: answer.foggedCells.map(shiftCell),
tools/ai-dm-screenshot-probe.ts:2521:      return answer.foggedCells.length + answer.obscuredCells.length > 0;
tools/d569-second-family-manifest.ts:469:        'first_regeneration',
tools/ai-dm-blind-board-snapshot-check.ts:113:  requireCondition(dm.fogMarks === board.foggedCells.length, 'DM image fog differs from projection.');
tools/assets/generate-starter-art.ts:115:  return `<svg xmlns="http://www.w3.org/2000/svg" width="2840" height="${String(height)}" viewBox="0 0 2840 ${String(height)}" data-generator="${STARTER_ART_MANIFEST.generator.id}" data-generator-version="${STARTER_ART_MANIFEST.generator.version}"><defs>${definitions}</defs><style>text{font-family:ui-monospace,monospace}.title{font-size:22px;font-weight:700}.heading{font-size:16px;font-weight:700}.label{font-size:9px;text-anchor:middle}image{image-rendering:pixelated}</style><rect width="2840" height="${String(height)}" fill="#11131a"/><text x="28" y="30" class="title" fill="#edf0f7">Starter Pixel Art — deterministic fixture preview</text><g data-sprite-inventory="${String(tokenAssets.length)}">${inventory}</g>${boardPreview('Player', base, 28, boardsY)}${boardPreview('DM', { ...base, foggedCells: [{ column: 8, row: 1 }, { column: 8, row: 2 }] }, 1480, boardsY)}<metadata>pure-procedural-only; cc-by-4.0; focus.active-pc; event.adjudicated; fog.hidden; terrain; one-room; player-projection; dm-projection</metadata></svg>\n`;
src/vtt/semantic-board-payload.ts:52:  | 'foggedCells'
src/vtt/semantic-board-payload.ts:324:  const foggedCells = projectDmView(state).state.foggedCells;
src/vtt/semantic-board-payload.ts:445:      foggedCells: foggedCells.map((cell) => ({ ...cell })),
src/vtt/semantic-board-payload.ts:540:  const foggedCells = board.foggedCells;
src/vtt/semantic-board-payload.ts:654:      fogged: encodedCells(foggedCells),
src/vtt/semantic-board-payload.ts:655:      obscured_or_fogged: encodedCells([...obscuredCells, ...foggedCells]),
src/vtt/encounter-app.ts:2208:    const boardFog = new Set(projection.board.foggedCells.map(
src/vtt/encounter-app.ts:2211:    if (projection.encounter.dmOnly.foggedCells.some(
tests/helpers/database-image-equality.ts:17: * both byte values, which the structural diff did not.
tests/fixtures/arena-basis-hard/seed-5117005.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117005,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":7,"row":0},{"column":7,"row":1},{"column":7,"row":2},{"column":7,"row":3},{"column":7,"row":4},{"column":7,"row":5},{"column":7,"row":6},{"column":7,"row":7},{"column":7,"row":8},{"column":7,"row":9},{"column":7,"row":10},{"column":7,"row":12},{"column":7,"row":13},{"column":7,"row":14},{"column":7,"row":15},{"column":7,"row":16}],"bounds":{"columns":15,"rows":17},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":34,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":13,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":4},{"level":2,"maximum":3,"remaining":2},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":19,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":3},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117005-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117005-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117005-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117005-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117005-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117005-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":32,"life":"living","profile":{"id":"combatant:generated-5117005-monster-4","kind":"monster","name":"Tough","rules":{"abilityScores":{"charisma":11,"constitution":14,"dexterity":12,"intelligence":10,"strength":15,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":32,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":2,"dexterity":1,"intelligence":0,"strength":2,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:tough","tokenId":"token:generated-5117005-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117005-monster-5","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117005-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117005."],"effects":[],"environment":{"difficultTerrainRegions":[{"cells":[{"column":5,"row":10},{"column":5,"row":11},{"column":5,"row":12},{"column":6,"row":10},{"column":6,"row":11},{"column":6,"row":12},{"column":8,"row":10},{"column":8,"row":11},{"column":8,"row":12},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":12}],"id":"difficult:generated-5117005-approach"}],"lightRegions":[{"cells":[{"column":9,"row":5},{"column":10,"row":5},{"column":9,"row":6},{"column":10,"row":6}],"id":"light:world-object:generated-5117005-2","level":"bright"},{"cells":[{"column":3,"row":3},{"column":4,"row":3},{"column":5,"row":3},{"column":6,"row":3},{"column":3,"row":4},{"column":4,"row":4},{"column":5,"row":4},{"column":6,"row":4}],"id":"light:world-object:generated-5117005-3","level":"dim"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":1,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117005-monster-1","id":"token:generated-5117005-monster-1","position":{"column":13,"row":1}},{"combatantId":"combatant:generated-5117005-monster-2","id":"token:generated-5117005-monster-2","position":{"column":12,"row":1}},{"combatantId":"combatant:generated-5117005-monster-3","id":"token:generated-5117005-monster-3","position":{"column":13,"row":3}},{"combatantId":"combatant:generated-5117005-monster-4","id":"token:generated-5117005-monster-4","position":{"column":12,"row":3}},{"combatantId":"combatant:generated-5117005-monster-5","id":"token:generated-5117005-monster-5","position":{"column":13,"row":5}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":6,"row":6}],"id":"world-object:generated-5117005-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":6,"row":6}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":12,"row":7}],"id":"world-object:generated-5117005-2","kind":"light-source","name":"Runed Brazier","position":{"column":12,"row":7}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":7}],"id":"world-object:generated-5117005-3","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":7}}]}},"spec":{"blockedCells":[{"column":7,"row":0},{"column":7,"row":1},{"column":7,"row":2},{"column":7,"row":3},{"column":7,"row":4},{"column":7,"row":5},{"column":7,"row":6},{"column":7,"row":7},{"column":7,"row":8},{"column":7,"row":9},{"column":7,"row":10},{"column":7,"row":12},{"column":7,"row":13},{"column":7,"row":14},{"column":7,"row":15},{"column":7,"row":16}],"challengeBudgetEighths":33,"challengeSpentEighths":29,"difficultyProfile":"hard","dimensions":{"columns":15,"rows":17},"hardFeatures":{"chokepointCells":[{"column":7,"row":11}],"likelyApproachTerrainRegionIds":["difficult:generated-5117005-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117005-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117005-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117005-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117005-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117005-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117005-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117005-monster-4","statblockId":"statblock:tough","tokenId":"token:generated-5117005-monster-4"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117005-monster-5","statblockId":"statblock:scout","tokenId":"token:generated-5117005-monster-5"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":34,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":13,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":4,"spent":0},{"level":2,"maximum":3,"remaining":2,"spent":1},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":19,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":3,"spent":0},{"level":4,"maximum":1,"remaining":1,"spent":0}]}],"seed":5117005,"terrain":[{"cells":[{"column":5,"row":10},{"column":5,"row":11},{"column":5,"row":12},{"column":6,"row":10},{"column":6,"row":11},{"column":6,"row":12},{"column":8,"row":10},{"column":8,"row":11},{"column":8,"row":12},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":12}],"id":"difficult:generated-5117005-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":6,"row":6}],"id":"world-object:generated-5117005-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":6,"row":6}}},{"cells":[{"column":9,"row":5},{"column":10,"row":5},{"column":9,"row":6},{"column":10,"row":6}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":12,"row":7}],"id":"world-object:generated-5117005-2","kind":"light-source","name":"Runed Brazier","position":{"column":12,"row":7}}},{"cells":[{"column":3,"row":3},{"column":4,"row":3},{"column":5,"row":3},{"column":6,"row":3},{"column":3,"row":4},{"column":4,"row":4},{"column":5,"row":4},{"column":6,"row":4}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":7}],"id":"world-object:generated-5117005-3","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":7}}}]}}
tests/fixtures/arena-basis-hard/seed-5117003.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117003,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":7,"row":0},{"column":7,"row":1},{"column":7,"row":2},{"column":7,"row":3},{"column":7,"row":4},{"column":7,"row":5},{"column":7,"row":6},{"column":7,"row":7},{"column":7,"row":8},{"column":7,"row":9},{"column":7,"row":10},{"column":7,"row":11},{"column":7,"row":13},{"column":7,"row":14},{"column":7,"row":15},{"column":7,"row":16},{"column":7,"row":17},{"column":7,"row":18},{"column":7,"row":19},{"column":7,"row":20},{"column":7,"row":21},{"column":7,"row":22}],"bounds":{"columns":14,"rows":23},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":17,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":13,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":4},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":0},{"level":2,"maximum":3,"remaining":2},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117003-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117003-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117003-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117003-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117003-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117003-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":32,"life":"living","profile":{"id":"combatant:generated-5117003-monster-4","kind":"monster","name":"Tough","rules":{"abilityScores":{"charisma":11,"constitution":14,"dexterity":12,"intelligence":10,"strength":15,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":32,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":2,"dexterity":1,"intelligence":0,"strength":2,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:tough","tokenId":"token:generated-5117003-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117003-monster-5","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117003-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117003."],"effects":[{"concentrationOwner":"combatant:cleric","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:cleric","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:cleric"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":5,"row":11},{"column":5,"row":12},{"column":5,"row":13},{"column":6,"row":11},{"column":6,"row":12},{"column":6,"row":13},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":9,"row":11},{"column":9,"row":12},{"column":9,"row":13}],"id":"difficult:generated-5117003-approach"}],"lightRegions":[{"cells":[{"column":8,"row":4},{"column":9,"row":4},{"column":8,"row":5},{"column":9,"row":5},{"column":8,"row":6},{"column":9,"row":6},{"column":8,"row":7},{"column":9,"row":7}],"id":"light:world-object:generated-5117003-3","level":"bright"},{"cells":[{"column":8,"row":9},{"column":9,"row":9},{"column":10,"row":9},{"column":8,"row":10},{"column":9,"row":10},{"column":10,"row":10},{"column":10,"row":11},{"column":10,"row":12}],"id":"light:world-object:generated-5117003-4","level":"bright"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":2,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117003-monster-1","id":"token:generated-5117003-monster-1","position":{"column":12,"row":1}},{"combatantId":"combatant:generated-5117003-monster-2","id":"token:generated-5117003-monster-2","position":{"column":11,"row":1}},{"combatantId":"combatant:generated-5117003-monster-3","id":"token:generated-5117003-monster-3","position":{"column":12,"row":3}},{"combatantId":"combatant:generated-5117003-monster-4","id":"token:generated-5117003-monster-4","position":{"column":11,"row":3}},{"combatantId":"combatant:generated-5117003-monster-5","id":"token:generated-5117003-monster-5","position":{"column":12,"row":5}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":18}],"id":"world-object:generated-5117003-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":18}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":15}],"id":"world-object:generated-5117003-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":15}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":20}],"id":"world-object:generated-5117003-3","kind":"light-source","name":"Runed Brazier","position":{"column":5,"row":20}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":15}],"id":"world-object:generated-5117003-4","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":15}}]}},"spec":{"blockedCells":[{"column":7,"row":0},{"column":7,"row":1},{"column":7,"row":2},{"column":7,"row":3},{"column":7,"row":4},{"column":7,"row":5},{"column":7,"row":6},{"column":7,"row":7},{"column":7,"row":8},{"column":7,"row":9},{"column":7,"row":10},{"column":7,"row":11},{"column":7,"row":13},{"column":7,"row":14},{"column":7,"row":15},{"column":7,"row":16},{"column":7,"row":17},{"column":7,"row":18},{"column":7,"row":19},{"column":7,"row":20},{"column":7,"row":21},{"column":7,"row":22}],"challengeBudgetEighths":31,"challengeSpentEighths":26,"difficultyProfile":"hard","dimensions":{"columns":14,"rows":23},"hardFeatures":{"chokepointCells":[{"column":7,"row":12}],"likelyApproachTerrainRegionIds":["difficult:generated-5117003-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117003-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117003-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117003-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117003-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117003-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117003-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117003-monster-4","statblockId":"statblock:tough","tokenId":"token:generated-5117003-monster-4"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117003-monster-5","statblockId":"statblock:guard","tokenId":"token:generated-5117003-monster-5"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":17,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":true,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":13,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":4,"spent":0},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":38,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":0,"spent":4},{"level":2,"maximum":3,"remaining":2,"spent":1},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":1,"spent":0}]}],"seed":5117003,"terrain":[{"cells":[{"column":5,"row":11},{"column":5,"row":12},{"column":5,"row":13},{"column":6,"row":11},{"column":6,"row":12},{"column":6,"row":13},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":9,"row":11},{"column":9,"row":12},{"column":9,"row":13}],"id":"difficult:generated-5117003-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":18}],"id":"world-object:generated-5117003-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":18}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":15}],"id":"world-object:generated-5117003-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":15}}},{"cells":[{"column":8,"row":4},{"column":9,"row":4},{"column":8,"row":5},{"column":9,"row":5},{"column":8,"row":6},{"column":9,"row":6},{"column":8,"row":7},{"column":9,"row":7}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":20}],"id":"world-object:generated-5117003-3","kind":"light-source","name":"Runed Brazier","position":{"column":5,"row":20}}},{"cells":[{"column":8,"row":9},{"column":9,"row":9},{"column":10,"row":9},{"column":8,"row":10},{"column":9,"row":10},{"column":10,"row":10},{"column":10,"row":11},{"column":10,"row":12}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":15}],"id":"world-object:generated-5117003-4","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":15}}}]}}
tests/fixtures/arena-basis-hard/seed-5117002.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117002,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":8,"row":0},{"column":8,"row":1},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":7},{"column":8,"row":8},{"column":8,"row":9},{"column":8,"row":10},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":8,"row":14},{"column":8,"row":15},{"column":8,"row":16},{"column":8,"row":17},{"column":8,"row":18},{"column":8,"row":19}],"bounds":{"columns":16,"rows":20},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":17,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":39,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":1},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":10,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":1},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117002-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117002-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117002-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117002-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117002-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117002-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":32,"life":"living","profile":{"id":"combatant:generated-5117002-monster-4","kind":"monster","name":"Tough","rules":{"abilityScores":{"charisma":11,"constitution":14,"dexterity":12,"intelligence":10,"strength":15,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":32,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":2,"dexterity":1,"intelligence":0,"strength":2,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:tough","tokenId":"token:generated-5117002-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117002-monster-5","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117002-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117002-monster-6","kind":"monster","name":"Bandit","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":11,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":0,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:bandit","tokenId":"token:generated-5117002-monster-6"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117002-monster-7","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117002-monster-7"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117002."],"effects":[],"environment":{"difficultTerrainRegions":[{"cells":[{"column":6,"row":5},{"column":6,"row":6},{"column":6,"row":7},{"column":7,"row":5},{"column":7,"row":6},{"column":7,"row":7},{"column":9,"row":5},{"column":9,"row":6},{"column":9,"row":7},{"column":10,"row":5},{"column":10,"row":6},{"column":10,"row":7}],"id":"difficult:generated-5117002-approach"}],"lightRegions":[{"cells":[{"column":6,"row":15},{"column":7,"row":15},{"column":9,"row":15},{"column":6,"row":16},{"column":7,"row":16},{"column":9,"row":16},{"column":6,"row":17},{"column":7,"row":17},{"column":9,"row":17},{"column":6,"row":18},{"column":7,"row":18},{"column":9,"row":18}],"id":"light:world-object:generated-5117002-2","level":"dim"},{"cells":[{"column":7,"row":16},{"column":7,"row":17}],"id":"light:world-object:generated-5117002-3","level":"bright"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":1,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117002-monster-1","id":"token:generated-5117002-monster-1","position":{"column":14,"row":1}},{"combatantId":"combatant:generated-5117002-monster-2","id":"token:generated-5117002-monster-2","position":{"column":13,"row":1}},{"combatantId":"combatant:generated-5117002-monster-3","id":"token:generated-5117002-monster-3","position":{"column":14,"row":3}},{"combatantId":"combatant:generated-5117002-monster-4","id":"token:generated-5117002-monster-4","position":{"column":13,"row":3}},{"combatantId":"combatant:generated-5117002-monster-5","id":"token:generated-5117002-monster-5","position":{"column":14,"row":5}},{"combatantId":"combatant:generated-5117002-monster-6","id":"token:generated-5117002-monster-6","position":{"column":13,"row":5}},{"combatantId":"combatant:generated-5117002-monster-7","id":"token:generated-5117002-monster-7","position":{"column":14,"row":7}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":12,"row":2}],"id":"world-object:generated-5117002-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":12,"row":2}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":2,"row":13}],"id":"world-object:generated-5117002-2","kind":"light-source","name":"Runed Brazier","position":{"column":2,"row":13}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":13}],"id":"world-object:generated-5117002-3","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":13}}]}},"spec":{"blockedCells":[{"column":8,"row":0},{"column":8,"row":1},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":7},{"column":8,"row":8},{"column":8,"row":9},{"column":8,"row":10},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":8,"row":14},{"column":8,"row":15},{"column":8,"row":16},{"column":8,"row":17},{"column":8,"row":18},{"column":8,"row":19}],"challengeBudgetEighths":28,"challengeSpentEighths":28,"difficultyProfile":"hard","dimensions":{"columns":16,"rows":20},"hardFeatures":{"chokepointCells":[{"column":8,"row":6}],"likelyApproachTerrainRegionIds":["difficult:generated-5117002-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117002-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117002-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117002-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117002-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117002-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117002-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117002-monster-4","statblockId":"statblock:tough","tokenId":"token:generated-5117002-monster-4"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117002-monster-5","statblockId":"statblock:guard","tokenId":"token:generated-5117002-monster-5"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117002-monster-6","statblockId":"statblock:bandit","tokenId":"token:generated-5117002-monster-6"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117002-monster-7","statblockId":"statblock:guard","tokenId":"token:generated-5117002-monster-7"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":17,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.75,"hitPoints":39,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":1,"spent":3},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":1,"spent":0}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":10,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":1,"spent":3},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":5117002,"terrain":[{"cells":[{"column":6,"row":5},{"column":6,"row":6},{"column":6,"row":7},{"column":7,"row":5},{"column":7,"row":6},{"column":7,"row":7},{"column":9,"row":5},{"column":9,"row":6},{"column":9,"row":7},{"column":10,"row":5},{"column":10,"row":6},{"column":10,"row":7}],"id":"difficult:generated-5117002-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":12,"row":2}],"id":"world-object:generated-5117002-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":12,"row":2}}},{"cells":[{"column":6,"row":15},{"column":7,"row":15},{"column":9,"row":15},{"column":6,"row":16},{"column":7,"row":16},{"column":9,"row":16},{"column":6,"row":17},{"column":7,"row":17},{"column":9,"row":17},{"column":6,"row":18},{"column":7,"row":18},{"column":9,"row":18}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":2,"row":13}],"id":"world-object:generated-5117002-2","kind":"light-source","name":"Runed Brazier","position":{"column":2,"row":13}}},{"cells":[{"column":7,"row":16},{"column":7,"row":17}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":13}],"id":"world-object:generated-5117002-3","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":13}}}]}}
tests/fixtures/arena-basis-hard/seed-5117009.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117009,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":10,"row":0},{"column":10,"row":1},{"column":10,"row":2},{"column":10,"row":3},{"column":10,"row":5},{"column":10,"row":6},{"column":10,"row":7},{"column":10,"row":8},{"column":10,"row":9},{"column":10,"row":10},{"column":10,"row":11},{"column":10,"row":12},{"column":10,"row":13},{"column":10,"row":14},{"column":10,"row":15},{"column":10,"row":16},{"column":10,"row":17},{"column":10,"row":18}],"bounds":{"columns":21,"rows":19},"combatants":[{"deathAt":null,"deathSaves":{"failures":0,"successes":1},"hitPoints":0,"life":"dying","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":39,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":1},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":19,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117009-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117009-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117009-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117009-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117009-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117009-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117009-monster-4","kind":"monster","name":"Bandit","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":11,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":0,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:bandit","tokenId":"token:generated-5117009-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117009-monster-5","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117009-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117009-monster-6","kind":"monster","name":"Bandit","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":11,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":0,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:bandit","tokenId":"token:generated-5117009-monster-6"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117009."],"effects":[{"concentrationOwner":"combatant:wizard","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:wizard","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:wizard"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":11,"row":3},{"column":11,"row":4},{"column":11,"row":5},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5}],"id":"difficult:generated-5117009-approach"}],"lightRegions":[{"cells":[{"column":13,"row":3},{"column":13,"row":4},{"column":13,"row":5}],"id":"light:world-object:generated-5117009-3","level":"dim"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":2,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117009-monster-1","id":"token:generated-5117009-monster-1","position":{"column":19,"row":1}},{"combatantId":"combatant:generated-5117009-monster-2","id":"token:generated-5117009-monster-2","position":{"column":18,"row":1}},{"combatantId":"combatant:generated-5117009-monster-3","id":"token:generated-5117009-monster-3","position":{"column":19,"row":3}},{"combatantId":"combatant:generated-5117009-monster-4","id":"token:generated-5117009-monster-4","position":{"column":18,"row":3}},{"combatantId":"combatant:generated-5117009-monster-5","id":"token:generated-5117009-monster-5","position":{"column":19,"row":5}},{"combatantId":"combatant:generated-5117009-monster-6","id":"token:generated-5117009-monster-6","position":{"column":18,"row":5}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":5}],"id":"world-object:generated-5117009-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":1,"row":5}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":16}],"id":"world-object:generated-5117009-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":16}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":18,"row":15}],"id":"world-object:generated-5117009-3","kind":"light-source","name":"Runed Brazier","position":{"column":18,"row":15}}]}},"spec":{"blockedCells":[{"column":10,"row":0},{"column":10,"row":1},{"column":10,"row":2},{"column":10,"row":3},{"column":10,"row":5},{"column":10,"row":6},{"column":10,"row":7},{"column":10,"row":8},{"column":10,"row":9},{"column":10,"row":10},{"column":10,"row":11},{"column":10,"row":12},{"column":10,"row":13},{"column":10,"row":14},{"column":10,"row":15},{"column":10,"row":16},{"column":10,"row":17},{"column":10,"row":18}],"challengeBudgetEighths":30,"challengeSpentEighths":27,"difficultyProfile":"hard","dimensions":{"columns":21,"rows":19},"hardFeatures":{"chokepointCells":[{"column":10,"row":4}],"likelyApproachTerrainRegionIds":["difficult:generated-5117009-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117009-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117009-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117009-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117009-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117009-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117009-monster-3"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117009-monster-4","statblockId":"statblock:bandit","tokenId":"token:generated-5117009-monster-4"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117009-monster-5","statblockId":"statblock:scout","tokenId":"token:generated-5117009-monster-5"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117009-monster-6","statblockId":"statblock:bandit","tokenId":"token:generated-5117009-monster-6"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":{"failures":0,"successes":1},"hitPointFraction":0,"hitPoints":0,"life":"dying","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.75,"hitPoints":39,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":1,"spent":2},{"level":4,"maximum":1,"remaining":1,"spent":0}]},{"combatantId":"combatant:wizard","concentrating":true,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":19,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":5117009,"terrain":[{"cells":[{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":11,"row":3},{"column":11,"row":4},{"column":11,"row":5},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5}],"id":"difficult:generated-5117009-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":5}],"id":"world-object:generated-5117009-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":1,"row":5}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":16}],"id":"world-object:generated-5117009-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":16}}},{"cells":[{"column":13,"row":3},{"column":13,"row":4},{"column":13,"row":5}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":18,"row":15}],"id":"world-object:generated-5117009-3","kind":"light-source","name":"Runed Brazier","position":{"column":18,"row":15}}}]}}
tests/fixtures/arena-basis-hard/seed-5117007.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117007,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":8,"row":0},{"column":8,"row":1},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":6},{"column":8,"row":7},{"column":8,"row":8},{"column":8,"row":9},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":8,"row":14},{"column":8,"row":15},{"column":8,"row":16},{"column":8,"row":17},{"column":8,"row":18},{"column":8,"row":19}],"bounds":{"columns":17,"rows":20},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":17,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":13,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":0},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":3},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":4},{"level":2,"maximum":3,"remaining":2},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117007-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117007-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117007-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117007-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117007-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117007-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117007-monster-4","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117007-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117007-monster-5","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117007-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117007-monster-6","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117007-monster-6"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":32,"life":"living","profile":{"id":"combatant:generated-5117007-monster-7","kind":"monster","name":"Tough","rules":{"abilityScores":{"charisma":11,"constitution":14,"dexterity":12,"intelligence":10,"strength":15,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":32,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":2,"dexterity":1,"intelligence":0,"strength":2,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:tough","tokenId":"token:generated-5117007-monster-7"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117007."],"effects":[{"concentrationOwner":"combatant:wizard","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:wizard","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:wizard"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":6,"row":9},{"column":6,"row":10},{"column":6,"row":11},{"column":7,"row":9},{"column":7,"row":10},{"column":7,"row":11},{"column":9,"row":9},{"column":9,"row":10},{"column":9,"row":11},{"column":10,"row":9},{"column":10,"row":10},{"column":10,"row":11}],"id":"difficult:generated-5117007-approach"}],"lightRegions":[{"cells":[{"column":4,"row":10},{"column":5,"row":10},{"column":4,"row":11},{"column":5,"row":11}],"id":"light:world-object:generated-5117007-2","level":"bright"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":2,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117007-monster-1","id":"token:generated-5117007-monster-1","position":{"column":15,"row":1}},{"combatantId":"combatant:generated-5117007-monster-2","id":"token:generated-5117007-monster-2","position":{"column":14,"row":1}},{"combatantId":"combatant:generated-5117007-monster-3","id":"token:generated-5117007-monster-3","position":{"column":15,"row":3}},{"combatantId":"combatant:generated-5117007-monster-4","id":"token:generated-5117007-monster-4","position":{"column":14,"row":3}},{"combatantId":"combatant:generated-5117007-monster-5","id":"token:generated-5117007-monster-5","position":{"column":15,"row":5}},{"combatantId":"combatant:generated-5117007-monster-6","id":"token:generated-5117007-monster-6","position":{"column":14,"row":5}},{"combatantId":"combatant:generated-5117007-monster-7","id":"token:generated-5117007-monster-7","position":{"column":15,"row":7}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":6}],"id":"world-object:generated-5117007-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":13,"row":6}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":8}],"id":"world-object:generated-5117007-2","kind":"light-source","name":"Runed Brazier","position":{"column":3,"row":8}}]}},"spec":{"blockedCells":[{"column":8,"row":0},{"column":8,"row":1},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":6},{"column":8,"row":7},{"column":8,"row":8},{"column":8,"row":9},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":8,"row":14},{"column":8,"row":15},{"column":8,"row":16},{"column":8,"row":17},{"column":8,"row":18},{"column":8,"row":19}],"challengeBudgetEighths":43,"challengeSpentEighths":37,"difficultyProfile":"hard","dimensions":{"columns":17,"rows":20},"hardFeatures":{"chokepointCells":[{"column":8,"row":10}],"likelyApproachTerrainRegionIds":["difficult:generated-5117007-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117007-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117007-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117007-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117007-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117007-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117007-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117007-monster-4","statblockId":"statblock:scout","tokenId":"token:generated-5117007-monster-4"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117007-monster-5","statblockId":"statblock:scout","tokenId":"token:generated-5117007-monster-5"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117007-monster-6","statblockId":"statblock:scout","tokenId":"token:generated-5117007-monster-6"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117007-monster-7","statblockId":"statblock:tough","tokenId":"token:generated-5117007-monster-7"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":17,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":13,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":0,"spent":4},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":3,"spent":0},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":true,"deathSaves":null,"hitPointFraction":1,"hitPoints":38,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":4,"spent":0},{"level":2,"maximum":3,"remaining":2,"spent":1},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":5117007,"terrain":[{"cells":[{"column":6,"row":9},{"column":6,"row":10},{"column":6,"row":11},{"column":7,"row":9},{"column":7,"row":10},{"column":7,"row":11},{"column":9,"row":9},{"column":9,"row":10},{"column":9,"row":11},{"column":10,"row":9},{"column":10,"row":10},{"column":10,"row":11}],"id":"difficult:generated-5117007-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":6}],"id":"world-object:generated-5117007-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":13,"row":6}}},{"cells":[{"column":4,"row":10},{"column":5,"row":10},{"column":4,"row":11},{"column":5,"row":11}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":8}],"id":"world-object:generated-5117007-2","kind":"light-source","name":"Runed Brazier","position":{"column":3,"row":8}}}]}}
tests/fixtures/arena-basis-brutal-2/seed-6207010.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":6207010,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"adjudicationPending":[],"blockedCells":[{"column":7,"row":0},{"column":7,"row":1},{"column":7,"row":2},{"column":7,"row":3},{"column":7,"row":5},{"column":7,"row":6},{"column":7,"row":7},{"column":7,"row":8},{"column":7,"row":9},{"column":7,"row":10},{"column":7,"row":11},{"column":7,"row":12},{"column":7,"row":13},{"column":7,"row":14},{"column":7,"row":15},{"column":7,"row":16}],"bounds":{"columns":14,"rows":17},"combatants":[{"deathAt":null,"deathSaves":{"failures":2,"successes":0},"hitPoints":0,"life":"dying","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":52,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":0},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":1},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":29,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":1},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":3},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","limitedResources":[{"id":"monster-spell:divine-aid:bless","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:healing-word","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:sanctuary","maximum":1,"recharge":"long_rest","remaining":1}],"profile":{"id":"combatant:generated-6207010-monster-1","kind":"monster","name":"Priest Acolyte","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":10,"intelligence":10,"strength":14,"wisdom":14},"armorClass":13,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:divine-aid:bless","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:sanctuary","maximum":1,"recharge":"long_rest"}],"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":0,"intelligence":0,"strength":2,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","skillBonuses":{"medicine":4,"religion":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest-acolyte","tokenId":"token:generated-6207010-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":82,"life":"living","limitedResources":[{"id":"monster-spell:war-prayers:command","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:war-prayers:hold-person","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:cinder-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-6207010-monster-2","kind":"monster","name":"Ashmaw, Cinder Votary","rules":{"abilityScores":{"charisma":11,"constitution":15,"dexterity":12,"intelligence":10,"strength":17,"wisdom":16},"armorClass":15,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Fey","damageResponses":[],"detectionTraits":[],"hitPointMaximum":82,"initiativeBonus":1,"limitedResources":[{"id":"monster-spell:war-prayers:command","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:war-prayers:hold-person","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:cinder-aid:healing-word","maximum":3,"recharge":"long_rest"}],"passivePerception":13,"proficiencyBonus":2,"reach":10,"savingThrowBonuses":{"charisma":0,"constitution":4,"dexterity":1,"intelligence":0,"strength":5,"wisdom":5},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Medium","skillBonuses":{"medicine":5,"religion":4},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:vane-warren/ashmaw-brute-priest","tokenId":"token:generated-6207010-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":90,"life":"living","profile":{"id":"combatant:generated-6207010-monster-3","kind":"monster","name":"Archelon","rules":{"abilityScores":{"charisma":6,"constitution":13,"dexterity":16,"intelligence":4,"strength":18,"wisdom":14},"armorClass":17,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":90,"initiativeBonus":3,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":3,"intelligence":-3,"strength":4,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Huge","skillBonuses":{"stealth":5},"speed":20,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:archelon","tokenId":"token:generated-6207010-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 6207010."],"effects":[],"environment":{"difficultTerrainRegions":[{"cells":[{"column":5,"row":3},{"column":5,"row":4},{"column":5,"row":5},{"column":6,"row":3},{"column":6,"row":4},{"column":6,"row":5},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5}],"id":"difficult:generated-6207010-approach"},{"cells":[{"column":9,"row":11},{"column":10,"row":11},{"column":9,"row":12},{"column":10,"row":12},{"column":9,"row":13},{"column":10,"row":13},{"column":9,"row":14},{"column":10,"row":14}],"id":"difficult:generated-6207010-brutal-1"},{"cells":[{"column":4,"row":2},{"column":5,"row":2},{"column":6,"row":2},{"column":4,"row":3},{"column":4,"row":4},{"column":7,"row":4},{"column":4,"row":5}],"id":"difficult:generated-6207010-brutal-2"}],"lightRegions":[{"cells":[{"column":6,"row":7},{"column":8,"row":7},{"column":9,"row":7},{"column":6,"row":8},{"column":8,"row":8},{"column":9,"row":8},{"column":6,"row":9},{"column":8,"row":9},{"column":9,"row":9}],"id":"light:world-object:generated-6207010-4","level":"dim"},{"cells":[{"column":3,"row":13},{"column":4,"row":13},{"column":5,"row":13},{"column":6,"row":13},{"column":3,"row":14},{"column":4,"row":14},{"column":5,"row":14},{"column":6,"row":14}],"id":"light:world-object:generated-6207010-5","level":"dim"},{"cells":[{"column":8,"row":11},{"column":9,"row":11},{"column":10,"row":11},{"column":8,"row":12},{"column":9,"row":12},{"column":10,"row":12}],"id":"light:world-object:generated-6207010-6","level":"dim"}],"movementRegions":[],"narrowOpeningRegions":[],"obscurementRegions":[{"cells":[{"column":9,"row":14},{"column":10,"row":14},{"column":9,"row":15},{"column":10,"row":15}],"id":"obscurement:generated-6207010-1","obscurement":"heavy"},{"cells":[{"column":3,"row":5},{"column":3,"row":6},{"column":4,"row":6},{"column":5,"row":6},{"column":6,"row":6}],"id":"obscurement:generated-6207010-2","obscurement":"light"},{"cells":[{"column":8,"row":6},{"column":9,"row":6},{"column":8,"row":7},{"column":9,"row":7},{"column":8,"row":8},{"column":9,"row":8}],"id":"obscurement:generated-6207010-3","obscurement":"light"}]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":1,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"observationHistory":[],"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","sharedSpaceRelations":[],"tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":6}},{"combatantId":"combatant:generated-6207010-monster-1","id":"token:generated-6207010-monster-1","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":12,"row":1}},{"combatantId":"combatant:generated-6207010-monster-2","id":"token:generated-6207010-monster-2","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":11,"row":1}},{"combatantId":"combatant:generated-6207010-monster-3","id":"token:generated-6207010-monster-3","placementMode":{"actual":"Huge","kind":"normal"},"position":{"column":11,"row":2}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":7}],"id":"world-object:generated-6207010-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":7}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":8,"row":13}],"id":"world-object:generated-6207010-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":8,"row":13}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":10}],"id":"world-object:generated-6207010-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":10}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":3}],"id":"world-object:generated-6207010-4","kind":"light-source","name":"Runed Brazier","position":{"column":3,"row":3}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":2}],"id":"world-object:generated-6207010-5","kind":"light-source","name":"Runed Brazier","position":{"column":4,"row":2}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":5}],"id":"world-object:generated-6207010-6","kind":"light-source","name":"Runed Brazier","position":{"column":4,"row":5}}]}},"spec":{"blockedCells":[{"column":7,"row":0},{"column":7,"row":1},{"column":7,"row":2},{"column":7,"row":3},{"column":7,"row":5},{"column":7,"row":6},{"column":7,"row":7},{"column":7,"row":8},{"column":7,"row":9},{"column":7,"row":10},{"column":7,"row":11},{"column":7,"row":12},{"column":7,"row":13},{"column":7,"row":14},{"column":7,"row":15},{"column":7,"row":16}],"challengeBudgetEighths":66,"challengeSpentEighths":66,"difficultyProfile":"brutal","dimensions":{"columns":14,"rows":17},"hardFeatures":{"chokepointCells":[{"column":7,"row":4}],"likelyApproachTerrainRegionIds":["difficult:generated-6207010-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":2,"challengeRating":"1/4","combatantId":"combatant:generated-6207010-monster-1","statblockId":"statblock:priest-acolyte","tokenId":"token:generated-6207010-monster-1"},{"challengeEighths":32,"challengeRating":4,"combatantId":"combatant:generated-6207010-monster-2","statblockId":"statblock:vane-warren/ashmaw-brute-priest","tokenId":"token:generated-6207010-monster-2"},{"challengeEighths":32,"challengeRating":4,"combatantId":"combatant:generated-6207010-monster-3","statblockId":"statblock:archelon","tokenId":"token:generated-6207010-monster-3"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":{"failures":2,"successes":0},"hitPointFraction":0,"hitPoints":0,"life":"dying","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":52,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":0,"spent":4},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":1,"spent":2},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":0.75,"hitPoints":29,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":1,"spent":3},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":3,"spent":0},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":6207010,"terrain":[{"cells":[{"column":5,"row":3},{"column":5,"row":4},{"column":5,"row":5},{"column":6,"row":3},{"column":6,"row":4},{"column":6,"row":5},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5}],"id":"difficult:generated-6207010-approach","kind":"difficult-terrain-patch"},{"cells":[{"column":9,"row":11},{"column":10,"row":11},{"column":9,"row":12},{"column":10,"row":12},{"column":9,"row":13},{"column":10,"row":13},{"column":9,"row":14},{"column":10,"row":14}],"id":"difficult:generated-6207010-brutal-1","kind":"difficult-terrain-patch"},{"cells":[{"column":4,"row":2},{"column":5,"row":2},{"column":6,"row":2},{"column":4,"row":3},{"column":4,"row":4},{"column":7,"row":4},{"column":4,"row":5}],"id":"difficult:generated-6207010-brutal-2","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":7}],"id":"world-object:generated-6207010-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":7}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":8,"row":13}],"id":"world-object:generated-6207010-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":8,"row":13}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":10}],"id":"world-object:generated-6207010-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":10}}},{"cells":[{"column":6,"row":7},{"column":8,"row":7},{"column":9,"row":7},{"column":6,"row":8},{"column":8,"row":8},{"column":9,"row":8},{"column":6,"row":9},{"column":8,"row":9},{"column":9,"row":9}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":3}],"id":"world-object:generated-6207010-4","kind":"light-source","name":"Runed Brazier","position":{"column":3,"row":3}}},{"cells":[{"column":3,"row":13},{"column":4,"row":13},{"column":5,"row":13},{"column":6,"row":13},{"column":3,"row":14},{"column":4,"row":14},{"column":5,"row":14},{"column":6,"row":14}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":2}],"id":"world-object:generated-6207010-5","kind":"light-source","name":"Runed Brazier","position":{"column":4,"row":2}}},{"cells":[{"column":8,"row":11},{"column":9,"row":11},{"column":10,"row":11},{"column":8,"row":12},{"column":9,"row":12},{"column":10,"row":12}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":5}],"id":"world-object:generated-6207010-6","kind":"light-source","name":"Runed Brazier","position":{"column":4,"row":5}}},{"cells":[{"column":9,"row":14},{"column":10,"row":14},{"column":9,"row":15},{"column":10,"row":15}],"id":"obscurement:generated-6207010-1","kind":"obscurement-patch","obscurement":"heavy"},{"cells":[{"column":3,"row":5},{"column":3,"row":6},{"column":4,"row":6},{"column":5,"row":6},{"column":6,"row":6}],"id":"obscurement:generated-6207010-2","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":8,"row":6},{"column":9,"row":6},{"column":8,"row":7},{"column":9,"row":7},{"column":8,"row":8},{"column":9,"row":8}],"id":"obscurement:generated-6207010-3","kind":"obscurement-patch","obscurement":"light"}]}}
tests/fixtures/arena-basis-hard/seed-5117006.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117006,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":11,"row":0},{"column":11,"row":1},{"column":11,"row":2},{"column":11,"row":3},{"column":11,"row":5},{"column":11,"row":6},{"column":11,"row":7},{"column":11,"row":8},{"column":11,"row":9},{"column":11,"row":10},{"column":11,"row":11},{"column":11,"row":12},{"column":11,"row":13},{"column":11,"row":14}],"bounds":{"columns":22,"rows":15},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":67,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":39,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":10,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":1},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117006-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117006-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117006-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117006-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117006-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117006-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117006-monster-4","kind":"monster","name":"Bandit","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":11,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":0,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:bandit","tokenId":"token:generated-5117006-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117006-monster-5","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117006-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117006."],"effects":[{"concentrationOwner":"combatant:wizard","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:wizard","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:wizard"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":10,"row":3},{"column":10,"row":4},{"column":10,"row":5},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5},{"column":13,"row":3},{"column":13,"row":4},{"column":13,"row":5}],"id":"difficult:generated-5117006-approach"}],"lightRegions":[{"cells":[{"column":14,"row":6},{"column":15,"row":6},{"column":16,"row":6},{"column":17,"row":6},{"column":14,"row":7},{"column":15,"row":7},{"column":16,"row":7},{"column":17,"row":7},{"column":14,"row":8},{"column":15,"row":8},{"column":16,"row":8},{"column":17,"row":8},{"column":14,"row":9},{"column":15,"row":9},{"column":16,"row":9}],"id":"light:world-object:generated-5117006-2","level":"dim"},{"cells":[{"column":14,"row":5},{"column":15,"row":5},{"column":16,"row":5},{"column":17,"row":5},{"column":14,"row":6},{"column":15,"row":6},{"column":16,"row":6},{"column":17,"row":6},{"column":14,"row":7},{"column":15,"row":7},{"column":16,"row":7},{"column":17,"row":7},{"column":14,"row":8},{"column":15,"row":8},{"column":16,"row":8},{"column":17,"row":8}],"id":"light:world-object:generated-5117006-3","level":"bright"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":2,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117006-monster-1","id":"token:generated-5117006-monster-1","position":{"column":20,"row":1}},{"combatantId":"combatant:generated-5117006-monster-2","id":"token:generated-5117006-monster-2","position":{"column":19,"row":1}},{"combatantId":"combatant:generated-5117006-monster-3","id":"token:generated-5117006-monster-3","position":{"column":20,"row":3}},{"combatantId":"combatant:generated-5117006-monster-4","id":"token:generated-5117006-monster-4","position":{"column":19,"row":3}},{"combatantId":"combatant:generated-5117006-monster-5","id":"token:generated-5117006-monster-5","position":{"column":20,"row":5}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":17,"row":9}],"id":"world-object:generated-5117006-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":17,"row":9}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":3}],"id":"world-object:generated-5117006-2","kind":"light-source","name":"Runed Brazier","position":{"column":1,"row":3}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":16,"row":10}],"id":"world-object:generated-5117006-3","kind":"light-source","name":"Runed Brazier","position":{"column":16,"row":10}}]}},"spec":{"blockedCells":[{"column":11,"row":0},{"column":11,"row":1},{"column":11,"row":2},{"column":11,"row":3},{"column":11,"row":5},{"column":11,"row":6},{"column":11,"row":7},{"column":11,"row":8},{"column":11,"row":9},{"column":11,"row":10},{"column":11,"row":11},{"column":11,"row":12},{"column":11,"row":13},{"column":11,"row":14}],"challengeBudgetEighths":30,"challengeSpentEighths":26,"difficultyProfile":"hard","dimensions":{"columns":22,"rows":15},"hardFeatures":{"chokepointCells":[{"column":11,"row":4}],"likelyApproachTerrainRegionIds":["difficult:generated-5117006-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117006-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117006-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117006-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117006-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117006-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117006-monster-3"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117006-monster-4","statblockId":"statblock:bandit","tokenId":"token:generated-5117006-monster-4"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117006-monster-5","statblockId":"statblock:scout","tokenId":"token:generated-5117006-monster-5"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":67,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.75,"hitPoints":39,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":1,"spent":0}]},{"combatantId":"combatant:wizard","concentrating":true,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":10,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":1,"spent":3},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":5117006,"terrain":[{"cells":[{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":10,"row":3},{"column":10,"row":4},{"column":10,"row":5},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5},{"column":13,"row":3},{"column":13,"row":4},{"column":13,"row":5}],"id":"difficult:generated-5117006-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":17,"row":9}],"id":"world-object:generated-5117006-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":17,"row":9}}},{"cells":[{"column":14,"row":6},{"column":15,"row":6},{"column":16,"row":6},{"column":17,"row":6},{"column":14,"row":7},{"column":15,"row":7},{"column":16,"row":7},{"column":17,"row":7},{"column":14,"row":8},{"column":15,"row":8},{"column":16,"row":8},{"column":17,"row":8},{"column":14,"row":9},{"column":15,"row":9},{"column":16,"row":9}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":3}],"id":"world-object:generated-5117006-2","kind":"light-source","name":"Runed Brazier","position":{"column":1,"row":3}}},{"cells":[{"column":14,"row":5},{"column":15,"row":5},{"column":16,"row":5},{"column":17,"row":5},{"column":14,"row":6},{"column":15,"row":6},{"column":16,"row":6},{"column":17,"row":6},{"column":14,"row":7},{"column":15,"row":7},{"column":16,"row":7},{"column":17,"row":7},{"column":14,"row":8},{"column":15,"row":8},{"column":16,"row":8},{"column":17,"row":8}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":16,"row":10}],"id":"world-object:generated-5117006-3","kind":"light-source","name":"Runed Brazier","position":{"column":16,"row":10}}}]}}
tests/fixtures/arena-basis-brutal-2/seed-6207009.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":6207009,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"adjudicationPending":[],"blockedCells":[{"column":9,"row":0},{"column":9,"row":1},{"column":9,"row":2},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":9,"row":6},{"column":9,"row":7},{"column":9,"row":9},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":12},{"column":9,"row":13},{"column":9,"row":14},{"column":9,"row":15},{"column":9,"row":16},{"column":9,"row":17},{"column":9,"row":18},{"column":9,"row":19},{"column":9,"row":20},{"column":9,"row":21},{"column":9,"row":22},{"column":9,"row":23}],"bounds":{"columns":19,"rows":24},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":34,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":26,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":0},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":19,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":2},{"level":2,"maximum":3,"remaining":2},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-6207009-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-6207009-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":52,"life":"living","profile":{"id":"combatant:generated-6207009-monster-2","kind":"monster","name":"Doppelganger","rules":{"abilityScores":{"charisma":14,"constitution":14,"dexterity":18,"intelligence":11,"strength":11,"wisdom":12},"armorClass":14,"attacksPerAction":2,"conditionImmunities":["Charmed"],"contactMedium":"surface","creatureType":"Monstrosity","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":4,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":2,"constitution":2,"dexterity":4,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Medium","skillBonuses":{"deception":6,"insight":3},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:doppelganger","tokenId":"token:generated-6207009-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":50,"life":"living","profile":{"id":"combatant:generated-6207009-monster-3","kind":"monster","name":"Ironmaw Bear","rules":{"abilityScores":{"charisma":6,"constitution":15,"dexterity":15,"intelligence":3,"strength":18,"wisdom":12},"armorClass":13,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":50,"initiativeBonus":2,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":2,"dexterity":2,"intelligence":-4,"strength":4,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Large","speed":36,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/ironmaw-bear","tokenId":"token:generated-6207009-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-6207009-monster-4","kind":"monster","name":"Ambush Weaver","rules":{"abilityScores":{"charisma":6,"constitution":13,"dexterity":13,"intelligence":3,"strength":14,"wisdom":12},"armorClass":13,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":["web_sense"],"hitPointMaximum":16,"initiativeBonus":1,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":1,"intelligence":-4,"strength":2,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":32,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/ambush-weaver","tokenId":"token:generated-6207009-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 6207009."],"effects":[{"concentrationOwner":"combatant:wizard","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:wizard","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:wizard"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":7,"row":7},{"column":7,"row":8},{"column":7,"row":9},{"column":8,"row":7},{"column":8,"row":8},{"column":8,"row":9},{"column":10,"row":7},{"column":10,"row":8},{"column":10,"row":9},{"column":11,"row":7},{"column":11,"row":8},{"column":11,"row":9}],"id":"difficult:generated-6207009-approach"},{"cells":[{"column":7,"row":1},{"column":8,"row":1},{"column":10,"row":1},{"column":7,"row":2},{"column":8,"row":2},{"column":10,"row":2},{"column":7,"row":3},{"column":8,"row":3},{"column":10,"row":3},{"column":7,"row":4},{"column":8,"row":4},{"column":10,"row":4}],"id":"difficult:generated-6207009-brutal-1"},{"cells":[{"column":9,"row":8},{"column":8,"row":10},{"column":10,"row":10}],"id":"difficult:generated-6207009-brutal-2"},{"cells":[{"column":6,"row":8},{"column":6,"row":9},{"column":6,"row":10},{"column":7,"row":10},{"column":8,"row":10},{"column":6,"row":11},{"column":7,"row":11},{"column":8,"row":11}],"id":"difficult:generated-6207009-brutal-3"}],"lightRegions":[{"cells":[{"column":13,"row":20},{"column":14,"row":20},{"column":15,"row":20},{"column":13,"row":21},{"column":14,"row":21},{"column":15,"row":21}],"id":"light:world-object:generated-6207009-5","level":"bright"},{"cells":[{"column":7,"row":21},{"column":8,"row":21},{"column":7,"row":22},{"column":8,"row":22}],"id":"light:world-object:generated-6207009-6","level":"dim"}],"movementRegions":[],"narrowOpeningRegions":[],"obscurementRegions":[{"cells":[{"column":13,"row":13},{"column":14,"row":13},{"column":13,"row":14},{"column":14,"row":14},{"column":13,"row":15},{"column":14,"row":15}],"id":"obscurement:generated-6207009-1","obscurement":"light"},{"cells":[{"column":8,"row":16},{"column":10,"row":16},{"column":8,"row":17},{"column":10,"row":17},{"column":11,"row":17}],"id":"obscurement:generated-6207009-2","obscurement":"light"},{"cells":[{"column":4,"row":5},{"column":5,"row":5},{"column":4,"row":6},{"column":5,"row":6},{"column":4,"row":7},{"column":5,"row":7}],"id":"obscurement:generated-6207009-3","obscurement":"heavy"}]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":2,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"observationHistory":[],"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","sharedSpaceRelations":[],"tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":6}},{"combatantId":"combatant:generated-6207009-monster-1","id":"token:generated-6207009-monster-1","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":17,"row":1}},{"combatantId":"combatant:generated-6207009-monster-2","id":"token:generated-6207009-monster-2","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":16,"row":1}},{"combatantId":"combatant:generated-6207009-monster-3","id":"token:generated-6207009-monster-3","placementMode":{"actual":"Large","kind":"normal"},"position":{"column":17,"row":3}},{"combatantId":"combatant:generated-6207009-monster-4","id":"token:generated-6207009-monster-4","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":16,"row":3}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":15,"row":1}],"id":"world-object:generated-6207009-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":15,"row":1}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":16}],"id":"world-object:generated-6207009-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":16}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":17}],"id":"world-object:generated-6207009-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":4,"row":17}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":10,"row":2}],"id":"world-object:generated-6207009-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":10,"row":2}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":15,"row":16}],"id":"world-object:generated-6207009-5","kind":"light-source","name":"Runed Brazier","position":{"column":15,"row":16}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":20}],"id":"world-object:generated-6207009-6","kind":"light-source","name":"Runed Brazier","position":{"column":5,"row":20}}]}},"spec":{"blockedCells":[{"column":9,"row":0},{"column":9,"row":1},{"column":9,"row":2},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":9,"row":6},{"column":9,"row":7},{"column":9,"row":9},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":12},{"column":9,"row":13},{"column":9,"row":14},{"column":9,"row":15},{"column":9,"row":16},{"column":9,"row":17},{"column":9,"row":18},{"column":9,"row":19},{"column":9,"row":20},{"column":9,"row":21},{"column":9,"row":22},{"column":9,"row":23}],"challengeBudgetEighths":60,"challengeSpentEighths":60,"difficultyProfile":"brutal","dimensions":{"columns":19,"rows":24},"hardFeatures":{"chokepointCells":[{"column":9,"row":8}],"likelyApproachTerrainRegionIds":["difficult:generated-6207009-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-6207009-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-6207009-monster-1"},{"challengeEighths":24,"challengeRating":3,"combatantId":"combatant:generated-6207009-monster-2","statblockId":"statblock:doppelganger","tokenId":"token:generated-6207009-monster-2"},{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-6207009-monster-3","statblockId":"statblock:homebrew-beast/ironmaw-bear","tokenId":"token:generated-6207009-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-6207009-monster-4","statblockId":"statblock:homebrew-beast/ambush-weaver","tokenId":"token:generated-6207009-monster-4"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":34,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":26,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":0,"spent":4},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":true,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":19,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":2,"spent":2},{"level":2,"maximum":3,"remaining":2,"spent":1},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":6207009,"terrain":[{"cells":[{"column":7,"row":7},{"column":7,"row":8},{"column":7,"row":9},{"column":8,"row":7},{"column":8,"row":8},{"column":8,"row":9},{"column":10,"row":7},{"column":10,"row":8},{"column":10,"row":9},{"column":11,"row":7},{"column":11,"row":8},{"column":11,"row":9}],"id":"difficult:generated-6207009-approach","kind":"difficult-terrain-patch"},{"cells":[{"column":7,"row":1},{"column":8,"row":1},{"column":10,"row":1},{"column":7,"row":2},{"column":8,"row":2},{"column":10,"row":2},{"column":7,"row":3},{"column":8,"row":3},{"column":10,"row":3},{"column":7,"row":4},{"column":8,"row":4},{"column":10,"row":4}],"id":"difficult:generated-6207009-brutal-1","kind":"difficult-terrain-patch"},{"cells":[{"column":9,"row":8},{"column":8,"row":10},{"column":10,"row":10}],"id":"difficult:generated-6207009-brutal-2","kind":"difficult-terrain-patch"},{"cells":[{"column":6,"row":8},{"column":6,"row":9},{"column":6,"row":10},{"column":7,"row":10},{"column":8,"row":10},{"column":6,"row":11},{"column":7,"row":11},{"column":8,"row":11}],"id":"difficult:generated-6207009-brutal-3","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":15,"row":1}],"id":"world-object:generated-6207009-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":15,"row":1}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":16}],"id":"world-object:generated-6207009-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":16}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":17}],"id":"world-object:generated-6207009-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":4,"row":17}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":10,"row":2}],"id":"world-object:generated-6207009-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":10,"row":2}}},{"cells":[{"column":13,"row":20},{"column":14,"row":20},{"column":15,"row":20},{"column":13,"row":21},{"column":14,"row":21},{"column":15,"row":21}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":15,"row":16}],"id":"world-object:generated-6207009-5","kind":"light-source","name":"Runed Brazier","position":{"column":15,"row":16}}},{"cells":[{"column":7,"row":21},{"column":8,"row":21},{"column":7,"row":22},{"column":8,"row":22}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":20}],"id":"world-object:generated-6207009-6","kind":"light-source","name":"Runed Brazier","position":{"column":5,"row":20}}},{"cells":[{"column":13,"row":13},{"column":14,"row":13},{"column":13,"row":14},{"column":14,"row":14},{"column":13,"row":15},{"column":14,"row":15}],"id":"obscurement:generated-6207009-1","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":8,"row":16},{"column":10,"row":16},{"column":8,"row":17},{"column":10,"row":17},{"column":11,"row":17}],"id":"obscurement:generated-6207009-2","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":4,"row":5},{"column":5,"row":5},{"column":4,"row":6},{"column":5,"row":6},{"column":4,"row":7},{"column":5,"row":7}],"id":"obscurement:generated-6207009-3","kind":"obscurement-patch","obscurement":"heavy"}]}}
tests/fixtures/arena-basis-hard/seed-5117010.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117010,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":8,"row":0},{"column":8,"row":1},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":6},{"column":8,"row":8},{"column":8,"row":9},{"column":8,"row":10},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":8,"row":14},{"column":8,"row":15},{"column":8,"row":16},{"column":8,"row":17},{"column":8,"row":18},{"column":8,"row":19},{"column":8,"row":20},{"column":8,"row":21},{"column":8,"row":22}],"bounds":{"columns":16,"rows":23},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":67,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":26,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":{"failures":1,"successes":0},"hitPoints":0,"life":"dying","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117010-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117010-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117010-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117010-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117010-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117010-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117010-monster-4","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117010-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117010."],"effects":[],"environment":{"difficultTerrainRegions":[{"cells":[{"column":6,"row":6},{"column":6,"row":7},{"column":6,"row":8},{"column":7,"row":6},{"column":7,"row":7},{"column":7,"row":8},{"column":9,"row":6},{"column":9,"row":7},{"column":9,"row":8},{"column":10,"row":6},{"column":10,"row":7},{"column":10,"row":8}],"id":"difficult:generated-5117010-approach"}],"lightRegions":[{"cells":[{"column":5,"row":18},{"column":6,"row":18},{"column":5,"row":19},{"column":6,"row":19},{"column":5,"row":20},{"column":6,"row":20},{"column":5,"row":21},{"column":6,"row":21}],"id":"light:world-object:generated-5117010-3","level":"bright"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":1,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117010-monster-1","id":"token:generated-5117010-monster-1","position":{"column":14,"row":1}},{"combatantId":"combatant:generated-5117010-monster-2","id":"token:generated-5117010-monster-2","position":{"column":13,"row":1}},{"combatantId":"combatant:generated-5117010-monster-3","id":"token:generated-5117010-monster-3","position":{"column":14,"row":3}},{"combatantId":"combatant:generated-5117010-monster-4","id":"token:generated-5117010-monster-4","position":{"column":13,"row":3}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":12}],"id":"world-object:generated-5117010-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":12}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":7}],"id":"world-object:generated-5117010-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":1,"row":7}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":16}],"id":"world-object:generated-5117010-3","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":16}}]}},"spec":{"blockedCells":[{"column":8,"row":0},{"column":8,"row":1},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":6},{"column":8,"row":8},{"column":8,"row":9},{"column":8,"row":10},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":8,"row":14},{"column":8,"row":15},{"column":8,"row":16},{"column":8,"row":17},{"column":8,"row":18},{"column":8,"row":19},{"column":8,"row":20},{"column":8,"row":21},{"column":8,"row":22}],"challengeBudgetEighths":29,"challengeSpentEighths":22,"difficultyProfile":"hard","dimensions":{"columns":16,"rows":23},"hardFeatures":{"chokepointCells":[{"column":8,"row":7}],"likelyApproachTerrainRegionIds":["difficult:generated-5117010-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117010-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117010-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117010-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117010-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117010-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117010-monster-3"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117010-monster-4","statblockId":"statblock:guard","tokenId":"token:generated-5117010-monster-4"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":67,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":26,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":1,"spent":0}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":{"failures":1,"successes":0},"hitPointFraction":0,"hitPoints":0,"life":"dying","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":5117010,"terrain":[{"cells":[{"column":6,"row":6},{"column":6,"row":7},{"column":6,"row":8},{"column":7,"row":6},{"column":7,"row":7},{"column":7,"row":8},{"column":9,"row":6},{"column":9,"row":7},{"column":9,"row":8},{"column":10,"row":6},{"column":10,"row":7},{"column":10,"row":8}],"id":"difficult:generated-5117010-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":12}],"id":"world-object:generated-5117010-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":12}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":7}],"id":"world-object:generated-5117010-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":1,"row":7}}},{"cells":[{"column":5,"row":18},{"column":6,"row":18},{"column":5,"row":19},{"column":6,"row":19},{"column":5,"row":20},{"column":6,"row":20},{"column":5,"row":21},{"column":6,"row":21}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":16}],"id":"world-object:generated-5117010-3","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":16}}}]}}
src/vtt/encounter-board.ts:113:  readonly foggedCells?: readonly { readonly column: number; readonly row: number }[];
src/vtt/encounter-board.ts:263:  readonly foggedCells: readonly { readonly column: number; readonly row: number }[];
src/vtt/encounter-board.ts:716:    foggedCells: derivedFogCells(state, visibilityObserverIds),
src/vtt/encounter-board.ts:872:    ...(projection.foggedCells?.map(cellKey) ?? []),
tests/fixtures/arena-basis-brutal-2/seed-6207005.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":6207005,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"adjudicationPending":[],"blockedCells":[{"column":9,"row":0},{"column":9,"row":1},{"column":9,"row":2},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":9,"row":6},{"column":9,"row":7},{"column":9,"row":8},{"column":9,"row":9},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":12},{"column":9,"row":13},{"column":9,"row":14},{"column":9,"row":15},{"column":9,"row":16},{"column":9,"row":17},{"column":9,"row":18},{"column":9,"row":20},{"column":9,"row":21},{"column":9,"row":22}],"bounds":{"columns":18,"rows":23},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":34,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":39,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":0},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":1},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":19,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":4},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":3},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":82,"life":"living","limitedResources":[{"id":"monster-spell:war-prayers:command","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:war-prayers:hold-person","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:cinder-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-6207005-monster-1","kind":"monster","name":"Ashmaw, Cinder Votary","rules":{"abilityScores":{"charisma":11,"constitution":15,"dexterity":12,"intelligence":10,"strength":17,"wisdom":16},"armorClass":15,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Fey","damageResponses":[],"detectionTraits":[],"hitPointMaximum":82,"initiativeBonus":1,"limitedResources":[{"id":"monster-spell:war-prayers:command","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:war-prayers:hold-person","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:cinder-aid:healing-word","maximum":3,"recharge":"long_rest"}],"passivePerception":13,"proficiencyBonus":2,"reach":10,"savingThrowBonuses":{"charisma":0,"constitution":4,"dexterity":1,"intelligence":0,"strength":5,"wisdom":5},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Medium","skillBonuses":{"medicine":5,"religion":4},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:vane-warren/ashmaw-brute-priest","tokenId":"token:generated-6207005-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":80,"life":"living","profile":{"id":"combatant:generated-6207005-monster-2","kind":"monster","name":"Abyssal Cuttle","rules":{"abilityScores":{"charisma":6,"constitution":17,"dexterity":16,"intelligence":3,"strength":22,"wisdom":12},"armorClass":14,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":80,"initiativeBonus":3,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":3,"dexterity":3,"intelligence":-4,"strength":6,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Huge","speed":5,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/abyssal-cuttle","tokenId":"token:generated-6207005-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":15,"life":"living","profile":{"id":"combatant:generated-6207005-monster-3","kind":"monster","name":"Crag Eagle","rules":{"abilityScores":{"charisma":6,"constitution":13,"dexterity":13,"intelligence":3,"strength":14,"wisdom":12},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":["keen_sight","flyby"],"hitPointMaximum":15,"initiativeBonus":1,"passivePerception":14,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":1,"intelligence":-4,"strength":2,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","skillBonuses":{"perception":4},"speed":10,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/crag-eagle","tokenId":"token:generated-6207005-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-6207005-monster-4","kind":"monster","name":"Wolf","rules":{"abilityScores":{"charisma":6,"constitution":12,"dexterity":15,"intelligence":3,"strength":14,"wisdom":12},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":2,"intelligence":-4,"strength":2,"wisdom":1},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Medium","skillBonuses":{"perception":5,"stealth":4},"speed":40,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:wolf","tokenId":"token:generated-6207005-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 6207005."],"effects":[],"environment":{"difficultTerrainRegions":[{"cells":[{"column":7,"row":18},{"column":7,"row":19},{"column":7,"row":20},{"column":8,"row":18},{"column":8,"row":19},{"column":8,"row":20},{"column":10,"row":18},{"column":10,"row":19},{"column":10,"row":20},{"column":11,"row":18},{"column":11,"row":19},{"column":11,"row":20}],"id":"difficult:generated-6207005-approach"},{"cells":[{"column":3,"row":1},{"column":4,"row":1},{"column":5,"row":1},{"column":3,"row":2},{"column":4,"row":2},{"column":5,"row":2}],"id":"difficult:generated-6207005-brutal-1"},{"cells":[{"column":4,"row":7},{"column":5,"row":7},{"column":6,"row":7},{"column":4,"row":8},{"column":5,"row":8},{"column":6,"row":8},{"column":4,"row":9},{"column":5,"row":9},{"column":6,"row":9}],"id":"difficult:generated-6207005-brutal-2"}],"lightRegions":[{"cells":[{"column":8,"row":14},{"column":10,"row":14},{"column":8,"row":15},{"column":10,"row":15},{"column":8,"row":16},{"column":10,"row":16},{"column":8,"row":17},{"column":10,"row":17}],"id":"light:world-object:generated-6207005-4","level":"dim"},{"cells":[{"column":8,"row":9},{"column":10,"row":9},{"column":8,"row":10},{"column":10,"row":10}],"id":"light:world-object:generated-6207005-5","level":"dim"},{"cells":[{"column":5,"row":3},{"column":6,"row":3},{"column":7,"row":3},{"column":5,"row":4},{"column":6,"row":4},{"column":7,"row":4}],"id":"light:world-object:generated-6207005-6","level":"dim"}],"movementRegions":[],"narrowOpeningRegions":[],"obscurementRegions":[{"cells":[{"column":5,"row":19},{"column":6,"row":19},{"column":5,"row":20},{"column":6,"row":20}],"id":"obscurement:generated-6207005-1","obscurement":"light"},{"cells":[{"column":3,"row":10},{"column":4,"row":10},{"column":5,"row":10},{"column":4,"row":11},{"column":5,"row":11},{"column":3,"row":12},{"column":4,"row":12},{"column":5,"row":12}],"id":"obscurement:generated-6207005-2","obscurement":"light"},{"cells":[{"column":9,"row":19},{"column":7,"row":21},{"column":8,"row":21}],"id":"obscurement:generated-6207005-3","obscurement":"heavy"}]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":1,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"observationHistory":[],"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","sharedSpaceRelations":[],"tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":6}},{"combatantId":"combatant:generated-6207005-monster-1","id":"token:generated-6207005-monster-1","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":16,"row":1}},{"combatantId":"combatant:generated-6207005-monster-2","id":"token:generated-6207005-monster-2","placementMode":{"actual":"Huge","kind":"normal"},"position":{"column":14,"row":2}},{"combatantId":"combatant:generated-6207005-monster-3","id":"token:generated-6207005-monster-3","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":17,"row":2}},{"combatantId":"combatant:generated-6207005-monster-4","id":"token:generated-6207005-monster-4","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":13,"row":1}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":4}],"id":"world-object:generated-6207005-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":13,"row":4}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":13}],"id":"world-object:generated-6207005-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":13}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":15,"row":12}],"id":"world-object:generated-6207005-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":15,"row":12}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":12}],"id":"world-object:generated-6207005-4","kind":"light-source","name":"Runed Brazier","position":{"column":11,"row":12}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":8}],"id":"world-object:generated-6207005-5","kind":"light-source","name":"Runed Brazier","position":{"column":5,"row":8}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":11}],"id":"world-object:generated-6207005-6","kind":"light-source","name":"Runed Brazier","position":{"column":3,"row":11}}]}},"spec":{"blockedCells":[{"column":9,"row":0},{"column":9,"row":1},{"column":9,"row":2},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":9,"row":6},{"column":9,"row":7},{"column":9,"row":8},{"column":9,"row":9},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":12},{"column":9,"row":13},{"column":9,"row":14},{"column":9,"row":15},{"column":9,"row":16},{"column":9,"row":17},{"column":9,"row":18},{"column":9,"row":20},{"column":9,"row":21},{"column":9,"row":22}],"challengeBudgetEighths":70,"challengeSpentEighths":70,"difficultyProfile":"brutal","dimensions":{"columns":18,"rows":23},"hardFeatures":{"chokepointCells":[{"column":9,"row":19}],"likelyApproachTerrainRegionIds":["difficult:generated-6207005-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":32,"challengeRating":4,"combatantId":"combatant:generated-6207005-monster-1","statblockId":"statblock:vane-warren/ashmaw-brute-priest","tokenId":"token:generated-6207005-monster-1"},{"challengeEighths":32,"challengeRating":4,"combatantId":"combatant:generated-6207005-monster-2","statblockId":"statblock:homebrew-beast/abyssal-cuttle","tokenId":"token:generated-6207005-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-6207005-monster-3","statblockId":"statblock:homebrew-beast/crag-eagle","tokenId":"token:generated-6207005-monster-3"},{"challengeEighths":2,"challengeRating":"1/4","combatantId":"combatant:generated-6207005-monster-4","statblockId":"statblock:wolf","tokenId":"token:generated-6207005-monster-4"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":34,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.75,"hitPoints":39,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":0,"spent":4},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":1,"spent":2},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":19,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":4,"spent":0},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":3,"spent":0},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":6207005,"terrain":[{"cells":[{"column":7,"row":18},{"column":7,"row":19},{"column":7,"row":20},{"column":8,"row":18},{"column":8,"row":19},{"column":8,"row":20},{"column":10,"row":18},{"column":10,"row":19},{"column":10,"row":20},{"column":11,"row":18},{"column":11,"row":19},{"column":11,"row":20}],"id":"difficult:generated-6207005-approach","kind":"difficult-terrain-patch"},{"cells":[{"column":3,"row":1},{"column":4,"row":1},{"column":5,"row":1},{"column":3,"row":2},{"column":4,"row":2},{"column":5,"row":2}],"id":"difficult:generated-6207005-brutal-1","kind":"difficult-terrain-patch"},{"cells":[{"column":4,"row":7},{"column":5,"row":7},{"column":6,"row":7},{"column":4,"row":8},{"column":5,"row":8},{"column":6,"row":8},{"column":4,"row":9},{"column":5,"row":9},{"column":6,"row":9}],"id":"difficult:generated-6207005-brutal-2","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":4}],"id":"world-object:generated-6207005-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":13,"row":4}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":13}],"id":"world-object:generated-6207005-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":13}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":15,"row":12}],"id":"world-object:generated-6207005-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":15,"row":12}}},{"cells":[{"column":8,"row":14},{"column":10,"row":14},{"column":8,"row":15},{"column":10,"row":15},{"column":8,"row":16},{"column":10,"row":16},{"column":8,"row":17},{"column":10,"row":17}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":12}],"id":"world-object:generated-6207005-4","kind":"light-source","name":"Runed Brazier","position":{"column":11,"row":12}}},{"cells":[{"column":8,"row":9},{"column":10,"row":9},{"column":8,"row":10},{"column":10,"row":10}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":8}],"id":"world-object:generated-6207005-5","kind":"light-source","name":"Runed Brazier","position":{"column":5,"row":8}}},{"cells":[{"column":5,"row":3},{"column":6,"row":3},{"column":7,"row":3},{"column":5,"row":4},{"column":6,"row":4},{"column":7,"row":4}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":11}],"id":"world-object:generated-6207005-6","kind":"light-source","name":"Runed Brazier","position":{"column":3,"row":11}}},{"cells":[{"column":5,"row":19},{"column":6,"row":19},{"column":5,"row":20},{"column":6,"row":20}],"id":"obscurement:generated-6207005-1","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":3,"row":10},{"column":4,"row":10},{"column":5,"row":10},{"column":4,"row":11},{"column":5,"row":11},{"column":3,"row":12},{"column":4,"row":12},{"column":5,"row":12}],"id":"obscurement:generated-6207005-2","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":9,"row":19},{"column":7,"row":21},{"column":8,"row":21}],"id":"obscurement:generated-6207005-3","kind":"obscurement-patch","obscurement":"heavy"}]}}
tests/fixtures/arena-basis-hard/seed-5117012.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117012,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":6,"row":0},{"column":6,"row":1},{"column":6,"row":2},{"column":6,"row":3},{"column":6,"row":4},{"column":6,"row":6},{"column":6,"row":7},{"column":6,"row":8},{"column":6,"row":9},{"column":6,"row":10},{"column":6,"row":11},{"column":6,"row":12},{"column":6,"row":13},{"column":6,"row":14},{"column":6,"row":15}],"bounds":{"columns":13,"rows":16},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":34,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":26,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":1},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":0},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117012-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117012-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117012-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117012-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117012-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117012-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117012-monster-4","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117012-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117012-monster-5","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117012-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117012-monster-6","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117012-monster-6"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117012-monster-7","kind":"monster","name":"Bandit","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":11,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":0,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:bandit","tokenId":"token:generated-5117012-monster-7"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117012."],"effects":[{"concentrationOwner":"combatant:cleric","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:cleric","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:cleric"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":4,"row":4},{"column":4,"row":5},{"column":4,"row":6},{"column":5,"row":4},{"column":5,"row":5},{"column":5,"row":6},{"column":7,"row":4},{"column":7,"row":5},{"column":7,"row":6},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":6}],"id":"difficult:generated-5117012-approach"}],"lightRegions":[{"cells":[{"column":5,"row":7},{"column":5,"row":8},{"column":5,"row":9}],"id":"light:world-object:generated-5117012-2","level":"dim"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":2,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117012-monster-1","id":"token:generated-5117012-monster-1","position":{"column":11,"row":1}},{"combatantId":"combatant:generated-5117012-monster-2","id":"token:generated-5117012-monster-2","position":{"column":10,"row":1}},{"combatantId":"combatant:generated-5117012-monster-3","id":"token:generated-5117012-monster-3","position":{"column":11,"row":3}},{"combatantId":"combatant:generated-5117012-monster-4","id":"token:generated-5117012-monster-4","position":{"column":10,"row":3}},{"combatantId":"combatant:generated-5117012-monster-5","id":"token:generated-5117012-monster-5","position":{"column":11,"row":5}},{"combatantId":"combatant:generated-5117012-monster-6","id":"token:generated-5117012-monster-6","position":{"column":10,"row":5}},{"combatantId":"combatant:generated-5117012-monster-7","id":"token:generated-5117012-monster-7","position":{"column":11,"row":7}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":10,"row":12}],"id":"world-object:generated-5117012-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":10,"row":12}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":8,"row":14}],"id":"world-object:generated-5117012-2","kind":"light-source","name":"Runed Brazier","position":{"column":8,"row":14}}]}},"spec":{"blockedCells":[{"column":6,"row":0},{"column":6,"row":1},{"column":6,"row":2},{"column":6,"row":3},{"column":6,"row":4},{"column":6,"row":6},{"column":6,"row":7},{"column":6,"row":8},{"column":6,"row":9},{"column":6,"row":10},{"column":6,"row":11},{"column":6,"row":12},{"column":6,"row":13},{"column":6,"row":14},{"column":6,"row":15}],"challengeBudgetEighths":30,"challengeSpentEighths":25,"difficultyProfile":"hard","dimensions":{"columns":13,"rows":16},"hardFeatures":{"chokepointCells":[{"column":6,"row":5}],"likelyApproachTerrainRegionIds":["difficult:generated-5117012-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117012-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117012-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117012-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117012-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117012-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117012-monster-3"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117012-monster-4","statblockId":"statblock:guard","tokenId":"token:generated-5117012-monster-4"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117012-monster-5","statblockId":"statblock:guard","tokenId":"token:generated-5117012-monster-5"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117012-monster-6","statblockId":"statblock:guard","tokenId":"token:generated-5117012-monster-6"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117012-monster-7","statblockId":"statblock:bandit","tokenId":"token:generated-5117012-monster-7"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":34,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":true,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":26,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":1,"spent":2},{"level":4,"maximum":1,"remaining":1,"spent":0}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":38,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":0,"spent":4},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":5117012,"terrain":[{"cells":[{"column":4,"row":4},{"column":4,"row":5},{"column":4,"row":6},{"column":5,"row":4},{"column":5,"row":5},{"column":5,"row":6},{"column":7,"row":4},{"column":7,"row":5},{"column":7,"row":6},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":6}],"id":"difficult:generated-5117012-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":10,"row":12}],"id":"world-object:generated-5117012-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":10,"row":12}}},{"cells":[{"column":5,"row":7},{"column":5,"row":8},{"column":5,"row":9}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":8,"row":14}],"id":"world-object:generated-5117012-2","kind":"light-source","name":"Runed Brazier","position":{"column":8,"row":14}}}]}}
tests/fixtures/arena-basis-brutal-2/seed-6207002.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":6207002,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"adjudicationPending":[],"blockedCells":[{"column":12,"row":0},{"column":12,"row":1},{"column":12,"row":2},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5},{"column":12,"row":6},{"column":12,"row":7},{"column":12,"row":9},{"column":12,"row":10},{"column":12,"row":11}],"bounds":{"columns":24,"rows":12},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":17,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":26,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":1},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":1},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":29,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":4},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":82,"life":"living","limitedResources":[{"id":"monster-spell:war-prayers:command","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:war-prayers:hold-person","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:cinder-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-6207002-monster-1","kind":"monster","name":"Ashmaw, Cinder Votary","rules":{"abilityScores":{"charisma":11,"constitution":15,"dexterity":12,"intelligence":10,"strength":17,"wisdom":16},"armorClass":15,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Fey","damageResponses":[],"detectionTraits":[],"hitPointMaximum":82,"initiativeBonus":1,"limitedResources":[{"id":"monster-spell:war-prayers:command","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:war-prayers:hold-person","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:cinder-aid:healing-word","maximum":3,"recharge":"long_rest"}],"passivePerception":13,"proficiencyBonus":2,"reach":10,"savingThrowBonuses":{"charisma":0,"constitution":4,"dexterity":1,"intelligence":0,"strength":5,"wisdom":5},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Medium","skillBonuses":{"medicine":5,"religion":4},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:vane-warren/ashmaw-brute-priest","tokenId":"token:generated-6207002-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":43,"life":"living","profile":{"id":"combatant:generated-6207002-monster-2","kind":"monster","name":"Thunderfoot Saurian","rules":{"abilityScores":{"charisma":6,"constitution":15,"dexterity":15,"intelligence":3,"strength":18,"wisdom":12},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":43,"initiativeBonus":2,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":2,"dexterity":2,"intelligence":-4,"strength":4,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Large","speed":36,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/thunderfoot-saurian","tokenId":"token:generated-6207002-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":10,"life":"living","profile":{"id":"combatant:generated-6207002-monster-3","kind":"monster","name":"Goblin Warrior","rules":{"abilityScores":{"charisma":8,"constitution":10,"dexterity":15,"intelligence":10,"strength":8,"wisdom":8},"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Fey","damageResponses":[],"detectionTraits":[],"hitPointMaximum":10,"initiativeBonus":2,"passivePerception":9,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-1,"constitution":0,"dexterity":2,"intelligence":0,"strength":-1,"wisdom":-1},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Small","skillBonuses":{"stealth":6},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:goblin-warrior","tokenId":"token:generated-6207002-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":13,"life":"living","profile":{"id":"combatant:generated-6207002-monster-4","kind":"monster","name":"Boar","rules":{"abilityScores":{"charisma":5,"constitution":14,"dexterity":11,"intelligence":2,"strength":13,"wisdom":9},"armorClass":11,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":13,"initiativeBonus":0,"passivePerception":9,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-3,"constitution":2,"dexterity":0,"intelligence":-4,"strength":1,"wisdom":-1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":40,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:boar","tokenId":"token:generated-6207002-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":7,"life":"living","profile":{"id":"combatant:generated-6207002-monster-5","kind":"monster","name":"Goblin Minion","rules":{"abilityScores":{"charisma":8,"constitution":10,"dexterity":15,"intelligence":10,"strength":8,"wisdom":8},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Fey","damageResponses":[],"detectionTraits":[],"hitPointMaximum":7,"initiativeBonus":2,"passivePerception":9,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-1,"constitution":0,"dexterity":2,"intelligence":0,"strength":-1,"wisdom":-1},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Small","skillBonuses":{"stealth":6},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:goblin-minion","tokenId":"token:generated-6207002-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":8,"life":"living","profile":{"id":"combatant:generated-6207002-monster-6","kind":"monster","name":"Astraldendon","rules":{"abilityScores":{"charisma":5,"constitution":12,"dexterity":14,"intelligence":3,"strength":12,"wisdom":10},"armorClass":13,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Plant","damageResponses":[],"detectionTraits":[],"hitPointMaximum":8,"initiativeBonus":2,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-3,"constitution":1,"dexterity":2,"intelligence":-4,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Small","speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"homebrew:escape-the-astral-tower/astraldendon","tokenId":"token:generated-6207002-monster-6"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 6207002."],"effects":[],"environment":{"difficultTerrainRegions":[{"cells":[{"column":10,"row":7},{"column":10,"row":8},{"column":10,"row":9},{"column":11,"row":7},{"column":11,"row":8},{"column":11,"row":9},{"column":13,"row":7},{"column":13,"row":8},{"column":13,"row":9},{"column":14,"row":7},{"column":14,"row":8},{"column":14,"row":9}],"id":"difficult:generated-6207002-approach"},{"cells":[{"column":4,"row":8},{"column":5,"row":8},{"column":6,"row":8},{"column":4,"row":9},{"column":5,"row":9},{"column":6,"row":9},{"column":4,"row":10},{"column":5,"row":10},{"column":6,"row":10}],"id":"difficult:generated-6207002-brutal-1"},{"cells":[{"column":13,"row":6},{"column":14,"row":6},{"column":15,"row":6},{"column":16,"row":6},{"column":15,"row":7},{"column":16,"row":7},{"column":15,"row":8},{"column":16,"row":8}],"id":"difficult:generated-6207002-brutal-2"}],"lightRegions":[{"cells":[{"column":8,"row":4},{"column":9,"row":4},{"column":10,"row":4},{"column":11,"row":4},{"column":8,"row":5},{"column":9,"row":5},{"column":10,"row":5},{"column":11,"row":5},{"column":8,"row":6},{"column":9,"row":6},{"column":10,"row":6},{"column":11,"row":6},{"column":8,"row":7},{"column":9,"row":7}],"id":"light:world-object:generated-6207002-5","level":"dim"},{"cells":[{"column":16,"row":7},{"column":18,"row":7},{"column":16,"row":8},{"column":17,"row":8},{"column":18,"row":8},{"column":16,"row":9},{"column":17,"row":9},{"column":18,"row":9}],"id":"light:world-object:generated-6207002-6","level":"dim"}],"movementRegions":[],"narrowOpeningRegions":[],"obscurementRegions":[{"cells":[{"column":15,"row":7},{"column":16,"row":7},{"column":15,"row":8},{"column":16,"row":8},{"column":17,"row":8},{"column":15,"row":9},{"column":16,"row":9},{"column":17,"row":9},{"column":14,"row":10},{"column":15,"row":10},{"column":16,"row":10},{"column":17,"row":10}],"id":"obscurement:generated-6207002-1","obscurement":"light"},{"cells":[{"column":12,"row":8}],"id":"obscurement:generated-6207002-2","obscurement":"light"}]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":1,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"observationHistory":[],"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","sharedSpaceRelations":[],"tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":6}},{"combatantId":"combatant:generated-6207002-monster-1","id":"token:generated-6207002-monster-1","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":22,"row":1}},{"combatantId":"combatant:generated-6207002-monster-2","id":"token:generated-6207002-monster-2","placementMode":{"actual":"Large","kind":"normal"},"position":{"column":20,"row":0}},{"combatantId":"combatant:generated-6207002-monster-3","id":"token:generated-6207002-monster-3","placementMode":{"actual":"Small","kind":"normal"},"position":{"column":22,"row":3}},{"combatantId":"combatant:generated-6207002-monster-4","id":"token:generated-6207002-monster-4","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":21,"row":3}},{"combatantId":"combatant:generated-6207002-monster-5","id":"token:generated-6207002-monster-5","placementMode":{"actual":"Small","kind":"normal"},"position":{"column":22,"row":5}},{"combatantId":"combatant:generated-6207002-monster-6","id":"token:generated-6207002-monster-6","placementMode":{"actual":"Small","kind":"normal"},"position":{"column":21,"row":5}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":18,"row":10}],"id":"world-object:generated-6207002-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":18,"row":10}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":16,"row":1}],"id":"world-object:generated-6207002-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":16,"row":1}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":10}],"id":"world-object:generated-6207002-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":10}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":2,"row":1}],"id":"world-object:generated-6207002-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":2,"row":1}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":17,"row":7}],"id":"world-object:generated-6207002-5","kind":"light-source","name":"Runed Brazier","position":{"column":17,"row":7}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":22,"row":6}],"id":"world-object:generated-6207002-6","kind":"light-source","name":"Runed Brazier","position":{"column":22,"row":6}}]}},"spec":{"blockedCells":[{"column":12,"row":0},{"column":12,"row":1},{"column":12,"row":2},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5},{"column":12,"row":6},{"column":12,"row":7},{"column":12,"row":9},{"column":12,"row":10},{"column":12,"row":11}],"challengeBudgetEighths":54,"challengeSpentEighths":54,"difficultyProfile":"brutal","dimensions":{"columns":24,"rows":12},"hardFeatures":{"chokepointCells":[{"column":12,"row":8}],"likelyApproachTerrainRegionIds":["difficult:generated-6207002-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":32,"challengeRating":4,"combatantId":"combatant:generated-6207002-monster-1","statblockId":"statblock:vane-warren/ashmaw-brute-priest","tokenId":"token:generated-6207002-monster-1"},{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-6207002-monster-2","statblockId":"statblock:homebrew-beast/thunderfoot-saurian","tokenId":"token:generated-6207002-monster-2"},{"challengeEighths":2,"challengeRating":"1/4","combatantId":"combatant:generated-6207002-monster-3","statblockId":"statblock:goblin-warrior","tokenId":"token:generated-6207002-monster-3"},{"challengeEighths":2,"challengeRating":"1/4","combatantId":"combatant:generated-6207002-monster-4","statblockId":"statblock:boar","tokenId":"token:generated-6207002-monster-4"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-6207002-monster-5","statblockId":"statblock:goblin-minion","tokenId":"token:generated-6207002-monster-5"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-6207002-monster-6","statblockId":"homebrew:escape-the-astral-tower/astraldendon","tokenId":"token:generated-6207002-monster-6"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":17,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":26,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":1,"spent":3},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":1,"spent":2},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":0.75,"hitPoints":29,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":4,"spent":0},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":1,"spent":0}]}],"seed":6207002,"terrain":[{"cells":[{"column":10,"row":7},{"column":10,"row":8},{"column":10,"row":9},{"column":11,"row":7},{"column":11,"row":8},{"column":11,"row":9},{"column":13,"row":7},{"column":13,"row":8},{"column":13,"row":9},{"column":14,"row":7},{"column":14,"row":8},{"column":14,"row":9}],"id":"difficult:generated-6207002-approach","kind":"difficult-terrain-patch"},{"cells":[{"column":4,"row":8},{"column":5,"row":8},{"column":6,"row":8},{"column":4,"row":9},{"column":5,"row":9},{"column":6,"row":9},{"column":4,"row":10},{"column":5,"row":10},{"column":6,"row":10}],"id":"difficult:generated-6207002-brutal-1","kind":"difficult-terrain-patch"},{"cells":[{"column":13,"row":6},{"column":14,"row":6},{"column":15,"row":6},{"column":16,"row":6},{"column":15,"row":7},{"column":16,"row":7},{"column":15,"row":8},{"column":16,"row":8}],"id":"difficult:generated-6207002-brutal-2","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":18,"row":10}],"id":"world-object:generated-6207002-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":18,"row":10}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":16,"row":1}],"id":"world-object:generated-6207002-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":16,"row":1}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":10}],"id":"world-object:generated-6207002-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":10}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":2,"row":1}],"id":"world-object:generated-6207002-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":2,"row":1}}},{"cells":[{"column":8,"row":4},{"column":9,"row":4},{"column":10,"row":4},{"column":11,"row":4},{"column":8,"row":5},{"column":9,"row":5},{"column":10,"row":5},{"column":11,"row":5},{"column":8,"row":6},{"column":9,"row":6},{"column":10,"row":6},{"column":11,"row":6},{"column":8,"row":7},{"column":9,"row":7}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":17,"row":7}],"id":"world-object:generated-6207002-5","kind":"light-source","name":"Runed Brazier","position":{"column":17,"row":7}}},{"cells":[{"column":16,"row":7},{"column":18,"row":7},{"column":16,"row":8},{"column":17,"row":8},{"column":18,"row":8},{"column":16,"row":9},{"column":17,"row":9},{"column":18,"row":9}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":22,"row":6}],"id":"world-object:generated-6207002-6","kind":"light-source","name":"Runed Brazier","position":{"column":22,"row":6}}},{"cells":[{"column":15,"row":7},{"column":16,"row":7},{"column":15,"row":8},{"column":16,"row":8},{"column":17,"row":8},{"column":15,"row":9},{"column":16,"row":9},{"column":17,"row":9},{"column":14,"row":10},{"column":15,"row":10},{"column":16,"row":10},{"column":17,"row":10}],"id":"obscurement:generated-6207002-1","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":12,"row":8}],"id":"obscurement:generated-6207002-2","kind":"obscurement-patch","obscurement":"light"}]}}
tests/fixtures/arena-basis-hard/seed-5117011.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117011,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":12,"row":0},{"column":12,"row":1},{"column":12,"row":2},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5},{"column":12,"row":6},{"column":12,"row":7},{"column":12,"row":8},{"column":12,"row":10},{"column":12,"row":11},{"column":12,"row":12},{"column":12,"row":13},{"column":12,"row":14},{"column":12,"row":15},{"column":12,"row":16},{"column":12,"row":17},{"column":12,"row":18},{"column":12,"row":19},{"column":12,"row":20},{"column":12,"row":21},{"column":12,"row":22}],"bounds":{"columns":24,"rows":23},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":17,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":13,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":4},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":10,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":4},{"level":2,"maximum":3,"remaining":2},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117011-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117011-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117011-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117011-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117011-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117011-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":32,"life":"living","profile":{"id":"combatant:generated-5117011-monster-4","kind":"monster","name":"Tough","rules":{"abilityScores":{"charisma":11,"constitution":14,"dexterity":12,"intelligence":10,"strength":15,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":32,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":2,"dexterity":1,"intelligence":0,"strength":2,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:tough","tokenId":"token:generated-5117011-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117011-monster-5","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117011-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117011-monster-6","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117011-monster-6"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117011-monster-7","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117011-monster-7"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117011."],"effects":[{"concentrationOwner":"combatant:wizard","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:wizard","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:wizard"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":10,"row":8},{"column":10,"row":9},{"column":10,"row":10},{"column":11,"row":8},{"column":11,"row":9},{"column":11,"row":10},{"column":13,"row":8},{"column":13,"row":9},{"column":13,"row":10},{"column":14,"row":8},{"column":14,"row":9},{"column":14,"row":10}],"id":"difficult:generated-5117011-approach"}],"lightRegions":[{"cells":[{"column":10,"row":7},{"column":11,"row":7}],"id":"light:world-object:generated-5117011-3","level":"bright"},{"cells":[{"column":15,"row":13},{"column":16,"row":13},{"column":15,"row":14},{"column":16,"row":14},{"column":15,"row":15},{"column":16,"row":15},{"column":15,"row":16},{"column":16,"row":16}],"id":"light:world-object:generated-5117011-4","level":"dim"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":2,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117011-monster-1","id":"token:generated-5117011-monster-1","position":{"column":22,"row":1}},{"combatantId":"combatant:generated-5117011-monster-2","id":"token:generated-5117011-monster-2","position":{"column":21,"row":1}},{"combatantId":"combatant:generated-5117011-monster-3","id":"token:generated-5117011-monster-3","position":{"column":22,"row":3}},{"combatantId":"combatant:generated-5117011-monster-4","id":"token:generated-5117011-monster-4","position":{"column":21,"row":3}},{"combatantId":"combatant:generated-5117011-monster-5","id":"token:generated-5117011-monster-5","position":{"column":22,"row":5}},{"combatantId":"combatant:generated-5117011-monster-6","id":"token:generated-5117011-monster-6","position":{"column":21,"row":5}},{"combatantId":"combatant:generated-5117011-monster-7","id":"token:generated-5117011-monster-7","position":{"column":22,"row":7}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":15,"row":21}],"id":"world-object:generated-5117011-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":15,"row":21}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":21}],"id":"world-object:generated-5117011-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":4,"row":21}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":19,"row":10}],"id":"world-object:generated-5117011-3","kind":"light-source","name":"Runed Brazier","position":{"column":19,"row":10}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":14}],"id":"world-object:generated-5117011-4","kind":"light-source","name":"Runed Brazier","position":{"column":4,"row":14}}]}},"spec":{"blockedCells":[{"column":12,"row":0},{"column":12,"row":1},{"column":12,"row":2},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5},{"column":12,"row":6},{"column":12,"row":7},{"column":12,"row":8},{"column":12,"row":10},{"column":12,"row":11},{"column":12,"row":12},{"column":12,"row":13},{"column":12,"row":14},{"column":12,"row":15},{"column":12,"row":16},{"column":12,"row":17},{"column":12,"row":18},{"column":12,"row":19},{"column":12,"row":20},{"column":12,"row":21},{"column":12,"row":22}],"challengeBudgetEighths":36,"challengeSpentEighths":34,"difficultyProfile":"hard","dimensions":{"columns":24,"rows":23},"hardFeatures":{"chokepointCells":[{"column":12,"row":9}],"likelyApproachTerrainRegionIds":["difficult:generated-5117011-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117011-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117011-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117011-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117011-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117011-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117011-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117011-monster-4","statblockId":"statblock:tough","tokenId":"token:generated-5117011-monster-4"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117011-monster-5","statblockId":"statblock:guard","tokenId":"token:generated-5117011-monster-5"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117011-monster-6","statblockId":"statblock:scout","tokenId":"token:generated-5117011-monster-6"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117011-monster-7","statblockId":"statblock:scout","tokenId":"token:generated-5117011-monster-7"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":17,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":13,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":4,"spent":0},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":1,"spent":0}]},{"combatantId":"combatant:wizard","concentrating":true,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":10,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":4,"spent":0},{"level":2,"maximum":3,"remaining":2,"spent":1},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":5117011,"terrain":[{"cells":[{"column":10,"row":8},{"column":10,"row":9},{"column":10,"row":10},{"column":11,"row":8},{"column":11,"row":9},{"column":11,"row":10},{"column":13,"row":8},{"column":13,"row":9},{"column":13,"row":10},{"column":14,"row":8},{"column":14,"row":9},{"column":14,"row":10}],"id":"difficult:generated-5117011-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":15,"row":21}],"id":"world-object:generated-5117011-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":15,"row":21}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":21}],"id":"world-object:generated-5117011-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":4,"row":21}}},{"cells":[{"column":10,"row":7},{"column":11,"row":7}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":19,"row":10}],"id":"world-object:generated-5117011-3","kind":"light-source","name":"Runed Brazier","position":{"column":19,"row":10}}},{"cells":[{"column":15,"row":13},{"column":16,"row":13},{"column":15,"row":14},{"column":16,"row":14},{"column":15,"row":15},{"column":16,"row":15},{"column":15,"row":16},{"column":16,"row":16}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":14}],"id":"world-object:generated-5117011-4","kind":"light-source","name":"Runed Brazier","position":{"column":4,"row":14}}}]}}
tests/unit/vtt/room-generator.test.ts:65:  // D635.48: each old projection differs only by the removed state.foggedCells: [] key.
tests/unit/vtt/room-generator.test.ts:97:  // D635.48: every old projection differs only by the removed state.foggedCells: [] key.
tests/unit/vtt/room-generator.test.ts:287:  delete (legacy.encounter.state as { foggedCells?: unknown }).foggedCells;
tests/unit/vtt/room-generator.test.ts:517:      delete (legacyFixture.encounter.state as { foggedCells?: unknown }).foggedCells;
tests/fixtures/arena-basis-brutal-2/seed-6207006.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":6207006,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"adjudicationPending":[],"blockedCells":[{"column":9,"row":0},{"column":9,"row":1},{"column":9,"row":2},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":9,"row":6},{"column":9,"row":7},{"column":9,"row":8},{"column":9,"row":9},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":13},{"column":9,"row":14},{"column":9,"row":15},{"column":9,"row":16},{"column":9,"row":17}],"bounds":{"columns":19,"rows":18},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":17,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":13,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":3},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":{"failures":0,"successes":2},"hitPoints":0,"life":"dying","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":4},{"level":2,"maximum":3,"remaining":2},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","limitedResources":[{"id":"monster-spell:divine-aid:bless","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:healing-word","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:sanctuary","maximum":1,"recharge":"long_rest","remaining":1}],"profile":{"id":"combatant:generated-6207006-monster-1","kind":"monster","name":"Priest Acolyte","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":10,"intelligence":10,"strength":14,"wisdom":14},"armorClass":13,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:divine-aid:bless","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:sanctuary","maximum":1,"recharge":"long_rest"}],"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":0,"intelligence":0,"strength":2,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","skillBonuses":{"medicine":4,"religion":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest-acolyte","tokenId":"token:generated-6207006-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":90,"life":"living","profile":{"id":"combatant:generated-6207006-monster-2","kind":"monster","name":"Killer Whale","rules":{"abilityScores":{"charisma":7,"constitution":13,"dexterity":14,"intelligence":3,"strength":19,"wisdom":12},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":90,"initiativeBonus":2,"passivePerception":13,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":2,"intelligence":-4,"strength":4,"wisdom":1},"senses":[{"kind":"normal_sight"},{"kind":"blindsight","rangeFeet":120}],"sizeCategory":"Huge","skillBonuses":{"perception":3,"stealth":4},"speed":5,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:killer-whale","tokenId":"token:generated-6207006-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":36,"life":"living","profile":{"id":"combatant:generated-6207006-monster-3","kind":"monster","name":"Ghast","rules":{"abilityScores":{"charisma":8,"constitution":10,"dexterity":17,"intelligence":11,"strength":16,"wisdom":10},"armorClass":13,"attacksPerAction":1,"conditionImmunities":["Charmed","Exhaustion","Poisoned"],"contactMedium":"surface","creatureType":"Undead","damageResponses":[{"response":"resistant","type":"Necrotic"},{"response":"immune","type":"Poison"}],"detectionTraits":[],"hitPointMaximum":36,"initiativeBonus":3,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-1,"constitution":0,"dexterity":3,"intelligence":0,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Medium","speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:ghast","tokenId":"token:generated-6207006-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","profile":{"id":"combatant:generated-6207006-monster-4","kind":"monster","name":"Ironweb Weaver","rules":{"abilityScores":{"charisma":6,"constitution":15,"dexterity":15,"intelligence":3,"strength":18,"wisdom":12},"armorClass":14,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":["web_sense"],"hitPointMaximum":38,"initiativeBonus":2,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":2,"dexterity":2,"intelligence":-4,"strength":4,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Large","speed":36,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/ironweb-weaver","tokenId":"token:generated-6207006-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":8,"life":"living","profile":{"id":"combatant:generated-6207006-monster-5","kind":"monster","name":"Cliff Glider","rules":{"abilityScores":{"charisma":6,"constitution":12,"dexterity":12,"intelligence":3,"strength":12,"wisdom":12},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":["flyby"],"hitPointMaximum":8,"initiativeBonus":1,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":1,"intelligence":-4,"strength":1,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":10,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/cliff-glider","tokenId":"token:generated-6207006-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 6207006."],"effects":[{"concentrationOwner":"combatant:cleric","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:cleric","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:cleric"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":7,"row":11},{"column":7,"row":12},{"column":7,"row":13},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":10,"row":11},{"column":10,"row":12},{"column":10,"row":13},{"column":11,"row":11},{"column":11,"row":12},{"column":11,"row":13}],"id":"difficult:generated-6207006-approach"},{"cells":[{"column":4,"row":11},{"column":5,"row":11},{"column":6,"row":11},{"column":4,"row":12},{"column":5,"row":12},{"column":6,"row":12}],"id":"difficult:generated-6207006-brutal-1"},{"cells":[{"column":5,"row":2},{"column":6,"row":2},{"column":7,"row":2},{"column":5,"row":3},{"column":6,"row":3},{"column":7,"row":3}],"id":"difficult:generated-6207006-brutal-2"}],"lightRegions":[{"cells":[{"column":12,"row":12},{"column":13,"row":12},{"column":12,"row":13},{"column":13,"row":13},{"column":10,"row":14},{"column":11,"row":14},{"column":12,"row":14},{"column":13,"row":14}],"id":"light:world-object:generated-6207006-5","level":"dim"},{"cells":[{"column":12,"row":4},{"column":13,"row":4},{"column":12,"row":5},{"column":13,"row":5},{"column":12,"row":6},{"column":13,"row":6}],"id":"light:world-object:generated-6207006-6","level":"bright"}],"movementRegions":[],"narrowOpeningRegions":[],"obscurementRegions":[{"cells":[{"column":8,"row":10},{"column":10,"row":10},{"column":11,"row":10}],"id":"obscurement:generated-6207006-1","obscurement":"light"},{"cells":[{"column":13,"row":11},{"column":14,"row":11},{"column":15,"row":11},{"column":13,"row":12},{"column":14,"row":12},{"column":15,"row":12}],"id":"obscurement:generated-6207006-2","obscurement":"heavy"}]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":2,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"observationHistory":[],"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","sharedSpaceRelations":[],"tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":6}},{"combatantId":"combatant:generated-6207006-monster-1","id":"token:generated-6207006-monster-1","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":17,"row":1}},{"combatantId":"combatant:generated-6207006-monster-2","id":"token:generated-6207006-monster-2","placementMode":{"actual":"Huge","kind":"normal"},"position":{"column":15,"row":2}},{"combatantId":"combatant:generated-6207006-monster-3","id":"token:generated-6207006-monster-3","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":18,"row":2}},{"combatantId":"combatant:generated-6207006-monster-4","id":"token:generated-6207006-monster-4","placementMode":{"actual":"Large","kind":"normal"},"position":{"column":14,"row":5}},{"combatantId":"combatant:generated-6207006-monster-5","id":"token:generated-6207006-monster-5","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":17,"row":5}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":10,"row":2}],"id":"world-object:generated-6207006-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":10,"row":2}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":16}],"id":"world-object:generated-6207006-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":4,"row":16}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":8,"row":4}],"id":"world-object:generated-6207006-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":8,"row":4}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":2}],"id":"world-object:generated-6207006-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":13,"row":2}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":2,"row":16}],"id":"world-object:generated-6207006-5","kind":"light-source","name":"Runed Brazier","position":{"column":2,"row":16}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":9}],"id":"world-object:generated-6207006-6","kind":"light-source","name":"Runed Brazier","position":{"column":13,"row":9}}]}},"spec":{"blockedCells":[{"column":9,"row":0},{"column":9,"row":1},{"column":9,"row":2},{"column":9,"row":3},{"column":9,"row":4},{"column":9,"row":5},{"column":9,"row":6},{"column":9,"row":7},{"column":9,"row":8},{"column":9,"row":9},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":13},{"column":9,"row":14},{"column":9,"row":15},{"column":9,"row":16},{"column":9,"row":17}],"challengeBudgetEighths":60,"challengeSpentEighths":60,"difficultyProfile":"brutal","dimensions":{"columns":19,"rows":18},"hardFeatures":{"chokepointCells":[{"column":9,"row":12}],"likelyApproachTerrainRegionIds":["difficult:generated-6207006-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":2,"challengeRating":"1/4","combatantId":"combatant:generated-6207006-monster-1","statblockId":"statblock:priest-acolyte","tokenId":"token:generated-6207006-monster-1"},{"challengeEighths":24,"challengeRating":3,"combatantId":"combatant:generated-6207006-monster-2","statblockId":"statblock:killer-whale","tokenId":"token:generated-6207006-monster-2"},{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-6207006-monster-3","statblockId":"statblock:ghast","tokenId":"token:generated-6207006-monster-3"},{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-6207006-monster-4","statblockId":"statblock:homebrew-beast/ironweb-weaver","tokenId":"token:generated-6207006-monster-4"},{"challengeEighths":2,"challengeRating":"1/4","combatantId":"combatant:generated-6207006-monster-5","statblockId":"statblock:homebrew-beast/cliff-glider","tokenId":"token:generated-6207006-monster-5"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":17,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":true,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":13,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":3,"spent":0},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":{"failures":0,"successes":2},"hitPointFraction":0,"hitPoints":0,"life":"dying","spellSlots":[{"level":1,"maximum":4,"remaining":4,"spent":0},{"level":2,"maximum":3,"remaining":2,"spent":1},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":6207006,"terrain":[{"cells":[{"column":7,"row":11},{"column":7,"row":12},{"column":7,"row":13},{"column":8,"row":11},{"column":8,"row":12},{"column":8,"row":13},{"column":10,"row":11},{"column":10,"row":12},{"column":10,"row":13},{"column":11,"row":11},{"column":11,"row":12},{"column":11,"row":13}],"id":"difficult:generated-6207006-approach","kind":"difficult-terrain-patch"},{"cells":[{"column":4,"row":11},{"column":5,"row":11},{"column":6,"row":11},{"column":4,"row":12},{"column":5,"row":12},{"column":6,"row":12}],"id":"difficult:generated-6207006-brutal-1","kind":"difficult-terrain-patch"},{"cells":[{"column":5,"row":2},{"column":6,"row":2},{"column":7,"row":2},{"column":5,"row":3},{"column":6,"row":3},{"column":7,"row":3}],"id":"difficult:generated-6207006-brutal-2","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":10,"row":2}],"id":"world-object:generated-6207006-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":10,"row":2}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":16}],"id":"world-object:generated-6207006-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":4,"row":16}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":8,"row":4}],"id":"world-object:generated-6207006-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":8,"row":4}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":2}],"id":"world-object:generated-6207006-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":13,"row":2}}},{"cells":[{"column":12,"row":12},{"column":13,"row":12},{"column":12,"row":13},{"column":13,"row":13},{"column":10,"row":14},{"column":11,"row":14},{"column":12,"row":14},{"column":13,"row":14}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":2,"row":16}],"id":"world-object:generated-6207006-5","kind":"light-source","name":"Runed Brazier","position":{"column":2,"row":16}}},{"cells":[{"column":12,"row":4},{"column":13,"row":4},{"column":12,"row":5},{"column":13,"row":5},{"column":12,"row":6},{"column":13,"row":6}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":9}],"id":"world-object:generated-6207006-6","kind":"light-source","name":"Runed Brazier","position":{"column":13,"row":9}}},{"cells":[{"column":8,"row":10},{"column":10,"row":10},{"column":11,"row":10}],"id":"obscurement:generated-6207006-1","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":13,"row":11},{"column":14,"row":11},{"column":15,"row":11},{"column":13,"row":12},{"column":14,"row":12},{"column":15,"row":12}],"id":"obscurement:generated-6207006-2","kind":"obscurement-patch","obscurement":"heavy"}]}}
tests/fixtures/arena-basis-hard/seed-5117013.json:1:{"encounter":{"schemaVersion":1,"provenance":{"source":"generated","licenseTag":"MIT","seed":5117013},"state":{"config":{"initiativeMode":"shared_enemy"},"phase":{"kind":"active"},"rulesEdition":"2024","hiddenRolls":[],"revision":0,"nextEventSequence":1,"nextDecisionSequence":1,"nextEffectSequence":1,"bounds":{"columns":24,"rows":15},"blockedCells":[{"column":12,"row":0},{"column":12,"row":1},{"column":12,"row":2},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5},{"column":12,"row":7},{"column":12,"row":8},{"column":12,"row":9},{"column":12,"row":10},{"column":12,"row":11},{"column":12,"row":12},{"column":12,"row":13},{"column":12,"row":14}],"worldObjects":[{"id":"world-object:generated-5117013-1","name":"Unstable Arcane Vent","kind":"hazard","position":{"column":8,"row":11},"footprint":[{"column":8,"row":11}],"durability":{"kind":"indestructible"},"armorClass":15,"damageResponses":[],"blocking":{"movement":false,"lineOfSight":false,"cover":"none"},"createdRevision":0},{"id":"world-object:generated-5117013-2","name":"Runed Brazier","kind":"light-source","position":{"column":9,"row":1},"footprint":[{"column":9,"row":1}],"durability":{"kind":"indestructible"},"armorClass":15,"damageResponses":[],"blocking":{"movement":false,"lineOfSight":false,"cover":"none"},"createdRevision":0}],"nextWorldObjectSequence":1,"environment":{"difficultTerrainRegions":[{"id":"difficult:generated-5117013-approach","cells":[{"column":10,"row":5},{"column":10,"row":6},{"column":10,"row":7},{"column":11,"row":5},{"column":11,"row":6},{"column":11,"row":7},{"column":13,"row":5},{"column":13,"row":6},{"column":13,"row":7},{"column":14,"row":5},{"column":14,"row":6},{"column":14,"row":7}]}],"lightRegions":[{"id":"light:world-object:generated-5117013-2","cells":[{"column":13,"row":8},{"column":14,"row":8},{"column":15,"row":8},{"column":13,"row":9},{"column":14,"row":9},{"column":15,"row":9},{"column":13,"row":10},{"column":14,"row":10},{"column":15,"row":10},{"column":13,"row":11},{"column":14,"row":11},{"column":15,"row":11}],"level":"dim"}],"obscurementRegions":[],"movementRegions":[]},"foggedCells":[],"dmNotes":["Deterministic generated room seed 5117013."],"combatants":[{"profile":{"id":"combatant:fighter","tokenId":"token:fighter","name":"Reference Fighter","rules":{"armorClass":18,"hitPointMaximum":67,"speed":30,"initiativeBonus":100,"savingThrowBonuses":{"strength":3,"dexterity":2,"constitution":3,"intelligence":1,"wisdom":2,"charisma":0},"attacksPerAction":2,"reach":5,"damageResponses":[],"conditionImmunities":[],"usesDeathSaves":true,"senses":[{"kind":"normal_sight"}],"passivePerception":12,"detectionTraits":[],"contactMedium":"surface","spellSlots":[]},"kind":"player_character","characterId":1,"wildShape":null},"hitPoints":67,"life":"living","deathSaves":null,"deathAt":null,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"reactionAvailable":false,"movement":{"speed":0,"spent":0,"remaining":0},"disengaging":false,"dodging":false},"temporaryHitPoints":0,"wildShapeUses":null,"spellSlots":[]},{"profile":{"id":"combatant:cleric","tokenId":"token:cleric","name":"Reference Cleric","rules":{"armorClass":18,"hitPointMaximum":52,"speed":30,"initiativeBonus":70,"savingThrowBonuses":{"strength":3,"dexterity":2,"constitution":3,"intelligence":1,"wisdom":2,"charisma":0},"attacksPerAction":1,"reach":5,"damageResponses":[],"conditionImmunities":[],"usesDeathSaves":true,"senses":[{"kind":"normal_sight"}],"passivePerception":12,"detectionTraits":[],"contactMedium":"surface","spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}]},"kind":"player_character","characterId":2,"wildShape":null},"hitPoints":39,"life":"living","deathSaves":null,"deathAt":null,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"reactionAvailable":false,"movement":{"speed":0,"spent":0,"remaining":0},"disengaging":false,"dodging":false},"temporaryHitPoints":0,"wildShapeUses":null,"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":3},{"level":4,"maximum":1,"remaining":1}]},{"profile":{"id":"combatant:wizard","tokenId":"token:wizard","name":"Reference Wizard","rules":{"armorClass":15,"hitPointMaximum":38,"speed":30,"initiativeBonus":40,"savingThrowBonuses":{"strength":3,"dexterity":2,"constitution":3,"intelligence":1,"wisdom":2,"charisma":0},"attacksPerAction":1,"reach":5,"damageResponses":[],"conditionImmunities":[],"usesDeathSaves":true,"senses":[{"kind":"normal_sight"}],"passivePerception":12,"detectionTraits":[],"contactMedium":"surface","spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}]},"kind":"player_character","characterId":3,"wildShape":null},"hitPoints":38,"life":"living","deathSaves":null,"deathAt":null,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"reactionAvailable":false,"movement":{"speed":0,"spent":0,"remaining":0},"disengaging":false,"dodging":false},"temporaryHitPoints":0,"wildShapeUses":null,"spellSlots":[{"level":1,"maximum":4,"remaining":2},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":1}]},{"profile":{"kind":"monster","id":"combatant:generated-5117013-monster-1","tokenId":"token:generated-5117013-monster-1","name":"Priest","statblockId":"statblock:priest","rules":{"armorClass":13,"hitPointMaximum":38,"speed":30,"initiativeBonus":0,"abilityScores":{"strength":16,"dexterity":10,"constitution":12,"intelligence":13,"wisdom":16,"charisma":13},"proficiencyBonus":2,"savingThrowBonuses":{"strength":3,"dexterity":0,"constitution":1,"intelligence":1,"wisdom":3,"charisma":1},"attacksPerAction":2,"reach":5,"damageResponses":[],"conditionImmunities":[],"usesDeathSaves":false,"senses":[{"kind":"normal_sight"}],"passivePerception":15,"detectionTraits":[],"contactMedium":"surface","creatureType":"Humanoid","spellSlots":[],"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5}}},"hitPoints":38,"life":"living","deathSaves":null,"deathAt":null,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"reactionAvailable":false,"movement":{"speed":0,"spent":0,"remaining":0},"disengaging":false,"dodging":false},"temporaryHitPoints":0,"wildShapeUses":null,"spellSlots":[],"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}]},{"profile":{"kind":"monster","id":"combatant:generated-5117013-monster-2","tokenId":"token:generated-5117013-monster-2","name":"Guard","statblockId":"statblock:guard","rules":{"armorClass":16,"hitPointMaximum":11,"speed":30,"initiativeBonus":1,"abilityScores":{"strength":13,"dexterity":12,"constitution":12,"intelligence":10,"wisdom":11,"charisma":10},"proficiencyBonus":2,"savingThrowBonuses":{"strength":1,"dexterity":1,"constitution":1,"intelligence":0,"wisdom":0,"charisma":0},"attacksPerAction":1,"reach":5,"damageResponses":[],"conditionImmunities":[],"usesDeathSaves":false,"senses":[{"kind":"normal_sight"}],"passivePerception":12,"detectionTraits":[],"contactMedium":"surface","creatureType":"Humanoid","spellSlots":[],"skillBonuses":{"perception":2}}},"hitPoints":11,"life":"living","deathSaves":null,"deathAt":null,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"reactionAvailable":false,"movement":{"speed":0,"spent":0,"remaining":0},"disengaging":false,"dodging":false},"temporaryHitPoints":0,"wildShapeUses":null,"spellSlots":[]},{"profile":{"kind":"monster","id":"combatant:generated-5117013-monster-3","tokenId":"token:generated-5117013-monster-3","name":"Scout","statblockId":"statblock:scout","rules":{"armorClass":13,"hitPointMaximum":16,"speed":30,"initiativeBonus":2,"abilityScores":{"strength":11,"dexterity":14,"constitution":12,"intelligence":11,"wisdom":13,"charisma":11},"proficiencyBonus":2,"savingThrowBonuses":{"strength":0,"dexterity":2,"constitution":1,"intelligence":0,"wisdom":1,"charisma":0},"attacksPerAction":2,"reach":5,"damageResponses":[],"conditionImmunities":[],"usesDeathSaves":false,"senses":[{"kind":"normal_sight"}],"passivePerception":15,"detectionTraits":[],"contactMedium":"surface","creatureType":"Humanoid","spellSlots":[],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5}}},"hitPoints":16,"life":"living","deathSaves":null,"deathAt":null,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"reactionAvailable":false,"movement":{"speed":0,"spent":0,"remaining":0},"disengaging":false,"dodging":false},"temporaryHitPoints":0,"wildShapeUses":null,"spellSlots":[]},{"profile":{"kind":"monster","id":"combatant:generated-5117013-monster-4","tokenId":"token:generated-5117013-monster-4","name":"Tough","statblockId":"statblock:tough","rules":{"armorClass":12,"hitPointMaximum":32,"speed":30,"initiativeBonus":1,"abilityScores":{"strength":15,"dexterity":12,"constitution":14,"intelligence":10,"wisdom":10,"charisma":11},"proficiencyBonus":2,"savingThrowBonuses":{"strength":2,"dexterity":1,"constitution":2,"intelligence":0,"wisdom":0,"charisma":0},"attacksPerAction":1,"reach":5,"damageResponses":[],"conditionImmunities":[],"usesDeathSaves":false,"senses":[{"kind":"normal_sight"}],"passivePerception":10,"detectionTraits":[],"contactMedium":"surface","creatureType":"Humanoid","spellSlots":[]}},"hitPoints":32,"life":"living","deathSaves":null,"deathAt":null,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"reactionAvailable":false,"movement":{"speed":0,"spent":0,"remaining":0},"disengaging":false,"dodging":false},"temporaryHitPoints":0,"wildShapeUses":null,"spellSlots":[]}],"tokens":[{"id":"token:fighter","combatantId":"combatant:fighter","position":{"column":1,"row":2}},{"id":"token:cleric","combatantId":"combatant:cleric","position":{"column":2,"row":4}},{"id":"token:wizard","combatantId":"combatant:wizard","position":{"column":1,"row":6}},{"id":"token:generated-5117013-monster-1","combatantId":"combatant:generated-5117013-monster-1","position":{"column":22,"row":1}},{"id":"token:generated-5117013-monster-2","combatantId":"combatant:generated-5117013-monster-2","position":{"column":21,"row":1}},{"id":"token:generated-5117013-monster-3","combatantId":"combatant:generated-5117013-monster-3","position":{"column":22,"row":3}},{"id":"token:generated-5117013-monster-4","combatantId":"combatant:generated-5117013-monster-4","position":{"column":21,"row":3}}],"initiative":[],"activeCombatant":null,"activeInitiativeIndex":null,"round":0,"effects":[],"persistentAreas":[],"nextPersistentAreaSequence":1,"eventLog":[],"hiddenCombatants":[],"pendingDecisions":[],"reactionPolicies":[]}},"spec":{"seed":5117013,"difficultyProfile":"hard","dimensions":{"columns":24,"rows":15},"terrain":[{"kind":"difficult-terrain-patch","id":"difficult:generated-5117013-approach","cells":[{"column":10,"row":5},{"column":10,"row":6},{"column":10,"row":7},{"column":11,"row":5},{"column":11,"row":6},{"column":11,"row":7},{"column":13,"row":5},{"column":13,"row":6},{"column":13,"row":7},{"column":14,"row":5},{"column":14,"row":6},{"column":14,"row":7}]},{"kind":"hazard-object","object":{"id":"world-object:generated-5117013-1","name":"Unstable Arcane Vent","kind":"hazard","position":{"column":8,"row":11},"footprint":[{"column":8,"row":11}],"durability":{"kind":"indestructible"},"armorClass":15,"damageResponses":[],"blocking":{"movement":false,"lineOfSight":false,"cover":"none"},"createdRevision":0}},{"kind":"light-source","object":{"id":"world-object:generated-5117013-2","name":"Runed Brazier","kind":"light-source","position":{"column":9,"row":1},"footprint":[{"column":9,"row":1}],"durability":{"kind":"indestructible"},"armorClass":15,"damageResponses":[],"blocking":{"movement":false,"lineOfSight":false,"cover":"none"},"createdRevision":0},"level":"dim","cells":[{"column":13,"row":8},{"column":14,"row":8},{"column":15,"row":8},{"column":13,"row":9},{"column":14,"row":9},{"column":15,"row":9},{"column":13,"row":10},{"column":14,"row":10},{"column":15,"row":10},{"column":13,"row":11},{"column":14,"row":11},{"column":15,"row":11}]}],"blockedCells":[{"column":12,"row":0},{"column":12,"row":1},{"column":12,"row":2},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5},{"column":12,"row":7},{"column":12,"row":8},{"column":12,"row":9},{"column":12,"row":10},{"column":12,"row":11},{"column":12,"row":12},{"column":12,"row":13},{"column":12,"row":14}],"monsterRoster":[{"combatantId":"combatant:generated-5117013-monster-1","tokenId":"token:generated-5117013-monster-1","statblockId":"statblock:priest","challengeRating":2,"challengeEighths":16},{"combatantId":"combatant:generated-5117013-monster-2","tokenId":"token:generated-5117013-monster-2","statblockId":"statblock:guard","challengeRating":"1/8","challengeEighths":1},{"combatantId":"combatant:generated-5117013-monster-3","tokenId":"token:generated-5117013-monster-3","statblockId":"statblock:scout","challengeRating":"1/2","challengeEighths":4},{"combatantId":"combatant:generated-5117013-monster-4","tokenId":"token:generated-5117013-monster-4","statblockId":"statblock:tough","challengeRating":"1/2","challengeEighths":4}],"challengeBudgetEighths":29,"challengeSpentEighths":25,"partyState":[{"combatantId":"combatant:fighter","hitPointFraction":1,"hitPoints":67,"life":"living","deathSaves":null,"spellSlots":[],"concentrating":false},{"combatantId":"combatant:cleric","hitPointFraction":0.75,"hitPoints":39,"life":"living","deathSaves":null,"spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":3,"spent":0},{"level":4,"maximum":1,"remaining":1,"spent":0}],"concentrating":false},{"combatantId":"combatant:wizard","hitPointFraction":1,"hitPoints":38,"life":"living","deathSaves":null,"spellSlots":[{"level":1,"maximum":4,"remaining":2,"spent":2},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":1,"spent":0}],"concentrating":false}],"hardFeatures":{"shape":"single-gate","chokepointCells":[{"column":12,"row":6}],"likelyApproachTerrainRegionIds":["difficult:generated-5117013-approach"]}}}
tests/fixtures/arena-basis-brutal-2/seed-6207003.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":6207003,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"adjudicationPending":[],"blockedCells":[{"column":6,"row":0},{"column":6,"row":1},{"column":6,"row":2},{"column":6,"row":4},{"column":6,"row":5},{"column":6,"row":6},{"column":6,"row":7},{"column":6,"row":8},{"column":6,"row":9},{"column":6,"row":10},{"column":6,"row":11}],"bounds":{"columns":13,"rows":12},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":17,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":13,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":0},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":52,"life":"living","profile":{"id":"combatant:generated-6207003-monster-1","kind":"monster","name":"Doppelganger","rules":{"abilityScores":{"charisma":14,"constitution":14,"dexterity":18,"intelligence":11,"strength":11,"wisdom":12},"armorClass":14,"attacksPerAction":2,"conditionImmunities":["Charmed"],"contactMedium":"surface","creatureType":"Monstrosity","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":4,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":2,"constitution":2,"dexterity":4,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Medium","skillBonuses":{"deception":6,"insight":3},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:doppelganger","tokenId":"token:generated-6207003-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":78,"life":"living","profile":{"id":"combatant:generated-6207003-monster-2","kind":"monster","name":"Ironhorn Saurian","rules":{"abilityScores":{"charisma":6,"constitution":17,"dexterity":16,"intelligence":3,"strength":22,"wisdom":12},"armorClass":14,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":78,"initiativeBonus":3,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":3,"dexterity":3,"intelligence":-4,"strength":6,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Huge","speed":40,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/ironhorn-saurian","tokenId":"token:generated-6207003-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-6207003-monster-3","kind":"monster","name":"Wolf","rules":{"abilityScores":{"charisma":6,"constitution":12,"dexterity":15,"intelligence":3,"strength":14,"wisdom":12},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":2,"intelligence":-4,"strength":2,"wisdom":1},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Medium","skillBonuses":{"perception":5,"stealth":4},"speed":40,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:wolf","tokenId":"token:generated-6207003-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":15,"life":"living","profile":{"id":"combatant:generated-6207003-monster-4","kind":"monster","name":"Crag Eagle","rules":{"abilityScores":{"charisma":6,"constitution":13,"dexterity":13,"intelligence":3,"strength":14,"wisdom":12},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":["keen_sight","flyby"],"hitPointMaximum":15,"initiativeBonus":1,"passivePerception":14,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":1,"intelligence":-4,"strength":2,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","skillBonuses":{"perception":4},"speed":10,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/crag-eagle","tokenId":"token:generated-6207003-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 6207003."],"effects":[{"concentrationOwner":"combatant:wizard","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:wizard","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:wizard"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":4,"row":2},{"column":4,"row":3},{"column":4,"row":4},{"column":5,"row":2},{"column":5,"row":3},{"column":5,"row":4},{"column":7,"row":2},{"column":7,"row":3},{"column":7,"row":4},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4}],"id":"difficult:generated-6207003-approach"},{"cells":[{"column":4,"row":5},{"column":5,"row":5},{"column":4,"row":6},{"column":5,"row":6},{"column":4,"row":7},{"column":5,"row":7},{"column":4,"row":8},{"column":5,"row":8}],"id":"difficult:generated-6207003-brutal-1"},{"cells":[{"column":7,"row":7},{"column":8,"row":7},{"column":7,"row":8},{"column":8,"row":8},{"column":7,"row":9},{"column":8,"row":9}],"id":"difficult:generated-6207003-brutal-2"}],"lightRegions":[{"cells":[{"column":3,"row":1},{"column":4,"row":1},{"column":5,"row":1},{"column":3,"row":2},{"column":3,"row":3},{"column":6,"row":3}],"id":"light:world-object:generated-6207003-5","level":"bright"},{"cells":[{"column":7,"row":5},{"column":8,"row":5},{"column":9,"row":5},{"column":7,"row":6},{"column":8,"row":6},{"column":9,"row":6},{"column":7,"row":7},{"column":8,"row":7},{"column":9,"row":7},{"column":7,"row":8},{"column":8,"row":8}],"id":"light:world-object:generated-6207003-6","level":"dim"}],"movementRegions":[],"narrowOpeningRegions":[],"obscurementRegions":[{"cells":[{"column":7,"row":1},{"column":6,"row":3}],"id":"obscurement:generated-6207003-1","obscurement":"light"},{"cells":[{"column":5,"row":9},{"column":7,"row":9},{"column":4,"row":10},{"column":5,"row":10},{"column":7,"row":10}],"id":"obscurement:generated-6207003-2","obscurement":"light"}]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":2,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"observationHistory":[],"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","sharedSpaceRelations":[],"tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":6}},{"combatantId":"combatant:generated-6207003-monster-1","id":"token:generated-6207003-monster-1","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":11,"row":1}},{"combatantId":"combatant:generated-6207003-monster-2","id":"token:generated-6207003-monster-2","placementMode":{"actual":"Huge","kind":"normal"},"position":{"column":9,"row":2}},{"combatantId":"combatant:generated-6207003-monster-3","id":"token:generated-6207003-monster-3","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":12,"row":2}},{"combatantId":"combatant:generated-6207003-monster-4","id":"token:generated-6207003-monster-4","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":8,"row":1}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":9}],"id":"world-object:generated-6207003-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":4,"row":9}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":7}],"id":"world-object:generated-6207003-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":7}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":2}],"id":"world-object:generated-6207003-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":2}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":8}],"id":"world-object:generated-6207003-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":8}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":8}],"id":"world-object:generated-6207003-5","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":8}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":3}],"id":"world-object:generated-6207003-6","kind":"light-source","name":"Runed Brazier","position":{"column":1,"row":3}}]}},"spec":{"blockedCells":[{"column":6,"row":0},{"column":6,"row":1},{"column":6,"row":2},{"column":6,"row":4},{"column":6,"row":5},{"column":6,"row":6},{"column":6,"row":7},{"column":6,"row":8},{"column":6,"row":9},{"column":6,"row":10},{"column":6,"row":11}],"challengeBudgetEighths":62,"challengeSpentEighths":62,"difficultyProfile":"brutal","dimensions":{"columns":13,"rows":12},"hardFeatures":{"chokepointCells":[{"column":6,"row":3}],"likelyApproachTerrainRegionIds":["difficult:generated-6207003-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":24,"challengeRating":3,"combatantId":"combatant:generated-6207003-monster-1","statblockId":"statblock:doppelganger","tokenId":"token:generated-6207003-monster-1"},{"challengeEighths":32,"challengeRating":4,"combatantId":"combatant:generated-6207003-monster-2","statblockId":"statblock:homebrew-beast/ironhorn-saurian","tokenId":"token:generated-6207003-monster-2"},{"challengeEighths":2,"challengeRating":"1/4","combatantId":"combatant:generated-6207003-monster-3","statblockId":"statblock:wolf","tokenId":"token:generated-6207003-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-6207003-monster-4","statblockId":"statblock:homebrew-beast/crag-eagle","tokenId":"token:generated-6207003-monster-4"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":17,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":13,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":true,"deathSaves":null,"hitPointFraction":1,"hitPoints":38,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":0,"spent":4},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]}],"seed":6207003,"terrain":[{"cells":[{"column":4,"row":2},{"column":4,"row":3},{"column":4,"row":4},{"column":5,"row":2},{"column":5,"row":3},{"column":5,"row":4},{"column":7,"row":2},{"column":7,"row":3},{"column":7,"row":4},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4}],"id":"difficult:generated-6207003-approach","kind":"difficult-terrain-patch"},{"cells":[{"column":4,"row":5},{"column":5,"row":5},{"column":4,"row":6},{"column":5,"row":6},{"column":4,"row":7},{"column":5,"row":7},{"column":4,"row":8},{"column":5,"row":8}],"id":"difficult:generated-6207003-brutal-1","kind":"difficult-terrain-patch"},{"cells":[{"column":7,"row":7},{"column":8,"row":7},{"column":7,"row":8},{"column":8,"row":8},{"column":7,"row":9},{"column":8,"row":9}],"id":"difficult:generated-6207003-brutal-2","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":9}],"id":"world-object:generated-6207003-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":4,"row":9}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":7}],"id":"world-object:generated-6207003-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":7}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":11,"row":2}],"id":"world-object:generated-6207003-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":11,"row":2}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":8}],"id":"world-object:generated-6207003-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":8}}},{"cells":[{"column":3,"row":1},{"column":4,"row":1},{"column":5,"row":1},{"column":3,"row":2},{"column":3,"row":3},{"column":6,"row":3}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":9,"row":8}],"id":"world-object:generated-6207003-5","kind":"light-source","name":"Runed Brazier","position":{"column":9,"row":8}}},{"cells":[{"column":7,"row":5},{"column":8,"row":5},{"column":9,"row":5},{"column":7,"row":6},{"column":8,"row":6},{"column":9,"row":6},{"column":7,"row":7},{"column":8,"row":7},{"column":9,"row":7},{"column":7,"row":8},{"column":8,"row":8}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":3}],"id":"world-object:generated-6207003-6","kind":"light-source","name":"Runed Brazier","position":{"column":1,"row":3}}},{"cells":[{"column":7,"row":1},{"column":6,"row":3}],"id":"obscurement:generated-6207003-1","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":5,"row":9},{"column":7,"row":9},{"column":4,"row":10},{"column":5,"row":10},{"column":7,"row":10}],"id":"obscurement:generated-6207003-2","kind":"obscurement-patch","obscurement":"light"}]}}
tests/fixtures/arena-basis-hard/seed-5117004.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117004,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":8,"row":0},{"column":8,"row":1},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":6},{"column":8,"row":7},{"column":8,"row":8},{"column":8,"row":9},{"column":8,"row":10},{"column":8,"row":12},{"column":8,"row":13},{"column":8,"row":14}],"bounds":{"columns":17,"rows":15},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":17,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":26,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":2},{"level":2,"maximum":3,"remaining":0},{"level":3,"maximum":3,"remaining":3},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":0},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117004-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117004-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117004-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117004-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117004-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117004-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117004-monster-4","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117004-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117004-monster-5","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117004-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":32,"life":"living","profile":{"id":"combatant:generated-5117004-monster-6","kind":"monster","name":"Tough","rules":{"abilityScores":{"charisma":11,"constitution":14,"dexterity":12,"intelligence":10,"strength":15,"wisdom":10},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":32,"initiativeBonus":1,"passivePerception":10,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":2,"dexterity":1,"intelligence":0,"strength":2,"wisdom":0},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:tough","tokenId":"token:generated-5117004-monster-6"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117004-monster-7","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117004-monster-7"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117004."],"effects":[],"environment":{"difficultTerrainRegions":[{"cells":[{"column":6,"row":10},{"column":6,"row":11},{"column":6,"row":12},{"column":7,"row":10},{"column":7,"row":11},{"column":7,"row":12},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":12},{"column":10,"row":10},{"column":10,"row":11},{"column":10,"row":12}],"id":"difficult:generated-5117004-approach"}],"lightRegions":[{"cells":[{"column":8,"row":11}],"id":"light:world-object:generated-5117004-2","level":"bright"},{"cells":[{"column":4,"row":6},{"column":6,"row":6},{"column":4,"row":7},{"column":5,"row":7},{"column":6,"row":7},{"column":4,"row":8},{"column":5,"row":8},{"column":6,"row":8}],"id":"light:world-object:generated-5117004-3","level":"bright"}],"movementRegions":[],"obscurementRegions":[]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":1,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","position":{"column":1,"row":6}},{"combatantId":"combatant:generated-5117004-monster-1","id":"token:generated-5117004-monster-1","position":{"column":15,"row":1}},{"combatantId":"combatant:generated-5117004-monster-2","id":"token:generated-5117004-monster-2","position":{"column":14,"row":1}},{"combatantId":"combatant:generated-5117004-monster-3","id":"token:generated-5117004-monster-3","position":{"column":15,"row":3}},{"combatantId":"combatant:generated-5117004-monster-4","id":"token:generated-5117004-monster-4","position":{"column":14,"row":3}},{"combatantId":"combatant:generated-5117004-monster-5","id":"token:generated-5117004-monster-5","position":{"column":15,"row":5}},{"combatantId":"combatant:generated-5117004-monster-6","id":"token:generated-5117004-monster-6","position":{"column":14,"row":5}},{"combatantId":"combatant:generated-5117004-monster-7","id":"token:generated-5117004-monster-7","position":{"column":15,"row":7}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":11}],"id":"world-object:generated-5117004-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":13,"row":11}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":6,"row":9}],"id":"world-object:generated-5117004-2","kind":"light-source","name":"Runed Brazier","position":{"column":6,"row":9}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":6}],"id":"world-object:generated-5117004-3","kind":"light-source","name":"Runed Brazier","position":{"column":5,"row":6}}]}},"spec":{"blockedCells":[{"column":8,"row":0},{"column":8,"row":1},{"column":8,"row":2},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5},{"column":8,"row":6},{"column":8,"row":7},{"column":8,"row":8},{"column":8,"row":9},{"column":8,"row":10},{"column":8,"row":12},{"column":8,"row":13},{"column":8,"row":14}],"challengeBudgetEighths":36,"challengeSpentEighths":34,"difficultyProfile":"hard","dimensions":{"columns":17,"rows":15},"hardFeatures":{"chokepointCells":[{"column":8,"row":11}],"likelyApproachTerrainRegionIds":["difficult:generated-5117004-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-5117004-monster-1","statblockId":"statblock:priest","tokenId":"token:generated-5117004-monster-1"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117004-monster-2","statblockId":"statblock:guard","tokenId":"token:generated-5117004-monster-2"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117004-monster-3","statblockId":"statblock:scout","tokenId":"token:generated-5117004-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117004-monster-4","statblockId":"statblock:scout","tokenId":"token:generated-5117004-monster-4"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117004-monster-5","statblockId":"statblock:scout","tokenId":"token:generated-5117004-monster-5"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-5117004-monster-6","statblockId":"statblock:tough","tokenId":"token:generated-5117004-monster-6"},{"challengeEighths":1,"challengeRating":"1/8","combatantId":"combatant:generated-5117004-monster-7","statblockId":"statblock:guard","tokenId":"token:generated-5117004-monster-7"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":17,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.5,"hitPoints":26,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":2,"spent":2},{"level":2,"maximum":3,"remaining":0,"spent":3},{"level":3,"maximum":3,"remaining":3,"spent":0},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":38,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":0,"spent":4},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":1,"spent":0}]}],"seed":5117004,"terrain":[{"cells":[{"column":6,"row":10},{"column":6,"row":11},{"column":6,"row":12},{"column":7,"row":10},{"column":7,"row":11},{"column":7,"row":12},{"column":9,"row":10},{"column":9,"row":11},{"column":9,"row":12},{"column":10,"row":10},{"column":10,"row":11},{"column":10,"row":12}],"id":"difficult:generated-5117004-approach","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":13,"row":11}],"id":"world-object:generated-5117004-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":13,"row":11}}},{"cells":[{"column":8,"row":11}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":6,"row":9}],"id":"world-object:generated-5117004-2","kind":"light-source","name":"Runed Brazier","position":{"column":6,"row":9}}},{"cells":[{"column":4,"row":6},{"column":6,"row":6},{"column":4,"row":7},{"column":5,"row":7},{"column":6,"row":7},{"column":4,"row":8},{"column":5,"row":8},{"column":6,"row":8}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":6}],"id":"world-object:generated-5117004-3","kind":"light-source","name":"Runed Brazier","position":{"column":5,"row":6}}}]}}
tests/fixtures/arena-basis-brutal-2/seed-6207008.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":6207008,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"adjudicationPending":[],"blockedCells":[{"column":6,"row":0},{"column":6,"row":1},{"column":6,"row":2},{"column":6,"row":3},{"column":6,"row":5},{"column":6,"row":6},{"column":6,"row":7},{"column":6,"row":8},{"column":6,"row":9},{"column":6,"row":10},{"column":6,"row":11},{"column":6,"row":12},{"column":6,"row":13},{"column":6,"row":14},{"column":6,"row":15},{"column":6,"row":16},{"column":6,"row":17}],"bounds":{"columns":12,"rows":18},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":67,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":52,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":1},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":10,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":2},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":0},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":52,"life":"living","profile":{"id":"combatant:generated-6207008-monster-1","kind":"monster","name":"Doppelganger","rules":{"abilityScores":{"charisma":14,"constitution":14,"dexterity":18,"intelligence":11,"strength":11,"wisdom":12},"armorClass":14,"attacksPerAction":2,"conditionImmunities":["Charmed"],"contactMedium":"surface","creatureType":"Monstrosity","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":4,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":2,"constitution":2,"dexterity":4,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Medium","skillBonuses":{"deception":6,"insight":3},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:doppelganger","tokenId":"token:generated-6207008-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":55,"life":"living","profile":{"id":"combatant:generated-6207008-monster-2","kind":"monster","name":"Razorcrest Saurian","rules":{"abilityScores":{"charisma":6,"constitution":16,"dexterity":16,"intelligence":3,"strength":20,"wisdom":12},"armorClass":14,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":55,"initiativeBonus":3,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":3,"dexterity":3,"intelligence":-4,"strength":5,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Large","speed":38,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/razorcrest-saurian","tokenId":"token:generated-6207008-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","profile":{"id":"combatant:generated-6207008-monster-3","kind":"monster","name":"Ironweb Weaver","rules":{"abilityScores":{"charisma":6,"constitution":15,"dexterity":15,"intelligence":3,"strength":18,"wisdom":12},"armorClass":14,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":["web_sense"],"hitPointMaximum":38,"initiativeBonus":2,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":2,"dexterity":2,"intelligence":-4,"strength":4,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Large","speed":36,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/ironweb-weaver","tokenId":"token:generated-6207008-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":23,"life":"living","profile":{"id":"combatant:generated-6207008-monster-4","kind":"monster","name":"Cave Bear","rules":{"abilityScores":{"charisma":6,"constitution":13,"dexterity":13,"intelligence":3,"strength":14,"wisdom":12},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":23,"initiativeBonus":1,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":1,"intelligence":-4,"strength":2,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":32,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/cave-bear","tokenId":"token:generated-6207008-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 6207008."],"effects":[],"environment":{"difficultTerrainRegions":[{"cells":[{"column":4,"row":3},{"column":4,"row":4},{"column":4,"row":5},{"column":5,"row":3},{"column":5,"row":4},{"column":5,"row":5},{"column":7,"row":3},{"column":7,"row":4},{"column":7,"row":5},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5}],"id":"difficult:generated-6207008-approach"},{"cells":[{"column":3,"row":3},{"column":3,"row":4},{"column":6,"row":4}],"id":"difficult:generated-6207008-brutal-1"},{"cells":[{"column":6,"row":4},{"column":7,"row":6}],"id":"difficult:generated-6207008-brutal-2"},{"cells":[{"column":4,"row":7},{"column":5,"row":7},{"column":4,"row":8},{"column":5,"row":8},{"column":4,"row":9},{"column":5,"row":9}],"id":"difficult:generated-6207008-brutal-3"}],"lightRegions":[{"cells":[{"column":6,"row":4}],"id":"light:world-object:generated-6207008-5","level":"bright"},{"cells":[{"column":5,"row":7},{"column":5,"row":8},{"column":5,"row":9}],"id":"light:world-object:generated-6207008-6","level":"dim"}],"movementRegions":[],"narrowOpeningRegions":[],"obscurementRegions":[{"cells":[{"column":3,"row":10},{"column":4,"row":10},{"column":5,"row":10},{"column":3,"row":11},{"column":4,"row":11},{"column":5,"row":11},{"column":4,"row":12},{"column":5,"row":12}],"id":"obscurement:generated-6207008-1","obscurement":"heavy"},{"cells":[{"column":5,"row":12},{"column":7,"row":12},{"column":5,"row":13},{"column":7,"row":13},{"column":5,"row":14},{"column":7,"row":14}],"id":"obscurement:generated-6207008-2","obscurement":"heavy"},{"cells":[{"column":5,"row":2},{"column":7,"row":2},{"column":6,"row":4}],"id":"obscurement:generated-6207008-3","obscurement":"light"}]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":1,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"observationHistory":[],"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","sharedSpaceRelations":[],"tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":6}},{"combatantId":"combatant:generated-6207008-monster-1","id":"token:generated-6207008-monster-1","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":10,"row":1}},{"combatantId":"combatant:generated-6207008-monster-2","id":"token:generated-6207008-monster-2","placementMode":{"actual":"Large","kind":"normal"},"position":{"column":8,"row":0}},{"combatantId":"combatant:generated-6207008-monster-3","id":"token:generated-6207008-monster-3","placementMode":{"actual":"Large","kind":"normal"},"position":{"column":10,"row":3}},{"combatantId":"combatant:generated-6207008-monster-4","id":"token:generated-6207008-monster-4","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":9,"row":3}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":4}],"id":"world-object:generated-6207008-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":4}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":12}],"id":"world-object:generated-6207008-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":12}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":8,"row":10}],"id":"world-object:generated-6207008-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":8,"row":10}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":15}],"id":"world-object:generated-6207008-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":15}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":2}],"id":"world-object:generated-6207008-5","kind":"light-source","name":"Runed Brazier","position":{"column":4,"row":2}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":16}],"id":"world-object:generated-6207008-6","kind":"light-source","name":"Runed Brazier","position":{"column":3,"row":16}}]}},"spec":{"blockedCells":[{"column":6,"row":0},{"column":6,"row":1},{"column":6,"row":2},{"column":6,"row":3},{"column":6,"row":5},{"column":6,"row":6},{"column":6,"row":7},{"column":6,"row":8},{"column":6,"row":9},{"column":6,"row":10},{"column":6,"row":11},{"column":6,"row":12},{"column":6,"row":13},{"column":6,"row":14},{"column":6,"row":15},{"column":6,"row":16},{"column":6,"row":17}],"challengeBudgetEighths":68,"challengeSpentEighths":68,"difficultyProfile":"brutal","dimensions":{"columns":12,"rows":18},"hardFeatures":{"chokepointCells":[{"column":6,"row":4}],"likelyApproachTerrainRegionIds":["difficult:generated-6207008-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":24,"challengeRating":3,"combatantId":"combatant:generated-6207008-monster-1","statblockId":"statblock:doppelganger","tokenId":"token:generated-6207008-monster-1"},{"challengeEighths":24,"challengeRating":3,"combatantId":"combatant:generated-6207008-monster-2","statblockId":"statblock:homebrew-beast/razorcrest-saurian","tokenId":"token:generated-6207008-monster-2"},{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-6207008-monster-3","statblockId":"statblock:homebrew-beast/ironweb-weaver","tokenId":"token:generated-6207008-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-6207008-monster-4","statblockId":"statblock:homebrew-beast/cave-bear","tokenId":"token:generated-6207008-monster-4"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":67,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":52,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":1,"spent":3},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":2,"spent":1},{"level":4,"maximum":1,"remaining":0,"spent":1}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":0.25,"hitPoints":10,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":2,"spent":2},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":0,"spent":3},{"level":4,"maximum":1,"remaining":1,"spent":0}]}],"seed":6207008,"terrain":[{"cells":[{"column":4,"row":3},{"column":4,"row":4},{"column":4,"row":5},{"column":5,"row":3},{"column":5,"row":4},{"column":5,"row":5},{"column":7,"row":3},{"column":7,"row":4},{"column":7,"row":5},{"column":8,"row":3},{"column":8,"row":4},{"column":8,"row":5}],"id":"difficult:generated-6207008-approach","kind":"difficult-terrain-patch"},{"cells":[{"column":3,"row":3},{"column":3,"row":4},{"column":6,"row":4}],"id":"difficult:generated-6207008-brutal-1","kind":"difficult-terrain-patch"},{"cells":[{"column":6,"row":4},{"column":7,"row":6}],"id":"difficult:generated-6207008-brutal-2","kind":"difficult-terrain-patch"},{"cells":[{"column":4,"row":7},{"column":5,"row":7},{"column":4,"row":8},{"column":5,"row":8},{"column":4,"row":9},{"column":5,"row":9}],"id":"difficult:generated-6207008-brutal-3","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":4}],"id":"world-object:generated-6207008-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":4}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":12}],"id":"world-object:generated-6207008-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":3,"row":12}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":8,"row":10}],"id":"world-object:generated-6207008-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":8,"row":10}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":15}],"id":"world-object:generated-6207008-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":15}}},{"cells":[{"column":6,"row":4}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":4,"row":2}],"id":"world-object:generated-6207008-5","kind":"light-source","name":"Runed Brazier","position":{"column":4,"row":2}}},{"cells":[{"column":5,"row":7},{"column":5,"row":8},{"column":5,"row":9}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":3,"row":16}],"id":"world-object:generated-6207008-6","kind":"light-source","name":"Runed Brazier","position":{"column":3,"row":16}}},{"cells":[{"column":3,"row":10},{"column":4,"row":10},{"column":5,"row":10},{"column":3,"row":11},{"column":4,"row":11},{"column":5,"row":11},{"column":4,"row":12},{"column":5,"row":12}],"id":"obscurement:generated-6207008-1","kind":"obscurement-patch","obscurement":"heavy"},{"cells":[{"column":5,"row":12},{"column":7,"row":12},{"column":5,"row":13},{"column":7,"row":13},{"column":5,"row":14},{"column":7,"row":14}],"id":"obscurement:generated-6207008-2","kind":"obscurement-patch","obscurement":"heavy"},{"cells":[{"column":5,"row":2},{"column":7,"row":2},{"column":6,"row":4}],"id":"obscurement:generated-6207008-3","kind":"obscurement-patch","obscurement":"light"}]}}
src/vtt/encounter-state-codec.ts:35:  'contentPacks', 'equipment', 'groundItems', 'itemContacts', 'foggedCells',
src/vtt/encounter-state-codec.ts:406:  if (own(state, 'foggedCells')) {
src/vtt/encounter-state-codec.ts:407:    const foggedCells = array(state['foggedCells'], 'state.foggedCells').map((entry, index) =>
src/vtt/encounter-state-codec.ts:408:      decodeCell(entry, `state.foggedCells[${String(index)}]`));
src/vtt/encounter-state-codec.ts:409:    unique(foggedCells.map((cell) => `${String(cell.column)},${String(cell.row)}`), 'Fogged cells');
src/vtt/encounter-state-codec.ts:410:    if (foggedCells.some((cell) =>
src/vtt/encounter-state-codec.ts:414:    delete state['foggedCells'];
tests/fixtures/arena-basis-hard/seed-5117008.json:1:{"encounter":{"provenance":{"licenseTag":"MIT","seed":5117008,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"blockedCells":[{"column":12,"row":0},{"column":12,"row":1},{"column":12,"row":2},{"column":12,"row":3},{"column":12,"row":4},{"column":12,"row":5},{"column":12,"row":7},{"column":12,"row":8},{"column":12,"row":9},{"column":12,"row":10},{"column":12,"row":11},{"column":12,"row":12},{"column":12,"row":13},{"column":12,"row":14},{"column":12,"row":15},{"column":12,"row":16},{"column":12,"row":17},{"column":12,"row":18},{"column":12,"row":19},{"column":12,"row":20},{"column":12,"row":21},{"column":12,"row":22},{"column":12,"row":23}],"bounds":{"columns":24,"rows":24},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":51,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":26,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":2},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":0}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":29,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":1},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":2},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest","remaining":3},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest","remaining":3}],"profile":{"id":"combatant:generated-5117008-monster-1","kind":"monster","name":"Priest","rules":{"abilityScores":{"charisma":13,"constitution":12,"dexterity":10,"intelligence":13,"strength":16,"wisdom":16},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":0,"limitedResources":[{"id":"monster-spell:spellcasting:spirit-guardians","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:bless","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:dispel-magic","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:healing-word","maximum":3,"recharge":"long_rest"},{"id":"monster-spell:divine-aid:lesser-restoration","maximum":3,"recharge":"long_rest"}],"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":1,"constitution":1,"dexterity":0,"intelligence":1,"strength":3,"wisdom":3},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"medicine":7,"perception":5,"religion":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:priest","tokenId":"token:generated-5117008-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117008-monster-2","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117008-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":16,"life":"living","profile":{"id":"combatant:generated-5117008-monster-3","kind":"monster","name":"Scout","rules":{"abilityScores":{"charisma":11,"constitution":12,"dexterity":14,"intelligence":11,"strength":11,"wisdom":13},"armorClass":13,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":16,"initiativeBonus":2,"passivePerception":15,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":2,"intelligence":0,"strength":0,"wisdom":1},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"nature":4,"perception":5,"stealth":6,"survival":5},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:scout","tokenId":"token:generated-5117008-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117008-monster-4","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117008-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":11,"life":"living","profile":{"id":"combatant:generated-5117008-monster-5","kind":"monster","name":"Guard","rules":{"abilityScores":{"charisma":10,"constitution":12,"dexterity":12,"intelligence":10,"strength":13,"wisdom":11},"armorClass":16,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Humanoid","damageResponses":[],"detectionTraits":[],"hitPointMaximum":11,"initiativeBonus":1,"passivePerception":12,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":1,"dexterity":1,"intelligence":0,"strength":1,"wisdom":0},"senses":[{"kind":"normal_sight"}],"skillBonuses":{"perception":2},"speed":30,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:guard","tokenId":"token:generated-5117008-monster-5"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 5117008."],"effects":[{"concentrationOwner":"combatant:cleric","createdRevision":0,"damageBreak":null,"duration":{"kind":"permanent"},"id":"effect:generated-1","payload":{"amount":1,"kind":"armor_class_modifier"},"repeatedSave":null,"source":"combatant:cleric","stacking":"replace_same_source","stackingIdentity":"generated:active-concentration","targets":["combatant:cleric"]}],"environment":{"difficultTerrainRegions":[{"cells":[{"column":10,"row":5},{"column":10,"row":6},{"column":10,"row":7},{"column":11,"row":5},{"column":11,"row":6},{"column":11,"row":7},{"column":13,"row":5},{"column":13,"row":6},{"column":13,"row":7},{"column":14,"row":5},{"column":14,"row":6},{"column":14,"row":7}],"id":"difficult:generated-5117008-approach"}],"lightRegions":[{"cells":[{"column":15,"row":9},{"column":16,"row":9},{"column":1
... 808234 bytes omitted ...
 ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc 'npx vitest run --configLoader runner --reporter=verbose tests/unit/tools/d569-second-family-manifest.test.ts' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 7107ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118001.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 10ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118002.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 4ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118003.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118004.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118005.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118006.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118007.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118008.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118009.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118010.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207001.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207002.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207003.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207004.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 10ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207005.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207006.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207007.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207008.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207009.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207010.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > pins the amended exact contiguous ranges and verbatim independent ledger evidence 181ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118001.json satisfies every hard state-property invariant before its pin 5ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118002.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118003.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118004.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118005.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118006.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118007.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118008.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118009.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118010.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207001.json retains historical integrity plus static budget, caster, and terrain invariants 128ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207002.json retains historical integrity plus static budget, caster, and terrain invariants 49ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207003.json retains historical integrity plus static budget, caster, and terrain invariants 121ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207004.json retains historical integrity plus static budget, caster, and terrain invariants 261ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207005.json retains historical integrity plus static budget, caster, and terrain invariants 87ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207006.json retains historical integrity plus static budget, caster, and terrain invariants 77ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207007.json retains historical integrity plus static budget, caster, and terrain invariants 245ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207008.json retains historical integrity plus static budget, caster, and terrain invariants 94ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207009.json retains historical integrity plus static budget, caster, and terrain invariants 73ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207010.json retains historical integrity plus static budget, caster, and terrain invariants 91ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > second_family_uses_primary_seed: rejects primary or previously used seed namespaces 205ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > brutal_membership_checks_name_only: rejects structural failures even with the roster names intact 86ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > rejects missing or rewritten supervisor ledger evidence 95ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > rejects fixture bytes that no longer match the manifest hash 86ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > seed_hash_without_properties: a matching digest cannot excuse failed membership 85ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > second_generation_not_compared: rejects a divergent independent regeneration 86ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > legacy_projection_drops_all_state: retains any second committed state difference 86ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > legacy_projection_requires_the_committed_legacy_key 84ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key 1208ms

 Test Files  1 passed (1)
      Tests  50 passed (50)
   Start at  21:12:21
   Duration  6.79s (transform 1.22s, setup 0ms, import 1.82s, tests 3.52s, environment 0ms)


diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc 'npx vitest run --configLoader runner --reporter=verbose tests/unit/tools/d569-v5.test.ts' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 19255ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json

⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-v5.test.ts [ tests/unit/tools/d569-v5.test.ts ]
TypeError: Registered context caps mismatch at 1:1.
 ❯ tools/d569-v5/validate-first-arm.ts:369:13
    367|     const semanticCap = current ? caps['semanticBytes'] : caps['config…
    368|     if (baseCap !== cell.baseContextCapBytes || semanticCap !== cell.s…
    369|       throw new TypeError(`Registered context caps mismatch at ${obser…
       |             ^
    370|     }
    371|     const truncatedBlocks = row['turnContextBudget'] === undefined
 ❯ validateD569RegisteredFirstArm tools/d569-v5/validate-first-arm.ts:297:56
 ❯ validateBrutal tests/unit/tools/d569-v5.test.ts:232:10
 ❯ evaluateRunnerTimeoutConsumers tests/unit/tools/d569-v5.test.ts:475:27
 ❯ tests/unit/tools/d569-v5.test.ts:574:34

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  no tests
   Start at  21:12:31
   Duration  18.96s (transform 1.69s, setup 0ms, import 0ms, tests 0ms, environment 0ms)


diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

codex
The second-family suite is now green at `50/50`: all 20 structural rows still print the sole fog-key path, v1 has zero violations, and the three negative witnesses plus the independent-regeneration mutant witness pass. The v5 suite now gets past D569 validation but exposes a later collection-time mismatch in its synthetic runner row (`Registered context caps mismatch at 1:1`). I’m checking whether that is the plan-required historical v5 test setup (and therefore fixable in its allowed file) or an infeasible baseline gap before changing it.
exec
/bin/bash -lc "jq '{baseContextCapBytes,semanticContextCapBytes}' tests/fixtures/d569-blind-experiment-manifest.json
rg -n \"baseContextCapBytes|semanticContextCapBytes|BLIND_TURN_CONTEXT_MAX_BYTES|SEMANTIC_BOARD_MAX_BYTES\" tools/d569-blind-experiment.ts tools/ai-dm-arena.ts tools/d569-v5/validate-first-arm.ts tests/unit/tools/d569-v5.test.ts | head -160
nl -ba tests/unit/tools/d569-v5.test.ts | sed -n '440,490p'
nl -ba tools/d569-v5/validate-first-arm.ts | sed -n '345,378p'" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
{
  "baseContextCapBytes": null,
  "semanticContextCapBytes": null
}
tests/unit/tools/d569-v5.test.ts:133:    turnContextConfiguredCaps: { baseBytes: cell.baseContextCapBytes, semanticBytes: cell.semanticContextCapBytes },
tests/unit/tools/d569-v5.test.ts:150:      configuredBaseBytes: cell.baseContextCapBytes,
tests/unit/tools/d569-v5.test.ts:151:      configuredSemanticBytes: cell.semanticContextCapBytes,
tests/unit/tools/d569-v5.test.ts:495:    visualArtifactHash: String(boardImage['sha256']), baseContextCapBytes: cell.baseContextCapBytes,
tests/unit/tools/d569-v5.test.ts:496:    semanticContextCapBytes: cell.semanticContextCapBytes, truncatedBlocks: [],
tools/d569-v5/validate-first-arm.ts:368:    if (baseCap !== cell.baseContextCapBytes || semanticCap !== cell.semanticContextCapBytes) {
tools/d569-v5/validate-first-arm.ts:409:      baseContextCapBytes: requiredInteger(baseCap, 'base context cap'),
tools/d569-v5/validate-first-arm.ts:410:      semanticContextCapBytes: requiredInteger(semanticCap, 'semantic context cap'),
tools/d569-blind-experiment.ts:3:import { BLIND_TURN_CONTEXT_MAX_BYTES } from '../src/vtt/blind-turn-context';
tools/d569-blind-experiment.ts:8:import { SEMANTIC_BOARD_MAX_BYTES, TURN_CONTEXT_MAX_BYTES } from '../src/vtt/mcp/engine-server';
tools/d569-blind-experiment.ts:284:    blindBaseBytes: z.literal(BLIND_TURN_CONTEXT_MAX_BYTES),
tools/d569-blind-experiment.ts:285:    semanticBytes: z.literal(SEMANTIC_BOARD_MAX_BYTES),
tools/d569-blind-experiment.ts:651:  readonly baseContextCapBytes: number;
tools/d569-blind-experiment.ts:652:  readonly semanticContextCapBytes: number;
tools/d569-blind-experiment.ts:709:        baseContextCapBytes: arm.dmMode === 'blind'
tools/d569-blind-experiment.ts:712:        semanticContextCapBytes: SEMANTIC_BOARD_MAX_BYTES,
tools/d569-blind-experiment.ts:751:  readonly baseContextCapBytes: number;
tools/d569-blind-experiment.ts:752:  readonly semanticContextCapBytes: number;
tools/d569-blind-experiment.ts:837:    addViolation(violations, row.baseContextCapBytes === cell.baseContextCapBytes &&
tools/d569-blind-experiment.ts:838:      row.semanticContextCapBytes === cell.semanticContextCapBytes,
tools/ai-dm-arena.ts:68:import { BLIND_TURN_CONTEXT_MAX_BYTES } from '../src/vtt/blind-turn-context';
tools/ai-dm-arena.ts:466:      ? BLIND_TURN_CONTEXT_MAX_BYTES
tools/ai-dm-arena.ts:496:    turnContextMaximumBytes !== BLIND_TURN_CONTEXT_MAX_BYTES)) {
   440	  const evidence = classifyEngineCatalogEvidence({
   441	    dispatchId, expectedProfile: 'blind', expectedPhase: 'primary',
   442	    expectedRequestId: 'request:d569-invalid-catalog', expectedToolNames: ['engine.get_turn_context'],
   443	    events: [], readiness, completed: false, requiredStartupFailed: false,
   444	    ...(source === 'malformed' ? { malformedReadiness: true } : {}),
   445	  });
   446	  if (evidence.status !== 'inconclusive' || evidence.reason !== 'invalid_catalog_response') {
   447	    throw new Error(`${source} consumer fixture did not classify as invalid catalog evidence.`);
   448	  }
   449	  return evidence;
   450	}
   451	
   452	function evaluateRunnerTimeoutConsumers(): {
   453	  readonly packetRows: number;
   454	  readonly registeredRows: number;
   455	  readonly observedViolations: readonly string[];
   456	  readonly contradictoryPacketError: string;
   457	  readonly contradictoryRegisteredError: string;
   458	  readonly contradictoryObservedViolations: readonly string[];
   459	  readonly invalidCatalogResults: readonly {
   460	    readonly exit: 'timed_out' | 'cancelled';
   461	    readonly source: InvalidCatalogSource;
   462	    readonly packetError: string;
   463	    readonly registeredError: string;
   464	    readonly observedViolations: readonly string[];
   465	  }[];
   466	} {
   467	  const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
   468	  const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
   469	    .map((cell) => current(cell, 'authorized'));
   470	  packetRows[0] = produced;
   471	  const packetCount = pairedDocuments(packetRows).packet.length;
   472	  const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
   473	    .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   474	  registeredRows[0] = produced;
   475	  const registeredCount = validateBrutal(raw(registeredRows)).length;
   476	  const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   477	  if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   478	  const boardImage = record(produced['boardImage']);
   479	  const observedSessionId = produced['sessionId'];
   480	  const scheduledCellKey = produced['scheduledCellKey'];
   481	  const dispatchId = produced['dispatchId'];
   482	  if (typeof observedSessionId !== 'string' || observedSessionId.length === 0 ||
   483	    typeof scheduledCellKey !== 'string' || typeof dispatchId !== 'string') {
   484	    throw new Error('Runner timeout row omitted its observed session or dispatch identity.');
   485	  }
   486	  const observed: D569ObservedRow = {
   487	    arm: cell.arm, cli: 'codex', cliVersion: 'codex-cli 0.153.4', model: cell.model, effort: cell.effort,
   488	    family: cell.family, basis: cell.basis, seed: cell.seed, rep: cell.rep,
   489	    sessionId: observedSessionId, outcome: 'refused', scheduledCellKey, dispatchId,
   490	    engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
   345	      captureTilePx: visualProfile['captureTilePx'],
   346	    };
   347	    if (boardImage['mode'] !== 'png' || canonicalJson(visualDescriptor) !== canonicalJson(visualPin) || !Array.isArray(images) ||
   348	      images.length !== 1 || objectValue(images[0], 'visualProfile.images[0]')['sha256'] !== boardImage['sha256']) {
   349	      throw new TypeError(`Registered visual provenance mismatch at ${observation.scheduledCellKey}.`);
   350	    }
   351	    const ingressAudit = objectValue(row['blindIngressAudit'], 'blindIngressAudit');
   352	    if (current) {
   353	      const deliveryStatus = deliverySchema.parse(row['turnContextDelivery']).status;
   354	      const delivered = deliveryStatus === 'delivered';
   355	      if (ingressAudit['version'] !== 2 || ingressAudit['forbiddenContentPassed'] !== true ||
   356	        (delivered && (ingressAudit['status'] !== 'complete' || ingressAudit['passed'] !== true)) ||
   357	        (!delivered && (ingressAudit['status'] !== 'incomplete' || ingressAudit['passed'] !== false))) {
   358	        throw new TypeError(`Registered ingress provenance mismatch at ${observation.scheduledCellKey}.`);
   359	      }
   360	    } else if (ingressAudit['passed'] !== true) {
   361	      throw new TypeError(`Historical ingress provenance mismatch at ${observation.scheduledCellKey}.`);
   362	    }
   363	    const caps = current
   364	      ? objectValue(row['turnContextConfiguredCaps'], 'turnContextConfiguredCaps')
   365	      : objectValue(row['turnContextBudget'], 'turnContextBudget');
   366	    const baseCap = current ? caps['baseBytes'] : caps['configuredBaseBytes'];
   367	    const semanticCap = current ? caps['semanticBytes'] : caps['configuredSemanticBytes'];
   368	    if (baseCap !== cell.baseContextCapBytes || semanticCap !== cell.semanticContextCapBytes) {
   369	      throw new TypeError(`Registered context caps mismatch at ${observation.scheduledCellKey}.`);
   370	    }
   371	    const truncatedBlocks = row['turnContextBudget'] === undefined
   372	      ? []
   373	      : objectValue(row['turnContextBudget'], 'turnContextBudget')['truncatedBlocks'];
   374	    if (!Array.isArray(truncatedBlocks) || truncatedBlocks.some((entry) => typeof entry !== 'string')) {
   375	      throw new TypeError(`Invalid truncation provenance at ${observation.scheduledCellKey}.`);
   376	    }
   377	    const boardHash = requiredString(boardImage['sha256'], 'boardImage.sha256');
   378	    return {

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc "jq '.contextCaps' tests/fixtures/d569-blind-experiment-manifest.json
nl -ba tools/d569-blind-experiment.ts | sed -n '700,718p'
nl -ba tools/ai-dm-arena.ts | sed -n '450,505p'
rg -n \"turnContextConfiguredCaps\" tools/ai-dm-arena.ts src -g '*.ts' | head -30" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
null
   700	        stateHash: fixture.sha256,
   701	        sharedKbHash: manifest.sharedKb.combinedStartupHash,
   702	        visualSourceHash: sha256(canonicalJson({ stateHash: fixture.sha256, role: visualPin.imageRole })),
   703	        visualProfileHash: sha256(canonicalJson(visualPin)),
   704	        sessionPolicy: 'fresh',
   705	        sessionKey: sha256(canonicalJson({ family, arm: arm.id, seed: fixture.seed, rep })),
   706	        timeoutMs: manifest.experimentWallMs,
   707	        escalation: false,
   708	        modelDefaultFallback: false,
   709	        baseContextCapBytes: arm.dmMode === 'blind'
   710	          ? manifest.caps.blindBaseBytes
   711	          : manifest.caps.adviceBaseBytes,
   712	        semanticContextCapBytes: SEMANTIC_BOARD_MAX_BYTES,
   713	        requiredBlocks: ['semantic_board', 'creature_facts', 'initiative', 'legal_movement'],
   714	        truncatedBlocks: [],
   715	      };
   716	    }))));
   717	}
   718	
   450	  if (!SCRIPTED_PARTY_DECISION_POLICIES.includes(partyPolicy as ScriptedPartyDecisionPolicy)) {
   451	    throw new TypeError('--party-policy must be heuristic_v0 or symmetric_evaluator_v1.');
   452	  }
   453	  const intelMode = values.get('--intel-mode') ?? 'full';
   454	  if (intelMode !== 'full' && intelMode !== 'off') {
   455	    throw new TypeError('--intel-mode must be full or off.');
   456	  }
   457	  const overridePolicy = values.get('--override-policy') ?? DEFAULT_OVERRIDE_POLICY;
   458	  if (!OVERRIDE_POLICIES.includes(overridePolicy as OverridePolicy)) {
   459	    throw new TypeError('--override-policy must be strict or typed_reason.');
   460	  }
   461	  const rendererProfile = values.has('--renderer-profile')
   462	    ? rendererProfileSchema.parse(JSON.parse(values.get('--renderer-profile') ?? ''))
   463	    : DEFAULT_RENDERER_PROFILE;
   464	  const turnContextMaximumBytes = positiveInteger(
   465	    values.get('--turn-context-max-bytes') ?? String(dmMode === 'blind'
   466	      ? BLIND_TURN_CONTEXT_MAX_BYTES
   467	      : TURN_CONTEXT_MAX_BYTES),
   468	    '--turn-context-max-bytes',
   469	  );
   470	  const boardImageMode = values.get('--board-image') ?? (dmModeExplicit ? 'png' : 'off');
   471	  if (!BOARD_IMAGE_MODES.includes(boardImageMode as BoardImageMode)) {
   472	    throw new TypeError('--board-image must be off, png, or capture_only.');
   473	  }
   474	  if (boardImageMode === 'png' && selectedCli === 'local-openai') {
   475	    throw new TypeError('--board-image png requires an MCP-backed CLI.');
   476	  }
   477	  if (boardImageMode === 'png' && transport !== 'mcp_minimal') {
   478	    throw new TypeError('--board-image png requires --transport mcp_minimal.');
   479	  }
   480	  const blindRepairArm = values.get('--blind-repair-arm') ?? 'code_only';
   481	  if (!BLIND_REPAIR_ARMS.includes(blindRepairArm as BlindRepairArm)) {
   482	    throw new TypeError('--blind-repair-arm must be code_only or minimal_legal_alternative.');
   483	  }
   484	  const blindMaxAttempts = blindMaxAttemptsSchema.parse(values.has('--blind-max-attempts')
   485	    ? positiveInteger(values.get('--blind-max-attempts') ?? '', '--blind-max-attempts')
   486	    : BLIND_MAX_ATTEMPTS_DEFAULT);
   487	  const blindFactsValue = values.get('--blind-facts') ?? 'off';
   488	  if (blindFactsValue !== 'on' && blindFactsValue !== 'off') {
   489	    throw new TypeError('--blind-facts must be on or off.');
   490	  }
   491	  if (dmMode !== 'blind' && (values.has('--blind-repair-arm') ||
   492	    values.has('--blind-max-attempts') || values.has('--blind-facts'))) {
   493	    throw new TypeError('Blind repair, attempt, and fact options require --dm-mode blind.');
   494	  }
   495	  if (dmMode === 'blind' && (transport !== 'mcp_minimal' || boardImageMode !== 'png' ||
   496	    turnContextMaximumBytes !== BLIND_TURN_CONTEXT_MAX_BYTES)) {
   497	    throw new TypeError('Blind mode requires MCP-minimal, PNG delivery, and a 65536-byte base cap.');
   498	  }
   499	  if (dmModeExplicit && (escalationModel !== null || escalationEffort !== null || captureRlData)) {
   500	    throw new TypeError('D569 experiment modes prohibit escalation and RL capture.');
   501	  }
   502	  if (dmModeExplicit && (instructionSource.instructionSource !== 'kb' ||
   503	    instructionSource.kbPath !== resolve(cwd, D569_AI_DM_KB_ROOT))) {
   504	    throw new TypeError('Explicit D569 DM modes require the shared D570 knowledge bundle.');
   505	  }

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc "jq '.caps' tests/fixtures/d569-blind-experiment-manifest.json
rg -n \"turnContextConfiguredCaps:\" tools/ai-dm-arena.ts src/vtt -g '*.ts' | head -30
rg -n \"semanticBytes.*SEMANTIC|configuredCaps\" tools/ai-dm-arena.ts | head -50" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
{
  "blindBaseBytes": 65536,
  "semanticBytes": 8192,
  "adviceBaseBytes": 32768
}

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc "rg -n \"turnContextConfiguredCaps\" . --glob '"'!node_modules/**'"' --glob '"'!tests/fixtures/**'"' --glob '"'!docs/**'"'
rg -n \"SEMANTIC_BOARD_MAX_BYTES\" src/vtt tools/ai-dm-arena.ts | head -50
rg -n \"semanticContextMaximum|semantic.*max.*bytes|semanticBytes\" tools/ai-dm-arena.ts | head -100" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
./tools/ai-dm-rerun-packet.ts:343:  turnContextConfiguredCaps: z.object({ baseBytes: safeIntegerSchema.min(1), semanticBytes: safeIntegerSchema.min(0) }).strict(),
./tools/ai-dm-rerun-packet.ts:754:  'turnContextConfiguredCaps', 'dispatchId', 'hostContextDiagnostic',
./tools/ai-dm-rerun-packet.ts:882:  for (const field of ['engineCatalogEvidence', 'turnContextDelivery', 'turnContextConfiguredCaps',
./tools/ai-dm-conversation.ts:680:  readonly turnContextConfiguredCaps?: TurnContextConfiguredCaps;
./tools/ai-dm-conversation.ts:7465:            turnContextConfiguredCaps: {
./tools/d569-v5/validate-first-arm.ts:80:  'turnContextConfiguredCaps', 'hostContextDiagnostic',
./tools/d569-v5/validate-first-arm.ts:115:    .parse(row['turnContextConfiguredCaps']);
./tools/d569-v5/validate-first-arm.ts:364:      ? objectValue(row['turnContextConfiguredCaps'], 'turnContextConfiguredCaps')
./tests/unit/tools/ai-dm-rerun-packet.test.ts:123:    turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 8_192 },
./tests/unit/tools/ai-dm-rerun-packet.test.ts:164:      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 8_192 },
./tests/unit/tools/ai-dm-rerun-packet.test.ts:196:      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 8_192 },
./tests/unit/tools/d569-v5.test.ts:133:    turnContextConfiguredCaps: { baseBytes: cell.baseContextCapBytes, semanticBytes: cell.semanticContextCapBytes },
./tests/unit/tools/d569-v5.test.ts:200:    'turnContextConfiguredCaps', 'hostContextDiagnostic', 'failingDispatch',
src/vtt/mcp/engine-server.ts:166:  BLIND_SEMANTIC_BOARD_MAX_BYTES,
src/vtt/mcp/engine-server.ts:268:export const SEMANTIC_BOARD_MAX_BYTES = 8 * 1024;
src/vtt/mcp/engine-server.ts:1595:    (dmMode === 'blind' ? BLIND_SEMANTIC_BOARD_MAX_BYTES : SEMANTIC_BOARD_MAX_BYTES);
src/vtt/blind-turn-context.ts:35:export const BLIND_SEMANTIC_BOARD_MAX_BYTES = 32_768 as const;
src/vtt/blind-turn-context.ts:64:  readonly configuredSemanticBytes: typeof BLIND_SEMANTIC_BOARD_MAX_BYTES;
src/vtt/blind-turn-context.ts:1237:  const semanticMaximumBytes = input.semanticMaximumBytes ?? BLIND_SEMANTIC_BOARD_MAX_BYTES;
src/vtt/blind-turn-context.ts:1241:  if (semanticMaximumBytes !== BLIND_SEMANTIC_BOARD_MAX_BYTES) {
src/vtt/blind-turn-context.ts:1242:    throw new RangeError(`Blind semantic-board cap must be ${String(BLIND_SEMANTIC_BOARD_MAX_BYTES)} bytes.`);
src/vtt/blind-turn-context.ts:1313:      configuredSemanticBytes: BLIND_SEMANTIC_BOARD_MAX_BYTES,

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc "rg -n \"RUNNER_TIMEOUT|CANCELLATION_AFTER|context caps|configured\" tests/unit/tools/d569-v5.test.ts
nl -ba tests/unit/tools/d569-v5.test.ts | sed -n '650,780p'
nl -ba tools/ai-dm-conversation.ts | sed -n '7448,7475p'" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
150:      configuredBaseBytes: cell.baseContextCapBytes,
151:      configuredSemanticBytes: cell.semanticContextCapBytes,
414:const RUNNER_CANCELLATION_AFTER_DELIVERY_ROW = await produceCancellationAfterDeliveryRow();
415:const RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW = await produceTimeoutBeforeDeliveryRow();
467:  const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
574:const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
666:    const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
691:    const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
697:    expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
698:    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
699:    expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
700:    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
701:    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');
702:    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryObservedViolations).toContain('integrity_indeterminate');
706:    expect(RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults).toHaveLength(4);
707:    expect(RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults.map(({ exit, source }) => `${exit}:${source}`))
712:    for (const result of RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults) {
   650	    const cancelled = mutateFirst(BRUTAL_SOURCE, (row) => {
   651	      const dispatchId = String(row['dispatchId']);
   652	      return {
   653	        ...row, outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
   654	        failingDispatch: {
   655	          phase: 'primary', exit: 'cancelled', dispatchId,
   656	          engineCatalogEvidence: row['engineCatalogEvidence'],
   657	          turnContextDelivery: row['turnContextDelivery'],
   658	          failureReason: 'operator cancelled after delivery',
   659	        },
   660	      };
   661	    });
   662	    expect(validateBrutal(cancelled)).toHaveLength(30);
   663	  });
   664	
   665	  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
   666	    const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
   667	    expect(produced).toMatchObject({
   668	      outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
   669	      turnContextDelivery: { status: 'delivered', measurement: { semanticBytes: expect.any(Number) } },
   670	      failingDispatch: {
   671	        phase: 'primary', exit: 'cancelled',
   672	        turnContextDelivery: { status: 'delivered', measurement: { semanticBytes: expect.any(Number) } },
   673	      },
   674	    });
   675	    expect(produced['semanticBoardBytes']).toEqual(expect.any(Number));
   676	    expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
   677	    const failure = record(produced['failingDispatch']);
   678	    expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
   679	    const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
   680	      .map((cell) => current(cell, 'authorized'));
   681	    packetRows[0] = produced;
   682	    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
   683	
   684	    const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
   685	      .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   686	    registeredRows[0] = produced;
   687	    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
   688	  });
   689	
   690	  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
   691	    const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
   692	    expect(produced).toMatchObject({
   693	      outcome: 'refused', fallbackReason: 'timeout',
   694	      engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
   695	      turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
   696	    });
   697	    expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
   698	    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
   699	    expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
   700	    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
   701	    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');
   702	    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryObservedViolations).toContain('integrity_indeterminate');
   703	  });
   704	
   705	  it('rejects malformed and wrong-profile catalog evidence after timeout or cancellation in every consumer', () => {
   706	    expect(RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults).toHaveLength(4);
   707	    expect(RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults.map(({ exit, source }) => `${exit}:${source}`))
   708	      .toEqual([
   709	        'timed_out:malformed', 'timed_out:wrong_profile',
   710	        'cancelled:malformed', 'cancelled:wrong_profile',
   711	      ]);
   712	    for (const result of RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults) {
   713	      expect(result.packetError).toContain('integrity evidence');
   714	      expect(result.registeredError).toContain('integrity-indeterminate');
   715	      expect(result.observedViolations).toContain('integrity_indeterminate');
   716	    }
   717	  });
   718	
   719	  it('rejects incomplete v3 evidence objects', () => {
   720	    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => {
   721	      const catalog = record(row['engineCatalogEvidence']);
   722	      return { ...row, engineCatalogEvidence: { status: catalog['status'], dispatchId: catalog['dispatchId'] } };
   723	    }))).toThrow();
   724	  });
   725	
   726	  it('rejects an indeterminate delivery when the companion v3 catalog field is absent', () => {
   727	    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => {
   728	      const { engineCatalogEvidence: _omitted, ...withoutCatalog } = row;
   729	      return {
   730	        ...withoutCatalog,
   731	        turnContextDelivery: {
   732	          status: 'indeterminate', dispatchId: row['dispatchId'],
   733	          reason: 'catalog_inconclusive_empty_context_spool', measurement: null,
   734	          integrityAction: 'stop_after_persist',
   735	        },
   736	      };
   737	    }))).toThrow();
   738	  });
   739	
   740	  it.each(['conflicting_success_and_failure', 'invalid_catalog_response'] as const)(
   741	    'rejects a %s catalog when the companion v3 delivery field is absent',
   742	    (reason) => {
   743	      expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => {
   744	        const { turnContextDelivery: _omitted, ...withoutDelivery } = row;
   745	        return {
   746	          ...withoutDelivery,
   747	          engineCatalogEvidence: { status: 'inconclusive', dispatchId: row['dispatchId'], reason },
   748	        };
   749	      }))).toThrow();
   750	    },
   751	  );
   752	
   753	  it('rejects incomplete v3 evidence nested in a failing dispatch', () => {
   754	    const rows = BRUTAL_SOURCE.trimEnd().split('\n').map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   755	    const index = rows.findIndex((row) => row['outcome'] === 'infrastructure_failed');
   756	    if (index < 0) throw new Error('Registered fixture lacks an infrastructure row.');
   757	    const row = rows[index]!;
   758	    const failure = record(row['failingDispatch']);
   759	    const catalog = record(failure['engineCatalogEvidence']);
   760	    rows[index] = {
   761	      ...row,
   762	      failingDispatch: {
   763	        ...failure,
   764	        engineCatalogEvidence: { status: catalog['status'], dispatchId: catalog['dispatchId'] },
   765	      },
   766	    };
   767	    expect(() => validateBrutal(raw(rows))).toThrow();
   768	  });
   769	
   770	  it('rejects a missing v3 host diagnostic', () => {
   771	    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => {
   772	      const { hostContextDiagnostic: _hostContextDiagnostic, ...withoutHostDiagnostic } = row;
   773	      return withoutHostDiagnostic;
   774	    }))).toThrow();
   775	  });
   776	
   777	  it('rejects partial v3 hybrids and integrity-indeterminate rows before analysis', () => {
   778	    const historicalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) =>
   779	      historical(roomIndex + 1, rep, `historical-${String(roomIndex + 1)}-${String(rep)}`))).flat();
   780	    expect(() => validateD569FirstArm(raw(historicalRows.map((row, index) => index === 0
  7448	          },
  7449	        };
  7450	      }
  7451	      const row: ConversationRow = {
  7452	        ...(config.dmModeExplicit ? (() => {
  7453	          if (primaryTurn === null || primaryCatalogEvidence === null || primaryDelivery === null) {
  7454	            throw new Error('D569 row lacks primary dispatch evidence.');
  7455	          }
  7456	          if (outcome === 'infrastructure_failed' && failingDispatch === null) {
  7457	            throw new D569IntegrityStop('D569 infrastructure outcome lacks failing-dispatch evidence.');
  7458	          }
  7459	          return {
  7460	            rowContractVersion: 'arena-row-v3' as const,
  7461	            scheduledCellKey: config.scheduledCellKey ?? `${String(room)}:${String(round)}`,
  7462	            dispatchId: primaryCatalogEvidence.dispatchId,
  7463	            engineCatalogEvidence: primaryCatalogEvidence,
  7464	            turnContextDelivery: primaryDelivery,
  7465	            turnContextConfiguredCaps: {
  7466	              baseBytes: config.turnContextMaximumBytes,
  7467	              semanticBytes: config.dmMode === 'blind' ? BLIND_SEMANTIC_BOARD_MAX_BYTES : SEMANTIC_BOARD_MAX_BYTES,
  7468	            },
  7469	            hostContextDiagnostic,
  7470	          };
  7471	        })() : {}),
  7472	        ...(failingDispatch === null ? {} : { failingDispatch }),
  7473	        knowledgeModel: 'engine_state',
  7474	        hiddenOptions,
  7475	        intelMode: config.intelMode,

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc 'rg -n "Registered context caps mismatch|context caps mismatch|semantic cap|32,768|32768|8,192|8192" /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md | tail -80' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
22928:Sol (impl-b2-sol.md): all four D635.25 rulings applied — detectCombatant relocated verbatim to visibility-field.ts (state-only: sight evaluation, combat rules/conditions, spaces/distance, affected cells, terrain walls, domain types; no path to combat/random.ts), encounter.ts re-exports, the query port imports it from visibility-field and keeps no ladder (the AST witness now checks the visibility-field import); decoder-equality assertions in los-cover (:271 → the frozen text still contains the legacy key and decodes equal) and two-room (tracked source has no key; decoded state equals the generated one); two-room.v1.json regenerated by the real generator with exactly the empty-key removal (+0/−1), snapshots/examples byte-identical; scene-snapshot normalized again (revision df6f2cce… → 35c96ecc…, fingerprint 163cf200… → dd802783…, request/coordinator hashes unchanged, old values in the comment); semantic-board-payload reads fog via projectDmView; SIGHT_TRACER_HAS_SINGLE_OWNER updated to the Batch 2 stage; content-pack schema regenerated (+57/−0, three devils_sight additions); RED-first 41/8; all five mutants killed with fresh shas and cp-restored; tsc 0/0; OFFERS checks 0/0; pair 39.1 s; B2 suites 50/50; capsule graph 11/11; los-cover 111/111; 29 other core files 476/476; semantic 7/8 (only M576-E3 stays, B6); OFFERS set green EXCEPT engine-mcp-handler "validates emitted adjustment_delta_truncated_board against the handler output schema": expected dropped classes ["light"], received ["light","adjacency","objects"] — the calibration board's semantic bytes grew 7,817 → 9,322 (cap SEMANTIC_BOARD_MAX_BYTES 8,192) because the semantic source now carries the DM-derived fog (106 cells; the fixture had 0 authored fog). All OFFERS digest/identity pins unchanged. RULING: that pin is B6's by plan text — B6 (:228-240) owns "truncation ordering/class/cap is explicit" and tests/unit/tools/engine-mcp-handler.test.ts is in B6's manifest (:230); the red is added to the known intermediate reds (now: semantic-board-payload M576-E3, engine-mcp-handler truncated-board calibration — both B6). B2 is otherwise complete; my verification runs now, then astra at medium.
22934:Astra r1 (review-impl-b2-astra-r1.md): detectCombatant relocation byte-identical (43 lines, sha 90e42286…); the moved web helper's terrainWallCells substitution equals the forwarding wrapper at encounter.ts:2110; runtime import walk from visibility-field reaches 40 files, zero forbidden, no runtime path to encounter.ts; the AST witness is real (named import, no top-level detect, exactly two canonical calls in visibility) with its limit stated (no symbol resolution); the four-outcome matrix hand-derived; schema regenerated in memory byte-for-byte, three additions; two-room generator check passes, one deleted line; decoder validation/discard proven; 82 JSON + 2 TS legacy fixtures untouched; every remaining foggedCells reference classified (36 lines: derived projection I/O, codec migration, B6 semantic, probe I/O); D569 persistence demonstration reproduced (df6f2cce… → 35c96ecc…; fingerprint dd802783… recomputed; request/coordinator unchanged); both B6 reds reproduced with the byte growth 7,817 → 9,322 vs 8,192 and plan :230 confirmed; two plan mutants + its own boundary mutant killed; tsc 0/0; pair 38.5 s. B2-F1 (P2): src/content/content-pack.ts:291 keeps z.array(combatSenseSchema).min(1).max(5) — a monster declaring all six supported senses (normal sight, darkvision, blindsight, tremorsense, truesight, devils_sight) is rejected as malformed; .max(6) fixes it (astra verified in memory). ACCEPTED — the plan's IMPORTED_DEVILS_SIGHT_ACCEPTED intent is defeated at the limit. Fix r4 (resumed lane): .max(6) with duplicate-kind rejection retained; regenerate the schema (diff = the maxItems change only); add an independent six-kind acceptance literal and keep the unknown-sense rejection; the byte-for-value pin unchanged. Astra r2 confirms.
22940:Astra r2 (review-impl-b2-astra-r2.md): 3 files 37+/4−; exactly three maxItems 5→6 changes, in-memory generation byte-identical; the six-kind literal asserts zero diagnostics and exact imported + runtime senses; its own .max(6)→.max(5) mutant red at :174; unknown-sense rejection and the byte-for-value pin unchanged; tsc 0/0; content-pack 27/27. vis-field = 4c6a3315: B1, B3, SEAM, B4, B5, REBASE (main 9f148666), B2, B2-F1. B6 dispatched (fresh sol; brief impl-vis-field-b6.md; log impl-b6.log; 10-file manifest per plan :230 with the turn-context schema by generator only; owns the two known reds — the semantic partition and the OFFERS truncation calibration, re-derived by hand from class sizes against the 8,192 cap; six plan mutants; the D569 E1C/FOOTPRINTS demonstration with its hard stops; the §8 checkpoint re-verification by symbols; the final aggregate of every B1–B6 suite and `npx tsc -b --force`). After B6 and astra: the VIS-FIELD landing gate on the branch merged with main (gate-wt2 + the OFFERS checks + the D630 pair and arena round), then "Land VIS-FIELD-01" on main, mirror push (D640), then ELEVATION-02 B0.
22945:## D635.32 — SUPERVISOR (2026-09-17 01:24): B6 r2 complete on 11 files; its only red is a pre-existing D524 probe test now deterministically over its 5 s budget because of derived visibility — A/B measured by me; NAMED RE-BUDGET ruled (5 s → 10 s for that one test); the raised blind semantic cap flagged for astra and the owner; r3 dispatched
22946:Sol (impl-b6-sol.md, 381 k tokens): v2 visibility block (observers + subjects transposes, strict schemas, generator-only JSON schema), player output omits it (with a forbidden-field control), serializer sentinel seam, effect obscurement union, Invisible subject keeps visible_from, truncation ordered light → adjacency → objects → visibility with the 8,192-byte calibration re-derived by hand (16,120 → 15,167 → 13,168 → 12,259 → 5,492 bytes), truncation recorded, the final single-owner stage, source-binding witnesses (standard-case string leaves 493 → 550, paths 131 → 143, explained), the semantic partition pin hand-derived [[4,0],[3,1]]; six plan mutants killed with sha-restored files; semantic 11/11, engine-mcp-handler 131/131, source-binding 6/6, support 227/227; tsc 0/0; OFFERS checks 0/0; D569 E1C/FOOTPRINTS unchanged; pair 38.85 → 38.72 s; 36 compiled foggedCells references, none a runtime state consumer; the final aggregate of all 30 plan-named suites 759/760 in 1,172 s — the sole red tests/unit/tools/ai-dm-screenshot-probe.test.ts:464 "is versioned, uses generic classic-board conventions, and leaks no non-rendered probe identity" at 5.33 s (isolated 5.18 / 5.28 s) against its D524 5 s budget; sol stopped rather than raise a timeout (correct). ALSO CHANGED BY SOL (flagged): the blind semantic cap SEMANTIC_BOARD_MAX_BYTES (or its blind-context counterpart) raised 8,192 → 32,768 because the retained hard fixtures' semantic boards now measure 13,406–17,597 bytes with the visibility block; that is a blind-DM context-size decision the plan left open ("cap is explicit") — astra reviews it; the owner is told. A/B BY ME (ab-probe.log, isolated, alternating, load ≈1): main 9f148666 passes at 4,205 / 4,210 ms; vis-field (B6 dirty) fails at 5,243 / 5,094 / 5,246 ms — +21–25% on this single test, the derived-visibility cost of the primer's board projections, at the edge of D630's 25% and over a fixed 5 s budget. RULING: a NAMED re-budget for this one test — timeout 5,000 → 10,000 ms — recorded here with the numbers as the justification (the plan's "raise timeouts only for D544-named flaky tests" is a lane rule; this is not flakiness but a measured cost of the plan's own behaviour, and the supervisor may name it); manifest +1 for that timeout line only (12 files); the cost itself is queued for TEST-PERF-01 / the owner's engine-cache decision (D630 brainstorm rank 6) with sol's mechanism note (how many projections the primer render computes derived fog for). r3 on the resumed session: apply the re-budget, state the mechanism, justify the cap change with per-fixture bytes and what the 8,192 cap would have dropped, then the forced build and the second aggregate.
22949:Sol r3 (impl-b6-sol.md): re-budget applied exactly as ruled; mechanism traced (44 fact sheets → 88 projection calls → 73 fresh derived-fog evaluations, because each encounter-board clone is a distinct state root — ai-dm-screenshot-probe.ts:829, visibility.ts:524, encounter-board.ts:716, visibility-field.ts:550); cap justified per fixture (with/without visibility bytes and the classes the 8,192 cap would drop from each: all four; 16,384 rejects 5117001/5117002/5117007; 32,768 = half the 65,536 blind base cap); D569 blind fixtures unchanged (10 files, inventory sha 3cb2a649…); E1C/FOOTPRINTS unchanged checks 2/2; six mutants killed; tsc 0/0; tsc -b --force 0; OFFERS checks 0/0; pair 38.7 s; second final aggregate of all 30 plan suites 760/760 in 1,171 s. VERIFIED BY ME (verify-b6.log; dirty tree 11 files 516+/32−): tsc app/node 0; tsc -b --force 0; both OFFERS checks 0; 0 forbidden, 0 long lines; schema generator idempotent (a08a7359… → a08a7359…); tests/fixtures diff empty; MY MUTANT PLAYER_OUTPUT_KEEPS_VISIBILITY (the audience === 'dm' guard dropped at semantic-board-payload.ts:601) → the sentinel/player-omission test RED, restored 77cfdb2d…; 11 B6 suites 328/328 (semantic, blind-turn-context, source-binding, engine-mcp-handler, board-delivery, visibility-field, screenshot-probe, engine-mcp-server, engine-mcp-boundary, stable-dom-render, accessible-board); pair 36/36 in 39 s. Committed 10cc9f5e. vis-field = 10cc9f5e: B1, B3, SEAM, B4, B5, REBASE, B2, B2-F1, B6 — every batch of the frozen plan. Astra (resume 01a0a642…, medium; brief review-impl-b6-astra.md; log review-b6-astra-r1.log) reviews the last batch and is asked to state whether the 32,768-byte blind semantic cap is within B6's authority or a product decision; the owner has been told and can rule before landing. Next after ACCEPT: the VIS-FIELD LANDING GATE — merge main into the branch (main has not moved since the rebase: 9f148666), gate-wt2 (tsc -b, sg, full vitest, Playwright), OFFERS checks, D630 pair and one arena round on the merged tip, then "Land VIS-FIELD-01" on main and the mirror push (D640); then ELEVATION-02 B0.
22952:Astra r1 (review-impl-b6-astra-r1.md, 1.72 M tokens): 11 files 516+/32−; schema regenerated in memory byte-identical (159,453 bytes); the strict v2 objects reject unknown keys (schemas.ts:671-683) with the blind validator mirroring them; no DM-semantic-to-player delivery path (projectPlayerBoard builds from PlayerView; serialization requires the DM audience at :601; turn-context delivery rejects player projections at :673; MCP attachment paths DM-only at engine-server.ts:1568-1579); one sight service (visibilityField / cellsThatCanSee at :334/:344; the serializer consumes supplied rows at :604-612); the truncation calibration reproduced exactly (16,120 → 15,167 → 13,168 → 12,259 → 5,492); the semantic partition oracle re-enumerated by rational arithmetic (18 visible, only [(4,0),(3,1)] concealed); the effect-line and Invisible-subject oracles confirmed; the source-binding counts recounted (57 = 3 + 6×5 + 6×4; 12 paths) and all three mutations rejected; the 10 D569 fixtures and the 31,995 / 32,000-byte raw-context pins byte-identical; three own mutants killed; tsc 0/0; pair 39.2 s. Its noted limits: the final single-owner test is a lexical check (no alias bypass found); its sandbox could not execute the D569 pins (sol 2/2 and my board-delivery 17/17 stand as the evidence). BLIND CAP: astra judges 32,768 bytes within B6's explicitly open cap choice, with the owner notification retained; two corrections to sol's account, accepted: blind delivery REJECTS an oversized protected semantic block rather than truncating it (blind-turn-context.ts:1304-1316) — so under the old 8,192 cap every retained hard fixture's blind context would have been refused outright, not thinned — and the semantic allowance is ADDITIONAL to the 65,536-byte base allowance, not within it. The owner may still rule the cap smaller before the landing merge; absent a ruling it lands at 32,768. VIS-FIELD-01 = 10cc9f5e: B1, B3, SEAM, B4, B5, REBASE, B2, B2-F1, B6. LANDING GATE launched now (gate-vis-field-land.log): gate-wt2 wt-vis-field (tsc -b --force, sg, full vitest, Playwright chromium) + the two OFFERS checks + the D630 pair twice + one arena round; main 9f148666 is already an ancestor, so the landing is a "Land VIS-FIELD-01" no-ff merge once the gate reads green (timeouts, if any, rerun isolated per the offers precedent D617.50); then the mirror push (D640); then ELEVATION-02 B0 on a fresh worktree from the landed main.
23005:(d) B6 CAP PIN: ai-dm-arena.test.ts blind rows expect configuredSemanticBytes 8192, receive 32768 — the B6 cap change (BLIND_SEMANTIC_BOARD_MAX_BYTES, awaiting the owner's ratification) was never reconciled with these pins; four reds.
23012:CLASSES: (1) B2's missed consequence — 27 byte pins + M576 + probably the three 6204 digests + legacy-invariance identities → LAND-04 Task A/B under D569 protocol; (2) B6's missed consequence — four ai-dm-arena blind-row pins at 8192 vs the 32,768 cap → re-pin to the recorded default (owner may still reduce it); (3) THE SEAM RULE's productivity consequence, now known to cover ALL frozen families: brutal (8/10 seeds), brutal-b (10/10), brutal-2 (7/10 + ledger + d569-v5 at file level), and hard-family behaviour pins (composite-turn-proposals: the scout offers only Dodge/End Turn; opportunity intel: 5117008 monster-1 dash → dodge; speculative-planning: empty scenario menu) — LAND-04 Task C investigates with per-ray evidence and a three-way offer count (as-is / seam off / seam+LOS-refusal off) per family for the owner's question 5; NO re-pins; (4) load candidates being rerun isolated (branch-isolated.log).
23017:SOL: three test files 58+/16−, nothing under tests/fixtures. Task A: structural diff = exactly $.encounter.state.foggedCells for all 28 byte pins (12 basis, 12 hard, 3 brutal, M576); normalizer drops the key with a D635.48 comment; the three BRUTAL_BASIS_GENERATED_DIGESTS and all ten D466_6204_NON_ROSTER_DIGESTS (the projection carries the state field for every seed, not only the three replacements) re-derived with old values kept as comments; reverse mutant (key added back) → exactly 12 basis pins red. Task B: four ai-dm-arena blind-row pins → 32,768 with the D635.48 comment (actual semantic bytes observed 10,399); legacy-invariance now derives the starting-room digest IN the test (reconstructs the pre-B2 state as current + foggedCells: [], asserts that exact single difference, hashes both: 34287ad8… → 4757f909…) and approves only row[0].startingRoomDigest; d569-v5 left red (frozen second family). Sol's runs: room-generator 58 passed + exactly the 8 brutal productivity reds; ai-dm-arena 58/58; legacy-invariance 10/10; tsc 0/0.
23032:Astra: all 28 pin cases and 13 digest pairs re-derived in memory with the OLD constants taken from `git show 9a220fce:…`, not the comments (restoring only the key reproduces the old hashes; removing it the new); legacy-invariance sound — it decoded the committed approved-main oracle and both rows record 34287ad8…, equal to the reconstructed pre-B2 hash, and the approved-change map keeps every other field byte-compared; the cap pins are bounded both ways (0 < 10,399 ≤ 32,768) and the screenshot-only case still asserts zero semantic bytes; nothing normalizes a seam consequence (58 + exactly 8; the three behaviour witnesses still red); tsc 0/0; production hashes unchanged. MY MUTANT DISCREPANCY EXPLAINED by astra: keeping the key fails 26 of the 28 selected pins (6203002/3 compare through digests, not the helper) + 8 baseline = 34. LAND4-F1 (P3, parked, not blocking): no committed test exercises the oversized blind semantic-block REJECTION at blind-turn-context.ts:1304; astra's probe (a 40,000-char creature name) was rejected at 32,768 and passed with the guard disabled — add that witness when the owner rules on the cap (it pins whichever value is ratified).
23061:Authorized: (1) WALL-01 (D642, in flight) lands the solid-wall vertex rule with VIS-FIELD; (2) QUESTION 5 = A1: normalize the removed fog key in the second-family validator's regeneration comparison (demonstrated per fixture as a single-key diff, as LAND-04 did), then regenerate ONLY the three brutal cohorts as new versioned families under the sealed geometry (brutal-v2 6209xxx, brutal-b-v2 6210xxx, brutal-2-v2 6211xxx; hard-2 5118xxx kept as the historical hard half), D569 precedent in full (verbatim namespace audit, membership from decoded state, two byte-identical generations, hashes pinned only then), manifests d569-second-family-manifest-v2 + d569-blind-experiment-manifest-v6 with v1/v5 frozen, versioned arena/rerun routes (brutal-v2, brutal-10-v2, brutal-10-b-v2), hand re-pins for composite-turn-proposals and Room-8 opportunity-intel, a small hand-authored scene for speculative-planning (never an empty-menu normalization); (3) the blind semantic cap stays 32,768 and the oversized-block rejection witness (astra's LAND4-F1, P3) is added; (4) after landing: the VIS-FIELD-PERF batch — visibility-epoch cache with the from-scratch differential witness, range-before-LOS prefilter inside offer generation, and the precomputed rectilinear-region/rectangle wall representation (Debenham–Solis-Oba) — planned and reviewed like any unit, plus the two test-only items into TEST-PERF-01. ORDER: WALL-01 (verify, astra) → A1 as VIS-FIELD-01 batch COHORT-01 (planned as a brief with the file-level costing from investigate-option-a-costing-sol.md as its binding spec; astra reviews) → untruncated full gate → land → mirror push (D640) → ELEVATION-02 B0 and the perf batch.

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

codex
I found a frozen-plan infeasibility and am stopping implementation changes as instructed. After normalization, `d569-v5.test.ts` reaches a pre-existing cap conflict the plan says should not exist: historical v5 dry-run cells are pinned to `8,192` semantic bytes, while the current blind runner emits `32,768` (D643’s retained cap). The suite fails during collection at `validate-first-arm.ts:369`, before any v5 tests run. Fixing that would require an unplanned historical-v5 test projection or changing frozen semantics, so I’m only running read-only/type/diff/freeze diagnostics now and will report the exact gap as BLOCKED.
exec
/bin/bash -lc 'npx tsc -p tsconfig.app.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 14279ms:
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc 'npx tsc -p tsconfig.node.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 28798ms:
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc "git diff --check
git status --short
awk 'length("'$0) > 120 { print FNR ":" length($0) ":" $0 }'"' tools/d569-second-family-manifest.ts tests/unit/tools/d569-second-family-manifest.test.ts tests/unit/tools/d569-v5.test.ts
git diff --stat
git diff -- tools/d569-second-family-manifest.ts tests/unit/tools/d569-second-family-manifest.test.ts tests/unit/tools/d569-v5.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
 M tests/unit/tools/d569-second-family-manifest.test.ts
 M tests/unit/tools/d569-v5.test.ts
 M tools/d569-second-family-manifest.ts
25:309:  'seed ranges 5118001-5118010 and 6207001-6207010 have no occurrence in .claude/decisions.md, tests/fixtures or tools as of 2026-09-07 (supervisor grep); ranges 5117xxx, 6203xxx, 6204xxx, 6206xxx and 6208xxx are used (primary hard, primary brutal, D466 override/protocol, D572 brutal-b, D572 pool)' as const;
175:145:      message: `seed ${String(room.spec.seed)} has an undecodable creature footprint: ${error instanceof Error ? error.message : String(error)}`,
208:130:      message: `seed ${String(room.spec.seed)} failed reducer loading: ${error instanceof Error ? error.message : String(error)}`,
528:133:          message: `seed ${String(fixture.seed)} fixture is invalid JSON: ${error instanceof Error ? error.message : String(error)}`,
88:139:    ? { status: 'absent' as const, basis: 'required_engine_initialization_failed' as const, dispatchId, corroboration: ['startup failed'] }
89:165:    : { status: 'ready' as const, basis: 'required_cli_completed_with_valid_catalog' as const, dispatchId, advertisedInvocationCount: 0, resourceOperationCount: 0 };
93:135:      ? { status: 'delivered' as const, dispatchId, contextSha256: 'a'.repeat(64), measurement: { baseBytes: 100, semanticBytes: 10 } }
94:130:      : { status: 'not_requested' as const, dispatchId, reason: 'catalog_ready_model_did_not_fetch' as const, measurement: null };
95:121:  const state = applyRoomInitiativeProfile(decodeArenaFixtureText(readFileSync(cell.fixturePath, 'utf8')), 'derived_v1');
103:125:    escalationModel: null, escalationEffort: null, midRoundAdjustmentsEnabled: false, fallbackReason: null, planner: 'model',
148:141:      : { version: 2, status: 'incomplete', passed: false, forbiddenContentPassed: true, delivery, missingRequiredFields: ['turn_context'] },
180:132:      reviewerApproval: { reviewer: 'independent-reviewer', approvedAt: '2026-09-09T20:00:00.000Z', decision: 'approved' as const },
188:135:function mutateFirst(source: string, mutation: (row: Readonly<Record<string, unknown>>) => Readonly<Record<string, unknown>>): string {
215:130:  const scores = Object.fromEntries(D569_PRIMARY_JUDGES.map(({ seat }) => [seat, [] as Readonly<Record<string, unknown>>[]])) as {
220:129:      scores[seat].push({ blindId: entry.blindId, targetPriority: 2, actionEconomy: 2, coherence: 1, positioning: 1, total: 6 });
476:126:  const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
597:123:      const cell = hardCells.find((candidate) => `${String(candidate.seed - 5_117_000)}:${String(candidate.rep)}` === key);
665:134:  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
690:129:  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
754:122:    const rows = BRUTAL_SOURCE.trimEnd().split('\n').map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
 .../unit/tools/d569-second-family-manifest.test.ts | 87 ++++++++++++++++++++-
 tests/unit/tools/d569-v5.test.ts                   | 12 +++
 tools/d569-second-family-manifest.ts               | 90 +++++++++++++++++++---
 3 files changed, 176 insertions(+), 13 deletions(-)
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089b..93df1172 100644
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@ function room(path: (typeof ALL_FIXTURE_PATHS)[number]): GeneratedRoom {
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@ function codes(
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@ describe('D569 independent second-family manifest', () => {
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@ describe('D569 independent second-family manifest', () => {
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8..6ca4f57e 100644
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@ import {
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@ function evaluateRunnerTimeoutConsumers(): {
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095..a26ca872 100644
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@ export const D569_USED_SEED_NAMESPACES = [
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,7 +64,18 @@ const manifestSchema = z.strictObject({
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
+
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
 
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
@@ -283,8 +293,7 @@ function everyMonsterProductive(room: GeneratedRoom): boolean {
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@ export function brutalRoomMembershipViolations(
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@ function sameNamespace(
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@ export function validateD569SecondFamilyManifest(
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@ export function validateD569SecondFamilyManifest(
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@ export function validateD569SecondFamilyManifest(
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc "git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2
find tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2 -type f -print0 | sort -z | xargs -0 sha256sum
sha256sum tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json
git diff --unified=0 | awk '/"'^'"\\+\\+\\+/{next} /"'^'"\\+/{line=substr("'$0,2); if (length(line)>120) print length(line) ":" line}'"'" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
6847b5d44b2577aa31a87a2eb83220ca17f54f8a8b160741d61d47019c75e51c  tests/fixtures/arena-basis-brutal-2/seed-6207001.json
9b1b9be44245659fb1a95922c685bbb1f9082b3f0fa591bd822553573a9d5c66  tests/fixtures/arena-basis-brutal-2/seed-6207002.json
afe31b4cb79f606bd56f29d37267814319de161db745747dae1a70a528617d6f  tests/fixtures/arena-basis-brutal-2/seed-6207003.json
96701e3136318b9f4dd8859311e30bb85586d129c8ef4424fcea398f3bbc813e  tests/fixtures/arena-basis-brutal-2/seed-6207004.json
a3a6774081b01b743091115b29ccfbb81d109e2f26867ce74a1c431b9a56a63a  tests/fixtures/arena-basis-brutal-2/seed-6207005.json
28800721eac25312561d04c49389f95a05ae24e81ff13b1ed6191951a7b34082  tests/fixtures/arena-basis-brutal-2/seed-6207006.json
eb9a566eab8c314fd754d85402e280e5f75ee30e114451b9405007a536375f8f  tests/fixtures/arena-basis-brutal-2/seed-6207007.json
5926d642f77ca2cdbf8cbed29408cfbf4165d6a05b453c2203a0aace93827a2b  tests/fixtures/arena-basis-brutal-2/seed-6207008.json
d398b3ad8a24ecae7d3989a1d47be1bedf69b1b989b17d967278be2b2ca7a50c  tests/fixtures/arena-basis-brutal-2/seed-6207009.json
b7efa3c2df793fcbbdba1d453358577a1d4eb7c6b2fe8c9b575d7fe85c3adf04  tests/fixtures/arena-basis-brutal-2/seed-6207010.json
7cc25c0ab9a0e380373a306874290d0f5247e74c0efb71e43ab593b58f0d9d3b  tests/fixtures/arena-basis-brutal-b/seed-6206001.json
3fdfe05812aeb6724789b4ff6f753ce68c523c13ff024d9ee275bd1060adbcaa  tests/fixtures/arena-basis-brutal-b/seed-6206002.json
36828c26a62a8af8c9cc32db7f3a13124d897a8aacf8ce4d4df2c11d2c742d3c  tests/fixtures/arena-basis-brutal-b/seed-6206003.json
35a5980497b2184a4d684e1849117bb70e4887cb40264ea33117e3e14322c60a  tests/fixtures/arena-basis-brutal-b/seed-6206004.json
dc48b6cdd7d4e33dc3c9a0188586ebd1537deac4dec4284c7e9404cec652e2f1  tests/fixtures/arena-basis-brutal-b/seed-6206005.json
d66fc0a9f29d84b5f1bc5a46b86483f91236d05b5cb1b951519cce7db98e2269  tests/fixtures/arena-basis-brutal-b/seed-6206006.json
1c4c337148afad9aff2a86143bb3156cc446ffabd2ccc8cc29eb004d83034bd9  tests/fixtures/arena-basis-brutal-b/seed-6206007.json
11a065b490f6e68effaddb0e919b9f9f9f7ae300f19a97f3359d3d4c86658a32  tests/fixtures/arena-basis-brutal-b/seed-6206008.json
19808aca9fb3ca7e4a77b6222cfae31c4f69a16b9b74ecc0706cb1eee58d4f5e  tests/fixtures/arena-basis-brutal-b/seed-6206009.json
a9efe78460c66972d09c6efd1c7cc04409ba6fff5a0028657d2442a0ec2fbc83  tests/fixtures/arena-basis-brutal-b/seed-6206010.json
3f737f1ddf714b0381abdc0e822b3a07cda4c55287a4bc0b1a97cd4d7d71d63b  tests/fixtures/arena-basis-brutal/seed-6203001.json
8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860  tests/fixtures/arena-basis-brutal/seed-6203002.json
d54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f  tests/fixtures/arena-basis-brutal/seed-6203003.json
6f9465eccac77eb60ffab2be1832f8f1bcd3290c655130c2ac752a38e6d75a20  tests/fixtures/arena-basis-brutal/seed-6203004.json
5b3be6abbdf2fcccff3243639d582bea2fb1098099fa91cf2208a0f143a2364e  tests/fixtures/arena-basis-brutal/seed-6203005.json
3e6818fe70e276e9ee7407d76b01e3afab7254cc1c27e649d5f445a5f6a6480c  tests/fixtures/arena-basis-brutal/seed-6203006.json
e6215e5b6388a79bc850e7ff97867f2618733acbb434526a77133e382cba2d1b  tests/fixtures/arena-basis-brutal/seed-6203007.json
b067b1aa081f368859979415c468d997b6fa504cad6ad70dc6998c90eeb3207c  tests/fixtures/arena-basis-brutal/seed-6203008.json
9780276cdec0ee71c99cc13217d73376990866f5ab3ab471c7a793196665f7ce  tests/fixtures/arena-basis-brutal/seed-6203009.json
2ec7f3c1298e7b6f3519cf765bcb88f36be3426e02cf7572730a77af9e32114d  tests/fixtures/arena-basis-brutal/seed-6203010.json
83daa7ea3ca89b270e5149368d09fa97074124dfe438cf95b346e996996026c8  tests/fixtures/d569-second-family-manifest.json
8c0bcb3c0afa8358c26129efbef82d0d2882135389d8dd8040fe7e4b6451e13a  tests/fixtures/d569-blind-experiment-manifest.json

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc "rg -n \"export const BLIND_SEMANTIC_BOARD_MAX_BYTES|export const SEMANTIC_BOARD_MAX_BYTES\" src/vtt/blind-turn-context.ts src/vtt/mcp/engine-server.ts
jq '.caps' tests/fixtures/d569-blind-experiment-manifest.json
sed -n '705,714p' tools/d569-blind-experiment.ts
sed -n '7463,7469p' tools/ai-dm-conversation.ts
sed -n '363,370p' tools/d569-v5/validate-first-arm.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
src/vtt/mcp/engine-server.ts:268:export const SEMANTIC_BOARD_MAX_BYTES = 8 * 1024;
src/vtt/blind-turn-context.ts:35:export const BLIND_SEMANTIC_BOARD_MAX_BYTES = 32_768 as const;
{
  "blindBaseBytes": 65536,
  "semanticBytes": 8192,
  "adviceBaseBytes": 32768
}
        sessionKey: sha256(canonicalJson({ family, arm: arm.id, seed: fixture.seed, rep })),
        timeoutMs: manifest.experimentWallMs,
        escalation: false,
        modelDefaultFallback: false,
        baseContextCapBytes: arm.dmMode === 'blind'
          ? manifest.caps.blindBaseBytes
          : manifest.caps.adviceBaseBytes,
        semanticContextCapBytes: SEMANTIC_BOARD_MAX_BYTES,
        requiredBlocks: ['semantic_board', 'creature_facts', 'initiative', 'legal_movement'],
        truncatedBlocks: [],
            engineCatalogEvidence: primaryCatalogEvidence,
            turnContextDelivery: primaryDelivery,
            turnContextConfiguredCaps: {
              baseBytes: config.turnContextMaximumBytes,
              semanticBytes: config.dmMode === 'blind' ? BLIND_SEMANTIC_BOARD_MAX_BYTES : SEMANTIC_BOARD_MAX_BYTES,
            },
            hostContextDiagnostic,
    const caps = current
      ? objectValue(row['turnContextConfiguredCaps'], 'turnContextConfiguredCaps')
      : objectValue(row['turnContextBudget'], 'turnContextBudget');
    const baseCap = current ? caps['baseBytes'] : caps['configuredBaseBytes'];
    const semanticCap = current ? caps['semanticBytes'] : caps['configuredSemanticBytes'];
    if (baseCap !== cell.baseContextCapBytes || semanticCap !== cell.semanticContextCapBytes) {
      throw new TypeError(`Registered context caps mismatch at ${observation.scheduledCellKey}.`);
    }

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

exec
/bin/bash -lc "git show HEAD:tools/d569-second-family-manifest.ts | awk 'length("'$0) > 120 { count += 1 } END { print count + 0 }'"'
git show HEAD:tests/unit/tools/d569-second-family-manifest.test.ts | awk 'length("'$0) > 120 { count += 1 } END { print count + 0 }'"'
git show HEAD:tests/unit/tools/d569-v5.test.ts | awk 'length("'$0) > 120 { count += 1 } END { print count + 0 }'"'
awk 'length("'$0) > 120 { count += 1 } END { print count + 0 }'"' tools/d569-second-family-manifest.ts
awk 'length("'$0) > 120 { count += 1 } END { print count + 0 }'"' tests/unit/tools/d569-second-family-manifest.test.ts
awk 'length("'$0) > 120 { count += 1 } END { print count + 0 }'"' tests/unit/tools/d569-v5.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
4
0
16
4
0
16

diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

codex
BLOCKED: the frozen plan’s claim that `d569-v5.test.ts` becomes green is infeasible at `6887755f`.

The historical v5 experiment expects an 8,192-byte semantic cap, while the current blind runner correctly emits D643’s retained 32,768-byte cap:

- `SEMANTIC_BOARD_MAX_BYTES = 8 * 1024`
- `BLIND_SEMANTIC_BOARD_MAX_BYTES = 32_768`
- v5 manifest `caps.semanticBytes = 8192`
- failure: `Registered context caps mismatch at 1:1` from `validate-first-arm.ts:369`

Resolving this requires an unplanned historical-v5 test projection or changing frozen v5 semantics. I stopped rather than introduce either shortcut.

Files modified, all allowed:

- [d569-second-family-manifest.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-second-family-manifest.ts)
- [d569-second-family-manifest.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts)
- [d569-v5.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts)

Implemented before the blocker:

- Strict v1/v2 schema union.
- Raw-byte hashing before projection.
- Legacy projection removes only the committed own `encounter.state.foggedCells` key.
- Generated bytes are never projected.
- V1 retains integrity/static brutal checks while retiring frozen productivity.
- V2 brutal fixtures retain full productivity validation and reject legacy fog keys.
- Historical key absence is rejected.
- v5/v1 path and version assertions added.

RED → GREEN:

- Second-family initial run: `46 passed / 4 failed`.
- Historical manifest initially produced 47 violations: 40 regeneration plus 7 retired productivity violations.
- Second-family final run: `50/50 passed`, zero v1 violations.
- v5 initial failure: `second_family_first_regeneration`.
- v5 after normalization: advances further but fails during collection on the unrelated 8,192/32,768 cap conflict; no tests execute.

Structural-difference table:

| Fixture | Only structural difference |
|---|---|
| hard-2/5118001 | `$.encounter.state.foggedCells` |
| hard-2/5118002 | `$.encounter.state.foggedCells` |
| hard-2/5118003 | `$.encounter.state.foggedCells` |
| hard-2/5118004 | `$.encounter.state.foggedCells` |
| hard-2/5118005 | `$.encounter.state.foggedCells` |
| hard-2/5118006 | `$.encounter.state.foggedCells` |
| hard-2/5118007 | `$.encounter.state.foggedCells` |
| hard-2/5118008 | `$.encounter.state.foggedCells` |
| hard-2/5118009 | `$.encounter.state.foggedCells` |
| hard-2/5118010 | `$.encounter.state.foggedCells` |
| brutal-2/6207001 | `$.encounter.state.foggedCells` |
| brutal-2/6207002 | `$.encounter.state.foggedCells` |
| brutal-2/6207003 | `$.encounter.state.foggedCells` |
| brutal-2/6207004 | `$.encounter.state.foggedCells` |
| brutal-2/6207005 | `$.encounter.state.foggedCells` |
| brutal-2/6207006 | `$.encounter.state.foggedCells` |
| brutal-2/6207007 | `$.encounter.state.foggedCells` |
| brutal-2/6207008 | `$.encounter.state.foggedCells` |
| brutal-2/6207009 | `$.encounter.state.foggedCells` |
| brutal-2/6207010 | `$.encounter.state.foggedCells` |

Mutant witnesses now green:

- Keep legacy key: killed by the 40 regeneration comparisons.
- Drop all committed state: killed by the extra `round` difference witness.
- Normalize generated bytes: killed by divergent generation 2.
- Allow legacy key in v2 brutal: killed by the rebound-hash v2 witness.
- Ignore regeneration 2: killed by the retained divergence witness.

Verification completed:

- Plan SHA-256: exact `2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802`; 399 lines.
- `npx tsc -p tsconfig.app.json --noEmit`: exit 0.
- `npx tsc -p tsconfig.node.json --noEmit`: exit 0.
- `git diff --check`: exit 0, empty.
- `git status --short`: exactly the three allowed files.
- V-freeze before and after: exit 0.
- All 32 historical inventory hashes remained identical.
- Manifest hashes remain `83daa7e…026c8` and `8c0bcb3…1e13a`.
- Added lines over 120 columns: 0. Existing counts remain unchanged at `4 / 0 / 16` across the three files.

Remaining red:

- `[suite collection] tests/unit/tools/d569-v5.test.ts` — historical v5 semantic cap 8,192 versus current blind-runner cap 32,768.

COHORT-01 B1 BLOCKED
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089bccefad7ffa76b6b71f9e6b1d2085f2f5..93df1172cfc29b979c29b130d3212f9fc884e249
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8d531087c53e4e7907f0293cbd5e58c08..6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -573,6 +574,17 @@
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095efbf5b163e76bc96283506b56b60acd4..a26ca87245a3ff9dab6a8cfe47d790045116ee6a
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,8 +64,19 @@
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
 
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
+
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
 
@@ -283,8 +293,7 @@
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

tokens used
134,616
BLOCKED: the frozen plan’s claim that `d569-v5.test.ts` becomes green is infeasible at `6887755f`.

The historical v5 experiment expects an 8,192-byte semantic cap, while the current blind runner correctly emits D643’s retained 32,768-byte cap:

- `SEMANTIC_BOARD_MAX_BYTES = 8 * 1024`
- `BLIND_SEMANTIC_BOARD_MAX_BYTES = 32_768`
- v5 manifest `caps.semanticBytes = 8192`
- failure: `Registered context caps mismatch at 1:1` from `validate-first-arm.ts:369`

Resolving this requires an unplanned historical-v5 test projection or changing frozen v5 semantics. I stopped rather than introduce either shortcut.

Files modified, all allowed:

- [d569-second-family-manifest.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-second-family-manifest.ts)
- [d569-second-family-manifest.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts)
- [d569-v5.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts)

Implemented before the blocker:

- Strict v1/v2 schema union.
- Raw-byte hashing before projection.
- Legacy projection removes only the committed own `encounter.state.foggedCells` key.
- Generated bytes are never projected.
- V1 retains integrity/static brutal checks while retiring frozen productivity.
- V2 brutal fixtures retain full productivity validation and reject legacy fog keys.
- Historical key absence is rejected.
- v5/v1 path and version assertions added.

RED → GREEN:

- Second-family initial run: `46 passed / 4 failed`.
- Historical manifest initially produced 47 violations: 40 regeneration plus 7 retired productivity violations.
- Second-family final run: `50/50 passed`, zero v1 violations.
- v5 initial failure: `second_family_first_regeneration`.
- v5 after normalization: advances further but fails during collection on the unrelated 8,192/32,768 cap conflict; no tests execute.

Structural-difference table:

| Fixture | Only structural difference |
|---|---|
| hard-2/5118001 | `$.encounter.state.foggedCells` |
| hard-2/5118002 | `$.encounter.state.foggedCells` |
| hard-2/5118003 | `$.encounter.state.foggedCells` |
| hard-2/5118004 | `$.encounter.state.foggedCells` |
| hard-2/5118005 | `$.encounter.state.foggedCells` |
| hard-2/5118006 | `$.encounter.state.foggedCells` |
| hard-2/5118007 | `$.encounter.state.foggedCells` |
| hard-2/5118008 | `$.encounter.state.foggedCells` |
| hard-2/5118009 | `$.encounter.state.foggedCells` |
| hard-2/5118010 | `$.encounter.state.foggedCells` |
| brutal-2/6207001 | `$.encounter.state.foggedCells` |
| brutal-2/6207002 | `$.encounter.state.foggedCells` |
| brutal-2/6207003 | `$.encounter.state.foggedCells` |
| brutal-2/6207004 | `$.encounter.state.foggedCells` |
| brutal-2/6207005 | `$.encounter.state.foggedCells` |
| brutal-2/6207006 | `$.encounter.state.foggedCells` |
| brutal-2/6207007 | `$.encounter.state.foggedCells` |
| brutal-2/6207008 | `$.encounter.state.foggedCells` |
| brutal-2/6207009 | `$.encounter.state.foggedCells` |
| brutal-2/6207010 | `$.encounter.state.foggedCells` |

Mutant witnesses now green:

- Keep legacy key: killed by the 40 regeneration comparisons.
- Drop all committed state: killed by the extra `round` difference witness.
- Normalize generated bytes: killed by divergent generation 2.
- Allow legacy key in v2 brutal: killed by the rebound-hash v2 witness.
- Ignore regeneration 2: killed by the retained divergence witness.

Verification completed:

- Plan SHA-256: exact `2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802`; 399 lines.
- `npx tsc -p tsconfig.app.json --noEmit`: exit 0.
- `npx tsc -p tsconfig.node.json --noEmit`: exit 0.
- `git diff --check`: exit 0, empty.
- `git status --short`: exactly the three allowed files.
- V-freeze before and after: exit 0.
- All 32 historical inventory hashes remained identical.
- Manifest hashes remain `83daa7e…026c8` and `8c0bcb3…1e13a`.
- Added lines over 120 columns: 0. Existing counts remain unchanged at `4 / 0 / 16` across the three files.

Remaining red:

- `[suite collection] tests/unit/tools/d569-v5.test.ts` — historical v5 semantic cap 8,192 versus current blind-runner cap 32,768.

COHORT-01 B1 BLOCKED

OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR] (network access enabled)
reasoning effort: high
reasoning summaries: none
session id: 01a0b20e-61fb-71f3-ad42-773bb45989b9
--------
user
RULES (binding, restated): You are the IMPLEMENTER (gpt-5.6-sol, high, RESUMED — COHORT-01 Batch 1, which you stopped BLOCKED correctly). Worktree /home/vagrant/PhpstormProjects/dnd-wt-cohort-01 (claude/cohort-01 at 6887755f + your three uncommitted Batch 1 files). Same sandbox/forbidden rules as your brief: workspace-write in that worktree only; no git writes; no .claude/** or docs/**; no claude / other agents; no Playwright/full vitest/build; never regenerate an expectation from our own output; the V-freeze gate (v1/v5 manifests, brutal/brutal-b/brutal-2/hard-2 fixtures byte-frozen) before and after. The plan is now FROZEN at sha256 4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69 (403 lines) — re-read it; the supervisor appended binding amendment N2 (at the end) which resolves your blocker: manifest-declared caps are the cell caps (version-aware at tools/d569-blind-experiment.ts:285/:712: v5 = 8192 history, v6 = 32768), and d569-v5.test.ts stays historical by PINNING that a row produced by today's live runner is refused with exactly "Registered context caps mismatch" (no skip, no relaxed cap), while its manifest validation, hashes and synthesized rows stay green. Mutants: v5 accepting a 32,768 row → red; (the v6 side belongs to Batch 6 — do not build v6 now, but leave the version-aware seam ready and tested with an in-test v6-shaped manifest fragment if the plan's Batch 1 Allowed files permit; otherwise note it for Batch 6). Keep every Batch 1 witness you already have green (second-family 50/50 etc.).

VERIFICATION: tsc app/node; git diff --check; the second-family suite, d569-v5.test.ts (now collecting and running — report Tests counts), d569-blind-experiment.test.ts (list reds by title: the frozen-family ones are expected); V-freeze before/after; git status --short = Batch 1 Allowed files only (list them). OUTPUT: report, then "COHORT-01 B1 FIX R1 DONE" (or BLOCKED with the gap).

codex
I’ll verify the amended 403-line plan and the preserved three-file worktree first, then rerun the expanded pre-change freeze gate including `hard-2`. After reading N2 in context, I’ll adjust only the historical v5 test behavior and the version-aware cap seam authorized by the amended Batch 1 scope.
exec
/bin/bash -lc "sha256sum .tmp-plans/2026-09-17-cohort-01-plan.md
wc -l .tmp-plans/2026-09-17-cohort-01-plan.md
git branch --show-current
git rev-parse HEAD
git status --short
git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2 tests/fixtures/arena-basis-hard-2
sed -n '1,460p' .tmp-plans/2026-09-17-cohort-01-plan.md" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69  .tmp-plans/2026-09-17-cohort-01-plan.md
403 .tmp-plans/2026-09-17-cohort-01-plan.md
claude/cohort-01
6887755fe0f204e4bf6d9a37147fa1476bdbada4
 M tests/unit/tools/d569-second-family-manifest.test.ts
 M tests/unit/tools/d569-v5.test.ts
 M tools/d569-second-family-manifest.ts
# VIS-FIELD-01 / COHORT-01 implementation plan r3 (D643 option A1)

Date: 2026-09-17. Baseline: detached clean `807d2e3c8cbbda11cfa26d078ac7739f2954e27e`.
D643 A1 keeps historical hard-2 and adds only three solid-wall brutal v2 families.

## Outcome and hard gates

1. WALL-01 is a hard predecessor. Generation cannot start until its D642 corner seal is
   accepted on a clean checkout. Recheck `src/vtt/room-generator.ts:1227-1231` still defines
   `generatedLineIsOpen` as `tier === 'none' && !blocksSight`. Re-derive all three Batch-9
   expectations against the accepted WALL-01 tree before editing tests; if any stated result
   differs, stop and amend/review this plan rather than copying changed-code output.
2. Before edits, inventory SHA-256 for the v1/v5 manifests and every file under historical
   6203, 6206, and 6207 directories. Repeat after every batch. No byte may change in:
   `tests/fixtures/d569-second-family-manifest.json`,
   `tests/fixtures/d569-blind-experiment-manifest.json`,
   `tests/fixtures/arena-basis-brutal`, `arena-basis-brutal-b`, or `arena-basis-brutal-2`.
3. Never rewrite a historical fixture/manifest, hand-edit generated JSON, add a D466 override,
   use `--terrain-profile`, or relax membership. One failing room rejects all ten seeds; no
   nine-room cohort and no hash is retained.
4. Expectations may come only from frozen pre-change bytes, the hand calculations named here,
   or hashes made after membership and double-generation gates. Generated changed-code output
   is never an oracle for a behavioural expectation.
5. No full Vitest, Playwright, build, or port 4173 in a batch. Later landing gates are separate.
6. Fixed scope is 10 batches with Allowed-file counts `3/3/10/10/10/8/4/4/3/1`: 52 unique
   paths = 18 existing + 34 new. A batch may touch only its manifest and accepted earlier files.

Common checks after every batch:

- **T:** `npx tsc -p tsconfig.app.json --noEmit` and
  `npx tsc -p tsconfig.node.json --noEmit` -> exit 0/0.
- **V:** `git diff --check` -> empty; `git status --short` contains only allowed/earlier paths.
- **V-freeze:** `git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2` -> exit 0.

## D569 demonstrate-then-normalize ledger

1. **Historical regeneration pins:** demonstrate each of 20 committed second-family fixtures
   differs from each canonical rebuild at exactly `$.encounter.state.foggedCells`; preserve raw
   hashes, remove exactly that own key from parsed COMMITTED state, then compare both rebuilds.
2. **New fixture pins:** dated namespace proof -> decoded structural/productivity membership ->
   two independent temp generations with empty `diff -rq` -> `sha256sum` = OpenSSL -> pin.
3. **Second-family v2 pin:** byte-copy hard-2 rows/hashes from v1, add proven 6211 rows, finalize,
   hash v2 independently, then place that hash in v6.
4. **Experiment v6 pin:** byte-carry all v5 fields except version, primary brutal, and nested
   second-family path/hash; pin the final outer v6 manifest hash separately from its contents.
5. **Behaviour pins:** use only Batch 9's explicit ray/path/table calculations, rechecked after
   WALL-01. Preserve stale values only as history comments.
6. **Cap pin:** D643 fixes 32,768; measure the hand-authored oversized block, never derive a cap.

## Pre-Batch-2 namespace reservation gate

Run this after accepted WALL-01 and before adding any range to source/tests. The first command
uses the authoritative MAIN file, not the stale 22,707-line worktree copy; the second audits
tracked consumers. Copy commands, stdout, and statuses verbatim into the dated v2 ledger entry.

```text
rg -n -o -e '6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
rg -n -o -e '6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' tests/fixtures tools
rg -n -o -e '\b6209\b|\b6210\b|\b6211\b' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
```

Verified 2026-09-17 output is, verbatim: command 1 -> `23048:6209xxx`,
`23048:6210xxx`, `23048:6211xxx`, `23061:6209xxx`, `23061:6210xxx`,
`23061:6211xxx`, `23071:6209xxx`, `23071:6_209_001`, status 0; command 2 ->
empty stdout/status 1; command 3 -> `23068:6209`, `23068:6210`, `23068:6211`,
status 0. Lines 23048/23061 are costing/authorized reservations, 23068 is D645's
shorthand plan summary, and 23071 is D646's review example: none is prior seed use.
Record one reservation covering all three families before any rejection/reselection.
Any new non-reservation match is a hard stop. If a range later fails, retain it as rejected,
audit a fresh contiguous range in all three representations, and have the supervisor amend this
plan's exact Batch-2 seed table, one Batch-3/4/5 Allowed-file manifest, and all downstream
manifest/route constants before creating replacement bytes. Unamended replacement paths are
unauthorized; the other two initial reservations remain reserved.

## Ordered batches

### Batch 1 — validator normalization and historical semantics

**Allowed files (3):**

- `tools/d569-second-family-manifest.ts`
- `tests/unit/tools/d569-second-family-manifest.test.ts`
- `tests/unit/tools/d569-v5.test.ts`

**R:** add a per-fixture 20-row structural-diff table (one legacy path each, hence today's 40
regeneration violations). Add rebound-hash witnesses proving (a) any second state-field/value
difference still yields both `first_regeneration` and `independent_regeneration`, and (b) a
NEW v2 brutal fixture containing `foggedCells` is rejected even if its hash is rebound. Retain
the two-generation divergence witness. These are hand-authored mutations, not regenerated pins.

**Implementation:** make the schema a strict v1/v2 union. Hash raw bytes first. For v1 hard and
brutal, and v2's carried hard-2 only, parse COMMITTED bytes, require the legacy key, delete only
own `encounter.state.foggedCells`, canonicalize with one newline, and compare each current
canonical rebuild to that projection. Never project generated bytes. V2 brutal-2-v2 compares
raw committed/current canonical bytes and rejects the legacy key. V1 retains integrity plus
static budget/caster/terrain checks but no longer recertifies frozen brutal productivity; v2
retains full current membership/productivity. `d569-v5.test.ts` imports/asserts only the v5/v1
paths and version, uses `canonicalD569SecondFamilyRegeneration`, and becomes green because this
normalization makes v1 valid; it must never resolve v6. `seed - 6203000` may remain only there.

**M:** keep the key, drop all state, normalize generated bytes, allow a new-v2 legacy key, and
ignore regeneration 2; the 40-count, extra-diff, new-v2, and divergence witnesses kill each.

**T/V:** common checks; serially run the two listed suites -> 20/20 one-key demonstrations,
zero v1 violations, all negative witnesses green.

### Batch 2 — RED active membership and frozen-productivity retirement

**Allowed files (3):**

- `tests/unit/vtt/arena-basis-brutal-v2.test.ts` (new)
- `tests/unit/vtt/room-generator.test.ts`
- `tests/unit/vtt/arena-basis-brutal-b.test.ts`

**R:** after the reservation gate, declare exact 6209001-10, 6210001-10, 6211001-10 paths;
expect exactly 30 missing fixtures. Add decoded clones for a nonproductive living monster, a
positive-movement-only Dash monster that MUST pass, and a targeted offensive option whose
resolved origin has a blocked or total trace and MUST fail.

**Implementation:** mirror `arena-basis-brutal-b.test.ts` but order proof before hashes:
identity/reducer load; brutal budget and 90-100% spend; implemented caster; terrain density/count;
then preserve the existing productivity predicate exactly: each living monster has some valid
option with `movementCostFeet > 0` OR an attack/save/cast/world-object slot. A Dash-only monster
therefore remains productive. If the qualifying option has targeted attack/save/cast components,
each target trace from `resolution.mechanics.finalPosition` must be sight-clear and non-total;
movement-only/world-object qualification does not invent a LOS target. Cite the contract and
`room-generator.ts:1227`'s stricter generated-room predicate. Mandatory offense is out of scope.

Move precisely these active assertions: retire `room-generator.test.ts:588-601` productivity for
frozen 6203001-10 while retaining its 6203001-3 byte/digest pins; add equivalent generation and
productivity assertions for 6209. In `arena-basis-brutal-b.test.ts:292-397`, retain structural,
reducer, SHA, and round-execution pins but retire only 378-393's 6206 productivity assertion.
Batch 1 retires only the analogous 6207 productivity assertion. Each comment cites D635/D642/
D643: frozen bytes are history; 6209/6210/6211 own the live contract. No skip/todo.

**M/T/V:** force productivity true, delete the movement arm, and treat `blocksSight:true` as open;
the nonproductive, Dash-only, and blocked-target clones kill each mutant. Common checks; active
suite is RED only for 30 missing files; frozen suites have no productivity red.

### Batch 3 — generate primary brutal-v2 6209001-6209010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-v2/seed-6209001.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209002.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209003.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209004.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209005.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209006.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209007.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209008.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209009.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209010.json`

**R/M/Generation/V:** Batch-2 tests are RED first. Twice run
`npx vite-node tools/generate-arena-basis.ts -- --difficulty brutal --seed 6209001 --rooms 10 --out <fresh-temp>/arena-basis-brutal-v2`.
Assert no parsed `terrainProfile` and `d466GeneratedRoomOverride(seed) === null` for 10/10.
Require empty `diff -rq A B`, then decoded membership 10/10 and both mutants. Only then move A
unchanged to Allowed files and rerun membership/common checks. Do not compute hashes yet.

### Batch 4 — generate brutal-b-v2 6210001-6210010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json`

**R/M/T/V:** identical to Batch 3 with seed 6210001/directory `arena-basis-brutal-b-v2`:
two temps, empty diff, no profile/override, decoded membership 10/10, mutants, common checks, no hashes.

### Batch 5 — generate D569 brutal-2-v2 6211001-6211010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json`

**R/M/T/V:** identical with seed 6211001/directory `arena-basis-brutal-2-v2`. End with one
membership-only run proving exactly 30/30 fixtures and every living monster; any failure rejects
that full ten-seed namespace and invokes the pre-Batch-2 replacement procedure.

### Batch 6 — v2/v6 manifests, identity boundary, and hashes

**Allowed files (8):**

- `tools/d569-second-family-manifest.ts`
- `tools/d569-blind-experiment.ts`
- `tests/fixtures/d569-second-family-manifest-v2.json` (new)
- `tests/fixtures/d569-blind-experiment-manifest-v6.json` (new)
- `tests/unit/tools/d569-second-family-manifest.test.ts`
- `tests/unit/tools/d569-blind-experiment.test.ts`
- `tests/unit/tools/d569-v6.test.ts` (new)
- `tests/unit/vtt/arena-basis-brutal-v2.test.ts`

**R:** strict version/path/range/ledger tests, the exact v5-v6 structural diff, outer-v6 hash,
and cardinalities. Add primary-hard and second-family-hard cross-version witnesses: a v5 observed
row with the same unchanged seed/state/visual hashes must be rejected by v6 as wrong experiment
identity. A missing/wrong identity must also reject. V6 ordinals use manifest order/seed maps;
no `seed - 6203000` assumption survives in a v6 path.

**Implementation:** v2 is `d569-second-family-manifest-v2`: byte-carried hard-2 rows/hashes,
proven brutal-2-v2 rows/hashes, the dated verbatim audit/classification, and this exact ordered
ten-namespace union: 5117xxx, 5118xxx, 6203xxx, 6204xxx, 6206xxx, 6207xxx, 6208xxx,
6209xxx, 6210xxx, 6211xxx. Keep v1's five-entry historical constant/policy unchanged; add a v2
constant. V2 overlap validation exempts only the expected fixture in its own exact cohort
namespace (hard 5118xxx; brutal 6211xxx); cross-cohort and all other overlaps still fail. Removing
ANY of ten namespaces fails an exact-ledger test; preserve the precedent mutant that removes
6204xxx. V6 is
`d569-blind-experiment-v6`: unchanged hard 5117, primary brutal 6209, nested v2 path/hash, every
other v5 field byte-carried. V1/v5 constants and files remain historical; add v2/v6 constants.

Create a separate `D569ExperimentIdentity = {version, manifestSha256}` from the exact outer
manifest bytes at the load boundary. Pin/export the final v6 SHA-256 outside the manifest.
V6 dry-run cells carry both fields; include both in `sessionKey`. V6 observed rows must carry an
exact match, and row lookup/duplicate/missing keys include identity. Branch by version so v5's
cell shape, legacy session-key derivation, and observed-row matching remain byte-behaviourally
unchanged. Content/state/visual hashes remain truthful content hashes, never overloaded as the
experiment identity. Thus unchanged hard cells cannot cross v5/v6.

The active `d569-v6.test.ts` access MUST set `regenerate:
canonicalD569SecondFamilyRegeneration`; the fixture-reader helper at
`d569-blind-experiment.test.ts:294-296` may serve historical/synthetic unit tests only. Add a spy
or deliberately divergent canonical regeneration proving v6 actually calls it. Only after all
30 membership checks pass, hash each fixture with SHA/OpenSSL, finalize/hash v2, insert its hash
in v6, finalize/hash v6, and pin that outer hash in code/test.

**M:** omit experiment identity from session/row keys; substitute a v5 hard row; use committed
bytes as v6 regeneration; remove 6204 or any ledger row; over-broaden the 5118/6211 exception;
admit v1/6203, a 6207/6209 overlap, a fourth carry-forward difference, or generation 2 divergence.
Named witnesses fail each mutant.

**T/V:** common checks; both hash tools agree for 30 fixtures, v2, and outer v6. Targeted D569
suites have zero violations and exact core/with-hint counts 480/660, primary-brutal 240/330,
second-family core 480. Historical v1/v5 suites remain green and never execute v6.

### Batch 7 — versioned arena and rerun routes

**Allowed files (4):**

- `tools/ai-dm-arena.ts`
- `tests/unit/tools/ai-dm-arena.test.ts`
- `tools/ai-dm-rerun-packet.ts`
- `tests/unit/tools/ai-dm-rerun-packet-protocol.test.ts`

**R/Implementation:** first fail exact parser/path/wrong-family cases. Add arena basis labels
`brutal-v2` -> 6209 and `brutal-b-v2` -> 6210, both difficulty brutal. At rerun definitions near
lines 26/547 add closed protocols `brutal-10-v2` and `brutal-10-b-v2`, exact ten-seed arrays and
directories. Historical labels/protocols retain their old paths and behaviour.

**M/T/V:** map new labels to old paths, swap new ranges, and remove an old label; exact new and
historical route tests kill each. Common checks; arena has prior 6 + 2 labels, rerun prior 3 + 2.

### Batch 8 — LOS audit and blind fixture report

**Allowed files (4):**

- `tools/los-cover-era-audit.ts`
- `tests/unit/tools/los-cover-era-audit.test.ts`
- `tools/blind-context-fixture-report.ts`
- `tests/unit/vtt/blind-turn-context.test.ts`

**R/Implementation:** first expect 30 missing rows. Add file-backed `brutal-v2`,
`brutal-b-v2`, `brutal-2-v2`; preserve historical entries. LOS audit is 93 rooms (63+30), ten
per new family. Blind report is 63 cases (33+30), decoded from exact committed paths.

**M/T/V:** omit or alias a family; family/seed/path/hash/count assertions fail. Common checks;
focused outputs are 93 and 63, with 10/10/10 new rows and 63/63 schema-valid cap-fit cases.

### Batch 9 — hand-authored behaviour pins

**Allowed files (3):**

- `tests/unit/vtt/composite-turn-proposals.test.ts`
- `tests/unit/vtt/engine-opportunity-movement-intel.test.ts`
- `tests/unit/vtt/speculative-planning.test.ts`

**R:** preserve the three current deterministic RED titles before edits. Re-run the following
geometry calculations after WALL-01; a discrepancy stops the batch, never selects a new oracle.

**Implementation:** composite hard 5117005 labels are exactly `Dodge`, `End Turn`, with no
`Longbow`. Comment the four corner-ray derivation: Scout `(13,3)` -> Fighter `(1,2)` has two rays
enter `(7,2)` and two on shared edge `(7,2)/(7,3)`; -> Cleric `(2,4)` has two on
`(7,3)/(7,4)` and two entering `(7,4)`; -> Wizard `(1,6)` all cross column 7. D635/D642 block
every set. Room 8 pins monsters 1 `(22,1)`, 2 `(21,1)`, 4 `(21,3)` to zero-foot Dodge at current
cells; monster 5 keeps 55-ft Dash `(22,5)->(12,6)` = ten 5-ft steps + 5 difficult. Replace the
stale comment; these are hand calculations.

For real integration, replace frozen 5117001 with a 15x8 state: Scout A at `(13,4)`, actionless
monster B at `(13,6)`, unacted 30-ft player P at `(1,4)`, ids `combatant:A/B/P`, base walls
`(7,0..3)` plus `(8,5..7)`, and a typed speed-set effect with
`enginePlanningSpeedFeet(A) === 0`. Define exact extracted selector strings
`DA=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:A'}})`
and `TAP=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:P'}})`.
Production `buildHostScenarioMenu` yields labels `[Longbow + Longbow -> combatant:P, Dodge]` and
exact menu rank 1 `adjacency:${DA}:${TAP}` (P moves to `(12,4)`, 55 ft) then rank 2
`visibility:${DA}:${TAP}` (P path `(1,4)->(0,3)->(0,2)->(0,1)->(1,0)`, 20 ft); each has one
proposal reference, only influencing player P, radius 60, and score `1*60=60`. Longbow reach
cannot flip inside 15x8; Dodge contributes no movement-change fact.

The mutation adds `(8,4)`, completing `(7,3)/(8,4)`. Assert the production trace changes
sight-clear -> blocked. Because A's speed is explicitly zero it cannot use Astra's 25-ft escape
to `(8,3)`; A and B now propose `[Dodge,Dodge]`, targeted dependencies disappear, and the exact
menu is `[]`. This is a contrast, not normalization of the frozen empty scene. Recheck the full
base/mutated oracle after WALL-01; any difference stops for plan review.

Separately test the eight-entry slice through `computeHostSplitCandidates` with two Scouts A/B,
P at `(4,2)`, awaiting A's turn, speed 30/radius 60, and a controlled query table. Define
`S(x)=canonicalJson({actorId:'combatant:'+x,selector:{kind:'combatant',combatantId:'combatant:'+x}})`.
All adjacency/reach baselines are false -> true; all visibility baselines are true -> false.
The nine non-shadowed candidates and hand oracle are:

|rank|fact key|refs|legal P destination/path ft|score|result|
|---:|---|---:|---|---:|---|
|1|`adjacency:S(A):S(P)`|A+B|`(2,1)`/10|`2*60=120`|kept|
|2|`adjacency:S(B):S(P)`|B|`(7,3)`/20|60|kept|
|3|`adjacency:S(P):S(A)`|A|`(2,1)`/10|60|kept|
|4|`adjacency:S(P):S(B)`|B|`(7,3)`/20|60|kept|
|5|`reach:S(A):shortsword:S(P)`|A|`(2,1)`/10|60|kept|
|6|`reach:S(B):shortsword:S(P)`|B|`(7,3)`/20|60|kept|
|7|`visibility:S(A):S(P)`|A|`(6,1)`/15|60|kept|
|8|`visibility:S(B):S(P)`|B|`(6,3)`/15|60|kept|
|9|`visibility:S(P):S(A)`|A|`(6,1)`/15|60|excluded|

All costs are independently <=60; the controlled port rejects every unlisted destination.
Order is score, refs, then canonical fact-key bytes (`adjacency` < `reach` < `visibility`). Assert
exact keys/ranks 1-8 and excluded key 9, not merely `<=8`. This stipulated dependency/query table
tests only grouping, score, canonical tie ordering, filtering, and the eight-entry slice; it is
not evidence for D642 corner sealing, which the production integration witness above proves.

**M/T/V:** restore old Longbow/Dash values, let A move despite speed zero, disable corner sealing,
change score multiplication to 62/61, reverse the tie order, or remove `.slice(0,8)`; exact
labels/keys/paths, production mutation, and nine-row table fail. Common checks; run serially.

### Batch 10 — 32,768-byte cap rejection witness

**Allowed files (1):**

- `tests/unit/vtt/blind-turn-context.test.ts`

**R/Implementation:** clone a normal `projectEngineSemanticBoard`, set one schema-valid creature
name to 40,000 characters, prove schema parsing succeeds, then render normally. Assert measured
UTF-8 semantic block exceeds `BLIND_SEMANTIC_BOARD_MAX_BYTES === 32_768` and exact `RangeError`
states required/max byte counts. Baseline may be green because this witnesses existing policy.

**M/T/V:** disable `blind-turn-context.ts:1304-1307` (or make `>` false): the test must turn RED
because oversized context is accepted. Restore, verify source hash, run focused test/common checks.

## Final targeted verification and handoff

Run only touched suites serially: second-family, blind-experiment, v5, v6, room-generator,
brutal-v2, brutal-b, arena, rerun protocol, LOS audit, blind context, and the three behaviour
files. Expected: zero failures; D569 counts 480/660/240/330 and second-family 480; 30/30 new
fixtures; audits 93/63. Repeat T, freeze inventory, `git diff --check`, verify exact 52-path union,
and inspect all new JSON as parsed state. Supervisor later runs the untruncated full gate.

## Open questions

None. Any failed post-WALL derivation or rejected namespace invokes the stated stop/amend/review
procedure; it is not an implementation-time choice.

## Round 2 disposition (closed/superseded)

F1 remains closed at lines 211-250; F4 remains closed at lines 25-26 and 198-209.
F2's r2 work remains at 50-73 and F3 is superseded by the corrected r3 oracle at 304-348.
Supporting requirements remain at 8-12, 85-102, 236-239, and 293-348.

## Round 3 disposition

- **PLAN-F3 (P2; lines 304-348):** the production scene now makes A speed zero, uses extracted actor-relative
  selector keys, and pins both base candidates plus the exact empty post-seal menu; the verified
  nine-row table is explicitly ranking/filtering-only.
- **PLAN-F2 (P2; lines 50-73):** both absence commands now cover every 000-999 plain and underscore seed plus
  `xxx`; MAIN provenance, verbatim results, reservation classification, and replacement rules stay.
- **PLAN-F5 (P2; lines 217-246):** v2 now requires all ten historical/current namespaces, exact own-cohort
  exceptions, removal-of-any-row tests, and the 6204 mutant.
- **PLAN-F6 (P2; lines 115-138):** Batch 2 now preserves positive movement OR productive slots, keeps Dash-only
  monsters valid, and applies resolved-origin trace checks only to targeted offensive components.

COHORT-01 PLAN R3 DONE

## Binding amendment N1 (astra r3, D650 — supervisor-appended; part of the frozen plan)

Batch 9 must construct the hand-authored Scout A with `profile.rules.speed: feet(0)` before encounter creation, retain the typed zero-speed effect, and assert both declared and effective speed are zero (attack declarations take their movement allowance from `subject.profile.rules.speed`, offer-declarations.ts:431–465, so the effect alone still lets A move 25 ft to (8,3) and keep its Longbow). Preserve the exact base and mutated labels, selector keys, scores and menus at the lines the plan states (base: Longbow ×2 + Dodge, adjacency and visibility keys, scores 60/60; with (8,4) added: Dodge/Dodge, menu []). Require two mutants: restoring A's declared speed to 30 while retaining the effect, and separately disabling corner sealing, must each fail the mutation expectations. Re-verify against the ACCEPTED WALL-01 before implementation acceptance. This correction is confined to the allowed speculative-planning test file; no production movement change is authorized.

## Binding amendment N2 (supervisor ruling D651 — part of the frozen plan)

Batch 1 exposed a second historical discontinuity beside the fog key: the v5 manifest registers `caps.semanticBytes = 8192` (the engine-server `SEMANTIC_BOARD_MAX_BYTES`), while rows the live blind runner produces today carry the D643 cap `BLIND_SEMANTIC_BOARD_MAX_BYTES = 32,768`, so `validate-first-arm.ts:369` refuses them ("Registered context caps mismatch"). Ruling: (1) a cell's `semanticContextCapBytes` / `baseContextCapBytes` are the MANIFEST's declared caps (the registration), not an engine constant — `tools/d569-blind-experiment.ts:285` (schema literal) and `:712` (cell cap) become version-aware: v5 keeps 8192 as history, v6 declares `caps.semanticBytes: 32768`; (2) `tests/unit/tools/d569-v5.test.ts` stays HISTORICAL: it keeps validating the v5 manifest (fog-key normalization included), its hashes and its synthesized rows, and it PINS that a row produced by today's live runner is refused with exactly `Registered context caps mismatch` — v5 cannot be re-run as registered and the test says so; no skip, no relaxed cap; (3) the v6 active test (Batch 6) validates live rows at 32,768. Mutants: v5 accepting a 32,768 row must fail; v6 accepting an 8192 row must fail. No production cap value changes.

codex
The amended plan and worktree match exactly, and the expanded pre-change freeze gate exited 0. N2 does not add `tools/d569-blind-experiment.ts` to Batch 1’s three-file allowlist, so I will not edit that production file or add a synthetic v6 fragment here; the v6 half remains explicitly for Batch 6. For Batch 1, I’m changing the v5 test so synthesized registered rows remain green while live 32,768-byte runner rows are required to fail with the exact historical-cap mismatch.
exec
/bin/bash -lc "nl -ba tests/unit/tools/d569-v5.test.ts | sed -n '440,590p'
nl -ba tests/unit/tools/d569-v5.test.ts | sed -n '570,730p'
rg -n \"caps:|semanticBytes\" tools/d569-blind-experiment.ts | head -30
nl -ba tools/d569-blind-experiment.ts | sed -n '270,295p'" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
   440	  const evidence = classifyEngineCatalogEvidence({
   441	    dispatchId, expectedProfile: 'blind', expectedPhase: 'primary',
   442	    expectedRequestId: 'request:d569-invalid-catalog', expectedToolNames: ['engine.get_turn_context'],
   443	    events: [], readiness, completed: false, requiredStartupFailed: false,
   444	    ...(source === 'malformed' ? { malformedReadiness: true } : {}),
   445	  });
   446	  if (evidence.status !== 'inconclusive' || evidence.reason !== 'invalid_catalog_response') {
   447	    throw new Error(`${source} consumer fixture did not classify as invalid catalog evidence.`);
   448	  }
   449	  return evidence;
   450	}
   451	
   452	function evaluateRunnerTimeoutConsumers(): {
   453	  readonly packetRows: number;
   454	  readonly registeredRows: number;
   455	  readonly observedViolations: readonly string[];
   456	  readonly contradictoryPacketError: string;
   457	  readonly contradictoryRegisteredError: string;
   458	  readonly contradictoryObservedViolations: readonly string[];
   459	  readonly invalidCatalogResults: readonly {
   460	    readonly exit: 'timed_out' | 'cancelled';
   461	    readonly source: InvalidCatalogSource;
   462	    readonly packetError: string;
   463	    readonly registeredError: string;
   464	    readonly observedViolations: readonly string[];
   465	  }[];
   466	} {
   467	  const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
   468	  const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
   469	    .map((cell) => current(cell, 'authorized'));
   470	  packetRows[0] = produced;
   471	  const packetCount = pairedDocuments(packetRows).packet.length;
   472	  const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
   473	    .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   474	  registeredRows[0] = produced;
   475	  const registeredCount = validateBrutal(raw(registeredRows)).length;
   476	  const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   477	  if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   478	  const boardImage = record(produced['boardImage']);
   479	  const observedSessionId = produced['sessionId'];
   480	  const scheduledCellKey = produced['scheduledCellKey'];
   481	  const dispatchId = produced['dispatchId'];
   482	  if (typeof observedSessionId !== 'string' || observedSessionId.length === 0 ||
   483	    typeof scheduledCellKey !== 'string' || typeof dispatchId !== 'string') {
   484	    throw new Error('Runner timeout row omitted its observed session or dispatch identity.');
   485	  }
   486	  const observed: D569ObservedRow = {
   487	    arm: cell.arm, cli: 'codex', cliVersion: 'codex-cli 0.153.4', model: cell.model, effort: cell.effort,
   488	    family: cell.family, basis: cell.basis, seed: cell.seed, rep: cell.rep,
   489	    sessionId: observedSessionId, outcome: 'refused', scheduledCellKey, dispatchId,
   490	    engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
   491	    turnContextDelivery: { status: 'timeout_before_delivery' },
   492	    timeoutMs: cell.timeoutMs, escalationModel: null, modelDefaultFallback: false,
   493	    stateHash: cell.stateHash, sharedKbHash: cell.sharedKbHash,
   494	    visualSourceHash: cell.visualSourceHash, visualProfileHash: cell.visualProfileHash,
   495	    visualArtifactHash: String(boardImage['sha256']), baseContextCapBytes: cell.baseContextCapBytes,
   496	    semanticContextCapBytes: cell.semanticContextCapBytes, truncatedBlocks: [],
   497	  };
   498	  const observedViolations = validateD569ObservedRows([cell], [observed]).map((violation) => violation.code);
   499	  const catalog = record(produced['engineCatalogEvidence']);
   500	  const contradictory = {
   501	    ...produced,
   502	    engineCatalogEvidence: { ...catalog, reason: 'conflicting_success_and_failure' },
   503	  };
   504	  const contradictoryPacketRows = [...packetRows];
   505	  contradictoryPacketRows[0] = contradictory;
   506	  const contradictoryRegisteredRows = [...registeredRows];
   507	  contradictoryRegisteredRows[0] = contradictory;
   508	  const invalidCatalogResults = (['timed_out', 'cancelled'] as const).flatMap((exit) =>
   509	    (['malformed', 'wrong_profile'] as const).map((source) => {
   510	      const invalidCatalog = classifiedInvalidCatalogForConsumer(dispatchId, source);
   511	      const delivery = exit === 'timed_out'
   512	        ? produced['turnContextDelivery']
   513	        : {
   514	            status: 'not_requested' as const, dispatchId,
   515	            reason: 'dispatch_cancelled' as const, measurement: null,
   516	          };
   517	      const ingress = record(produced['blindIngressAudit']);
   518	      const invalidRow = {
   519	        ...produced,
   520	        ...(exit === 'cancelled'
   521	          ? {
   522	              outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
   523	              turnContextDelivery: delivery,
   524	              blindIngressAudit: { ...ingress, delivery },
   525	              failingDispatch: {
   526	                phase: 'primary', exit, dispatchId, engineCatalogEvidence: invalidCatalog,
   527	                turnContextDelivery: delivery, failureReason: 'operator cancelled before delivery',
   528	              },
   529	            }
   530	          : {}),
   531	        engineCatalogEvidence: invalidCatalog,
   532	      };
   533	      const invalidPacketRows = [...packetRows];
   534	      invalidPacketRows[0] = invalidRow;
   535	      const invalidRegisteredRows = [...registeredRows];
   536	      invalidRegisteredRows[0] = invalidRow;
   537	      const observedDelivery: D569ObservedRow['turnContextDelivery'] = exit === 'timed_out'
   538	        ? { status: 'timeout_before_delivery' }
   539	        : { status: 'not_requested', reason: 'dispatch_cancelled' };
   540	      const invalidObserved: D569ObservedRow = {
   541	        ...observed,
   542	        outcome: exit === 'timed_out' ? 'refused' : 'infrastructure_failed',
   543	        engineCatalogEvidence: { status: 'inconclusive', reason: invalidCatalog.reason },
   544	        turnContextDelivery: observedDelivery,
   545	        ...(exit === 'cancelled' ? {
   546	          failingDispatch: {
   547	            dispatchId, exit, engineCatalogEvidence: { status: 'inconclusive' },
   548	            turnContextDelivery: { status: 'not_requested' },
   549	          },
   550	        } : {}),
   551	      };
   552	      return {
   553	        exit, source,
   554	        packetError: capturedFailure(() => { pairedDocuments(invalidPacketRows); }),
   555	        registeredError: capturedFailure(() => { validateBrutal(raw(invalidRegisteredRows)); }),
   556	        observedViolations: validateD569ObservedRows([cell], [invalidObserved])
   557	          .map((violation) => violation.code),
   558	      };
   559	    }));
   560	  return {
   561	    packetRows: packetCount,
   562	    registeredRows: registeredCount,
   563	    observedViolations,
   564	    contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
   565	    contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
   566	    contradictoryObservedViolations: validateD569ObservedRows([cell], [{
   567	      ...observed,
   568	      engineCatalogEvidence: { status: 'inconclusive', reason: 'conflicting_success_and_failure' },
   569	    }]).map((violation) => violation.code),
   570	    invalidCatalogResults,
   571	  };
   572	}
   573	
   574	const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
   575	
   576	describe('D569 v5 replacement validation and paired analysis tools', () => {
   577	  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
   578	    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
   579	      readonly version?: unknown;
   580	    };
   581	    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
   582	    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
   583	    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
   584	    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
   585	    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
   586	  });
   587	
   588	  it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
   589	    const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
   590	      roomIndex + 1, rep,
   570	    invalidCatalogResults,
   571	  };
   572	}
   573	
   574	const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
   575	
   576	describe('D569 v5 replacement validation and paired analysis tools', () => {
   577	  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
   578	    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
   579	      readonly version?: unknown;
   580	    };
   581	    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
   582	    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
   583	    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
   584	    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
   585	    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
   586	  });
   587	
   588	  it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
   589	    const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
   590	      roomIndex + 1, rep,
   591	      [6, 19, 20].includes(roomIndex * 3 + rep) ? null : `historical-session-${String(roomIndex + 1)}-${String(rep)}`,
   592	    ))).flat();
   593	    const original = raw(originalRows);
   594	    const sidecar = reconciliation(original);
   595	    const hardCells = CELLS.filter((cell) => cell.basis === 'hard');
   596	    const replacements = raw(D569_HARD_REPLACEMENT_KEYS.map((key) => {
   597	      const cell = hardCells.find((candidate) => `${String(candidate.seed - 5_117_000)}:${String(candidate.rep)}` === key);
   598	      if (cell === undefined) throw new Error(`Missing hard replacement cell ${key}.`);
   599	      return current(cell, key === '2:1' ? 'infrastructure_failed' : key === '4:1' ? 'service_null' : 'authorized');
   600	    }));
   601	    const mixed = mergeRepairedHard(original, replacements);
   602	    const originalLines = original.trimEnd().split('\n');
   603	    const mixedLines = mixed.trimEnd().split('\n');
   604	    expect(mixedLines.filter((line, index) => line === originalLines[index])).toHaveLength(27);
   605	    expect(validateD569FirstArm(mixed, sidecar)).toHaveLength(30);
   606	  });
   607	
   608	  it('migrates every registered validator invariant for a complete current brutal grid', () => {
   609	    expect(validateBrutal(BRUTAL_SOURCE)).toHaveLength(30);
   610	  });
   611	
   612	  it('validates registered hard replacements against retained original cells and reconciliation provenance', () => {
   613	    const hardCells = CELLS.filter((cell) => cell.basis === 'hard');
   614	    const original = raw(hardCells.map((cell, index) => retainedHistorical(cell, index + 1)));
   615	    const sidecar = reconciliation(original);
   616	    const replacements = raw(D569_HARD_REPLACEMENT_KEYS.map((key) => {
   617	      const [roomText, repText] = key.split(':');
   618	      const seed = 5_117_000 + Number(roomText);
   619	      const cell = hardCells.find((candidate) => candidate.seed === seed && candidate.rep === Number(repText));
   620	      if (cell === undefined) throw new Error(`Missing registered hard replacement ${key}.`);
   621	      return current(cell, 'authorized');
   622	    }));
   623	    const merged = mergeRepairedHard(original, replacements);
   624	    const observations = validateD569RegisteredFirstArm({
   625	      rawText: merged, basis: 'hard', cliVersion: 'codex-cli 0.153.4',
   626	      patchedCommit: PATCHED_COMMIT, sidecar,
   627	    });
   628	    expect(observations).toHaveLength(30);
   629	    expect(observations.filter((entry) => entry.row['sessionId'] === null)
   630	      .map((entry) => entry.effectiveSessionId)).toEqual([
   631	        'recovered-session-6', 'recovered-session-19', 'recovered-session-20',
   632	      ]);
   633	  });
   634	
   635	  it('accepts a registered host-authorization refusal without treating attribution as a default', () => {
   636	    const refused = mutateFirst(BRUTAL_SOURCE, (_row) => ({
   637	      ...current(CELLS.find((cell) => cell.basis === 'brutal')!, 'refused'),
   638	      fallbackReason: 'host_authorization_failed', planner: 'model',
   639	    }));
   640	    expect(validateBrutal(refused)).toHaveLength(30);
   641	  });
   642	
   643	  it('rejects a row that actually executed a model default', () => {
   644	    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => ({
   645	      ...row, planner: 'engine_default', fallbackReason: 'no_proposal',
   646	    })))).toThrow('model_default_fallback');
   647	  });
   648	
   649	  it('accepts cancellation after delivered context in registered validation', () => {
   650	    const cancelled = mutateFirst(BRUTAL_SOURCE, (row) => {
   651	      const dispatchId = String(row['dispatchId']);
   652	      return {
   653	        ...row, outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
   654	        failingDispatch: {
   655	          phase: 'primary', exit: 'cancelled', dispatchId,
   656	          engineCatalogEvidence: row['engineCatalogEvidence'],
   657	          turnContextDelivery: row['turnContextDelivery'],
   658	          failureReason: 'operator cancelled after delivery',
   659	        },
   660	      };
   661	    });
   662	    expect(validateBrutal(cancelled)).toHaveLength(30);
   663	  });
   664	
   665	  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
   666	    const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
   667	    expect(produced).toMatchObject({
   668	      outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
   669	      turnContextDelivery: { status: 'delivered', measurement: { semanticBytes: expect.any(Number) } },
   670	      failingDispatch: {
   671	        phase: 'primary', exit: 'cancelled',
   672	        turnContextDelivery: { status: 'delivered', measurement: { semanticBytes: expect.any(Number) } },
   673	      },
   674	    });
   675	    expect(produced['semanticBoardBytes']).toEqual(expect.any(Number));
   676	    expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
   677	    const failure = record(produced['failingDispatch']);
   678	    expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
   679	    const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
   680	      .map((cell) => current(cell, 'authorized'));
   681	    packetRows[0] = produced;
   682	    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
   683	
   684	    const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
   685	      .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   686	    registeredRows[0] = produced;
   687	    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
   688	  });
   689	
   690	  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
   691	    const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
   692	    expect(produced).toMatchObject({
   693	      outcome: 'refused', fallbackReason: 'timeout',
   694	      engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
   695	      turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
   696	    });
   697	    expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
   698	    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
   699	    expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
   700	    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
   701	    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');
   702	    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryObservedViolations).toContain('integrity_indeterminate');
   703	  });
   704	
   705	  it('rejects malformed and wrong-profile catalog evidence after timeout or cancellation in every consumer', () => {
   706	    expect(RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults).toHaveLength(4);
   707	    expect(RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults.map(({ exit, source }) => `${exit}:${source}`))
   708	      .toEqual([
   709	        'timed_out:malformed', 'timed_out:wrong_profile',
   710	        'cancelled:malformed', 'cancelled:wrong_profile',
   711	      ]);
   712	    for (const result of RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults) {
   713	      expect(result.packetError).toContain('integrity evidence');
   714	      expect(result.registeredError).toContain('integrity-indeterminate');
   715	      expect(result.observedViolations).toContain('integrity_indeterminate');
   716	    }
   717	  });
   718	
   719	  it('rejects incomplete v3 evidence objects', () => {
   720	    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => {
   721	      const catalog = record(row['engineCatalogEvidence']);
   722	      return { ...row, engineCatalogEvidence: { status: catalog['status'], dispatchId: catalog['dispatchId'] } };
   723	    }))).toThrow();
   724	  });
   725	
   726	  it('rejects an indeterminate delivery when the companion v3 catalog field is absent', () => {
   727	    expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => {
   728	      const { engineCatalogEvidence: _omitted, ...withoutCatalog } = row;
   729	      return {
   730	        ...withoutCatalog,
283:  caps: z.strictObject({
285:    semanticBytes: z.literal(SEMANTIC_BOARD_MAX_BYTES),
   270	      ),
   271	    }),
   272	  ]),
   273	  experimentWallMs: z.literal(D569_EXPERIMENT_WALL_MS),
   274	  reps: z.literal(D569_REPS),
   275	  blindMaxAttempts: z.literal(3),
   276	  execution: z.strictObject({
   277	    freshSessionPerCell: z.literal(true),
   278	    escalation: z.literal(false),
   279	    modelDefaultFallback: z.literal(false),
   280	    transport: z.literal('mcp_minimal'),
   281	    boardImage: z.literal('png'),
   282	  }),
   283	  caps: z.strictObject({
   284	    blindBaseBytes: z.literal(BLIND_TURN_CONTEXT_MAX_BYTES),
   285	    semanticBytes: z.literal(SEMANTIC_BOARD_MAX_BYTES),
   286	    adviceBaseBytes: z.literal(TURN_CONTEXT_MAX_BYTES),
   287	  }),
   288	  bootstrap: z.strictObject({
   289	    resamples: z.literal(D569_BOOTSTRAP_RESAMPLES),
   290	    seed: z.number().int().nonnegative(),
   291	    confidence: z.literal(0.95),
   292	    noninferiorityMargin: z.literal(D569_NONINFERIORITY_MARGIN),
   293	  }),
   294	  sharedKb: z.strictObject({
   295	    rootPath: z.literal(D569_AI_DM_KB_ROOT),

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..1ae35898641e03686f89d367519155d2bf0a74a6
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,6 +678,7 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
@@ -684,18 +687,21 @@
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..1ae35898641e03686f89d367519155d2bf0a74a6
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,6 +678,7 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
@@ -684,18 +687,21 @@
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

exec
/bin/bash -lc 'npx vitest run --configLoader runner --reporter=verbose tests/unit/tools/d569-v5.test.ts' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 24164ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > resolves only the historical v5 experiment and v1 second-family manifests 2ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > validates a mixed 27-historical/3-current hard grid without rewriting retained bytes 6ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > migrates every registered validator invariant for a complete current brutal grid 221ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > validates registered hard replacements against retained original cells and reconciliation provenance 248ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > accepts a registered host-authorization refusal without treating attribution as a default 217ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a row that actually executed a model default 221ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > accepts cancellation after delivered context in registered validation 217ms
 × tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a live-runner cancellation row under the historical v5 semantic cap 37ms
   → row 1.turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a live-runner timeout row under the historical v5 semantic cap 1ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects malformed and wrong-profile catalog evidence after timeout or cancellation in every consumer 0ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects incomplete v3 evidence objects 188ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects an indeterminate delivery when the companion v3 catalog field is absent 186ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a conflicting_success_and_failure catalog when the companion v3 delivery field is absent 185ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a invalid_catalog_response catalog when the companion v3 delivery field is absent 186ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects incomplete v3 evidence nested in a failing dispatch 185ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a missing v3 host diagnostic 185ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects partial v3 hybrids and integrity-indeterminate rows before analysis 1ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered seed mutations 189ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered fixture-state mutations 190ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered code-cohort mutations 190ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered model provenance mutations 189ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered effort provenance mutations 188ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered KB hash provenance mutations 192ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered KB components provenance mutations 189ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered visual pin provenance mutations 191ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered manifest mutations before accepting rows 93ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > runs raw outcomes through packet/key construction and the registered paired scorer 41ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a live-runner cancellation row under the historical v5 semantic cap
TypeError: row 1.turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.
 ❯ parseArenaRow tools/ai-dm-rerun-packet.ts:878:13
    876|       const issue = parsed.error.issues[0];
    877|       const property = issue === undefined || issue.path.length === 0 …
    878|       throw new TypeError(`${sourceLabel}${property} is invalid: ${iss…
       |             ^
    879|     }
    880|     return { rowEra: 'post_shift', row: parsed.data };
 ❯ tools/ai-dm-rerun-packet.ts:998:22
 ❯ validateRows tools/ai-dm-rerun-packet.ts:992:24
 ❯ buildPacket tools/ai-dm-rerun-packet.ts:1257:25
 ❯ buildRerunPacket tools/ai-dm-rerun-packet.ts:1354:10
 ❯ pairedDocuments tests/unit/tools/d569-v5.test.ts:212:23
 ❯ tests/unit/tools/d569-v5.test.ts:685:12

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 26 passed (27)
   Start at  21:25:51
   Duration  23.86s (transform 1.71s, setup 0ms, import 18.43s, tests 3.94s, environment 0ms)


diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..1ae35898641e03686f89d367519155d2bf0a74a6
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,6 +678,7 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
@@ -684,18 +687,21 @@
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

exec
/bin/bash -lc "nl -ba tools/d569-v5/validate-first-arm.ts | sed -n '90,125p'
rg -n \"turnContextBudget\" tools/d569-v5/validate-first-arm.ts | head -20" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
    90	}
    91	
    92	export const D569_RAW_OUTCOMES = [
    93	  'authorized', 'refused', 'auto_resolved', 'awaiting_dm_adjudication', 'local_error',
    94	  'execution_failed', 'partial_execution', 'service_null', 'infrastructure_failed',
    95	  'integrity_indeterminate',
    96	] as const;
    97	export type D569RawOutcome = (typeof D569_RAW_OUTCOMES)[number];
    98	const outcomeSchema = z.enum(D569_RAW_OUTCOMES);
    99	
   100	function historicalCellKey(row: Readonly<Record<string, unknown>>): string {
   101	  if (typeof row['room'] !== 'number' || !Number.isSafeInteger(row['room']) ||
   102	    typeof row['round'] !== 'number' || !Number.isSafeInteger(row['round'])) {
   103	    throw new TypeError('Historical D569 row lacks integer room/round identity.');
   104	  }
   105	  return `${String(row['room'])}:${String(row['round'])}`;
   106	}
   107	
   108	function validateV3(row: Readonly<Record<string, unknown>>): void {
   109	  if (row['rowContractVersion'] !== 'arena-row-v3') throw new TypeError('Unknown row contract version.');
   110	  const scheduledCellKey = z.string().regex(/^\d+:[1-9]\d*$/u).parse(row['scheduledCellKey']);
   111	  const dispatchId = z.string().min(16).parse(row['dispatchId']);
   112	  const catalog = catalogSchema.parse(row['engineCatalogEvidence']);
   113	  const delivery = deliverySchema.parse(row['turnContextDelivery']);
   114	  z.object({ baseBytes: z.number().nonnegative(), semanticBytes: z.number().nonnegative() })
   115	    .parse(row['turnContextConfiguredCaps']);
   116	  hostContextDiagnosticSchema.parse(row['hostContextDiagnostic']);
   117	  if (catalog.dispatchId !== dispatchId || delivery.dispatchId !== dispatchId) {
   118	    throw new TypeError(`D569 v3 dispatch correlation failed for ${scheduledCellKey}.`);
   119	  }
   120	  if (scheduledCellKey !== historicalCellKey(row)) {
   121	    throw new TypeError(`D569 v3 scheduled key does not match room/round for ${scheduledCellKey}.`);
   122	  }
   123	  if (row['outcome'] === 'integrity_indeterminate' || d569DeliveryHasIntegritySignal(catalog, delivery)) {
   124	    throw new TypeError(`D569 integrity-indeterminate observation ${scheduledCellKey} cannot enter validation.`);
   125	  }
365:      : objectValue(row['turnContextBudget'], 'turnContextBudget');
371:    const truncatedBlocks = row['turnContextBudget'] === undefined
373:      : objectValue(row['turnContextBudget'], 'turnContextBudget')['truncatedBlocks'];

diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..1ae35898641e03686f89d367519155d2bf0a74a6
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,6 +678,7 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
@@ -684,18 +687,21 @@
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

exec
/bin/bash -lc 'npx vitest run --configLoader runner --reporter=verbose tests/unit/tools/d569-v5.test.ts' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 24978ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > resolves only the historical v5 experiment and v1 second-family manifests 2ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > validates a mixed 27-historical/3-current hard grid without rewriting retained bytes 7ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > migrates every registered validator invariant for a complete current brutal grid 260ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > validates registered hard replacements against retained original cells and reconciliation provenance 255ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > accepts a registered host-authorization refusal without treating attribution as a default 234ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a row that actually executed a model default 225ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > accepts cancellation after delivered context in registered validation 226ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a live-runner cancellation row under the historical v5 semantic cap 233ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a live-runner timeout row under the historical v5 semantic cap 1ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects malformed and wrong-profile catalog evidence after timeout or cancellation in every consumer 0ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects incomplete v3 evidence objects 194ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects an indeterminate delivery when the companion v3 catalog field is absent 190ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a conflicting_success_and_failure catalog when the companion v3 delivery field is absent 186ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a invalid_catalog_response catalog when the companion v3 delivery field is absent 185ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects incomplete v3 evidence nested in a failing dispatch 187ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a missing v3 host diagnostic 188ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects partial v3 hybrids and integrity-indeterminate rows before analysis 1ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered seed mutations 193ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered fixture-state mutations 192ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered code-cohort mutations 190ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered model provenance mutations 194ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered effort provenance mutations 193ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered KB hash provenance mutations 194ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered KB components provenance mutations 193ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered visual pin provenance mutations 200ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects registered manifest mutations before accepting rows 93ms
 ✓ tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > runs raw outcomes through packet/key construction and the registered paired scorer 44ms

 Test Files  1 passed (1)
      Tests  27 passed (27)
   Start at  21:26:40
   Duration  24.65s (transform 1.82s, setup 0ms, import 18.75s, tests 4.26s, environment 0ms)


diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

codex
The amended v5 behavior is now green: `27/27`. Synthesized v5 rows still validate at the manifest-declared 8,192-byte cap; a live timeout row with 32,768 is rejected with the exact `Registered context caps mismatch at 1:1.` message, and a delivered live cancellation row is also rejected by the historical 8,192-byte packet schema before registered validation. I’m running the other required suites serially now.
exec
/bin/bash -lc 'npx vitest run --configLoader runner --reporter=verbose tests/unit/tools/d569-second-family-manifest.test.ts' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 7087ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118001.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 10ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118002.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118003.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118004.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118005.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118006.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118007.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118008.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118009.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118010.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207001.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207002.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207003.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207004.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 9ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207005.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207006.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207007.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207008.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207009.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207010.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > pins the amended exact contiguous ranges and verbatim independent ledger evidence 174ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118001.json satisfies every hard state-property invariant before its pin 5ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118002.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118003.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118004.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118005.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118006.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118007.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118008.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118009.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118010.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207001.json retains historical integrity plus static budget, caster, and terrain invariants 128ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207002.json retains historical integrity plus static budget, caster, and terrain invariants 49ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207003.json retains historical integrity plus static budget, caster, and terrain invariants 121ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207004.json retains historical integrity plus static budget, caster, and terrain invariants 262ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207005.json retains historical integrity plus static budget, caster, and terrain invariants 86ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207006.json retains historical integrity plus static budget, caster, and terrain invariants 77ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207007.json retains historical integrity plus static budget, caster, and terrain invariants 245ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207008.json retains historical integrity plus static budget, caster, and terrain invariants 96ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207009.json retains historical integrity plus static budget, caster, and terrain invariants 71ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207010.json retains historical integrity plus static budget, caster, and terrain invariants 91ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > second_family_uses_primary_seed: rejects primary or previously used seed namespaces 204ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > brutal_membership_checks_name_only: rejects structural failures even with the roster names intact 87ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > rejects missing or rewritten supervisor ledger evidence 95ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > rejects fixture bytes that no longer match the manifest hash 87ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > seed_hash_without_properties: a matching digest cannot excuse failed membership 85ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > second_generation_not_compared: rejects a divergent independent regeneration 85ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > legacy_projection_drops_all_state: retains any second committed state difference 86ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > legacy_projection_requires_the_committed_legacy_key 85ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key 1224ms

 Test Files  1 passed (1)
      Tests  50 passed (50)
   Start at  21:27:12
   Duration  6.76s (transform 1.22s, setup 0ms, import 1.78s, tests 3.53s, environment 0ms)


diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

exec
/bin/bash -lc 'npx vitest run --configLoader runner --reporter=verbose tests/unit/tools/d569-blind-experiment.test.ts' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 6727ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 × tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment 183ms
   → expected [ …(40) ] to deeply equal []
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > keeps historical amendments exact while excluding Opus 5 from every active registry 1ms
 × tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows 80ms
   → Invalid D569 experiment manifest: second_family_first_regeneration
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects the Opus notes source counted in the scoring panel or eligibility 4ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects a panel of only two scoring seats in manifest and registered analysis 1ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects Opus 5 in any active arm or panel position 140ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > accepts only notes or empty exposure and rejects a deciding Opus policy 1ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects reversed notes and scoring stages and altered panel aggregation 1ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > preserves all 32 committed input hash pins across Amendment 2 140ms
 × tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > adds exactly 180 separately labeled advice-assisted hint cells, 660 total 67ms
   → Invalid D569 experiment manifest: second_family_first_regeneration
 × tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > consumes the frozen second encounter family for all 480 core cells 67ms
   → Invalid D569 experiment manifest: second_family_first_regeneration
 × tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > records exact model effort cli and cliVersion for every observed row 67ms
   → Invalid D569 experiment manifest: second_family_first_regeneration
 × tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects either decisive integrity field alone while preserving rows with neither field 66ms
   → Invalid D569 experiment manifest: second_family_first_regeneration
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects changed advice baseline settings, primer, and model-default policy 64ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects changed KB and fixture bytes 128ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > requires every Astra arm and its code-only versus minhint repair contrast 127ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects an Astra hint arm missing its registered repair mode 62ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects Astra blind when code-only repair is replaced by a minimal hint 65ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects removal of the Astra code-only versus minhint repair comparison 61ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > registers direct high-effort Astra versus Sol contrasts in both modes 121ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > scores Astra by Astra and Sol by Sol while Fable has no player arm 124ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects a mutated historical amendment record 1ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > fluke_guard_uses_shuffle_seed: rejects a trained or replaced second encounter cohort 133ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 maxed effort state machine > maxed_after_one_no_gain: refuses medium before two consecutive high misses and low before two medium misses 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 maxed effort state machine > invalid gates do not count and a repeated positive gain resets no-gain progress 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal interval boundary matrix classifies [ -0.1, -0.01 ] as 'noninferior' 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal interval boundary matrix classifies [ -0.1, +0 ] as 'noninferior' 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal interval boundary matrix classifies [ +0, +0 ] as 'noninferior' 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal interval boundary matrix classifies [ -0.1, 0.01 ] as 'not_noninferior' 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal interval boundary matrix classifies [ +0, 0.01 ] as 'not_noninferior' 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal interval boundary matrix classifies [ 0.01, 0.1 ] as 'not_noninferior' 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal_upper_bound_not_lower: fails when only the refusal lower bound clears the margin 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal_direction_is_blind_minus_advice: accepts a nonpositive interval and rejects a positive interval 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal_margin_is_at_most: accepts an upper bound exactly equal to the named zero margin 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal_interval_not_mean: a negative mean with a positive upper bound fails 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > offense_is_strict_and_conjunctive: exact margin fails and just above passes only with refusal passing 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > invalid_interval_guard: missing, non-finite, or reversed interval evidence never qualifies 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > proves pairing direction, panel-seat averaging, components, and 100000-resample reproducibility 425ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > registered analysis rejects both executed sides when Astra is absent 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > registered analysis rejects an unknown third seat substituted for Astra 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > requires Fable Astra and Sol when the opposite side failed execution 1ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > registered analysis exposes no Opus input output or deciding path 1ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > rejects Opus-only and mixed Opus scoring diagnostic subsets 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > keeps registered scoring-seat subsets diagnostic and unable to qualify 1ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > success_uses_executed_only: refused rows remain zero in primary while executed-only is selection-conditioned 7ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal_cluster_distribution_end_to_end: zero-and-one clusters fail, while swapped arms pass 39ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > noninferiority_pools_bases: never lets hard success conceal brutal failure 6ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > requires the second encounter cohort to point the same way and independently pass 12ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 paired cluster analysis and success labels > refusal_only_failure_propagates: either primary basis or second family prevents overall success 211ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 override-ready sealed evidence > override_exposes_top_before_blind_seal: requires sealing before top reveal and answer-key-only top evidence 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 override-ready sealed evidence > requires identical capsules and validates the sealed blind-intent hash 0ms
 ✓ tests/unit/tools/d569-blind-experiment.test.ts > D569 override-ready sealed evidence > defines improvement as final policy minus always-engine-top, not blind-intent subset gain 0ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 6 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment
AssertionError: expected [ …(40) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+ ]

 ❯ tests/unit/tools/d569-blind-experiment.test.ts:409:27
    407|   it('validates the exact v5 Opus 4.8 arms and notes-only Fable Astra …
    408|     const frozen = manifest();
    409|     expect(codes(frozen)).toEqual([]);
       |                           ^
    410|     expect(frozen.version).toBe('d569-blind-experiment-v5');
    411|     expect(frozen.preregistrationAmendments).toEqual(EXPECTED_V5_AMEND…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:473:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > adds exactly 180 separately labeled advice-assisted hint cells, 660 total
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:635:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > consumes the frozen second encounter family for all 480 core cells
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:644:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > records exact model effort cli and cliVersion for every observed row
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:653:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects either decisive integrity field alone while preserving rows with neither field
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:683:18

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/6]⎯


 Test Files  1 failed (1)
      Tests  6 failed | 46 passed (52)
   Start at  21:27:23
   Duration  6.39s (transform 1.59s, setup 0ms, import 2.49s, tests 2.41s, environment 0ms)


diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

exec
/bin/bash -lc "nl -ba tests/unit/tools/d569-blind-experiment.test.ts | sed -n '260,330p'
rg -n \"secondFamilyAccess|regenerate:\" tests/unit/tools/d569-blind-experiment.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
   260	
   261	const EXPECTED_V5_SCORING_SEATS = [
   262	  { id: 'claude-fable-5-1', model: 'claude-fable-5-1', effort: 'high', role: 'scoring' },
   263	  { id: 'gpt-6-astra', model: 'gpt-6-astra', effort: 'high', role: 'scoring' },
   264	  { id: 'gpt-5.6-sol', model: 'gpt-5.6-sol', effort: 'high', role: 'scoring' },
   265	] as const;
   266	
   267	const EXPECTED_V5_ADVISORY_NOTES_STAGE = {
   268	  source: { id: 'claude-opus-4-8', model: 'claude-opus-4-8', effort: 'high', role: 'advisory_notes' },
   269	  stageOrder: ['advisory_notes_first', 'scoring_panel_second'],
   270	  advisoryTransmission: 'labelled_per_entry_note_text_only',
   271	  advisoryPromptNoteMaxWords: 15,
   272	  advisoryNoteMaxChars: 120,
   273	  advisoryInvalidPolicy: 'nonzero_exit_or_invalid_existing_output_becomes_empty_block',
   274	  advisoryExposureStatuses: ['notes', 'empty'],
   275	  advisoryAnalysisPolicy: 'exposure_status_only_source_artifact_never_loaded',
   276	  scoringSeatAdvisoryPolicy: 'consider_or_ignore_untrusted_no_weight_no_anchor',
   277	} as const;
   278	
   279	function readText(path: string): string {
   280	  const bytes = fixtureBytes.get(path);
   281	  if (bytes === undefined) throw new Error(`undeclared fixture ${path}`);
   282	  return bytes;
   283	}
   284	
   285	function manifest(): D569ExperimentManifest {
   286	  return JSON.parse(readText(D569_EXPERIMENT_MANIFEST_PATH)) as D569ExperimentManifest;
   287	}
   288	
   289	function access(readOverride?: (path: string) => string | undefined): D569ExperimentValidationAccess {
   290	  return {
   291	    readText: (path) => readOverride?.(path) ?? readText(path),
   292	    secondFamilyAccess: {
   293	      readFixture: (path) => readOverride?.(path) ?? readText(path),
   294	      regenerate: (seed, difficulty) => readText(
   295	        `tests/fixtures/arena-basis-${difficulty}-2/seed-${String(seed)}.json`,
   296	      ),
   297	    },
   298	  };
   299	}
   300	
   301	function codes(candidate: unknown, validationAccess = access()): readonly string[] {
   302	  return validateD569ExperimentManifest(candidate, validationAccess).map((violation) => violation.code);
   303	}
   304	
   305	function observedRows(cells: ReturnType<typeof dryRunD569Experiment>): readonly D569ObservedRow[] {
   306	  return cells.map((cell, index) => ({
   307	    arm: cell.arm,
   308	    cli: '/usr/local/bin/codex',
   309	    cliVersion: 'codex-cli 0.148.0',
   310	    model: cell.model,
   311	    effort: cell.effort,
   312	    family: cell.family,
   313	    basis: cell.basis,
   314	    seed: cell.seed,
   315	    rep: cell.rep,
   316	    sessionId: `fresh-session-${String(index)}`,
   317	    timeoutMs: cell.timeoutMs,
   318	    escalationModel: null,
   319	    modelDefaultFallback: false,
   320	    stateHash: cell.stateHash,
   321	    sharedKbHash: cell.sharedKbHash,
   322	    visualSourceHash: cell.visualSourceHash,
   323	    visualProfileHash: cell.visualProfileHash,
   324	    visualArtifactHash: sha256(`${cell.visualSourceHash}:${cell.visualProfileHash}`),
   325	    baseContextCapBytes: cell.baseContextCapBytes,
   326	    semanticContextCapBytes: cell.semanticContextCapBytes,
   327	    truncatedBlocks: [],
   328	  }));
   329	}
   330	
292:    secondFamilyAccess: {
294:      regenerate: (seed, difficulty) => readText(

diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

codex
The required blind-experiment baseline is unchanged: `46 passed / 6 failed`. All six reds are the frozen-family tests whose local helper returns committed legacy bytes as “regenerations,” producing the expected 40 second-family regeneration violations; no new red appeared. I’m finishing the two TypeScript checks and the post-change diff/status/freeze gates.
exec
/bin/bash -lc 'npx tsc -p tsconfig.app.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 14038ms:
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

exec
/bin/bash -lc 'npx tsc -p tsconfig.node.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 28763ms:
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

exec
/bin/bash -lc "git diff --check
git status --short
git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2 tests/fixtures/arena-basis-hard-2
git diff --unified=0 | awk '/"'^'"\\+\\+\\+/{next} /"'^'"\\+/{line=substr("'$0,2); if (length(line)>120) print length(line) ":" line}'"'
git diff --stat" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
 M tests/unit/tools/d569-second-family-manifest.test.ts
 M tests/unit/tools/d569-v5.test.ts
 M tools/d569-second-family-manifest.ts
 .../unit/tools/d569-second-family-manifest.test.ts | 87 ++++++++++++++++++++-
 tests/unit/tools/d569-v5.test.ts                   | 35 +++++++--
 tools/d569-second-family-manifest.ts               | 90 +++++++++++++++++++---
 3 files changed, 191 insertions(+), 21 deletions(-)

diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

exec
/bin/bash -lc "git diff -- tests/unit/tools/d569-v5.test.ts
git diff -- tools/d569-second-family-manifest.ts tests/unit/tools/d569-second-family-manifest.test.ts | sed -n '1,520p'" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index b20498b8..b8f6042e 100644
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -43,6 +43,7 @@ import {
 } from '../../../tools/d569-v5/validate-first-arm';
 import {
   D569_EXPERIMENT_MANIFEST_PATH,
+  D569_SECOND_FAMILY_MANIFEST_PATH,
   dryRunD569Experiment,
   validateD569ObservedRows,
   type D569DryRunCell,
@@ -450,7 +451,7 @@ function classifiedInvalidCatalogForConsumer(
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -471,7 +472,7 @@ function evaluateRunnerTimeoutConsumers(): {
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -558,7 +559,7 @@ function evaluateRunnerTimeoutConsumers(): {
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -573,6 +574,19 @@ function evaluateRunnerTimeoutConsumers(): {
 const RUNNER_TIMEOUT_CONSUMERS = evaluateRunnerTimeoutConsumers();
 
 describe('D569 v5 replacement validation and paired analysis tools', () => {
+  it('resolves only the historical v5 experiment and v1 second-family manifests', () => {
+    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
+      readonly version?: unknown;
+    };
+    expect(D569_EXPERIMENT_MANIFEST_PATH).toBe('tests/fixtures/d569-blind-experiment-manifest.json');
+    expect(D569_SECOND_FAMILY_MANIFEST_PATH).toBe('tests/fixtures/d569-second-family-manifest.json');
+    expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
+    expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
+    expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
+  });
+
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
     const originalRows = Array.from({ length: 10 }, (_, roomIndex) => [1, 2, 3].map((rep) => historical(
       roomIndex + 1, rep,
@@ -650,7 +664,7 @@ describe('D569 v5 replacement validation and paired analysis tools', () => {
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -664,26 +678,31 @@ describe('D569 v5 replacement validation and paired analysis tools', () => {
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 2613089b..93df1172 100644
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -55,6 +55,38 @@ function room(path: (typeof ALL_FIXTURE_PATHS)[number]): GeneratedRoom {
   return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
 }
 
+function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
+  if (Object.is(left, right)) return [];
+  if (Array.isArray(left) && Array.isArray(right)) {
+    const indexes = Array.from({ length: Math.max(left.length, right.length) }, (_, index) => index);
+    return indexes.flatMap((index) => structuralDifferencePaths(left[index], right[index], `${path}[${index}]`));
+  }
+  if (typeof left === 'object' && left !== null && typeof right === 'object' && right !== null) {
+    const leftRecord = left as Readonly<Record<string, unknown>>;
+    const rightRecord = right as Readonly<Record<string, unknown>>;
+    const keys = [...new Set([...Object.keys(leftRecord), ...Object.keys(rightRecord)])].sort();
+    return keys.flatMap((key) => {
+      if (!Object.hasOwn(leftRecord, key) || !Object.hasOwn(rightRecord, key)) return [`${path}.${key}`];
+      return structuralDifferencePaths(leftRecord[key], rightRecord[key], `${path}.${key}`);
+    });
+  }
+  return [path];
+}
+
+function replaceFixtureBytes(
+  candidate: D569SecondFamilyManifest,
+  path: string,
+  bytes: string,
+): D569SecondFamilyValidationAccess {
+  const entry = candidate.cohorts.flatMap((cohort) => cohort.fixtures)
+    .find((fixture) => fixture.path === path);
+  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
+  entry.sha256 = sha256(bytes);
+  return access({
+    readFixture: (fixturePath) => fixturePath === path ? bytes : access().readFixture(fixturePath),
+  });
+}
+
 function access(overrides: Partial<D569SecondFamilyValidationAccess> = {}): D569SecondFamilyValidationAccess {
   return {
     readFixture: (path) => {
@@ -75,6 +107,18 @@ function codes(
 }
 
 describe('D569 independent second-family manifest', () => {
+  it.each(ALL_FIXTURE_PATHS)(
+    '%s differs from its current canonical rebuild only at $.encounter.state.foggedCells',
+    (path) => {
+      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
+      const difficulty = path.includes('/arena-basis-hard-2/') ? 'hard' : 'brutal';
+      const seed = room(path).spec.seed;
+      const regenerated = JSON.parse(canonicalD569SecondFamilyRegeneration(seed, difficulty, 1)) as unknown;
+      expect(structuralDifferencePaths(committed, regenerated))
+        .toEqual(['$.encounter.state.foggedCells']);
+    },
+  );
+
   it('pins the amended exact contiguous ranges and verbatim independent ledger evidence', () => {
     const frozen = manifest();
     expect(D569_SECOND_FAMILY_SEEDS.hard).toEqual([
@@ -96,11 +140,12 @@ describe('D569 independent second-family manifest', () => {
   });
 
   it.each(BRUTAL_FIXTURE_PATHS)(
-    '%s satisfies budget, caster, terrain, and productive-offer invariants before its pin',
+    '%s retains historical integrity plus static budget, caster, and terrain invariants',
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
-      expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
+      expect(brutalRoomMembershipViolations(frozen)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
   );
 
@@ -185,4 +230,42 @@ describe('D569 independent second-family manifest', () => {
       },
     }))).toContain('independent_regeneration');
   });
+
+  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath);
+    (targetRoom.encounter.state as { round: number }).round += 1;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
+    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
+      'first_regeneration',
+      'independent_regeneration',
+    ]);
+  });
+
+  it('legacy_projection_requires_the_committed_legacy_key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = HARD_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
+    };
+    delete targetRoom.encounter.state.foggedCells;
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
+      .toContain('legacy_fogged_cells_missing');
+  });
+
+  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
+    const changed = structuredClone(manifest());
+    const targetPath = BRUTAL_FIXTURE_PATHS[0];
+    const targetRoom = room(targetPath) as GeneratedRoom & {
+      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
+    };
+    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
+    const changedBytes = `${canonicalJson(targetRoom)}\n`;
+    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
+    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
+    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
+  });
 });
diff --git a/tools/d569-second-family-manifest.ts b/tools/d569-second-family-manifest.ts
index 308a0095..a26ca872 100644
--- a/tools/d569-second-family-manifest.ts
+++ b/tools/d569-second-family-manifest.ts
@@ -43,8 +43,7 @@ export const D569_USED_SEED_NAMESPACES = [
   { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
 ] as const;
 
-const manifestSchema = z.strictObject({
-  version: z.literal('d569-second-family-manifest-v1'),
+const manifestFields = {
   generator: z.strictObject({
     tool: z.literal('tools/generate-arena-basis.ts'),
     canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
@@ -65,7 +64,18 @@ const manifestSchema = z.strictObject({
       sha256: z.string().regex(/^[a-f0-9]{64}$/u),
     })),
   })),
-});
+} as const;
+
+const manifestSchema = z.discriminatedUnion('version', [
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v1'),
+    ...manifestFields,
+  }),
+  z.strictObject({
+    version: z.literal('d569-second-family-manifest-v2'),
+    ...manifestFields,
+  }),
+]);
 
 export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
 export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
@@ -283,8 +293,7 @@ function everyMonsterProductive(room: GeneratedRoom): boolean {
       }));
 }
 
-/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
-export function brutalRoomMembershipViolations(
+function brutalRoomStaticMembershipViolations(
   room: GeneratedRoom,
   sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
 ): readonly D569SecondFamilyViolation[] {
@@ -323,6 +332,15 @@ export function brutalRoomMembershipViolations(
     'brutal_terrain',
     'brutal terrain must meet every configured minimum/count band and exceed hard density',
   );
+  return violations;
+}
+
+/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
+export function brutalRoomMembershipViolations(
+  room: GeneratedRoom,
+  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
+): readonly D569SecondFamilyViolation[] {
+  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   addViolation(
     violations,
     everyMonsterProductive(room),
@@ -342,6 +360,38 @@ function sameNamespace(
   return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
 }
 
+function encounterStateRecord(input: unknown): Record<string, unknown> {
+  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
+    throw new TypeError('fixture root is not an object');
+  }
+  const encounter = (input as Record<string, unknown>)['encounter'];
+  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
+    throw new TypeError('fixture encounter is not an object');
+  }
+  const state = (encounter as Record<string, unknown>)['state'];
+  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
+    throw new TypeError('fixture encounter state is not an object');
+  }
+  return state as Record<string, unknown>;
+}
+
+function legacyCommittedProjection(
+  parsed: unknown,
+  seed: number,
+  violations: D569SecondFamilyViolation[],
+): string {
+  const state = encounterStateRecord(parsed);
+  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
+  addViolation(
+    violations,
+    hasLegacyFoggedCells,
+    'legacy_fogged_cells_missing',
+    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
+  );
+  if (hasLegacyFoggedCells) delete state['foggedCells'];
+  return `${canonicalJson(parsed)}\n`;
+}
+
 /**
  * Validates the pins only after state membership and two independent canonical
  * regenerations. It is pure/model-free; callers provide repository bytes.
@@ -442,8 +492,22 @@ export function validateD569SecondFamilyManifest(
         `seed ${String(fixture.seed)} fixture hash changed`,
       );
 
+      let comparisonFixtureBytes = fixtureBytes;
       try {
-        const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+        const parsed = JSON.parse(fixtureBytes) as unknown;
+        const room = parsed as GeneratedRoom;
+        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
+          difficulty === 'hard';
+        if (usesLegacyProjection) {
+          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
+        } else {
+          addViolation(
+            violations,
+            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
+            'unexpected_legacy_fogged_cells',
+            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
+          );
+        }
         addViolation(
           violations,
           room.spec.seed === fixture.seed,
@@ -451,9 +515,13 @@ export function validateD569SecondFamilyManifest(
           `seed ${String(fixture.seed)} fixture contains a different seed`,
         );
         violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
-        violations.push(...(difficulty === 'hard'
-          ? hardRoomMembershipViolations(room)
-          : brutalRoomMembershipViolations(room)));
+        if (difficulty === 'hard') {
+          violations.push(...hardRoomMembershipViolations(room));
+        } else if (manifest.version === 'd569-second-family-manifest-v1') {
+          violations.push(...brutalRoomStaticMembershipViolations(room));
+        } else {
+          violations.push(...brutalRoomMembershipViolations(room));
+        }
       } catch (error) {
         violations.push({
           code: 'fixture_parse',
@@ -465,13 +533,13 @@ export function validateD569SecondFamilyManifest(
       const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
       addViolation(
         violations,
-        firstGeneration === fixtureBytes,
+        firstGeneration === comparisonFixtureBytes,
         'first_regeneration',
         `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
       );
       addViolation(
         violations,
-        secondGeneration === firstGeneration && secondGeneration === fixtureBytes,
+        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
         'independent_regeneration',
         `seed ${String(fixture.seed)} independent canonical regeneration differs`,
       );

diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

codex
Batch 1 Fix R1 is complete.

Changed only the three allowed files:

- [d569-second-family-manifest.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-second-family-manifest.ts)
- [d569-second-family-manifest.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts)
- [d569-v5.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts)

Implemented:

- Strict v1/v2 second-family schema.
- Raw fixture hashes checked before projection.
- Historical projection removes only the committed own `$.encounter.state.foggedCells`.
- Generated bytes remain untouched.
- V1 brutal fixtures retain static checks but no frozen productivity recertification.
- V2 brutal fixtures retain full productivity checks and reject legacy fog keys.
- All 20 fixtures demonstrate exactly one structural difference: `$.encounter.state.foggedCells`.
- Historical v5 paths, versions, manifest hashes, and synthesized rows remain pinned.
- v5 cells remain at the manifest-declared 8,192-byte semantic cap.
- Live 32,768-byte timeout rows are rejected exactly with `Registered context caps mismatch at 1:1.`
- Live delivered cancellation rows are rejected by both the historical packet schema and registered validation.

Test results:

- Second-family: `50/50 passed`.
- D569 v5: `27/27 passed`.
- D569 blind experiment: `46 passed / 6 expected frozen-family failures`.

Expected blind-experiment reds:

1. `validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment`
2. `dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows`
3. `adds exactly 180 separately labeled advice-assisted hint cells, 660 total`
4. `consumes the frozen second encounter family for all 480 core cells`
5. `records exact model effort cli and cliVersion for every observed row`
6. `rejects either decisive integrity field alone while preserving rows with neither field`

All six arise because that historical test helper deliberately returns committed legacy fixture bytes as both regenerations, leaving exactly 40 expected regeneration violations.

Verification:

- Frozen plan: SHA-256 `4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69`, 403 lines.
- `npx tsc -p tsconfig.app.json --noEmit`: exit 0.
- `npx tsc -p tsconfig.node.json --noEmit`: exit 0.
- `git diff --check`: exit 0, empty.
- Added lines over 120 columns: 0.
- Expanded V-freeze before/after, including `hard-2`: exit 0/0.
- No frozen fixture or manifest bytes changed.
- `git status --short`: exactly the three allowed files above.

The production v5/v6 schema seam in `tools/d569-blind-experiment.ts` remains for Batch 6 because that file is outside Batch 1’s allowlist. Batch 1 pins the required v5 half; Batch 6 must add v6’s 32,768 declaration and reject 8,192-byte v6 rows.

COHORT-01 B1 FIX R1 DONE
diff --git a/tests/unit/tools/d569-v5.test.ts b/tests/unit/tools/d569-v5.test.ts
index 6ca4f57e9a6dd7b8fadfc8f90a03ef5a84f803b5..b8f6042e7527e0fd72e256428b086fa5407e63be
--- a/tests/unit/tools/d569-v5.test.ts
+++ b/tests/unit/tools/d569-v5.test.ts
@@ -451,7 +451,7 @@
 
 function evaluateRunnerTimeoutConsumers(): {
   readonly packetRows: number;
-  readonly registeredRows: number;
+  readonly historicalRegistrationError: string;
   readonly observedViolations: readonly string[];
   readonly contradictoryPacketError: string;
   readonly contradictoryRegisteredError: string;
@@ -472,7 +472,7 @@
   const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
     .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   registeredRows[0] = produced;
-  const registeredCount = validateBrutal(raw(registeredRows)).length;
+  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
   const cell = CELLS.find((candidate) => candidate.basis === 'brutal' && candidate.seed === 6_203_001 && candidate.rep === 1);
   if (cell === undefined) throw new Error('Registered timeout cell is absent.');
   const boardImage = record(produced['boardImage']);
@@ -559,7 +559,7 @@
     }));
   return {
     packetRows: packetCount,
-    registeredRows: registeredCount,
+    historicalRegistrationError,
     observedViolations,
     contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
     contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
@@ -583,6 +583,8 @@
     expect(MANIFEST.version).toBe('d569-blind-experiment-v5');
     expect(MANIFEST.secondFamily.manifestPath).toBe(D569_SECOND_FAMILY_MANIFEST_PATH);
     expect(secondFamily.version).toBe('d569-second-family-manifest-v1');
+    expect(MANIFEST.caps.semanticBytes).toBe(8_192);
+    expect(new Set(CELLS.map((cell) => cell.semanticContextCapBytes))).toEqual(new Set([8_192]));
   });
 
   it('validates a mixed 27-historical/3-current hard grid without rewriting retained bytes', () => {
@@ -662,7 +664,7 @@
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

tokens used
181,713
Batch 1 Fix R1 is complete.

Changed only the three allowed files:

- [d569-second-family-manifest.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-second-family-manifest.ts)
- [d569-second-family-manifest.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts)
- [d569-v5.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts)

Implemented:

- Strict v1/v2 second-family schema.
- Raw fixture hashes checked before projection.
- Historical projection removes only the committed own `$.encounter.state.foggedCells`.
- Generated bytes remain untouched.
- V1 brutal fixtures retain static checks but no frozen productivity recertification.
- V2 brutal fixtures retain full productivity checks and reject legacy fog keys.
- All 20 fixtures demonstrate exactly one structural difference: `$.encounter.state.foggedCells`.
- Historical v5 paths, versions, manifest hashes, and synthesized rows remain pinned.
- v5 cells remain at the manifest-declared 8,192-byte semantic cap.
- Live 32,768-byte timeout rows are rejected exactly with `Registered context caps mismatch at 1:1.`
- Live delivered cancellation rows are rejected by both the historical packet schema and registered validation.

Test results:

- Second-family: `50/50 passed`.
- D569 v5: `27/27 passed`.
- D569 blind experiment: `46 passed / 6 expected frozen-family failures`.

Expected blind-experiment reds:

1. `validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment`
2. `dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows`
3. `adds exactly 180 separately labeled advice-assisted hint cells, 660 total`
4. `consumes the frozen second encounter family for all 480 core cells`
5. `records exact model effort cli and cliVersion for every observed row`
6. `rejects either decisive integrity field alone while preserving rows with neither field`

All six arise because that historical test helper deliberately returns committed legacy fixture bytes as both regenerations, leaving exactly 40 expected regeneration violations.

Verification:

- Frozen plan: SHA-256 `4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69`, 403 lines.
- `npx tsc -p tsconfig.app.json --noEmit`: exit 0.
- `npx tsc -p tsconfig.node.json --noEmit`: exit 0.
- `git diff --check`: exit 0, empty.
- Added lines over 120 columns: 0.
- Expanded V-freeze before/after, including `hard-2`: exit 0/0.
- No frozen fixture or manifest bytes changed.
- `git status --short`: exactly the three allowed files above.

The production v5/v6 schema seam in `tools/d569-blind-experiment.ts` remains for Batch 6 because that file is outside Batch 1’s allowlist. Batch 1 pins the required v5 half; Batch 6 must add v6’s 32,768 declaration and reject 8,192-byte v6 rows.

COHORT-01 B1 FIX R1 DONE
{
  "name": "srd-55",
  "private": true,
  "license": "MIT",
  "version": "0.0.0",
  "description": "SRD-55 is a local-first character builder",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader runner",
    "build": "tsc -b && node tools/dist-build-cache.mjs",
    "build:dist": "vite build --configLoader runner",
    "build:dist:validated": "tsc -b && npm run build:dist && node tools/assert-dist-clean.mjs",
    "serve": "node tools/serve.mjs",
    "serve:check": "playwright test --config=tests/serving/playwright.config.ts",
    "typecheck": "scripts/check-command-outcomes.sh && tsc -b",
    "check:command-outcomes": "scripts/check-command-outcomes.sh",
    "typecheck:fast": "tsgo -p tsconfig.app.json --noEmit && tsgo -p tsconfig.node.json --noEmit",
    "test": "vitest run --configLoader runner",
    "test:gate": "node scripts/check-command-outcomes.mjs && node tools/gate-vitest.mjs",
    "test:gate:browser": "node tools/gate-playwright.mjs",
    "test:affected": "node scripts/test-affected.mjs",
    "heldout:guard": "node --experimental-strip-types tools/heldout-leak-check.ts",
    "census:modules": "node scripts/module-state-census.mjs",
    "test:shuffled": "vitest run --configLoader runner --sequence.shuffle --sequence.seed=20260825",
    "test:unit": "vitest run --configLoader runner tests/unit",
    "test:live": "AI_BRIDGE_LIVE=1 vitest run --configLoader runner tests/live",
    "test:browser": "playwright test",
    "test:mutation": "node scripts/patch-stryker-vitest-runner.mjs && node scripts/mutation-loud.mjs",
    "test:mutation:sharded": "node scripts/patch-stryker-vitest-runner.mjs && node scripts/mutation-shard.mjs run-all --shards 8",
    "vtt:replay": "vite-node tools/vtt-replay.ts --",
    "vtt:soak": "vite-node tools/vtt-soak.ts --",
    "vtt:experiment": "vite-node tools/vtt-experiment.ts --",
    "vtt:regret": "vite-node tools/vtt-regret.ts --",
    "rehearse": "vite-node tools/rehearsal/run.ts --",
    "test:all": "npm run test && npm run build && npm run test:browser",
    "scrape": "vite-node tools/scrape/cli.ts --",
    "db:schema": "vite-node scripts/build-schema.ts",
    "db:migrations": "vitest run --configLoader runner tests/unit/db/migrations.test.ts",
    "db:contracts": "vite-node scripts/build-row-contracts.ts",
    "schema:content-pack": "vite-node tools/generate-content-pack-schema.ts",
    "schema:engine-mcp": "vite-node tools/generate-engine-mcp-schemas.ts",
    "attribute:round-input": "vite-node tools/attribute-round-input.ts --",
    "rl:extract-sft": "vite-node tools/rl/extract-sft.ts --",
    "rl:generate-data": "vite-node tools/rl/generate-data.ts --",
    "bootstrap": "node --experimental-strip-types tools/vtt-handoff/bootstrap.ts --",
    "doctor": "node --experimental-strip-types tools/vtt-handoff/doctor.ts --",
    "test:engine": "vitest run --configLoader runner tests/unit/vtt/encounter-session-service.test.ts tests/unit/vtt/door-intent.test.ts tests/unit/vtt/encounter-selectors.test.ts tests/unit/vtt/engine-boundary.test.ts",
    "test:protocol": "vitest run --configLoader runner tests/unit/vtt/handoff-contract.test.ts tests/unit/vtt/protocol-runtime.test.ts tests/unit/vtt/in-process-transport.test.ts tests/unit/vtt/engine-boundary.test.ts",
    "test:worker": "playwright test --config=tests/browser/vtt-handoff/playwright.config.ts worker.spec.ts",
    "test:runtime-node": "vitest run --configLoader runner tests/unit/vtt/node-runtime.test.ts tests/unit/vtt/node-websocket-transport.test.ts tests/unit/vtt/protocol-runtime.test.ts tests/unit/vtt/engine-boundary.test.ts",
    "handoff:publish": "node --experimental-strip-types tools/vtt-handoff/publish.ts",
    "handoff:gate": "node --experimental-strip-types tools/vtt-handoff/gate-inventory.ts",
    "art:request": "node --experimental-strip-types tools/vtt-handoff/art-request.ts --",
    "art:validate": "node --experimental-strip-types tools/vtt-handoff/art-validator.ts --",
    "art:stage": "node --experimental-strip-types tools/vtt-handoff/art-stage.ts --",
    "windows:probe": "node --experimental-strip-types tools/vtt-handoff/windows-probe.ts --"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "3.53.0-build1",
    "qrcode": "^1.5.4",
    "trystero": "0.25.3",
    "ws": "8.18.3",
    "yjs": "13.6.32",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@stryker-mutator/core": "9.6.1",
    "@stryker-mutator/typescript-checker": "9.6.1",
    "@stryker-mutator/vitest-runner": "9.6.1",
    "@types/node": "^24.0.0",
    "@types/qrcode": "^1.5.6",
    "@types/ws": "8.18.1",
    "@typescript/native-preview": "7.0.0-dev.20260707.2",
    "ajv": "8.18.0",
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "drizzle-zod": "^0.8.3",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vite-node": "3.2.4",
    "vitest": "^4.1.10"
  }
}
import { configDefaults, defineConfig } from 'vitest/config';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

/**
 * `tests/live/*.live-test.ts` probe the real `claude` CLI to check that the
 * bridge's invocation is still capability-free. They cost money, need an
 * authenticated login and need the network, so they are OPT-IN via
 * `npm run test:live`.
 *
 * Note the direction: setting the flag ADDS a suite and clearing it removes only
 * that suite. Nothing in the default set is excluded, skipped or relaxed by this
 * switch — the ordinary run is exactly the run it has always been. (The `-test`
 * suffix does not match the `.test.ts` pattern below, so these files are opt-in
 * by name as well as by flag; verified, not assumed.)
 */
const liveInclusions =
  process.env.AI_BRIDGE_LIVE === '1'
    ? ['tests/live/**/*.live-test.ts']
    : [];
const verdictRecorderSetup =
  process.env.VERDICT_FS_OBSERVATIONS_DIR === undefined
    ? []
    : ['tests/helpers/verdict-fs-recorder-setup.mjs'];

export default defineConfig({
  cacheDir:
    process.env.STATIC_APP_CACHE_DIR ??
    join(tmpdir(), 'dnd-multiclass-spells-static-vitest'),
  test: {
    environment: 'node',
    // Run BOTH unit and integration .test.ts under vitest. Browser tests are
    // .spec.ts under tests/browser and belong to Playwright (npm run test:browser).
    include: ['tests/**/*.test.ts', ...liveInclusions],
    exclude: [...configDefaults.exclude, 'tests/integration-supervisor/**'],
    setupFiles: verdictRecorderSetup,
    /**
     * Derives the SRD spell-source parse ONCE, before any worker is forked,
     * and hands the workers a file path and a key. It is a pure-parse cache
     * and nothing else: the worker re-checks the key against the corpus it is
     * actually holding, re-runs every freeze and every mint itself, and a
     * change to either corpus file or to `spell-source-reader.ts` is a miss
     * and a full re-derivation.
     *
     * Like the live-suite switch above, this adds no suite, excludes nothing
     * and relaxes nothing — it only moves work that already ran 38 times into
     * running once. The reasoning lives in the setup file; the guarantee lives
     * in `src/simulation/spell-source-parse-cache.ts`.
     */
    globalSetup: ['tests/helpers/spell-source-parse-cache-global-setup.ts'],
    // Shared workers require every test to restore mocks, globals, environments,
    // native-prototype mutations, and scoped module mocks at its suite boundary.
    isolate: false,
    // The migration-heavy suite oversubscribes constrained runners when
    // Vitest mirrors the host's full CPU count, causing unrelated fixed-timeout
    // tests to fail under contention. Bound scheduling without changing any
    // suite membership, assertion, or timeout contract.
    maxWorkers: 8,
    clearMocks: true,
    restoreMocks: true,
    unstubGlobals: true,
    unstubEnvs: true,
  },
});

exec
/bin/bash -lc "nl -ba tools/d569-second-family-manifest.ts | sed -n '1,130p;170,215p;295,565p'; nl -ba tests/unit/tools/d569-second-family-manifest.test.ts | sed -n '1,60p;155,290p'; sed -n '365,410p' .tmp-plans/2026-09-17-cohort-01-plan.md" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
     1	import { canonicalJson } from '../src/commands/canonical-json';
     2	import { combatantSpace, reduceEncounter } from '../src/combat/encounter';
     3	import { spacesIntersect } from '../src/combat/creature-space';
     4	import { mulberry32 } from '../src/combat/random';
     5	import { BUNDLED_MONSTER_ROSTER, STARTER_MONSTER_ROSTER } from '../src/combat/statblocks/roster';
     6	import { spellDefinition } from '../src/combat/spells/definitions';
     7	import { sha256 } from '../src/crypto/sha256';
     8	import { d466GeneratedRoomOverride } from '../src/vtt/d466-room-overrides';
     9	import { availableEngineActorOptions, resolveEngineActorOption } from '../src/vtt/intent-resolver';
    10	import { decodeArenaFixtureText } from '../src/vtt/mcp/entrypoint';
    11	import { freshMonsterPlanningState } from '../src/vtt/monster-planning-state';
    12	import { buildOfferEnvironment } from '../src/vtt/offers/build-offer-environment';
    13	import {
    14	  BRUTAL_CHALLENGE_BUDGET_SCALE,
    15	  BRUTAL_TERRAIN_FEATURE_COUNT_BAND,
    16	  generateRoom,
    17	  type GeneratedRoom,
    18	  type RoomDifficultyProfile,
    19	} from '../src/vtt/room-generator';
    20	import { z } from 'zod';
    21	
    22	const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
    23	
    24	export const D569_SECOND_FAMILY_LEDGER_ENTRY =
    25	  'seed ranges 5118001-5118010 and 6207001-6207010 have no occurrence in .claude/decisions.md, tests/fixtures or tools as of 2026-09-07 (supervisor grep); ranges 5117xxx, 6203xxx, 6204xxx, 6206xxx and 6208xxx are used (primary hard, primary brutal, D466 override/protocol, D572 brutal-b, D572 pool)' as const;
    26	
    27	export const D569_SECOND_FAMILY_SEEDS = {
    28	  hard: [
    29	    5_118_001, 5_118_002, 5_118_003, 5_118_004, 5_118_005,
    30	    5_118_006, 5_118_007, 5_118_008, 5_118_009, 5_118_010,
    31	  ],
    32	  brutal: [
    33	    6_207_001, 6_207_002, 6_207_003, 6_207_004, 6_207_005,
    34	    6_207_006, 6_207_007, 6_207_008, 6_207_009, 6_207_010,
    35	  ],
    36	} as const;
    37	
    38	export const D569_USED_SEED_NAMESPACES = [
    39	  { label: '5117xxx', start: 5_117_000, end: 5_117_999 },
    40	  { label: '6203xxx', start: 6_203_000, end: 6_203_999 },
    41	  { label: '6204xxx', start: 6_204_000, end: 6_204_999 },
    42	  { label: '6206xxx', start: 6_206_000, end: 6_206_999 },
    43	  { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
    44	] as const;
    45	
    46	const manifestFields = {
    47	  generator: z.strictObject({
    48	    tool: z.literal('tools/generate-arena-basis.ts'),
    49	    canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
    50	    independentRegenerations: z.literal(2),
    51	  }),
    52	  ledgerEntry: z.string(),
    53	  usedSeedNamespaces: z.array(z.strictObject({
    54	    label: z.string(),
    55	    start: z.number().int().nonnegative(),
    56	    end: z.number().int().nonnegative(),
    57	  })),
    58	  cohorts: z.array(z.strictObject({
    59	    difficulty: z.enum(['hard', 'brutal']),
    60	    directory: z.string(),
    61	    fixtures: z.array(z.strictObject({
    62	      seed: z.number().int().nonnegative(),
    63	      path: z.string(),
    64	      sha256: z.string().regex(/^[a-f0-9]{64}$/u),
    65	    })),
    66	  })),
    67	} as const;
    68	
    69	const manifestSchema = z.discriminatedUnion('version', [
    70	  z.strictObject({
    71	    version: z.literal('d569-second-family-manifest-v1'),
    72	    ...manifestFields,
    73	  }),
    74	  z.strictObject({
    75	    version: z.literal('d569-second-family-manifest-v2'),
    76	    ...manifestFields,
    77	  }),
    78	]);
    79	
    80	export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
    81	export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;
    82	
    83	export interface D569SecondFamilyValidationAccess {
    84	  readonly readFixture: (path: string) => string;
    85	  readonly regenerate: (
    86	    seed: number,
    87	    difficulty: D569SecondFamilyDifficulty,
    88	    generation: 1 | 2,
    89	  ) => string;
    90	}
    91	
    92	export interface D569SecondFamilyViolation {
    93	  readonly code: string;
    94	  readonly message: string;
    95	}
    96	
    97	function addViolation(
    98	  violations: D569SecondFamilyViolation[],
    99	  condition: boolean,
   100	  code: string,
   101	  message: string,
   102	): void {
   103	  if (!condition) violations.push({ code, message });
   104	}
   105	
   106	function cellsInBounds(
   107	  cells: readonly { readonly column: number; readonly row: number }[],
   108	  bounds: { readonly columns: number; readonly rows: number },
   109	): boolean {
   110	  return cells.every((cell) =>
   111	    Number.isInteger(cell.column) && Number.isInteger(cell.row) &&
   112	    cell.column >= 0 && cell.column < bounds.columns &&
   113	    cell.row >= 0 && cell.row < bounds.rows);
   114	}
   115	
   116	/** Shared state/reducer invariants for the existing generator suite and D569 freeze. */
   117	export function generatedRoomIntegrityViolations(
   118	  room: GeneratedRoom,
   119	  fixtureBytes: string = `${canonicalJson(room)}\n`,
   120	): readonly D569SecondFamilyViolation[] {
   121	  const violations: D569SecondFamilyViolation[] = [];
   122	  const state = room.encounter.state;
   123	  addViolation(
   124	    violations,
   125	    state.bounds.columns === room.spec.dimensions.columns &&
   126	      state.bounds.rows === room.spec.dimensions.rows,
   127	    'dimension_mismatch',
   128	    `seed ${String(room.spec.seed)} spec and encounter bounds differ`,
   129	  );
   130	  addViolation(
   170	      `seed ${String(room.spec.seed)} has overlapping creature footprints`,
   171	    );
   172	  } catch (error) {
   173	    violations.push({
   174	      code: 'token_footprint_invalid',
   175	      message: `seed ${String(room.spec.seed)} has an undecodable creature footprint: ${error instanceof Error ? error.message : String(error)}`,
   176	    });
   177	  }
   178	
   179	  const environmentRegions = [
   180	    ...state.environment.difficultTerrainRegions,
   181	    ...state.environment.lightRegions,
   182	    ...state.environment.obscurementRegions,
   183	    ...(state.environment.movementRegions ?? []),
   184	    ...(state.environment.narrowOpeningRegions ?? []),
   185	  ];
   186	  const regionIds = environmentRegions.map((region) => region.id);
   187	  addViolation(
   188	    violations,
   189	    new Set(regionIds).size === regionIds.length && environmentRegions.every((region) =>
   190	      region.id.trim().length > 0 && region.cells.length > 0 && cellsInBounds(region.cells, state.bounds)),
   191	    'environment_region_invalid',
   192	    `seed ${String(room.spec.seed)} has an empty, duplicate, or out-of-bounds environment region`,
   193	  );
   194	
   195	  try {
   196	    const loaded = decodeArenaFixtureText(fixtureBytes);
   197	    const reduced = reduceEncounter(loaded, { type: 'roll_initiative' }, mulberry32(901));
   198	    addViolation(
   199	      violations,
   200	      reduced.state.revision === loaded.revision + 1 &&
   201	        reduced.state.initiative.length === loaded.combatants.length,
   202	      'reducer_load_invalid',
   203	      `seed ${String(room.spec.seed)} did not initialize through the encounter reducer`,
   204	    );
   205	  } catch (error) {
   206	    violations.push({
   207	      code: 'reducer_load_invalid',
   208	      message: `seed ${String(room.spec.seed)} failed reducer loading: ${error instanceof Error ? error.message : String(error)}`,
   209	    });
   210	  }
   211	  return violations;
   212	}
   213	
   214	function executableControlCaster(room: GeneratedRoom): boolean {
   215	  return room.spec.monsterRoster.some((entry) => {
   295	
   296	function brutalRoomStaticMembershipViolations(
   297	  room: GeneratedRoom,
   298	  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
   299	): readonly D569SecondFamilyViolation[] {
   300	  const violations: D569SecondFamilyViolation[] = [];
   301	  addViolation(violations, room.spec.difficultyProfile === 'brutal', 'brutal_difficulty', 'room is not tagged brutal');
   302	  addViolation(
   303	    violations,
   304	    room.spec.challengeBudgetEighths ===
   305	      sameSeedHard.spec.challengeBudgetEighths * BRUTAL_CHALLENGE_BUDGET_SCALE &&
   306	      room.spec.challengeSpentEighths >= room.spec.challengeBudgetEighths * 0.9 &&
   307	      room.spec.challengeSpentEighths <= room.spec.challengeBudgetEighths,
   308	    'brutal_budget',
   309	    'brutal budget must be scaled from hard and spend 90-100 percent',
   310	  );
   311	  addViolation(
   312	    violations,
   313	    executableCaster(room),
   314	    'brutal_caster',
   315	    'brutal roster must contain an executable implemented caster',
   316	  );
   317	
   318	  const terrainCounts = {
   319	    difficult: room.spec.terrain.filter((feature) => feature.kind === 'difficult-terrain-patch').length,
   320	    hazards: room.spec.terrain.filter((feature) => feature.kind === 'hazard-object').length,
   321	    lights: room.spec.terrain.filter((feature) => feature.kind === 'light-source').length,
   322	    obscurement: room.spec.terrain.filter((feature) => feature.kind === 'obscurement-patch').length,
   323	  };
   324	  const terrainTotal = Object.values(terrainCounts).reduce((total, count) => total + count, 0);
   325	  addViolation(
   326	    violations,
   327	    terrainCounts.difficult >= 3 && terrainCounts.hazards >= 3 && terrainCounts.lights >= 2 &&
   328	      terrainCounts.obscurement >= 2 &&
   329	      terrainTotal >= BRUTAL_TERRAIN_FEATURE_COUNT_BAND.minimum &&
   330	      terrainTotal <= BRUTAL_TERRAIN_FEATURE_COUNT_BAND.maximum &&
   331	      room.spec.terrain.length > sameSeedHard.spec.terrain.length,
   332	    'brutal_terrain',
   333	    'brutal terrain must meet every configured minimum/count band and exceed hard density',
   334	  );
   335	  return violations;
   336	}
   337	
   338	/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
   339	export function brutalRoomMembershipViolations(
   340	  room: GeneratedRoom,
   341	  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
   342	): readonly D569SecondFamilyViolation[] {
   343	  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
   344	  addViolation(
   345	    violations,
   346	    everyMonsterProductive(room),
   347	    'brutal_productivity',
   348	    'every brutal monster must have a productive first-turn offer',
   349	  );
   350	  return violations;
   351	}
   352	
   353	function expectedDirectory(difficulty: D569SecondFamilyDifficulty): string {
   354	  return `tests/fixtures/arena-basis-${difficulty}-2`;
   355	}
   356	
   357	function sameNamespace(
   358	  actual: readonly { readonly label: string; readonly start: number; readonly end: number }[],
   359	): boolean {
   360	  return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
   361	}
   362	
   363	function encounterStateRecord(input: unknown): Record<string, unknown> {
   364	  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
   365	    throw new TypeError('fixture root is not an object');
   366	  }
   367	  const encounter = (input as Record<string, unknown>)['encounter'];
   368	  if (typeof encounter !== 'object' || encounter === null || Array.isArray(encounter)) {
   369	    throw new TypeError('fixture encounter is not an object');
   370	  }
   371	  const state = (encounter as Record<string, unknown>)['state'];
   372	  if (typeof state !== 'object' || state === null || Array.isArray(state)) {
   373	    throw new TypeError('fixture encounter state is not an object');
   374	  }
   375	  return state as Record<string, unknown>;
   376	}
   377	
   378	function legacyCommittedProjection(
   379	  parsed: unknown,
   380	  seed: number,
   381	  violations: D569SecondFamilyViolation[],
   382	): string {
   383	  const state = encounterStateRecord(parsed);
   384	  const hasLegacyFoggedCells = Object.hasOwn(state, 'foggedCells');
   385	  addViolation(
   386	    violations,
   387	    hasLegacyFoggedCells,
   388	    'legacy_fogged_cells_missing',
   389	    `seed ${String(seed)} historical fixture omitted encounter.state.foggedCells`,
   390	  );
   391	  if (hasLegacyFoggedCells) delete state['foggedCells'];
   392	  return `${canonicalJson(parsed)}\n`;
   393	}
   394	
   395	/**
   396	 * Validates the pins only after state membership and two independent canonical
   397	 * regenerations. It is pure/model-free; callers provide repository bytes.
   398	 */
   399	export function validateD569SecondFamilyManifest(
   400	  input: unknown,
   401	  access: D569SecondFamilyValidationAccess,
   402	): readonly D569SecondFamilyViolation[] {
   403	  const decoded = manifestSchema.safeParse(input);
   404	  if (!decoded.success) {
   405	    return [{ code: 'manifest_shape', message: decoded.error.message }];
   406	  }
   407	  const manifest = decoded.data;
   408	  const violations: D569SecondFamilyViolation[] = [];
   409	  addViolation(
   410	    violations,
   411	    manifest.ledgerEntry === D569_SECOND_FAMILY_LEDGER_ENTRY,
   412	    'ledger_evidence',
   413	    'manifest must cite the supervisor ledger entry verbatim',
   414	  );
   415	  addViolation(
   416	    violations,
   417	    sameNamespace(manifest.usedSeedNamespaces),
   418	    'used_seed_ledger',
   419	    'manifest must list every used seed namespace from the supervisor ledger',
   420	  );
   421	  addViolation(
   422	    violations,
   423	    manifest.cohorts.length === 2 && new Set(manifest.cohorts.map((cohort) => cohort.difficulty)).size === 2,
   424	    'cohort_set',
   425	    'manifest must contain exactly one hard and one brutal cohort',
   426	  );
   427	
   428	  const seenSeeds = new Set<number>();
   429	  for (const difficulty of ['hard', 'brutal'] as const) {
   430	    const cohort = manifest.cohorts.find((candidate) => candidate.difficulty === difficulty);
   431	    if (cohort === undefined) continue;
   432	    const expectedSeeds = D569_SECOND_FAMILY_SEEDS[difficulty];
   433	    addViolation(
   434	      violations,
   435	      cohort.directory === expectedDirectory(difficulty),
   436	      'cohort_directory',
   437	      `${difficulty} cohort directory is not the frozen second-family directory`,
   438	    );
   439	    addViolation(
   440	      violations,
   441	      canonicalJson(cohort.fixtures.map((fixture) => fixture.seed)) === canonicalJson(expectedSeeds),
   442	      'cohort_seed_range',
   443	      `${difficulty} cohort must contain its exact ten contiguous second-family seeds`,
   444	    );
   445	
   446	    for (const [index, fixture] of cohort.fixtures.entries()) {
   447	      const expectedSeed = expectedSeeds[index];
   448	      addViolation(
   449	        violations,
   450	        !seenSeeds.has(fixture.seed),
   451	        'duplicate_seed',
   452	        `seed ${String(fixture.seed)} appears more than once`,
   453	      );
   454	      seenSeeds.add(fixture.seed);
   455	      for (const namespace of manifest.usedSeedNamespaces) {
   456	        addViolation(
   457	          violations,
   458	          fixture.seed < namespace.start || fixture.seed > namespace.end,
   459	          'seed_overlap',
   460	          `seed ${String(fixture.seed)} overlaps used namespace ${namespace.label}`,
   461	        );
   462	      }
   463	      addViolation(
   464	        violations,
   465	        d466GeneratedRoomOverride(fixture.seed) === null,
   466	        'generator_override',
   467	        `seed ${String(fixture.seed)} is subject to a generator override`,
   468	      );
   469	      if (expectedSeed === undefined || fixture.seed !== expectedSeed) continue;
   470	
   471	      const expectedPath = `${cohort.directory}/seed-${String(fixture.seed)}.json`;
   472	      addViolation(
   473	        violations,
   474	        fixture.path === expectedPath,
   475	        'fixture_path',
   476	        `seed ${String(fixture.seed)} has a non-canonical fixture path`,
   477	      );
   478	      let fixtureBytes: string;
   479	      try {
   480	        fixtureBytes = access.readFixture(fixture.path);
   481	      } catch (error) {
   482	        violations.push({
   483	          code: 'fixture_missing',
   484	          message: `cannot read ${fixture.path}: ${error instanceof Error ? error.message : String(error)}`,
   485	        });
   486	        continue;
   487	      }
   488	      addViolation(
   489	        violations,
   490	        sha256(fixtureBytes) === fixture.sha256,
   491	        'fixture_hash',
   492	        `seed ${String(fixture.seed)} fixture hash changed`,
   493	      );
   494	
   495	      let comparisonFixtureBytes = fixtureBytes;
   496	      try {
   497	        const parsed = JSON.parse(fixtureBytes) as unknown;
   498	        const room = parsed as GeneratedRoom;
   499	        const usesLegacyProjection = manifest.version === 'd569-second-family-manifest-v1' ||
   500	          difficulty === 'hard';
   501	        if (usesLegacyProjection) {
   502	          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
   503	        } else {
   504	          addViolation(
   505	            violations,
   506	            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
   507	            'unexpected_legacy_fogged_cells',
   508	            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
   509	          );
   510	        }
   511	        addViolation(
   512	          violations,
   513	          room.spec.seed === fixture.seed,
   514	          'fixture_seed',
   515	          `seed ${String(fixture.seed)} fixture contains a different seed`,
   516	        );
   517	        violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
   518	        if (difficulty === 'hard') {
   519	          violations.push(...hardRoomMembershipViolations(room));
   520	        } else if (manifest.version === 'd569-second-family-manifest-v1') {
   521	          violations.push(...brutalRoomStaticMembershipViolations(room));
   522	        } else {
   523	          violations.push(...brutalRoomMembershipViolations(room));
   524	        }
   525	      } catch (error) {
   526	        violations.push({
   527	          code: 'fixture_parse',
   528	          message: `seed ${String(fixture.seed)} fixture is invalid JSON: ${error instanceof Error ? error.message : String(error)}`,
   529	        });
   530	      }
   531	
   532	      const firstGeneration = access.regenerate(fixture.seed, difficulty, 1);
   533	      const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
   534	      addViolation(
   535	        violations,
   536	        firstGeneration === comparisonFixtureBytes,
   537	        'first_regeneration',
   538	        `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
   539	      );
   540	      addViolation(
   541	        violations,
   542	        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
   543	        'independent_regeneration',
   544	        `seed ${String(fixture.seed)} independent canonical regeneration differs`,
   545	      );
   546	    }
   547	  }
   548	  return violations;
   549	}
   550	
   551	export function canonicalD569SecondFamilyRegeneration(
   552	  seed: number,
   553	  difficulty: D569SecondFamilyDifficulty,
   554	  _generation: 1 | 2,
   555	): string {
   556	  return `${canonicalJson(generateRoom(seed, { difficulty: difficulty as RoomDifficultyProfile }))}\n`;
   557	}
     1	import { describe, expect, it } from 'vitest';
     2	import { canonicalJson } from '../../../src/commands/canonical-json';
     3	import { sha256 } from '../../../src/crypto/sha256';
     4	import type { GeneratedRoom } from '../../../src/vtt/room-generator';
     5	import {
     6	  D569_SECOND_FAMILY_LEDGER_ENTRY,
     7	  D569_SECOND_FAMILY_SEEDS,
     8	  brutalRoomMembershipViolations,
     9	  canonicalD569SecondFamilyRegeneration,
    10	  generatedRoomIntegrityViolations,
    11	  hardRoomMembershipViolations,
    12	  validateD569SecondFamilyManifest,
    13	  type D569SecondFamilyManifest,
    14	  type D569SecondFamilyValidationAccess,
    15	} from '../../../tools/d569-second-family-manifest';
    16	import { declareTestInputs } from '../../helpers/test-inputs';
    17	
    18	const MANIFEST_PATH = 'tests/fixtures/d569-second-family-manifest.json' as const;
    19	const HARD_FIXTURE_PATHS = [
    20	  'tests/fixtures/arena-basis-hard-2/seed-5118001.json',
    21	  'tests/fixtures/arena-basis-hard-2/seed-5118002.json',
    22	  'tests/fixtures/arena-basis-hard-2/seed-5118003.json',
    23	  'tests/fixtures/arena-basis-hard-2/seed-5118004.json',
    24	  'tests/fixtures/arena-basis-hard-2/seed-5118005.json',
    25	  'tests/fixtures/arena-basis-hard-2/seed-5118006.json',
    26	  'tests/fixtures/arena-basis-hard-2/seed-5118007.json',
    27	  'tests/fixtures/arena-basis-hard-2/seed-5118008.json',
    28	  'tests/fixtures/arena-basis-hard-2/seed-5118009.json',
    29	  'tests/fixtures/arena-basis-hard-2/seed-5118010.json',
    30	] as const;
    31	const BRUTAL_FIXTURE_PATHS = [
    32	  'tests/fixtures/arena-basis-brutal-2/seed-6207001.json',
    33	  'tests/fixtures/arena-basis-brutal-2/seed-6207002.json',
    34	  'tests/fixtures/arena-basis-brutal-2/seed-6207003.json',
    35	  'tests/fixtures/arena-basis-brutal-2/seed-6207004.json',
    36	  'tests/fixtures/arena-basis-brutal-2/seed-6207005.json',
    37	  'tests/fixtures/arena-basis-brutal-2/seed-6207006.json',
    38	  'tests/fixtures/arena-basis-brutal-2/seed-6207007.json',
    39	  'tests/fixtures/arena-basis-brutal-2/seed-6207008.json',
    40	  'tests/fixtures/arena-basis-brutal-2/seed-6207009.json',
    41	  'tests/fixtures/arena-basis-brutal-2/seed-6207010.json',
    42	] as const;
    43	const ALL_FIXTURE_PATHS = [...HARD_FIXTURE_PATHS, ...BRUTAL_FIXTURE_PATHS] as const;
    44	const inputs = declareTestInputs({ fixtures: [MANIFEST_PATH, ...ALL_FIXTURE_PATHS] });
    45	const fixtureBytes = new Map<string, string>(ALL_FIXTURE_PATHS.map((path) => [
    46	  path,
    47	  inputs.fixtures.readText(path),
    48	]));
    49	
    50	function manifest(): D569SecondFamilyManifest {
    51	  return JSON.parse(inputs.fixtures.readText(MANIFEST_PATH)) as D569SecondFamilyManifest;
    52	}
    53	
    54	function room(path: (typeof ALL_FIXTURE_PATHS)[number]): GeneratedRoom {
    55	  return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
    56	}
    57	
    58	function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
    59	  if (Object.is(left, right)) return [];
    60	  if (Array.isArray(left) && Array.isArray(right)) {
   155	    if (hard === undefined) throw new Error('missing hard cohort');
   156	    hard.fixtures[0] = { ...hard.fixtures[0]!, seed: 5_117_001 };
   157	    expect(codes(changed)).toContain('cohort_seed_range');
   158	    expect(codes(changed)).toContain('seed_overlap');
   159	  });
   160	
   161	  it('brutal_membership_checks_name_only: rejects structural failures even with the roster names intact', () => {
   162	    const brutal = structuredClone(room(BRUTAL_FIXTURE_PATHS[0]));
   163	    const lastMonster = brutal.encounter.state.combatants.findLast((combatant) =>
   164	      combatant.profile.kind === 'monster');
   165	    if (lastMonster === undefined) throw new Error('missing brutal monster');
   166	    const broken: GeneratedRoom = {
   167	      ...brutal,
   168	      encounter: {
   169	        ...brutal.encounter,
   170	        state: {
   171	          ...brutal.encounter.state,
   172	          combatants: brutal.encounter.state.combatants.map((combatant) =>
   173	            combatant.profile.id === lastMonster.profile.id
   174	              ? { ...combatant, hitPoints: 0, life: 'dead' }
   175	              : combatant),
   176	        },
   177	      },
   178	      spec: {
   179	        ...brutal.spec,
   180	        challengeBudgetEighths: brutal.spec.challengeBudgetEighths + 1,
   181	        terrain: [],
   182	      },
   183	    };
   184	    expect(brutalRoomMembershipViolations(broken).map((violation) => violation.code))
   185	      .toEqual(expect.arrayContaining(['brutal_budget', 'brutal_terrain', 'brutal_productivity']));
   186	  });
   187	
   188	  it('rejects missing or rewritten supervisor ledger evidence', () => {
   189	    const changed = structuredClone(manifest());
   190	    changed.ledgerEntry = '';
   191	    expect(codes(changed)).toContain('ledger_evidence');
   192	  });
   193	
   194	  it('rejects fixture bytes that no longer match the manifest hash', () => {
   195	    const changedBytes = `${fixtureBytes.get(HARD_FIXTURE_PATHS[0]) ?? ''} `;
   196	    expect(codes(manifest(), access({
   197	      readFixture: (path) => path === HARD_FIXTURE_PATHS[0]
   198	        ? changedBytes
   199	        : access().readFixture(path),
   200	    }))).toContain('fixture_hash');
   201	  });
   202	
   203	  it('seed_hash_without_properties: a matching digest cannot excuse failed membership', () => {
   204	    const changed = structuredClone(manifest());
   205	    const hardCohort = changed.cohorts.find((cohort) => cohort.difficulty === 'hard');
   206	    const entry = hardCohort?.fixtures[0];
   207	    if (entry === undefined) throw new Error('missing hard fixture entry');
   208	    const original = room(HARD_FIXTURE_PATHS[0]);
   209	    const { hardFeatures: _removedHardFeatures, ...specWithoutHardFeatures } = original.spec;
   210	    const invalid: GeneratedRoom = {
   211	      ...original,
   212	      spec: specWithoutHardFeatures,
   213	    };
   214	    const invalidBytes = `${canonicalJson(invalid)}\n`;
   215	    entry.sha256 = sha256(invalidBytes);
   216	    expect(codes(changed, access({
   217	      readFixture: (path) => path === entry.path ? invalidBytes : access().readFixture(path),
   218	      regenerate: (seed, difficulty, generation) => seed === entry.seed
   219	        ? invalidBytes
   220	        : canonicalD569SecondFamilyRegeneration(seed, difficulty, generation),
   221	    }))).toContain('hard_chokepoint_structure');
   222	  });
   223	
   224	  it('second_generation_not_compared: rejects a divergent independent regeneration', () => {
   225	    const targetSeed = D569_SECOND_FAMILY_SEEDS.hard[0];
   226	    expect(codes(manifest(), access({
   227	      regenerate: (seed, difficulty, generation) => {
   228	        const regenerated = canonicalD569SecondFamilyRegeneration(seed, difficulty, generation);
   229	        return seed === targetSeed && generation === 2 ? `${regenerated} ` : regenerated;
   230	      },
   231	    }))).toContain('independent_regeneration');
   232	  });
   233	
   234	  it('legacy_projection_drops_all_state: retains any second committed state difference', () => {
   235	    const changed = structuredClone(manifest());
   236	    const targetPath = HARD_FIXTURE_PATHS[0];
   237	    const targetRoom = room(targetPath);
   238	    (targetRoom.encounter.state as { round: number }).round += 1;
   239	    const changedBytes = `${canonicalJson(targetRoom)}\n`;
   240	    const rejectionCodes = codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes));
   241	    expect(rejectionCodes.filter((code) => code.endsWith('regeneration'))).toEqual([
   242	      'first_regeneration',
   243	      'independent_regeneration',
   244	    ]);
   245	  });
   246	
   247	  it('legacy_projection_requires_the_committed_legacy_key', () => {
   248	    const changed = structuredClone(manifest());
   249	    const targetPath = HARD_FIXTURE_PATHS[0];
   250	    const targetRoom = room(targetPath) as GeneratedRoom & {
   251	      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells?: unknown } };
   252	    };
   253	    delete targetRoom.encounter.state.foggedCells;
   254	    const changedBytes = `${canonicalJson(targetRoom)}\n`;
   255	    expect(codes(changed, replaceFixtureBytes(changed, targetPath, changedBytes)))
   256	      .toContain('legacy_fogged_cells_missing');
   257	  });
   258	
   259	  it('new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key', () => {
   260	    const changed = structuredClone(manifest());
   261	    const targetPath = BRUTAL_FIXTURE_PATHS[0];
   262	    const targetRoom = room(targetPath) as GeneratedRoom & {
   263	      encounter: { state: GeneratedRoom['encounter']['state'] & { foggedCells: unknown } };
   264	    };
   265	    targetRoom.encounter.state.foggedCells = [{ column: 0, row: 0 }];
   266	    const changedBytes = `${canonicalJson(targetRoom)}\n`;
   267	    const changedAccess = replaceFixtureBytes(changed, targetPath, changedBytes);
   268	    const v2Candidate = { ...changed, version: 'd569-second-family-manifest-v2' };
   269	    expect(codes(v2Candidate, changedAccess)).toContain('unexpected_legacy_fogged_cells');
   270	  });
   271	});

Run only touched suites serially: second-family, blind-experiment, v5, v6, room-generator,
brutal-v2, brutal-b, arena, rerun protocol, LOS audit, blind context, and the three behaviour
files. Expected: zero failures; D569 counts 480/660/240/330 and second-family 480; 30/30 new
fixtures; audits 93/63. Repeat T, freeze inventory, `git diff --check`, verify exact 52-path union,
and inspect all new JSON as parsed state. Supervisor later runs the untruncated full gate.

## Open questions

None. Any failed post-WALL derivation or rejected namespace invokes the stated stop/amend/review
procedure; it is not an implementation-time choice.

## Round 2 disposition (closed/superseded)

F1 remains closed at lines 211-250; F4 remains closed at lines 25-26 and 198-209.
F2's r2 work remains at 50-73 and F3 is superseded by the corrected r3 oracle at 304-348.
Supporting requirements remain at 8-12, 85-102, 236-239, and 293-348.

## Round 3 disposition

- **PLAN-F3 (P2; lines 304-348):** the production scene now makes A speed zero, uses extracted actor-relative
  selector keys, and pins both base candidates plus the exact empty post-seal menu; the verified
  nine-row table is explicitly ranking/filtering-only.
- **PLAN-F2 (P2; lines 50-73):** both absence commands now cover every 000-999 plain and underscore seed plus
  `xxx`; MAIN provenance, verbatim results, reservation classification, and replacement rules stay.
- **PLAN-F5 (P2; lines 217-246):** v2 now requires all ten historical/current namespaces, exact own-cohort
  exceptions, removal-of-any-row tests, and the 6204 mutant.
- **PLAN-F6 (P2; lines 115-138):** Batch 2 now preserves positive movement OR productive slots, keeps Dash-only
  monsters valid, and applies resolved-origin trace checks only to targeted offensive components.

COHORT-01 PLAN R3 DONE

## Binding amendment N1 (astra r3, D650 — supervisor-appended; part of the frozen plan)

Batch 9 must construct the hand-authored Scout A with `profile.rules.speed: feet(0)` before encounter creation, retain the typed zero-speed effect, and assert both declared and effective speed are zero (attack declarations take their movement allowance from `subject.profile.rules.speed`, offer-declarations.ts:431–465, so the effect alone still lets A move 25 ft to (8,3) and keep its Longbow). Preserve the exact base and mutated labels, selector keys, scores and menus at the lines the plan states (base: Longbow ×2 + Dodge, adjacency and visibility keys, scores 60/60; with (8,4) added: Dodge/Dodge, menu []). Require two mutants: restoring A's declared speed to 30 while retaining the effect, and separately disabling corner sealing, must each fail the mutation expectations. Re-verify against the ACCEPTED WALL-01 before implementation acceptance. This correction is confined to the allowed speculative-planning test file; no production movement change is authorized.

## Binding amendment N2 (supervisor ruling D651 — part of the frozen plan)

Batch 1 exposed a second historical discontinuity beside the fog key: the v5 manifest registers `caps.semanticBytes = 8192` (the engine-server `SEMANTIC_BOARD_MAX_BYTES`), while rows the live blind runner produces today carry the D643 cap `BLIND_SEMANTIC_BOARD_MAX_BYTES = 32,768`, so `validate-first-arm.ts:369` refuses them ("Registered context caps mismatch"). Ruling: (1) a cell's `semanticContextCapBytes` / `baseContextCapBytes` are the MANIFEST's declared caps (the registration), not an engine constant — `tools/d569-blind-experiment.ts:285` (schema literal) and `:712` (cell cap) become version-aware: v5 keeps 8192 as history, v6 declares `caps.semanticBytes: 32768`; (2) `tests/unit/tools/d569-v5.test.ts` stays HISTORICAL: it keeps validating the v5 manifest (fog-key normalization included), its hashes and its synthesized rows, and it PINS that a row produced by today's live runner is refused with exactly `Registered context caps mismatch` — v5 cannot be re-run as registered and the test says so; no skip, no relaxed cap; (3) the v6 active test (Batch 6) validates live rows at 32,768. Mutants: v5 accepting a 32,768 row must fail; v6 accepting an 8192 row must fail. No production cap value changes.

exec
/bin/bash -lc "sed -n '23060,23100p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md; tail -100 /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01/impl-b1-fix-r1-sol.md; cat vitest.config.ts" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
## D643 — OWNER (2026-09-17 19:36): "Do your proposal" — the supervisor's recommendations are authorized as a whole
Authorized: (1) WALL-01 (D642, in flight) lands the solid-wall vertex rule with VIS-FIELD; (2) QUESTION 5 = A1: normalize the removed fog key in the second-family validator's regeneration comparison (demonstrated per fixture as a single-key diff, as LAND-04 did), then regenerate ONLY the three brutal cohorts as new versioned families under the sealed geometry (brutal-v2 6209xxx, brutal-b-v2 6210xxx, brutal-2-v2 6211xxx; hard-2 5118xxx kept as the historical hard half), D569 precedent in full (verbatim namespace audit, membership from decoded state, two byte-identical generations, hashes pinned only then), manifests d569-second-family-manifest-v2 + d569-blind-experiment-manifest-v6 with v1/v5 frozen, versioned arena/rerun routes (brutal-v2, brutal-10-v2, brutal-10-b-v2), hand re-pins for composite-turn-proposals and Room-8 opportunity-intel, a small hand-authored scene for speculative-planning (never an empty-menu normalization); (3) the blind semantic cap stays 32,768 and the oversized-block rejection witness (astra's LAND4-F1, P3) is added; (4) after landing: the VIS-FIELD-PERF batch — visibility-epoch cache with the from-scratch differential witness, range-before-LOS prefilter inside offer generation, and the precomputed rectilinear-region/rectangle wall representation (Debenham–Solis-Oba) — planned and reviewed like any unit, plus the two test-only items into TEST-PERF-01. ORDER: WALL-01 (verify, astra) → A1 as VIS-FIELD-01 batch COHORT-01 (planned as a brief with the file-level costing from investigate-option-a-costing-sol.md as its binding spec; astra reviews) → untruncated full gate → land → mirror push (D640) → ELEVATION-02 B0 and the perf batch.

## D644 — OWNER (2026-09-17 19:45): TEST-PERF ranks 6/8/9 = option A
Rank 6 limited to the visibility-field caches (the VIS-FIELD-PERF batch authorized in D643: explicit visibility epoch + from-scratch differential witness per revision); rank 9 (vitest worker topology / duration-balanced shards / experimental fsModuleCache — gate config only) scheduled as a small measured batch after VIS-FIELD lands, savings measured back-to-back on a quiet box per D632's invariants; rank 6 engine-wide and rank 8 stay deferred until the visibility epoch has proven the pattern. Sequence after landing: TEST-PERF-01 (ranks 1–5, 7) → rank 9 → VIS-FIELD-PERF.

## D645 — SUPERVISOR (2026-09-17 19:53): WALL-01 REJECTED by my verification (WALL-F1, P1: the vertex rule is inert for axis-aligned rays whose sealing pair straddles the line); fix r1 dispatched; COHORT-01 plan (400 lines, sha 6852051d…) harvested and sent to astra HIGH
SOL WALL-01 (saved impl-wall-01-sol.md): gcd lattice enumeration, TL+BR / TR+BL pairs, progress-ordered merge of interior/edge/vertex events, physical tier untouched; 43/43; three mutants killed; counts table "unchanged"; 42 seeds byte-identical; pair 38.6 s; two los-cover budget timeouts. VERIFIED BY ME (clean checkout + patch, traceTerrainLine, Medium tokens, 15x8): blockers (7,3)&(8,4) with the ray cells (13,4)→(1,4): blocksSight FALSE (must be true); (7,4)&(8,3): FALSE; vertical (4,2)&(5,3) with (5,0)→(5,6): FALSE; the diagonal crack cases TRUE. CAUSE (read in cover.ts): sharedVertexBlockingCells is built from `candidates`, which traceSpaces filters by the bounding box of the eight corners; for a ray along a grid line between Medium cells the far-side flank row is outside the box, so the vertex is never registered. Sol's STAIRCASE_WALL_SEALED witness passes only because its geometry keeps both cells inside the box — it does not exercise the case it is named for, and the "counts unchanged" table is not evidence about D642. FIX R1 (sol resumed; brief impl-vis-field-wall-01-fix-r1.md; log impl-wall-01-fix-r1.log): on-the-fly sightBlockingAt lookups around each interior lattice vertex (as the edge rule does), witnesses replaced with my three geometries + a split-pair control, a bbox-regression mutant, counts and generation bytes redone.
COHORT-01 PLAN (sol 01a0b1bb…, saved plan-cohort-01-sol.md; copied to .tmp-plans/2026-09-17-cohort-01-plan.md): 10 batches (validator normalization → RED membership witnesses + frozen-contract retirement → generate 6209 → 6210 → 6211 → manifests v2/v6 + dated ledger → routes → audit/report coverage → three behaviour pins → cap witness), a byte-freeze inventory of v1/v5/6203/6206/6207 checked after every batch, "reject all ten seeds if one fails", no open questions. Astra HIGH (fresh; brief review-plan-cohort-01-astra.md; log review-plan-cohort-01-astra-r1.log) reviewing now, in parallel with the WALL-01 fix.

## D646 — SUPERVISOR (2026-09-17 20:07): COHORT-01 plan r1 REJECTED by astra r1 (HIGH; 0 P1, 3 P2, 1 P3; saved review-plan-cohort-01-astra-r1.md); r2 dispatched to the planner
PLAN-F1 (P2): v6 has no experiment-identity boundary — session keys {family, arm, seed, rep} (d569-blind-experiment.ts:705) and row matching (:762) omit the manifest identity, and the unchanged hard fixtures keep their hashes, so 240/330/240 cells are indistinguishable across v5/v6. PLAN-F2 (P2): the namespace audit is unsound — stale worktree decisions file, patterns miss `6209xxx` and `6_209_001` forms, and D643/D645's reservations must be classified as such. PLAN-F3 (P2): the speculative-planning oracle must be a predeclared hand-calculated scene (≥9 eligible candidates, tie order, the excluded ninth, the exact mutation result). PLAN-F4 (P3): Batch 6 has eight files. Astra independently confirmed the rest: 20/20 fog-key single-key diffs via the real generator; the scout's 12+4 blocked rays per PC (wizard 16) and labels exactly Dodge/End Turn; Room-8 monsters 1/2/4 Dodge, monster 5's 55-ft Dash (10 steps × 5 + 5 difficult); the cap guard rejects a 57,580-byte block and accepts it with the guard disabled; 52 paths (18 existing + 34 new), all existing ones present. All four findings accepted. r2 (planner resumed 01a0b1bb…; brief plan-cohort-01-r2.md; r1 archived as .tmp-plans/2026-09-17-cohort-01-plan.r1.md) must also require canonicalD569SecondFamilyRegeneration in the active v6 test and rejection witnesses for non-fog-key differences and for legacy keys in new fixtures.

## D647 — SUPERVISOR (2026-09-17 20:22): WALL-01 fix r1 harvested (sol; saved impl-wall-01-fix-r1-sol.md) — my verification running on the throwaway checkout; COHORT-01 plan r2 harvested (sha 4f3385b4…, 373 lines; r1 archived) — astra r2 (HIGH, resumed) running
SOL fix r1: vertex lookup now queries sightBlockingAt directly around each GCD-enumerated interior lattice vertex (no candidate bbox), also on the coverTierBetweenObjects path; the three axis witnesses (my geometries: first cells (7,3) / (8,3) / (4,2)) + split control; 46/46; four mutants incl. the bbox-regression one (exactly the three axis witnesses red); counts table unchanged before/after (hard 142/35, brutal 57/34, brutal-b 15/41, brutal-2 58/36 — now with a working rule); 42 seeds byte-identical (aggregate 7b884bf9…); pair 39.1 s; the 35 frozen/behaviour reds unchanged; four los-cover 5 s budget timeouts under the combined audit (110/112 isolated with two) — perf, not re-pinned. MY VERIFICATION (verify-wall-01-r1.log; ../dnd-wt-seam-ab reset to 807d2e3c + patch): tsc, my seven-case probe (A/B/C/D must block, E split pair / F outer-face graze / G single corner must stay clear), a vertex-lookup-null mutant, the suites, los-cover alone, pair.
PLAN r2 disposition (planner): F1 → separately pinned outer-v6 identity in session keys/cells/rows/matching, v5 preserved, v5 hard rows rejected; F2 → pre-Batch-2 gate against MAIN decisions, plain/xxx/underscore forms, verbatim output, reservations classified, three families reserved together, Allowed-file amendment before replacement generation; F3 → fixed two-proposal integration scene + nine-candidate hand table (radii, paths, scores, byte tie order, excluded ninth, exact seam mutation); F4 → counts fixed; canonical regeneration required for active v6; rejection witnesses for non-fog differences and legacy keys in v2. Astra r2 told to verify the F3 hand table itself.

## D648 — SUPERVISOR (2026-09-17 20:38): FINDING AGAINST MY OWN REVIEW — WALL-F1 (D645) RETRACTED; a real D642 consequence found in scene-snapshot; COHORT-01 plan r2 REJECTED by astra r2 (0 P1, 4 P2) → r3 (final round) dispatched
RETRACTION, at full length. In D645 I rejected WALL-01 as P1 on two grounds, both wrong: (1) I claimed the vertex map was built from the bounding-box-filtered `candidates`; it was built from `candidateEntries` — the UNFILTERED union of indexed sources and blocked cells — so no cell was ever excluded; (2) I read my probe's aggregate blocksSight=false for two isolated diagonal blockers as "the rule is inert", but blocksSight is true only when EVERY corner ray is blocked, and in that geometry the ray along the far face (y=5) is legitimately clear (a graze) — the aggregate false was CORRECT. The decisive test I should have run first: a FULL stepped wall (column 7 rows 0–3, column 8 rows 4–7), Medium tokens (13,4)→(1,4): baseline 807d2e3c leaks (false); ORIGINAL WALL-01 blocks (true, first (7,3)); fix r1 blocks (true). Verified by me tonight on the throwaway checkout, all three trees. So the original WALL-01 was correct; fix r1 replaced a correct precomputed map with equivalent on-the-fly lookups and added per-ray witnesses for my three geometries (valid per-ray, and the split-pair control) — harmless, kept as the candidate, but the "bbox regression" mutant sol reports is a restriction the original never had. Sol did not push back on a false P1; the brief told it what to find. Lesson recorded in memory (aggregate vs per-ray; read the code path before naming a cause).
REAL FINDING (WALL-F2, D642 consequence, not a defect): tests/unit/vtt/scene-snapshot.test.ts "preserves full persisted request/coordinator bytes and the actual revision checksum" is red with the vertex rule (8/8 on clean 807d2e3c): the two-room scene's derived fog gains cells (8,3) and (9,3) — a sealed diagonal crack now conceals two cells the party could previously see through. Sol's suite audit did not include scene-snapshot. Handling = D569 demonstrate-then-normalize exactly as B4/B2 did for this test (D635.5–.7: show the vertex, the ray, the two cells; then re-pin the persisted expectation and the revision checksum/fingerprint with the derivation). An UNTRUNCATED full vitest on the r1 candidate is running (full-vitest-wall-r1.log) to find every other consequence before the fix-r2 brief.
MY VERIFICATION of fix r1 otherwise (verify-wall-01-r1.log): tsc 0/0; diff-check 0; vertex-lookup-null mutant → 5 sealed witnesses red; suites 2150/2186 = the 35 known frozen/behaviour reds + scene-snapshot; los-cover 110/112 (the two 5 s budget timeouts, perf); pair 36/36 in 39 s.
PLAN r2 → astra r2 REJECT (saved review-plan-cohort-01-astra-r2.md): F1, F4 closed; F3 still open (astra ran the real planner: the base menu has two candidates not one, the selector key shape is wrong, and under sealed corners A keeps its shot by moving 25 ft to (8,3) so the mutated menu has three entries, not []); F2 partial (search must cover the whole 000–999 namespaces); F5 NEW (v2's ledger drops the five historical namespaces and the own-cohort handling); F6 NEW (membership silently became mandatory offense; 6203001 monsters 3/4 are Dash-only productive). The nine-candidate table verified correct under its stipulated responses. r3 = the final round (planner resumed; brief plan-cohort-01-r3.md; r2 archived).

## D649 — OWNER (2026-09-17 20:56): "Purge now with filter-repo, force-push the mirror, recreate the four worktrees from the rewritten main" — DONE in place (purge.log)
Inventory before acting: 136 worktrees (not four; 9 dirty, mostly stale lanes), remote `mirror` = github.com/derrickschoen/srd55-private, git-filter-repo 2.47.0, the blob .tmp-lane-4b.log = 68,500,144 bytes introduced at 2ba5adba and untracked at bade7b57; 28 other .tmp-*.log blobs (~115 MB) remain in history — NOT purged (scope = the owner's named file; a second in-place rewrite is cheap). Deviation from the option text, deliberately: no worktree was deleted or recreated — filter-repo rewrote refs in place, branch-attached worktrees follow their branches (identical trees), and the 16 detached worktrees were remapped through .git/filter-repo/commit-map with update-ref --no-deref HEAD; nothing uncommitted anywhere was touched. Preflight: no codex/vitest readers; dry run 4634 commits OK; refs (174) and detached HEADs snapshotted; the WALL-01 r1 candidate committed on claude/vis-field (7acb936d → 6887755f after rewrite) and the records committed on main (0847602b → 3be26bd8) so nothing sat uncommitted. RESULT: main tree 524f110d… identical before and after; refs 174/174; blob occurrences 1 → 0; pack 67.6 MiB (one pack, 56,225 objects); `git push --force mirror main`: 9f148666...3be26bd8 (forced update); ls-remote main = local main. KEY SHA TRANSLATIONS (old → new; the full 4635-line map is committed at .claude/consensus/purge-2026-09-17-commit-map.txt): 9f148666 -> 29a7d2e2; 0847602b -> 3be26bd8; 807d2e3c -> e86a4d26; 9a220fce -> 213f9a7e; 61dd454e -> 8352aed5; ebfcacaa -> 34c564f0; 10cc9f5e -> 30403378; 5492f782 -> e143e899; 9a3235da -> 33aa146c; 78ee31b0 -> bf85fb89; dcca84fd -> a46eea99; 1d58ef72 -> 9407dfa8; 7acb936d -> 6887755f; 4a99570d -> 495d9eb2; 75c2f44a -> df694f93. Every decision written before this entry cites OLD ids; translate through the map.

## D650 — SUPERVISOR (2026-09-17 21:08): COHORT-01 plan ACCEPTED WITH AMENDMENT by astra r3 (HIGH, final round) — FROZEN at sha256 2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802 (399 lines incl. binding note N1); Batch 1 dispatched on a new worktree; WALL-01 fix r2 re-dispatched after a sol capacity outage
ASTRA r3 (saved review-plan-cohort-01-astra-r3.md): F2 closed (all three namespace commands re-run verbatim, statuses 0/1/0, 1,000/1,000 plain and underscore seeds per namespace + the xxx form); F5 closed (ten-namespace union, own-cohort exceptions, remove-any-row tests, the 6204 mutant); F6 closed (positive movement stays productive; Dash-only witness + movement-arm mutant; sight/cover only on targeted offensive components); F1/F4 closed; counts 3/3/10/10/10/8/4/4/3/1 = 52 paths. F3 resolved by AMENDMENT: with the typed zero-speed effect alone the real planner still offers A a 25-ft move to (8,3) + Longbow (attack declarations read subject.profile.rules.speed, offer-declarations.ts:431–465); with the profile's rules.speed also feet(0) the real planner + an in-memory D642 reference gives base = Longbow ×2 + Dodge with adjacency/visibility keys scoring 60/60 and, with (8,4) added, Dodge/Dodge and menu [] — and removing corner sealing restores the Longbow and the two-entry menu. N1 (appended verbatim to the plan, all copies): construct A with rules.speed feet(0), keep the effect, assert declared and effective speed zero, preserve the exact labels/keys/scores/menus, two mutants (speed 30 restored; sealing disabled), re-verify against accepted WALL-01; test-file only. Plan copies: .tmp-plans (main), ../dnd-wt-seam-verify, ../dnd-wt-cohort-01, ../dnd-wt-vis-field.
OUTAGE: the first WALL-01 fix r2 lane died with "Selected model is at capacity" + "thread … not found" before editing (worktree clean); probes SOL-OK / TERRA-OK at 21:10; re-dispatched FRESH on sol (log impl-wall-01-fix-r2b.log). Per the routing rule the next sol failure on this scope goes to Terra xhigh.
COHORT-01 B1 (validator normalization; not gated on WALL-01): new worktree ../dnd-wt-cohort-01 = branch claude/cohort-01 from 6887755f (node_modules symlinked, plan copied); fresh sol; brief .tmp/runs/cohort-01/impl-cohort-01-b1.md; log impl-b1.log; consensus dir .claude/consensus/cohort-01/. Generation batches (3–5) stay gated on WALL-01's acceptance; the cohort branch merges into claude/vis-field once both are accepted.

## D651 — SUPERVISOR (2026-09-17 21:25): two correct BLOCKED stops ruled — WALL-01 fix r2's four scene subjects (test-scene edits approved) and COHORT-01 B1's v5 cap discontinuity (amendment N2; plan re-frozen at sha256 4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69, 403 lines)
WALL-01 fix r2 (sol fresh 01a0b20e-61fa…; saved impl-wall-01-fix-r2-sol.md; no files changed): all five reds are correct D642 consequences — (1) scene-snapshot: the wall cell (7,2) and the door (6,3) meet diagonally at vertex (7,3); the fighter's and wizard's last-clear row-3 rays (3,3)→(8,3), (2,3)→(9,3) become blocked; DM fog {(4,3)} → {(4,3),(8,3),(9,3)}; hashes derived (pendingRequestHash 78f31379… → 0ec0fbbc…, coordinatorHash f8fa0780… → 79b076c9…, revisionChecksum 35c96ecc… → 52305455…, fingerprint unchanged dd802783… since encounter-state bytes are unchanged); (2)(3) the conversation helper's re-authored blockers (9,1)/(9,5) now seal with (10,2)/(10,6) and the five opened pairs vanish; (4)(5) the R02 helpers' re-authored blocker (9,2) seals with (10,3) at vertex (10,3): the R02 row becomes HIDDEN and the first Scout's 90-ft shot becomes illegal. Sol stopped per the brief and proposed minimal scene edits: (9,1)/(9,5) → (8,1)/(8,5) and (9,2) → (8,2), temporarily proven (4/4) and restored. RULING: apply the scene-snapshot normalization under D569 (as B4/B2) and the two scene edits with derivations; test files only. Dispatched fix r3 (resume 01a0b20e-61fa…; brief impl-vis-field-wall-01-fix-r3.md).
COHORT-01 B1 (sol 01a0b20e-61fb…; saved impl-b1-sol.md; three allowed files, 176+/13−): second-family validator now a strict v1/v2 union with raw-byte hashing before a legacy projection that drops only the committed own foggedCells key; 20/20 single-key structural diffs; 50/50 second-family (47 historical violations → 0); five mutants killed; V-freeze 32/32 hashes identical; tsc 0/0. BLOCKED correctly: d569-v5.test.ts still fails at collection — now on "Registered context caps mismatch" (tools/d569-v5/validate-first-arm.ts:369): the v5 manifest registers caps.semanticBytes 8192 (engine-server SEMANTIC_BOARD_MAX_BYTES, unchanged by B6) while rows produced by today's live blind runner carry 32,768 (D643). VERIFIED BY ME (test run + guard read). RULING N2 (appended to the plan, all copies): manifest-declared caps are the cell caps, version-aware (v5 = 8192 history; v6 = 32768); the v5 test stays historical and PINS the refusal of live rows under today's runner (no skip, no relaxed cap); v6's active test validates at 32,768; mutants both ways. Dispatched B1 fix r1 (resume 01a0b20e-61fb…). PARKED for the owner (design): making the blind runner take its semantic cap from the experiment manifest end-to-end would let historical experiments dry-run as registered; not in COHORT-01.

## D652 — SUPERVISOR (2026-09-17 21:51): COHORT-01 B1 (+fix r1) VERIFIED BY ME and committed e92ebc93 on claude/cohort-01; astra medium review dispatched
VERIFIED (verify-b1.log, clean checkout at 6887755f + patch): tsc 0/0; diff-check 0; 0 long lines; V-freeze exit 0 (v1/v5 manifests, brutal/brutal-b/brutal-2/hard-2 fixtures byte-identical); MY MUTANT — the legacy projection also drops `round` → 2 red; second-family 50/50; d569-v5 27/27; blind-experiment 46 + the six frozen-family titles (now the helper-fed-committed-bytes artefact assigned to Batch 6). Committed e92ebc93. Astra (fresh, medium; brief review-b1-astra.md; log review-b1-astra-r1.log) on the detached checkout at e92ebc93.
     expect(validateBrutal(cancelled)).toHaveLength(30);
   });
 
-  it('carries one finalized semantic delivery object through runner cancellation, packet decoding, and registered validation', () => {
+  it('rejects a live-runner cancellation row under the historical v5 semantic cap', () => {
     const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'infrastructure_failed', fallbackReason: 'dispatch_cancelled',
@@ -676,26 +678,31 @@
     expect(Number(produced['semanticBoardBytes'])).toBeGreaterThan(0);
     const failure = record(produced['failingDispatch']);
     expect(failure['turnContextDelivery']).toEqual(produced['turnContextDelivery']);
+    expect(produced['turnContextConfiguredCaps']).toEqual({ baseBytes: 65_536, semanticBytes: 32_768 });
     const packetRows: object[] = CELLS.filter((cell) => cell.basis === 'brutal')
       .map((cell) => current(cell, 'authorized'));
     packetRows[0] = produced;
-    expect(pairedDocuments(packetRows).packet).toHaveLength(60);
+    expect(() => pairedDocuments(packetRows))
+      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
 
     const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
       .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
     registeredRows[0] = produced;
-    expect(validateBrutal(raw(registeredRows))).toHaveLength(30);
+    expect(() => validateBrutal(raw(registeredRows)))
+      .toThrow('Registered context caps mismatch at 1:1.');
   });
 
-  it('carries a runner timeout with an ordinary incomplete catalog through packet and registered observation validation', () => {
+  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
     const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
     expect(produced).toMatchObject({
       outcome: 'refused', fallbackReason: 'timeout',
       engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
       turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
+      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
     });
     expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
-    expect(RUNNER_TIMEOUT_CONSUMERS.registeredRows).toBe(30);
+    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
+      .toBe('Registered context caps mismatch at 1:1.');
     expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
     expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');

tokens used
181,713
Batch 1 Fix R1 is complete.

Changed only the three allowed files:

- [d569-second-family-manifest.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-second-family-manifest.ts)
- [d569-second-family-manifest.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts)
- [d569-v5.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts)

Implemented:

- Strict v1/v2 second-family schema.
- Raw fixture hashes checked before projection.
- Historical projection removes only the committed own `$.encounter.state.foggedCells`.
- Generated bytes remain untouched.
- V1 brutal fixtures retain static checks but no frozen productivity recertification.
- V2 brutal fixtures retain full productivity checks and reject legacy fog keys.
- All 20 fixtures demonstrate exactly one structural difference: `$.encounter.state.foggedCells`.
- Historical v5 paths, versions, manifest hashes, and synthesized rows remain pinned.
- v5 cells remain at the manifest-declared 8,192-byte semantic cap.
- Live 32,768-byte timeout rows are rejected exactly with `Registered context caps mismatch at 1:1.`
- Live delivered cancellation rows are rejected by both the historical packet schema and registered validation.

Test results:

- Second-family: `50/50 passed`.
- D569 v5: `27/27 passed`.
- D569 blind experiment: `46 passed / 6 expected frozen-family failures`.

Expected blind-experiment reds:

1. `validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment`
2. `dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows`
3. `adds exactly 180 separately labeled advice-assisted hint cells, 660 total`
4. `consumes the frozen second encounter family for all 480 core cells`
5. `records exact model effort cli and cliVersion for every observed row`
6. `rejects either decisive integrity field alone while preserving rows with neither field`

All six arise because that historical test helper deliberately returns committed legacy fixture bytes as both regenerations, leaving exactly 40 expected regeneration violations.

Verification:

- Frozen plan: SHA-256 `4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69`, 403 lines.
- `npx tsc -p tsconfig.app.json --noEmit`: exit 0.
- `npx tsc -p tsconfig.node.json --noEmit`: exit 0.
- `git diff --check`: exit 0, empty.
- Added lines over 120 columns: 0.
- Expanded V-freeze before/after, including `hard-2`: exit 0/0.
- No frozen fixture or manifest bytes changed.
- `git status --short`: exactly the three allowed files above.

The production v5/v6 schema seam in `tools/d569-blind-experiment.ts` remains for Batch 6 because that file is outside Batch 1’s allowlist. Batch 1 pins the required v5 half; Batch 6 must add v6’s 32,768 declaration and reject 8,192-byte v6 rows.

COHORT-01 B1 FIX R1 DONE
import { configDefaults, defineConfig } from 'vitest/config';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

/**
 * `tests/live/*.live-test.ts` probe the real `claude` CLI to check that the
 * bridge's invocation is still capability-free. They cost money, need an
 * authenticated login and need the network, so they are OPT-IN via
 * `npm run test:live`.
 *
 * Note the direction: setting the flag ADDS a suite and clearing it removes only
 * that suite. Nothing in the default set is excluded, skipped or relaxed by this
 * switch — the ordinary run is exactly the run it has always been. (The `-test`
 * suffix does not match the `.test.ts` pattern below, so these files are opt-in
 * by name as well as by flag; verified, not assumed.)
 */
const liveInclusions =
  process.env.AI_BRIDGE_LIVE === '1'
    ? ['tests/live/**/*.live-test.ts']
    : [];
const verdictRecorderSetup =
  process.env.VERDICT_FS_OBSERVATIONS_DIR === undefined
    ? []
    : ['tests/helpers/verdict-fs-recorder-setup.mjs'];

export default defineConfig({
  cacheDir:
    process.env.STATIC_APP_CACHE_DIR ??
    join(tmpdir(), 'dnd-multiclass-spells-static-vitest'),
  test: {
    environment: 'node',
    // Run BOTH unit and integration .test.ts under vitest. Browser tests are
    // .spec.ts under tests/browser and belong to Playwright (npm run test:browser).
    include: ['tests/**/*.test.ts', ...liveInclusions],
    exclude: [...configDefaults.exclude, 'tests/integration-supervisor/**'],
    setupFiles: verdictRecorderSetup,
    /**
     * Derives the SRD spell-source parse ONCE, before any worker is forked,
     * and hands the workers a file path and a key. It is a pure-parse cache
     * and nothing else: the worker re-checks the key against the corpus it is
     * actually holding, re-runs every freeze and every mint itself, and a
     * change to either corpus file or to `spell-source-reader.ts` is a miss
     * and a full re-derivation.
     *
     * Like the live-suite switch above, this adds no suite, excludes nothing
     * and relaxes nothing — it only moves work that already ran 38 times into
     * running once. The reasoning lives in the setup file; the guarantee lives
     * in `src/simulation/spell-source-parse-cache.ts`.
     */
    globalSetup: ['tests/helpers/spell-source-parse-cache-global-setup.ts'],
    // Shared workers require every test to restore mocks, globals, environments,
    // native-prototype mutations, and scoped module mocks at its suite boundary.
    isolate: false,
    // The migration-heavy suite oversubscribes constrained runners when
    // Vitest mirrors the host's full CPU count, causing unrelated fixed-timeout
    // tests to fail under contention. Bound scheduling without changing any
    // suite membership, assertion, or timeout contract.
    maxWorkers: 8,
    clearMocks: true,
    restoreMocks: true,
    unstubGlobals: true,
    unstubEnvs: true,
  },
});

exec
/bin/bash -lc "sed -n '1,115p' tools/d569-second-family-manifest.ts; sed -n '130,210p' tools/d569-second-family-manifest.ts; sed -n '390,430p' tools/d569-second-family-manifest.ts; sed -n '270,305p' tests/unit/tools/d569-blind-experiment.test.ts; rg -n 'vite|vitest|cache' package.json vitest*; sed -n '70,125p' tests/unit/tools/d569-v5.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
import { canonicalJson } from '../src/commands/canonical-json';
import { combatantSpace, reduceEncounter } from '../src/combat/encounter';
import { spacesIntersect } from '../src/combat/creature-space';
import { mulberry32 } from '../src/combat/random';
import { BUNDLED_MONSTER_ROSTER, STARTER_MONSTER_ROSTER } from '../src/combat/statblocks/roster';
import { spellDefinition } from '../src/combat/spells/definitions';
import { sha256 } from '../src/crypto/sha256';
import { d466GeneratedRoomOverride } from '../src/vtt/d466-room-overrides';
import { availableEngineActorOptions, resolveEngineActorOption } from '../src/vtt/intent-resolver';
import { decodeArenaFixtureText } from '../src/vtt/mcp/entrypoint';
import { freshMonsterPlanningState } from '../src/vtt/monster-planning-state';
import { buildOfferEnvironment } from '../src/vtt/offers/build-offer-environment';
import {
  BRUTAL_CHALLENGE_BUDGET_SCALE,
  BRUTAL_TERRAIN_FEATURE_COUNT_BAND,
  generateRoom,
  type GeneratedRoom,
  type RoomDifficultyProfile,
} from '../src/vtt/room-generator';
import { z } from 'zod';

const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });

export const D569_SECOND_FAMILY_LEDGER_ENTRY =
  'seed ranges 5118001-5118010 and 6207001-6207010 have no occurrence in .claude/decisions.md, tests/fixtures or tools as of 2026-09-07 (supervisor grep); ranges 5117xxx, 6203xxx, 6204xxx, 6206xxx and 6208xxx are used (primary hard, primary brutal, D466 override/protocol, D572 brutal-b, D572 pool)' as const;

export const D569_SECOND_FAMILY_SEEDS = {
  hard: [
    5_118_001, 5_118_002, 5_118_003, 5_118_004, 5_118_005,
    5_118_006, 5_118_007, 5_118_008, 5_118_009, 5_118_010,
  ],
  brutal: [
    6_207_001, 6_207_002, 6_207_003, 6_207_004, 6_207_005,
    6_207_006, 6_207_007, 6_207_008, 6_207_009, 6_207_010,
  ],
} as const;

export const D569_USED_SEED_NAMESPACES = [
  { label: '5117xxx', start: 5_117_000, end: 5_117_999 },
  { label: '6203xxx', start: 6_203_000, end: 6_203_999 },
  { label: '6204xxx', start: 6_204_000, end: 6_204_999 },
  { label: '6206xxx', start: 6_206_000, end: 6_206_999 },
  { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
] as const;

const manifestFields = {
  generator: z.strictObject({
    tool: z.literal('tools/generate-arena-basis.ts'),
    canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
    independentRegenerations: z.literal(2),
  }),
  ledgerEntry: z.string(),
  usedSeedNamespaces: z.array(z.strictObject({
    label: z.string(),
    start: z.number().int().nonnegative(),
    end: z.number().int().nonnegative(),
  })),
  cohorts: z.array(z.strictObject({
    difficulty: z.enum(['hard', 'brutal']),
    directory: z.string(),
    fixtures: z.array(z.strictObject({
      seed: z.number().int().nonnegative(),
      path: z.string(),
      sha256: z.string().regex(/^[a-f0-9]{64}$/u),
    })),
  })),
} as const;

const manifestSchema = z.discriminatedUnion('version', [
  z.strictObject({
    version: z.literal('d569-second-family-manifest-v1'),
    ...manifestFields,
  }),
  z.strictObject({
    version: z.literal('d569-second-family-manifest-v2'),
    ...manifestFields,
  }),
]);

export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;

export interface D569SecondFamilyValidationAccess {
  readonly readFixture: (path: string) => string;
  readonly regenerate: (
    seed: number,
    difficulty: D569SecondFamilyDifficulty,
    generation: 1 | 2,
  ) => string;
}

export interface D569SecondFamilyViolation {
  readonly code: string;
  readonly message: string;
}

function addViolation(
  violations: D569SecondFamilyViolation[],
  condition: boolean,
  code: string,
  message: string,
): void {
  if (!condition) violations.push({ code, message });
}

function cellsInBounds(
  cells: readonly { readonly column: number; readonly row: number }[],
  bounds: { readonly columns: number; readonly rows: number },
): boolean {
  return cells.every((cell) =>
    Number.isInteger(cell.column) && Number.isInteger(cell.row) &&
    cell.column >= 0 && cell.column < bounds.columns &&
    cell.row >= 0 && cell.row < bounds.rows);
}

  addViolation(
    violations,
    cellsInBounds(room.spec.blockedCells, state.bounds),
    'blocked_cell_invalid',
    `seed ${String(room.spec.seed)} has an out-of-bounds blocked cell`,
  );

  const combatantIds = new Set(state.combatants.map((combatant) => String(combatant.profile.id)));
  const tokenIds = state.tokens.map((token) => String(token.id));
  addViolation(
    violations,
    new Set(tokenIds).size === tokenIds.length && state.tokens.length === state.combatants.length,
    'token_set_invalid',
    `seed ${String(room.spec.seed)} has missing or duplicate tokens`,
  );
  addViolation(
    violations,
    state.tokens.every((token) =>
      combatantIds.has(String(token.combatantId)) && cellsInBounds([token.position], state.bounds)),
    'token_position_invalid',
    `seed ${String(room.spec.seed)} has an invalid token position or combatant reference`,
  );
  try {
    const spaces = state.tokens.map((token) => ({
      token,
      space: combatantSpace(state, token.combatantId),
    }));
    const blocked = new Set(state.blockedCells.map((cell) => `${String(cell.column)},${String(cell.row)}`));
    addViolation(
      violations,
      spaces.every(({ space }) => cellsInBounds(space.cells, state.bounds) &&
        space.cells.every((cell) => !blocked.has(`${String(cell.column)},${String(cell.row)}`))),
      'token_footprint_invalid',
      `seed ${String(room.spec.seed)} has an out-of-bounds or blocked creature footprint`,
    );
    addViolation(
      violations,
      spaces.every((left, leftIndex) => spaces.slice(leftIndex + 1).every((right) =>
        !spacesIntersect(left.space, right.space))),
      'token_footprint_overlap',
      `seed ${String(room.spec.seed)} has overlapping creature footprints`,
    );
  } catch (error) {
    violations.push({
      code: 'token_footprint_invalid',
      message: `seed ${String(room.spec.seed)} has an undecodable creature footprint: ${error instanceof Error ? error.message : String(error)}`,
    });
  }

  const environmentRegions = [
    ...state.environment.difficultTerrainRegions,
    ...state.environment.lightRegions,
    ...state.environment.obscurementRegions,
    ...(state.environment.movementRegions ?? []),
    ...(state.environment.narrowOpeningRegions ?? []),
  ];
  const regionIds = environmentRegions.map((region) => region.id);
  addViolation(
    violations,
    new Set(regionIds).size === regionIds.length && environmentRegions.every((region) =>
      region.id.trim().length > 0 && region.cells.length > 0 && cellsInBounds(region.cells, state.bounds)),
    'environment_region_invalid',
    `seed ${String(room.spec.seed)} has an empty, duplicate, or out-of-bounds environment region`,
  );

  try {
    const loaded = decodeArenaFixtureText(fixtureBytes);
    const reduced = reduceEncounter(loaded, { type: 'roll_initiative' }, mulberry32(901));
    addViolation(
      violations,
      reduced.state.revision === loaded.revision + 1 &&
        reduced.state.initiative.length === loaded.combatants.length,
      'reducer_load_invalid',
      `seed ${String(room.spec.seed)} did not initialize through the encounter reducer`,
    );
  } catch (error) {
    violations.push({
      code: 'reducer_load_invalid',
      message: `seed ${String(room.spec.seed)} failed reducer loading: ${error instanceof Error ? error.message : String(error)}`,
    });
  }
  );
  if (hasLegacyFoggedCells) delete state['foggedCells'];
  return `${canonicalJson(parsed)}\n`;
}

/**
 * Validates the pins only after state membership and two independent canonical
 * regenerations. It is pure/model-free; callers provide repository bytes.
 */
export function validateD569SecondFamilyManifest(
  input: unknown,
  access: D569SecondFamilyValidationAccess,
): readonly D569SecondFamilyViolation[] {
  const decoded = manifestSchema.safeParse(input);
  if (!decoded.success) {
    return [{ code: 'manifest_shape', message: decoded.error.message }];
  }
  const manifest = decoded.data;
  const violations: D569SecondFamilyViolation[] = [];
  addViolation(
    violations,
    manifest.ledgerEntry === D569_SECOND_FAMILY_LEDGER_ENTRY,
    'ledger_evidence',
    'manifest must cite the supervisor ledger entry verbatim',
  );
  addViolation(
    violations,
    sameNamespace(manifest.usedSeedNamespaces),
    'used_seed_ledger',
    'manifest must list every used seed namespace from the supervisor ledger',
  );
  addViolation(
    violations,
    manifest.cohorts.length === 2 && new Set(manifest.cohorts.map((cohort) => cohort.difficulty)).size === 2,
    'cohort_set',
    'manifest must contain exactly one hard and one brutal cohort',
  );

  const seenSeeds = new Set<number>();
  for (const difficulty of ['hard', 'brutal'] as const) {
    const cohort = manifest.cohorts.find((candidate) => candidate.difficulty === difficulty);
  advisoryTransmission: 'labelled_per_entry_note_text_only',
  advisoryPromptNoteMaxWords: 15,
  advisoryNoteMaxChars: 120,
  advisoryInvalidPolicy: 'nonzero_exit_or_invalid_existing_output_becomes_empty_block',
  advisoryExposureStatuses: ['notes', 'empty'],
  advisoryAnalysisPolicy: 'exposure_status_only_source_artifact_never_loaded',
  scoringSeatAdvisoryPolicy: 'consider_or_ignore_untrusted_no_weight_no_anchor',
} as const;

function readText(path: string): string {
  const bytes = fixtureBytes.get(path);
  if (bytes === undefined) throw new Error(`undeclared fixture ${path}`);
  return bytes;
}

function manifest(): D569ExperimentManifest {
  return JSON.parse(readText(D569_EXPERIMENT_MANIFEST_PATH)) as D569ExperimentManifest;
}

function access(readOverride?: (path: string) => string | undefined): D569ExperimentValidationAccess {
  return {
    readText: (path) => readOverride?.(path) ?? readText(path),
    secondFamilyAccess: {
      readFixture: (path) => readOverride?.(path) ?? readText(path),
      regenerate: (seed, difficulty) => readText(
        `tests/fixtures/arena-basis-${difficulty}-2/seed-${String(seed)}.json`,
      ),
    },
  };
}

function codes(candidate: unknown, validationAccess = access()): readonly string[] {
  return validateD569ExperimentManifest(candidate, validationAccess).map((violation) => violation.code);
}

function observedRows(cells: ReturnType<typeof dryRunD569Experiment>): readonly D569ObservedRow[] {
vitest.config.ts:1:import { configDefaults, defineConfig } from 'vitest/config';
vitest.config.ts:27:  cacheDir:
vitest.config.ts:29:    join(tmpdir(), 'dnd-multiclass-spells-static-vitest'),
vitest.config.ts:32:    // Run BOTH unit and integration .test.ts under vitest. Browser tests are
vitest.config.ts:39:     * and hands the workers a file path and a key. It is a pure-parse cache
vitest.config.ts:48:     * in `src/simulation/spell-source-parse-cache.ts`.
vitest.config.ts:50:    globalSetup: ['tests/helpers/spell-source-parse-cache-global-setup.ts'],
package.json:9:    "dev": "vite --configLoader runner",
package.json:10:    "build": "tsc -b && node tools/dist-build-cache.mjs",
package.json:11:    "build:dist": "vite build --configLoader runner",
package.json:18:    "test": "vitest run --configLoader runner",
package.json:19:    "test:gate": "node scripts/check-command-outcomes.mjs && node tools/gate-vitest.mjs",
package.json:24:    "test:shuffled": "vitest run --configLoader runner --sequence.shuffle --sequence.seed=20260825",
package.json:25:    "test:unit": "vitest run --configLoader runner tests/unit",
package.json:26:    "test:live": "AI_BRIDGE_LIVE=1 vitest run --configLoader runner tests/live",
package.json:28:    "test:mutation": "node scripts/patch-stryker-vitest-runner.mjs && node scripts/mutation-loud.mjs",
package.json:29:    "test:mutation:sharded": "node scripts/patch-stryker-vitest-runner.mjs && node scripts/mutation-shard.mjs run-all --shards 8",
package.json:30:    "vtt:replay": "vite-node tools/vtt-replay.ts --",
package.json:31:    "vtt:soak": "vite-node tools/vtt-soak.ts --",
package.json:32:    "vtt:experiment": "vite-node tools/vtt-experiment.ts --",
package.json:33:    "vtt:regret": "vite-node tools/vtt-regret.ts --",
package.json:34:    "rehearse": "vite-node tools/rehearsal/run.ts --",
package.json:36:    "scrape": "vite-node tools/scrape/cli.ts --",
package.json:37:    "db:schema": "vite-node scripts/build-schema.ts",
package.json:38:    "db:migrations": "vitest run --configLoader runner tests/unit/db/migrations.test.ts",
package.json:39:    "db:contracts": "vite-node scripts/build-row-contracts.ts",
package.json:40:    "schema:content-pack": "vite-node tools/generate-content-pack-schema.ts",
package.json:41:    "schema:engine-mcp": "vite-node tools/generate-engine-mcp-schemas.ts",
package.json:42:    "attribute:round-input": "vite-node tools/attribute-round-input.ts --",
package.json:43:    "rl:extract-sft": "vite-node tools/rl/extract-sft.ts --",
package.json:44:    "rl:generate-data": "vite-node tools/rl/generate-data.ts --",
package.json:47:    "test:engine": "vitest run --configLoader runner tests/unit/vtt/encounter-session-service.test.ts tests/unit/vtt/door-intent.test.ts tests/unit/vtt/encounter-selectors.test.ts tests/unit/vtt/engine-boundary.test.ts",
package.json:48:    "test:protocol": "vitest run --configLoader runner tests/unit/vtt/handoff-contract.test.ts tests/unit/vtt/protocol-runtime.test.ts tests/unit/vtt/in-process-transport.test.ts tests/unit/vtt/engine-boundary.test.ts",
package.json:50:    "test:runtime-node": "vitest run --configLoader runner tests/unit/vtt/node-runtime.test.ts tests/unit/vtt/node-websocket-transport.test.ts tests/unit/vtt/protocol-runtime.test.ts tests/unit/vtt/engine-boundary.test.ts",
package.json:70:    "@stryker-mutator/vitest-runner": "9.6.1",
package.json:81:    "vite": "^7.0.0",
package.json:82:    "vite-node": "3.2.4",
package.json:83:    "vitest": "^4.1.10"
vitest.stryker.config.ts:1:import { configDefaults, defineConfig, mergeConfig } from 'vitest/config';
vitest.stryker.config.ts:2:import rootConfig from './vitest.config';
  cell.arm === 'gpt-5.6-luna-blind');
const FIRST_BRUTAL_STATE = applyRoomInitiativeProfile(
  decodeArenaFixtureText(readFileSync('tests/fixtures/arena-basis-brutal/seed-6203001.json', 'utf8')),
  'derived_v1',
);

function historical(room: number, rep: number, sessionId: string | null) {
  return { room, round: rep, outcome: 'authorized', sessionId };
}

function current(cell: D569DryRunCell, outcome: 'authorized' | 'refused' | 'service_null' | 'infrastructure_failed') {
  const room = cell.seed - (cell.basis === 'hard' ? 5_117_000 : 6_203_000);
  const scheduledCellKey = `${String(room)}:${String(cell.rep)}`;
  const dispatchId = `engine-dispatch:grid-${String(room)}-${String(cell.rep)}-0001`;
  const infrastructure = outcome === 'infrastructure_failed';
  const delivered = outcome === 'authorized' || outcome === 'refused';
  const boardSha = sha256(`board:${cell.basis}:${scheduledCellKey}`);
  const catalog = infrastructure
    ? { status: 'absent' as const, basis: 'required_engine_initialization_failed' as const, dispatchId, corroboration: ['startup failed'] }
    : { status: 'ready' as const, basis: 'required_cli_completed_with_valid_catalog' as const, dispatchId, advertisedInvocationCount: 0, resourceOperationCount: 0 };
  const delivery = infrastructure
    ? { status: 'infrastructure_absent' as const, dispatchId, measurement: null }
    : delivered
      ? { status: 'delivered' as const, dispatchId, contextSha256: 'a'.repeat(64), measurement: { baseBytes: 100, semanticBytes: 10 } }
      : { status: 'not_requested' as const, dispatchId, reason: 'catalog_ready_model_did_not_fetch' as const, measurement: null };
  const state = applyRoomInitiativeProfile(decodeArenaFixtureText(readFileSync(cell.fixturePath, 'utf8')), 'derived_v1');
  return {
    rowContractVersion: 'arena-row-v3', arm: 'single', basis: cell.basis, seed: cell.seed,
    room, round: cell.rep, scheduledCellKey, dispatchId, outcome,
    sessionId: infrastructure ? null : `agent-session:grid-${String(room)}-${String(cell.rep)}`,
    repoCommit: PATCHED_COMMIT, cli: 'codex', model: cell.model, effort: cell.effort,
    dmMode: 'blind', blindRepairArm: 'code_only', blindMaxAttempts: 3, blindFacts: false,
    decisionTransport: 'mcp_minimal', instructionSource: 'kb', escalated: false,
    escalationModel: null, escalationEffort: null, midRoundAdjustmentsEnabled: false, fallbackReason: null, planner: 'model',
    combatModel: 'initiative_segments_v1', initiativeOrder: [], plannedBy: { model: cell.model, effort: cell.effort },
    roundNarrative: null,
    authorizedPlan: outcome === 'authorized' ? [{
      actorId: 'monster:test', resolutionSummary: { actionSlots: [{ kind: 'wait', targetIds: [] }], movementFeet: 0 },
    }] : null,
    firstDecisionAccepted: outcome === 'authorized', decisionAttempts: 1,
    decisionRejectionCodes: [], normalizationCodes: [], skillName: null, skillHash: null,
    rationale: null, overridePolicy: 'strict',
    blindContextVersion: 'blind-turn-context-v1', blindIntentVersion: 'blind-round-intent-v1',
    blindIntentText: null, blindIntents: null, blindResolverOutcome: [], blindRejectionCodes: [],
    blindAttempts: [], blindResolverLatencyMs: 0, semanticBoardEvidence: null,
    creatureFactsEvidence: { sha256: 'c'.repeat(64), utf8Bytes: 1 },
    legalMovementEvidence: { sha256: 'd'.repeat(64), utf8Bytes: 1 },
    blindPrivateAnswerKey: {},
    startingRoomDigest: sha256(canonicalJson(state)), kbHash: cell.sharedKbHash,
    sharedKbComponentHashes: MANIFEST.sharedKb.componentHashes,
    boardImage: {
      mode: 'png', sha256: boardSha, bytes: 24, width: 1, height: 1, captureMs: 0,
      relativePath: `board-images/${boardSha}.png`,
    },
    uiFeedback: null,
    visualProfile: {

codex
The plan hash and 403-line count match. The diff is limited to the three allowed files; raw hashing precedes a projection that deletes only the committed own `foggedCells` key. I’m checking the rejection witnesses and tracing the six blind-experiment failures to their fixture-reader helper.

exec
/bin/bash -lc "tail -55 /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01/impl-b1-fix-r1-sol.md; rg -n 'startVitest|createServer|ViteNode|node --|mutant|50 passed' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01/impl-b1-sol.md | tail -20; cat tsconfig.app.json tsconfig.node.json" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
tokens used
181,713
Batch 1 Fix R1 is complete.

Changed only the three allowed files:

- [d569-second-family-manifest.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-second-family-manifest.ts)
- [d569-second-family-manifest.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts)
- [d569-v5.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts)

Implemented:

- Strict v1/v2 second-family schema.
- Raw fixture hashes checked before projection.
- Historical projection removes only the committed own `$.encounter.state.foggedCells`.
- Generated bytes remain untouched.
- V1 brutal fixtures retain static checks but no frozen productivity recertification.
- V2 brutal fixtures retain full productivity checks and reject legacy fog keys.
- All 20 fixtures demonstrate exactly one structural difference: `$.encounter.state.foggedCells`.
- Historical v5 paths, versions, manifest hashes, and synthesized rows remain pinned.
- v5 cells remain at the manifest-declared 8,192-byte semantic cap.
- Live 32,768-byte timeout rows are rejected exactly with `Registered context caps mismatch at 1:1.`
- Live delivered cancellation rows are rejected by both the historical packet schema and registered validation.

Test results:

- Second-family: `50/50 passed`.
- D569 v5: `27/27 passed`.
- D569 blind experiment: `46 passed / 6 expected frozen-family failures`.

Expected blind-experiment reds:

1. `validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment`
2. `dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows`
3. `adds exactly 180 separately labeled advice-assisted hint cells, 660 total`
4. `consumes the frozen second encounter family for all 480 core cells`
5. `records exact model effort cli and cliVersion for every observed row`
6. `rejects either decisive integrity field alone while preserving rows with neither field`

All six arise because that historical test helper deliberately returns committed legacy fixture bytes as both regenerations, leaving exactly 40 expected regeneration violations.

Verification:

- Frozen plan: SHA-256 `4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69`, 403 lines.
- `npx tsc -p tsconfig.app.json --noEmit`: exit 0.
- `npx tsc -p tsconfig.node.json --noEmit`: exit 0.
- `git diff --check`: exit 0, empty.
- Added lines over 120 columns: 0.
- Expanded V-freeze before/after, including `hard-2`: exit 0/0.
- No frozen fixture or manifest bytes changed.
- `git status --short`: exactly the three allowed files above.

The production v5/v6 schema seam in `tools/d569-blind-experiment.ts` remains for Batch 6 because that file is outside Batch 1’s allowlist. Batch 1 pins the required v5 half; Batch 6 must add v6’s 32,768 declaration and reject 8,192-byte v6 rows.

COHORT-01 B1 FIX R1 DONE
417:  exceptions, removal-of-any-row tests, and the 6204 mutant.
425:Batch 9 must construct the hand-authored Scout A with `profile.rules.speed: feet(0)` before encounter creation, retain the typed zero-speed effect, and assert both declared and effective speed are zero (attack declarations take their movement allowance from `subject.profile.rules.speed`, offer-declarations.ts:431–465, so the effect alone still lets A move 25 ft to (8,3) and keep its Longbow). Preserve the exact base and mutated labels, selector keys, scores and menus at the lines the plan states (base: Longbow ×2 + Dodge, adjacency and visibility keys, scores 60/60; with (8,4) added: Dodge/Dodge, menu []). Require two mutants: restoring A's declared speed to 30 while retaining the effect, and separately disabling corner sealing, must each fail the mutation expectations. Re-verify against the ACCEPTED WALL-01 before implementation acceptance. This correction is confined to the allowed speculative-planning test file; no production movement change is authorized.
442:/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md:22969:Sol (impl-land-01-sol.md): engine-query-port.ts:1167 refuses attack/save offers whose trace blocksSight with the new exact code target_outside_line_of_sight, tracing from the proposed origin (execution's sourceAnchor semantics); the targetability audit found no other authorization gate (query-port :1234/:1547 are tactical-scoring cover inputs and :1830 reports physical cover — left alone, and changing them broke valid intel expectations); room-generator.ts:1227 generatedLineIsOpen = tier 'none' && !blocksSight for both opposing-pair and creature-to-cell predicates; the brutal-b helper materializes the first declared legal activation choice (keeps the engine-ranked option). Witnesses: the two-blocker seam fixture (rays (14,6)/(15,6)→(1,6)/(2,6) along row line 6 between (8,5)/(8,6): closed → half/blocksSight true, no attack offered, LOS code; one flank removed → offered); the AGREEMENT PROPERTY over hard 5117001-013, brutal 6203001-010, brutal-b 6206001-010: exactly 214 offered attack/save uses, every one executes through the reducer without the LOS/Total-Cover error; mutant OFFER_IGNORES_BLOCKS_SIGHT → both red (the cardinality would be 451); OPEN_RAY_IGNORES_BLOCKS_SIGHT → red. GENERATOR BYTES: in-memory regeneration of all 42 checked seeds is byte-equal before/after the fix — so the fixed generator does not cure the frozen families; restoring productivity needs a NEW cohort (reselected seeds), which sharpens question 5's option A. Suites: engine-query-port 19/19; los-cover 112/112; brutal-b 37 passed / 10 failed — the ten productivity pins only (the 19 c1 rows and the four helper rows now execute; "rejects rather than skips" is GREEN, contrary to my brief's prediction — the attended pending decision still forms); d569-blind-experiment 46 / 6 retained (frozen-family, owner's decision); OFFERS 11 files 329/329; dm-tactical-intel + challenge-room 23/23; tsc 0/0; OFFERS checks 0/0; pair 38.6 s; 5 files 260+/9−. ONE CONSEQUENCE outside the manifest: tests/unit/vtt/tactical-evaluator-r02.test.ts:262 pins a hand-authored expected-value probability 0.380769180625 that scored three attacks from origins the reducer would have REFUSED (priest m1, scout m3, bandit m4 at {half, blocksSight true}); with the corrected query the offers come from executable origins (priest 30-ft move to (13,4) none/false; scout 5 ft to (18,4) half/false; bandit 5 ft to (17,4) half/false) and the evaluator scores from each option's finalPosition, giving 0.4323649600000002. Legitimate: the old number rated offers that could not execute. RULING: manifest +1 — tests/unit/vtt/tactical-evaluator-r02.test.ts, that ONE expectation, re-derived BY HAND in its comment (each monster's new origin, line tier/sight, hit chance and damage terms, the sum; the old value kept as pre-LAND-01), never copied from the run; r2 on the resumed session; then my verification, astra medium, and LAND-02.
535:Owner also confirmed on the annotated image: "The orange and purple lines should not work" (all four corner rays through the column-7 wall are blocked — the seam rule's result). WALL-01 dispatched to sol (resume the seam-rule session 01a0ab3f…, brief impl-vis-field-wall-01.md, log impl-wall-01.log) on the worktree at 807d2e3c: the vertex rule in cover.ts with exact lattice-point enumeration, hand-authored witnesses (crack sealed, single-blocker control, staircase wall sealed at a step vertex — the case the edge rule alone misses, outer-face graze clear, 2x2 solid unchanged), the SEAM batch's DIAGONAL_CORNER_TOUCH_UNCHANGED pin re-pinned by hand to the sealed result (renamed _SEALED_D642), three mutants, per-family consequence counts before/after, the red-set audit (a red in a non-frozen suite is a finding, not a re-pin), and proof that generation bytes are untouched. Astra medium review follows; then question 5 (A1) regenerates the cohorts under the sealed geometry.
544:SOL WALL-01 (saved impl-wall-01-sol.md): gcd lattice enumeration, TL+BR / TR+BL pairs, progress-ordered merge of interior/edge/vertex events, physical tier untouched; 43/43; three mutants killed; counts table "unchanged"; 42 seeds byte-identical; pair 38.6 s; two los-cover budget timeouts. VERIFIED BY ME (clean checkout + patch, traceTerrainLine, Medium tokens, 15x8): blockers (7,3)&(8,4) with the ray cells (13,4)→(1,4): blocksSight FALSE (must be true); (7,4)&(8,3): FALSE; vertical (4,2)&(5,3) with (5,0)→(5,6): FALSE; the diagonal crack cases TRUE. CAUSE (read in cover.ts): sharedVertexBlockingCells is built from `candidates`, which traceSpaces filters by the bounding box of the eight corners; for a ray along a grid line between Medium cells the far-side flank row is outside the box, so the vertex is never registered. Sol's STAIRCASE_WALL_SEALED witness passes only because its geometry keeps both cells inside the box — it does not exercise the case it is named for, and the "counts unchanged" table is not evidence about D642. FIX R1 (sol resumed; brief impl-vis-field-wall-01-fix-r1.md; log impl-wall-01-fix-r1.log): on-the-fly sightBlockingAt lookups around each interior lattice vertex (as the edge rule does), witnesses replaced with my three geometries + a split-pair control, a bbox-regression mutant, counts and generation bytes redone.
551:SOL fix r1: vertex lookup now queries sightBlockingAt directly around each GCD-enumerated interior lattice vertex (no candidate bbox), also on the coverTierBetweenObjects path; the three axis witnesses (my geometries: first cells (7,3) / (8,3) / (4,2)) + split control; 46/46; four mutants incl. the bbox-regression one (exactly the three axis witnesses red); counts table unchanged before/after (hard 142/35, brutal 57/34, brutal-b 15/41, brutal-2 58/36 — now with a working rule); 42 seeds byte-identical (aggregate 7b884bf9…); pair 39.1 s; the 35 frozen/behaviour reds unchanged; four los-cover 5 s budget timeouts under the combined audit (110/112 isolated with two) — perf, not re-pinned. MY VERIFICATION (verify-wall-01-r1.log; ../dnd-wt-seam-ab reset to 807d2e3c + patch): tsc, my seven-case probe (A/B/C/D must block, E split pair / F outer-face graze / G single corner must stay clear), a vertex-lookup-null mutant, the suites, los-cover alone, pair.
555:RETRACTION, at full length. In D645 I rejected WALL-01 as P1 on two grounds, both wrong: (1) I claimed the vertex map was built from the bounding-box-filtered `candidates`; it was built from `candidateEntries` — the UNFILTERED union of indexed sources and blocked cells — so no cell was ever excluded; (2) I read my probe's aggregate blocksSight=false for two isolated diagonal blockers as "the rule is inert", but blocksSight is true only when EVERY corner ray is blocked, and in that geometry the ray along the far face (y=5) is legitimately clear (a graze) — the aggregate false was CORRECT. The decisive test I should have run first: a FULL stepped wall (column 7 rows 0–3, column 8 rows 4–7), Medium tokens (13,4)→(1,4): baseline 807d2e3c leaks (false); ORIGINAL WALL-01 blocks (true, first (7,3)); fix r1 blocks (true). Verified by me tonight on the throwaway checkout, all three trees. So the original WALL-01 was correct; fix r1 replaced a correct precomputed map with equivalent on-the-fly lookups and added per-ray witnesses for my three geometries (valid per-ray, and the split-pair control) — harmless, kept as the candidate, but the "bbox regression" mutant sol reports is a restriction the original never had. Sol did not push back on a false P1; the brief told it what to find. Lesson recorded in memory (aggregate vs per-ray; read the code path before naming a cause).
557:MY VERIFICATION of fix r1 otherwise (verify-wall-01-r1.log): tsc 0/0; diff-check 0; vertex-lookup-null mutant → 5 sealed witnesses red; suites 2150/2186 = the 35 known frozen/behaviour reds + scene-snapshot; los-cover 110/112 (the two 5 s budget timeouts, perf); pair 36/36 in 39 s.
564:ASTRA r3 (saved review-plan-cohort-01-astra-r3.md): F2 closed (all three namespace commands re-run verbatim, statuses 0/1/0, 1,000/1,000 plain and underscore seeds per namespace + the xxx form); F5 closed (ten-namespace union, own-cohort exceptions, remove-any-row tests, the 6204 mutant); F6 closed (positive movement stays productive; Dash-only witness + movement-arm mutant; sight/cover only on targeted offensive components); F1/F4 closed; counts 3/3/10/10/10/8/4/4/3/1 = 52 paths. F3 resolved by AMENDMENT: with the typed zero-speed effect alone the real planner still offers A a 25-ft move to (8,3) + Longbow (attack declarations read subject.profile.rules.speed, offer-declarations.ts:431–465); with the profile's rules.speed also feet(0) the real planner + an in-memory D642 reference gives base = Longbow ×2 + Dodge with adjacency/visibility keys scoring 60/60 and, with (8,4) added, Dodge/Dodge and menu [] — and removing corner sealing restores the Longbow and the two-entry menu. N1 (appended verbatim to the plan, all copies): construct A with rules.speed feet(0), keep the effect, assert declared and effective speed zero, preserve the exact labels/keys/scores/menus, two mutants (speed 30 restored; sealing disabled), re-verify against accepted WALL-01; test-file only. Plan copies: .tmp-plans (main), ../dnd-wt-seam-verify, ../dnd-wt-cohort-01, ../dnd-wt-vis-field.
5686:      Tests  50 passed (50)
6341:The second-family suite is now green at `50/50`: all 20 structural rows still print the sole fog-key path, v1 has zero violations, and the three negative witnesses plus the independent-regeneration mutant witness pass. The v5 suite now gets past D569 validation but exposes a later collection-time mismatch in its synthetic runner row (`Registered context caps mismatch at 1:1`). I’m checking whether that is the plan-required historical v5 test setup (and therefore fixable in its allowed file) or an infeasible baseline gap before changing it.
8308:22928:Sol (impl-b2-sol.md): all four D635.25 rulings applied — detectCombatant relocated verbatim to visibility-field.ts (state-only: sight evaluation, combat rules/conditions, spaces/distance, affected cells, terrain walls, domain types; no path to combat/random.ts), encounter.ts re-exports, the query port imports it from visibility-field and keeps no ladder (the AST witness now checks the visibility-field import); decoder-equality assertions in los-cover (:271 → the frozen text still contains the legacy key and decodes equal) and two-room (tracked source has no key; decoded state equals the generated one); two-room.v1.json regenerated by the real generator with exactly the empty-key removal (+0/−1), snapshots/examples byte-identical; scene-snapshot normalized again (revision df6f2cce… → 35c96ecc…, fingerprint 163cf200… → dd802783…, request/coordinator hashes unchanged, old values in the comment); semantic-board-payload reads fog via projectDmView; SIGHT_TRACER_HAS_SINGLE_OWNER updated to the Batch 2 stage; content-pack schema regenerated (+57/−0, three devils_sight additions); RED-first 41/8; all five mutants killed with fresh shas and cp-restored; tsc 0/0; OFFERS checks 0/0; pair 39.1 s; B2 suites 50/50; capsule graph 11/11; los-cover 111/111; 29 other core files 476/476; semantic 7/8 (only M576-E3 stays, B6); OFFERS set green EXCEPT engine-mcp-handler "validates emitted adjustment_delta_truncated_board against the handler output schema": expected dropped classes ["light"], received ["light","adjacency","objects"] — the calibration board's semantic bytes grew 7,817 → 9,322 (cap SEMANTIC_BOARD_MAX_BYTES 8,192) because the semantic source now carries the DM-derived fog (106 cells; the fixture had 0 authored fog). All OFFERS digest/identity pins unchanged. RULING: that pin is B6's by plan text — B6 (:228-240) owns "truncation ordering/class/cap is explicit" and tests/unit/tools/engine-mcp-handler.test.ts is in B6's manifest (:230); the red is added to the known intermediate reds (now: semantic-board-payload M576-E3, engine-mcp-handler truncated-board calibration — both B6). B2 is otherwise complete; my verification runs now, then astra at medium.
8309:22934:Astra r1 (review-impl-b2-astra-r1.md): detectCombatant relocation byte-identical (43 lines, sha 90e42286…); the moved web helper's terrainWallCells substitution equals the forwarding wrapper at encounter.ts:2110; runtime import walk from visibility-field reaches 40 files, zero forbidden, no runtime path to encounter.ts; the AST witness is real (named import, no top-level detect, exactly two canonical calls in visibility) with its limit stated (no symbol resolution); the four-outcome matrix hand-derived; schema regenerated in memory byte-for-byte, three additions; two-room generator check passes, one deleted line; decoder validation/discard proven; 82 JSON + 2 TS legacy fixtures untouched; every remaining foggedCells reference classified (36 lines: derived projection I/O, codec migration, B6 semantic, probe I/O); D569 persistence demonstration reproduced (df6f2cce… → 35c96ecc…; fingerprint dd802783… recomputed; request/coordinator unchanged); both B6 reds reproduced with the byte growth 7,817 → 9,322 vs 8,192 and plan :230 confirmed; two plan mutants + its own boundary mutant killed; tsc 0/0; pair 38.5 s. B2-F1 (P2): src/content/content-pack.ts:291 keeps z.array(combatSenseSchema).min(1).max(5) — a monster declaring all six supported senses (normal sight, darkvision, blindsight, tremorsense, truesight, devils_sight) is rejected as malformed; .max(6) fixes it (astra verified in memory). ACCEPTED — the plan's IMPORTED_DEVILS_SIGHT_ACCEPTED intent is defeated at the limit. Fix r4 (resumed lane): .max(6) with duplicate-kind rejection retained; regenerate the schema (diff = the maxItems change only); add an independent six-kind acceptance literal and keep the unknown-sense rejection; the byte-for-value pin unchanged. Astra r2 confirms.
8310:22940:Astra r2 (review-impl-b2-astra-r2.md): 3 files 37+/4−; exactly three maxItems 5→6 changes, in-memory generation byte-identical; the six-kind literal asserts zero diagnostics and exact imported + runtime senses; its own .max(6)→.max(5) mutant red at :174; unknown-sense rejection and the byte-for-value pin unchanged; tsc 0/0; content-pack 27/27. vis-field = 4c6a3315: B1, B3, SEAM, B4, B5, REBASE (main 9f148666), B2, B2-F1. B6 dispatched (fresh sol; brief impl-vis-field-b6.md; log impl-b6.log; 10-file manifest per plan :230 with the turn-context schema by generator only; owns the two known reds — the semantic partition and the OFFERS truncation calibration, re-derived by hand from class sizes against the 8,192 cap; six plan mutants; the D569 E1C/FOOTPRINTS demonstration with its hard stops; the §8 checkpoint re-verification by symbols; the final aggregate of every B1–B6 suite and `npx tsc -b --force`). After B6 and astra: the VIS-FIELD landing gate on the branch merged with main (gate-wt2 + the OFFERS checks + the D630 pair and arena round), then "Land VIS-FIELD-01" on main, mirror push (D640), then ELEVATION-02 B0.
8312:22946:Sol (impl-b6-sol.md, 381 k tokens): v2 visibility block (observers + subjects transposes, strict schemas, generator-only JSON schema), player output omits it (with a forbidden-field control), serializer sentinel seam, effect obscurement union, Invisible subject keeps visible_from, truncation ordered light → adjacency → objects → visibility with the 8,192-byte calibration re-derived by hand (16,120 → 15,167 → 13,168 → 12,259 → 5,492 bytes), truncation recorded, the final single-owner stage, source-binding witnesses (standard-case string leaves 493 → 550, paths 131 → 143, explained), the semantic partition pin hand-derived [[4,0],[3,1]]; six plan mutants killed with sha-restored files; semantic 11/11, engine-mcp-handler 131/131, source-binding 6/6, support 227/227; tsc 0/0; OFFERS checks 0/0; D569 E1C/FOOTPRINTS unchanged; pair 38.85 → 38.72 s; 36 compiled foggedCells references, none a runtime state consumer; the final aggregate of all 30 plan-named suites 759/760 in 1,172 s — the sole red tests/unit/tools/ai-dm-screenshot-probe.test.ts:464 "is versioned, uses generic classic-board conventions, and leaks no non-rendered probe identity" at 5.33 s (isolated 5.18 / 5.28 s) against its D524 5 s budget; sol stopped rather than raise a timeout (correct). ALSO CHANGED BY SOL (flagged): the blind semantic cap SEMANTIC_BOARD_MAX_BYTES (or its blind-context counterpart) raised 8,192 → 32,768 because the retained hard fixtures' semantic boards now measure 13,406–17,597 bytes with the visibility block; that is a blind-DM context-size decision the plan left open ("cap is explicit") — astra reviews it; the owner is told. A/B BY ME (ab-probe.log, isolated, alternating, load ≈1): main 9f148666 passes at 4,205 / 4,210 ms; vis-field (B6 dirty) fails at 5,243 / 5,094 / 5,246 ms — +21–25% on this single test, the derived-visibility cost of the primer's board projections, at the edge of D630's 25% and over a fixed 5 s budget. RULING: a NAMED re-budget for this one test — timeout 5,000 → 10,000 ms — recorded here with the numbers as the justification (the plan's "raise timeouts only for D544-named flaky tests" is a lane rule; this is not flakiness but a measured cost of the plan's own behaviour, and the supervisor may name it); manifest +1 for that timeout line only (12 files); the cost itself is queued for TEST-PERF-01 / the owner's engine-cache decision (D630 brainstorm rank 6) with sol's mechanism note (how many projections the primer render computes derived fog for). r3 on the resumed session: apply the re-budget, state the mechanism, justify the cap change with per-fixture bytes and what the 8,192 cap would have dropped, then the forced build and the second aggregate.
8313:22949:Sol r3 (impl-b6-sol.md): re-budget applied exactly as ruled; mechanism traced (44 fact sheets → 88 projection calls → 73 fresh derived-fog evaluations, because each encounter-board clone is a distinct state root — ai-dm-screenshot-probe.ts:829, visibility.ts:524, encounter-board.ts:716, visibility-field.ts:550); cap justified per fixture (with/without visibility bytes and the classes the 8,192 cap would drop from each: all four; 16,384 rejects 5117001/5117002/5117007; 32,768 = half the 65,536 blind base cap); D569 blind fixtures unchanged (10 files, inventory sha 3cb2a649…); E1C/FOOTPRINTS unchanged checks 2/2; six mutants killed; tsc 0/0; tsc -b --force 0; OFFERS checks 0/0; pair 38.7 s; second final aggregate of all 30 plan suites 760/760 in 1,171 s. VERIFIED BY ME (verify-b6.log; dirty tree 11 files 516+/32−): tsc app/node 0; tsc -b --force 0; both OFFERS checks 0; 0 forbidden, 0 long lines; schema generator idempotent (a08a7359… → a08a7359…); tests/fixtures diff empty; MY MUTANT PLAYER_OUTPUT_KEEPS_VISIBILITY (the audience === 'dm' guard dropped at semantic-board-payload.ts:601) → the sentinel/player-omission test RED, restored 77cfdb2d…; 11 B6 suites 328/328 (semantic, blind-turn-context, source-binding, engine-mcp-handler, board-delivery, visibility-field, screenshot-probe, engine-mcp-server, engine-mcp-boundary, stable-dom-render, accessible-board); pair 36/36 in 39 s. Committed 10cc9f5e. vis-field = 10cc9f5e: B1, B3, SEAM, B4, B5, REBASE, B2, B2-F1, B6 — every batch of the frozen plan. Astra (resume 01a0a642…, medium; brief review-impl-b6-astra.md; log review-b6-astra-r1.log) reviews the last batch and is asked to state whether the 32,768-byte blind semantic cap is within B6's authority or a product decision; the owner has been told and can rule before landing. Next after ACCEPT: the VIS-FIELD LANDING GATE — merge main into the branch (main has not moved since the rebase: 9f148666), gate-wt2 (tsc -b, sg, full vitest, Playwright), OFFERS checks, D630 pair and one arena round on the merged tip, then "Land VIS-FIELD-01" on main and the mirror push (D640); then ELEVATION-02 B0.
8314:22952:Astra r1 (review-impl-b6-astra-r1.md, 1.72 M tokens): 11 files 516+/32−; schema regenerated in memory byte-identical (159,453 bytes); the strict v2 objects reject unknown keys (schemas.ts:671-683) with the blind validator mirroring them; no DM-semantic-to-player delivery path (projectPlayerBoard builds from PlayerView; serialization requires the DM audience at :601; turn-context delivery rejects player projections at :673; MCP attachment paths DM-only at engine-server.ts:1568-1579); one sight service (visibilityField / cellsThatCanSee at :334/:344; the serializer consumes supplied rows at :604-612); the truncation calibration reproduced exactly (16,120 → 15,167 → 13,168 → 12,259 → 5,492); the semantic partition oracle re-enumerated by rational arithmetic (18 visible, only [(4,0),(3,1)] concealed); the effect-line and Invisible-subject oracles confirmed; the source-binding counts recounted (57 = 3 + 6×5 + 6×4; 12 paths) and all three mutations rejected; the 10 D569 fixtures and the 31,995 / 32,000-byte raw-context pins byte-identical; three own mutants killed; tsc 0/0; pair 39.2 s. Its noted limits: the final single-owner test is a lexical check (no alias bypass found); its sandbox could not execute the D569 pins (sol 2/2 and my board-delivery 17/17 stand as the evidence). BLIND CAP: astra judges 32,768 bytes within B6's explicitly open cap choice, with the owner notification retained; two corrections to sol's account, accepted: blind delivery REJECTS an oversized protected semantic block rather than truncating it (blind-turn-context.ts:1304-1316) — so under the old 8,192 cap every retained hard fixture's blind context would have been refused outright, not thinned — and the semantic allowance is ADDITIONAL to the 65,536-byte base allowance, not within it. The owner may still rule the cap smaller before the landing merge; absent a ruling it lands at 32,768. VIS-FIELD-01 = 10cc9f5e: B1, B3, SEAM, B4, B5, REBASE, B2, B2-F1, B6. LANDING GATE launched now (gate-vis-field-land.log): gate-wt2 wt-vis-field (tsc -b --force, sg, full vitest, Playwright chromium) + the two OFFERS checks + the D630 pair twice + one arena round; main 9f148666 is already an ancestor, so the landing is a "Land VIS-FIELD-01" no-ff merge once the gate reads green (timeouts, if any, rerun isolated per the offers precedent D617.50); then the mirror push (D640); then ELEVATION-02 B0 on a fresh worktree from the landed main.
8317:23017:SOL: three test files 58+/16−, nothing under tests/fixtures. Task A: structural diff = exactly $.encounter.state.foggedCells for all 28 byte pins (12 basis, 12 hard, 3 brutal, M576); normalizer drops the key with a D635.48 comment; the three BRUTAL_BASIS_GENERATED_DIGESTS and all ten D466_6204_NON_ROSTER_DIGESTS (the projection carries the state field for every seed, not only the three replacements) re-derived with old values kept as comments; reverse mutant (key added back) → exactly 12 basis pins red. Task B: four ai-dm-arena blind-row pins → 32,768 with the D635.48 comment (actual semantic bytes observed 10,399); legacy-invariance now derives the starting-room digest IN the test (reconstructs the pre-B2 state as current + foggedCells: [], asserts that exact single difference, hashes both: 34287ad8… → 4757f909…) and approves only row[0].startingRoomDigest; d569-v5 left red (frozen second family). Sol's runs: room-generator 58 passed + exactly the 8 brutal productivity reds; ai-dm-arena 58/58; legacy-invariance 10/10; tsc 0/0.
10953:- Second-family final run: `50/50 passed`, zero v1 violations.
11349:- Second-family final run: `50/50 passed`, zero v1 violations.
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "WebWorker"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "Bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "isolatedModules": true,
    "tsBuildInfoFile": "/tmp/dnd-multiclass-spells-static-app.tsbuildinfo",
    "noEmit": true,
    "strict": true
  },
  "include": ["src"]
}
{
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023", "DOM"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "Bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "isolatedModules": true,
    "tsBuildInfoFile": "/tmp/dnd-multiclass-spells-static-node.tsbuildinfo",
    "noEmit": true,
    "strict": true,
    "types": ["node", "vite/client"]
  },
  "include": [
    "vite.config.ts",
    "vitest.config.ts",
    "playwright.config.ts",
    "src/vite-env.d.ts",
    "db",
    "scripts",
    "tests",
    "tools"
  ]
}

exec
/bin/bash -lc 'npx tsc -p tsconfig.app.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
exec
/bin/bash -lc 'npx tsc -p tsconfig.node.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
exec
/bin/bash -lc "cat vitest.config.ts; sed -n '1,110p' tests/helpers/test-inputs.ts; sed -n '340,380p' tools/d569-v5/validate-first-arm.ts; rg -n 'RUNNER_.*ROW|runArena|runAi|capturedFailure|canonicalD569' tests/unit/tools/d569-v5.test.ts; sed -n '285,300p;704,717p' tools/d569-blind-experiment.ts" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
import { configDefaults, defineConfig } from 'vitest/config';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

/**
 * `tests/live/*.live-test.ts` probe the real `claude` CLI to check that the
 * bridge's invocation is still capability-free. They cost money, need an
 * authenticated login and need the network, so they are OPT-IN via
 * `npm run test:live`.
 *
 * Note the direction: setting the flag ADDS a suite and clearing it removes only
 * that suite. Nothing in the default set is excluded, skipped or relaxed by this
 * switch — the ordinary run is exactly the run it has always been. (The `-test`
 * suffix does not match the `.test.ts` pattern below, so these files are opt-in
 * by name as well as by flag; verified, not assumed.)
 */
const liveInclusions =
  process.env.AI_BRIDGE_LIVE === '1'
    ? ['tests/live/**/*.live-test.ts']
    : [];
const verdictRecorderSetup =
  process.env.VERDICT_FS_OBSERVATIONS_DIR === undefined
    ? []
    : ['tests/helpers/verdict-fs-recorder-setup.mjs'];

export default defineConfig({
  cacheDir:
    process.env.STATIC_APP_CACHE_DIR ??
    join(tmpdir(), 'dnd-multiclass-spells-static-vitest'),
  test: {
    environment: 'node',
    // Run BOTH unit and integration .test.ts under vitest. Browser tests are
    // .spec.ts under tests/browser and belong to Playwright (npm run test:browser).
    include: ['tests/**/*.test.ts', ...liveInclusions],
    exclude: [...configDefaults.exclude, 'tests/integration-supervisor/**'],
    setupFiles: verdictRecorderSetup,
    /**
     * Derives the SRD spell-source parse ONCE, before any worker is forked,
     * and hands the workers a file path and a key. It is a pure-parse cache
     * and nothing else: the worker re-checks the key against the corpus it is
     * actually holding, re-runs every freeze and every mint itself, and a
     * change to either corpus file or to `spell-source-reader.ts` is a miss
     * and a full re-derivation.
     *
     * Like the live-suite switch above, this adds no suite, excludes nothing
     * and relaxes nothing — it only moves work that already ran 38 times into
     * running once. The reasoning lives in the setup file; the guarantee lives
     * in `src/simulation/spell-source-parse-cache.ts`.
     */
    globalSetup: ['tests/helpers/spell-source-parse-cache-global-setup.ts'],
    // Shared workers require every test to restore mocks, globals, environments,
    // native-prototype mutations, and scoped module mocks at its suite boundary.
    isolate: false,
    // The migration-heavy suite oversubscribes constrained runners when
    // Vitest mirrors the host's full CPU count, causing unrelated fixed-timeout
    // tests to fail under contention. Bound scheduling without changing any
    // suite membership, assertion, or timeout contract.
    maxWorkers: 8,
    clearMocks: true,
    restoreMocks: true,
    unstubGlobals: true,
    unstubEnvs: true,
  },
});
import { readdirSync, readFileSync } from 'node:fs';
import { resolve, sep } from 'node:path';

const testInputBrand: unique symbol = Symbol('test-input');
const recorderStateSymbol = Symbol.for('dnd.verdict-fs-recorder');

type FixturePath = `tests/fixtures/${string}`;
type SchemaSqlPath =
  | `drizzle/${string}.sql`
  | `src/db/${string}.sql`
  | `tests/fixtures/${string}.sql`;
type SrdTextPath = `docs/srd/${string}`;
type GuidePath = `docs/guides/${string}`;
type PublicDataPath = `public/${string}`;
type ContentPath = `content/${string}`;
type ContentDirectoryPath = `content/${string}`;

export interface TestInputSpec {
  readonly fixtures?: readonly FixturePath[];
  readonly schemaSql?: readonly SchemaSqlPath[];
  readonly srdText?: readonly SrdTextPath[];
  readonly guides?: readonly GuidePath[];
  readonly publicData?: readonly PublicDataPath[];
  readonly content?: readonly ContentPath[];
  readonly contentDirectories?: readonly ContentDirectoryPath[];
}

type TestInputCategory = Exclude<keyof TestInputSpec, 'contentDirectories'>;
type DeclaredPath<
  Spec extends TestInputSpec,
  Category extends TestInputCategory,
> = Spec[Category] extends readonly (infer Path extends string)[] ? Path : never;
type DeclaredContentDirectory<Spec extends TestInputSpec> =
  Spec['contentDirectories'] extends readonly (infer Path extends string)[] ? Path : never;

export type TestInputText<
  Category extends TestInputCategory,
  Path extends string,
> = string & {
  readonly [testInputBrand]: {
    readonly category: Category;
    readonly path: Path;
  };
};

export type TestInputBytes<
  Category extends TestInputCategory,
  Path extends string,
> = Uint8Array & {
  readonly [testInputBrand]: {
    readonly category: Category;
    readonly path: Path;
  };
};

export type TestInputDirectoryEntries<Path extends string> = readonly string[] & {
  readonly [testInputBrand]: {
    readonly category: 'contentDirectories';
    readonly path: Path;
  };
};

export interface DeclaredInputReader<
  Category extends TestInputCategory,
  Path extends string,
> {
  readText<Selected extends Path>(
    path: Selected,
    encoding?: 'utf8',
  ): TestInputText<Category, Selected>;
  readBytes<Selected extends Path>(path: Selected): TestInputBytes<Category, Selected>;
}

export interface DeclaredContentDirectoryReader<Path extends string> {
  list<Selected extends Path>(path: Selected): TestInputDirectoryEntries<Selected>;
}

export type DeclaredTestInputs<Spec extends TestInputSpec> = {
  readonly [Category in TestInputCategory]: DeclaredInputReader<
    Category,
    DeclaredPath<Spec, Category>
  >;
} & {
  readonly contentDirectories: DeclaredContentDirectoryReader<DeclaredContentDirectory<Spec>>;
};

interface RecorderFileState {
  declaredInputs?: Set<string>;
  readonly testFile: string;
}

interface RecorderState {
  current?: RecorderFileState;
}

const categoryPrefixes = {
  fixtures: ['tests/fixtures/'],
  schemaSql: ['drizzle/', 'src/db/', 'tests/fixtures/'],
  srdText: ['docs/srd/'],
  guides: ['docs/guides/'],
  publicData: ['public/'],
  content: ['content/'],
} as const satisfies Record<TestInputCategory, readonly string[]>;

function repositoryRoot(): string {
  return process.env.VERDICT_REPOSITORY_ROOT ?? process.cwd();
}

function validatePath(category: TestInputCategory, path: string): void {
  const prefixes = categoryPrefixes[category];
    const visualDescriptor = {
      informationMode: 'blind_state',
      primerVersion: visualProfile['primerVersion'],
      imageRole: Array.isArray(images) ? objectValue(images[0], 'visualProfile.images[0]')['role'] : null,
      glyphMode: visualProfile['glyphMode'],
      captureTilePx: visualProfile['captureTilePx'],
    };
    if (boardImage['mode'] !== 'png' || canonicalJson(visualDescriptor) !== canonicalJson(visualPin) || !Array.isArray(images) ||
      images.length !== 1 || objectValue(images[0], 'visualProfile.images[0]')['sha256'] !== boardImage['sha256']) {
      throw new TypeError(`Registered visual provenance mismatch at ${observation.scheduledCellKey}.`);
    }
    const ingressAudit = objectValue(row['blindIngressAudit'], 'blindIngressAudit');
    if (current) {
      const deliveryStatus = deliverySchema.parse(row['turnContextDelivery']).status;
      const delivered = deliveryStatus === 'delivered';
      if (ingressAudit['version'] !== 2 || ingressAudit['forbiddenContentPassed'] !== true ||
        (delivered && (ingressAudit['status'] !== 'complete' || ingressAudit['passed'] !== true)) ||
        (!delivered && (ingressAudit['status'] !== 'incomplete' || ingressAudit['passed'] !== false))) {
        throw new TypeError(`Registered ingress provenance mismatch at ${observation.scheduledCellKey}.`);
      }
    } else if (ingressAudit['passed'] !== true) {
      throw new TypeError(`Historical ingress provenance mismatch at ${observation.scheduledCellKey}.`);
    }
    const caps = current
      ? objectValue(row['turnContextConfiguredCaps'], 'turnContextConfiguredCaps')
      : objectValue(row['turnContextBudget'], 'turnContextBudget');
    const baseCap = current ? caps['baseBytes'] : caps['configuredBaseBytes'];
    const semanticCap = current ? caps['semanticBytes'] : caps['configuredSemanticBytes'];
    if (baseCap !== cell.baseContextCapBytes || semanticCap !== cell.semanticContextCapBytes) {
      throw new TypeError(`Registered context caps mismatch at ${observation.scheduledCellKey}.`);
    }
    const truncatedBlocks = row['turnContextBudget'] === undefined
      ? []
      : objectValue(row['turnContextBudget'], 'turnContextBudget')['truncatedBlocks'];
    if (!Array.isArray(truncatedBlocks) || truncatedBlocks.some((entry) => typeof entry !== 'string')) {
      throw new TypeError(`Invalid truncation provenance at ${observation.scheduledCellKey}.`);
    }
    const boardHash = requiredString(boardImage['sha256'], 'boardImage.sha256');
    return {
      arm: cell.arm, cli: 'codex', cliVersion: input.cliVersion,
      model: cell.model, effort: cell.effort, family: cell.family, basis: cell.basis,
53:import { canonicalD569SecondFamilyRegeneration } from '../../../tools/d569-second-family-manifest';
55:import { parseArenaArgs, runArena } from '../../../tools/ai-dm-arena';
66:    regenerate: canonicalD569SecondFamilyRegeneration,
382:  const rows = await runArena(parseArenaArgs([
400:  const rows = await runArena(parseArenaArgs([
414:const RUNNER_CANCELLATION_AFTER_DELIVERY_ROW = await produceCancellationAfterDeliveryRow();
415:const RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW = await produceTimeoutBeforeDeliveryRow();
417:function capturedFailure(action: () => void): string {
467:  const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
475:  const historicalRegistrationError = capturedFailure(() => { validateBrutal(raw(registeredRows)); });
554:        packetError: capturedFailure(() => { pairedDocuments(invalidPacketRows); }),
555:        registeredError: capturedFailure(() => { validateBrutal(raw(invalidRegisteredRows)); }),
564:    contradictoryPacketError: capturedFailure(() => { pairedDocuments(contradictoryPacketRows); }),
565:    contradictoryRegisteredError: capturedFailure(() => { validateBrutal(raw(contradictoryRegisteredRows)); }),
668:    const produced = RUNNER_CANCELLATION_AFTER_DELIVERY_ROW;
696:    const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
    semanticBytes: z.literal(SEMANTIC_BOARD_MAX_BYTES),
    adviceBaseBytes: z.literal(TURN_CONTEXT_MAX_BYTES),
  }),
  bootstrap: z.strictObject({
    resamples: z.literal(D569_BOOTSTRAP_RESAMPLES),
    seed: z.number().int().nonnegative(),
    confidence: z.literal(0.95),
    noninferiorityMargin: z.literal(D569_NONINFERIORITY_MARGIN),
  }),
  sharedKb: z.strictObject({
    rootPath: z.literal(D569_AI_DM_KB_ROOT),
    combinedStartupHash: hashSchema,
    componentHashes: z.record(z.string(), hashSchema),
  }),
  visualPins: z.strictObject({
    blind: z.strictObject({
        sessionPolicy: 'fresh',
        sessionKey: sha256(canonicalJson({ family, arm: arm.id, seed: fixture.seed, rep })),
        timeoutMs: manifest.experimentWallMs,
        escalation: false,
        modelDefaultFallback: false,
        baseContextCapBytes: arm.dmMode === 'blind'
          ? manifest.caps.blindBaseBytes
          : manifest.caps.adviceBaseBytes,
        semanticContextCapBytes: SEMANTIC_BOARD_MAX_BYTES,
        requiredBlocks: ['semantic_board', 'creature_facts', 'initiative', 'legal_movement'],
        truncatedBlocks: [],
      };
    }))));
}

exec
/bin/bash -lc "node --input-type=module <<'JS'
import { startVitest } from 'vitest/node';
const ctx = await startVitest('test', ['tests/unit/tools/d569-second-family-manifest.test.ts'], { config: false, watch: false, cache: false, maxWorkers: 1, fileParallelism: false, reporters: ['verbose'] });
await ctx.close();
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 exited 1 in 262ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify


⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts [ tests/unit/tools/d569-second-family-manifest.test.ts ]
Error: ENOENT: no such file or directory, mkdir '/tmp/EKEGQgI0nDBcOw6nFMfmX/ssr'
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  no tests
   Start at  21:52:44
   Duration  120ms (transform 0ms, setup 0ms, import 0ms, tests 0ms, environment 0ms)


 succeeded in 14914ms:
exec
/bin/bash -lc "rg -n 'fsModuleCache|experimental.*Cache|mkdir.*ssr|tmpdir' node_modules/vitest/dist/chunks/* | head -35; sed -n '370,424p' tests/unit/tools/d569-v5.test.ts; tail -80 /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01/impl-b1-sol.md" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
node_modules/vitest/dist/chunks/index.UpGiHP7g.js:1555:			join(process.versions.pnp ? join(nodeos__default.tmpdir(), this.project.hash) : distDir, "tsconfig.tmp.tsbuildinfo")
node_modules/vitest/dist/chunks/plugin.d.DwFIiJ7i.d.ts:32:	* This is called only if `experimental.fsModuleCache` is defined.
node_modules/vitest/dist/chunks/plugin.d.DwFIiJ7i.d.ts:35:	experimental_defineCacheKeyGenerator: (callback: CacheKeyIdGenerator) => void;
node_modules/vitest/dist/chunks/reporters.d.DtoKVV2s.d.ts:676:	* This is used to generate the persistent file hash by `fsModuleCache`
node_modules/vitest/dist/chunks/reporters.d.DtoKVV2s.d.ts:1348:	* Deletes all Vitest caches, including `experimental.fsModuleCache`.
node_modules/vitest/dist/chunks/reporters.d.DtoKVV2s.d.ts:1351:	experimental_clearCache(): Promise<void>;
node_modules/vitest/dist/chunks/reporters.d.DtoKVV2s.d.ts:3275:		fsModuleCache?: boolean;
node_modules/vitest/dist/chunks/reporters.d.DtoKVV2s.d.ts:3280:		fsModuleCachePath?: string;
node_modules/vitest/dist/chunks/reporters.d.DtoKVV2s.d.ts:3489:	* Delete all Vitest caches, including `experimental.fsModuleCache`.
node_modules/vitest/dist/chunks/coverage.DM_a_rWm.js:549:	if (resolved.experimental.fsModuleCachePath) resolved.experimental.fsModuleCachePath = resolve(resolved.root, resolved.experimental.fsModuleCachePath);
node_modules/vitest/dist/chunks/config.d.A1h_Y6Jt.d.ts:190:		fsModuleCache: boolean;
node_modules/vitest/dist/chunks/cac.DdICfEr1.js:1200:	clearCache: { description: "Delete all Vitest caches, including `experimental.fsModuleCache`, without running any tests. This will reduce the performance in the subsequent test run." },
node_modules/vitest/dist/chunks/cac.DdICfEr1.js:1211:			fsModuleCache: { description: "Enable caching of modules on the file system between reruns." },
node_modules/vitest/dist/chunks/cac.DdICfEr1.js:1212:			fsModuleCachePath: null,
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:12:import nodeos__default, { tmpdir } from 'node:os';
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:640:		this.rootCache = vitest.config.experimental.fsModuleCachePath || join(workspaceRoot, "node_modules", ".experimental-vitest-cache");
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:648:			return r.config.experimental.fsModuleCachePath || this.rootCache;
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:759:				plugins: config.plugins.filter((p) => p.api?.vitest?.experimental?.ignoreFsModuleCache !== true).map((p) => p.name),
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:773:			cacheRoot = vitestConfig.experimental.fsModuleCachePath || this.rootCache;
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:791:		// metadata is shared between every projects in the workspace, so we ignore project's fsModuleCachePath
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:803:		if (![this.vitest.getRootProject(), ...this.vitest.projects].some((p) => p.config.experimental.fsModuleCache)) return;
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:971:		this.fsCacheEnabled = config.experimental?.fsModuleCache === true;
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:9261:			fsModuleCache: config.experimental.fsModuleCache ?? false,
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:10305:				// always inherit the global `fsModuleCache` value even without `extends: true`
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:10306:				if (testConfig.experimental?.fsModuleCache == null && project.vitest.config.experimental?.fsModuleCache != null) vitestConfig.experimental.fsModuleCache = project.vitest.config.experimental.fsModuleCache;
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:10307:				if (testConfig.experimental?.fsModuleCachePath == null && project.vitest.config.experimental?.fsModuleCachePath != null) vitestConfig.experimental.fsModuleCachePath = project.vitest.config.experimental.fsModuleCachePath;
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:10640:		this.tmpDir = tmpDir || join(tmpdir(), nanoid());
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:11127:				api: { vitest: { experimental: { ignoreFsModuleCache: true } } }
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:13020:	/** @internal */ _tmpDir = join(tmpdir(), nanoid());
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:13157:				experimental_defineCacheKeyGenerator: (callback) => this._fsCache.defineCacheKeyGenerator(callback)
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:13323:	* Deletes all Vitest caches, including `experimental.fsModuleCache`.
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:13326:	async experimental_clearCache() {
node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js:14551:		else if (ctx.config.clearCache) await ctx.experimental_clearCache();

  async resume(_binding: AgentSessionBinding, invocation: AgentInvocation): Promise<AgentTurnResult> {
    return this.start(invocation);
  }

  classifyFailure(): 'unknown' { return 'unknown'; }
}

async function produceCancellationAfterDeliveryRow(): Promise<Readonly<Record<string, unknown>>> {
  const directory = mkdtempSync(join(tmpdir(), 'd569-runner-cancelled-delivery-'));
  const service = new D569CancellationSnapshotService();
  const state = structuredClone(FIRST_BRUTAL_STATE);
  const rows = await runArena(parseArenaArgs([
    '--rooms', '1', '--reps', '1', '--seed', '6203001', '--basis', 'brutal',
    '--out', join(directory, 'rows.jsonl'), '--dm-mode', 'blind', '--cli', 'codex',
    '--model', 'gpt-5.6-luna', '--effort', 'high',
  ]), {
    adapter: new D569CancellationAfterDeliveryAdapter(state), repoCommit: PATCHED_COMMIT,
    heartbeat: () => undefined, boardSnapshotServiceFactory: async () => service,
    fixtureStates: [state],
  });
  const produced = rows[0];
  if (produced === undefined) throw new Error('Cancellation arena produced no row.');
  return JSON.parse(JSON.stringify(produced)) as Readonly<Record<string, unknown>>;
}

async function produceTimeoutBeforeDeliveryRow(): Promise<Readonly<Record<string, unknown>>> {
  const directory = mkdtempSync(join(tmpdir(), 'd569-runner-timeout-before-delivery-'));
  const service = new D569CancellationSnapshotService();
  const state = structuredClone(FIRST_BRUTAL_STATE);
  const rows = await runArena(parseArenaArgs([
    '--rooms', '1', '--reps', '1', '--seed', '6203001', '--basis', 'brutal',
    '--out', join(directory, 'rows.jsonl'), '--dm-mode', 'blind', '--cli', 'codex',
    '--model', 'gpt-5.6-luna', '--effort', 'high',
  ]), {
    adapter: new D569TimeoutBeforeDeliveryAdapter(), repoCommit: PATCHED_COMMIT,
    heartbeat: () => undefined, boardSnapshotServiceFactory: async () => service,
    fixtureStates: [state],
  });
  const produced = rows[0];
  if (produced === undefined) throw new Error('Timeout arena produced no row.');
  return JSON.parse(JSON.stringify(produced)) as Readonly<Record<string, unknown>>;
}

const RUNNER_CANCELLATION_AFTER_DELIVERY_ROW = await produceCancellationAfterDeliveryRow();
const RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW = await produceTimeoutBeforeDeliveryRow();

function capturedFailure(action: () => void): string {
  try {
    action();
  } catch (error) {
    return error instanceof Error ? error.message : String(error);
  }
  throw new Error('Expected consumer validation to reject contradictory evidence.');
}
- v5 manifest `caps.semanticBytes = 8192`
- failure: `Registered context caps mismatch at 1:1` from `validate-first-arm.ts:369`

Resolving this requires an unplanned historical-v5 test projection or changing frozen v5 semantics. I stopped rather than introduce either shortcut.

Files modified, all allowed:

- [d569-second-family-manifest.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-second-family-manifest.ts)
- [d569-second-family-manifest.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts)
- [d569-v5.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts)

Implemented before the blocker:

- Strict v1/v2 schema union.
- Raw-byte hashing before projection.
- Legacy projection removes only the committed own `encounter.state.foggedCells` key.
- Generated bytes are never projected.
- V1 retains integrity/static brutal checks while retiring frozen productivity.
- V2 brutal fixtures retain full productivity validation and reject legacy fog keys.
- Historical key absence is rejected.
- v5/v1 path and version assertions added.

RED → GREEN:

- Second-family initial run: `46 passed / 4 failed`.
- Historical manifest initially produced 47 violations: 40 regeneration plus 7 retired productivity violations.
- Second-family final run: `50/50 passed`, zero v1 violations.
- v5 initial failure: `second_family_first_regeneration`.
- v5 after normalization: advances further but fails during collection on the unrelated 8,192/32,768 cap conflict; no tests execute.

Structural-difference table:

| Fixture | Only structural difference |
|---|---|
| hard-2/5118001 | `$.encounter.state.foggedCells` |
| hard-2/5118002 | `$.encounter.state.foggedCells` |
| hard-2/5118003 | `$.encounter.state.foggedCells` |
| hard-2/5118004 | `$.encounter.state.foggedCells` |
| hard-2/5118005 | `$.encounter.state.foggedCells` |
| hard-2/5118006 | `$.encounter.state.foggedCells` |
| hard-2/5118007 | `$.encounter.state.foggedCells` |
| hard-2/5118008 | `$.encounter.state.foggedCells` |
| hard-2/5118009 | `$.encounter.state.foggedCells` |
| hard-2/5118010 | `$.encounter.state.foggedCells` |
| brutal-2/6207001 | `$.encounter.state.foggedCells` |
| brutal-2/6207002 | `$.encounter.state.foggedCells` |
| brutal-2/6207003 | `$.encounter.state.foggedCells` |
| brutal-2/6207004 | `$.encounter.state.foggedCells` |
| brutal-2/6207005 | `$.encounter.state.foggedCells` |
| brutal-2/6207006 | `$.encounter.state.foggedCells` |
| brutal-2/6207007 | `$.encounter.state.foggedCells` |
| brutal-2/6207008 | `$.encounter.state.foggedCells` |
| brutal-2/6207009 | `$.encounter.state.foggedCells` |
| brutal-2/6207010 | `$.encounter.state.foggedCells` |

Mutant witnesses now green:

- Keep legacy key: killed by the 40 regeneration comparisons.
- Drop all committed state: killed by the extra `round` difference witness.
- Normalize generated bytes: killed by divergent generation 2.
- Allow legacy key in v2 brutal: killed by the rebound-hash v2 witness.
- Ignore regeneration 2: killed by the retained divergence witness.

Verification completed:

- Plan SHA-256: exact `2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802`; 399 lines.
- `npx tsc -p tsconfig.app.json --noEmit`: exit 0.
- `npx tsc -p tsconfig.node.json --noEmit`: exit 0.
- `git diff --check`: exit 0, empty.
- `git status --short`: exactly the three allowed files.
- V-freeze before and after: exit 0.
- All 32 historical inventory hashes remained identical.
- Manifest hashes remain `83daa7e…026c8` and `8c0bcb3…1e13a`.
- Added lines over 120 columns: 0. Existing counts remain unchanged at `4 / 0 / 16` across the three files.

Remaining red:

- `[suite collection] tests/unit/tools/d569-v5.test.ts` — historical v5 semantic cap 8,192 versus current blind-runner cap 32,768.

COHORT-01 B1 BLOCKED

 succeeded in 28778ms:
exec
/bin/bash -lc "node --input-type=module <<'JS'
import {createServer} from 'vite';
import {ViteNodeServer} from 'vite-node/server';
import {ViteNodeRunner} from 'vite-node/client';
const server=await createServer({configFile:false,server:{middlewareMode:true},appType:'custom', optimizeDeps:{noDiscovery:true,include:[]}});
const ns=new ViteNodeServer(server);
const runner=new ViteNodeRunner({root:process.cwd(),base:server.config.base,fetchModule:id=>ns.fetchModule(id),resolveId:(id,importer)=>ns.resolveId(id,importer)});
const mod=await runner.executeFile('tools/d569-second-family-manifest.ts');
console.log(Object.keys(mod));
await server.close();
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 4348ms:
[
  'D569_SECOND_FAMILY_LEDGER_ENTRY',
  'D569_SECOND_FAMILY_SEEDS',
  'D569_USED_SEED_NAMESPACES',
  'generatedRoomIntegrityViolations',
  'hardRoomMembershipViolations',
  'brutalRoomMembershipViolations',
  'validateD569SecondFamilyManifest',
  'canonicalD569SecondFamilyRegeneration'
]

exec
/bin/bash -lc "sed -n '940,1040p' node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js; rg -n 'fetchCache|fetchModule|cache.*false|moduleRunner' node_modules/vitest/dist/chunks/cli-api.BK8pd4xc.js | head -30" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
function normalizeResolvedIdToUrl(environment, resolvedId) {
	const root = environment.config.root;
	const depsOptimizer = environment.depsOptimizer;
	let url;
	// normalize all imports into resolved URLs
	// e.g. `import 'foo'` -> `import '/@fs/.../node_modules/foo/index.js'`
	if (resolvedId.startsWith(withTrailingSlash(root)))
 // in root: infer short absolute path from root
	url = resolvedId.slice(root.length);
	else if (depsOptimizer?.isOptimizedDepFile(resolvedId) || resolvedId !== "/@react-refresh" && path.isAbsolute(resolvedId) && existsSync(cleanUrl(resolvedId)))
 // an optimized deps may not yet exists in the filesystem, or
	// a regular file exists but is out of root: rewrite to absolute /@fs/ paths
	url = path.posix.join("/@fs/", resolvedId);
	else url = resolvedId;
	// if the resolved id is not a valid browser import specifier,
	// prefix it to make it valid. We will strip this before feeding it
	// back into the transform pipeline
	if (url[0] !== "." && url[0] !== "/") url = wrapId(resolvedId);
	return url;
}

const saveCachePromises = /* @__PURE__ */ new Map();
const readFilePromises = /* @__PURE__ */ new Map();
class ModuleFetcher {
	tmpDirectories = /* @__PURE__ */ new Set();
	fsCacheEnabled;
	constructor(resolver, config, fsCache, tmpProjectDir) {
		this.resolver = resolver;
		this.config = config;
		this.fsCache = fsCache;
		this.tmpProjectDir = tmpProjectDir;
		this.fsCacheEnabled = config.experimental?.fsModuleCache === true;
	}
	async fetch(trace, url, importer, environment, makeTmpCopies, options) {
		if (url.startsWith("data:")) {
			trace.setAttribute("vitest.module.external", url);
			return {
				externalize: url,
				type: "builtin"
			};
		}
		if (url === "/@vite/client" || url === "@vite/client") {
			trace.setAttribute("vitest.module.external", url);
			return {
				externalize: "/@vite/client",
				type: "module"
			};
		}
		const isFileUrl = url.startsWith("file://");
		if (isExternalUrl(url) && !isFileUrl) {
			trace.setAttribute("vitest.module.external", url);
			return {
				externalize: url,
				type: "network"
			};
		}
		// handle unresolved id of dynamic import skipped by Vite import analysis
		if (url[0] !== "/") {
			const resolved = await environment.pluginContainer.resolveId(url, importer);
			if (resolved) url = normalizeResolvedIdToUrl(environment, resolved.id);
		}
		const moduleGraphModule = await environment.moduleGraph.ensureEntryFromUrl(unwrapId(url));
		const cached = !!moduleGraphModule.transformResult;
		if (moduleGraphModule.file) trace.setAttribute("code.file.path", moduleGraphModule.file);
		if (options?.cached && cached) return { cache: true };
		const externalize = await this.resolver.shouldExternalize(moduleGraphModule.id);
		if (externalize) return {
			externalize,
			type: "module"
		};
		const cachePath = await this.getCachePath(environment, moduleGraphModule);
		// full fs caching is disabled, but we still want to keep tmp files if makeTmpCopies is enabled
		// this is primarily used by the forks pool to avoid using process.send(bigBuffer)
		if (cachePath == null) {
			const result = await this.fetchAndProcess(environment, url, importer, moduleGraphModule, options);
			this.recordResult(trace, result);
			if (!makeTmpCopies || !("code" in result)) return result;
			const transformResult = moduleGraphModule.transformResult;
			const tmpPath = transformResult && Reflect.get(transformResult, "_vitest_tmp");
			if (typeof tmpPath === "string") return getCachedResult(result, tmpPath);
			const tmpDir = join(this.tmpProjectDir, environment.name);
			if (!this.tmpDirectories.has(tmpDir)) {
				if (!existsSync(tmpDir)) mkdirSync(tmpDir, { recursive: true });
				this.tmpDirectories.add(tmpDir);
			}
			const tmpFile = join(tmpDir, hash("sha1", result.id, "hex"));
			return this.cacheResult(result, tmpFile).then((result) => {
				if (transformResult) Reflect.set(transformResult, "_vitest_tmp", tmpFile);
				return result;
			});
		}
		if (saveCachePromises.has(cachePath)) return saveCachePromises.get(cachePath).then((result) => {
			this.recordResult(trace, result);
			return result;
		});
		const cachedModule = await this.getCachedModule(cachePath, environment, moduleGraphModule, importer);
		if (cachedModule) {
			this.recordResult(trace, cachedModule);
			return cachedModule;
		}
		const result = await this.fetchAndProcess(environment, url, importer, moduleGraphModule, options);
9:import { parseAst, searchForWorkspaceRoot, fetchModule, version, createServer, isFileLoadingAllowed, normalizePath, isRunnableDevEnvironment, mergeConfig } from 'vite';
691:	async saveCachedModule(cachedFilePath, fetchResult, importedUrls = [], mappings = false) {
1064:				"vitest.fetched_module.cache": false
1134:		return processResultSource(environment, await fetchModule(environment, url, importer, {
1139:	async cacheResult(result, cachePath, importedUrls = [], mappings = false) {
1260:				if (name !== "fetchModule") return { error: /* @__PURE__ */ new Error(`Unknown method: ${name}. Expected "fetchModule".`) };
2667:	const cacheFs = methodsOptions.cacheFs ?? false;
10025:				viteConfig.cacheDir = VitestCache.resolveCacheDir(resolve(root || process.cwd()), testConfig.cache != null && testConfig.cache !== false ? testConfig.cache.dir : viteConfig.cacheDir, label);
10157:						environment.dev.moduleRunnerTransform = false;
10159:					} else environment.dev.moduleRunnerTransform = true;

exec
/bin/bash -lc "node --input-type=module <<'JS'
import { startVitest } from 'vitest/node';
const ctx = await startVitest('test', ['tests/unit/tools/d569-second-family-manifest.test.ts'], { config: false, watch: false, cache: false, pool: 'threads', maxWorkers: 1, fileParallelism: false, reporters: ['verbose'] });
await ctx.close();
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
exec
/bin/bash -lc "node --input-type=module <<'JS'
import {createServer} from 'vite';
import {ViteNodeServer} from 'vite-node/server';
import {ViteNodeRunner} from 'vite-node/client';
import {readFileSync} from 'node:fs';
import {createHash} from 'node:crypto';
import assert from 'node:assert/strict';
const server=await createServer({configFile:false,server:{middlewareMode:true,hmr:false,watch:null},appType:'custom',optimizeDeps:{noDiscovery:true,include:[]}});
try {
const ns=new ViteNodeServer(server);
const runner=new ViteNodeRunner({root:process.cwd(),base:server.config.base,fetchModule:id=>ns.fetchModule(id),resolveId:(id,importer)=>ns.resolveId(id,importer)});
const m=await runner.executeFile('tools/d569-second-family-manifest.ts');
const {canonicalJson}=await runner.executeFile('src/commands/canonical-json.ts');
const manifest=JSON.parse(readFileSync('tests/fixtures/d569-second-family-manifest.json','utf8'));
const cache=new Map();
function diffs(a,b,p='"'$'"'){if(Object.is(a,b))return [];if(a&&b&&typeof a==='object'&&typeof b==='object'){return [...new Set([...Object.keys(a),...Object.keys(b)])].flatMap(k=>"'!Object.hasOwn(a,k)||!Object.hasOwn(b,k)?[p+'"'.'+k]:diffs(a[k],b[k],p+'.'+k));}return [p];}
for(const c of manifest.cohorts)for(const f of c.fixtures){const raw=readFileSync(f.path,'utf8');assert.equal(createHash('sha256').update(raw).digest('hex'),f.sha256);for(const g of [1,2]){const gen=m.canonicalD569SecondFamilyRegeneration(f.seed,c.difficulty,g);assert.deepEqual(diffs(JSON.parse(raw),JSON.parse(gen)),['"'$.encounter.state.foggedCells'"']);cache.set(f.seed+':'+g,gen);}console.log(f.seed,'both generations: "'$.encounter.state.foggedCells'"');}
const access={readFixture:p=>readFileSync(p,'utf8'),regenerate:(s,d,g)=>cache.get(s+':'+g)};
const codes=(x,a=access)=>m.validateD569SecondFamilyManifest(x,a).map(v=>v.code);
assert.deepEqual(codes(manifest),[]);console.log('raw hashes 20/20; structural diffs 20/20 x2; v1 violations 0');
const f=manifest.cohorts[0].fixtures[0];
for(const [name,change] of [['round',r=>r.encounter.state.round++],['other root',r=>r.unexpected=1],['missing legacy',r=>delete r.encounter.state.foggedCells]]){
const x=structuredClone(manifest),r=JSON.parse(access.readFixture(f.path));change(r);const bytes=canonicalJson(r)+'\\n';x.cohorts[0].fixtures[0].sha256=createHash('sha256').update(bytes).digest('hex');const cs=codes(x,{...access,readFixture:p=>p===f.path?bytes:access.readFixture(p)});console.log(name,JSON.stringify(cs));if(name"'!=='"'missing legacy')assert.deepEqual(cs.filter(c=>c.endsWith('regeneration')),['first_regeneration','independent_regeneration']);else assert(cs.includes('legacy_fogged_cells_missing'));
}
console.log('divergent generation 2',codes(manifest,{...access,regenerate:(s,d,g)=>access.regenerate(s,d,g)+(s===f.seed&&g===2?' ':'')}));
console.log('generated legacy retained',codes(manifest,{...access,regenerate:(s,d,g)=>{const r=JSON.parse(access.regenerate(s,d,g));if(s===f.seed)r.encounter.state.foggedCells=[];return canonicalJson(r)+'\\n';}}));
console.log('helper-fed committed',JSON.stringify(codes(manifest,{...access,regenerate:(s,d)=>access.readFixture("'`tests/fixtures/arena-basis-${d}-2/seed-${s}.json`)}).reduce((a,c)=>(a[c]=(a[c]??0)+1,a),{})));
for(const version of ['"'d569-second-family-manifest-v1','d569-second-family-manifest-v2']){assert.deepEqual(codes({...manifest,version,extra:true}),['manifest_shape']);console.log(version,'unknown-field rejected');}
const v2={...manifest,version:'d569-second-family-manifest-v2'};console.log('v2 legacy rejections',codes(v2).filter(c=>c==='unexpected_legacy_fogged_cells').length);
} finally {await server.close();}
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 4887ms:
5118001 both generations: $.encounter.state.foggedCells
5118002 both generations: $.encounter.state.foggedCells
5118003 both generations: $.encounter.state.foggedCells
5118004 both generations: $.encounter.state.foggedCells
5118005 both generations: $.encounter.state.foggedCells
5118006 both generations: $.encounter.state.foggedCells
5118007 both generations: $.encounter.state.foggedCells
5118008 both generations: $.encounter.state.foggedCells
5118009 both generations: $.encounter.state.foggedCells
5118010 both generations: $.encounter.state.foggedCells
6207001 both generations: $.encounter.state.foggedCells
6207002 both generations: $.encounter.state.foggedCells
6207003 both generations: $.encounter.state.foggedCells
6207004 both generations: $.encounter.state.foggedCells
6207005 both generations: $.encounter.state.foggedCells
6207006 both generations: $.encounter.state.foggedCells
6207007 both generations: $.encounter.state.foggedCells
6207008 both generations: $.encounter.state.foggedCells
6207009 both generations: $.encounter.state.foggedCells
6207010 both generations: $.encounter.state.foggedCells
raw hashes 20/20; structural diffs 20/20 x2; v1 violations 0
round ["first_regeneration","independent_regeneration"]
other root ["reducer_load_invalid","first_regeneration","independent_regeneration"]
missing legacy ["legacy_fogged_cells_missing"]
divergent generation 2 [ 'independent_regeneration' ]
generated legacy retained [ 'first_regeneration', 'independent_regeneration' ]
helper-fed committed {"first_regeneration":20,"independent_regeneration":20}
d569-second-family-manifest-v1 unknown-field rejected
d569-second-family-manifest-v2 unknown-field rejected
v2 legacy rejections 10

 succeeded in 6928ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118001.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 9ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118002.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118003.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118004.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118005.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118006.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118007.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118008.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118009.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118010.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207001.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207002.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207003.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207004.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207005.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207006.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207007.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207008.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207009.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207010.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > pins the amended exact contiguous ranges and verbatim independent ledger evidence 186ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118001.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118002.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118003.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118004.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118005.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118006.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118007.json satisfies every hard state-property invariant before its pin 6ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118008.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118009.json satisfies every hard state-property invariant before its pin 2ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-hard-2/seed-5118010.json satisfies every hard state-property invariant before its pin 3ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207001.json retains historical integrity plus static budget, caster, and terrain invariants 137ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207002.json retains historical integrity plus static budget, caster, and terrain invariants 53ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207003.json retains historical integrity plus static budget, caster, and terrain invariants 125ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207004.json retains historical integrity plus static budget, caster, and terrain invariants 270ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207005.json retains historical integrity plus static budget, caster, and terrain invariants 90ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207006.json retains historical integrity plus static budget, caster, and terrain invariants 83ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207007.json retains historical integrity plus static budget, caster, and terrain invariants 251ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207008.json retains historical integrity plus static budget, caster, and terrain invariants 95ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207009.json retains historical integrity plus static budget, caster, and terrain invariants 75ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207010.json retains historical integrity plus static budget, caster, and terrain invariants 95ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > second_family_uses_primary_seed: rejects primary or previously used seed namespaces 214ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > brutal_membership_checks_name_only: rejects structural failures even with the roster names intact 88ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > rejects missing or rewritten supervisor ledger evidence 101ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > rejects fixture bytes that no longer match the manifest hash 91ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > seed_hash_without_properties: a matching digest cannot excuse failed membership 85ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > second_generation_not_compared: rejects a divergent independent regeneration 86ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > legacy_projection_drops_all_state: retains any second committed state difference 88ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > legacy_projection_requires_the_committed_legacy_key 88ms
 ✓ tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key 1257ms

 Test Files  1 passed (1)
      Tests  50 passed (50)
   Start at  21:53:54
   Duration  6.77s (transform 1.37s, setup 0ms, import 3.06s, tests 3.63s, environment 0ms)


exec
/bin/bash -lc "git diff --check 6887755f e92ebc93 && git diff --exit-code 6887755f e92ebc93 -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2 tests/fixtures/arena-basis-hard-2 && git status --short; python3 - <<'PY'
import subprocess
s=subprocess.check_output(['git','diff','--unified=0','6887755f','e92ebc93'],text=True)
a=[x[1:] for x in s.splitlines() if x.startswith('+') and not x.startswith('+++')]
print('added lines:',len(a),'over 120:',sum(len(x)>120 for x in a))
PY" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
added lines: 191 over 120: 0

codex
Both TypeScript checks passed. The second-family suite passed 50/50, and independent in-memory generation confirmed the single-key difference for all 20 fixtures against both rebuilds. The helper’s committed-byte regenerations produce exactly 20 `first_regeneration` plus 20 `independent_regeneration` violations. Rebound `round` changes and divergent second generations are still rejected.

exec
/bin/bash -lc "node --input-type=module <<'JS'
import { startVitest } from 'vitest/node';
for (const file of ['tests/unit/tools/d569-v5.test.ts','tests/unit/tools/d569-blind-experiment.test.ts']) {
const ctx = await startVitest('test', [file], { config: false, watch: false, cache: false, pool: 'threads', maxWorkers: 1, fileParallelism: false, reporters: ['default'] });
await ctx.close();
}
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
exec
/bin/bash -lc "rg -n 'D635|D642|D643' tools/d569-second-family-manifest.ts tests/unit/tools/d569-second-family-manifest.test.ts; sed -n '294,350p' tools/d569-second-family-manifest.ts; rg -n 'memfs' node_modules/memfs/package.json; sed -n '245,265p' .tmp-plans/2026-09-17-cohort-01-plan.md; git diff --name-only 6887755f e92ebc93" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
}

function brutalRoomStaticMembershipViolations(
  room: GeneratedRoom,
  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
): readonly D569SecondFamilyViolation[] {
  const violations: D569SecondFamilyViolation[] = [];
  addViolation(violations, room.spec.difficultyProfile === 'brutal', 'brutal_difficulty', 'room is not tagged brutal');
  addViolation(
    violations,
    room.spec.challengeBudgetEighths ===
      sameSeedHard.spec.challengeBudgetEighths * BRUTAL_CHALLENGE_BUDGET_SCALE &&
      room.spec.challengeSpentEighths >= room.spec.challengeBudgetEighths * 0.9 &&
      room.spec.challengeSpentEighths <= room.spec.challengeBudgetEighths,
    'brutal_budget',
    'brutal budget must be scaled from hard and spend 90-100 percent',
  );
  addViolation(
    violations,
    executableCaster(room),
    'brutal_caster',
    'brutal roster must contain an executable implemented caster',
  );

  const terrainCounts = {
    difficult: room.spec.terrain.filter((feature) => feature.kind === 'difficult-terrain-patch').length,
    hazards: room.spec.terrain.filter((feature) => feature.kind === 'hazard-object').length,
    lights: room.spec.terrain.filter((feature) => feature.kind === 'light-source').length,
    obscurement: room.spec.terrain.filter((feature) => feature.kind === 'obscurement-patch').length,
  };
  const terrainTotal = Object.values(terrainCounts).reduce((total, count) => total + count, 0);
  addViolation(
    violations,
    terrainCounts.difficult >= 3 && terrainCounts.hazards >= 3 && terrainCounts.lights >= 2 &&
      terrainCounts.obscurement >= 2 &&
      terrainTotal >= BRUTAL_TERRAIN_FEATURE_COUNT_BAND.minimum &&
      terrainTotal <= BRUTAL_TERRAIN_FEATURE_COUNT_BAND.maximum &&
      room.spec.terrain.length > sameSeedHard.spec.terrain.length,
    'brutal_terrain',
    'brutal terrain must meet every configured minimum/count band and exceed hard density',
  );
  return violations;
}

/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
export function brutalRoomMembershipViolations(
  room: GeneratedRoom,
  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
): readonly D569SecondFamilyViolation[] {
  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
  addViolation(
    violations,
    everyMonsterProductive(room),
    'brutal_productivity',
    'every brutal monster must have a productive first-turn offer',
  );
  return violations;
rg: node_modules/memfs/package.json: IO error for operation on node_modules/memfs/package.json: No such file or directory (os error 2)
admit v1/6203, a 6207/6209 overlap, a fourth carry-forward difference, or generation 2 divergence.
Named witnesses fail each mutant.

**T/V:** common checks; both hash tools agree for 30 fixtures, v2, and outer v6. Targeted D569
suites have zero violations and exact core/with-hint counts 480/660, primary-brutal 240/330,
second-family core 480. Historical v1/v5 suites remain green and never execute v6.

### Batch 7 — versioned arena and rerun routes

**Allowed files (4):**

- `tools/ai-dm-arena.ts`
- `tests/unit/tools/ai-dm-arena.test.ts`
- `tools/ai-dm-rerun-packet.ts`
- `tests/unit/tools/ai-dm-rerun-packet-protocol.test.ts`

**R/Implementation:** first fail exact parser/path/wrong-family cases. Add arena basis labels
`brutal-v2` -> 6209 and `brutal-b-v2` -> 6210, both difficulty brutal. At rerun definitions near
lines 26/547 add closed protocols `brutal-10-v2` and `brutal-10-b-v2`, exact ten-seed arrays and
directories. Historical labels/protocols retain their old paths and behaviour.

tests/unit/tools/d569-second-family-manifest.test.ts
tests/unit/tools/d569-v5.test.ts
tools/d569-second-family-manifest.ts

 exited 1 in 10044ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

 ❯ tests/unit/tools/d569-v5.test.ts (0 test)

⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-v5.test.ts [ tests/unit/tools/d569-v5.test.ts ]
Error: EROFS: read-only file system, mkdtemp '/tmp/d569-runner-cancelled-delivery-XXXXXX'
 ❯ produceCancellationAfterDeliveryRow tests/unit/tools/d569-v5.test.ts:379:21
    377|
    378| async function produceCancellationAfterDeliveryRow(): Promise<Readonly…
    379|   const directory = mkdtempSync(join(tmpdir(), 'd569-runner-cancelled-…
       |                     ^
    380|   const service = new D569CancellationSnapshotService();
    381|   const state = structuredClone(FIRST_BRUTAL_STATE);
 ❯ tests/unit/tools/d569-v5.test.ts:414:54

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  no tests
   Start at  21:54:22
   Duration  4.14s (transform 1.77s, setup 0ms, import 0ms, tests 0ms, environment 0ms)


 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

 ❯ tests/unit/tools/d569-blind-experiment.test.ts (52 tests | 6 failed) 2391ms
     × validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment 171ms
     ✓ keeps historical amendments exact while excluding Opus 5 from every active registry 1ms
     × dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows 76ms
     ✓ rejects the Opus notes source counted in the scoring panel or eligibility 4ms
     ✓ rejects a panel of only two scoring seats in manifest and registered analysis 1ms
     ✓ rejects Opus 5 in any active arm or panel position 141ms
     ✓ accepts only notes or empty exposure and rejects a deciding Opus policy 1ms
     ✓ rejects reversed notes and scoring stages and altered panel aggregation 1ms
     ✓ preserves all 32 committed input hash pins across Amendment 2 134ms
     × adds exactly 180 separately labeled advice-assisted hint cells, 660 total 63ms
     × consumes the frozen second encounter family for all 480 core cells 63ms
     × records exact model effort cli and cliVersion for every observed row 63ms
     × rejects either decisive integrity field alone while preserving rows with neither field 62ms
     ✓ rejects changed advice baseline settings, primer, and model-default policy 62ms
     ✓ rejects changed KB and fixture bytes 124ms
     ✓ requires every Astra arm and its code-only versus minhint repair contrast 141ms
     ✓ rejects an Astra hint arm missing its registered repair mode 62ms
     ✓ rejects Astra blind when code-only repair is replaced by a minimal hint 65ms
     ✓ rejects removal of the Astra code-only versus minhint repair comparison 62ms
     ✓ registers direct high-effort Astra versus Sol contrasts in both modes 127ms
     ✓ scores Astra by Astra and Sol by Sol while Fable has no player arm 125ms
     ✓ rejects a mutated historical amendment record 1ms
     ✓ fluke_guard_uses_shuffle_seed: rejects a trained or replaced second encounter cohort 123ms
     ✓ maxed_after_one_no_gain: refuses medium before two consecutive high misses and low before two medium misses 1ms
     ✓ invalid gates do not count and a repeated positive gain resets no-gain progress 0ms
     ✓ refusal interval boundary matrix classifies [ -0.1, -0.01 ] as 'noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ -0.1, +0 ] as 'noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ +0, +0 ] as 'noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ -0.1, 0.01 ] as 'not_noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ +0, 0.01 ] as 'not_noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ 0.01, 0.1 ] as 'not_noninferior' 0ms
     ✓ refusal_upper_bound_not_lower: fails when only the refusal lower bound clears the margin 0ms
     ✓ refusal_direction_is_blind_minus_advice: accepts a nonpositive interval and rejects a positive interval 0ms
     ✓ refusal_margin_is_at_most: accepts an upper bound exactly equal to the named zero margin 0ms
     ✓ refusal_interval_not_mean: a negative mean with a positive upper bound fails 0ms
     ✓ offense_is_strict_and_conjunctive: exact margin fails and just above passes only with refusal passing 0ms
     ✓ invalid_interval_guard: missing, non-finite, or reversed interval evidence never qualifies 0ms
     ✓ proves pairing direction, panel-seat averaging, components, and 100000-resample reproducibility  434ms
     ✓ registered analysis rejects both executed sides when Astra is absent 1ms
     ✓ registered analysis rejects an unknown third seat substituted for Astra 0ms
     ✓ requires Fable Astra and Sol when the opposite side failed execution 1ms
     ✓ registered analysis exposes no Opus input output or deciding path 1ms
     ✓ rejects Opus-only and mixed Opus scoring diagnostic subsets 0ms
     ✓ keeps registered scoring-seat subsets diagnostic and unable to qualify 1ms
     ✓ success_uses_executed_only: refused rows remain zero in primary while executed-only is selection-conditioned 7ms
     ✓ refusal_cluster_distribution_end_to_end: zero-and-one clusters fail, while swapped arms pass 40ms
     ✓ noninferiority_pools_bases: never lets hard success conceal brutal failure 6ms
     ✓ requires the second encounter cohort to point the same way and independently pass 11ms
     ✓ refusal_only_failure_propagates: either primary basis or second family prevents overall success 210ms
     ✓ override_exposes_top_before_blind_seal: requires sealing before top reveal and answer-key-only top evidence 0ms
     ✓ requires identical capsules and validates the sealed blind-intent hash 0ms
     ✓ defines improvement as final policy minus always-engine-top, not blind-intent subset gain 0ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 6 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment
AssertionError: expected [ …(40) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+   "second_family_first_regeneration",
+   "second_family_independent_regeneration",
+ ]

 ❯ tests/unit/tools/d569-blind-experiment.test.ts:409:27
    407|   it('validates the exact v5 Opus 4.8 arms and notes-only Fable Astra …
    408|     const frozen = manifest();
    409|     expect(codes(frozen)).toEqual([]);
       |                           ^
    410|     expect(frozen.version).toBe('d569-blind-experiment-v5');
    411|     expect(frozen.preregistrationAmendments).toEqual(EXPECTED_V5_AMEND…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:473:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > adds exactly 180 separately labeled advice-assisted hint cells, 660 total
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:635:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > consumes the frozen second encounter family for all 480 core cells
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:644:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > records exact model effort cli and cliVersion for every observed row
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:653:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/6]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects either decisive integrity field alone while preserving rows with neither field
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:683:18

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/6]⎯


 Test Files  1 failed (1)
      Tests  6 failed | 46 passed (52)
   Start at  21:54:26
   Duration  5.64s (transform 1.30s, setup 0ms, import 3.18s, tests 2.39s, environment 0ms)


exec
/bin/bash -lc "rg -n 'node:fs|writeFile|mkdir|appendFile|mkdtemp|readFile|rmSync' tests/unit/tools/d569-v5.test.ts tools/ai-dm-arena.ts | head -70; sed -n '1,18p' tools/ai-dm-arena.ts; sed -n '260,320p' tests/unit/tools/d569-v5.test.ts; sed -n '290,365p' .tmp-plans/2026-09-17-cohort-01-plan.md" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
tools/ai-dm-arena.ts:1:import { access, mkdtemp, readFile, writeFile } from 'node:fs/promises';
tools/ai-dm-arena.ts:762:        const sidecar = decodeChallengeRoomProvenanceV1(JSON.parse(await readFile(sidecarPath, 'utf8')) as unknown);
tools/ai-dm-arena.ts:763:        const room = decodeArenaBasisEnvelopeV1(JSON.parse(await readFile(fixturePath, 'utf8')) as unknown, { mode: 'challenge' });
tools/ai-dm-arena.ts:935:    const temporaryDirectory = await mkdtemp(join(tmpdir(), 'dnd-ai-dm-arena-independent-'));
tools/ai-dm-arena.ts:962:    const temporaryDirectory = await mkdtemp(join(tmpdir(), 'dnd-ai-dm-arena-interleaved-'));
tools/ai-dm-arena.ts:1028:    await writeFile(config.outPath, rows.map((row) => JSON.stringify(row)).join('\n') + '\n', 'utf8');
tests/unit/tools/d569-v5.test.ts:58:import { mkdirSync, mkdtempSync, readFileSync, writeFileSync } from '../../helpers/test-filesystem';
tests/unit/tools/d569-v5.test.ts:61:const MANIFEST = JSON.parse(readFileSync(D569_EXPERIMENT_MANIFEST_PATH, 'utf8')) as D569ExperimentManifest;
tests/unit/tools/d569-v5.test.ts:63:  readText: (path: string): string => readFileSync(path, 'utf8'),
tests/unit/tools/d569-v5.test.ts:65:    readFixture: (path: string): string => readFileSync(path, 'utf8'),
tests/unit/tools/d569-v5.test.ts:72:  decodeArenaFixtureText(readFileSync('tests/fixtures/arena-basis-brutal/seed-6203001.json', 'utf8')),
tests/unit/tools/d569-v5.test.ts:95:  const state = applyRoomInitiativeProfile(decodeArenaFixtureText(readFileSync(cell.fixturePath, 'utf8')), 'derived_v1');
tests/unit/tools/d569-v5.test.ts:238:  readonly outputDirectory = mkdtempSync(join(tmpdir(), 'd569-cancelled-delivery-board-'));
tests/unit/tools/d569-v5.test.ts:248:    mkdirSync(join(this.outputDirectory, 'board-images'), { recursive: true });
tests/unit/tools/d569-v5.test.ts:249:    writeFileSync(join(this.outputDirectory, relativePath), png);
tests/unit/tools/d569-v5.test.ts:253:    mkdirSync(join(this.outputDirectory, 'board-html', htmlDigest), { recursive: true });
tests/unit/tools/d569-v5.test.ts:254:    writeFileSync(join(this.outputDirectory, htmlRelativePath), html);
tests/unit/tools/d569-v5.test.ts:289:    const manifest = JSON.parse(readFileSync(invocation.launcherToken, 'utf8')) as EngineMcpLauncherManifest;
tests/unit/tools/d569-v5.test.ts:310:    writeFileSync(manifest.turnContextSpoolPath, `${JSON.stringify({
tests/unit/tools/d569-v5.test.ts:314:    const existing = readFileSync(manifest.blindIngressSpoolPath, 'utf8').split('\n')
tests/unit/tools/d569-v5.test.ts:319:    writeFileSync(manifest.blindIngressSpoolPath,
tests/unit/tools/d569-v5.test.ts:379:  const directory = mkdtempSync(join(tmpdir(), 'd569-runner-cancelled-delivery-'));
tests/unit/tools/d569-v5.test.ts:397:  const directory = mkdtempSync(join(tmpdir(), 'd569-runner-timeout-before-delivery-'));
tests/unit/tools/d569-v5.test.ts:578:    const secondFamily = JSON.parse(readFileSync(D569_SECOND_FAMILY_MANIFEST_PATH, 'utf8')) as {
import { access, mkdtemp, readFile, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { isAbsolute, join, relative, resolve } from 'node:path';
import {
  CONVERSATION_CLIS,
  CONVERSATION_EFFORTS,
  CONVERSATION_TRANSPORTS,
  BOARD_IMAGE_MODES,
  COMBAT_MODELS,
  boardImageOutputDirectory,
  runConversation,
  type ConversationCli,
  type ConversationEffort,
  type ConversationRowPersisted,
  type ConversationRunOptions,
  type ConversationTransport,
  type CombatModel,
  type BoardImageMode,
      html: { relativePath: htmlRelativePath, sha256: htmlDigest, bytes: html.byteLength },
      blindState: {
        informationMode: 'blind_state', role: 'dm_board', ordinal: 1,
        primerVersion: MANIFEST.visualPins.blind.primerVersion,
        glyphMode: MANIFEST.visualPins.blind.glyphMode,
        captureTilePx: MANIFEST.visualPins.blind.captureTilePx,
        domEvidence: {
          optionSurfaceAbsent: true, nextEventPreviewAbsent: true,
          coordinateLabels: 1, creatureBadges: 1, rosterEntries: 1, hpBars: 1,
          legendEntries: 1, wallCells: 0, halfCoverCells: 0, threeQuartersCoverCells: 0,
          difficultCells: 0, obscuredCells: 0, illuminatedCells: 0, fogMarks: 0,
          doors: 0, objects: 0, hiddenMarks: 0, multiCellFootprints: 0,
        },
      },
    };
  }

  async close(): Promise<void> { return undefined; }
}

class D569CancellationAfterDeliveryAdapter implements AgentSessionAdapter {
  readonly kind = 'codex' as const;

  constructor(private readonly state: EncounterState) {}

  async probe() { return { present: true, version: 'D569-CANCELLATION-AFTER-DELIVERY' }; }

  async start(invocation: AgentInvocation): Promise<AgentTurnResult> {
    if (invocation.engineDispatchId === undefined) throw new Error('Cancellation fixture omitted dispatch identity.');
    const manifest = JSON.parse(readFileSync(invocation.launcherToken, 'utf8')) as EngineMcpLauncherManifest;
    if (manifest.turnContextSpoolPath === undefined || manifest.blindIngressSpoolPath === undefined ||
      manifest.dispatchPhase === undefined) {
      throw new Error('Cancellation fixture launcher omitted blind evidence spools.');
    }
    const runtime = createEngineMcpRuntime(this.state, {
      runId: manifest.runId, branchId: manifest.branchId, revision: manifest.revision,
      requestId: manifest.requestId, phase: manifest.phase, correctionNumber: manifest.correctionNumber,
      room: manifest.room, historyKind: manifest.historyKind, toolProfile: 'blind', dmMode: 'blind',
      offerEnvironment: buildOfferEnvironment({ kind: 'binding', binding: manifest.offerEnvironment }),
    });
    const context = record(runtime.toolSurface.execute('engine.get_turn_context', {
      run_id: manifest.runId, expected_revision: manifest.revision, scope: 'round', granularity: 'full',
    }));
    const deliveredContext = {
      ...context,
      semantic_board: {
        provenance: { format: 'd569-cancellation-semantic-board-v1' },
        cells: [{ badge: 1, relation: 'occupied' }],
      },
    };
    writeFileSync(manifest.turnContextSpoolPath, `${JSON.stringify({
      ...deliveredContext, dispatchId: invocation.engineDispatchId,
      dispatchProfile: 'blind', dispatchPhase: manifest.dispatchPhase,
    })}\n`, 'utf8');
    const existing = readFileSync(manifest.blindIngressSpoolPath, 'utf8').split('\n')
      .filter((line) => line.trim().length > 0)
      .map((line) => JSON.parse(line) as BlindIngressRecord);
    const ingress = new BlindModelIngressRecorder(existing);
    ingress.record('turn_context', canonicalJson(deliveredContext));
    writeFileSync(manifest.blindIngressSpoolPath,
      `${ingress.records().map((entry) => JSON.stringify(entry)).join('\n')}\n`, 'utf8');
- `tests/unit/vtt/engine-opportunity-movement-intel.test.ts`
- `tests/unit/vtt/speculative-planning.test.ts`

**R:** preserve the three current deterministic RED titles before edits. Re-run the following
geometry calculations after WALL-01; a discrepancy stops the batch, never selects a new oracle.

**Implementation:** composite hard 5117005 labels are exactly `Dodge`, `End Turn`, with no
`Longbow`. Comment the four corner-ray derivation: Scout `(13,3)` -> Fighter `(1,2)` has two rays
enter `(7,2)` and two on shared edge `(7,2)/(7,3)`; -> Cleric `(2,4)` has two on
`(7,3)/(7,4)` and two entering `(7,4)`; -> Wizard `(1,6)` all cross column 7. D635/D642 block
every set. Room 8 pins monsters 1 `(22,1)`, 2 `(21,1)`, 4 `(21,3)` to zero-foot Dodge at current
cells; monster 5 keeps 55-ft Dash `(22,5)->(12,6)` = ten 5-ft steps + 5 difficult. Replace the
stale comment; these are hand calculations.

For real integration, replace frozen 5117001 with a 15x8 state: Scout A at `(13,4)`, actionless
monster B at `(13,6)`, unacted 30-ft player P at `(1,4)`, ids `combatant:A/B/P`, base walls
`(7,0..3)` plus `(8,5..7)`, and a typed speed-set effect with
`enginePlanningSpeedFeet(A) === 0`. Define exact extracted selector strings
`DA=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:A'}})`
and `TAP=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:P'}})`.
Production `buildHostScenarioMenu` yields labels `[Longbow + Longbow -> combatant:P, Dodge]` and
exact menu rank 1 `adjacency:${DA}:${TAP}` (P moves to `(12,4)`, 55 ft) then rank 2
`visibility:${DA}:${TAP}` (P path `(1,4)->(0,3)->(0,2)->(0,1)->(1,0)`, 20 ft); each has one
proposal reference, only influencing player P, radius 60, and score `1*60=60`. Longbow reach
cannot flip inside 15x8; Dodge contributes no movement-change fact.

The mutation adds `(8,4)`, completing `(7,3)/(8,4)`. Assert the production trace changes
sight-clear -> blocked. Because A's speed is explicitly zero it cannot use Astra's 25-ft escape
to `(8,3)`; A and B now propose `[Dodge,Dodge]`, targeted dependencies disappear, and the exact
menu is `[]`. This is a contrast, not normalization of the frozen empty scene. Recheck the full
base/mutated oracle after WALL-01; any difference stops for plan review.

Separately test the eight-entry slice through `computeHostSplitCandidates` with two Scouts A/B,
P at `(4,2)`, awaiting A's turn, speed 30/radius 60, and a controlled query table. Define
`S(x)=canonicalJson({actorId:'combatant:'+x,selector:{kind:'combatant',combatantId:'combatant:'+x}})`.
All adjacency/reach baselines are false -> true; all visibility baselines are true -> false.
The nine non-shadowed candidates and hand oracle are:

|rank|fact key|refs|legal P destination/path ft|score|result|
|---:|---|---:|---|---:|---|
|1|`adjacency:S(A):S(P)`|A+B|`(2,1)`/10|`2*60=120`|kept|
|2|`adjacency:S(B):S(P)`|B|`(7,3)`/20|60|kept|
|3|`adjacency:S(P):S(A)`|A|`(2,1)`/10|60|kept|
|4|`adjacency:S(P):S(B)`|B|`(7,3)`/20|60|kept|
|5|`reach:S(A):shortsword:S(P)`|A|`(2,1)`/10|60|kept|
|6|`reach:S(B):shortsword:S(P)`|B|`(7,3)`/20|60|kept|
|7|`visibility:S(A):S(P)`|A|`(6,1)`/15|60|kept|
|8|`visibility:S(B):S(P)`|B|`(6,3)`/15|60|kept|
|9|`visibility:S(P):S(A)`|A|`(6,1)`/15|60|excluded|

All costs are independently <=60; the controlled port rejects every unlisted destination.
Order is score, refs, then canonical fact-key bytes (`adjacency` < `reach` < `visibility`). Assert
exact keys/ranks 1-8 and excluded key 9, not merely `<=8`. This stipulated dependency/query table
tests only grouping, score, canonical tie ordering, filtering, and the eight-entry slice; it is
not evidence for D642 corner sealing, which the production integration witness above proves.

**M/T/V:** restore old Longbow/Dash values, let A move despite speed zero, disable corner sealing,
change score multiplication to 62/61, reverse the tie order, or remove `.slice(0,8)`; exact
labels/keys/paths, production mutation, and nine-row table fail. Common checks; run serially.

### Batch 10 — 32,768-byte cap rejection witness

**Allowed files (1):**

- `tests/unit/vtt/blind-turn-context.test.ts`

**R/Implementation:** clone a normal `projectEngineSemanticBoard`, set one schema-valid creature
name to 40,000 characters, prove schema parsing succeeds, then render normally. Assert measured
UTF-8 semantic block exceeds `BLIND_SEMANTIC_BOARD_MAX_BYTES === 32_768` and exact `RangeError`
states required/max byte counts. Baseline may be green because this witnesses existing policy.

**M/T/V:** disable `blind-turn-context.ts:1304-1307` (or make `>` false): the test must turn RED
because oversized context is accepted. Restore, verify source hash, run focused test/common checks.

## Final targeted verification and handoff


exec
/bin/bash -lc "node --input-type=module <<'JS'
import { startVitest } from 'vitest/node';
const target='tools/d569-second-family-manifest.ts';
const plugin={name:'review-ignore-second-generation',enforce:'pre',transform(code,id){if("'!id.endsWith('"'/'+target))return;const from='secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes';if("'!code.includes(from))throw Error('"'mutant anchor absent');return code.replace(from,'firstGeneration === comparisonFixtureBytes');}};
const ctx=await startVitest('test',['tests/unit/tools/d569-second-family-manifest.test.ts'],{config:false,watch:false,cache:false,pool:'threads',maxWorkers:1,reporters:['default']},{plugins:[plugin]});
await ctx.close();
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
exec
/bin/bash -lc "node --input-type=module <<'JS'
import {startVitest} from 'vitest/node';
const shim=String.raw"'`
import {vi} from '"'vitest';
const mem = vi.hoisted(()=>({files:new Map(),dirs:new Set(),n:0}));
vi.mock('node:fs',async(importOriginal)=>{
 const real=await importOriginal();
 const virtual=p=>String(p).startsWith('/tmp/cohort-review-memory-');
 const read=(p,opt)=>{const b=mem.files.get(String(p));if("'!b)throw Object.assign(Error('"'ENOENT '+p),{code:'ENOENT'});return typeof opt==='string'||opt?.encoding?b.toString(typeof opt==='string'?opt:opt.encoding):Buffer.from(b);};
 const write=(p,data)=>{mem.files.set(String(p),Buffer.from(data));};
 return {...real,
 mkdtempSync:()=>{const p='/tmp/cohort-review-memory-'+(++mem.n);mem.dirs.add(p);return p;},
 mkdirSync:(p,o)=>virtual(p)?mem.dirs.add(String(p)):real.mkdirSync(p,o),
 writeFileSync:(p,d,o)=>virtual(p)?write(p,d):real.writeFileSync(p,d,o),
 appendFileSync:(p,d,o)=>virtual(p)?write(p,Buffer.concat([mem.files.get(String(p))??Buffer.alloc(0),Buffer.from(d)])):real.appendFileSync(p,d,o),
 readFileSync:(p,o)=>virtual(p)?read(p,o):real.readFileSync(p,o),
 existsSync:p=>virtual(p)?mem.files.has(String(p))||mem.dirs.has(String(p)):real.existsSync(p),
 rmSync:(p,o)=>virtual(p)?undefined:real.rmSync(p,o),
 };
});
vi.mock('node:fs/promises',async(importOriginal)=>{
 const real=await importOriginal();const sync=await import('node:fs');const virtual=p=>String(p).startsWith('/tmp/cohort-review-memory-');
 return {...real,mkdtemp:async()=>sync.mkdtempSync(''),
 mkdir:async(p,o)=>virtual(p)?sync.mkdirSync(p,o):real.mkdir(p,o),
 writeFile:async(p,d,o)=>virtual(p)?sync.writeFileSync(p,d,o):real.writeFile(p,d,o),
 appendFile:async(p,d,o)=>virtual(p)?sync.appendFileSync(p,d,o):real.appendFile(p,d,o),
 readFile:async(p,o)=>virtual(p)?sync.readFileSync(p,o):real.readFile(p,o),
 access:async(p,o)=>{if("'!virtual(p))return real.access(p,o);if(!sync.existsSync(p))throw Object.assign(Error('"'ENOENT '+p),{code:'ENOENT'});},
 rm:async(p,o)=>virtual(p)?undefined:real.rm(p,o),
 };
});
"'`;
const plugin={name:'"'review-memory-temp-files',enforce:'pre',transform(code,id){if(id.endsWith('/tests/unit/tools/d569-v5.test.ts'))return shim+code;}};
const ctx=await startVitest('test',['tests/unit/tools/d569-v5.test.ts'],{config:false,watch:false,cache:false,pool:'threads',maxWorkers:1,reporters:['default']},{plugins:[plugin]});
await ctx.close();
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 exited 1 in 4471ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

 ❯ tests/unit/tools/d569-v5.test.ts (0 test)

⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-v5.test.ts [ tests/unit/tools/d569-v5.test.ts ]
Error: ENOENT: no such file or directory, symlink '/home/vagrant/.codex-aidm/config.toml' -> '/tmp/cohort-review-memory-4/codex-home-kb/config.toml'
 ❯ buildIsolatedCodexHome tools/ai-dm-conversation.ts:1282:3
    1280|   const skillsDirectory = resolve(codexHome, 'skills');
    1281|   await mkdir(skillsDirectory, { recursive: true });
    1282|   await Promise.all(['auth.json', 'config.toml'].map(async (name) =>
       |   ^
    1283|     symlink(resolve(operatorHome, name), resolve(codexHome, name), 'fi…
    1284|   if (config.instructionSource !== 'skill') return { codexHome, skillH…
 ❯ runConversationWithConfiguredIntel tools/ai-dm-conversation.ts:4252:7
 ❯ runConversation tools/ai-dm-conversation.ts:7653:12
 ❯ Module.runArena tools/ai-dm-arena.ts:940:24
 ❯ produceCancellationAfterDeliveryRow tests/unit/tools/d569-v5.test.ts:411:16
 ❯ tests/unit/tools/d569-v5.test.ts:443:48

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  no tests
   Start at  21:55:14
   Duration  4.30s (transform 1.87s, setup 0ms, import 0ms, tests 0ms, environment 0ms)


 exited 1 in 6965ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

 ❯ tests/unit/tools/d569-second-family-manifest.test.ts (50 tests | 1 failed) 3662ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118001.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 9ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118002.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 3ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118003.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118004.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118005.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118006.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118007.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118008.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118009.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118010.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207001.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207002.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207003.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207004.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207005.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207006.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207007.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207008.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207009.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 2ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207010.json differs from its current canonical rebuild only at $.encounter.state.foggedCells 1ms
     ✓ pins the amended exact contiguous ranges and verbatim independent ledger evidence 193ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118001.json satisfies every hard state-property invariant before its pin 3ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118002.json satisfies every hard state-property invariant before its pin 3ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118003.json satisfies every hard state-property invariant before its pin 3ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118004.json satisfies every hard state-property invariant before its pin 2ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118005.json satisfies every hard state-property invariant before its pin 2ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118006.json satisfies every hard state-property invariant before its pin 2ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118007.json satisfies every hard state-property invariant before its pin 6ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118008.json satisfies every hard state-property invariant before its pin 2ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118009.json satisfies every hard state-property invariant before its pin 2ms
     ✓ tests/fixtures/arena-basis-hard-2/seed-5118010.json satisfies every hard state-property invariant before its pin 3ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207001.json retains historical integrity plus static budget, caster, and terrain invariants 131ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207002.json retains historical integrity plus static budget, caster, and terrain invariants 50ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207003.json retains historical integrity plus static budget, caster, and terrain invariants 124ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207004.json retains historical integrity plus static budget, caster, and terrain invariants 275ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207005.json retains historical integrity plus static budget, caster, and terrain invariants 89ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207006.json retains historical integrity plus static budget, caster, and terrain invariants 86ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207007.json retains historical integrity plus static budget, caster, and terrain invariants 262ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207008.json retains historical integrity plus static budget, caster, and terrain invariants 96ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207009.json retains historical integrity plus static budget, caster, and terrain invariants 78ms
     ✓ tests/fixtures/arena-basis-brutal-2/seed-6207010.json retains historical integrity plus static budget, caster, and terrain invariants 99ms
     ✓ second_family_uses_primary_seed: rejects primary or previously used seed namespaces 222ms
     ✓ brutal_membership_checks_name_only: rejects structural failures even with the roster names intact 90ms
     ✓ rejects missing or rewritten supervisor ledger evidence 102ms
     ✓ rejects fixture bytes that no longer match the manifest hash 92ms
     ✓ seed_hash_without_properties: a matching digest cannot excuse failed membership 88ms
     × second_generation_not_compared: rejects a divergent independent regeneration 90ms
     ✓ legacy_projection_drops_all_state: retains any second committed state difference 87ms
     ✓ legacy_projection_requires_the_committed_legacy_key 95ms
     ✓ new_v2_legacy_fogged_cells: rejects a rebound-hash v2 brutal fixture with the legacy key  1242ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > second_generation_not_compared: rejects a divergent independent regeneration
AssertionError: expected [] to include 'independent_regeneration'
 ❯ tests/unit/tools/d569-second-family-manifest.test.ts:231:10
    229|         return seed === targetSeed && generation === 2 ? `${regenerate…
    230|       },
    231|     }))).toContain('independent_regeneration');
       |          ^
    232|   });
    233|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 49 passed (50)
   Start at  21:55:12
   Duration  6.79s (transform 1.33s, setup 0ms, import 3.05s, tests 3.66s, environment 0ms)


exec
/bin/bash -lc "node --input-type=module <<'JS'
import {startVitest} from 'vitest/node';
const plugin={name:'review-canonical-helper-only',enforce:'pre',transform(code,id){if("'!id.endsWith('"'/tests/unit/tools/d569-blind-experiment.test.ts'))return;const old=\"regenerate: (seed, difficulty) => readText(\\n        "'`tests/fixtures/arena-basis-${difficulty}-2/seed-${String(seed)}.json`,'"\\n      ),\";if("'!code.includes(old))throw Error('"'anchor absent');return \"import { canonicalD569SecondFamilyRegeneration } from '../../../tools/d569-second-family-manifest';\\n\"+code.replace(old,'regenerate: canonicalD569SecondFamilyRegeneration,');}};
const ctx=await startVitest('test',['tests/unit/tools/d569-blind-experiment.test.ts'],{config:false,watch:false,cache:false,pool:'threads',maxWorkers:1,reporters:['default']},{plugins:[plugin]});
await ctx.close();
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 exited 1 in 6933ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

 ❯ tests/unit/tools/d569-blind-experiment.test.ts (52 tests | 1 failed) 3154ms
     ✓ validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment 216ms
     ✓ keeps historical amendments exact while excluding Opus 5 from every active registry 0ms
     ✓ dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows 116ms
     ✓ rejects the Opus notes source counted in the scoring panel or eligibility 4ms
     ✓ rejects a panel of only two scoring seats in manifest and registered analysis 1ms
     ✓ rejects Opus 5 in any active arm or panel position 204ms
     ✓ accepts only notes or empty exposure and rejects a deciding Opus policy 1ms
     ✓ rejects reversed notes and scoring stages and altered panel aggregation 1ms
     ✓ preserves all 32 committed input hash pins across Amendment 2 198ms
     ✓ adds exactly 180 separately labeled advice-assisted hint cells, 660 total 102ms
     ✓ consumes the frozen second encounter family for all 480 core cells 98ms
     ✓ records exact model effort cli and cliVersion for every observed row 108ms
     ✓ rejects either decisive integrity field alone while preserving rows with neither field 99ms
     ✓ rejects changed advice baseline settings, primer, and model-default policy 90ms
     × rejects changed KB and fixture bytes 185ms
     ✓ requires every Astra arm and its code-only versus minhint repair contrast 179ms
     ✓ rejects an Astra hint arm missing its registered repair mode 89ms
     ✓ rejects Astra blind when code-only repair is replaced by a minimal hint 89ms
     ✓ rejects removal of the Astra code-only versus minhint repair comparison 89ms
     ✓ registers direct high-effort Astra versus Sol contrasts in both modes 179ms
     ✓ scores Astra by Astra and Sol by Sol while Fable has no player arm 179ms
     ✓ rejects a mutated historical amendment record 1ms
     ✓ fluke_guard_uses_shuffle_seed: rejects a trained or replaced second encounter cohort 175ms
     ✓ maxed_after_one_no_gain: refuses medium before two consecutive high misses and low before two medium misses 1ms
     ✓ invalid gates do not count and a repeated positive gain resets no-gain progress 0ms
     ✓ refusal interval boundary matrix classifies [ -0.1, -0.01 ] as 'noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ -0.1, +0 ] as 'noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ +0, +0 ] as 'noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ -0.1, 0.01 ] as 'not_noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ +0, 0.01 ] as 'not_noninferior' 0ms
     ✓ refusal interval boundary matrix classifies [ 0.01, 0.1 ] as 'not_noninferior' 0ms
     ✓ refusal_upper_bound_not_lower: fails when only the refusal lower bound clears the margin 0ms
     ✓ refusal_direction_is_blind_minus_advice: accepts a nonpositive interval and rejects a positive interval 0ms
     ✓ refusal_margin_is_at_most: accepts an upper bound exactly equal to the named zero margin 0ms
     ✓ refusal_interval_not_mean: a negative mean with a positive upper bound fails 0ms
     ✓ offense_is_strict_and_conjunctive: exact margin fails and just above passes only with refusal passing 0ms
     ✓ invalid_interval_guard: missing, non-finite, or reversed interval evidence never qualifies 0ms
     ✓ proves pairing direction, panel-seat averaging, components, and 100000-resample reproducibility  451ms
     ✓ registered analysis rejects both executed sides when Astra is absent 1ms
     ✓ registered analysis rejects an unknown third seat substituted for Astra 0ms
     ✓ requires Fable Astra and Sol when the opposite side failed execution 1ms
     ✓ registered analysis exposes no Opus input output or deciding path 10ms
     ✓ rejects Opus-only and mixed Opus scoring diagnostic subsets 1ms
     ✓ keeps registered scoring-seat subsets diagnostic and unable to qualify 2ms
     ✓ success_uses_executed_only: refused rows remain zero in primary while executed-only is selection-conditioned 8ms
     ✓ refusal_cluster_distribution_end_to_end: zero-and-one clusters fail, while swapped arms pass 41ms
     ✓ noninferiority_pools_bases: never lets hard success conceal brutal failure 6ms
     ✓ requires the second encounter cohort to point the same way and independently pass 12ms
     ✓ refusal_only_failure_propagates: either primary basis or second family prevents overall success 213ms
     ✓ override_exposes_top_before_blind_seal: requires sealing before top reveal and answer-key-only top evidence 0ms
     ✓ requires identical capsules and validates the sealed blind-intent hash 0ms
     ✓ defines improvement as final policy minus always-engine-top, not blind-intent subset gain 0ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects changed KB and fixture bytes
AssertionError: expected [ 'second_family_fixture_hash' ] to deeply equal ArrayContaining{…}

- Expected
+ Received

- ArrayContaining [
+ [
    "second_family_fixture_hash",
-   "second_family_independent_regeneration",
  ]

 ❯ tests/unit/tools/d569-blind-experiment.test.ts:726:8
    724|     expect(codes(kb)).toContain('kb_component_hash');
    725|     const changedSecond = `${readText(SECOND_HARD_PATHS[0])} `;
    726|     expect(codes(manifest(), access((path) => path === SECOND_HARD_PAT…
       |        ^
    727|       .toEqual(expect.arrayContaining(['second_family_fixture_hash', '…
    728|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 51 passed (52)
   Start at  21:55:38
   Duration  6.76s (transform 1.55s, setup 0ms, import 3.53s, tests 3.15s, environment 0ms)


exec
/bin/bash -lc "node --input-type=module <<'JS'
import {startVitest} from 'vitest/node';
const shim=String.raw"'`
import {vi} from '"'vitest';
const mem=vi.hoisted(()=>({files:new Map(),dirs:new Set(),links:new Map(),n:0}));
vi.mock('node:fs',async original=>{
 const r=await original(),v=p=>String(p).startsWith('/tmp/cohort-review-memory-');
 const read=(p,o)=>{if(mem.links.has(String(p)))return r.readFileSync(mem.links.get(String(p)),o);const b=mem.files.get(String(p));if("'!b)throw Object.assign(Error('"'ENOENT '+p),{code:'ENOENT'});return typeof o==='string'||o?.encoding?b.toString(typeof o==='string'?o:o.encoding):Buffer.from(b);};
 return {...r,
 mkdtempSync:()=>{const p='/tmp/cohort-review-memory-'+(++mem.n);mem.dirs.add(p);return p;},
 mkdirSync:(p,o)=>v(p)?void mem.dirs.add(String(p)):r.mkdirSync(p,o),
 writeFileSync:(p,d,o)=>v(p)?void mem.files.set(String(p),Buffer.from(d)):r.writeFileSync(p,d,o),
 appendFileSync:(p,d,o)=>v(p)?void mem.files.set(String(p),Buffer.concat([mem.files.get(String(p))??Buffer.alloc(0),Buffer.from(d)])):r.appendFileSync(p,d,o),
 readFileSync:(p,o)=>v(p)?read(p,o):r.readFileSync(p,o),
 existsSync:p=>v(p)?mem.files.has(String(p))||mem.dirs.has(String(p))||mem.links.has(String(p)):r.existsSync(p),
 symlinkSync:(a,b,t)=>v(b)?void mem.links.set(String(b),String(a)):r.symlinkSync(a,b,t),
 rmSync:(p,o)=>v(p)?undefined:r.rmSync(p,o),
 };
});
vi.mock('node:fs/promises',async original=>{
 const r=await original(),s=await import('node:fs'),v=p=>String(p).startsWith('/tmp/cohort-review-memory-');
 const m={...r,mkdtemp:async()=>s.mkdtempSync(''),symlink:async(a,b,t)=>s.symlinkSync(a,b,t),
 access:async(p,o)=>{if("'!v(p))return r.access(p,o);if(!s.existsSync(p))throw Object.assign(Error('"'ENOENT '+p),{code:'ENOENT'});}};
 for(const n of ['mkdir','writeFile','appendFile','readFile','rm'])m[n]=async(p,...a)=>v(p)?s[n+'Sync'](p,...a):r[n](p,...a);
 return m;
});
"'`;
const plugin={name:'"'review-memory-temp-files',enforce:'pre',transform(code,id){if(id.endsWith('/tests/unit/tools/d569-v5.test.ts'))return shim+code;}};
const ctx=await startVitest('test',['tests/unit/tools/d569-v5.test.ts'],{config:false,watch:false,cache:false,pool:'threads',maxWorkers:1,reporters:['default']},{plugins:[plugin]});
await ctx.close();
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 exited 1 in 4923ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

 ❯ tests/unit/tools/d569-v5.test.ts (0 test)

⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-v5.test.ts [ tests/unit/tools/d569-v5.test.ts ]
Error: ENOENT: no such file or directory, realpath '/tmp/cohort-review-memory-2/board-images/cb1a89ee5c74fc7fe596d478216de177acd8b92021b8845497f49e3ea279b725.png'
 ❯ readValidatedBoardImage tools/ai-dm-board-snapshot.ts:475:32
    473|     throw new Error('Board image escaped its artifact root.');
    474|   }
    475|   const [realRoot, realPath] = await Promise.all([realpath(root), real…
       |                                ^
    476|   const realInside = relative(realRoot, realPath);
    477|   if (realInside === '..' || realInside.startsWith(`..${sep}`) || isAb…
 ❯ runConversationWithConfiguredIntel tools/ai-dm-conversation.ts:4494:9
 ❯ runConversation tools/ai-dm-conversation.ts:7653:12
 ❯ Module.runArena tools/ai-dm-arena.ts:940:24
 ❯ produceCancellationAfterDeliveryRow tests/unit/tools/d569-v5.test.ts:406:16
 ❯ tests/unit/tools/d569-v5.test.ts:438:48

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  no tests
   Start at  21:56:06
   Duration  4.75s (transform 1.88s, setup 0ms, import 0ms, tests 0ms, environment 0ms)


codex
The “ignore regeneration 2” mutant is killed by the retained divergence test: 49 passed, 1 failed. The blind-experiment suite reproduces 46 passed and the same six failures. Replacing only its regeneration helper in memory clears all six; it also exposes a separate whitespace-mutation expectation that Batch 6 will need to update. The v5 suite requires writable temporary files, so I’m rerunning it with temporary I/O held in memory.

exec
/bin/bash -lc "node --input-type=module <<'JS'
import {startVitest} from 'vitest/node';
const shim=String.raw"'`
import {vi} from '"'vitest';
const mem=vi.hoisted(()=>({files:new Map(),dirs:new Set(),links:new Map(),n:0}));
vi.mock('node:fs',async original=>{
 const r=await original(),v=p=>String(p).startsWith('/tmp/cohort-review-memory-');
 const read=(p,o)=>{if(mem.links.has(String(p)))return r.readFileSync(mem.links.get(String(p)),o);const b=mem.files.get(String(p));if("'!b)throw Object.assign(Error('"'ENOENT '+p),{code:'ENOENT'});return typeof o==='string'||o?.encoding?b.toString(typeof o==='string'?o:o.encoding):Buffer.from(b);};
 return {...r,
 mkdtempSync:()=>{const p='/tmp/cohort-review-memory-'+(++mem.n);mem.dirs.add(p);return p;},
 mkdirSync:(p,o)=>v(p)?void mem.dirs.add(String(p)):r.mkdirSync(p,o),
 writeFileSync:(p,d,o)=>v(p)?void mem.files.set(String(p),Buffer.from(d)):r.writeFileSync(p,d,o),
 appendFileSync:(p,d,o)=>v(p)?void mem.files.set(String(p),Buffer.concat([mem.files.get(String(p))??Buffer.alloc(0),Buffer.from(d)])):r.appendFileSync(p,d,o),
 readFileSync:(p,o)=>v(p)?read(p,o):r.readFileSync(p,o),
 existsSync:p=>v(p)?mem.files.has(String(p))||mem.dirs.has(String(p))||mem.links.has(String(p)):r.existsSync(p),
 symlinkSync:(a,b,t)=>v(b)?void mem.links.set(String(b),String(a)):r.symlinkSync(a,b,t),
 rmSync:(p,o)=>v(p)?undefined:r.rmSync(p,o),
 };
});
vi.mock('node:fs/promises',async original=>{
 const r=await original(),s=await import('node:fs'),v=p=>String(p).startsWith('/tmp/cohort-review-memory-');
 const m={...r,realpath:async p=>v(p)?String(p):r.realpath(p),stat:async p=>v(p)?{size:mem.files.get(String(p))?.length??0,isFile:()=>mem.files.has(String(p)),isDirectory:()=>mem.dirs.has(String(p))}:r.stat(p),mkdtemp:async()=>s.mkdtempSync(''),symlink:async(a,b,t)=>s.symlinkSync(a,b,t),
 access:async(p,o)=>{if("'!v(p))return r.access(p,o);if(!s.existsSync(p))throw Object.assign(Error('"'ENOENT '+p),{code:'ENOENT'});}};
 for(const n of ['mkdir','writeFile','appendFile','readFile','rm'])m[n]=async(p,...a)=>v(p)?s[n+'Sync'](p,...a):r[n](p,...a);
 return m;
});
"'`;
const plugin={name:'"'review-memory-temp-files',enforce:'pre',transform(code,id){if(id.endsWith('/tests/unit/tools/d569-v5.test.ts'))return shim+code;}};
const ctx=await startVitest('test',['tests/unit/tools/d569-v5.test.ts'],{config:false,watch:false,cache:false,pool:'threads',maxWorkers:1,reporters:['default']},{plugins:[plugin]});
await ctx.close();
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
exec
/bin/bash -lc "git status --short; nl -ba tests/unit/tools/d569-second-family-manifest.test.ts | sed -n '138,152p'; nl -ba tests/unit/tools/d569-v5.test.ts | sed -n '685,710p'; git diff --numstat 6887755f e92ebc93" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
   138	    expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
   139	    expect(hardRoomMembershipViolations(frozen)).toEqual([]);
   140	  });
   141	
   142	  it.each(BRUTAL_FIXTURE_PATHS)(
   143	    '%s retains historical integrity plus static budget, caster, and terrain invariants',
   144	    (path) => {
   145	      const frozen = room(path);
   146	      expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
   147	      expect(brutalRoomMembershipViolations(frozen)
   148	        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
   149	    },
   150	  );
   151	
   152	  it('second_family_uses_primary_seed: rejects primary or previously used seed namespaces', () => {
   685	    expect(() => pairedDocuments(packetRows))
   686	      .toThrow('turnContextBudget.configuredSemanticBytes is invalid: Invalid input: expected 8192.');
   687	
   688	    const registeredRows = BRUTAL_SOURCE.trimEnd().split('\n')
   689	      .map((line) => JSON.parse(line) as Readonly<Record<string, unknown>>);
   690	    registeredRows[0] = produced;
   691	    expect(() => validateBrutal(raw(registeredRows)))
   692	      .toThrow('Registered context caps mismatch at 1:1.');
   693	  });
   694	
   695	  it('rejects a live-runner timeout row under the historical v5 semantic cap', () => {
   696	    const produced = RUNNER_TIMEOUT_BEFORE_DELIVERY_ROW;
   697	    expect(produced).toMatchObject({
   698	      outcome: 'refused', fallbackReason: 'timeout',
   699	      engineCatalogEvidence: { status: 'inconclusive', reason: 'no_correlated_catalog' },
   700	      turnContextDelivery: { status: 'timeout_before_delivery', measurement: null },
   701	      turnContextConfiguredCaps: { baseBytes: 65_536, semanticBytes: 32_768 },
   702	    });
   703	    expect(RUNNER_TIMEOUT_CONSUMERS.packetRows).toBe(60);
   704	    expect(RUNNER_TIMEOUT_CONSUMERS.historicalRegistrationError)
   705	      .toBe('Registered context caps mismatch at 1:1.');
   706	    expect(RUNNER_TIMEOUT_CONSUMERS.observedViolations).toEqual([]);
   707	    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryPacketError).toContain('integrity evidence');
   708	    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryRegisteredError).toContain('integrity-indeterminate');
   709	    expect(RUNNER_TIMEOUT_CONSUMERS.contradictoryObservedViolations).toContain('integrity_indeterminate');
   710	  });
85	2	tests/unit/tools/d569-second-family-manifest.test.ts
27	8	tests/unit/tools/d569-v5.test.ts
79	11	tools/d569-second-family-manifest.ts

 succeeded in 24525ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

 ✓ tests/unit/tools/d569-v5.test.ts (27 tests) 4293ms

 Test Files  1 passed (1)
      Tests  27 passed (27)
   Start at  21:56:24
   Duration  24.32s (transform 1.83s, setup 0ms, import 19.95s, tests 4.29s, environment 0ms)


exec
/bin/bash -lc "node --input-type=module <<'JS'
import {startVitest} from 'vitest/node';
const shim=String.raw"'`
import {vi} from '"'vitest';
const mem=vi.hoisted(()=>({files:new Map(),dirs:new Set(),links:new Map(),n:0}));
vi.mock('node:fs',async original=>{
 const r=await original(),v=p=>String(p).startsWith('/tmp/cohort-review-memory-');
 const read=(p,o)=>{if(mem.links.has(String(p)))return r.readFileSync(mem.links.get(String(p)),o);const b=mem.files.get(String(p));if("'!b)throw Object.assign(Error('"'ENOENT '+p),{code:'ENOENT'});return typeof o==='string'||o?.encoding?b.toString(typeof o==='string'?o:o.encoding):Buffer.from(b);};
 return {...r,
 mkdtempSync:()=>{const p='/tmp/cohort-review-memory-'+(++mem.n);mem.dirs.add(p);return p;},
 mkdirSync:(p,o)=>v(p)?void mem.dirs.add(String(p)):r.mkdirSync(p,o),
 writeFileSync:(p,d,o)=>v(p)?void mem.files.set(String(p),Buffer.from(d)):r.writeFileSync(p,d,o),
 appendFileSync:(p,d,o)=>v(p)?void mem.files.set(String(p),Buffer.concat([mem.files.get(String(p))??Buffer.alloc(0),Buffer.from(d)])):r.appendFileSync(p,d,o),
 readFileSync:(p,o)=>v(p)?read(p,o):r.readFileSync(p,o),
 existsSync:p=>v(p)?mem.files.has(String(p))||mem.dirs.has(String(p))||mem.links.has(String(p)):r.existsSync(p),
 symlinkSync:(a,b,t)=>v(b)?void mem.links.set(String(b),String(a)):r.symlinkSync(a,b,t),
 rmSync:(p,o)=>v(p)?undefined:r.rmSync(p,o),
 };
});
vi.mock('node:fs/promises',async original=>{
 const r=await original(),s=await import('node:fs'),v=p=>String(p).startsWith('/tmp/cohort-review-memory-');
 const m={...r,realpath:async p=>v(p)?String(p):r.realpath(p),stat:async p=>v(p)?{size:mem.files.get(String(p))?.length??0,isFile:()=>mem.files.has(String(p)),isDirectory:()=>mem.dirs.has(String(p))}:r.stat(p),mkdtemp:async()=>s.mkdtempSync(''),symlink:async(a,b,t)=>s.symlinkSync(a,b,t),
 access:async(p,o)=>{if("'!v(p))return r.access(p,o);if(!s.existsSync(p))throw Object.assign(Error('"'ENOENT '+p),{code:'ENOENT'});}};
 for(const n of ['mkdir','writeFile','appendFile','readFile','rm'])m[n]=async(p,...a)=>v(p)?s[n+'Sync'](p,...a):r[n](p,...a);
 return m;
});
"'`;
const plugin={name:'"'review-memory-temp-files',enforce:'pre',transform(code,id){if(id.endsWith('/tools/d569-v5/validate-first-arm.ts')){const from='semanticCap "'!== cell.semanticContextCapBytes'"';if("'!code.includes(from))throw Error('"'cap mutant anchor absent');return code.replace(from,'(semanticCap "'!== cell.semanticContextCapBytes && semanticCap !== 32_768)'"');}if(id.endsWith('/tests/unit/tools/d569-v5.test.ts'))return shim+code;}};
const ctx=await startVitest('test',['tests/unit/tools/d569-v5.test.ts'],{config:false,watch:false,cache:false,pool:'threads',maxWorkers:1,reporters:['default']},{plugins:[plugin]});
await ctx.close();
JS" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
codex
The v5 suite passes 27/27 with temporary files held in memory. Its rows come from `runArena` using synthetic adapters, then pass through the real registered validator, so the refusal pin exercises enforcement. I found one minor plan omission: the productivity-retirement assertion lacks the required D635/D642/D643 rationale comment.

exec
/bin/bash -lc 'git status --short && git diff --check 6887755f e92ebc93 && sha256sum .tmp-plans/2026-09-17-cohort-01-plan.md' in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69  .tmp-plans/2026-09-17-cohort-01-plan.md

 exited 1 in 24576ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

 ❯ tests/unit/tools/d569-v5.test.ts (27 tests | 2 failed) 4374ms
     ✓ resolves only the historical v5 experiment and v1 second-family manifests 1ms
     ✓ validates a mixed 27-historical/3-current hard grid without rewriting retained bytes 6ms
     ✓ migrates every registered validator invariant for a complete current brutal grid 226ms
     ✓ validates registered hard replacements against retained original cells and reconciliation provenance 257ms
     ✓ accepts a registered host-authorization refusal without treating attribution as a default 230ms
     ✓ rejects a row that actually executed a model default 223ms
     ✓ accepts cancellation after delivered context in registered validation 233ms
     × rejects a live-runner cancellation row under the historical v5 semantic cap 276ms
     × rejects a live-runner timeout row under the historical v5 semantic cap 1ms
     ✓ rejects malformed and wrong-profile catalog evidence after timeout or cancellation in every consumer 1ms
     ✓ rejects incomplete v3 evidence objects 206ms
     ✓ rejects an indeterminate delivery when the companion v3 catalog field is absent 196ms
     ✓ rejects a conflicting_success_and_failure catalog when the companion v3 delivery field is absent 192ms
     ✓ rejects a invalid_catalog_response catalog when the companion v3 delivery field is absent 189ms
     ✓ rejects incomplete v3 evidence nested in a failing dispatch 190ms
     ✓ rejects a missing v3 host diagnostic 190ms
     ✓ rejects partial v3 hybrids and integrity-indeterminate rows before analysis 1ms
     ✓ rejects registered seed mutations 198ms
     ✓ rejects registered fixture-state mutations 198ms
     ✓ rejects registered code-cohort mutations 196ms
     ✓ rejects registered model provenance mutations 193ms
     ✓ rejects registered effort provenance mutations 197ms
     ✓ rejects registered KB hash provenance mutations 200ms
     ✓ rejects registered KB components provenance mutations 199ms
     ✓ rejects registered visual pin provenance mutations 202ms
     ✓ rejects registered manifest mutations before accepting rows 98ms
     ✓ runs raw outcomes through packet/key construction and the registered paired scorer 71ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a live-runner cancellation row under the historical v5 semantic cap
AssertionError: expected [Function] to throw error including 'Registered context caps mismatch at 1…' but got '[\n  {\n    "code": "context_cap",\n …'

- Expected
+ Received

- Registered context caps mismatch at 1:1.
+ [
+   {
+     "code": "context_cap",
+     "message": "row primary:gpt-5.6-luna-blind:brutal:6203001:1 changed context caps"
+   }
+ ]

 ❯ tests/unit/tools/d569-v5.test.ts:716:8
    714|     expect(RUNNER_TIMEOUT_CONSUMERS.invalidCatalogResults.map(({ exit,…
    715|       .toEqual([
    716|         'timed_out:malformed', 'timed_out:wrong_profile',
       |        ^
    717|         'cancelled:malformed', 'cancelled:wrong_profile',
    718|       ]);

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/unit/tools/d569-v5.test.ts > D569 v5 replacement validation and paired analysis tools > rejects a live-runner timeout row under the historical v5 semantic cap
AssertionError: expected '[\n  {\n    "code": "context_cap",\n …' to be 'Registered context caps mismatch at 1…' // Object.is equality

- Expected
+ Received

- Registered context caps mismatch at 1:1.
+ [
+   {
+     "code": "context_cap",
+     "message": "row primary:gpt-5.6-luna-blind:brutal:6203001:1 changed context caps"
+   }
+ ]

 ❯ tests/unit/tools/d569-v5.test.ts:729:8
    727|     expect(() => validateBrutal(mutateFirst(BRUTAL_SOURCE, (row) => {
    728|       const catalog = record(row['engineCatalogEvidence']);
    729|       return { ...row, engineCatalogEvidence: { status: catalog['statu…
       |        ^
    730|     }))).toThrow();
    731|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


 Test Files  1 failed (1)
      Tests  2 failed | 25 passed (27)
   Start at  21:56:57
   Duration  24.37s (transform 1.82s, setup 0ms, import 19.91s, tests 4.37s, environment 0ms)


codex
**B1-F1 — P3 — Missing retirement rationale.** [d569-second-family-manifest.test.ts:147](/home/vagrant/PhpstormProjects/dnd-wt-seam-verify/tests/unit/tools/d569-second-family-manifest.test.ts:147) filters `brutal_productivity` without the plan-required D635/D642/D643 comment. The retirement itself is authorized. **Fix:** explain that 6207 fixtures retain historical integrity/static checks while 6211 will own live productivity.

No P1/P2 findings.

Verified:

- Frozen plan: **403 lines**, SHA-256 `4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69`.
- Diff: **3 allowed files, 191 insertions / 21 deletions**.
- Real generator, in memory: **20/20 fixtures × both generations** differ only at `$.encounter.state.foggedCells`; **20/20 raw hashes match**, v1 validation yields **0 violations**.
- Projection deletes exactly the committed own key. Generated bytes remain untouched. Rebound `round` and unrelated root changes retain both regeneration failures; missing historical keys and v2 legacy keys reject.
- Only frozen brutal productivity assertions are retired. Integrity/reducer loading, static budget/caster/terrain, hashes, ledger, namespaces, and both regenerations remain enforced. The discriminated union and nested objects are strict; version-specific namespace/path rules remain Batch 6 work.
- N2 is a real enforcement witness: `runArena` produces rows using synthetic adapters, and the registered validator rejects their **32,768** cap against v5’s **8,192** registration. No production cap changed. The v6 seam is correctly deferred outside Batch 1’s allowlist.

Commands/results:

| Command/check | Result |
|---|---|
| `npx tsc -p tsconfig.app.json --noEmit` | exit **0** |
| `npx tsc -p tsconfig.node.json --noEmit` | exit **0** |
| `git diff --check 6887755f e92ebc93` | exit **0** |
| Expanded `git diff --exit-code 6887755f e92ebc93 -- …` freeze check, including hard-2 | exit **0** |
| `git status --short` | empty |
| Python scan of added lines from `git diff --unified=0 6887755f e92ebc93` | **191 added; 0 over 120 columns** |

Targeted suites ran through `node --input-type=module` and `startVitest`, with `config:false`, `cache:false`, `pool:'threads'`, and `maxWorkers:1`. Exact stdin commands are recorded above. Temporary v5 I/O used an in-memory filesystem overlay because `/tmp` is read-only; assertions and validation logic were retained.

- Second-family: **50/50 passed**.
- V5 with memory I/O: **27/27 passed**.
- Ignore-second-generation mutant: **49 passed / 1 failed**, specifically the divergence witness.
- Registered cap-guard mutant allowing 32,768 through that guard: **25 passed / 2 failed**, both live-row refusal pins.
- Blind-experiment: **46 passed / 6 failed**, exactly the reported helper-fed-committed-byte artefact: **20 + 20 regeneration violations**. Substituting canonical regeneration only in memory clears all six. That probe yields **51/52**, exposing the separate whitespace-only expectation at `d569-blind-experiment.test.ts:727` for Batch 6 follow-up.

VERDICT: ACCEPT

REVIEW DONE
tokens used
94,072
**B1-F1 — P3 — Missing retirement rationale.** [d569-second-family-manifest.test.ts:147](/home/vagrant/PhpstormProjects/dnd-wt-seam-verify/tests/unit/tools/d569-second-family-manifest.test.ts:147) filters `brutal_productivity` without the plan-required D635/D642/D643 comment. The retirement itself is authorized. **Fix:** explain that 6207 fixtures retain historical integrity/static checks while 6211 will own live productivity.

No P1/P2 findings.

Verified:

- Frozen plan: **403 lines**, SHA-256 `4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69`.
- Diff: **3 allowed files, 191 insertions / 21 deletions**.
- Real generator, in memory: **20/20 fixtures × both generations** differ only at `$.encounter.state.foggedCells`; **20/20 raw hashes match**, v1 validation yields **0 violations**.
- Projection deletes exactly the committed own key. Generated bytes remain untouched. Rebound `round` and unrelated root changes retain both regeneration failures; missing historical keys and v2 legacy keys reject.
- Only frozen brutal productivity assertions are retired. Integrity/reducer loading, static budget/caster/terrain, hashes, ledger, namespaces, and both regenerations remain enforced. The discriminated union and nested objects are strict; version-specific namespace/path rules remain Batch 6 work.
- N2 is a real enforcement witness: `runArena` produces rows using synthetic adapters, and the registered validator rejects their **32,768** cap against v5’s **8,192** registration. No production cap changed. The v6 seam is correctly deferred outside Batch 1’s allowlist.

Commands/results:

| Command/check | Result |
|---|---|
| `npx tsc -p tsconfig.app.json --noEmit` | exit **0** |
| `npx tsc -p tsconfig.node.json --noEmit` | exit **0** |
| `git diff --check 6887755f e92ebc93` | exit **0** |
| Expanded `git diff --exit-code 6887755f e92ebc93 -- …` freeze check, including hard-2 | exit **0** |
| `git status --short` | empty |
| Python scan of added lines from `git diff --unified=0 6887755f e92ebc93` | **191 added; 0 over 120 columns** |

Targeted suites ran through `node --input-type=module` and `startVitest`, with `config:false`, `cache:false`, `pool:'threads'`, and `maxWorkers:1`. Exact stdin commands are recorded above. Temporary v5 I/O used an in-memory filesystem overlay because `/tmp` is read-only; assertions and validation logic were retained.

- Second-family: **50/50 passed**.
- V5 with memory I/O: **27/27 passed**.
- Ignore-second-generation mutant: **49 passed / 1 failed**, specifically the divergence witness.
- Registered cap-guard mutant allowing 32,768 through that guard: **25 passed / 2 failed**, both live-row refusal pins.
- Blind-experiment: **46 passed / 6 failed**, exactly the reported helper-fed-committed-byte artefact: **20 + 20 regeneration violations**. Substituting canonical regeneration only in memory clears all six. That probe yields **51/52**, exposing the separate whitespace-only expectation at `d569-blind-experiment.test.ts:727` for Batch 6 follow-up.

VERDICT: ACCEPT

REVIEW DONE
