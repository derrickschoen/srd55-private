
OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR] (network access enabled)
reasoning effort: high
reasoning summaries: none
session id: 01a0b245-5a53-7990-9cf5-5abf61f029d7
--------
user
COMMON RULES (binding, restated in full): You are the IMPLEMENTER (gpt-5.6-sol, high, FRESH session) for VIS-FIELD-01 batch group COHORT-01, Batch 2 ONLY. Worktree /home/vagrant/PhpstormProjects/dnd-wt-cohort-01 (branch claude/cohort-01 at e92ebc93 = WALL-01 candidate 6887755f + Batch 1 (ACCEPTED by astra); clean; node_modules symlinked). Sandbox workspace-write in that worktree ONLY (+ /tmp). No git write commands (the supervisor commits). No edits under .claude/** or docs/**. Do NOT invoke claude, `claude -p`, any reviewer session, any consensus workflow or any other agent; skills under ~/.codex do NOT apply. No Playwright, no full vitest, no npm run build; never port 4173. Model calls WILL fail. NEVER regenerate an expectation from our own output; never delete or weaken a test (retirements only where the plan names them, with the D635/D642/D643 rationale comment); never fresh hashes without a printed derivation; NO byte of tests/fixtures/d569-second-family-manifest.json, tests/fixtures/d569-blind-experiment-manifest.json, tests/fixtures/arena-basis-brutal*, -brutal-b, -brutal-2, -hard-2 may change (V-freeze before and after). Report exact commands and numbers; end with the named marker. The MAIN repo's .claude/decisions.md (D643, D648–D653) and .claude/consensus/cohort-01/*.md are readable.

THE PLAN (binding, FROZEN): .tmp-plans/2026-09-17-cohort-01-plan.md in your worktree — sha256 4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69, 403 lines incl. binding amendments N1 and N2 (verify with sha256sum first; STOP if it differs). Read it fully. Implement EXACTLY "### Batch 2 — RED active-cohort membership witnesses and frozen-contract retirement": its Allowed files, its RED-first witnesses for the v2 families (write them FIRST and show them red — the v2 directories do not exist yet, so the witnesses must fail for the right reason: missing cohort, not a syntax error), the retirement of the frozen families' PRODUCTIVITY assertions ONLY (byte pins, integrity, static checks, hashes and round-execution tests stay) with the plan's rationale comment, the productivity predicate PRESERVED per N2's sibling ruling PLAN-F6 (positive movement stays productive; the sight/cover checks apply only to targeted offensive components; the Dash-only witness and the movement-arm mutant), the mutants the plan lists, and its T/V commands with expected numbers. ALSO (astra B1-F1, P3, one line): at tests/unit/tools/d569-second-family-manifest.test.ts:~147 where brutal_productivity is filtered for the frozen 6207 family, add the comment that 6207 keeps historical integrity/static checks while 6211 (v2) owns live productivity, citing D635/D642/D643. Do NOT generate any cohort (Batches 3–5 are gated on WALL-01's review) and do NOT start Batch 6. If the plan's Batch 2 text is ambiguous or infeasible at e92ebc93, STOP and report BLOCKED with the exact gap.

VERIFICATION: the batch's T and V exactly as the plan states; npx tsc -p tsconfig.app.json --noEmit; npx tsc -p tsconfig.node.json --noEmit; git diff --check; no line > 120; git status --short = the Allowed files (+ the one-line B1-F1 comment file); V-freeze before/after; list every remaining red by title with its one-line cause (expected: the v2 witnesses red for "cohort missing", the six blind-experiment helper reds, brutal-b/brutal/behaviour reds only where the plan says they persist until later batches). OUTPUT: report, then "COHORT-01 B2 DONE" (or BLOCKED with the gap).

codex
I’ll first verify the frozen plan hash. If it matches, I’ll read the full Batch 2 contract and capture the required pre-change freeze before touching any file.
exec
/bin/bash -lc 'sha256sum .tmp-plans/2026-09-17-cohort-01-plan.md && wc -l .tmp-plans/2026-09-17-cohort-01-plan.md' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69  .tmp-plans/2026-09-17-cohort-01-plan.md
403 .tmp-plans/2026-09-17-cohort-01-plan.md

codex
The plan is exactly the required SHA-256 and 403 lines. I’m reading it end-to-end now, then I’ll run its stated V-freeze baseline unchanged.
exec
/bin/bash -lc "sed -n '1,403p' .tmp-plans/2026-09-17-cohort-01-plan.md" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
# VIS-FIELD-01 / COHORT-01 implementation plan r3 (D643 option A1)

Date: 2026-09-17. Baseline: detached clean `807d2e3c8cbbda11cfa26d078ac7739f2954e27e`.
D643 A1 keeps historical hard-2 and adds only three solid-wall brutal v2 families.

## Outcome and hard gates

1. WALL-01 is a hard predecessor. Generation cannot start until its D642 corner seal is
   accepted on a clean checkout. Recheck `src/vtt/room-generator.ts:1227-1231` still defines
   `generatedLineIsOpen` as `tier === 'none' && !blocksSight`. Re-derive all three Batch-9
   expectations against the accepted WALL-01 tree before editing tests; if any stated result
   differs, stop and amend/review this plan rather than copying changed-code output.
2. Before edits, inventory SHA-256 for the v1/v5 manifests and every file under historical
   6203, 6206, and 6207 directories. Repeat after every batch. No byte may change in:
   `tests/fixtures/d569-second-family-manifest.json`,
   `tests/fixtures/d569-blind-experiment-manifest.json`,
   `tests/fixtures/arena-basis-brutal`, `arena-basis-brutal-b`, or `arena-basis-brutal-2`.
3. Never rewrite a historical fixture/manifest, hand-edit generated JSON, add a D466 override,
   use `--terrain-profile`, or relax membership. One failing room rejects all ten seeds; no
   nine-room cohort and no hash is retained.
4. Expectations may come only from frozen pre-change bytes, the hand calculations named here,
   or hashes made after membership and double-generation gates. Generated changed-code output
   is never an oracle for a behavioural expectation.
5. No full Vitest, Playwright, build, or port 4173 in a batch. Later landing gates are separate.
6. Fixed scope is 10 batches with Allowed-file counts `3/3/10/10/10/8/4/4/3/1`: 52 unique
   paths = 18 existing + 34 new. A batch may touch only its manifest and accepted earlier files.

Common checks after every batch:

- **T:** `npx tsc -p tsconfig.app.json --noEmit` and
  `npx tsc -p tsconfig.node.json --noEmit` -> exit 0/0.
- **V:** `git diff --check` -> empty; `git status --short` contains only allowed/earlier paths.
- **V-freeze:** `git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2` -> exit 0.

## D569 demonstrate-then-normalize ledger

1. **Historical regeneration pins:** demonstrate each of 20 committed second-family fixtures
   differs from each canonical rebuild at exactly `$.encounter.state.foggedCells`; preserve raw
   hashes, remove exactly that own key from parsed COMMITTED state, then compare both rebuilds.
2. **New fixture pins:** dated namespace proof -> decoded structural/productivity membership ->
   two independent temp generations with empty `diff -rq` -> `sha256sum` = OpenSSL -> pin.
3. **Second-family v2 pin:** byte-copy hard-2 rows/hashes from v1, add proven 6211 rows, finalize,
   hash v2 independently, then place that hash in v6.
4. **Experiment v6 pin:** byte-carry all v5 fields except version, primary brutal, and nested
   second-family path/hash; pin the final outer v6 manifest hash separately from its contents.
5. **Behaviour pins:** use only Batch 9's explicit ray/path/table calculations, rechecked after
   WALL-01. Preserve stale values only as history comments.
6. **Cap pin:** D643 fixes 32,768; measure the hand-authored oversized block, never derive a cap.

## Pre-Batch-2 namespace reservation gate

Run this after accepted WALL-01 and before adding any range to source/tests. The first command
uses the authoritative MAIN file, not the stale 22,707-line worktree copy; the second audits
tracked consumers. Copy commands, stdout, and statuses verbatim into the dated v2 ledger entry.

```text
rg -n -o -e '6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
rg -n -o -e '6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' tests/fixtures tools
rg -n -o -e '\b6209\b|\b6210\b|\b6211\b' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
```

Verified 2026-09-17 output is, verbatim: command 1 -> `23048:6209xxx`,
`23048:6210xxx`, `23048:6211xxx`, `23061:6209xxx`, `23061:6210xxx`,
`23061:6211xxx`, `23071:6209xxx`, `23071:6_209_001`, status 0; command 2 ->
empty stdout/status 1; command 3 -> `23068:6209`, `23068:6210`, `23068:6211`,
status 0. Lines 23048/23061 are costing/authorized reservations, 23068 is D645's
shorthand plan summary, and 23071 is D646's review example: none is prior seed use.
Record one reservation covering all three families before any rejection/reselection.
Any new non-reservation match is a hard stop. If a range later fails, retain it as rejected,
audit a fresh contiguous range in all three representations, and have the supervisor amend this
plan's exact Batch-2 seed table, one Batch-3/4/5 Allowed-file manifest, and all downstream
manifest/route constants before creating replacement bytes. Unamended replacement paths are
unauthorized; the other two initial reservations remain reserved.

## Ordered batches

### Batch 1 — validator normalization and historical semantics

**Allowed files (3):**

- `tools/d569-second-family-manifest.ts`
- `tests/unit/tools/d569-second-family-manifest.test.ts`
- `tests/unit/tools/d569-v5.test.ts`

**R:** add a per-fixture 20-row structural-diff table (one legacy path each, hence today's 40
regeneration violations). Add rebound-hash witnesses proving (a) any second state-field/value
difference still yields both `first_regeneration` and `independent_regeneration`, and (b) a
NEW v2 brutal fixture containing `foggedCells` is rejected even if its hash is rebound. Retain
the two-generation divergence witness. These are hand-authored mutations, not regenerated pins.

**Implementation:** make the schema a strict v1/v2 union. Hash raw bytes first. For v1 hard and
brutal, and v2's carried hard-2 only, parse COMMITTED bytes, require the legacy key, delete only
own `encounter.state.foggedCells`, canonicalize with one newline, and compare each current
canonical rebuild to that projection. Never project generated bytes. V2 brutal-2-v2 compares
raw committed/current canonical bytes and rejects the legacy key. V1 retains integrity plus
static budget/caster/terrain checks but no longer recertifies frozen brutal productivity; v2
retains full current membership/productivity. `d569-v5.test.ts` imports/asserts only the v5/v1
paths and version, uses `canonicalD569SecondFamilyRegeneration`, and becomes green because this
normalization makes v1 valid; it must never resolve v6. `seed - 6203000` may remain only there.

**M:** keep the key, drop all state, normalize generated bytes, allow a new-v2 legacy key, and
ignore regeneration 2; the 40-count, extra-diff, new-v2, and divergence witnesses kill each.

**T/V:** common checks; serially run the two listed suites -> 20/20 one-key demonstrations,
zero v1 violations, all negative witnesses green.

### Batch 2 — RED active membership and frozen-productivity retirement

**Allowed files (3):**

- `tests/unit/vtt/arena-basis-brutal-v2.test.ts` (new)
- `tests/unit/vtt/room-generator.test.ts`
- `tests/unit/vtt/arena-basis-brutal-b.test.ts`

**R:** after the reservation gate, declare exact 6209001-10, 6210001-10, 6211001-10 paths;
expect exactly 30 missing fixtures. Add decoded clones for a nonproductive living monster, a
positive-movement-only Dash monster that MUST pass, and a targeted offensive option whose
resolved origin has a blocked or total trace and MUST fail.

**Implementation:** mirror `arena-basis-brutal-b.test.ts` but order proof before hashes:
identity/reducer load; brutal budget and 90-100% spend; implemented caster; terrain density/count;
then preserve the existing productivity predicate exactly: each living monster has some valid
option with `movementCostFeet > 0` OR an attack/save/cast/world-object slot. A Dash-only monster
therefore remains productive. If the qualifying option has targeted attack/save/cast components,
each target trace from `resolution.mechanics.finalPosition` must be sight-clear and non-total;
movement-only/world-object qualification does not invent a LOS target. Cite the contract and
`room-generator.ts:1227`'s stricter generated-room predicate. Mandatory offense is out of scope.

Move precisely these active assertions: retire `room-generator.test.ts:588-601` productivity for
frozen 6203001-10 while retaining its 6203001-3 byte/digest pins; add equivalent generation and
productivity assertions for 6209. In `arena-basis-brutal-b.test.ts:292-397`, retain structural,
reducer, SHA, and round-execution pins but retire only 378-393's 6206 productivity assertion.
Batch 1 retires only the analogous 6207 productivity assertion. Each comment cites D635/D642/
D643: frozen bytes are history; 6209/6210/6211 own the live contract. No skip/todo.

**M/T/V:** force productivity true, delete the movement arm, and treat `blocksSight:true` as open;
the nonproductive, Dash-only, and blocked-target clones kill each mutant. Common checks; active
suite is RED only for 30 missing files; frozen suites have no productivity red.

### Batch 3 — generate primary brutal-v2 6209001-6209010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-v2/seed-6209001.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209002.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209003.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209004.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209005.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209006.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209007.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209008.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209009.json`
- `tests/fixtures/arena-basis-brutal-v2/seed-6209010.json`

**R/M/Generation/V:** Batch-2 tests are RED first. Twice run
`npx vite-node tools/generate-arena-basis.ts -- --difficulty brutal --seed 6209001 --rooms 10 --out <fresh-temp>/arena-basis-brutal-v2`.
Assert no parsed `terrainProfile` and `d466GeneratedRoomOverride(seed) === null` for 10/10.
Require empty `diff -rq A B`, then decoded membership 10/10 and both mutants. Only then move A
unchanged to Allowed files and rerun membership/common checks. Do not compute hashes yet.

### Batch 4 — generate brutal-b-v2 6210001-6210010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json`
- `tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json`

**R/M/T/V:** identical to Batch 3 with seed 6210001/directory `arena-basis-brutal-b-v2`:
two temps, empty diff, no profile/override, decoded membership 10/10, mutants, common checks, no hashes.

### Batch 5 — generate D569 brutal-2-v2 6211001-6211010

**Allowed files (10):**

- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json`
- `tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json`

**R/M/T/V:** identical with seed 6211001/directory `arena-basis-brutal-2-v2`. End with one
membership-only run proving exactly 30/30 fixtures and every living monster; any failure rejects
that full ten-seed namespace and invokes the pre-Batch-2 replacement procedure.

### Batch 6 — v2/v6 manifests, identity boundary, and hashes

**Allowed files (8):**

- `tools/d569-second-family-manifest.ts`
- `tools/d569-blind-experiment.ts`
- `tests/fixtures/d569-second-family-manifest-v2.json` (new)
- `tests/fixtures/d569-blind-experiment-manifest-v6.json` (new)
- `tests/unit/tools/d569-second-family-manifest.test.ts`
- `tests/unit/tools/d569-blind-experiment.test.ts`
- `tests/unit/tools/d569-v6.test.ts` (new)
- `tests/unit/vtt/arena-basis-brutal-v2.test.ts`

**R:** strict version/path/range/ledger tests, the exact v5-v6 structural diff, outer-v6 hash,
and cardinalities. Add primary-hard and second-family-hard cross-version witnesses: a v5 observed
row with the same unchanged seed/state/visual hashes must be rejected by v6 as wrong experiment
identity. A missing/wrong identity must also reject. V6 ordinals use manifest order/seed maps;
no `seed - 6203000` assumption survives in a v6 path.

**Implementation:** v2 is `d569-second-family-manifest-v2`: byte-carried hard-2 rows/hashes,
proven brutal-2-v2 rows/hashes, the dated verbatim audit/classification, and this exact ordered
ten-namespace union: 5117xxx, 5118xxx, 6203xxx, 6204xxx, 6206xxx, 6207xxx, 6208xxx,
6209xxx, 6210xxx, 6211xxx. Keep v1's five-entry historical constant/policy unchanged; add a v2
constant. V2 overlap validation exempts only the expected fixture in its own exact cohort
namespace (hard 5118xxx; brutal 6211xxx); cross-cohort and all other overlaps still fail. Removing
ANY of ten namespaces fails an exact-ledger test; preserve the precedent mutant that removes
6204xxx. V6 is
`d569-blind-experiment-v6`: unchanged hard 5117, primary brutal 6209, nested v2 path/hash, every
other v5 field byte-carried. V1/v5 constants and files remain historical; add v2/v6 constants.

Create a separate `D569ExperimentIdentity = {version, manifestSha256}` from the exact outer
manifest bytes at the load boundary. Pin/export the final v6 SHA-256 outside the manifest.
V6 dry-run cells carry both fields; include both in `sessionKey`. V6 observed rows must carry an
exact match, and row lookup/duplicate/missing keys include identity. Branch by version so v5's
cell shape, legacy session-key derivation, and observed-row matching remain byte-behaviourally
unchanged. Content/state/visual hashes remain truthful content hashes, never overloaded as the
experiment identity. Thus unchanged hard cells cannot cross v5/v6.

The active `d569-v6.test.ts` access MUST set `regenerate:
canonicalD569SecondFamilyRegeneration`; the fixture-reader helper at
`d569-blind-experiment.test.ts:294-296` may serve historical/synthetic unit tests only. Add a spy
or deliberately divergent canonical regeneration proving v6 actually calls it. Only after all
30 membership checks pass, hash each fixture with SHA/OpenSSL, finalize/hash v2, insert its hash
in v6, finalize/hash v6, and pin that outer hash in code/test.

**M:** omit experiment identity from session/row keys; substitute a v5 hard row; use committed
bytes as v6 regeneration; remove 6204 or any ledger row; over-broaden the 5118/6211 exception;
admit v1/6203, a 6207/6209 overlap, a fourth carry-forward difference, or generation 2 divergence.
Named witnesses fail each mutant.

**T/V:** common checks; both hash tools agree for 30 fixtures, v2, and outer v6. Targeted D569
suites have zero violations and exact core/with-hint counts 480/660, primary-brutal 240/330,
second-family core 480. Historical v1/v5 suites remain green and never execute v6.

### Batch 7 — versioned arena and rerun routes

**Allowed files (4):**

- `tools/ai-dm-arena.ts`
- `tests/unit/tools/ai-dm-arena.test.ts`
- `tools/ai-dm-rerun-packet.ts`
- `tests/unit/tools/ai-dm-rerun-packet-protocol.test.ts`

**R/Implementation:** first fail exact parser/path/wrong-family cases. Add arena basis labels
`brutal-v2` -> 6209 and `brutal-b-v2` -> 6210, both difficulty brutal. At rerun definitions near
lines 26/547 add closed protocols `brutal-10-v2` and `brutal-10-b-v2`, exact ten-seed arrays and
directories. Historical labels/protocols retain their old paths and behaviour.

**M/T/V:** map new labels to old paths, swap new ranges, and remove an old label; exact new and
historical route tests kill each. Common checks; arena has prior 6 + 2 labels, rerun prior 3 + 2.

### Batch 8 — LOS audit and blind fixture report

**Allowed files (4):**

- `tools/los-cover-era-audit.ts`
- `tests/unit/tools/los-cover-era-audit.test.ts`
- `tools/blind-context-fixture-report.ts`
- `tests/unit/vtt/blind-turn-context.test.ts`

**R/Implementation:** first expect 30 missing rows. Add file-backed `brutal-v2`,
`brutal-b-v2`, `brutal-2-v2`; preserve historical entries. LOS audit is 93 rooms (63+30), ten
per new family. Blind report is 63 cases (33+30), decoded from exact committed paths.

**M/T/V:** omit or alias a family; family/seed/path/hash/count assertions fail. Common checks;
focused outputs are 93 and 63, with 10/10/10 new rows and 63/63 schema-valid cap-fit cases.

### Batch 9 — hand-authored behaviour pins

**Allowed files (3):**

- `tests/unit/vtt/composite-turn-proposals.test.ts`
- `tests/unit/vtt/engine-opportunity-movement-intel.test.ts`
- `tests/unit/vtt/speculative-planning.test.ts`

**R:** preserve the three current deterministic RED titles before edits. Re-run the following
geometry calculations after WALL-01; a discrepancy stops the batch, never selects a new oracle.

**Implementation:** composite hard 5117005 labels are exactly `Dodge`, `End Turn`, with no
`Longbow`. Comment the four corner-ray derivation: Scout `(13,3)` -> Fighter `(1,2)` has two rays
enter `(7,2)` and two on shared edge `(7,2)/(7,3)`; -> Cleric `(2,4)` has two on
`(7,3)/(7,4)` and two entering `(7,4)`; -> Wizard `(1,6)` all cross column 7. D635/D642 block
every set. Room 8 pins monsters 1 `(22,1)`, 2 `(21,1)`, 4 `(21,3)` to zero-foot Dodge at current
cells; monster 5 keeps 55-ft Dash `(22,5)->(12,6)` = ten 5-ft steps + 5 difficult. Replace the
stale comment; these are hand calculations.

For real integration, replace frozen 5117001 with a 15x8 state: Scout A at `(13,4)`, actionless
monster B at `(13,6)`, unacted 30-ft player P at `(1,4)`, ids `combatant:A/B/P`, base walls
`(7,0..3)` plus `(8,5..7)`, and a typed speed-set effect with
`enginePlanningSpeedFeet(A) === 0`. Define exact extracted selector strings
`DA=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:A'}})`
and `TAP=canonicalJson({actorId:'combatant:A',selector:{kind:'combatant',combatantId:'combatant:P'}})`.
Production `buildHostScenarioMenu` yields labels `[Longbow + Longbow -> combatant:P, Dodge]` and
exact menu rank 1 `adjacency:${DA}:${TAP}` (P moves to `(12,4)`, 55 ft) then rank 2
`visibility:${DA}:${TAP}` (P path `(1,4)->(0,3)->(0,2)->(0,1)->(1,0)`, 20 ft); each has one
proposal reference, only influencing player P, radius 60, and score `1*60=60`. Longbow reach
cannot flip inside 15x8; Dodge contributes no movement-change fact.

The mutation adds `(8,4)`, completing `(7,3)/(8,4)`. Assert the production trace changes
sight-clear -> blocked. Because A's speed is explicitly zero it cannot use Astra's 25-ft escape
to `(8,3)`; A and B now propose `[Dodge,Dodge]`, targeted dependencies disappear, and the exact
menu is `[]`. This is a contrast, not normalization of the frozen empty scene. Recheck the full
base/mutated oracle after WALL-01; any difference stops for plan review.

Separately test the eight-entry slice through `computeHostSplitCandidates` with two Scouts A/B,
P at `(4,2)`, awaiting A's turn, speed 30/radius 60, and a controlled query table. Define
`S(x)=canonicalJson({actorId:'combatant:'+x,selector:{kind:'combatant',combatantId:'combatant:'+x}})`.
All adjacency/reach baselines are false -> true; all visibility baselines are true -> false.
The nine non-shadowed candidates and hand oracle are:

|rank|fact key|refs|legal P destination/path ft|score|result|
|---:|---|---:|---|---:|---|
|1|`adjacency:S(A):S(P)`|A+B|`(2,1)`/10|`2*60=120`|kept|
|2|`adjacency:S(B):S(P)`|B|`(7,3)`/20|60|kept|
|3|`adjacency:S(P):S(A)`|A|`(2,1)`/10|60|kept|
|4|`adjacency:S(P):S(B)`|B|`(7,3)`/20|60|kept|
|5|`reach:S(A):shortsword:S(P)`|A|`(2,1)`/10|60|kept|
|6|`reach:S(B):shortsword:S(P)`|B|`(7,3)`/20|60|kept|
|7|`visibility:S(A):S(P)`|A|`(6,1)`/15|60|kept|
|8|`visibility:S(B):S(P)`|B|`(6,3)`/15|60|kept|
|9|`visibility:S(P):S(A)`|A|`(6,1)`/15|60|excluded|

All costs are independently <=60; the controlled port rejects every unlisted destination.
Order is score, refs, then canonical fact-key bytes (`adjacency` < `reach` < `visibility`). Assert
exact keys/ranks 1-8 and excluded key 9, not merely `<=8`. This stipulated dependency/query table
tests only grouping, score, canonical tie ordering, filtering, and the eight-entry slice; it is
not evidence for D642 corner sealing, which the production integration witness above proves.

**M/T/V:** restore old Longbow/Dash values, let A move despite speed zero, disable corner sealing,
change score multiplication to 62/61, reverse the tie order, or remove `.slice(0,8)`; exact
labels/keys/paths, production mutation, and nine-row table fail. Common checks; run serially.

### Batch 10 — 32,768-byte cap rejection witness

**Allowed files (1):**

- `tests/unit/vtt/blind-turn-context.test.ts`

**R/Implementation:** clone a normal `projectEngineSemanticBoard`, set one schema-valid creature
name to 40,000 characters, prove schema parsing succeeds, then render normally. Assert measured
UTF-8 semantic block exceeds `BLIND_SEMANTIC_BOARD_MAX_BYTES === 32_768` and exact `RangeError`
states required/max byte counts. Baseline may be green because this witnesses existing policy.

**M/T/V:** disable `blind-turn-context.ts:1304-1307` (or make `>` false): the test must turn RED
because oversized context is accepted. Restore, verify source hash, run focused test/common checks.

## Final targeted verification and handoff

Run only touched suites serially: second-family, blind-experiment, v5, v6, room-generator,
brutal-v2, brutal-b, arena, rerun protocol, LOS audit, blind context, and the three behaviour
files. Expected: zero failures; D569 counts 480/660/240/330 and second-family 480; 30/30 new
fixtures; audits 93/63. Repeat T, freeze inventory, `git diff --check`, verify exact 52-path union,
and inspect all new JSON as parsed state. Supervisor later runs the untruncated full gate.

## Open questions

None. Any failed post-WALL derivation or rejected namespace invokes the stated stop/amend/review
procedure; it is not an implementation-time choice.

## Round 2 disposition (closed/superseded)

F1 remains closed at lines 211-250; F4 remains closed at lines 25-26 and 198-209.
F2's r2 work remains at 50-73 and F3 is superseded by the corrected r3 oracle at 304-348.
Supporting requirements remain at 8-12, 85-102, 236-239, and 293-348.

## Round 3 disposition

- **PLAN-F3 (P2; lines 304-348):** the production scene now makes A speed zero, uses extracted actor-relative
  selector keys, and pins both base candidates plus the exact empty post-seal menu; the verified
  nine-row table is explicitly ranking/filtering-only.
- **PLAN-F2 (P2; lines 50-73):** both absence commands now cover every 000-999 plain and underscore seed plus
  `xxx`; MAIN provenance, verbatim results, reservation classification, and replacement rules stay.
- **PLAN-F5 (P2; lines 217-246):** v2 now requires all ten historical/current namespaces, exact own-cohort
  exceptions, removal-of-any-row tests, and the 6204 mutant.
- **PLAN-F6 (P2; lines 115-138):** Batch 2 now preserves positive movement OR productive slots, keeps Dash-only
  monsters valid, and applies resolved-origin trace checks only to targeted offensive components.

COHORT-01 PLAN R3 DONE

## Binding amendment N1 (astra r3, D650 — supervisor-appended; part of the frozen plan)

Batch 9 must construct the hand-authored Scout A with `profile.rules.speed: feet(0)` before encounter creation, retain the typed zero-speed effect, and assert both declared and effective speed are zero (attack declarations take their movement allowance from `subject.profile.rules.speed`, offer-declarations.ts:431–465, so the effect alone still lets A move 25 ft to (8,3) and keep its Longbow). Preserve the exact base and mutated labels, selector keys, scores and menus at the lines the plan states (base: Longbow ×2 + Dodge, adjacency and visibility keys, scores 60/60; with (8,4) added: Dodge/Dodge, menu []). Require two mutants: restoring A's declared speed to 30 while retaining the effect, and separately disabling corner sealing, must each fail the mutation expectations. Re-verify against the ACCEPTED WALL-01 before implementation acceptance. This correction is confined to the allowed speculative-planning test file; no production movement change is authorized.

## Binding amendment N2 (supervisor ruling D651 — part of the frozen plan)

Batch 1 exposed a second historical discontinuity beside the fog key: the v5 manifest registers `caps.semanticBytes = 8192` (the engine-server `SEMANTIC_BOARD_MAX_BYTES`), while rows the live blind runner produces today carry the D643 cap `BLIND_SEMANTIC_BOARD_MAX_BYTES = 32,768`, so `validate-first-arm.ts:369` refuses them ("Registered context caps mismatch"). Ruling: (1) a cell's `semanticContextCapBytes` / `baseContextCapBytes` are the MANIFEST's declared caps (the registration), not an engine constant — `tools/d569-blind-experiment.ts:285` (schema literal) and `:712` (cell cap) become version-aware: v5 keeps 8192 as history, v6 declares `caps.semanticBytes: 32768`; (2) `tests/unit/tools/d569-v5.test.ts` stays HISTORICAL: it keeps validating the v5 manifest (fog-key normalization included), its hashes and its synthesized rows, and it PINS that a row produced by today's live runner is refused with exactly `Registered context caps mismatch` — v5 cannot be re-run as registered and the test says so; no skip, no relaxed cap; (3) the v6 active test (Batch 6) validates live rows at 32,768. Mutants: v5 accepting a 32,768 row must fail; v6 accepting an 8192 row must fail. No production cap value changes.

codex
The Batch 2 contract is unambiguous. I’m now proving the accepted WALL predicate, running the three namespace-reservation audits, and recording both forms of freeze evidence: the required `git diff --exit-code` gate and SHA-256 inventory of all protected manifests/families.
exec
/bin/bash -lc "git rev-parse HEAD
git status --short
sed -n '1218,1235p' src/vtt/room-generator.ts
git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2
freeze_files="'$(find tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2 -type f -print | LC_ALL=C sort)
printf '"'%s\\n' tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json "'$freeze_files | xargs sha256sum' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
e92ebc934d8e475d1e3a8d41a800b10b4d5db97e
  tier: CoverTier,
  authoredSourceIds: ReadonlySet<string>,
): boolean {
  return livingOpposingPairs(state).some(({ source, target }) => {
    const trace = traceCombatantLine(state, source.profile.id, target.profile.id);
    return trace.tier === tier && trace.sourceIds.some((id) => authoredSourceIds.has(id));
  });
}

export function generatedLineIsOpen(
  trace: Pick<TerrainLineTrace, 'tier' | 'blocksSight'>,
): boolean {
  return trace.tier === 'none' && !trace.blocksSight;
}

function hasOpenOpposingRay(state: EncounterState): boolean {
  if (livingOpposingPairs(state).some(({ source, target }) =>
    generatedLineIsOpen(traceCombatantLine(state, source.profile.id, target.profile.id)))) return true;
83daa7ea3ca89b270e5149368d09fa97074124dfe438cf95b346e996996026c8  tests/fixtures/d569-second-family-manifest.json
8c0bcb3c0afa8358c26129efbef82d0d2882135389d8dd8040fe7e4b6451e13a  tests/fixtures/d569-blind-experiment-manifest.json
6847b5d44b2577aa31a87a2eb83220ca17f54f8a8b160741d61d47019c75e51c  tests/fixtures/arena-basis-brutal-2/seed-6207001.json
9b1b9be44245659fb1a95922c685bbb1f9082b3f0fa591bd822553573a9d5c66  tests/fixtures/arena-basis-brutal-2/seed-6207002.json
afe31b4cb79f606bd56f29d37267814319de161db745747dae1a70a528617d6f  tests/fixtures/arena-basis-brutal-2/seed-6207003.json
96701e3136318b9f4dd8859311e30bb85586d129c8ef4424fcea398f3bbc813e  tests/fixtures/arena-basis-brutal-2/seed-6207004.json
a3a6774081b01b743091115b29ccfbb81d109e2f26867ce74a1c431b9a56a63a  tests/fixtures/arena-basis-brutal-2/seed-6207005.json
28800721eac25312561d04c49389f95a05ae24e81ff13b1ed6191951a7b34082  tests/fixtures/arena-basis-brutal-2/seed-6207006.json
eb9a566eab8c314fd754d85402e280e5f75ee30e114451b9405007a536375f8f  tests/fixtures/arena-basis-brutal-2/seed-6207007.json
5926d642f77ca2cdbf8cbed29408cfbf4165d6a05b453c2203a0aace93827a2b  tests/fixtures/arena-basis-brutal-2/seed-6207008.json
d398b3ad8a24ecae7d3989a1d47be1bedf69b1b989b17d967278be2b2ca7a50c  tests/fixtures/arena-basis-brutal-2/seed-6207009.json
b7efa3c2df793fcbbdba1d453358577a1d4eb7c6b2fe8c9b575d7fe85c3adf04  tests/fixtures/arena-basis-brutal-2/seed-6207010.json
7cc25c0ab9a0e380373a306874290d0f5247e74c0efb71e43ab593b58f0d9d3b  tests/fixtures/arena-basis-brutal-b/seed-6206001.json
3fdfe05812aeb6724789b4ff6f753ce68c523c13ff024d9ee275bd1060adbcaa  tests/fixtures/arena-basis-brutal-b/seed-6206002.json
36828c26a62a8af8c9cc32db7f3a13124d897a8aacf8ce4d4df2c11d2c742d3c  tests/fixtures/arena-basis-brutal-b/seed-6206003.json
35a5980497b2184a4d684e1849117bb70e4887cb40264ea33117e3e14322c60a  tests/fixtures/arena-basis-brutal-b/seed-6206004.json
dc48b6cdd7d4e33dc3c9a0188586ebd1537deac4dec4284c7e9404cec652e2f1  tests/fixtures/arena-basis-brutal-b/seed-6206005.json
d66fc0a9f29d84b5f1bc5a46b86483f91236d05b5cb1b951519cce7db98e2269  tests/fixtures/arena-basis-brutal-b/seed-6206006.json
1c4c337148afad9aff2a86143bb3156cc446ffabd2ccc8cc29eb004d83034bd9  tests/fixtures/arena-basis-brutal-b/seed-6206007.json
11a065b490f6e68effaddb0e919b9f9f9f7ae300f19a97f3359d3d4c86658a32  tests/fixtures/arena-basis-brutal-b/seed-6206008.json
19808aca9fb3ca7e4a77b6222cfae31c4f69a16b9b74ecc0706cb1eee58d4f5e  tests/fixtures/arena-basis-brutal-b/seed-6206009.json
a9efe78460c66972d09c6efd1c7cc04409ba6fff5a0028657d2442a0ec2fbc83  tests/fixtures/arena-basis-brutal-b/seed-6206010.json
3f737f1ddf714b0381abdc0e822b3a07cda4c55287a4bc0b1a97cd4d7d71d63b  tests/fixtures/arena-basis-brutal/seed-6203001.json
8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860  tests/fixtures/arena-basis-brutal/seed-6203002.json
d54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f  tests/fixtures/arena-basis-brutal/seed-6203003.json
6f9465eccac77eb60ffab2be1832f8f1bcd3290c655130c2ac752a38e6d75a20  tests/fixtures/arena-basis-brutal/seed-6203004.json
5b3be6abbdf2fcccff3243639d582bea2fb1098099fa91cf2208a0f143a2364e  tests/fixtures/arena-basis-brutal/seed-6203005.json
3e6818fe70e276e9ee7407d76b01e3afab7254cc1c27e649d5f445a5f6a6480c  tests/fixtures/arena-basis-brutal/seed-6203006.json
e6215e5b6388a79bc850e7ff97867f2618733acbb434526a77133e382cba2d1b  tests/fixtures/arena-basis-brutal/seed-6203007.json
b067b1aa081f368859979415c468d997b6fa504cad6ad70dc6998c90eeb3207c  tests/fixtures/arena-basis-brutal/seed-6203008.json
9780276cdec0ee71c99cc13217d73376990866f5ab3ab471c7a793196665f7ce  tests/fixtures/arena-basis-brutal/seed-6203009.json
2ec7f3c1298e7b6f3519cf765bcb88f36be3426e02cf7572730a77af9e32114d  tests/fixtures/arena-basis-brutal/seed-6203010.json

exec
/bin/bash -lc "set +e
rg -n -o -e '6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
s1="'$?
printf '"'COMMAND_1_STATUS=%s\\n' \""'$s1"
rg -n -o -e '"'6209[0-9]{3}|6210[0-9]{3}|6211[0-9]{3}|6_209_[0-9]{3}|6_210_[0-9]{3}|6_211_[0-9]{3}|6209xxx|6210xxx|6211xxx' tests/fixtures tools
s2="'$?
printf '"'COMMAND_2_STATUS=%s\\n' \""'$s2"
rg -n -o -e '"'\\b6209\\b|\\b6210\\b|\\b6211\\b' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
s3="'$?
printf '"'COMMAND_3_STATUS=%s\\n' \""'$s3"
exit 0' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
23048:6209xxx
23048:6210xxx
23048:6211xxx
23061:6209xxx
23061:6210xxx
23061:6211xxx
23071:6209xxx
23071:6_209_001
COMMAND_1_STATUS=0
COMMAND_2_STATUS=1
23068:6209
23068:6210
23068:6211
COMMAND_3_STATUS=0

codex
The reservation gate matches the frozen expected output exactly: statuses `0/1/0`, with no tracked consumer collision. The pre-freeze gate is exit 0 and all 32 protected hashes are recorded. I’m mapping the existing test helpers and exact retirement blocks before writing the three RED witnesses.
exec
/bin/bash -lc "sed -n '1,180p' tests/unit/vtt/room-generator.test.ts
sed -n '500,620p' tests/unit/vtt/room-generator.test.ts
sed -n '1,430p' tests/unit/vtt/arena-basis-brutal-b.test.ts
sed -n '130,160p' tests/unit/tools/d569-second-family-manifest.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
import { describe, expect, it } from 'vitest';
import { canonicalJson } from '../../../src/commands/canonical-json';
import { monsterCombatantProfile } from '../../../src/combat/combatant';
import { reduceEncounter } from '../../../src/combat/encounter';
import { mulberry32 } from '../../../src/combat/random';
import { BUNDLED_MONSTER_ROSTER, STARTER_MONSTER_ROSTER } from '../../../src/combat/statblocks/roster';
import { sha256 } from '../../../src/crypto/sha256';
import { creatureSizes, type KnownCreatureSize } from '../../../src/domain/enums';
import {
  D466_GENERATED_ROOM_OVERRIDES,
  d466CreatureReplacement,
  d466ReplacementStatblock,
} from '../../../src/vtt/d466-room-overrides';
import {
  generateRoom,
  heldoutMaximizingMonsterRosters,
  HELDOUT_STARTER_MONSTER_FAMILIES,
  ROOM_GRID_DIMENSIONS,
  type GeneratedRoom,
} from '../../../src/vtt/room-generator';
import {
  brutalRoomMembershipViolations,
  hardRoomMembershipViolations,
} from '../../../tools/d569-second-family-manifest';
import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
import { declareTestInputs } from '../../helpers/test-inputs';

const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });

const BASIS_SEEDS = [
  3_943_001,
  3_943_002,
  3_943_003,
  3_943_004,
  3_943_005,
  3_943_006,
  3_943_007,
  3_943_008,
  3_943_009,
  3_943_010,
  3_943_011,
  3_943_012,
] as const;

const HARD_BASIS_SEEDS = [
  5_117_001,
  5_117_002,
  5_117_003,
  5_117_004,
  5_117_005,
  5_117_006,
  5_117_007,
  5_117_008,
  5_117_009,
  5_117_010,
  5_117_011,
  5_117_012,
] as const;

const BRUTAL_BASIS_SEEDS = [6_203_001, 6_203_002, 6_203_003] as const;
const BRUTAL_BASIS_GENERATED_DIGESTS = {
  // D635.48: each old projection differs only by the removed state.foggedCells: [] key.
  // 6203001 old 3f737f1ddf714b0381abdc0e822b3a07cda4c55287a4bc0b1a97cd4d7d71d63b
  6_203_001: 'dee4cc931af96a3276308cdd37a7c169b3cb250d5fa24896b97356b868f4f0c2',
  // 6203002 old cdb567192800c4dbea964fbabf03d88013d7d6402da8b391cf2f60e19e69a0c9
  6_203_002: 'b190349ee0bfa1b40ef9f6d76fa46f15e985e9442a70295f4b23b604ecd99fd3',
  // 6203003 old 6c470c04fd6373f7fa999d5f0c66539c54eb71e6094e2b83a2ca972ab91720f8
  6_203_003: 'f5db8767bbbef7376423bc0b1eeb307e39fc41d54fda879292fde90bcf0cf3db',
} as const;
const BRUTAL_BASIS_FIXTURE_DIGESTS = {
  6_203_001: '3f737f1ddf714b0381abdc0e822b3a07cda4c55287a4bc0b1a97cd4d7d71d63b',
  6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
  6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
} as const;
const BRUTAL_PRODUCTIVITY_SEEDS = [
  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
] as const;

const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
  6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
  6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
  6_204_003: '105b5a7ff69ff572606a494fc336b29badb16f5c08438ec83fe0f1b7cf264e6f',
  6_204_004: 'e3b822d18589cfabdd6acc579daec22325540661869850ba55c583970725fd75',
  6_204_005: '3af3cf9a27216bc25dad9586696d47f587bec1e7ed8879569c6052e892693e72',
  6_204_006: 'be0fcd2d2cbf0415fc6b07189132ba665e30973b86a3847e43b427c11a238b4f',
  6_204_007: '56665e115694a30c5ff7da200e10394ae55093e5b85e03252bc2f36717cc5ba4',
  6_204_008: 'ae387c2541e566bdf565334837d2729eb9c1a65303fe0f412eed3288fd86d306',
  6_204_009: '04fbc7fd4cd029c63d0d936539ab53aca3d50c80206764aede55fbc0ba72a88e',
  6_204_010: '2768168a0da1fa28d35e86febb92a6035f892fbe5a1f703c89c13fe6ace3339b',
} as const;

const D466_6204_NON_ROSTER_DIGESTS = {
  // D635.48: every old projection differs only by the removed state.foggedCells: [] key.
  // 6204001 old a510aaa8e785c641bcf3455df6f7ce8fbf71b0f2ec978e3b5e6f2ac62f289a3f
  6_204_001: 'b0c7344d10a0059c9b703b474c4b3a4e65c9d328d8c4f6edf8e55e62623fd5df',
  // 6204002 old c26ea2b4bca6ed4f7afbf044a9853b410287e80dc9a4a4fc6f322083ea6eb501
  6_204_002: 'b92cdc924d3a7b72da1cefb57a4bbb1821174b96f18b409bd88d2dda87905428',
  // 6204003 old 16bb7c43f6a2a1751928de993d50e5bd27e69b64c9ac8a4db9efbb72d94e2f66
  6_204_003: '1fa38b1c2f731d9b800013ccf71a065869d9156af876a07b1c2c5407012dac7a',
  // 6204004 old 9fb5c7812054c026eb9f8d070691fbf8af5925d5e6ce8123a33a3085af961990
  6_204_004: 'c1ccfbc1ba22774cb77f22e1a1aa0b303314670120d8326f7f33e0048e14bd46',
  // 6204005 old 0ad33edb24bda5de199144bf9c6d6f1412e61d2915486a1364817cd47438259d
  6_204_005: '4648396c6dbcc96611120ad186f6079534485cd6af2f4fd26f475c8b1db7a9e6',
  // 6204006 old d0138274a6f2b4cdf7996578b6408803ad723d808fb4b6d5487cc4b767dee880
  6_204_006: '407ee43e5d286f6649a1b6002daa8c2301df08fac106d2e58ff52b2343f3e9fc',
  // 6204007 old bcd1545c7a397c86a37c7a672aab72c99689a94a9df812e5a39db830cbb7c326
  6_204_007: '343ef8a190c7f3559f5fa98d18228c60733c3385a6d3b70ccf64f22d9ba1515e',
  // 6204008 old 1bf591adce1bceac8ea3a8e44f9fcfed5b252266ceccfcc96991cb20670bc8b1
  6_204_008: '1b9c525fd3d36bc0ede5353c9626208b664e5604254eb3bcfc0ec47ba6e1ebed',
  // 6204009 old a323c1eb80becabbae04661c815887440ddc89e02aa56a757cb776d5348d8255
  6_204_009: 'b88590db462f9e1a238658a6b3ee17c75e58c6c5aefb97cc695926c18ed83304',
  // 6204010 old 2439eb3aa80907618d35f188429eb9ca07c56e3b0c35b1218e7732917e6b7ee9
  6_204_010: '5e7fbe28033ec0ad69ae49c5bdf576c26c2299c7658e5487c2e0324129ec076e',
} as const;

const D466_6204_CR_SPEND = {
  6_204_001: 60, 6_204_002: 50, 6_204_003: 64, 6_204_004: 54, 6_204_005: 58,
  6_204_006: 74, 6_204_007: 66, 6_204_008: 52, 6_204_009: 58, 6_204_010: 46,
} as const;

const inputs = declareTestInputs({
  fixtures: [
    'tests/fixtures/arena-basis/seed-3943001.json',
    'tests/fixtures/arena-basis/seed-3943002.json',
    'tests/fixtures/arena-basis/seed-3943003.json',
    'tests/fixtures/arena-basis/seed-3943004.json',
    'tests/fixtures/arena-basis/seed-3943005.json',
    'tests/fixtures/arena-basis/seed-3943006.json',
    'tests/fixtures/arena-basis/seed-3943007.json',
    'tests/fixtures/arena-basis/seed-3943008.json',
    'tests/fixtures/arena-basis/seed-3943009.json',
    'tests/fixtures/arena-basis/seed-3943010.json',
    'tests/fixtures/arena-basis/seed-3943011.json',
    'tests/fixtures/arena-basis/seed-3943012.json',
    'tests/fixtures/arena-basis-hard/seed-5117001.json',
    'tests/fixtures/arena-basis-hard/seed-5117002.json',
    'tests/fixtures/arena-basis-hard/seed-5117003.json',
    'tests/fixtures/arena-basis-hard/seed-5117004.json',
    'tests/fixtures/arena-basis-hard/seed-5117005.json',
    'tests/fixtures/arena-basis-hard/seed-5117006.json',
    'tests/fixtures/arena-basis-hard/seed-5117007.json',
    'tests/fixtures/arena-basis-hard/seed-5117008.json',
    'tests/fixtures/arena-basis-hard/seed-5117009.json',
    'tests/fixtures/arena-basis-hard/seed-5117010.json',
    'tests/fixtures/arena-basis-hard/seed-5117011.json',
    'tests/fixtures/arena-basis-hard/seed-5117012.json',
    'tests/fixtures/arena-basis-brutal/seed-6203001.json',
    'tests/fixtures/arena-basis-brutal/seed-6203002.json',
    'tests/fixtures/arena-basis-brutal/seed-6203003.json',
    'tests/fixtures/heldout-party/level-3.json',
    'tests/fixtures/heldout-party/level-4.json',
    'tests/fixtures/heldout-party/level-5.json',
    'tests/fixtures/heldout-party/level-6.json',
  ],
});

function heldoutParty(level: 3 | 4 | 5 | 6) {
  const paths = {
    3: 'tests/fixtures/heldout-party/level-3.json',
    4: 'tests/fixtures/heldout-party/level-4.json',
    5: 'tests/fixtures/heldout-party/level-5.json',
    6: 'tests/fixtures/heldout-party/level-6.json',
  } as const;
  const loaded = loadExternalPartyPackBytes(inputs.fixtures.readText(paths[level]));
  if (loaded.status !== 'loaded') throw new Error(`Held-out party ${String(level)} did not load.`);
  return loaded.party;
}

function independentMulberry32(seed: number): () => number {
  let state = seed >>> 0;
  return () => {
    state = (state + 0x6d2b79f5) >>> 0;
    let mixed = state;
    mixed = Math.imul(mixed ^ (mixed >>> 15), mixed | 1);
    mixed ^= mixed + Math.imul(mixed ^ (mixed >>> 7), mixed | 61);
    return ((mixed ^ (mixed >>> 14)) >>> 0) / 4_294_967_296;
    const path = `tests/fixtures/arena-basis-hard/seed-${String(seed)}.json` as
      `tests/fixtures/arena-basis-hard/seed-${typeof seed}.json`;
    const room = JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
    expect(hardRoomMembershipViolations(room)).toEqual([]);
  });

  it.each(BRUTAL_BASIS_SEEDS)('pins frozen brutal arena basis seed %s byte-for-byte', (seed) => {
    const path = `tests/fixtures/arena-basis-brutal/seed-${String(seed)}.json` as
      `tests/fixtures/arena-basis-brutal/seed-${typeof seed}.json`;
    const generatedBytes = `${canonicalJson(generateRoom(seed, { difficulty: 'brutal' }))}\n`;
    const fixtureBytes = inputs.fixtures.readText(path);
    const legacyGeneratedBytes = `${legacyCreatureSpaceProjection(
      generateRoom(seed, { difficulty: 'brutal' }),
    )}\n`;
    if (seed === 6_203_001) {
      const legacyFixture = JSON.parse(fixtureBytes) as GeneratedRoom;
      // D635.48: the raw brutal fixture carries the same obsolete V1 fog key.
      delete (legacyFixture.encounter.state as { foggedCells?: unknown }).foggedCells;
      expect(legacyGeneratedBytes).toBe(`${canonicalJson(legacyFixture)}\n`);
    }
    expect(sha256(legacyGeneratedBytes),
      `seed ${String(seed)} pre-replacement generation changed`).toBe(
      BRUTAL_BASIS_GENERATED_DIGESTS[seed],
    );
    expect(sha256(fixtureBytes), `seed ${String(seed)} typed fixture changed`).toBe(
      BRUTAL_BASIS_FIXTURE_DIGESTS[seed],
    );
    if (seed === 6_203_001) expect(generatedBytes).toBe(expectedCreatureSpaceFixture(fixtureBytes));
  });

  it('applies only the three 6204 post-sampling replacements without changing RNG-derived room data', () => {
    expect(D466_GENERATED_ROOM_OVERRIDES.map((override) => override.seed)).toEqual([
      6_204_004, 6_204_006, 6_204_009,
    ]);
    for (let seed = 6_204_001; seed <= 6_204_010; seed += 1) {
      const typedSeed = seed as keyof typeof D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS;
      const room = generateRoom(seed, { difficulty: 'brutal' });
      expect(
        sha256(canonicalJson(d466NonRosterProjection(room))),
        `seed ${String(seed)} changed terrain, party, ids, placement, or RNG-derived state`,
      ).toBe(D466_6204_NON_ROSTER_DIGESTS[typedSeed]);
      expect(room.spec.challengeBudgetEighths, `seed ${String(seed)} changed CR budget`).toBe(
        D466_6204_CR_SPEND[typedSeed],
      );
      expect(room.spec.challengeSpentEighths, `seed ${String(seed)} changed CR spend`).toBe(
        D466_6204_CR_SPEND[typedSeed],
      );

      const override = D466_GENERATED_ROOM_OVERRIDES.find((candidate) => candidate.seed === seed);
      if (override === undefined) {
        expect(
          sha256(canonicalJson(room.spec.monsterRoster)),
          `seed ${String(seed)} roster changed; making the override also fire for 6204005 must fail`,
        ).toBe(D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS[typedSeed]);
        continue;
      }

      const replacement = d466CreatureReplacement(override.original);
      const replacementEntries = room.spec.monsterRoster.filter(
        (entry) => entry.statblockId === replacement.replacement,
      );
      expect(replacementEntries.length, `seed ${String(seed)} did not receive its replacement`).toBeGreaterThan(0);
      expect(room.spec.monsterRoster.some((entry) => entry.statblockId === replacement.original)).toBe(false);
      for (const entry of replacementEntries) {
        expect(entry).toMatchObject({
          challengeRating: replacement.challengeRating,
          challengeEighths: replacement.crEighths,
        });
        const combatant = room.encounter.state.combatants.find(
          (candidate) => candidate.profile.id === entry.combatantId,
        );
        expect(combatant?.profile).toEqual(monsterCombatantProfile(
          d466ReplacementStatblock(replacement),
          { combatantId: entry.combatantId, tokenId: entry.tokenId },
        ));
      }
      const restoredRoster = room.spec.monsterRoster.map((entry) =>
        entry.statblockId !== replacement.replacement ? entry : {
          ...entry,
          statblockId: replacement.original,
        });
      expect(
        sha256(canonicalJson(restoredRoster)),
        `seed ${String(seed)} changed sampled roster data beyond the typed id replacement`,
      ).toBe(D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS[typedSeed]);
    }
  });

  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
    (seed) => {
      const brutal = generateRoom(seed, { difficulty: 'brutal' });
      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
      const state = freshMonsterPlanningState(brutal.encounter.state);
      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
      for (const monster of monsters) {
        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
        expect(productive, monster.profile.id).toBe(true);
      }
    },
  );

  it.each([
    [3, 900],
    [4, 1_500],
    [5, 3_000],
    [6, 4_000],
  ] as const)('builds four full level-matched PCs at level %s', (level, targetXp) => {
    const party = heldoutParty(level);
    const room = generateRoom(7_850_001 + level, {
      initiativeProfile: 'derived_v1',
      heldoutOrdinary: {
        protocol: 'heldout-development-v1',
        party,
        partyLevel: level,
        targetXp,
      },
    });
    const players = room.encounter.state.combatants.filter((subject) =>
      subject.profile.kind === 'player_character');
import { describe, expect, it } from 'vitest';
import { createEncounter, combatantSpace, reduceEncounter, type EncounterState } from '../../../src/combat/encounter';
import { monsterCombatantProfile } from '../../../src/combat/combatant';
import { spaceFitsBounds, spacesIntersect, spaceTouchesCellSet } from '../../../src/combat/creature-space';
import { mulberry32 } from '../../../src/combat/random';
import { encounterBranchId, encounterSessionId, type CombatantId } from '../../../src/combat/values';
import { UNICORN } from '../../../src/combat/statblocks/monsters';
import { BUNDLED_MONSTER_ROSTER } from '../../../src/combat/statblocks/roster';
import { sha256 } from '../../../src/crypto/sha256';
import { D466_GENERATED_ROOM_OVERRIDES } from '../../../src/vtt/d466-room-overrides';
import { EngineRoundSession, type AuthorizedEngineTurnProposal } from '../../../src/vtt/engine-round-session';
import {
  availableEngineActorOptions,
  createPureTurnProposalResolver,
  resolveEngineActorOption,
  type EngineTurnProposal,
} from '../../../src/vtt/intent-resolver';
import type {
  EngineActivationChoice,
  EngineOfferableOption,
} from '../../../src/vtt/turn-proposal';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
import { actorOpportunityReport } from '../../../src/vtt/intel/opportunity-cost';
import { decodeArenaFixture } from '../../../src/vtt/mcp/entrypoint';
import { freshMonsterPlanningState, projectFutureMonsterTurns } from '../../../src/vtt/monster-planning-state';
import type { ReactionOfferHostPolicy } from '../../../src/vtt/reaction-offer-host-policy';
import {
  applyRoomInitiativeProfile,
  BRUTAL_CHALLENGE_BUDGET_SCALE,
  BRUTAL_CHALLENGE_SPEND_FRACTION_BAND,
  BRUTAL_TERRAIN_FEATURE_COUNT_BAND,
  generateRoom,
  type GeneratedRoom,
} from '../../../src/vtt/room-generator';
import {
  createScriptedPartyPlan,
  DEFAULT_SCRIPTED_PARTY_DECISION_POLICY,
  materializeScriptedPartyTurn,
} from '../../../src/vtt/scripted-party-round';
import { BRUTAL_10_B_SEEDS, BRUTAL_10_SEEDS, R1_10_SEEDS } from '../../../tools/ai-dm-rerun-packet';
import { declareTestInputs } from '../../helpers/test-inputs';
import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';

const FIXTURE_PATHS = [
  'tests/fixtures/arena-basis-brutal-b/seed-6206001.json',
  'tests/fixtures/arena-basis-brutal-b/seed-6206002.json',
  'tests/fixtures/arena-basis-brutal-b/seed-6206003.json',
  'tests/fixtures/arena-basis-brutal-b/seed-6206004.json',
  'tests/fixtures/arena-basis-brutal-b/seed-6206005.json',
  'tests/fixtures/arena-basis-brutal-b/seed-6206006.json',
  'tests/fixtures/arena-basis-brutal-b/seed-6206007.json',
  'tests/fixtures/arena-basis-brutal-b/seed-6206008.json',
  'tests/fixtures/arena-basis-brutal-b/seed-6206009.json',
  'tests/fixtures/arena-basis-brutal-b/seed-6206010.json',
] as const;

const HARD_EXECUTION_FIXTURE_PATHS = [
  'tests/fixtures/arena-basis-hard/seed-5117001.json',
  'tests/fixtures/arena-basis-hard/seed-5117002.json',
  'tests/fixtures/arena-basis-hard/seed-5117003.json',
  'tests/fixtures/arena-basis-hard/seed-5117004.json',
  'tests/fixtures/arena-basis-hard/seed-5117005.json',
  'tests/fixtures/arena-basis-hard/seed-5117006.json',
  'tests/fixtures/arena-basis-hard/seed-5117007.json',
  'tests/fixtures/arena-basis-hard/seed-5117008.json',
  'tests/fixtures/arena-basis-hard/seed-5117009.json',
  'tests/fixtures/arena-basis-hard/seed-5117010.json',
  'tests/fixtures/arena-basis-hard/seed-5117011.json',
  'tests/fixtures/arena-basis-hard/seed-5117012.json',
  'tests/fixtures/arena-basis-hard/seed-5117013.json',
] as const;

const BRUTAL_EXECUTION_FIXTURE_PATHS = [
  'tests/fixtures/arena-basis-brutal/seed-6203001.json',
  'tests/fixtures/arena-basis-brutal/seed-6203002.json',
  'tests/fixtures/arena-basis-brutal/seed-6203003.json',
  'tests/fixtures/arena-basis-brutal/seed-6203004.json',
  'tests/fixtures/arena-basis-brutal/seed-6203005.json',
  'tests/fixtures/arena-basis-brutal/seed-6203006.json',
  'tests/fixtures/arena-basis-brutal/seed-6203007.json',
  'tests/fixtures/arena-basis-brutal/seed-6203008.json',
  'tests/fixtures/arena-basis-brutal/seed-6203009.json',
  'tests/fixtures/arena-basis-brutal/seed-6203010.json',
] as const;

const EXECUTION_FIXTURE_PATHS = [
  ...HARD_EXECUTION_FIXTURE_PATHS,
  ...BRUTAL_EXECUTION_FIXTURE_PATHS,
  ...FIXTURE_PATHS,
] as const;

const EXECUTION_FIXTURE_FAMILIES = [
  ['hard', HARD_EXECUTION_FIXTURE_PATHS],
  ['brutal', BRUTAL_EXECUTION_FIXTURE_PATHS],
  ['brutal-b', FIXTURE_PATHS],
] as const;

const FIXTURE_DIGESTS = {
  6_206_001: '7cc25c0ab9a0e380373a306874290d0f5247e74c0efb71e43ab593b58f0d9d3b',
  6_206_002: '3fdfe05812aeb6724789b4ff6f753ce68c523c13ff024d9ee275bd1060adbcaa',
  6_206_003: '36828c26a62a8af8c9cc32db7f3a13124d897a8aacf8ce4d4df2c11d2c742d3c',
  6_206_004: '35a5980497b2184a4d684e1849117bb70e4887cb40264ea33117e3e14322c60a',
  6_206_005: 'dc48b6cdd7d4e33dc3c9a0188586ebd1537deac4dec4284c7e9404cec652e2f1',
  6_206_006: 'd66fc0a9f29d84b5f1bc5a46b86483f91236d05b5cb1b951519cce7db98e2269',
  6_206_007: '1c4c337148afad9aff2a86143bb3156cc446ffabd2ccc8cc29eb004d83034bd9',
  6_206_008: '11a065b490f6e68effaddb0e919b9f9f9f7ae300f19a97f3359d3d4c86658a32',
  6_206_009: '19808aca9fb3ca7e4a77b6222cfae31c4f69a16b9b74ecc0706cb1eee58d4f5e',
  6_206_010: 'a9efe78460c66972d09c6efd1c7cc04409ba6fff5a0028657d2442a0ec2fbc83',
} as const satisfies Readonly<Record<(typeof BRUTAL_10_B_SEEDS)[number], string>>;

const inputs = declareTestInputs({ fixtures: EXECUTION_FIXTURE_PATHS });
const OFFER_ENVIRONMENT = buildOfferEnvironment({
  kind: 'configuration',
  mode: 'legacy_standard',
});
const TURN_PROPOSAL_RESOLVER = createPureTurnProposalResolver(OFFER_ENVIRONMENT);

type RoundExecutionResult =
  | { readonly seed: number; readonly outcome: 'executed'; readonly state: EncounterState }
  | {
      readonly seed: number;
      readonly outcome: 'execution_failed';
      readonly error: string;
      readonly state: EncounterState;
    };

function livingMonsterIds(state: EncounterState): readonly CombatantId[] {
  return state.combatants.flatMap((combatant) =>
    combatant.profile.kind === 'monster' && combatant.life === 'living'
      ? [combatant.profile.id]
      : []);
}

function maximalMonsterSegment(state: EncounterState): readonly CombatantId[] {
  if (state.activeInitiativeIndex === null) return [];
  const actors: CombatantId[] = [];
  for (let index = state.activeInitiativeIndex; index < state.initiative.length; index += 1) {
    const initiative = state.initiative[index];
    if (initiative === undefined) throw new Error('Execution invariant encountered a missing initiative entry.');
    const combatant = state.combatants.find((candidate) => candidate.profile.id === initiative.combatant);
    if (combatant === undefined) throw new Error(`Execution invariant cannot find ${initiative.combatant}.`);
    if (combatant.life === 'dead') continue;
    if (combatant.profile.kind === 'player_character') break;
    actors.push(combatant.profile.id);
  }
  return actors;
}

function deterministicActivationChoice(option: EngineOfferableOption): EngineActivationChoice | undefined {
  const slot = option.activationChoice;
  if (slot === undefined || slot === null) return undefined;
  switch (slot.kind) {
    case 'command_word': return { kind: slot.kind, value: slot.values[0] };
    case 'unicorns_blessing_spell': return { kind: slot.kind, value: slot.values[0] };
    case 'dispel_evil_and_good_mode': return { kind: slot.kind, value: slot.values[0] };
    case 'calm_emotions_per_target': return {
      kind: slot.kind,
      selections: slot.targetIds.map((targetId) => ({ targetId, mode: slot.values[0] })),
    };
  }
}

function dryRunMonsterPlan(state: EncounterState): ReadonlyMap<CombatantId, AuthorizedEngineTurnProposal> {
  const actorIds = [...livingMonsterIds(state)].sort((left, right) => left.localeCompare(right));
  const planningState = projectFutureMonsterTurns(state, actorIds);
  return new Map(actorIds.map((actorId) => {
    const report = actorOpportunityReport(
      planningState,
      actorId,
      OFFER_ENVIRONMENT,
      state.revision,
    );
    const primary = report.defaultOption;
    const fallback = availableEngineActorOptions(
      planningState,
      actorId,
      OFFER_ENVIRONMENT,
      state.revision,
    ).find((option) => option.optionId !== primary.optionId);
    if (fallback === undefined) throw new Error(`Dry-run plan has no independent fallback for ${actorId}.`);
    const activationChoice = deterministicActivationChoice(primary);
    const proposal: EngineTurnProposal = {
      actorId,
      expectedRevision: state.revision,
      primaryOptionId: primary.optionId,
      fallbackOptionId: fallback.optionId,
      reason: 'Use the engine-ranked option to maximize immediate tactical value.',
      ...(activationChoice === undefined ? {} : { activationChoice }),
      overrideJustification: null,
    };
    const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
    if (!resolution.valid) {
      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
    }
    return [actorId, {
      proposal,
      option: resolution.option,
      primaryOption: resolution.primaryOption,
      fallbackOption: resolution.fallbackOption,
      mechanics: resolution.mechanics,
      selectedBranch: resolution.selectedBranch,
    }] as const;
  }));
}

function executeFrozenRound(
  room: GeneratedRoom,
  policy: ReactionOfferHostPolicy = { kind: 'unattended', askDefault: 'decline' },
): RoundExecutionResult {
  const seed = room.spec.seed;
  const session = new EngineRoundSession(
    applyRoomInitiativeProfile(decodeArenaFixture(room), 'derived_v1'),
    mulberry32(8_274_113),
    policy,
    OFFER_ENVIRONMENT,
  );
  try {
    session.beginRoundWithoutSkipping({
      runId: encounterSessionId(`encounter:arena-execution-invariant:${String(seed)}`),
      branchId: encounterBranchId(`branch:arena-execution-invariant:${String(seed)}`),
      revision: 1,
      requestId: `request:arena-execution-invariant:${String(seed)}`,
      phase: 'initial',
      room: 1,
      historyKind: 'room_ready',
    }, null);
    const roundStart = session.currentState();
    const round = roundStart.round;
    const partyPlan = createScriptedPartyPlan(roundStart, {
      decisionPolicy: DEFAULT_SCRIPTED_PARTY_DECISION_POLICY,
    });
    const monsterPlan = dryRunMonsterPlan(roundStart);
    const acted = new Set<CombatantId>();
    for (let step = 0; step <= roundStart.initiative.length; step += 1) {
      const state = session.currentState();
      if (state.phase.kind === 'concluded' || state.round > round) {
        return { seed, outcome: 'executed', state };
      }
      if (state.activeCombatant === null) throw new Error('Round execution lost its active combatant.');
      if (acted.has(state.activeCombatant)) {
        throw new Error(state.pendingDecisions.length > 0
          ? 'Round execution reached an already-acted actor with unresolved pending decisions.'
          : 'Round execution stalled on an already-acted actor before advancing the round.');
      }
      const active = state.combatants.find((combatant) =>
        combatant.profile.id === state.activeCombatant);
      if (active === undefined) throw new Error(`Execution invariant cannot find ${state.activeCombatant}.`);
      if (active.profile.kind === 'player_character') {
        session.completeScriptedPcTurn(materializeScriptedPartyTurn({
          state,
          plan: partyPlan,
          actorId: active.profile.id,
        }), null);
        acted.add(active.profile.id);
        continue;
      }
      const segment = maximalMonsterSegment(state);
      const applications = segment.map((actorId) => {
        const application = monsterPlan.get(actorId);
        if (application === undefined) throw new Error(`Dry-run plan omitted ${actorId}.`);
        acted.add(actorId);
        return application;
      });
      session.applyConsecutiveMonsterSegment(applications, null);
    }
    throw new Error(`Round ${String(round)} did not complete within one initiative cycle.`);
  } catch (error) {
    return {
      seed,
      outcome: 'execution_failed',
      error: error instanceof Error ? error.message : String(error),
      state: session.currentState(),
    };
  }
}

function assertRoundsExecuted(results: readonly RoundExecutionResult[]): void {
  const failure = results.find((result) => result.outcome === 'execution_failed');
  if (failure !== undefined) {
    throw new Error(`Seed ${String(failure.seed)} failed round execution: ${failure.error}`);
  }
}

function cellIsInBounds(
  cell: { readonly column: number; readonly row: number },
  bounds: { readonly columns: number; readonly rows: number },
): boolean {
  return cell.column >= 0 && cell.column < bounds.columns &&
    cell.row >= 0 && cell.row < bounds.rows;
}

function assertBrutalMembership(
  room: GeneratedRoom,
  fixtureBytes: string,
  expectedDigest: string,
): void {
  const seed = room.spec.seed;
  const hard = generateRoom(seed, { difficulty: 'hard' });
  expect(room.spec.difficultyProfile, `seed ${String(seed)} difficulty`).toBe('brutal');
  expect(room.spec.challengeBudgetEighths, `seed ${String(seed)} scaled budget`).toBe(
    hard.spec.challengeBudgetEighths * BRUTAL_CHALLENGE_BUDGET_SCALE,
  );
  expect(room.spec.challengeSpentEighths, `seed ${String(seed)} minimum spend`).toBeGreaterThanOrEqual(
    room.spec.challengeBudgetEighths * BRUTAL_CHALLENGE_SPEND_FRACTION_BAND.minimum,
  );
  expect(room.spec.challengeSpentEighths, `seed ${String(seed)} maximum spend`).toBeLessThanOrEqual(
    room.spec.challengeBudgetEighths * BRUTAL_CHALLENGE_SPEND_FRACTION_BAND.maximum,
  );

  const roster = room.spec.monsterRoster.map((entry) => {
    const row = BUNDLED_MONSTER_ROSTER.find((candidate) => candidate.id === entry.statblockId);
    if (row === undefined) throw new Error(`Seed ${String(seed)} references unknown ${entry.statblockId}.`);
    return row;
  });
  expect(roster.some((row) => {
    const actions = row.statblock.sourceDetails.actions;
    return actions.kind === 'present' && actions.value.some((action) =>
      action.kind === 'spellcasting' && action.execution?.kind !== 'absent' &&
      action.spells.some((spell) => spell.manifestStatus === 'implemented'));
  }), `seed ${String(seed)} executable caster`).toBe(true);

  const terrainCounts = {
    difficult: room.spec.terrain.filter((feature) => feature.kind === 'difficult-terrain-patch').length,
    hazards: room.spec.terrain.filter((feature) => feature.kind === 'hazard-object').length,
    lights: room.spec.terrain.filter((feature) => feature.kind === 'light-source').length,
    obscurement: room.spec.terrain.filter((feature) => feature.kind === 'obscurement-patch').length,
  };
  expect(terrainCounts.difficult, `seed ${String(seed)} difficult terrain`).toBeGreaterThanOrEqual(3);
  expect(terrainCounts.hazards, `seed ${String(seed)} hazards`).toBeGreaterThanOrEqual(3);
  expect(terrainCounts.lights, `seed ${String(seed)} lights`).toBeGreaterThanOrEqual(2);
  expect(terrainCounts.obscurement, `seed ${String(seed)} obscurement`).toBeGreaterThanOrEqual(2);
  expect(room.spec.terrain.length, `seed ${String(seed)} terrain exceeds hard`).toBeGreaterThan(
    hard.spec.terrain.length,
  );
  expect(room.spec.terrain.length, `seed ${String(seed)} terrain minimum`).toBeGreaterThanOrEqual(
    BRUTAL_TERRAIN_FEATURE_COUNT_BAND.minimum,
  );
  expect(room.spec.terrain.length, `seed ${String(seed)} terrain maximum`).toBeLessThanOrEqual(
    BRUTAL_TERRAIN_FEATURE_COUNT_BAND.maximum,
  );

  const state = decodeArenaFixture(room);
  const regions = [
    ...state.environment.difficultTerrainRegions,
    ...state.environment.lightRegions,
    ...state.environment.obscurementRegions,
    ...(state.environment.movementRegions ?? []),
  ];
  for (const region of regions) {
    expect(region.id.trim(), `seed ${String(seed)} region id`).not.toBe('');
    expect(region.cells.length, `seed ${String(seed)} region cells`).toBeGreaterThan(0);
    expect(region.cells.every((cell) => cellIsInBounds(cell, state.bounds)),
      `seed ${String(seed)} region bounds`).toBe(true);
  }
  expect(state.worldObjects.every((object) =>
    object.footprint.length > 0 && object.footprint.every((cell) => cellIsInBounds(cell, state.bounds))),
  `seed ${String(seed)} world-object positions`).toBe(true);

  const spaces = state.combatants.map((combatant) => ({
    id: combatant.profile.id,
    space: combatantSpace(state, combatant.profile.id),
  }));
  for (const entry of spaces) {
    expect(spaceFitsBounds(entry.space, state.bounds), `${entry.id} position bounds`).toBe(true);
    expect(spaceTouchesCellSet(entry.space, state.blockedCells), `${entry.id} blocked overlap`).toBe(false);
  }
  for (let left = 0; left < spaces.length; left += 1) {
    for (let right = left + 1; right < spaces.length; right += 1) {
      expect(spacesIntersect(spaces[left]!.space, spaces[right]!.space),
        `${spaces[left]!.id}/${spaces[right]!.id} overlap`).toBe(false);
    }
  }

  const reduced = reduceEncounter(state, { type: 'roll_initiative' }, mulberry32(seed));
  expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
  expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);

  const planningState = freshMonsterPlanningState(state);
  for (const monster of planningState.combatants.filter((combatant) =>
    combatant.profile.kind === 'monster')) {
    const productive = availableEngineActorOptions(
      planningState,
      monster.profile.id,
      OFFER_ENVIRONMENT,
    ).some((option) => {
      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
        resolution.mechanics.actionSlots.some((slot) =>
          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
          slot.kind === 'use_world_object'));
    });
    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
  }

  // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
  expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
}

function fixtureAt(index: number): { readonly room: GeneratedRoom; readonly bytes: string } {
  const path = FIXTURE_PATHS[index];
  if (path === undefined) throw new RangeError(`No brutal-b fixture at index ${String(index)}.`);
  const bytes = inputs.fixtures.readText(path);
  return { room: JSON.parse(bytes) as GeneratedRoom, bytes };
}

function executionFixtureAt(path: (typeof EXECUTION_FIXTURE_PATHS)[number]): GeneratedRoom {
  return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
}

describe('second brutal arena basis family', () => {
  it('uses one clean contiguous range disjoint from every existing seeded fixture family', () => {
    const existingSeeds = new Set([
      ...Array.from({ length: 12 }, (_unused, index) => 3_943_001 + index),
      ...R1_10_SEEDS,
      5_117_011, 5_117_012, 5_117_013,
      ...BRUTAL_10_SEEDS,
    ]);
    expect(BRUTAL_10_B_SEEDS.every((seed) => !existingSeeds.has(seed))).toBe(true);
    expect(BRUTAL_10_B_SEEDS).toEqual(Array.from({ length: 10 }, (_unused, index) => 6_206_001 + index));
    expect(BRUTAL_10_B_SEEDS.every((seed) =>
      !D466_GENERATED_ROOM_OVERRIDES.some((override) => Number(override.seed) === seed))).toBe(true);
    expect(D466_GENERATED_ROOM_OVERRIDES.map((override) => override.seed)).toEqual([
      6_204_004, 6_204_006, 6_204_009,
    ]);
  });

  it.each(BRUTAL_10_B_SEEDS)(
    'pins seed %s only after proving every brutal membership property from state',
    (seed) => {
      const index = seed - BRUTAL_10_B_SEEDS[0];
      6_207_006, 6_207_007, 6_207_008, 6_207_009, 6_207_010,
    ]);
    expect(frozen.ledgerEntry).toBe(D569_SECOND_FAMILY_LEDGER_ENTRY);
    expect(codes(frozen)).toEqual([]);
  });

  it.each(HARD_FIXTURE_PATHS)('%s satisfies every hard state-property invariant before its pin', (path) => {
    const frozen = room(path);
    expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
    expect(hardRoomMembershipViolations(frozen)).toEqual([]);
  });

  it.each(BRUTAL_FIXTURE_PATHS)(
    '%s retains historical integrity plus static budget, caster, and terrain invariants',
    (path) => {
      const frozen = room(path);
      expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
      expect(brutalRoomMembershipViolations(frozen)
        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
    },
  );

  it('second_family_uses_primary_seed: rejects primary or previously used seed namespaces', () => {
    const changed = structuredClone(manifest());
    const hard = changed.cohorts.find((cohort) => cohort.difficulty === 'hard');
    if (hard === undefined) throw new Error('missing hard cohort');
    hard.fixtures[0] = { ...hard.fixtures[0]!, seed: 5_117_001 };
    expect(codes(changed)).toContain('cohort_seed_range');
    expect(codes(changed)).toContain('seed_overlap');
  });


exec
/bin/bash -lc "sed -n '360,553p' tests/unit/vtt/arena-basis-brutal-b.test.ts
rg -n \"missing fixture|missing fixtures|readText\\(|tryRead|exists|fileExists|declareTestInputs\\(\\{ fixtures\" tests/unit/vtt tests/unit/tools | head -200
sed -n '1,220p' tests/helpers/test-inputs.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
    id: combatant.profile.id,
    space: combatantSpace(state, combatant.profile.id),
  }));
  for (const entry of spaces) {
    expect(spaceFitsBounds(entry.space, state.bounds), `${entry.id} position bounds`).toBe(true);
    expect(spaceTouchesCellSet(entry.space, state.blockedCells), `${entry.id} blocked overlap`).toBe(false);
  }
  for (let left = 0; left < spaces.length; left += 1) {
    for (let right = left + 1; right < spaces.length; right += 1) {
      expect(spacesIntersect(spaces[left]!.space, spaces[right]!.space),
        `${spaces[left]!.id}/${spaces[right]!.id} overlap`).toBe(false);
    }
  }

  const reduced = reduceEncounter(state, { type: 'roll_initiative' }, mulberry32(seed));
  expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
  expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);

  const planningState = freshMonsterPlanningState(state);
  for (const monster of planningState.combatants.filter((combatant) =>
    combatant.profile.kind === 'monster')) {
    const productive = availableEngineActorOptions(
      planningState,
      monster.profile.id,
      OFFER_ENVIRONMENT,
    ).some((option) => {
      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
        resolution.mechanics.actionSlots.some((slot) =>
          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
          slot.kind === 'use_world_object'));
    });
    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
  }

  // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
  expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
}

function fixtureAt(index: number): { readonly room: GeneratedRoom; readonly bytes: string } {
  const path = FIXTURE_PATHS[index];
  if (path === undefined) throw new RangeError(`No brutal-b fixture at index ${String(index)}.`);
  const bytes = inputs.fixtures.readText(path);
  return { room: JSON.parse(bytes) as GeneratedRoom, bytes };
}

function executionFixtureAt(path: (typeof EXECUTION_FIXTURE_PATHS)[number]): GeneratedRoom {
  return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
}

describe('second brutal arena basis family', () => {
  it('uses one clean contiguous range disjoint from every existing seeded fixture family', () => {
    const existingSeeds = new Set([
      ...Array.from({ length: 12 }, (_unused, index) => 3_943_001 + index),
      ...R1_10_SEEDS,
      5_117_011, 5_117_012, 5_117_013,
      ...BRUTAL_10_SEEDS,
    ]);
    expect(BRUTAL_10_B_SEEDS.every((seed) => !existingSeeds.has(seed))).toBe(true);
    expect(BRUTAL_10_B_SEEDS).toEqual(Array.from({ length: 10 }, (_unused, index) => 6_206_001 + index));
    expect(BRUTAL_10_B_SEEDS.every((seed) =>
      !D466_GENERATED_ROOM_OVERRIDES.some((override) => Number(override.seed) === seed))).toBe(true);
    expect(D466_GENERATED_ROOM_OVERRIDES.map((override) => override.seed)).toEqual([
      6_204_004, 6_204_006, 6_204_009,
    ]);
  });

  it.each(BRUTAL_10_B_SEEDS)(
    'pins seed %s only after proving every brutal membership property from state',
    (seed) => {
      const index = seed - BRUTAL_10_B_SEEDS[0];
      const fixture = fixtureAt(index);
      expect(fixture.room.spec.seed).toBe(seed);
      assertBrutalMembership(fixture.room, fixture.bytes, FIXTURE_DIGESTS[seed]);
    },
  );

  describe.each(EXECUTION_FIXTURE_FAMILIES)('%s frozen-room execution membership', (_basis, paths) => {
    it.each(paths)('executes round 1 for %s under the unattended scripted-party policy', (path) => {
      const result = executeFrozenRound(executionFixtureAt(path));

      expect(result.outcome).toBe('executed');
      assertRoundsExecuted([result]);
    });
  });

  it('replays end_turn after draining the final unattended legendary-action window', () => {
    const activeMonster = monsterProfile('boundary-active-monster', { initiativeBonus: 100 });
    const legendaryBase = monsterCombatantProfile(UNICORN, {
      combatantId: 'combatant:boundary-legendary-monster',
      tokenId: 'token:boundary-legendary-monster',
    });
    const legendaryMonster = {
      ...legendaryBase,
      rules: { ...legendaryBase.rules, initiativeBonus: 50 },
    };
    const player = playerProfile('boundary-player', { initiativeBonus: 0 });
    const profiles = [activeMonster, legendaryMonster, player];
    const initial = createEncounter({
      bounds: { columns: 16, rows: 5 },
      combatants: profiles,
      tokens: [
        placedToken(activeMonster, 0, 1),
        placedToken(legendaryMonster, 5, 1),
        placedToken(player, 13, 1),
      ],
      config: { initiativeMode: 'per_combatant' },
    });
    const started = reduceEncounter(initial, { type: 'roll_initiative' }, mulberry32(62_060_009)).state;
    expect(started.activeCombatant).toBe(activeMonster.id);

    const queued = reduceEncounter(started, {
      type: 'end_turn', actor: activeMonster.id,
    }, mulberry32(62_060_010)).state;
    const [window] = queued.pendingDecisions;
    expect(window).toMatchObject({
      kind: 'legendary_action_window',
      combatant: legendaryMonster.id,
      boundary: { activeCombatant: activeMonster.id, round: 1 },
    });
    if (window === undefined || window.kind !== 'legendary_action_window') {
      throw new Error('Minimal boundary fixture did not queue its legendary-action window.');
    }
    const passedWithoutReplay = reduceEncounter(queued, {
      type: 'resolve_pending_decision', decisionId: window.id, optionId: 'pass',
    }, mulberry32(62_060_011)).state;
    expect(passedWithoutReplay.pendingDecisions).toEqual([]);
    expect(passedWithoutReplay.activeCombatant).toBe(activeMonster.id);
    expect(passedWithoutReplay.eventLog.some((event) =>
      event.type === 'turn_ended' && event.combatant === activeMonster.id)).toBe(false);

    const planningState = projectFutureMonsterTurns(started, [activeMonster.id]);
    const dodge = availableEngineActorOptions(planningState, activeMonster.id, OFFER_ENVIRONMENT)
      .find((option) => option.actionSlots.some((slot) =>
        slot.slot === 'main' && slot.use.kind === 'dodge'));
    if (dodge === undefined) throw new Error('Minimal boundary fixture omitted Dodge.');
    const proposal: EngineTurnProposal = {
      actorId: activeMonster.id,
      expectedRevision: started.revision,
      primaryOptionId: dodge.optionId,
      fallbackOptionId: null,
      reason: 'Exercise the unattended legendary-window boundary.',
      overrideJustification: null,
    };
    const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
    if (!resolution.valid) throw new Error('Minimal boundary fixture could not resolve Dodge.');
    const session = new EngineRoundSession(
      started,
      mulberry32(62_060_012),
      { kind: 'unattended', askDefault: 'decline' },
      OFFER_ENVIRONMENT,
    );

    session.applyResolvedMechanics([{
      proposal,
      option: resolution.option,
      primaryOption: resolution.primaryOption,
      fallbackOption: resolution.fallbackOption,
      mechanics: resolution.mechanics,
      selectedBranch: resolution.selectedBranch,
    }], null);

    const advanced = session.currentState();
    expect(advanced.pendingDecisions).toEqual([]);
    expect(advanced.activeCombatant).toBe(legendaryMonster.id);
    expect(advanced.activeInitiativeIndex).toBe(1);
    expect(advanced.eventLog.some((event) =>
      event.type === 'turn_ended' && event.combatant === activeMonster.id)).toBe(true);
  });

  it('rejects rather than skips an execution_failed row from a seeded fixture', () => {
    const result = executeFrozenRound(
      executionFixtureAt('tests/fixtures/arena-basis-brutal-b/seed-6206009.json'),
      { kind: 'dm_attended' },
    );

    expect(result.outcome).toBe('execution_failed');
    expect(result.state.pendingDecisions.length).toBeGreaterThan(0);
    expect(() => assertRoundsExecuted([result])).toThrow('Seed 6206009 failed round execution');
  });

  it('fails structurally when a brutal-named fixture does not satisfy the membership predicates', () => {
    const fixture = fixtureAt(0);
    const structurallyInvalid: GeneratedRoom = {
      ...fixture.room,
      spec: { ...fixture.room.spec, terrain: [] },
    };
    expect(() => assertBrutalMembership(
      structurallyInvalid,
      fixture.bytes,
      FIXTURE_DIGESTS[BRUTAL_10_B_SEEDS[0]],
    )).toThrow();
  });
});
tests/unit/tools/dist-build-cache.test.ts:5:  existsSync,
tests/unit/tools/dist-build-cache.test.ts:162:    "import { appendFileSync, existsSync, mkdirSync, readFileSync, rmSync, writeFileSync } from 'node:fs';",
tests/unit/tools/dist-build-cache.test.ts:165:    "const count = existsSync(countFile) ? Number(readFileSync(countFile, 'utf8')) + 1 : 1;",
tests/unit/tools/dist-build-cache.test.ts:168:    "if (existsSync('node_modules/mutate-input')) appendFileSync('src/app.ts', '// changed during build\\n');",
tests/unit/tools/dist-build-cache.test.ts:169:    "const commit = existsSync('node_modules/wrong-head') ? '0'.repeat(40) : head;",
tests/unit/tools/dist-build-cache.test.ts:249:  const command = existsSync(npmCliCandidate) ? process.execPath : 'npm';
tests/unit/tools/dist-build-cache.test.ts:250:  const args = existsSync(npmCliCandidate) ? [npmCliCandidate, 'run', 'build'] : ['run', 'build'];
tests/unit/tools/dist-build-cache.test.ts:546:    expect(existsSync(cacheRoot(fixture))).toBe(false);
tests/unit/tools/dist-build-cache.test.ts:706:    expect(existsSync(join(cacheRoot(fixture), `${before}.json`))).toBe(false);
tests/unit/tools/dist-build-cache.test.ts:707:    const entries = existsSync(cacheRoot(fixture)) ? readdirSync(cacheRoot(fixture), { recursive: true }) : [];
tests/unit/tools/ai-dm-legacy-invariance.test.ts:256:    return inputs.fixtures.readText(runnerOraclePath);
tests/unit/tools/ai-dm-legacy-invariance.test.ts:1097:  const root = record(JSON.parse(inputs.fixtures.readText(legacyFixturePath)) as unknown, 'legacy fixture');
tests/unit/tools/generate-arena-basis.test.ts:52:    writeFileSync(join(path, `level-${String(level)}.json`), inputs.fixtures.readText(fixtures[level]), {
tests/unit/tools/gate-runners.test.ts:7:  existsSync,
tests/unit/tools/gate-runners.test.ts:170:  const logs = existsSync(logPath)
tests/unit/tools/scrape-fetcher.test.ts:169:    // a perfectly periodic crawler is the behaviour jitter exists to avoid. At
tests/unit/tools/ai-dm-knowledge-base.test.ts:27:  existsSync,
tests/unit/tools/ai-dm-knowledge-base.test.ts:97:const fixtureText = (path: (typeof fixturePaths)[number]): string => inputs.fixtures.readText(path);
tests/unit/tools/ai-dm-knowledge-base.test.ts:186:    expect(indexedPaths.every((path) => existsSync(resolve(process.cwd(), path)))).toBe(true);
tests/unit/tools/ai-dm-knowledge-base.test.ts:221:    expect(substituted.every((path) => isAbsolute(path) && existsSync(path))).toBe(true);
tests/unit/tools/ai-dm-knowledge-base.test.ts:378:    ), String(inputs.fixtures.readText(D569_AI_DM_KB_ROOT)));
tests/unit/tools/ai-dm-knowledge-base.test.ts:381:      ...D569_AI_DM_KB_COMPONENT_PATHS.slice(1).map((path) => inputs.fixtures.readText(path)),
tests/unit/tools/ai-dm-knowledge-base.test.ts:395:        sha256(inputs.fixtures.readText(path))),
tests/unit/tools/ai-dm-knowledge-base.test.ts:426:    const guide = inputs.fixtures.readText(`${AI_DM_KB_FIXTURE_DIRECTORY}/d569/protocol.md`);
tests/unit/tools/ai-dm-knowledge-base.test.ts:479:    const rawManifest = inputs.fixtures.readText(D569_AI_DM_KB_PROVENANCE);
tests/unit/tools/ai-dm-knowledge-base.test.ts:488:      D569_AI_DM_KB_COMPONENT_PATHS.map((path) => sha256(inputs.fixtures.readText(path))),
tests/unit/tools/ai-dm-knowledge-base.test.ts:496:    expect(inputs.fixtures.readText(D569_AI_DM_KB_ROOT).replace(/\s+/gu, ' ').trim())
tests/unit/tools/ai-dm-knowledge-base.test.ts:503:    const srdLines = inputs.srdText.readText(srdFixture).split('\n');
tests/unit/tools/ai-dm-knowledge-base.test.ts:516:      const markdownSections = [...inputs.fixtures.readText(component.path)
tests/unit/tools/ai-dm-knowledge-base.test.ts:546:    const guide = inputs.fixtures.readText(`${AI_DM_KB_FIXTURE_DIRECTORY}/d569/protocol.md`);
tests/unit/tools/ai-dm-knowledge-base.test.ts:554:    const guide = inputs.fixtures.readText(`${AI_DM_KB_FIXTURE_DIRECTORY}/d569/protocol.md`);
tests/unit/tools/scraped-output-is-never-committed.test.ts:2:import { existsSync, readFileSync, readdirSync, statSync } from '../../helpers/test-filesystem';
tests/unit/tools/scraped-output-is-never-committed.test.ts:112:      if (!existsSync(absolute) || !statSync(absolute).isFile()) {
tests/unit/tools/scraped-output-is-never-committed.test.ts:138:      expect(existsSync(join(repoRoot, path)), `${path} is allowlisted but absent`).toBe(
tests/unit/tools/scraped-output-is-never-committed.test.ts:146:    if (!existsSync(dist)) {
tests/unit/tools/scraped-output-is-never-committed.test.ts:161:    expect(files.length, 'dist/ exists but is empty').toBeGreaterThan(0);
tests/unit/tools/scrape-build.test.ts:377:    // The premise `prune()` exists to fix: seed() alone can never remove a
tests/unit/vtt/room-roster-preflight.test.ts:48:const inputs = declareTestInputs({ fixtures: [...BRUTAL_FIXTURE_PATHS, ...HARD_FIXTURE_PATHS] });
tests/unit/vtt/room-roster-preflight.test.ts:120:  return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
tests/unit/vtt/room-roster-preflight.test.ts:134:      const bytes = inputs.fixtures.readText(expected.path);
tests/unit/vtt/room-roster-preflight.test.ts:194:        sha256(inputs.fixtures.readText(path as keyof typeof UNCHANGED_FIXTURE_DIGESTS)),
tests/unit/tools/d569-second-family-manifest.test.ts:44:const inputs = declareTestInputs({ fixtures: [MANIFEST_PATH, ...ALL_FIXTURE_PATHS] });
tests/unit/tools/d569-second-family-manifest.test.ts:47:  inputs.fixtures.readText(path),
tests/unit/tools/d569-second-family-manifest.test.ts:51:  return JSON.parse(inputs.fixtures.readText(MANIFEST_PATH)) as D569SecondFamilyManifest;
tests/unit/tools/d569-second-family-manifest.test.ts:55:  return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
tests/unit/tools/d569-second-family-manifest.test.ts:83:  if (entry === undefined) throw new Error(`missing fixture entry ${path}`);
tests/unit/tools/d569-second-family-manifest.test.ts:113:      const committed = JSON.parse(inputs.fixtures.readText(path)) as unknown;
tests/unit/tools/d569-second-family-manifest.test.ts:138:    expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
tests/unit/tools/d569-second-family-manifest.test.ts:146:      expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
tests/unit/vtt/engine-opportunity-movement-intel.test.ts:50:  const state = decodeArenaFixtureText(inputs.fixtures.readText(
tests/unit/vtt/engine-opportunity-movement-intel.test.ts:57:  const state = decodeArenaFixtureText(inputs.fixtures.readText(
tests/unit/vtt/engine-opportunity-movement-intel.test.ts:73:    const failedRound = JSON.parse(inputs.fixtures.readText(
tests/unit/vtt/engine-opportunity-movement-intel.test.ts:264:  it('refuses every R02 Dodge default while retaining Dodge when no offense or approach exists', () => {
tests/unit/tools/ai-dm-conversation.test.ts:107:const kbInputs = declareTestInputs({ fixtures: [
tests/unit/tools/ai-dm-conversation.test.ts:3147:    const root = kbInputs.fixtures.readText('tests/fixtures/ai-dm-kb/ai-dm-core.md');
tests/unit/tools/ai-dm-conversation.test.ts:3148:    const tactics = kbInputs.fixtures.readText('tests/fixtures/ai-dm-kb/tactics.md');
tests/unit/tools/ai-dm-conversation.test.ts:3216:      skillHash: sha256(kbInputs.fixtures.readText(
tests/unit/vtt/handoff-publish.test.ts:2:  existsSync, mkdirSync, mkdtempSync, readFileSync, readdirSync, rmSync, statSync, symlinkSync, writeFileSync,
tests/unit/vtt/handoff-publish.test.ts:77:    existsSync: () => true,
tests/unit/vtt/handoff-publish.test.ts:239:    expect(existsSync(join(readyOnly, 'contracts/v1/protocol.schema.json'))).toBe(false);
tests/unit/vtt/handoff-publish.test.ts:253:    expect(existsSync(join(handoffRoot, 'contracts/v1/READY.json'))).toBe(false);
tests/unit/vtt/handoff-publish.test.ts:255:    expect(existsSync(join(handoffRoot, 'contracts/v1/READY.json'))).toBe(true);
tests/unit/vtt/handoff-publish.test.ts:276:    expect(existsSync(join(contractDirectory, 'protocol.schema.json'))).toBe(false);
tests/unit/vtt/handoff-publish.test.ts:292:    expect(existsSync(join(finalRace, 'contracts/v1/READY.json'))).toBe(false);
tests/unit/vtt/unicorns-blessing-consistency.test.ts:163:    'attaches $choice.kind to its owning spell when an earlier cast exists',
tests/unit/vtt/room-generator.test.ts:168:  const loaded = loadExternalPartyPackBytes(inputs.fixtures.readText(paths[level]));
tests/unit/vtt/room-generator.test.ts:478:    const fixtureBytes = inputs.fixtures.readText(path);
tests/unit/vtt/room-generator.test.ts:483:    const fixtureBytes = inputs.fixtures.readText('tests/fixtures/arena-basis/seed-3943001.json');
tests/unit/vtt/room-generator.test.ts:493:    const fixtureBytes = inputs.fixtures.readText(path);
tests/unit/vtt/room-generator.test.ts:502:    const room = JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
tests/unit/vtt/room-generator.test.ts:510:    const fixtureBytes = inputs.fixtures.readText(path);
tests/unit/vtt/room-generator.test.ts:711:    const original = JSON.parse(inputs.fixtures.readText(
tests/unit/tools/d569-blind-experiment.test.ts:106:const inputs = declareTestInputs({ fixtures: ALL_INPUT_PATHS });
tests/unit/tools/d569-blind-experiment.test.ts:109:  inputs.fixtures.readText(path),
tests/unit/tools/d569-blind-experiment.test.ts:279:function readText(path: string): string {
tests/unit/tools/d569-blind-experiment.test.ts:286:  return JSON.parse(readText(D569_EXPERIMENT_MANIFEST_PATH)) as D569ExperimentManifest;
tests/unit/tools/d569-blind-experiment.test.ts:291:    readText: (path) => readOverride?.(path) ?? readText(path),
tests/unit/tools/d569-blind-experiment.test.ts:293:      readFixture: (path) => readOverride?.(path) ?? readText(path),
tests/unit/tools/d569-blind-experiment.test.ts:294:      regenerate: (seed, difficulty) => readText(
tests/unit/tools/d569-blind-experiment.test.ts:628:    const changedBytes = `${readText(PRIMARY_HARD_PATHS[0])} `;
tests/unit/tools/d569-blind-experiment.test.ts:725:    const changedSecond = `${readText(SECOND_HARD_PATHS[0])} `;
tests/unit/tools/d569-blind-experiment.test.ts:845:    const second = JSON.parse(readText(D569_SECOND_FAMILY_MANIFEST_PATH)) as {
tests/unit/tools/scrape-parser.test.ts:118:    // assertion exists to prevent.
tests/unit/vtt/session-persistence.test.ts:192:    const bytes = declaredInputs.fixtures.readText(path);
tests/unit/vtt/session-persistence.test.ts:193:    const recorded = declaredInputs.fixtures.readText(
tests/unit/vtt/session-persistence.test.ts:213:    const baseline = JSON.parse(declaredInputs.fixtures.readText(
tests/unit/vtt/session-persistence.test.ts:459:      .readText('tests/fixtures/vtt/pre-agent-binding-v7-save.json')
tests/unit/tools/d586-mutation-contract.test.ts:12:const fixtureInputs = declareTestInputs({ fixtures: [MUTATION_FIXTURE, PLAN_FIXTURE] });
tests/unit/tools/d586-mutation-contract.test.ts:13:const mutationRegistry = fixtureInputs.fixtures.readText(MUTATION_FIXTURE);
tests/unit/tools/d586-mutation-contract.test.ts:14:const ownershipPlan = fixtureInputs.fixtures.readText(PLAN_FIXTURE);
tests/unit/vtt/challenge-feasibility-production.test.ts:20:    case 5831001: return Promise.resolve(inputs.fixtures.readText(
tests/unit/vtt/challenge-feasibility-production.test.ts:23:    case 5831002: return Promise.resolve(inputs.fixtures.readText(
tests/unit/vtt/challenge-feasibility-production.test.ts:26:    case 5831003: return Promise.resolve(inputs.fixtures.readText(
tests/unit/vtt/challenge-feasibility.test.ts:68:    case 5831001: return Promise.resolve(inputs.fixtures.readText(
tests/unit/vtt/challenge-feasibility.test.ts:71:    case 5831002: return Promise.resolve(inputs.fixtures.readText(
tests/unit/vtt/challenge-feasibility.test.ts:74:    case 5831003: return Promise.resolve(inputs.fixtures.readText(
tests/unit/vtt/challenge-feasibility.test.ts:214:  JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831003.json')) as unknown,
tests/unit/vtt/challenge-feasibility.test.ts:218:  JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831004.json')) as unknown,
tests/unit/vtt/challenge-feasibility.test.ts:382:      JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831001.json')) as unknown,
tests/unit/vtt/challenge-feasibility.test.ts:519:        JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831001.json')) as unknown,
tests/unit/vtt/challenge-feasibility.test.ts:569:      JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831003.json')) as unknown,
tests/unit/vtt/challenge-feasibility.test.ts:601:      JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831001.json')) as unknown,
tests/unit/vtt/challenge-feasibility.test.ts:621:      JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831001.json')) as unknown,
tests/unit/vtt/challenge-feasibility.test.ts:932:      JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831003.json')) as unknown,
tests/unit/vtt/challenge-feasibility.test.ts:963:      JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831001.json')) as unknown,
tests/unit/vtt/challenge-feasibility.test.ts:982:      JSON.parse(inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831001.json')) as unknown,
tests/unit/vtt/challenge-room-fixtures.test.ts:52:const testInputs = declareTestInputs({ fixtures: FIXTURE_INPUTS });
tests/unit/vtt/challenge-room-fixtures.test.ts:60:  return JSON.parse(testInputs.fixtures.readText(path)) as unknown;
tests/unit/vtt/stable-dom-render.test.ts:490:  it('copies mutable state only when that property exists on both relevant controls', () => {
tests/unit/tools/engine-mcp-handler.test.ts:1765:    expect(JSON.stringify(context)).not.toContain('No legal engine option exists for combatant:cleric');
tests/unit/tools/rl-extract-sft.test.ts:22:const fixtureSource = inputs.fixtures.readText(fixturePath);
tests/unit/vtt/scripted-party-round.test.ts:53:    const loaded = loadExternalPartyPackBytes(heldoutInputs.fixtures.readText(
tests/unit/tools/d584-contract-inventory.test.ts:26:const fixtureInputs = declareTestInputs({ fixtures: [PLAN_FIXTURE] });
tests/unit/tools/d584-contract-inventory.test.ts:27:const ownershipPlan = fixtureInputs.fixtures.readText(PLAN_FIXTURE);
tests/unit/tools/ai-dm-skills.test.ts:35:    const text = inputs.fixtures.readText(path);
tests/unit/tools/ai-dm-rerun-packet.test.ts:37:  return inputs.fixtures.readText(path).trim().split('\n').map((line) => {
tests/unit/vtt/party-pack.test.ts:2132:  it('enforces allowPartial by refusing the whole pack when any gap exists', () => {
tests/unit/vtt/engine-state-capsule.test.ts:1:import { existsSync, readFileSync } from '../../helpers/test-filesystem';
tests/unit/vtt/engine-state-capsule.test.ts:92:  ].find((candidate) => existsSync(candidate)) ?? null;
tests/unit/vtt/speculative-planning.test.ts:87:  return decodeArenaFixtureText(hardBasisInputs.fixtures.readText(path));
tests/unit/vtt/room-generator-los-cover.test.ts:299:    const authoredText = inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831001.json');
tests/unit/vtt/room-generator-los-cover.test.ts:311:      const fixtureText = inputs.fixtures.readText(path);
tests/unit/tools/typescript-compiler-is-never-in-encounter-bundle.test.ts:1:import { existsSync, readFileSync } from '../../helpers/test-filesystem';
tests/unit/tools/typescript-compiler-is-never-in-encounter-bundle.test.ts:63:  ].find((candidate) => existsSync(candidate)) ?? null;
tests/unit/vtt/session-command-transaction.test.ts:2:  existsSync,
tests/unit/vtt/session-command-transaction.test.ts:750:    if (!existsSync(candidate)) throw new Error(`Raw asset does not exist: ${specifier}.`);
tests/unit/vtt/windows-probe.test.ts:6:  existsSync, mkdtempSync, readFileSync, readdirSync, writeFileSync,
tests/unit/vtt/windows-probe.test.ts:112:    expect(existsSync(join(handoffRoot, '.vtt-interop'))).toBe(false);
tests/unit/vtt/arena-basis-brutal-b.test.ts:111:const inputs = declareTestInputs({ fixtures: EXECUTION_FIXTURE_PATHS });
tests/unit/vtt/arena-basis-brutal-b.test.ts:402:  const bytes = inputs.fixtures.readText(path);
tests/unit/vtt/arena-basis-brutal-b.test.ts:407:  return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
tests/unit/vtt/watabou-adapter.test.ts:11:const inputs = declareTestInputs({ fixtures: [FIXTURE] });
tests/unit/vtt/watabou-adapter.test.ts:15:    const source: unknown = JSON.parse(inputs.fixtures.readText(FIXTURE));
tests/unit/vtt/handoff-report.test.ts:6:  existsSync,
tests/unit/vtt/handoff-report.test.ts:450:    expect(existsSync(join(handoffRoot, 'reports/windows'))).toBe(false);
tests/unit/vtt/handoff-report.test.ts:1290:    expect(existsSync(join(handoffRoot, 'reports'))).toBe(false);
tests/unit/vtt/handoff-report.test.ts:1448:      { fileExists: () => true, commandPath: () => '/resolved/command', packageScripts: {} },
tests/unit/vtt/handoff-report.test.ts:1465:      fileExists: (path) => missing === null || !path.endsWith(missing),
tests/unit/vtt/handoff-report.test.ts:1512:      fileExists: () => true,
tests/unit/vtt/handoff-report.test.ts:1574:    expect(existsSync(passed.path)).toBe(true);
tests/unit/vtt/engine-boundary.test.ts:7:  existsSync, mkdtempSync, readFileSync, rmSync, writeFileSync,
tests/unit/vtt/engine-boundary.test.ts:44:    if (existsSync(candidate)) return candidate;
tests/unit/vtt/art-stage.test.ts:7:  closeSync, existsSync, mkdirSync, mkdtempSync, openSync, readFileSync, renameSync, rmSync, symlinkSync,
tests/unit/vtt/art-stage.test.ts:98:          expect(existsSync(join(directory, 'request.json'))).toBe(true);
tests/unit/vtt/art-stage.test.ts:99:          expect(existsSync(join(directory, 'result.json'))).toBe(true);
tests/unit/vtt/art-stage.test.ts:100:          expect(existsSync(join(directory, 'files', value.firstImage))).toBe(true);
tests/unit/vtt/art-stage.test.ts:101:          expect(existsSync(manifestPath(value.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:127:      expect(existsSync(manifestPath(value.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:137:    expect(existsSync(manifestPath(changed.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:152:    expect(existsSync(manifestPath(swapped.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:169:    expect(existsSync(manifestPath(finalSwap.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:185:    expect(existsSync(manifestPath(parentSwap.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:193:    expect(existsSync(manifestPath(oversized.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:214:    expect(existsSync(manifestPath(value.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:223:    expect(existsSync(join(interrupted.root, 'art/review', requestId, 'request.json'))).toBe(false);
tests/unit/vtt/art-stage.test.ts:224:    expect(existsSync(manifestPath(interrupted.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:248:      expect(existsSync(manifestPath(value.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:308:    expect(existsSync(manifestPath(value.root))).toBe(false);
tests/unit/vtt/art-stage.test.ts:335:    expect(existsSync(manifestPath(value.root))).toBe(false);
tests/unit/vtt/tactical-evaluator-r02.test.ts:76:  inputs.fixtures.readText('tests/fixtures/arena-basis-hard/seed-5117009.json');
tests/unit/vtt/art-request.test.ts:5:  existsSync, mkdirSync, mkdtempSync, readFileSync, readdirSync, writeFileSync,
tests/unit/vtt/art-request.test.ts:70:    expect(existsSync(`${destination}.partial.fixed`)).toBe(false);
tests/unit/vtt/art-request.test.ts:81:        expect(existsSync(path)).toBe(false);
tests/unit/vtt/art-request.test.ts:85:    expect(existsSync(destination)).toBe(false);
tests/unit/vtt/art-request.test.ts:86:    expect(existsSync(`${destination}.partial.fixed`)).toBe(false);
import { readdirSync, readFileSync } from 'node:fs';
import { resolve, sep } from 'node:path';

const testInputBrand: unique symbol = Symbol('test-input');
const recorderStateSymbol = Symbol.for('dnd.verdict-fs-recorder');

type FixturePath = `tests/fixtures/${string}`;
type SchemaSqlPath =
  | `drizzle/${string}.sql`
  | `src/db/${string}.sql`
  | `tests/fixtures/${string}.sql`;
type SrdTextPath = `docs/srd/${string}`;
type GuidePath = `docs/guides/${string}`;
type PublicDataPath = `public/${string}`;
type ContentPath = `content/${string}`;
type ContentDirectoryPath = `content/${string}`;

export interface TestInputSpec {
  readonly fixtures?: readonly FixturePath[];
  readonly schemaSql?: readonly SchemaSqlPath[];
  readonly srdText?: readonly SrdTextPath[];
  readonly guides?: readonly GuidePath[];
  readonly publicData?: readonly PublicDataPath[];
  readonly content?: readonly ContentPath[];
  readonly contentDirectories?: readonly ContentDirectoryPath[];
}

type TestInputCategory = Exclude<keyof TestInputSpec, 'contentDirectories'>;
type DeclaredPath<
  Spec extends TestInputSpec,
  Category extends TestInputCategory,
> = Spec[Category] extends readonly (infer Path extends string)[] ? Path : never;
type DeclaredContentDirectory<Spec extends TestInputSpec> =
  Spec['contentDirectories'] extends readonly (infer Path extends string)[] ? Path : never;

export type TestInputText<
  Category extends TestInputCategory,
  Path extends string,
> = string & {
  readonly [testInputBrand]: {
    readonly category: Category;
    readonly path: Path;
  };
};

export type TestInputBytes<
  Category extends TestInputCategory,
  Path extends string,
> = Uint8Array & {
  readonly [testInputBrand]: {
    readonly category: Category;
    readonly path: Path;
  };
};

export type TestInputDirectoryEntries<Path extends string> = readonly string[] & {
  readonly [testInputBrand]: {
    readonly category: 'contentDirectories';
    readonly path: Path;
  };
};

export interface DeclaredInputReader<
  Category extends TestInputCategory,
  Path extends string,
> {
  readText<Selected extends Path>(
    path: Selected,
    encoding?: 'utf8',
  ): TestInputText<Category, Selected>;
  readBytes<Selected extends Path>(path: Selected): TestInputBytes<Category, Selected>;
}

export interface DeclaredContentDirectoryReader<Path extends string> {
  list<Selected extends Path>(path: Selected): TestInputDirectoryEntries<Selected>;
}

export type DeclaredTestInputs<Spec extends TestInputSpec> = {
  readonly [Category in TestInputCategory]: DeclaredInputReader<
    Category,
    DeclaredPath<Spec, Category>
  >;
} & {
  readonly contentDirectories: DeclaredContentDirectoryReader<DeclaredContentDirectory<Spec>>;
};

interface RecorderFileState {
  declaredInputs?: Set<string>;
  readonly testFile: string;
}

interface RecorderState {
  current?: RecorderFileState;
}

const categoryPrefixes = {
  fixtures: ['tests/fixtures/'],
  schemaSql: ['drizzle/', 'src/db/', 'tests/fixtures/'],
  srdText: ['docs/srd/'],
  guides: ['docs/guides/'],
  publicData: ['public/'],
  content: ['content/'],
} as const satisfies Record<TestInputCategory, readonly string[]>;

function repositoryRoot(): string {
  return process.env.VERDICT_REPOSITORY_ROOT ?? process.cwd();
}

function validatePath(category: TestInputCategory, path: string): void {
  const prefixes = categoryPrefixes[category];
  const hasAllowedPrefix = prefixes.some((prefix) => path.startsWith(prefix));
  if (
    !hasAllowedPrefix ||
    path.includes('\\') ||
    path.split('/').includes('..') ||
    (category === 'schemaSql' && !path.endsWith('.sql'))
  ) {
    throw new TypeError(`Invalid ${category} test input path: ${path}`);
  }
}

function absoluteInputPath(path: string): string {
  const root = resolve(repositoryRoot());
  const absolute = resolve(root, path);
  if (absolute === root || !absolute.startsWith(`${root}${sep}`)) {
    throw new TypeError(`Test input escapes the repository: ${path}`);
  }
  return absolute;
}

function validateContentDirectoryPath(path: string): void {
  if (
    !path.startsWith('content/') ||
    path === 'content/' ||
    path.endsWith('/') ||
    path.includes('\\') ||
    path.split('/').includes('..')
  ) {
    throw new TypeError(`Invalid content test input directory: ${path}`);
  }
}

function reader<
  Category extends TestInputCategory,
  Path extends string,
>(category: Category, allowed: ReadonlySet<string>): DeclaredInputReader<Category, Path> {
  const assertAllowed = (path: string): void => {
    if (!allowed.has(path)) {
      throw new TypeError(`Undeclared ${category} test input: ${path}`);
    }
  };
  return {
    readText<Selected extends Path>(path: Selected): TestInputText<Category, Selected> {
      assertAllowed(path);
      return readFileSync(absoluteInputPath(path), 'utf8') as TestInputText<Category, Selected>;
    },
    readBytes<Selected extends Path>(path: Selected): TestInputBytes<Category, Selected> {
      assertAllowed(path);
      return new Uint8Array(readFileSync(absoluteInputPath(path))) as TestInputBytes<Category, Selected>;
    },
  };
}

function contentDirectoryReader<Path extends string>(
  allowed: ReadonlySet<string>,
): DeclaredContentDirectoryReader<Path> {
  return {
    list<Selected extends Path>(path: Selected): TestInputDirectoryEntries<Selected> {
      if (!allowed.has(path)) {
        throw new TypeError(`Undeclared content test input directory: ${path}`);
      }
      return readdirSync(absoluteInputPath(path), { encoding: 'utf8' }) as unknown as
        TestInputDirectoryEntries<Selected>;
    },
  };
}

/**
 * Declares every stable repository input a test file may read. The returned
 * readers accept only paths enumerated in this spec, and recording runs attach
 * the declaration to the recorder state for the current test file.
 */
export function declareTestInputs<const Spec extends TestInputSpec>(
  spec: Spec,
): DeclaredTestInputs<Spec> {
  const byCategory = {
    fixtures: new Set<string>(spec.fixtures ?? []),
    schemaSql: new Set<string>(spec.schemaSql ?? []),
    srdText: new Set<string>(spec.srdText ?? []),
    guides: new Set<string>(spec.guides ?? []),
    publicData: new Set<string>(spec.publicData ?? []),
    content: new Set<string>(spec.content ?? []),
  } satisfies Record<TestInputCategory, Set<string>>;
  const contentDirectories = new Set<string>(spec.contentDirectories ?? []);

  const declared = new Set<string>();
  for (const category of Object.keys(byCategory) as TestInputCategory[]) {
    for (const path of byCategory[category]) {
      validatePath(category, path);
      const observation = `file:${path}`;
      if (declared.has(observation)) {
        throw new TypeError(`Test input is declared more than once: ${path}`);
      }
      declared.add(observation);
    }
  }
  for (const path of contentDirectories) {
    validateContentDirectoryPath(path);
    const observation = `directory:${path}`;
    if (declared.has(observation)) {
      throw new TypeError(`Test input directory is declared more than once: ${path}`);
    }
    declared.add(observation);
  }

  const recorder = (globalThis as typeof globalThis & {
    [recorderStateSymbol]?: RecorderState;
  })[recorderStateSymbol];
  if (recorder?.current !== undefined) {
    if (recorder.current.declaredInputs !== undefined) {

exec
/bin/bash -lc "rg -n \"generatedLineIsOpen|traceCombatantLine|mechanics\\.finalPosition|actionSlots.*target|target.*actionSlots|use_world_object|movementCostFeet > 0\" src tests/unit/vtt tools | head -300
rg -n \"availableEngineActorOptions\\(|resolveEngineActorOption\\(\" tests/unit/vtt | head -150
rg -n \"function brutalRoomMembershipViolations|brutal_productivity|generatedLineIsOpen\" tools/d569-second-family-manifest.ts src/vtt/room-generator.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
src/combat/visibility-field.ts:8:  traceCombatantLineToCells,
src/combat/visibility-field.ts:301:    return traceCombatantLineToCells(context.state, context.observer.id, [target]);
src/combat/visibility-field.ts:303:  return traceCombatantLineToCells(context.state, context.observer.id, [target], {
tests/unit/vtt/turn-exhaustion-coordinator.test.ts:145:        actionSlots: [{ slot: 'main', kind: selectedBranch === 'fallback' ? 'end_turn' : 'dodge', actionId: engineActionId(selectedBranch === 'fallback' ? 'end_turn' : 'dodge'), spellId: null, targetIds: [], objectId: null, omittedRiders: [] }],
tools/d569-second-family-manifest.ts:289:        return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
tools/d569-second-family-manifest.ts:292:            slot.kind === 'use_world_object'));
tools/ai-dm-screenshot-probe.ts:16:  traceCombatantLine,
tools/ai-dm-screenshot-probe.ts:17:  traceCombatantLineToCells,
tools/ai-dm-screenshot-probe.ts:717:  traceCombatantLine(
tools/ai-dm-screenshot-probe.ts:722:  traceCombatantLineToCells(
tools/ai-dm-screenshot-probe.ts:730:  traceCombatantLine,
tools/ai-dm-screenshot-probe.ts:731:  traceCombatantLineToCells,
tools/ai-dm-screenshot-probe.ts:759:      const trace = tracer.traceCombatantLine(state, source.id, target.id);
tools/ai-dm-screenshot-probe.ts:786:        const trace = tracer.traceCombatantLineToCells(state, source.id, [target]);
tools/los-cover-era-audit.ts:5:import { rasterizeInterveningCells, traceCombatantLine } from '../src/combat/cover';
tools/los-cover-era-audit.ts:202:      const current = traceCombatantLine(state, source, target);
tests/unit/vtt/blind-intent-resolver.test.ts:483:        use: { kind: 'use_world_object', objectId: object.id, actionId: engineActionId('synthetic-use') },
tests/unit/vtt/blind-intent-resolver.test.ts:485:          slot: 'main', kind: 'use_world_object', actionId: engineActionId('synthetic-use'),
tests/unit/vtt/blind-intent-resolver.test.ts:632:        const selected = movement.cells.find((entry) => same(entry.cell, forward.actors[0]!.resolution.mechanics.finalPosition));
tests/unit/vtt/footprint-increment-four.test.ts:74:  'use_world_object',
tests/unit/vtt/footprint-increment-four.test.ts:75:  'dm_use_world_object',
tests/unit/vtt/encounter-session-service.test.ts:384:          ? 'dm_use_world_object'
tests/unit/vtt/encounter-session-service.test.ts:422:          ? 'dm_use_world_object'
tests/unit/vtt/encounter-session-service.test.ts:456:          ? 'dm_use_world_object'
src/combat/encounter.ts:53:  traceCombatantLine,
src/combat/encounter.ts:54:  traceCombatantLineToCells,
src/combat/encounter.ts:7183:  const attackLine = traceCombatantLine(context.state, command.actor, command.target);
src/combat/encounter.ts:7477:  const selectedLine = traceCombatantLineToCells(context.state, command.actor, [command.square]);
src/combat/encounter.ts:8566:  const selectedLine = traceCombatantLine(context.state, command.actor, target);
src/combat/encounter.ts:9051:  const selectedLine = traceCombatantLineToCells(context.state, command.actor, targetCells);
src/combat/encounter.ts:10685:        const selectedLine = traceCombatantLineToCells(context.state, command.actor, [destination]);
src/combat/encounter.ts:11759:  command: Extract<EncounterCommand, { readonly type: 'use_world_object' }>,
src/combat/encounter.ts:11779:  command: Extract<EncounterCommand, { readonly type: 'dm_use_world_object' }>,
src/combat/encounter.ts:12127:    case 'use_world_object':
src/combat/encounter.ts:12130:    case 'dm_use_world_object':
src/combat/coordinator.ts:474:    command: Extract<EncounterCommand, { readonly type: 'dm_use_world_object' }>,
tests/unit/vtt/last-seen.test.ts:8:import { traceCombatantLine } from '../../../src/combat/cover';
tests/unit/vtt/last-seen.test.ts:198:    const blockedTrace = traceCombatantLine(state, blocked.id, subject.id);
tests/unit/vtt/last-seen.test.ts:200:    expect(traceCombatantLine(state, seeing.id, subject.id).blocksSight).toBe(false);
tests/unit/vtt/challenge-room-fixtures.test.ts:6:import { traceCombatantLine } from '../../../src/combat/cover';
tests/unit/vtt/challenge-room-fixtures.test.ts:131:      (selector.movement === 'hold' ? resolved.mechanics.movementCostFeet === 0 : resolved.mechanics.movementCostFeet > 0);
tests/unit/vtt/challenge-room-fixtures.test.ts:217:    expect(traceCombatantLine(state, ogre, 'combatant:fighter' as CombatantId)).toMatchObject({ tier: 'none', blocksSight: false });
tests/unit/vtt/challenge-room-fixtures.test.ts:218:    expect(traceCombatantLine(state, ogre, 'combatant:wizard' as CombatantId)).toMatchObject({ tier: 'half', blocksSight: false });
tests/unit/vtt/challenge-room-fixtures.test.ts:255:      const fighterTrace = traceCombatantLine(state, 'combatant:fighter' as CombatantId, ogre);
tests/unit/vtt/challenge-room-fixtures.test.ts:256:      const clericTrace = traceCombatantLine(state, 'combatant:cleric' as CombatantId, ogre);
tests/unit/vtt/challenge-room-fixtures.test.ts:263:      expect(traceCombatantLine(state, ogre, 'combatant:wizard' as CombatantId)).toMatchObject({ tier: 'none', blocksSight: false });
tests/unit/vtt/challenge-room-fixtures.test.ts:269:      expect(resolved.mechanics.finalPosition).toEqual(placement.end);
tests/unit/vtt/challenge-room-fixtures.test.ts:288:      expect(traceCombatantLine(approached, 'combatant:fighter' as CombatantId, ogre, {
tests/unit/vtt/challenge-room-fixtures.test.ts:291:      expect(traceCombatantLine(approached, 'combatant:cleric' as CombatantId, ogre, {
tests/unit/vtt/challenge-room-fixtures.test.ts:331:    expect(traceCombatantLine(state, scouts[0]!, 'combatant:fighter' as CombatantId)).toMatchObject({ tier: 'half', blocksSight: false });
tests/unit/vtt/challenge-room-fixtures.test.ts:332:    expect(traceCombatantLine(state, scouts[1]!, 'combatant:wizard' as CombatantId)).toMatchObject({ tier: 'three_quarters', blocksSight: false });
tests/unit/vtt/challenge-room-fixtures.test.ts:347:    expect(traceCombatantLine(initial, fighter, guard)).toMatchObject({ tier: 'none', blocksSight: false });
tests/unit/vtt/challenge-room-fixtures.test.ts:353:    expect(traceCombatantLine(initial, scout1, fighter)).toMatchObject({ tier: 'half', blocksSight: false });
tests/unit/vtt/challenge-room-fixtures.test.ts:354:    expect(traceCombatantLine(initial, scout2, wizard)).toMatchObject({ tier: 'three_quarters', blocksSight: false });
tests/unit/vtt/challenge-room-fixtures.test.ts:380:    expect(traceCombatantLine(killed.state, scout1, fighter)).toMatchObject({ tier: 'none', blocksSight: false });
tests/unit/vtt/challenge-room-fixtures.test.ts:381:    expect(traceCombatantLine(killed.state, scout2, wizard)).toMatchObject({ tier: 'three_quarters', blocksSight: false });
tests/unit/vtt/dm-tactical-intel.test.ts:21:import { traceCombatantLine } from '../../../src/combat/cover';
tests/unit/vtt/dm-tactical-intel.test.ts:116:    const trace = traceCombatantLine(state, SCOUT, FIGHTER);
tests/unit/vtt/engine-opportunity-movement-intel.test.ts:139:        finalPosition: resolution.mechanics.finalPosition,
src/combat/world-object-controller.ts:15:      if (command.type !== 'use_world_object') return [];
tests/unit/vtt/composite-turn-proposals.test.ts:383:        slot.use.kind === 'use_world_object' && slot.use.actionId === 'ring-signal-bell'));
tests/unit/vtt/composite-turn-proposals.test.ts:385:    expect(JSON.stringify(offered)).not.toContain('dm_use_world_object');
tests/unit/vtt/composite-turn-proposals.test.ts:398:      option.actionSlots.some((slot) => slot.use.kind === 'use_world_object'))).toBe(false);
tests/unit/vtt/room-generator-los-cover.test.ts:5:  traceCombatantLine,
tests/unit/vtt/room-generator-los-cover.test.ts:6:  traceCombatantLineToCells,
tests/unit/vtt/room-generator-los-cover.test.ts:22:  generatedLineIsOpen,
tests/unit/vtt/room-generator-los-cover.test.ts:88:      traceCombatantLine(state, source.profile.id, target.profile.id))),
tests/unit/vtt/room-generator-los-cover.test.ts:90:      traceCombatantLine(state, source.profile.id, target.profile.id))),
tests/unit/vtt/room-generator-los-cover.test.ts:105:          traces.push(traceCombatantLineToCells(state, source.profile.id, [target]));
tests/unit/vtt/room-generator-los-cover.test.ts:188:    const current = traceCombatantLine(state, actor.profile.id, opponent.profile.id);
tests/unit/vtt/room-generator-los-cover.test.ts:199:        const changed = traceCombatantLine(state, actor.profile.id, opponent.profile.id, {
tests/unit/vtt/room-generator-los-cover.test.ts:222:      if (!opponents.some((opponent) => !traceCombatantLine(
tests/unit/vtt/room-generator-los-cover.test.ts:247:      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
tests/unit/vtt/room-generator-los-cover.test.ts:250:          slot.kind === 'cast_spell' || slot.kind === 'use_world_object'));
tests/unit/vtt/room-generator-los-cover.test.ts:273:    const closedTrace = traceCombatantLine(state, source.id, target.id);
tests/unit/vtt/room-generator-los-cover.test.ts:283:    expect(generatedLineIsOpen(closedRay)).toBe(false);
tests/unit/vtt/room-generator-los-cover.test.ts:286:    const openedTrace = traceCombatantLine(opened, source.id, target.id);
tests/unit/vtt/room-generator-los-cover.test.ts:292:    expect(generatedLineIsOpen(openedRay)).toBe(true);
tests/unit/vtt/hypnotic-pattern-probe.test.ts:144:    expect(new Set(damageResolution.mechanics.actionSlots.flatMap((slot) => slot.targetIds)))
tests/unit/vtt/engine-query-port.test.ts:10:import { traceCombatantLine } from '../../../src/combat/cover';
tests/unit/vtt/engine-query-port.test.ts:487:    const partialTrace = traceCombatantLine(partial, actor.id, target.id);
tests/unit/vtt/engine-query-port.test.ts:510:    expect(traceCombatantLine(walled, actor.id, target.id)).toMatchObject({ tier: 'total', blocksSight: true });
tests/unit/vtt/engine-query-port.test.ts:529:    expect(traceCombatantLine(closed.state, closed.actorId, closed.targetId)).toMatchObject({
tests/unit/vtt/engine-query-port.test.ts:544:    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
tests/unit/vtt/engine-query-port.test.ts:561:    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
tests/unit/vtt/engine-query-port.test.ts:576:    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
tests/unit/vtt/engine-query-port.test.ts:619:              resolution.mechanics.finalPosition,
tests/unit/vtt/engine-query-port.test.ts:634:              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
tests/unit/vtt/engine-query-port.test.ts:635:                sourceAnchor: resolution.mechanics.finalPosition,
tests/unit/vtt/engine-query-port.test.ts:757:      mechanics: { movementCostFeet: 0, actionSlots: [{ actionId: 'light-hammer', targetIds: [TARGET_ID] }] },
tests/unit/vtt/engine-query-port.test.ts:779:      mechanics: { movementCostFeet: 10, actionSlots: [{ actionId: 'grab', targetIds: [TARGET_ID] }] },
tests/unit/vtt/engine-round-session.test.ts:1044:      target: archer.mechanics.actionSlots[0]?.targetIds[0],
tests/unit/vtt/engine-round-session.test.ts:1047:      .toEqual(archer.mechanics.finalPosition);
tests/unit/vtt/tactical-evaluator-r02.test.ts:3:import { traceCombatantLine } from '../../../src/combat/cover';
tests/unit/vtt/tactical-evaluator-r02.test.ts:132:      const trace = traceCombatantLine(state, scoutId, FIGHTER);
tests/unit/vtt/tactical-evaluator-r02.test.ts:202:          component.target.combatantId === FIGHTER)) && option.actionSlots.length === 1);
tests/unit/vtt/arena-basis-brutal-b.test.ts:387:      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
tests/unit/vtt/arena-basis-brutal-b.test.ts:390:          slot.kind === 'use_world_object'));
tests/unit/vtt/vane-warren.test.ts:358:      candidate.type === 'use_world_object' && candidate.actionId === VANE_WARREN_SOUND_DRUM_ACTION_ID);
tests/unit/vtt/vane-warren.test.ts:359:    if (action?.type !== 'use_world_object') throw new Error('The adjacent drummer has no alarm action.');
tests/unit/vtt/vane-warren.test.ts:361:      type: 'use_world_object',
tests/unit/vtt/vane-warren.test.ts:404:    if (action?.type !== 'use_world_object') throw new Error('The adjacent drummer has no use-object action.');
tests/unit/vtt/vane-warren.test.ts:432:    if (action?.type !== 'use_world_object') throw new Error('The drummer has no use-object action.');
tests/unit/vtt/vane-warren.test.ts:451:    if (action?.type !== 'use_world_object') throw new Error('The drummer has no live use-object action.');
src/vtt/turn-proposal.ts:161:      readonly kind: 'use_world_object';
src/vtt/turn-proposal.ts:206:    | 'use_world_object'
src/combat/events.ts:166:      readonly type: 'use_world_object';
src/combat/events.ts:172:      readonly type: 'dm_use_world_object';
src/combat/world-object-actions.ts:57:        type: 'use_world_object',
src/combat/world-object-actions.ts:71:): Extract<EncounterCommand, { readonly type: 'dm_use_world_object' }> | null {
src/combat/world-object-actions.ts:81:    type: 'dm_use_world_object',
tests/unit/vtt/snippets.test.ts:90:      case 'use_world_object': return [use.actionId];
tests/unit/vtt/snippets.test.ts:110:      case 'use_world_object':
src/vtt/engine-round-session.ts:334:    case 'use_world_object': {
src/vtt/engine-round-session.ts:336:      state = reduce(state, { type: 'use_world_object', actor: mechanics.actorId, objectId: use.objectId, actionId: use.actionId });
src/combat/cover.ts:665:export function traceCombatantLine(
src/combat/cover.ts:696:export function traceCombatantLineToCells(
src/combat/cover.ts:777:  const trace = traceCombatantLine(state, sourceId, targetId, options);
src/vtt/offered-option-paths.ts:84:      case 'use_world_object': return 3;
src/vtt/vane-warren.ts:683:): Extract<EncounterCommand, { readonly type: 'dm_use_world_object' }> | null {
src/vtt/vane-warren.ts:694:  command: Extract<EncounterCommand, { readonly type: 'use_world_object' | 'dm_use_world_object' }>,
src/vtt/encounter-app.ts:332:    case 'use_world_object':
src/vtt/encounter-app.ts:334:    case 'dm_use_world_object':
src/vtt/offers/offer-declarations.ts:485:        use: { kind: 'use_world_object' as const, objectId: object.id, actionId: engineActionId(action.id) },
src/vtt/intent-resolver.ts:4:import { traceCombatantLine } from '../combat/cover';
src/vtt/intent-resolver.ts:159:    case 'use_world_object':
src/vtt/intent-resolver.ts:252:      case 'use_world_object':
src/vtt/intent-resolver.ts:279:        (constraint.source.requiresSight && traceCombatantLine(
src/vtt/intent-resolver.ts:447:    case 'use_world_object': {
src/vtt/intent-resolver.ts:511:      case 'use_world_object': resolved.push({ slot: slot.slot, kind: use.kind, actionId: use.actionId, spellId: null, targetIds: [], objectId: use.objectId, omittedRiders: [] }); break;
src/vtt/intent-resolver.ts:563:  if (actionSlots === null) return refused('OPTION_TARGET_UNRESOLVED', `${option.actorId}: ${option.label} target did not resolve`);
src/vtt/encounter-session-service.ts:644:  readonly #worldObjectOffers = new Map<string, Extract<EncounterCommand, { readonly type: 'dm_use_world_object' }>>();
src/vtt/dm-tactical-intel.ts:112:      case 'use_world_object':
src/vtt/dm-tactical-intel.ts:130:    case 'use_world_object': return main.actionId;
src/vtt/room-generator.ts:13:  traceCombatantLine,
src/vtt/room-generator.ts:14:  traceCombatantLineToCells,
src/vtt/room-generator.ts:1222:    const trace = traceCombatantLine(state, source.profile.id, target.profile.id);
src/vtt/room-generator.ts:1227:export function generatedLineIsOpen(
src/vtt/room-generator.ts:1235:    generatedLineIsOpen(traceCombatantLine(state, source.profile.id, target.profile.id)))) return true;
src/vtt/room-generator.ts:1243:        if (!occupied.has(cellKey(target)) && generatedLineIsOpen(
src/vtt/room-generator.ts:1244:          traceCombatantLineToCells(state, source.profile.id, [target]),
src/vtt/room-generator.ts:1265:        const trace = traceCombatantLineToCells(state, source.profile.id, [target]);
src/vtt/encounter-projections.ts:169:    readonly command: Extract<EncounterCommand, { readonly type: 'dm_use_world_object' }>;
src/vtt/plan-materiality.ts:112:        resolvedTargets: uniqueSorted(resolution.mechanics.actionSlots.flatMap((use) => use.targetIds)),
src/vtt/blind-intent-resolver.ts:312:    case 'use_world_object': return String(use.actionId);
src/vtt/blind-intent-resolver.ts:327:    case 'use_world_object': return 'interact';
src/vtt/blind-intent-resolver.ts:349:    final_cell: mechanics.finalPosition,
src/vtt/blind-intent-resolver.ts:383:      targetIds: [...new Set(resolution.mechanics.actionSlots.flatMap((slot) => slot.targetIds))],
src/vtt/blind-intent-resolver.ts:708:  const destinationMatches = candidates.filter((candidate) => sameCell(candidate.mechanics.finalPosition, destination));
src/vtt/dm-encounter-host.ts:1572:  dmUseWorldObject(command: Extract<EncounterCommand, { readonly type: 'dm_use_world_object' }>): void {
src/vtt/dm-encounter-host.ts:1578:    command: Extract<EncounterCommand, { readonly type: 'dm_use_world_object' }>,
src/vtt/intel/option-outcome.ts:616:    case 'use_world_object': return { kind: 'unsupported', reason: 'world_object_outcome_unsupported' };
src/vtt/intel/option-outcome.ts:922:      ? { ...token, position: { ...mechanics.finalPosition } }
src/vtt/intel/team-scorer.ts:231:  if (movementCostFeet > 0) return true;
src/vtt/intel/team-scorer.ts:238:      case 'use_world_object':
src/vtt/mcp/engine-server.ts:877:    world_object_id: use.kind === 'use_world_object' ? use.objectId : null,
src/vtt/mcp/engine-server.ts:914:      case 'use_world_object': return 3;
src/vtt/intel/opportunity-cost.ts:107:    case 'use_world_object':
src/vtt/intel/opportunity-cost.ts:170:  const movedState = stateWithActorAt(state, option.actorId, resolution.mechanics.finalPosition);
src/vtt/snippets/registry.ts:143:      case 'use_world_object':
src/vtt/option-modeling.ts:219:        case 'use_world_object':
src/vtt/engine-query-port.ts:6:import { coverBetweenCombatants, traceCombatantLine } from '../combat/cover';
src/vtt/engine-query-port.ts:1168:    const trace = traceCombatantLine(state, request.actorId, request.targetId, {
src/vtt/engine-query-port.ts:1547:    const cover = traceCombatantLine(state, request.actorId, request.targetId, { sourceAnchor: position }).tier;
src/vtt/engine-query-port.ts:1743:          ? { ...token, position: resolution.mechanics.finalPosition }
tests/unit/vtt/offer-environment-identity.test.ts:65:    const options = availableEngineActorOptions(state, actor.id, environment);
tests/unit/vtt/offer-environment-identity.test.ts:81:    expect(resolveEngineActorOption(state, fallback, differentPolicyEnvironment)).toMatchObject({
tests/unit/vtt/offer-environment-identity.test.ts:117:    const primary = availableEngineActorOptions(state, actor.id, environment)
tests/unit/vtt/engine-opportunity-movement-intel.test.ts:111:      const options = availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT, 203);
tests/unit/vtt/engine-opportunity-movement-intel.test.ts:131:      const option = availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT, 203)
tests/unit/vtt/engine-opportunity-movement-intel.test.ts:134:      const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
tests/unit/vtt/monster-omitted-riders.test.ts:46:  const option = availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).find((candidate) =>
tests/unit/vtt/monster-omitted-riders.test.ts:67:    expect(resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid).toBe(true);
tests/unit/vtt/monster-omitted-riders.test.ts:78:    expect(resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid).toBe(true);
tests/unit/vtt/monster-omitted-riders.test.ts:107:    expect(resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid).toBe(true);
tests/unit/vtt/monster-omitted-riders.test.ts:119:    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
tests/unit/vtt/room-generator.test.ts:596:        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
tests/unit/vtt/room-generator.test.ts:597:          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
tests/unit/vtt/challenge-room-fixtures.test.ts:102:  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT, state.revision).filter((option) => {
tests/unit/vtt/challenge-room-fixtures.test.ts:126:    const resolved = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
tests/unit/vtt/challenge-room-fixtures.test.ts:266:      const resolved = resolveEngineActorOption(state, defaultOption, OFFER_ENVIRONMENT);
tests/unit/vtt/challenge-room-fixtures.test.ts:305:    const options = availableEngineActorOptions(state, priest, OFFER_ENVIRONMENT, state.revision);
tests/unit/vtt/engine-round-session.test.ts:119:  const options = availableEngineActorOptions(planningState, actorId, OFFER_ENVIRONMENT);
tests/unit/vtt/engine-round-session.test.ts:176:  const dodge = availableEngineActorOptions(planningState, actorId, OFFER_ENVIRONMENT)
tests/unit/vtt/engine-round-session.test.ts:301:    const mixedOption = availableEngineActorOptions(state, BRUTE_ID, OFFER_ENVIRONMENT).find((option) =>
tests/unit/vtt/engine-round-session.test.ts:1056:    const secondOffense = availableEngineActorOptions(authorizationState, ARCHER_ID, OFFER_ENVIRONMENT)
tests/unit/vtt/offer-environment-board-sequence.test.ts:32:      const resolution = resolveEngineActorOption(state, option, environment);
tests/unit/vtt/blind-intent-resolver.test.ts:331:          availableEngineActorOptions(state, actorId, POLICY_OFFER_ENVIRONMENT, revision),
tests/unit/vtt/blind-intent-resolver.test.ts:720:    const real = availableEngineActorOptions(subject.state, subject.actorId, OFFER_ENVIRONMENT)
tests/unit/vtt/option-outcome.test.ts:72:    const option = availableEngineActorOptions(state, CASTER, OFFER_ENVIRONMENT).find((candidate) =>
tests/unit/vtt/option-outcome.test.ts:75:    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
tests/unit/vtt/option-outcome.test.ts:127:    const option = availableEngineActorOptions(state, CASTER, OFFER_ENVIRONMENT).find((candidate) =>
tests/unit/vtt/option-outcome.test.ts:132:    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
tests/unit/vtt/engine-state-capsule.test.ts:514:      const option = availableEngineActorOptions(fixture.state, actorId, REVISION_BOUND_OFFER_ENVIRONMENT)
tests/unit/vtt/engine-state-capsule.test.ts:592:    const option = availableEngineActorOptions(
tests/unit/vtt/standard-offer-generator.test.ts:253:        return resolveEngineActorOption(state, registered, OFFER_ENVIRONMENT);
tests/unit/vtt/mixed-kind-multiattack.test.ts:45:  const option = availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).find((candidate) =>
tests/unit/vtt/mixed-kind-multiattack.test.ts:99:      const options = availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT);
tests/unit/vtt/composite-turn-proposals.test.ts:134:    expect(availableEngineActorOptions(state, scout.id, OFFER_ENVIRONMENT).map((option) => option.optionId))
tests/unit/vtt/composite-turn-proposals.test.ts:169:    const option = availableEngineActorOptions(state, scout.id, OFFER_ENVIRONMENT)
tests/unit/vtt/composite-turn-proposals.test.ts:196:    const option = availableEngineActorOptions(state, priest.id, OFFER_ENVIRONMENT).find((candidate) =>
tests/unit/vtt/composite-turn-proposals.test.ts:240:    const disengage = availableEngineActorOptions(adjacent, spy.id, OFFER_ENVIRONMENT).find((candidate) =>
tests/unit/vtt/composite-turn-proposals.test.ts:259:    const attackAndDash = availableEngineActorOptions(distant, spy.id, OFFER_ENVIRONMENT).find((candidate) =>
tests/unit/vtt/composite-turn-proposals.test.ts:287:    expect(availableEngineActorOptions(spentMain, priest.id, OFFER_ENVIRONMENT)).toEqual([]);
tests/unit/vtt/composite-turn-proposals.test.ts:295:    expect(availableEngineActorOptions(spentBonus, priest.id, OFFER_ENVIRONMENT).every((option) =>
tests/unit/vtt/composite-turn-proposals.test.ts:320:    expect(availableEngineActorOptions(exhausted, priest.id, OFFER_ENVIRONMENT).some((option) =>
tests/unit/vtt/composite-turn-proposals.test.ts:328:    const options = availableEngineActorOptions(state, bear.id, OFFER_ENVIRONMENT);
tests/unit/vtt/composite-turn-proposals.test.ts:349:    expect(resolveEngineActorOption(illegalState, bite, OFFER_ENVIRONMENT).valid).toBe(true);
tests/unit/vtt/composite-turn-proposals.test.ts:350:    expect(resolveEngineActorOption(illegalState, option, OFFER_ENVIRONMENT)).toEqual({
tests/unit/vtt/composite-turn-proposals.test.ts:381:    const offered = availableEngineActorOptions(eligible, scout.id, OFFER_ENVIRONMENT)
tests/unit/vtt/composite-turn-proposals.test.ts:397:    expect(availableEngineActorOptions(ineligible, scout.id, OFFER_ENVIRONMENT).some((option) =>
tests/unit/vtt/room-generator-los-cover.test.ts:245:    const productive = availableEngineActorOptions(planning, monster.profile.id, OFFER_ENVIRONMENT).some((option) => {
tests/unit/vtt/room-generator-los-cover.test.ts:246:      const resolution = resolveEngineActorOption(planning, option, OFFER_ENVIRONMENT);
tests/unit/vtt/adjustment-exhaustion-coordinator.test.ts:44:  const options = availableEngineActorOptions(planningState, actorId, OFFER_ENVIRONMENT, 1);
tests/unit/vtt/hypnotic-pattern-probe.test.ts:116:    const options = availableEngineActorOptions(state, CASTER, OFFER_ENVIRONMENT);
tests/unit/vtt/hypnotic-pattern-probe.test.ts:139:    const controlResolution = resolveEngineActorOption(state, control, OFFER_ENVIRONMENT);
tests/unit/vtt/hypnotic-pattern-probe.test.ts:140:    const damageResolution = resolveEngineActorOption(state, damage, OFFER_ENVIRONMENT);
tests/unit/vtt/hypnotic-pattern-probe.test.ts:236:    const controlOption = hypnoticOption(availableEngineActorOptions(state, CASTER, OFFER_ENVIRONMENT));
tests/unit/vtt/hypnotic-pattern-probe.test.ts:237:    const damageEngineOption = damageOption(availableEngineActorOptions(state, CASTER, OFFER_ENVIRONMENT));
tests/unit/vtt/hypnotic-pattern-probe.test.ts:290:    const option = hypnoticOption(availableEngineActorOptions(crowded, CASTER, OFFER_ENVIRONMENT));
tests/unit/vtt/hypnotic-pattern-probe.test.ts:299:    const option = hypnoticOption(availableEngineActorOptions(state, CASTER, OFFER_ENVIRONMENT));
tests/unit/vtt/hypnotic-pattern-probe.test.ts:335:    const first = availableEngineActorOptions(
tests/unit/vtt/hypnotic-pattern-probe.test.ts:340:    const second = availableEngineActorOptions(
tests/unit/vtt/tactical-evaluator-r02.test.ts:193:      const option = availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).find(predicate);
tests/unit/vtt/offer-environment.test.ts:338:    const option = availableEngineActorOptions(state, actor.id, environment)[0];
tests/unit/vtt/offer-environment.test.ts:342:    expect(resolveEngineActorOption(state, option, reconstructed).valid).toBe(true);
tests/unit/vtt/offer-environment.test.ts:350:    expect(resolveEngineActorOption(state, option, environment)).toEqual({
tests/unit/vtt/offer-environment.test.ts:361:    const option = availableEngineActorOptions(state, actor.id, represented)[0];
tests/unit/vtt/offer-environment.test.ts:363:    expect(resolveEngineActorOption(state, option, legacy)).toEqual({
tests/unit/vtt/offered-option-paths.test.ts:108:  const options = orderOptionsAsTurnContext(availableEngineActorOptions(state, actor.id, OFFER_ENVIRONMENT));
tests/unit/vtt/arena-basis-brutal-b.test.ts:174:    const fallback = availableEngineActorOptions(
tests/unit/vtt/arena-basis-brutal-b.test.ts:381:    const productive = availableEngineActorOptions(
tests/unit/vtt/arena-basis-brutal-b.test.ts:386:      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
tests/unit/vtt/arena-basis-brutal-b.test.ts:492:    const dodge = availableEngineActorOptions(planningState, activeMonster.id, OFFER_ENVIRONMENT)
tests/unit/vtt/plan-materiality.test.ts:177:    const option = availableEngineActorOptions(beforeState, actor.profile.id, OFFER_ENVIRONMENT)
tests/unit/vtt/plan-materiality.test.ts:221:    const option = availableEngineActorOptions(state, actor.profile.id, OFFER_ENVIRONMENT)
tests/unit/vtt/engine-query-port.test.ts:383:    const options = availableEngineActorOptions(state, ACTOR_ID, OFFER_ENVIRONMENT);
tests/unit/vtt/engine-query-port.test.ts:461:    expect(availableEngineActorOptions(state, ACTOR_ID, OFFER_ENVIRONMENT).map(({ label }) => label))
tests/unit/vtt/engine-query-port.test.ts:494:    expect(availableEngineActorOptions(partial, actor.id, OFFER_ENVIRONMENT).some((option) =>
tests/unit/vtt/engine-query-port.test.ts:516:    expect(availableEngineActorOptions(walled, actor.id, OFFER_ENVIRONMENT).some((option) =>
tests/unit/vtt/engine-query-port.test.ts:539:    expect(availableEngineActorOptions(closed.state, closed.actorId, OFFER_ENVIRONMENT).some((option) =>
tests/unit/vtt/engine-query-port.test.ts:553:    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
tests/unit/vtt/engine-query-port.test.ts:571:    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
tests/unit/vtt/engine-query-port.test.ts:585:    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
tests/unit/vtt/engine-query-port.test.ts:601:        for (const option of availableEngineActorOptions(
tests/unit/vtt/engine-query-port.test.ts:606:          const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
tests/unit/vtt/engine-query-port.test.ts:743:    const fallback = availableEngineActorOptions(state, ACTOR_ID, OFFER_ENVIRONMENT).find((option) =>
tests/unit/vtt/engine-query-port.test.ts:767:    const grab = availableEngineActorOptions(state, ACTOR_ID, OFFER_ENVIRONMENT).find((option) =>
src/vtt/room-generator.ts:1227:export function generatedLineIsOpen(
src/vtt/room-generator.ts:1235:    generatedLineIsOpen(traceCombatantLine(state, source.profile.id, target.profile.id)))) return true;
src/vtt/room-generator.ts:1243:        if (!occupied.has(cellKey(target)) && generatedLineIsOpen(
tools/d569-second-family-manifest.ts:339:export function brutalRoomMembershipViolations(
tools/d569-second-family-manifest.ts:347:    'brutal_productivity',

exec
/bin/bash -lc "sed -n '210,305p' tests/unit/vtt/room-generator-los-cover.test.ts
sed -n '580,655p' tests/unit/vtt/engine-query-port.test.ts
sed -n '250,365p' tools/d569-second-family-manifest.ts
sed -n '120,230p' src/vtt/turn-proposal.ts
sed -n '470,530p' src/vtt/intent-resolver.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
  const state = room.encounter.state;
  const actor = state.combatants.find((candidate) => candidate.profile.id === actorId);
  const token = state.tokens.find((candidate) => candidate.combatantId === actorId);
  if (actor === undefined || token === undefined || actor.life !== 'living') return true;
  const opponents = state.combatants.filter((candidate) => candidate.life === 'living' &&
    candidate.profile.kind !== actor.profile.kind);
  const world = encounterMovementWorld(state);
  for (let row = 0; row < state.bounds.rows; row += 1) {
    for (let column = 0; column < state.bounds.columns; column += 1) {
      const destination = { column, row };
      const footprint = combatantSpaceAt(state, actorId, destination);
      if (!footprint.cells.every((cell) => isCellInside(state.bounds, cell))) continue;
      if (!opponents.some((opponent) => !traceCombatantLine(
        state,
        actorId,
        opponent.profile.id,
        { sourceAnchor: destination },
      ).blocksSight)) continue;
      const path = findPath(world, {
        actorId,
        start: token.position,
        goal: destination,
        maximumCost: feet(state.bounds.columns * state.bounds.rows * 10),
      });
      if (path.kind === 'found') return true;
    }
  }
  return false;
}

function expectProductiveMonsters(room: GeneratedRoom): void {
  const planning = freshMonsterPlanningState(room.encounter.state);
  const monsters = planning.combatants.filter((combatant) =>
    combatant.profile.kind === 'monster' && combatant.life === 'living');
  for (const monster of monsters) {
    const productive = availableEngineActorOptions(planning, monster.profile.id, OFFER_ENVIRONMENT).some((option) => {
      const resolution = resolveEngineActorOption(planning, option, OFFER_ENVIRONMENT);
      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
        resolution.mechanics.actionSlots.some((slot) =>
          slot.kind === 'attack' || slot.kind === 'saving_throw' ||
          slot.kind === 'cast_spell' || slot.kind === 'use_world_object'));
    });
    expect(productive, monster.profile.id).toBe(true);
  }
}

describe('D576 los_cover_v1 generator membership', () => {
  it('OPEN_RAY_IGNORES_BLOCKS_SIGHT rejects the only opposing pair across a two-blocker seam', () => {
    const source = monsterProfile('generator-seam-source');
    const target = playerProfile('generator-seam-target');
    const state = createEncounter({
      bounds: { columns: 16, rows: 8 },
      combatants: [source, target],
      tokens: [placedToken(source, 14, 5), placedToken(target, 1, 6)],
      blockedCells: [{ column: 8, row: 5 }, { column: 8, row: 6 }],
    });
    const opened = {
      ...state,
      blockedCells: [{ column: 8, row: 5 }],
    };

    // This is the state's only living opposing pair. The (14,6) -> (1,6) ray follows
    // row line 6 between blocker flanks (8,5)/(8,6), without entering either cell.
    const closedTrace = traceCombatantLine(state, source.id, target.id);
    expect(closedTrace).toMatchObject({
      tier: 'half',
      blocksSight: true,
      firstBlockingCell: { column: 8, row: 5 },
    });
    const closedRay = closedTrace.lines.find((line) =>
      line.targetCorner.column === 1 && line.targetCorner.row === 6);
    if (closedRay === undefined) throw new Error('Two-blocker fixture omitted the (14,6) -> (1,6) ray.');
    expect(closedRay).toMatchObject({ tier: 'none', blocksSight: true });
    expect(generatedLineIsOpen(closedRay)).toBe(false);

    // Removing (8,6) opens the same row-line-6 rays because only (8,5) remains.
    const openedTrace = traceCombatantLine(opened, source.id, target.id);
    expect(openedTrace).toMatchObject({ tier: 'none', blocksSight: false });
    const openedRay = openedTrace.lines.find((line) =>
      line.targetCorner.column === 1 && line.targetCorner.row === 6);
    if (openedRay === undefined) throw new Error('One-flank fixture omitted the (14,6) -> (1,6) ray.');
    expect(openedRay).toMatchObject({ tier: 'none', blocksSight: false });
    expect(generatedLineIsOpen(openedRay)).toBe(true);
  });

  it('generated and authored arena fixtures share one strict decoder', () => {
    const generated = generateRoom(5_762_001, { terrainProfile: 'los_cover_v1' });
    expect(canonicalJson(decodeArenaBasisEnvelopeV1(generated, { mode: 'legacy_basis' }).encounter.state))
      .toBe(canonicalJson(generated.encounter.state));
    const authoredText = inputs.fixtures.readText('tests/fixtures/arena-basis-challenge/seed-5831001.json');
    const authored = decodeArenaBasisEnvelopeV1(JSON.parse(authoredText) as unknown, { mode: 'challenge' });
    expect(authored.spec.seed).toBe(5_831_001);
    expect(authored.encounter.state.initiative).toHaveLength(4);
  });

  it.each(FIXTURE_CASES)(
    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
      actorId: opened.actorId,
      targetId: opened.targetId,
      actionId: 'web',
    })).toMatchObject({ legal: true });
    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
        slot.use.target.combatantId === opened.targetId))).toBe(true);
  });

  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
    const losFailures: string[] = [];
    let checkedOffers = 0;
    let checkedComponents = 0;
    let checkedSaveComponents = 0;
    for (const path of AGREEMENT_FIXTURE_PATHS) {
      const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
      const monsters = state.combatants.filter((combatant) =>
        combatant.profile.kind === 'monster' && combatant.life === 'living');
      for (const monster of monsters) {
        for (const option of availableEngineActorOptions(
          state,
          monster.profile.id,
          OFFER_ENVIRONMENT,
        )) {
          const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
          if (!resolution.valid) throw new Error(`${path}: offered option ${option.optionId} did not resolve.`);
          const uses = resolution.mechanics.actionSlots.filter((use) =>
            use.kind === 'attack' || use.kind === 'saving_throw');
          if (uses.length === 0) continue;
          checkedOffers += 1;
          for (const use of uses) {
            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
            checkedComponents += 1;
            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
            const executionState = stateAtResolvedOrigin(
              state,
              monster.profile.id,
              resolution.mechanics.finalPosition,
            );
            const sources = [
              ...monsterActions(executionState, monster.profile.id),
              ...monsterBonusActions(executionState, monster.profile.id),
            ];
            const action = sources.find(
              (candidate): candidate is MonsterAttackAction | MonsterSavingThrowAction =>
                (candidate.kind === 'attack' || candidate.kind === 'saving_throw') &&
                candidate.id === use.actionId && candidate.kind === use.kind,
            );
            if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
            for (const targetId of use.targetIds) {
              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
                sourceAnchor: resolution.mechanics.finalPosition,
              });
              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');

              if (action.kind === 'saving_throw') {
                // force_save currently has no sight gate; this is an additional general execution check only.
                expect(() => reduceEncounter(
                  executionState,
                  monsterSavingThrowCommand(
                    action,
                    monster.profile.id,
                    targetId,
                    use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                  ),
                  () => 0.5,
                )).not.toThrow();
                continue;
              }
              try {
                // Attack execution independently enforces the same sight/Total-Cover rule.
    'hard roster must contain an executable control caster plus Guard and Scout',
  );

  const difficultRegions = room.spec.terrain.filter((feature) =>
    feature.kind === 'difficult-terrain-patch');
  const hardFeatures = room.spec.hardFeatures;
  const [gate] = hardFeatures?.chokepointCells ?? [];
  const barrier = gate === undefined ? [] : room.spec.blockedCells.filter((cell) => cell.column === gate.column);
  addViolation(
    violations,
    hardFeatures?.shape === 'single-gate' && hardFeatures.chokepointCells.length === 1 &&
      gate !== undefined && barrier.length === room.spec.dimensions.rows - 1 &&
      !barrier.some((cell) => cell.row === gate.row) && difficultRegions.length >= 1 &&
      hardFeatures.likelyApproachTerrainRegionIds.length > 0 &&
      hardFeatures.likelyApproachTerrainRegionIds.every((id) =>
        difficultRegions.some((region) => region.id === id && region.cells.length > 0)),
    'hard_chokepoint_structure',
    'hard room must retain its single-gate barrier, chokepoint, and approach difficult terrain',
  );
  return violations;
}

function executableCaster(room: GeneratedRoom): boolean {
  return room.spec.monsterRoster.some((entry) => {
    const row = BUNDLED_MONSTER_ROSTER.find((candidate) => candidate.id === entry.statblockId);
    const actions = row?.statblock.sourceDetails.actions;
    return actions?.kind === 'present' && actions.value.some((action) =>
      action.kind === 'spellcasting' && action.execution?.kind !== 'absent' &&
      action.spells.some((spell) => spell.manifestStatus === 'implemented'));
  });
}

function everyMonsterProductive(room: GeneratedRoom): boolean {
  const planningState = freshMonsterPlanningState(room.encounter.state);
  return planningState.combatants
    .filter((combatant) => combatant.profile.kind === 'monster')
    .every((monster) => availableEngineActorOptions(planningState, monster.profile.id, OFFER_ENVIRONMENT)
      .some((option) => {
        const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
        return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
          resolution.mechanics.actionSlots.some((slot) =>
            slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
            slot.kind === 'use_world_object'));
      }));
}

function brutalRoomStaticMembershipViolations(
  room: GeneratedRoom,
  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
): readonly D569SecondFamilyViolation[] {
  const violations: D569SecondFamilyViolation[] = [];
  addViolation(violations, room.spec.difficultyProfile === 'brutal', 'brutal_difficulty', 'room is not tagged brutal');
  addViolation(
    violations,
    room.spec.challengeBudgetEighths ===
      sameSeedHard.spec.challengeBudgetEighths * BRUTAL_CHALLENGE_BUDGET_SCALE &&
      room.spec.challengeSpentEighths >= room.spec.challengeBudgetEighths * 0.9 &&
      room.spec.challengeSpentEighths <= room.spec.challengeBudgetEighths,
    'brutal_budget',
    'brutal budget must be scaled from hard and spend 90-100 percent',
  );
  addViolation(
    violations,
    executableCaster(room),
    'brutal_caster',
    'brutal roster must contain an executable implemented caster',
  );

  const terrainCounts = {
    difficult: room.spec.terrain.filter((feature) => feature.kind === 'difficult-terrain-patch').length,
    hazards: room.spec.terrain.filter((feature) => feature.kind === 'hazard-object').length,
    lights: room.spec.terrain.filter((feature) => feature.kind === 'light-source').length,
    obscurement: room.spec.terrain.filter((feature) => feature.kind === 'obscurement-patch').length,
  };
  const terrainTotal = Object.values(terrainCounts).reduce((total, count) => total + count, 0);
  addViolation(
    violations,
    terrainCounts.difficult >= 3 && terrainCounts.hazards >= 3 && terrainCounts.lights >= 2 &&
      terrainCounts.obscurement >= 2 &&
      terrainTotal >= BRUTAL_TERRAIN_FEATURE_COUNT_BAND.minimum &&
      terrainTotal <= BRUTAL_TERRAIN_FEATURE_COUNT_BAND.maximum &&
      room.spec.terrain.length > sameSeedHard.spec.terrain.length,
    'brutal_terrain',
    'brutal terrain must meet every configured minimum/count band and exceed hard density',
  );
  return violations;
}

/** State-derived brutal membership; hashes and creature names alone cannot satisfy it. */
export function brutalRoomMembershipViolations(
  room: GeneratedRoom,
  sameSeedHard: GeneratedRoom = generateRoom(room.spec.seed, { difficulty: 'hard' }),
): readonly D569SecondFamilyViolation[] {
  const violations = [...brutalRoomStaticMembershipViolations(room, sameSeedHard)];
  addViolation(
    violations,
    everyMonsterProductive(room),
    'brutal_productivity',
    'every brutal monster must have a productive first-turn offer',
  );
  return violations;
}

function expectedDirectory(difficulty: D569SecondFamilyDifficulty): string {
  return `tests/fixtures/arena-basis-${difficulty}-2`;
}

function sameNamespace(
  actual: readonly { readonly label: string; readonly start: number; readonly end: number }[],
): boolean {
  return canonicalJson(actual) === canonicalJson(D569_USED_SEED_NAMESPACES);
}

function encounterStateRecord(input: unknown): Record<string, unknown> {
  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
    throw new TypeError('fixture root is not an object');
export interface EngineMovementObjective {
  readonly preference: EngineMovementPreference;
  readonly engagement: EngineEngagement;
}

export interface EngineTargetedAttackUse {
  readonly kind: 'attack';
  readonly actionId: EngineActionId;
  readonly target: EngineTargetSelector;
}

export type EngineMultiattackComponentUse = (
  | EngineTargetedAttackUse
  | {
      readonly kind: 'saving_throw';
      readonly actionId: EngineActionId;
      readonly target: EngineTargetSelector;
    }
) & { readonly omittedRiders: readonly EngineOmittedRider[] };

export type EngineMainActionUse =
  | EngineTargetedAttackUse
  | {
      readonly kind: 'multiattack';
      readonly actionId: EngineActionId;
      readonly components: readonly EngineMultiattackComponentUse[];
    }
  | {
      readonly kind: 'saving_throw';
      readonly actionId: EngineActionId;
      readonly target: EngineTargetSelector;
    }
  | {
      readonly kind: 'cast_spell';
      readonly sourceActionId: EngineActionId;
      readonly spellId: EngineSpellId;
      readonly targets: readonly EngineTargetSelector[];
      /** Exact engine-owned placement; null for spells without a placed area. */
      readonly area: AreaTemplate | null;
    }
  | {
      readonly kind: 'use_world_object';
      readonly objectId: WorldObjectId;
      readonly actionId: EngineActionId;
    }
  | { readonly kind: 'dodge' | 'disengage' | 'dash' | 'end_turn' };

export type EngineBonusActionUse =
  | {
      readonly kind: 'cast_spell';
      readonly sourceActionId: EngineActionId;
      readonly spellId: EngineSpellId;
      readonly targets: readonly EngineTargetSelector[];
      /** Engine-bound condition encoded by a revision-bound offered option. */
      readonly selectedCondition?: LesserRestorationCondition;
      /** Exact engine-owned placement; null for spells without a placed area. */
      readonly area: AreaTemplate | null;
    }
  | {
      readonly kind: 'saving_throw';
      readonly actionId: EngineActionId;
      readonly target: EngineTargetSelector;
    }
  | { readonly kind: 'dash' | 'disengage' | 'hide' };

export type EngineActionSlotUse =
  | { readonly slot: 'main'; readonly use: EngineMainActionUse }
  | { readonly slot: 'bonus'; readonly use: EngineBonusActionUse };

export interface EngineTurnProposal {
  readonly actorId: CombatantId;
  readonly expectedRevision: number;
  readonly primaryOptionId: EngineOptionId;
  readonly fallbackOptionId: EngineOptionId | null;
  /** Verbatim, bounded explanation supplied by the decision author. */
  readonly reason: string;
  readonly activationChoice?: EngineActivationChoice | null;
  readonly overrideJustification: EngineOverrideJustification | null;
}

export interface ResolvedActionSlotUse {
  readonly slot: 'main' | 'bonus';
  readonly kind:
    | 'attack'
    | 'saving_throw'
    | 'cast_spell'
    | 'use_world_object'
    | 'dodge'
    | 'disengage'
    | 'dash'
    | 'hide'
    | 'end_turn';
  readonly actionId: EngineActionId;
  readonly spellId: EngineSpellId | null;
  readonly targetIds: readonly CombatantId[];
  readonly objectId: WorldObjectId | null;
  readonly omittedRiders: readonly EngineOmittedRider[];
  /** Engine-bound condition encoded by a revision-bound offered option. */
  readonly selectedCondition?: LesserRestorationCondition;
  readonly activationChoice?: EngineActivationChoice;
  readonly multiattackComponent?: true;
  /** Present only for a placed-area spell. */
  readonly area?: AreaTemplate;
}

export interface ResolvedTurnMechanics {
  readonly actorId: CombatantId;
  readonly optionId: EngineOptionId;
  readonly movementCostFeet: number;
  readonly path: readonly GridCell[];
  readonly finalPosition: GridCell;
  state: EncounterState,
  actorId: CombatantId,
  slots: readonly EngineActionSlotUse[],
  queries: EngineQueryPort,
  omittedRiders: readonly EngineOmittedRider[],
): readonly ResolvedActionSlotUse[] | null {
  const resolved: ResolvedActionSlotUse[] = [];
  for (const slot of slots) {
    const use = slot.use;
    const targets = useTargets(state, actorId, use, queries);
    if (targets === null) return null;
    switch (use.kind) {
      case 'multiattack':
        for (const component of use.components) {
          const targetId = resolveSelector(state, actorId, component.target, queries);
          if (targetId === null) return null;
          resolved.push({
            slot: slot.slot,
            kind: component.kind,
            actionId: component.actionId,
            spellId: null,
            targetIds: [targetId],
            objectId: null,
            omittedRiders: component.omittedRiders,
            multiattackComponent: true,
          });
        }
        break;
      case 'cast_spell': resolved.push({
        slot: slot.slot,
        kind: use.kind,
        actionId: use.sourceActionId,
        spellId: use.spellId,
        targetIds: targets,
        objectId: null,
        omittedRiders: omittedRiders.filter((rider) => rider.sourceActionId === use.sourceActionId),
        ...(!('selectedCondition' in use) || use.selectedCondition === undefined
          ? {}
          : { selectedCondition: use.selectedCondition }),
        ...(use.area === null ? {} : { area: use.area }),
      }); break;
      case 'use_world_object': resolved.push({ slot: slot.slot, kind: use.kind, actionId: use.actionId, spellId: null, targetIds: [], objectId: use.objectId, omittedRiders: [] }); break;
      case 'attack':
      case 'saving_throw': resolved.push({
        slot: slot.slot,
        kind: use.kind,
        actionId: use.actionId,
        spellId: null,
        targetIds: targets,
        objectId: null,
        omittedRiders: omittedRiders.filter((rider) => rider.sourceActionId === use.actionId),
      }); break;
      case 'dodge':
      case 'disengage':
      case 'dash':
      case 'hide':
      case 'end_turn': resolved.push({ slot: slot.slot, kind: use.kind, actionId: engineActionId(use.kind), spellId: null, targetIds: [], objectId: null, omittedRiders: [] }); break;
    }
  }
  return resolved;
}

exec
/bin/bash -lc "sed -n '1,220p' tests/unit/vtt/room-generator-los-cover.test.ts
sed -n '430,520p' tests/unit/vtt/engine-query-port.test.ts
sed -n '720,805p' tests/unit/vtt/engine-query-port.test.ts
sed -n '1,130p' tests/unit/vtt/challenge-room-fixtures.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
import { describe, expect, it } from 'vitest';
import { combatantSpace, combatantSpaceAt } from '../../../src/combat/combat-rules';
import { createEncounter } from '../../../src/combat/encounter';
import {
  traceCombatantLine,
  traceCombatantLineToCells,
  type TerrainLineTrace,
} from '../../../src/combat/cover';
import { encounterMovementWorld } from '../../../src/combat/encounter-movement-world';
import { isCellInside, type GridCell } from '../../../src/combat/grid';
import { findPath } from '../../../src/combat/movement';
import { terrainBlocking, type CoverTier } from '../../../src/combat/terrain';
import { feet, type CombatantId } from '../../../src/combat/values';
import { canonicalJson } from '../../../src/commands/canonical-json';
import { sha256 } from '../../../src/crypto/sha256';
import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
import { decodeArenaBasisEnvelopeV1 } from '../../../src/vtt/arena-fixture';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
import {
  generatedLineIsOpen,
  generateRoom,
  type GeneratedRoom,
  type RoomDifficultyProfile,
} from '../../../src/vtt/room-generator';
import { declareTestInputs } from '../../helpers/test-inputs';
import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';

const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });

const FIXTURE_CASES = [
  { difficulty: 'standard', seed: 5_762_001, path: 'tests/fixtures/arena-basis-los-cover-v1/seed-5762001.json' },
  { difficulty: 'standard', seed: 5_762_002, path: 'tests/fixtures/arena-basis-los-cover-v1/seed-5762002.json' },
  { difficulty: 'standard', seed: 5_762_003, path: 'tests/fixtures/arena-basis-los-cover-v1/seed-5762003.json' },
  { difficulty: 'hard', seed: 5_762_101, path: 'tests/fixtures/arena-basis-los-cover-v1/seed-5762101.json' },
  { difficulty: 'hard', seed: 5_762_102, path: 'tests/fixtures/arena-basis-los-cover-v1/seed-5762102.json' },
  { difficulty: 'hard', seed: 5_762_103, path: 'tests/fixtures/arena-basis-los-cover-v1/seed-5762103.json' },
  { difficulty: 'brutal', seed: 5_762_201, path: 'tests/fixtures/arena-basis-los-cover-v1/seed-5762201.json' },
  { difficulty: 'brutal', seed: 5_762_202, path: 'tests/fixtures/arena-basis-los-cover-v1/seed-5762202.json' },
  { difficulty: 'brutal', seed: 5_762_203, path: 'tests/fixtures/arena-basis-los-cover-v1/seed-5762203.json' },
] as const satisfies readonly {
  readonly difficulty: RoomDifficultyProfile;
  readonly seed: number;
  readonly path: `tests/fixtures/${string}`;
}[];

const inputs = declareTestInputs({
  fixtures: [
    'tests/fixtures/arena-basis-los-cover-v1/seed-5762001.json',
    'tests/fixtures/arena-basis-los-cover-v1/seed-5762002.json',
    'tests/fixtures/arena-basis-los-cover-v1/seed-5762003.json',
    'tests/fixtures/arena-basis-los-cover-v1/seed-5762101.json',
    'tests/fixtures/arena-basis-los-cover-v1/seed-5762102.json',
    'tests/fixtures/arena-basis-los-cover-v1/seed-5762103.json',
    'tests/fixtures/arena-basis-los-cover-v1/seed-5762201.json',
    'tests/fixtures/arena-basis-los-cover-v1/seed-5762202.json',
    'tests/fixtures/arena-basis-los-cover-v1/seed-5762203.json',
    'tests/fixtures/arena-basis-challenge/seed-5831001.json',
  ],
});

const PROPERTY_CASES: readonly {
  readonly difficulty: RoomDifficultyProfile;
  readonly seed: number;
}[] = (['standard', 'hard', 'brutal'] as const).flatMap((difficulty) =>
  Array.from({ length: 32 }, (_unused, seed) => ({ difficulty, seed })));

function cellKey(cell: GridCell): string {
  return `${String(cell.column)},${String(cell.row)}`;
}

function livingSides(room: GeneratedRoom) {
  const state = room.encounter.state;
  return {
    party: state.combatants.filter((combatant) =>
      combatant.life === 'living' && combatant.profile.kind === 'player_character'),
    monsters: state.combatants.filter((combatant) =>
      combatant.life === 'living' && combatant.profile.kind === 'monster'),
  };
}

function opposingPairTraces(room: GeneratedRoom): readonly TerrainLineTrace[] {
  const state = room.encounter.state;
  const { party, monsters } = livingSides(room);
  return [
    ...party.flatMap((source) => monsters.map((target) =>
      traceCombatantLine(state, source.profile.id, target.profile.id))),
    ...monsters.flatMap((source) => party.map((target) =>
      traceCombatantLine(state, source.profile.id, target.profile.id))),
  ];
}

function creatureToCellTraces(room: GeneratedRoom): readonly TerrainLineTrace[] {
  const state = room.encounter.state;
  const occupied = new Set(state.combatants.flatMap((combatant) =>
    combatant.life === 'dead' ? [] : combatantSpace(state, combatant.profile.id).cells.map(cellKey)));
  return state.combatants.flatMap((source) => {
    if (source.life !== 'living') return [];
    const traces: TerrainLineTrace[] = [];
    for (let row = 0; row < state.bounds.rows; row += 1) {
      for (let column = 0; column < state.bounds.columns; column += 1) {
        const target = { column, row };
        if (!occupied.has(cellKey(target))) {
          traces.push(traceCombatantLineToCells(state, source.profile.id, [target]));
        }
      }
    }
    return traces;
  });
}

function expectFeatureMembership(room: GeneratedRoom): void {
  const state = room.encounter.state;
  const half = room.spec.terrain.find((feature) =>
    feature.kind === 'cover-object' && feature.object.terrainKind === 'half_cover');
  const threeQuarters = room.spec.terrain.find((feature) =>
    feature.kind === 'cover-object' && feature.object.terrainKind === 'three_quarters_cover');
  const wall = room.spec.terrain.find((feature) => feature.kind === 'wall');
  expect(half?.kind).toBe('cover-object');
  expect(threeQuarters?.kind).toBe('cover-object');
  expect(wall?.kind).toBe('wall');
  if (half?.kind !== 'cover-object' || half.object.terrainKind !== 'half_cover' ||
    threeQuarters?.kind !== 'cover-object' ||
    threeQuarters.object.terrainKind !== 'three_quarters_cover' || wall?.kind !== 'wall') {
    throw new Error('los_cover_v1 typed terrain is incomplete.');
  }
  expect(half.object.blocking).toEqual(terrainBlocking('half_cover'));
  expect(threeQuarters.object.blocking).toEqual(terrainBlocking('three_quarters_cover'));
  expect(state.blockedCells).toEqual(wall.cells);

  const occupied = new Set(state.combatants.flatMap((combatant) =>
    combatant.life === 'dead' ? [] : combatantSpace(state, combatant.profile.id).cells.map(cellKey)));
  const doors = new Set(state.worldObjects.filter((object) => object.kind === 'door')
    .flatMap((object) => object.footprint.map(cellKey)));
  const movementBlocking = [threeQuarters.object.footprint, wall.cells];
  const seenBlocking = new Set<string>();
  for (const footprint of [half.object.footprint, threeQuarters.object.footprint, wall.cells]) {
    expect(new Set(footprint.map(cellKey)).size).toBe(footprint.length);
    expect(footprint.every((cell) => isCellInside(state.bounds, cell))).toBe(true);
    expect(footprint.every((cell) => !occupied.has(cellKey(cell)) && !doors.has(cellKey(cell)))).toBe(true);
  }
  for (const footprint of movementBlocking) {
    for (const cell of footprint) {
      expect(seenBlocking.has(cellKey(cell))).toBe(false);
      seenBlocking.add(cellKey(cell));
    }
  }

  const pairTraces = opposingPairTraces(room);
  const cellTraces = creatureToCellTraces(room);
  const allTraces = [...pairTraces, ...cellTraces];
  const halfSource = `object:${String(half.object.id)}`;
  const threeQuartersSource = `object:${String(threeQuarters.object.id)}`;
  const wallSources = new Set(wall.cells.map((cell) => `blocked:${cellKey(cell)}`));
  expect(pairTraces.some((trace) => trace.tier === 'half' && trace.sourceIds.includes(halfSource))).toBe(true);
  expect(pairTraces.some((trace) =>
    trace.tier === 'three_quarters' && trace.sourceIds.includes(threeQuartersSource))).toBe(true);
  expect(allTraces.some((trace) => trace.tier === 'total' && trace.blocksSight &&
    trace.sourceIds.some((id) => wallSources.has(id)))).toBe(true);
  expect(allTraces.some((trace) => trace.tier === 'none' && !trace.blocksSight)).toBe(true);

  for (const object of [half.object, threeQuarters.object]) {
    expect(object.footprint.some((cell) => {
      for (let rowOffset = -1; rowOffset <= 1; rowOffset += 1) {
        for (let columnOffset = -1; columnOffset <= 1; columnOffset += 1) {
          if (rowOffset === 0 && columnOffset === 0) continue;
          const adjacent = { column: cell.column + columnOffset, row: cell.row + rowOffset };
          if (isCellInside(state.bounds, adjacent) && !seenBlocking.has(cellKey(adjacent)) &&
            !occupied.has(cellKey(adjacent))) return true;
        }
      }
      return false;
    })).toBe(true);
  }
}

function sideCanChangeLine(
  room: GeneratedRoom,
  actors: readonly { readonly profile: { readonly id: CombatantId; readonly rules: { readonly speed: number } } }[],
  opponents: readonly { readonly profile: { readonly id: CombatantId } }[],
): boolean {
  const state = room.encounter.state;
  const world = encounterMovementWorld(state);
  return actors.some((actor) => opponents.some((opponent) => {
    const token = state.tokens.find((candidate) => candidate.combatantId === actor.profile.id);
    if (token === undefined) return false;
    const current = traceCombatantLine(state, actor.profile.id, opponent.profile.id);
    for (let row = 0; row < state.bounds.rows; row += 1) {
      for (let column = 0; column < state.bounds.columns; column += 1) {
        const destination = { column, row };
        const path = findPath(world, {
          actorId: actor.profile.id,
          start: token.position,
          goal: destination,
          maximumCost: feet(actor.profile.rules.speed),
        });
        if (path.kind !== 'found' || path.cells.length === 0) continue;
        const changed = traceCombatantLine(state, actor.profile.id, opponent.profile.id, {
          sourceAnchor: destination,
        });
        if (changed.tier !== current.tier || changed.blocksSight !== current.blocksSight) return true;
      }
    }
    return false;
  }));
}

function reachesProductiveRegion(room: GeneratedRoom, actorId: CombatantId): boolean {
  const state = room.encounter.state;
  const actor = state.combatants.find((candidate) => candidate.profile.id === actorId);
  const token = state.tokens.find((candidate) => candidate.combatantId === actorId);
  if (actor === undefined || token === undefined || actor.life !== 'living') return true;
  const opponents = state.combatants.filter((candidate) => candidate.life === 'living' &&
    candidate.profile.kind !== actor.profile.kind);
  const world = encounterMovementWorld(state);
  for (let row = 0; row < state.bounds.rows; row += 1) {
    for (let column = 0; column < state.bounds.columns; column += 1) {
      const destination = { column, row };
      const footprint = combatantSpaceAt(state, actorId, destination);
      } else if (call.case === 'dash') {
        // Seven ordinary entries cost 35 feet; Dash supplies the second 30-foot budget.
        expect(canonical).toMatchObject({ legal: true, costFeet: 35, budgetFeet: 60 });
      } else {
        // A hostile living creature occupies the requested endpoint.
        expect(canonical).toEqual({ legal: false, code: 'destination_unreachable' });
      }
    }
  });

  it('withholds Dash when an enclosed actor has no endpoint closer to its target', () => {
    const generated = placedState(
      SEED,
      new Map<CombatantId, GridCell>([
        [ACTOR_ID, { column: 0, row: 0 }],
        [TARGET_ID, { column: 20, row: 0 }],
      ]),
      [],
      1,
    );
    const state: EncounterState = {
      ...generated,
      blockedCells: [{ column: 1, row: 0 }],
    };

    expect(OFFER_ENVIRONMENT.queries.approach(state, {
      actorId: ACTOR_ID,
      target: { column: 20, row: 0 },
      movement: 'dash',
      maximumFeet: 60,
    })).toEqual({ legal: false, code: 'destination_unreachable' });
    expect(availableEngineActorOptions(state, ACTOR_ID, OFFER_ENVIRONMENT).map(({ label }) => label))
      .toEqual(['Dodge', 'End Turn']);
    expect(engineActorOptionsForEnvironment(state, ACTOR_ID, OFFER_ENVIRONMENT).humanOnly)
      .toContainEqual(expect.objectContaining({
      label: 'Disengage',
      noModeledEffect: { kind: 'disengage_without_movement', action: 'disengage' },
    }));
  });

  it('M576-E1A-QUERY-PORT-LEGACY-LOS keeps reducer, query, options, and board terrain on one trace', () => {
    const actor = monsterCombatantProfile(GOBLIN_WARRIOR, {
      combatantId: 'combatant:parity-goblin', tokenId: 'token:parity-goblin',
    });
    const target = playerProfile('parity-target', { initiativeBonus: -20 });
    const lowCover = {
      id: worldObjectId('object:parity-low-cover'), name: 'Parity low cover', kind: 'cover' as const,
      position: { column: 2, row: 0 }, footprint: [0, 1, 2, 3].map((row) => ({ column: 2, row })),
      durability: { kind: 'indestructible' as const }, armorClass: armorClass(10), damageResponses: [],
      blocking: terrainBlocking('half_cover'), createdRevision: 0,
    };
    const setup = (worldObjects: EncounterState['worldObjects'], blockedCells: EncounterState['blockedCells'] = []) =>
      reduceEncounter(createEncounter({
        bounds: { columns: 6, rows: 4 }, combatants: [actor, target],
        tokens: [placedToken(actor, 0, 0), placedToken(target, 5, 2)], worldObjects, blockedCells,
      }), { type: 'roll_initiative' }, () => 0.5).state;
    const partial = setup([lowCover]);
    const partialTrace = traceCombatantLine(partial, actor.id, target.id);
    expect(partialTrace).toMatchObject({ tier: 'half', blocksSight: false });
    expect(canCombatantSee(partial, actor.id, target.id)).toBe(true);
    expect(OFFER_ENVIRONMENT.queries.cover(partial, actor.id, target.id)).toEqual({
      tier: 'half', sourceIds: [`object:${lowCover.id}`],
    });
    expect(OFFER_ENVIRONMENT.queries.visibility(partial, actor.id, target.id)).toMatchObject({ visible: true });
    expect(availableEngineActorOptions(partial, actor.id, OFFER_ENVIRONMENT).some((option) =>
      option.actionSlots.some((slot) =>
      slot.use.kind === 'attack' && slot.use.target.kind === 'combatant' && slot.use.target.combatantId === target.id))).toBe(true);
    const boardObject = projectEncounterBoard(projectDmView(partial)).worldObjects.find((object) => object.id === lowCover.id);
    expect(boardObject === undefined ? null : terrainKindOfWireBlocking(boardObject.blocking)).toBe('half_cover');
    const attack = OFFER_ENVIRONMENT.queries.actions(partial, actor.id)
      .find((action) => action.kind === 'attack' && action.id === 'shortbow');
    if (attack === undefined || attack.kind !== 'attack') throw new Error('Parity fixture omitted Shortbow.');
    expect(() => reduceEncounter(partial, monsterAttackCommand(attack, actor.id, target.id), () => 0.5)).not.toThrow();

    const walledCells = [
      ...[0, 1, 2, 3].map((row) => ({ column: 2, row })),
      // Keep the actor at the tested origin so an option cannot move to a boundary-grazing line.
      { column: 1, row: 0 }, { column: 0, row: 1 }, { column: 1, row: 1 },
    ];
    const walled = setup([], walledCells);
    expect(traceCombatantLine(walled, actor.id, target.id)).toMatchObject({ tier: 'total', blocksSight: true });
    expect(canCombatantSee(walled, actor.id, target.id)).toBe(false);
    expect(OFFER_ENVIRONMENT.queries.cover(walled, actor.id, target.id)).toMatchObject({ tier: 'total' });
    expect(OFFER_ENVIRONMENT.queries.visibility(walled, actor.id, target.id)).toMatchObject({
      visible: false, reason: 'blocked',
    });
    expect(availableEngineActorOptions(walled, actor.id, OFFER_ENVIRONMENT).some((option) =>
      option.actionSlots.some((slot) =>
      slot.use.kind === 'attack' && slot.use.target.kind === 'combatant' && slot.use.target.combatantId === target.id))).toBe(false);
    expect(() => reduceEncounter(walled, monsterAttackCommand(attack, actor.id, target.id), () => 0.5))
      .toThrow('Total Cover or is outside line of sight');
      [TARGET_ID, { column: 5, row: 0 }],
    ]));
    // Five grid intervals are 25 feet: beyond 20 normal, within 60 long.
    expect(OFFER_ENVIRONMENT.queries.reach(state, {
      actorId: ACTOR_ID,
      targetId: TARGET_ID,
      actionId: 'light-hammer',
    })).toEqual({
      legal: true,
      distanceFeet: 25,
      rangeFeet: 20,
      rangeBand: 'long',
      normalRangeFeet: 20,
      longRangeFeet: 60,
    });
  });

  it('derives fallback behavior independently from fixed geometry', () => {
    const state = placedState(SEED, new Map<CombatantId, GridCell>([
      [ACTOR_ID, { column: 0, row: 0 }],
      [TARGET_ID, { column: 4, row: 0 }],
    ]));

    const fallback = availableEngineActorOptions(state, ACTOR_ID, OFFER_ENVIRONMENT).find((option) =>
      option.actionSlots.some((slot) => slot.use.kind === 'attack' && slot.use.actionId === 'light-hammer'));
    if (fallback === undefined) throw new Error('Fixture omitted the Light Hammer option.');
    const resolved = TURN_PROPOSAL_RESOLVER.resolve(state, {
      actorId: ACTOR_ID, expectedRevision: state.revision,
      primaryOptionId: engineOptionId('option:missing-grab'), fallbackOptionId: fallback.optionId,
      reason: 'Exercise the engine query proposal fixture.',
      overrideJustification: null,
    });

    // A missing revision-bound primary falls through to the offered Light Hammer option at 20 feet.
    expect(resolved).toMatchObject({
      valid: true,
      selectedBranch: 'fallback',
      mechanics: { movementCostFeet: 0, actionSlots: [{ actionId: 'light-hammer', targetIds: [TARGET_ID] }] },
    });
  });

  it('moves into melee before attacking when the declared stance and willingness permit it', () => {
    const state = placedState(SEED, new Map<CombatantId, GridCell>([
      [ACTOR_ID, { column: 0, row: 0 }],
      [TARGET_ID, { column: 4, row: 0 }],
    ]));

    const grab = availableEngineActorOptions(state, ACTOR_ID, OFFER_ENVIRONMENT).find((option) =>
      option.actionSlots.some((slot) => slot.use.kind === 'attack' && slot.use.actionId === 'grab'));
    if (grab === undefined) throw new Error('Fixture omitted the Grab option.');
    const resolved = TURN_PROPOSAL_RESOLVER.resolve(state, {
      actorId: ACTOR_ID, expectedRevision: state.revision,
      primaryOptionId: grab.optionId, fallbackOptionId: null,
      reason: 'Exercise the engine query grab fixture.', overrideJustification: null,
    });

    expect(resolved).toMatchObject({
      valid: true,
      selectedBranch: 'primary',
      mechanics: { movementCostFeet: 10, actionSlots: [{ actionId: 'grab', targetIds: [TARGET_ID] }] },
    });
  });

  it('refuses a declared dead target with a distinct code', () => {
    const placed = placedState(SEED, new Map<CombatantId, GridCell>([
      [ACTOR_ID, { column: 0, row: 0 }],
      [TARGET_ID, { column: 1, row: 0 }],
    ]));
    const state: EncounterState = {
      ...placed,
      combatants: placed.combatants.map((candidate) => candidate.profile.id === TARGET_ID
        ? { ...candidate, hitPoints: 0, life: 'dead', deathAt: { round: 1, initiativeIndex: 0 } }
        : candidate),
    };

    expect(TURN_PROPOSAL_RESOLVER.resolve(state, {
      actorId: ACTOR_ID, expectedRevision: state.revision,
      primaryOptionId: engineOptionId('option:dead-target-grab'), fallbackOptionId: null,
      reason: 'Exercise the engine query resolution fixture.',
      overrideJustification: null,
    })).toEqual({
      valid: false,
      selectedBranch: 'none',
      refusals: [{
        branch: 'primary',
        code: 'OPTION_NOT_OFFERED',
import { describe, expect, it } from 'vitest';
import { readFileSync } from '../../helpers/test-filesystem';
import { declareTestInputs } from '../../helpers/test-inputs';
import { canonicalJson } from '../../../src/commands/canonical-json';
import { combatantSpace } from '../../../src/combat/combat-rules';
import { traceCombatantLine } from '../../../src/combat/cover';
import { minimumSpaceDistance } from '../../../src/combat/creature-space';
import { encounterMovementWorld } from '../../../src/combat/encounter-movement-world';
import { findPath, findPathToAny } from '../../../src/combat/movement';
import { feet, type CombatantId } from '../../../src/combat/values';
import { defaultEncounterAlertingState } from '../../../src/combat/alerting';
import { projectDmView } from '../../../src/combat/visibility';
import { decodeArenaBasisEnvelopeV1, decodeSessionSnapshotV1 } from '../../../src/vtt/arena-fixture';
import {
  CHALLENGE_ROOM_IDS,
  decodeChallengeRoomProvenanceV1,
  type ChallengeRoomProvenanceV1,
  type OptionSelectorV1,
} from '../../../src/vtt/challenge-room-fixture';
import {
  LEGACY_BASIS_V1_NORMALIZED_OMISSIONS,
  decodeEncounterStateV1,
} from '../../../src/vtt/encounter-state-codec';
import { projectEncounterBoard } from '../../../src/vtt/encounter-board';
import { runCommandBoundaryTransaction } from '../../../src/vtt/engine-round-application';
import { resolveEngineActorOption, availableEngineActorOptions } from '../../../src/vtt/intent-resolver';
import { actorOpportunityReport } from '../../../src/vtt/intel/opportunity-cost';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
import { ARENA_REACTION_OFFER_POLICY } from '../../../src/vtt/reaction-offer-host-policy';
import { regretTurnLegalActions } from '../../../src/vtt/regret/legal-actions';
import { generateRoom, type GeneratedRoom } from '../../../src/vtt/room-generator';

const SEEDS = [5_831_001, 5_831_002, 5_831_003, 5_831_004] as const;
const ENVELOPE_PATHS = {
  5_831_001: 'tests/fixtures/arena-basis-challenge/seed-5831001.json',
  5_831_002: 'tests/fixtures/arena-basis-challenge/seed-5831002.json',
  5_831_003: 'tests/fixtures/arena-basis-challenge/seed-5831003.json',
  5_831_004: 'tests/fixtures/arena-basis-challenge/seed-5831004.json',
} as const;
const SIDECAR_PATHS = {
  5_831_001: 'tests/fixtures/arena-basis-challenge/seed-5831001.provenance.json',
  5_831_002: 'tests/fixtures/arena-basis-challenge/seed-5831002.provenance.json',
  5_831_003: 'tests/fixtures/arena-basis-challenge/seed-5831003.provenance.json',
  5_831_004: 'tests/fixtures/arena-basis-challenge/seed-5831004.provenance.json',
} as const;
const FIXTURE_INPUTS = [
  ...Object.values(ENVELOPE_PATHS),
  ...Object.values(SIDECAR_PATHS),
  'tests/fixtures/arena-basis-brutal/seed-6203001.json',
  'tests/fixtures/arena-basis-los-cover-v1/seed-5762001.json',
] as const;
const testInputs = declareTestInputs({ fixtures: FIXTURE_INPUTS });
const OFFER_ENVIRONMENT = buildOfferEnvironment({
  kind: 'configuration',
  mode: 'legacy_standard',
});
type FixtureInputPath = typeof FIXTURE_INPUTS[number];

async function json(path: FixtureInputPath): Promise<unknown> {
  return JSON.parse(testInputs.fixtures.readText(path)) as unknown;
}

async function room(seed: typeof SEEDS[number]): Promise<GeneratedRoom> {
  return decodeArenaBasisEnvelopeV1(await json(ENVELOPE_PATHS[seed]), { mode: 'challenge' });
}

async function sidecar(seed: typeof SEEDS[number]): Promise<ChallengeRoomProvenanceV1> {
  return decodeChallengeRoomProvenanceV1(await json(SIDECAR_PATHS[seed]));
}

function objectClone(value: unknown): Record<string, unknown> {
  const cloned: unknown = structuredClone(value);
  if (typeof cloned !== 'object' || cloned === null || Array.isArray(cloned)) throw new TypeError('Expected object clone.');
  return cloned as Record<string, unknown>;
}

function stateRecord(envelope: Record<string, unknown>): Record<string, unknown> {
  const encounter = objectClone(envelope['encounter']);
  envelope['encounter'] = encounter;
  const state = objectClone(encounter['state']);
  encounter['state'] = state;
  return state;
}

function monsterId(loaded: GeneratedRoom, suffix: string): CombatantId {
  const found = loaded.encounter.state.combatants.find((entry) => String(entry.profile.id).endsWith(suffix));
  if (found === undefined) throw new Error(`Missing monster ${suffix}.`);
  return found.profile.id;
}

function hypotheticalLongbowExpansionCost(effectiveArmorClass: number): number {
  const firstNoncriticalHit = Math.max(2, effectiveArmorClass - 4);
  const noncriticalHitFaces = Math.max(0, 20 - firstNoncriticalHit);
  return 20 + noncriticalHitFaces * 8 + 8 + 64;
}

function selectorMatches(
  state: GeneratedRoom['encounter']['state'],
  actorId: CombatantId,
  selector: OptionSelectorV1,
): readonly ReturnType<typeof availableEngineActorOptions>[number][] {
  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT, state.revision).filter((option) => {
    const main = option.actionSlots.find((slot) => slot.slot === 'main');
    const bonus = option.actionSlots.find((slot) => slot.slot === 'bonus');
    if (main === undefined || main.use.kind !== selector.requiredMainKind) return false;
    const mainActionId = main.use.kind === 'attack' || main.use.kind === 'multiattack'
      ? main.use.actionId
      : null;
    const components = main.use.kind === 'multiattack'
      ? main.use.components.map((component) => ({
          kind: component.kind,
          actionId: component.actionId,
          targetIds: component.target.kind === 'combatant' ? [component.target.combatantId] : [],
        }))
      : main.use.kind === 'attack'
        ? [{
            kind: main.use.kind,
            actionId: main.use.actionId,
            targetIds: main.use.target.kind === 'combatant' ? [main.use.target.combatantId] : [],
          }]
        : [];
    const bonusSpellId = bonus?.use.kind === 'cast_spell' ? bonus.use.spellId : null;
    const bonusTargetIds = bonus?.use.kind === 'cast_spell'
      ? bonus.use.targets.flatMap((target) => target.kind === 'combatant' ? [target.combatantId] : [])
      : [];
    const resolved = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
    if (!resolved.valid) return false;
    return mainActionId === selector.mainActionId && canonicalJson(components) === canonicalJson(selector.orderedComponents) &&
      bonusSpellId === selector.requiredBonusSpellId && canonicalJson(bonusTargetIds) === canonicalJson(selector.bonusTargetIds) &&
      selector.activationChoice === null &&

exec
/bin/bash -lc "rg -n \"export function monsterProfile|function monsterProfile|const monsterProfile\" tests/unit/combat/fixtures.ts
sed -n '1,180p' tests/unit/combat/fixtures.ts
rg -n \"interface CombatantProfile|type CombatantProfile|actionEconomy|mainAction|bonusAction|speed\" src/combat | head -120
rg -n \"freshMonsterPlanningState\" src/vtt/monster-planning-state.ts && sed -n '1,120p' src/vtt/monster-planning-state.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
13:export function monsterProfile(
import { abilities } from '../../../src/domain/enums';
import {
  characterCombatantProfile,
  combatToken,
  monsterCombatantProfile,
  type CharacterCombatSheet,
  type CombatantProfile,
  type CombatToken,
  type SpellSlotCapacity,
} from '../../../src/combat/combatant';
import { monsterStatblock } from '../../../src/combat/statblock';

export function monsterProfile(
  key: string,
  options: {
    readonly hitPoints?: number;
    readonly initiativeBonus?: number;
    readonly attacksPerAction?: number;
    readonly usesDeathSaves?: boolean;
    readonly conditionImmunities?: readonly string[];
    readonly constitutionSaveBonus?: number;
  } = {},
): CombatantProfile {
  const statblock = monsterStatblock({
    id: `statblock:${key}`,
    name: key,
    armorClass: 12,
    hitPointMaximum: options.hitPoints ?? 10,
    speedFeet: 30,
    initiativeBonus: options.initiativeBonus ?? 0,
    savingThrowBonuses: {
      strength: 0,
      dexterity: 0,
      constitution: options.constitutionSaveBonus ?? 0,
      intelligence: 0,
      wisdom: 0,
      charisma: 0,
    },
    ...(options.attacksPerAction === undefined
      ? {}
      : { attacksPerAction: options.attacksPerAction }),
    ...(options.conditionImmunities === undefined
      ? {}
      : { conditionImmunities: options.conditionImmunities }),
    ...(options.usesDeathSaves === undefined
      ? {}
      : { usesDeathSaves: options.usesDeathSaves }),
  });
  const profile = monsterCombatantProfile(statblock, {
    combatantId: `combatant:${key}`,
    tokenId: `token:${key}`,
  });
  return { ...profile, rules: { ...profile.rules, sizeCategory: 'Medium' } };
}

export function characterSheet(
  key: string,
  options: {
    readonly hitPoints?: number;
    readonly initiativeBonus?: number;
    readonly attacksPerAction?: number;
  } = {},
): CharacterCombatSheet {
  const initiativeBonus = options.initiativeBonus ?? 5;
  return {
    character_id: 1,
    name: key,
    creature_classification: { type: 'Humanoid', size: 'Medium' },
    hit_point_maximum: {
      id: 'hit_point_maximum',
      label: 'Hit Point maximum',
      value: options.hitPoints ?? 10,
      formula: 'fixture',
    },
    walking_speed: { kind: 'known', value: 30, detail: 'fixture' },
    armor_class: {
      id: 'armor_class',
      label: 'Armor Class',
      value: 14,
      formula: 'fixture',
      winner: {
        label: 'Unarmored',
        source: 'manual',
        expression: '10 + DEX',
        total: 14,
      },
      shields: [],
      bonuses: [],
      excluded: [],
      tie_break: null,
    },
    initiative: {
      id: 'initiative',
      label: 'Initiative',
      value: initiativeBonus,
      formula: 'fixture',
    },
    ability_scores: abilities.map((ability) => ({
      id: `ability:${ability}`,
      label: ability,
      ability,
      value: ability === 'wisdom' ? 18 : 10,
      score: ability === 'wisdom' ? 18 : 10,
      base_score: ability === 'wisdom' ? 18 : 10,
      increased_score: ability === 'wisdom' ? 18 : 10,
      override_terms: [],
      formula: 'fixture',
    })),
    proficiency_bonus: {
      id: 'proficiency_bonus',
      label: 'Proficiency Bonus',
      value: 2,
      formula: 'fixture',
    },
    saves: abilities.map((ability) => ({
      id: `save:${ability}`,
      label: ability,
      ability,
      value: 0,
      formula: 'fixture',
      proficient: false,
    })),
    attacks_per_action: {
      count: options.attacksPerAction ?? 1,
      unresolved: [],
    },
    damage_resistances: [],
    unchosen_damage_resistances: [],
  };
}

export function playerProfile(
  key: string,
  options: {
    readonly hitPoints?: number;
    readonly initiativeBonus?: number;
    readonly attacksPerAction?: number;
    readonly spellSlots?: readonly SpellSlotCapacity[];
  } = {},
): CombatantProfile {
  return characterCombatantProfile(characterSheet(key, options), {
    combatantId: `combatant:${key}`,
    tokenId: `token:${key}`,
    ...(options.spellSlots === undefined ? {} : { spellSlots: options.spellSlots }),
  });
}

export function placedToken(
  profile: CombatantProfile,
  column: number,
  row = 0,
): CombatToken {
  return combatToken(profile, { column, row });
}
src/combat/conditions.ts:63:      readonly speedPenaltyFeetPerLevel: -5;
src/combat/conditions.ts:73:      readonly speed: 0;
src/combat/conditions.ts:81:      readonly bonusActions: false;
src/combat/conditions.ts:99:      readonly speed: 0;
src/combat/conditions.ts:111:      readonly speed: 0;
src/combat/conditions.ts:123:      readonly movement: 'crawl_or_spend_half_speed_to_end';
src/combat/conditions.ts:132:      readonly speed: 0;
src/combat/conditions.ts:149:      readonly speed: 0;
src/combat/conditions.ts:215:      speedPenaltyFeetPerLevel: -5,
src/combat/conditions.ts:237:      speed: 0,
src/combat/conditions.ts:251:      bonusActions: false,
src/combat/conditions.ts:281:      speed: 0,
src/combat/conditions.ts:299:      speed: 0,
src/combat/conditions.ts:323:      movement: 'crawl_or_spend_half_speed_to_end',
src/combat/conditions.ts:338:      speed: 0,
src/combat/conditions.ts:367:      speed: 0,
src/combat/conditions.ts:398:      readonly speedPenaltyFeet: number;
src/combat/conditions.ts:403:  | { readonly kind: 'speed_zero' }
src/combat/conditions.ts:413:      readonly bonusAction: false;
src/combat/conditions.ts:438:      readonly options: 'crawl_or_spend_half_speed_to_end';
src/combat/conditions.ts:506:          speedPenaltyFeet: row.mechanics.speedPenaltyFeetPerLevel * condition.level,
src/combat/conditions.ts:525:          { kind: 'speed_zero' },
src/combat/conditions.ts:533:          { kind: 'inactive', action: false, bonusAction: false, reaction: false },
src/combat/conditions.ts:549:          { kind: 'inactive', action: false, bonusAction: false, reaction: false },
src/combat/conditions.ts:552:          { kind: 'speed_zero' },
src/combat/conditions.ts:561:          { kind: 'inactive', action: false, bonusAction: false, reaction: false },
src/combat/conditions.ts:564:          { kind: 'speed_zero' },
src/combat/conditions.ts:587:          { kind: 'speed_zero' },
src/combat/conditions.ts:595:          { kind: 'inactive', action: false, bonusAction: false, reaction: false },
src/combat/conditions.ts:605:          { kind: 'inactive', action: false, bonusAction: false, reaction: false },
src/combat/conditions.ts:608:          { kind: 'speed_zero' },
src/combat/conditions.ts:643:  if (state.clauses.some((clause) => clause.kind === 'speed_zero')) return Number.NEGATIVE_INFINITY;
src/combat/conditions.ts:646:      clause.kind === 'exhaustion' ? penalty + clause.speedPenaltyFeet : penalty,
src/combat/wild-shape.ts:19:  | { readonly kind: 'fly_speed'; readonly minimumDruidLevel: 8 };
src/combat/wild-shape.ts:40:  readonly speed: CombatRulesProfile['speed'];
src/combat/wild-shape.ts:84:  | 'fly_speed_gate'
src/combat/wild-shape.ts:212:    return { kind: 'fly_speed', minimumDruidLevel: 8 };
src/combat/wild-shape.ts:229:    speed: form.speed,
src/combat/wild-shape.ts:289:    speed: beast.speed,
src/combat/movement-evaluator.ts:38:  readonly speedPerTurn: Feet | null;
src/combat/movement-evaluator.ts:164:          | 'move_within_speed_to_enable_attack'
src/combat/movement-evaluator.ts:174:          | 'effective_speed_unresolved';
src/combat/movement-evaluator.ts:191:        | 'movement_speed_zero'
src/combat/movement-evaluator.ts:199:        | 'effective_speed_unresolved';
src/combat/movement-evaluator.ts:431:    if (input.speedPerTurn === null) {
src/combat/movement-evaluator.ts:432:      return { status: 'unresolved', reason: 'effective_speed_unresolved' };
src/combat/movement-evaluator.ts:434:    if (path.cost <= input.speedPerTurn) {
src/combat/movement-evaluator.ts:435:      return { status: 'resolved', kind: 'move_within_speed_to_enable_attack' };
src/combat/movement-evaluator.ts:451:): number | 'speed_zero' | 'future_action_unavailable' | 'speed_unresolved' {
src/combat/movement-evaluator.ts:460:  if (input.speedPerTurn === null) return 'speed_unresolved';
src/combat/movement-evaluator.ts:461:  if (input.speedPerTurn === feet(0)) return 'speed_zero';
src/combat/movement-evaluator.ts:466:      ? input.speedPerTurn
src/combat/movement-evaluator.ts:472:  if (costAfterCurrentTurn <= input.speedPerTurn) return 1;
src/combat/movement-evaluator.ts:473:  const preparationTurnMovement = input.speedPerTurn *
src/combat/movement-evaluator.ts:476:    (costAfterCurrentTurn - input.speedPerTurn) / preparationTurnMovement,
src/combat/movement-evaluator.ts:538:  if (legalTurns === 'speed_unresolved') {
src/combat/movement-evaluator.ts:539:    return { status: 'unresolved', reason: 'effective_speed_unresolved' };
src/combat/movement-evaluator.ts:541:  if (legalTurns === 'speed_zero') {
src/combat/movement-evaluator.ts:542:    return { status: 'unreachable', reason: 'movement_speed_zero' };
src/combat/statblock.ts:235:  | { readonly kind: 'speed_reduction'; readonly feet: 0; readonly duration: 'until_start_of_monster_next_turn' }
src/combat/statblock.ts:301:        readonly borrowedTargetStatistics: readonly ['speed', 'strength_modifier', 'dexterity_modifier', 'constitution_modifier'];
src/combat/statblock.ts:338:  readonly actionEconomy: 'action' | 'bonus_action';
src/combat/statblock.ts:358:      readonly movement: 'half_speed' | 'speed';
src/combat/statblock.ts:513:        readonly distance: 'half_speed';
src/combat/statblock.ts:592:  readonly bonusActions: DecodedField<readonly MonsterBonusAction[]>;
src/combat/statblock.ts:614:  readonly bonusActions: DecodedField<readonly MonsterBonusAction[]>;
src/combat/statblock.ts:627:  readonly speedFeet: number;
src/combat/statblock.ts:645:  readonly speed: Feet;
src/combat/statblock.ts:769:    case 'speed_reduction':
src/combat/statblock.ts:930:      exactTuple(mechanic.failure.borrowedTargetStatistics, ['speed', 'strength_modifier', 'dexterity_modifier', 'constitution_modifier'], `${label} borrowed statistics`);
src/combat/statblock.ts:1059:      exactValue(action.movement.distance, 'half_speed', 'Swoop movement distance');
src/combat/statblock.ts:1091:    conditionImmunities: absent(), traits: absent(), actions: absent(), bonusActions: absent(), reactions: absent(),
src/combat/statblock.ts:1096:function validateSourceDetails(input: MonsterSourceDetailsInput | undefined, speedFeet: number): MonsterSourceDetails {
src/combat/statblock.ts:1123:  if (walk === undefined || walk.feet !== speedFeet) throw new RangeError('Detailed walking speed must match combat walking speed.');
src/combat/statblock.ts:1124:  input.movement.forEach((movement) => nonNegativeInteger(movement.feet, `${movement.kind} speed`));
src/combat/statblock.ts:1193:    bonusActions: validateDecoded(input.bonusActions, 'Bonus actions', (bonusActions) => bonusActions.map((action) => validateAction(action))),
src/combat/statblock.ts:1214:  const sourceDetails = validateSourceDetails(input.sourceDetails, input.speedFeet);
src/combat/statblock.ts:1271:    hitPointMaximum, speed: feet(input.speedFeet), initiativeBonus: finiteInteger(input.initiativeBonus, 'Monster Initiative bonus'),
src/combat/movement.ts:413:  readonly speed: Feet;
src/combat/movement.ts:418:export function startTurnMovement(speed: Feet): TurnMovement {
src/combat/movement.ts:419:  const checkedSpeed = validatedCost(speed);
src/combat/movement.ts:420:  return { speed: checkedSpeed, spent: feet(0), remaining: checkedSpeed };
src/combat/movement.ts:433:    speed: current.speed,
src/combat/movement.ts:435:    remaining: feet(current.speed - spent),
src/combat/combatant.ts:48:  readonly speed: Feet;
src/combat/combatant.ts:102:export type CombatantProfile =
src/combat/combatant.ts:142:  | 'walking_speed'
src/combat/combatant.ts:183:  if (sheet.walking_speed.kind === 'unknown') {
src/combat/combatant.ts:209:      speed: feet(sheet.walking_speed.value),
src/combat/combatant.ts:250:    ...(statblock.sourceDetails.bonusActions.kind === 'present'
src/combat/combatant.ts:251:      ? statblock.sourceDetails.bonusActions.value.filter(
src/combat/combatant.ts:274:      speed: statblock.speed,
src/combat/movement-speeds.ts:23: * Establishes the non-empty, walk-present, unique-by-kind speed-set invariant.
src/combat/movement-speeds.ts:36:        'Movement speed feet must be a finite, non-negative number.',
src/combat/movement-speeds.ts:43:      throw new RangeError('Only a fly movement speed can carry hover.');
src/combat/movement-speeds.ts:51:    throw new RangeError('Movement speeds must include walk.');
src/combat/events.ts:200:      readonly bonusActionGrantEffectId?: EncounterEffectId;
src/combat/spells/kb/entries.ts:180:  entry('R-SPELL-120', 'fly', '3375', 'Grant a willing touched creature 60-foot flying speed and hover, adding one target per higher slot.'),
src/combat/spells/definitions.ts:192:    operation: { kind: 'attack_damage', attackKind: 'ranged', damageType: damageType('Cold'), dice: cantripDamage(1, 8), rider: effect({ kind: 'movement_modifier', speedDeltaFeet: -10 }) },
src/combat/spells/definitions.ts:548:    operation: { kind: 'effect', effect: effect({ kind: 'movement_modifier', speedDeltaFeet: 10 }, { durationRounds: 600 }) },
src/combat/spells/definitions.ts:1031:    operation: { kind: 'effect', effect: effect({ kind: 'flight', speedFeet: 60, canHover: true, fallsWhenEffectEnds: true }, { concentration: true, durationRounds: 100 }) },
src/combat/spells/definitions.ts:1049:    operation: { kind: 'effect', effect: effect({ kind: 'haste', speedMultiplier: 2, armorClassBonus: 2, dexteritySaveMode: 'advantage', extraActionOptions: ['attack_once', 'dash', 'disengage', 'hide', 'utilize'], lethargyCondition: 'Incapacitated', lethargySpeedFeet: 0, lethargyRounds: 1 }, { concentration: true, durationRounds: 10 }) },
src/combat/spells/definitions.ts:1055:    operation: { kind: 'save_effect', ability: 'wisdom', rollMode: 'normal', effect: effect({ kind: 'hypnotic_pattern', conditions: ['Charmed', 'Incapacitated'], speedFeet: 0, endsOnDamage: true, wakeAction: true }, { concentration: true, durationRounds: 10 }) },
src/combat/spells/definitions.ts:1097:    operation: { kind: 'utility', effect: { kind: 'phantom_steed', speedFeet: 100, travelMilesPerHour: 13, equipmentVanishDistanceFeet: 10, fadeRounds: 10 }, concentration: false, durationRounds: 600 },
src/combat/spells/definitions.ts:1138:    operation: { kind: 'save_effect', ability: 'wisdom', rollMode: 'normal', effect: effect({ kind: 'slow', speedMultiplier: 0.5, armorClassPenalty: 2, dexteritySavePenalty: 2, reactionsAllowed: false, actionOrBonusOnly: true, attacksPerAction: 1, somaticSpellFailurePercent: 25 }, { concentration: true, durationRounds: 10, expiresAt: 'target_end', repeatedSave: { ability: 'wisdom', rollMode: 'normal', timing: 'target_end' } }) },
src/combat/spells/definitions.ts:1178:                speedChange: { kind: 'reduce', reduction: { kind: 'multiplier', multiplier: 0.5 } },
src/combat/spells/definitions.ts:1264:    operation: { kind: 'save_effect', ability: 'wisdom', rollMode: 'normal', effect: effect({ kind: 'confusion_area', placement: 'selected_when_cast', behaviorDieSides: 10, directionDieSides: 4, bonusActionsAllowed: false, reactionsAllowed: false }, { concentration: true, durationRounds: 10, expiresAt: 'target_end', repeatedSave: { ability: 'wisdom', rollMode: 'normal', timing: 'target_end' } }) },
src/combat/spells/types.ts:541:  readonly speedFeet: number;
src/combat/spells/types.ts:834:        readonly speed: { readonly kind: 'fixed'; readonly feet: number } | { readonly kind: 'walking_speed' };
src/combat/spells/types.ts:855:      readonly kind: 'speed_modification';
src/combat/spells/types.ts:857:        | { readonly kind: 'set'; readonly speedFeet: number }
src/combat/spells/types.ts:1180:  'speed_modification',
src/combat/effects.ts:392:      readonly speedDeltaFeet: number;
src/combat/effects.ts:396:      readonly speedChange: 
src/combat/effects.ts:397:        | { readonly kind: 'set'; readonly speedFeet: number }
src/combat/effects.ts:402:        readonly speed: { readonly kind: 'fixed'; readonly feet: number } | { readonly kind: 'walking_speed' };
src/combat/effects.ts:866:      readonly speedFeet: number;
src/combat/effects.ts:893:      readonly speedMultiplier: 2;
src/combat/effects.ts:904:      readonly speedFeet: 0;
src/combat/effects.ts:948:      readonly speedFeet: number;
67:export function freshMonsterPlanningState(state: EncounterState): EncounterState {
import type { EncounterState } from '../combat/encounter';
import { monsterSpellMaximumUses, monsterSpellResourcePoolId } from '../combat/statblock';
import { feet, type CombatantId } from '../combat/values';
import { declaredMonsterBonusActions } from './engine-query-port';

function withChoicePools(state: EncounterState): EncounterState {
  return {
    ...state,
    combatants: state.combatants.map((combatant) => {
      if (combatant.profile.kind !== 'monster') return combatant;
      const existing = combatant.limitedResources ?? [];
      const additions = declaredMonsterBonusActions(state, combatant.profile.id).flatMap((action) => {
        if (action.kind !== 'spell_choice') return [];
        return action.spells.flatMap((spell) => {
          const id = monsterSpellResourcePoolId(action.id, spell);
          const maximum = monsterSpellMaximumUses(spell);
          return id === null || maximum === null || existing.some((pool) => pool.id === id)
            ? []
            : [{ id, maximum, remaining: maximum, recharge: 'long_rest' as const }];
        });
      });
      const uniqueAdditions = [...new Map(additions.map((pool) => [pool.id, pool] as const)).values()];
      return uniqueAdditions.length === 0
        ? combatant
        : { ...combatant, limitedResources: [...existing, ...uniqueAdditions] };
    }),
  };
}

function resetMonsterTurns(
  state: EncounterState,
  actorIds: ReadonlySet<CombatantId>,
  preserveActiveActor: boolean,
): EncounterState {
  return {
    ...state,
    combatants: state.combatants.map((combatant) =>
      combatant.profile.kind !== 'monster' || !actorIds.has(combatant.profile.id) ||
        (preserveActiveActor && combatant.profile.id === state.activeCombatant)
        ? combatant
        : ({
          ...combatant,
          turn: {
            ...combatant.turn,
            action: { kind: 'available' },
            bonusActionAvailable: true,
            reactionAvailable: true,
            movement: {
              speed: feet(combatant.profile.rules.speed),
              remaining: feet(combatant.profile.rules.speed),
              spent: feet(0),
            },
          },
        })),
  };
}

/** Requested non-active monsters are viewed at the fresh turn they will receive before execution. */
export function projectFutureMonsterTurns(
  state: EncounterState,
  actorIds: readonly CombatantId[],
): EncounterState {
  return withChoicePools(resetMonsterTurns(state, new Set(actorIds), true));
}

/** A direct fixture session starts with every monster at a fresh planning window. */
export function freshMonsterPlanningState(state: EncounterState): EncounterState {
  const actors = state.combatants.flatMap((combatant) =>
    combatant.profile.kind === 'monster' ? [combatant.profile.id] : []);
  return withChoicePools(resetMonsterTurns(state, new Set(actors), false));
}

exec
/bin/bash -lc "rg -n \"kind: 'saving_throw'\" src/combat/statblocks tests/unit/vtt | head -120
rg -n \"export const .*SPIDER|GIANT_SPIDER|WEB\" src/combat/statblocks -S
rg -n \"GOBLIN_WARRIOR|GIANT_SPIDER\" src/combat/statblocks/monsters.ts tests/unit/vtt/engine-query-port.test.ts | head -80
sed -n '1,100p' tests/unit/vtt/engine-query-port.test.ts
sed -n '360,440p' tests/unit/vtt/engine-query-port.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
src/combat/statblocks/companions.ts:127:    ? [{ kind: 'saving_throw' as const, id: 'fell-glare', name: 'Fell Glare', savingThrow: { ability: 'wisdom' as const, dc: context.spellSaveDc }, target: { rangeFeet: 60, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Frightened', null, { duration: 'until_end_of_monster_next_turn' })] }, success: { kind: 'none' as const } }]
src/combat/statblocks/companions.ts:156:    ? [{ kind: 'saving_throw' as const, id: 'venomous-spew', name: 'Venomous Spew', savingThrow: { ability: 'constitution' as const, dc: context.spellSaveDc }, target: { rangeFeet: 10, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Poisoned', null, { duration: 'until_start_of_monster_next_turn' })] }, success: { kind: 'none' as const } }]
src/combat/statblocks/undead-crypt.ts:126:      { kind: 'saving_throw', id: 'life-drain', name: 'Life Drain', savingThrow: { ability: 'constitution', dc: 13 }, target: { rangeFeet: 5, maximumSize: null, excludedKinds: [] }, failure: { damage: [damage(6, 1, 8, 2, 'Necrotic')], effects: [{ kind: 'hit_point_maximum_reduction', amount: 'damage_taken' }, { kind: 'raises_as_zombie', targetKind: 'Humanoid', delayHours: 24, controllerLimit: 12, preventedBy: ['restored_to_life', 'body_destroyed'] }] }, success: { kind: 'none' } },
tests/unit/vtt/plays-v1.test.ts:60:            : { kind: 'saving_throw', actionId: engineActionId(`${name}:grab`), target: { kind: 'combatant', combatantId: targetId ?? SCOUT } },
src/combat/statblocks/astral-tower.ts:89:        kind: 'saving_throw', id: 'whirlwind', name: 'Whirlwind', savingThrow: { ability: 'strength', dc: 13 },
src/combat/statblocks/astral-tower.ts:203:        kind: 'saving_throw', id: 'unsettling-visage', name: 'Unsettling Visage', savingThrow: { ability: 'wisdom', dc: 12 },
src/combat/statblocks/astral-tower.ts:306:        kind: 'saving_throw', id: 'horrific-visage', name: 'Horrific Visage', savingThrow: { ability: 'wisdom', dc: 13 },
src/combat/statblocks/astral-tower.ts:318:        kind: 'saving_throw', id: 'possession', name: 'Possession', savingThrow: { ability: 'charisma', dc: 13 },
src/combat/statblocks/homebrew-beast-families.ts:181:      const web: MonsterAction = { kind: 'saving_throw', id: 'web', name: 'Web', savingThrow: { ability: 'dexterity', dc }, target: { rangeFeet: 30 + index * 5, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Restrained', null, { escapeDc: dc, duration: 'until_escape' })] }, success: { kind: 'none' } };
src/combat/statblocks/goblinoid-warband.ts:80:    bonusActions: present([{ kind: 'saving_throw', id: 'quick-grapple', name: 'Quick Grapple', savingThrow: { ability: 'dexterity', dc: 13 }, target: { rangeFeet: 10, maximumSize: 'Medium', excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Grappled', 'Medium', { escapeDc: 13, duration: 'until_escape' })] }, success: { kind: 'none' } }]),
src/combat/statblocks/wild-beasts.ts:74:      { kind: 'saving_throw', id: 'roar', name: 'Roar', savingThrow: { ability: 'wisdom', dc: 11 }, target: { rangeFeet: 15, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Frightened', null, { duration: 'until_start_of_monster_next_turn' })] }, success: { kind: 'none' } },
src/combat/statblocks/wild-beasts.ts:177:      { kind: 'saving_throw', id: 'web', name: 'Web', savingThrow: { ability: 'dexterity', dc: 13 }, target: { rangeFeet: 60, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Restrained', null, { duration: 'until_escape' })] }, success: { kind: 'none' } },
src/combat/statblocks/wild-beasts.ts:192:      { kind: 'saving_throw', id: 'constrict', name: 'Constrict', savingThrow: { ability: 'strength', dc: 14 }, target: { rangeFeet: 10, maximumSize: 'Large', excludedKinds: [] }, failure: { damage: [damage(13, 2, 8, 4, 'Bludgeoning')], effects: [conditionOnHit('Grappled', 'Large', { escapeDc: 14, duration: 'until_escape' })] }, success: { kind: 'none' } },
src/combat/statblocks/wild-beasts.ts:227:    bonusActions: present([{ kind: 'saving_throw', id: 'trample', name: 'Trample', savingThrow: { ability: 'dexterity', dc: 16 }, target: { rangeFeet: 5, maximumSize: null, excludedKinds: [] }, failure: { damage: [damage(17, 2, 10, 6, 'Bludgeoning')], effects: [] }, success: { kind: 'half_damage' } }]), reactions: notListed('reactions'),
tests/unit/vtt/offer-environment.test.ts:67:      resolution: { kind: 'saving_throw', ability: 'dexterity' },
src/combat/statblocks/roster.ts:4:  CAMEL, CROCODILE, DIRE_WOLF, ELEPHANT, GHAST, GHOUL, GIANT_CONSTRICTOR_SNAKE, GIANT_CROCODILE, GIANT_SCORPION, GIANT_SHARK, GIANT_SPIDER, GOBLIN_BOSS, GOBLIN_MINION, GOBLIN_WARRIOR, GUARD,
src/combat/statblocks/roster.ts:69:  { id: 'statblock:giant-spider', name: 'Giant Spider', family: 'wild_beasts', challengeRating: 1, source: [{ path: SRD_PATH, lineStart: 23313, lineEnd: 23350 }], selectionNote: null, statblock: GIANT_SPIDER },
src/combat/statblocks/wild-beasts.ts:169:export const GIANT_SPIDER = monsterStatblock({
tests/unit/vtt/engine-query-port.test.ts:13:import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
tests/unit/vtt/engine-query-port.test.ts:14:import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
tests/unit/vtt/engine-query-port.test.ts:117:  const actorBase = monsterCombatantProfile(GOBLIN_WARRIOR, {
tests/unit/vtt/engine-query-port.test.ts:145:  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
tests/unit/vtt/engine-query-port.test.ts:471:    const actor = monsterCombatantProfile(GOBLIN_WARRIOR, {
src/combat/statblocks/monsters.ts:87:export const GOBLIN_WARRIOR = monsterStatblock({
src/combat/statblocks/monsters.ts:106:export const HOBGOBLIN_WARRIOR = monsterStatblock({
import ts from 'typescript';
import { describe, expect, it } from 'vitest';
import {
  canCombatantSee,
  createEncounter,
  detectCombatant,
  reduceEncounter,
  type EncounterState,
} from '../../../src/combat/encounter';
import { traceCombatantLine } from '../../../src/combat/cover';
import { monsterCombatantProfile } from '../../../src/combat/combatant';
import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
import type { GridCell } from '../../../src/combat/grid';
import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
import {
  armorClass,
  combatantId,
  feet,
  statblockId,
  worldObjectId,
  type CombatantId,
} from '../../../src/combat/values';
import { projectDmView } from '../../../src/combat/visibility';
import { ENCOUNTER_VIEW_CLASSIFICATION } from '../../../src/combat/visibility';
import { projectEncounterBoard } from '../../../src/vtt/encounter-board';
import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
import { monsterActions, monsterBonusActions } from '../../../src/vtt/engine-query-port';
import {
  availableEngineActorOptions,
  createPureTurnProposalResolver,
  engineActorOptionsForEnvironment,
  resolveEngineActorOption,
} from '../../../src/vtt/intent-resolver';
import { engineOptionId } from '../../../src/vtt/turn-proposal';
import { generateRoom } from '../../../src/vtt/room-generator';
import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
import { readFileSync } from '../../helpers/test-filesystem';

const SEED = 3_943_001;
const ACTOR_ID = combatantId('combatant:generated-3943001-monster-2');
const TARGET_ID = combatantId('combatant:fighter');
const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
const TURN_PROPOSAL_RESOLVER = createPureTurnProposalResolver(OFFER_ENVIRONMENT);

const AGREEMENT_FIXTURE_PATHS = [
  ...Array.from(
    { length: 13 },
    (_unused, index) => `tests/fixtures/arena-basis-hard/seed-${String(5_117_001 + index)}.json`,
  ),
  ...Array.from(
    { length: 10 },
    (_unused, index) => `tests/fixtures/arena-basis-brutal/seed-${String(6_203_001 + index)}.json`,
  ),
  ...Array.from(
    { length: 10 },
    (_unused, index) => `tests/fixtures/arena-basis-brutal-b/seed-${String(6_206_001 + index)}.json`,
  ),
] as const;

function legacyDocument(
  state: EncounterState,
  foggedCells: unknown,
): Record<string, unknown> {
  return {
    ...(JSON.parse(JSON.stringify(state)) as Record<string, unknown>),
    foggedCells,
  };
}

function minimalState(key: string, bounds: EncounterState['bounds']): EncounterState {
  const actor = monsterProfile(key);
  return createEncounter({
    bounds,
    combatants: [actor],
    tokens: [placedToken(actor, 0)],
  });
}

function placedState(
  seed: number,
  positions: ReadonlyMap<CombatantId, GridCell>,
  difficultCells: readonly GridCell[] = [],
  rows?: number,
): EncounterState {
  const state = generateRoom(seed).encounter.state;
  return freshMonsterPlanningState({
    ...state,
    bounds: rows === undefined ? state.bounds : { ...state.bounds, rows },
    blockedCells: [],
    worldObjects: [],
    persistentAreas: [],
    environment: {
      ...state.environment,
      difficultTerrainRegions: difficultCells.length === 0
    const state = minimalState('fresh-state-fog-actor', { columns: 2, rows: 1 });
    const bytes = JSON.stringify(state);
    const decoded = decodeEncounterStateV1(JSON.parse(bytes) as unknown, 'session');

    expect(bytes).not.toContain('foggedCells');
    expect('foggedCells' in decoded).toBe(false);
    expect(decoded).toEqual(state);
  });

  it('offers a modeled base attack despite an unresolved rider and partitions idle Disengage', () => {
    const generated = placedState(SEED, new Map<CombatantId, GridCell>([
      [ACTOR_ID, { column: 0, row: 0 }],
      [TARGET_ID, { column: 5, row: 0 }],
    ]));
    const state: EncounterState = {
      ...generated,
      combatants: generated.combatants.map((combatant) => combatant.profile.id === ACTOR_ID && combatant.profile.kind === 'monster'
        ? { ...combatant, profile: { ...combatant.profile, statblockId: statblockId('statblock:black-pudding') } }
        : combatant),
    };

    expect(OFFER_ENVIRONMENT.queries.actions(state, ACTOR_ID).map((action) => action.id))
      .toEqual(['dissolving-pseudopod']);
    const options = availableEngineActorOptions(state, ACTOR_ID, OFFER_ENVIRONMENT);
    expect(options.map(({ label }) => label)).toEqual([
      'Dash',
      'Dissolving Pseudopod -> combatant:fighter',
      'Dodge',
      'End Turn',
    ]);
    expect(options.flatMap(({ actionSlots }) => actionSlots.map(({ use }) => use.kind))).toEqual([
      'dash', 'attack', 'dodge', 'end_turn',
    ]);
    expect(engineActorOptionsForEnvironment(state, ACTOR_ID, OFFER_ENVIRONMENT).humanOnly)
      .toContainEqual(expect.objectContaining({
      label: 'Disengage',
      noModeledEffect: { kind: 'disengage_without_movement', action: 'disengage' },
    }));
  });

  it('returns independently hand-computed path legality and movement costs', () => {
    const occupantId = TARGET_ID;
    const calls = [
      { case: 'normal_move', actor: { column: 0, row: 0 }, destination: { column: 2, row: 0 }, difficultCells: [], maximumFeet: 30, movement: 'normal' },
      { case: 'difficult_terrain', actor: { column: 0, row: 0 }, destination: { column: 2, row: 0 }, difficultCells: [{ column: 1, row: 0 }, { column: 2, row: 0 }], maximumFeet: 30, movement: 'normal' },
      { case: 'dash', actor: { column: 0, row: 0 }, destination: { column: 7, row: 0 }, difficultCells: [], maximumFeet: 60, movement: 'dash' },
      { case: 'occupied_endpoint', actor: { column: 0, row: 0 }, occupant: { column: 2, row: 0 }, destination: { column: 2, row: 0 }, difficultCells: [], maximumFeet: 30, movement: 'normal' },
    ] as const;
    for (const call of calls) {
      const positions = new Map<CombatantId, GridCell>([[ACTOR_ID, call.actor]]);
      if ('occupant' in call) positions.set(occupantId, call.occupant);
      const state = placedState(
        SEED,
        positions,
        call.difficultCells,
        call.case === 'difficult_terrain' ? 1 : undefined,
      );
      const canonical = OFFER_ENVIRONMENT.queries.path(state, {
        actorId: ACTOR_ID,
        destination: call.destination,
        movement: call.movement,
        ...(call.movement === 'normal' ? { maximumFeet: call.maximumFeet } : {}),
      });

      if (call.case === 'normal_move') {
        // Two ordinary five-foot entries: 2 * 5 = 10 feet.
        expect(canonical).toMatchObject({ legal: true, costFeet: 10, budgetFeet: 30 });
      } else if (call.case === 'difficult_terrain') {
        // A one-row board forces two difficult entries: 2 * (5 * 2) = 20 feet.
        expect(canonical).toMatchObject({ legal: true, costFeet: 20, budgetFeet: 30 });
      } else if (call.case === 'dash') {
        // Seven ordinary entries cost 35 feet; Dash supplies the second 30-foot budget.
        expect(canonical).toMatchObject({ legal: true, costFeet: 35, budgetFeet: 60 });
      } else {
        // A hostile living creature occupies the requested endpoint.
        expect(canonical).toEqual({ legal: false, code: 'destination_unreachable' });
      }
    }
  });

  it('withholds Dash when an enclosed actor has no endpoint closer to its target', () => {

exec
/bin/bash -lc "sed -n '1,130p' tools/d569-second-family-manifest.ts
rg -n \"export function generatedRoomIntegrityViolations|function generatedRoomIntegrityViolations\" tools/d569-second-family-manifest.ts
sed -n '500,590p' tools/d569-second-family-manifest.ts
sed -n '140,175p' tests/unit/tools/d569-second-family-manifest.test.ts
sed -n '1,60p' tests/unit/tools/d569-second-family-manifest.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
import { canonicalJson } from '../src/commands/canonical-json';
import { combatantSpace, reduceEncounter } from '../src/combat/encounter';
import { spacesIntersect } from '../src/combat/creature-space';
import { mulberry32 } from '../src/combat/random';
import { BUNDLED_MONSTER_ROSTER, STARTER_MONSTER_ROSTER } from '../src/combat/statblocks/roster';
import { spellDefinition } from '../src/combat/spells/definitions';
import { sha256 } from '../src/crypto/sha256';
import { d466GeneratedRoomOverride } from '../src/vtt/d466-room-overrides';
import { availableEngineActorOptions, resolveEngineActorOption } from '../src/vtt/intent-resolver';
import { decodeArenaFixtureText } from '../src/vtt/mcp/entrypoint';
import { freshMonsterPlanningState } from '../src/vtt/monster-planning-state';
import { buildOfferEnvironment } from '../src/vtt/offers/build-offer-environment';
import {
  BRUTAL_CHALLENGE_BUDGET_SCALE,
  BRUTAL_TERRAIN_FEATURE_COUNT_BAND,
  generateRoom,
  type GeneratedRoom,
  type RoomDifficultyProfile,
} from '../src/vtt/room-generator';
import { z } from 'zod';

const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });

export const D569_SECOND_FAMILY_LEDGER_ENTRY =
  'seed ranges 5118001-5118010 and 6207001-6207010 have no occurrence in .claude/decisions.md, tests/fixtures or tools as of 2026-09-07 (supervisor grep); ranges 5117xxx, 6203xxx, 6204xxx, 6206xxx and 6208xxx are used (primary hard, primary brutal, D466 override/protocol, D572 brutal-b, D572 pool)' as const;

export const D569_SECOND_FAMILY_SEEDS = {
  hard: [
    5_118_001, 5_118_002, 5_118_003, 5_118_004, 5_118_005,
    5_118_006, 5_118_007, 5_118_008, 5_118_009, 5_118_010,
  ],
  brutal: [
    6_207_001, 6_207_002, 6_207_003, 6_207_004, 6_207_005,
    6_207_006, 6_207_007, 6_207_008, 6_207_009, 6_207_010,
  ],
} as const;

export const D569_USED_SEED_NAMESPACES = [
  { label: '5117xxx', start: 5_117_000, end: 5_117_999 },
  { label: '6203xxx', start: 6_203_000, end: 6_203_999 },
  { label: '6204xxx', start: 6_204_000, end: 6_204_999 },
  { label: '6206xxx', start: 6_206_000, end: 6_206_999 },
  { label: '6208xxx', start: 6_208_000, end: 6_208_999 },
] as const;

const manifestFields = {
  generator: z.strictObject({
    tool: z.literal('tools/generate-arena-basis.ts'),
    canonicalFormat: z.literal('canonical-json-with-trailing-newline'),
    independentRegenerations: z.literal(2),
  }),
  ledgerEntry: z.string(),
  usedSeedNamespaces: z.array(z.strictObject({
    label: z.string(),
    start: z.number().int().nonnegative(),
    end: z.number().int().nonnegative(),
  })),
  cohorts: z.array(z.strictObject({
    difficulty: z.enum(['hard', 'brutal']),
    directory: z.string(),
    fixtures: z.array(z.strictObject({
      seed: z.number().int().nonnegative(),
      path: z.string(),
      sha256: z.string().regex(/^[a-f0-9]{64}$/u),
    })),
  })),
} as const;

const manifestSchema = z.discriminatedUnion('version', [
  z.strictObject({
    version: z.literal('d569-second-family-manifest-v1'),
    ...manifestFields,
  }),
  z.strictObject({
    version: z.literal('d569-second-family-manifest-v2'),
    ...manifestFields,
  }),
]);

export type D569SecondFamilyManifest = z.infer<typeof manifestSchema>;
export type D569SecondFamilyDifficulty = keyof typeof D569_SECOND_FAMILY_SEEDS;

export interface D569SecondFamilyValidationAccess {
  readonly readFixture: (path: string) => string;
  readonly regenerate: (
    seed: number,
    difficulty: D569SecondFamilyDifficulty,
    generation: 1 | 2,
  ) => string;
}

export interface D569SecondFamilyViolation {
  readonly code: string;
  readonly message: string;
}

function addViolation(
  violations: D569SecondFamilyViolation[],
  condition: boolean,
  code: string,
  message: string,
): void {
  if (!condition) violations.push({ code, message });
}

function cellsInBounds(
  cells: readonly { readonly column: number; readonly row: number }[],
  bounds: { readonly columns: number; readonly rows: number },
): boolean {
  return cells.every((cell) =>
    Number.isInteger(cell.column) && Number.isInteger(cell.row) &&
    cell.column >= 0 && cell.column < bounds.columns &&
    cell.row >= 0 && cell.row < bounds.rows);
}

/** Shared state/reducer invariants for the existing generator suite and D569 freeze. */
export function generatedRoomIntegrityViolations(
  room: GeneratedRoom,
  fixtureBytes: string = `${canonicalJson(room)}\n`,
): readonly D569SecondFamilyViolation[] {
  const violations: D569SecondFamilyViolation[] = [];
  const state = room.encounter.state;
  addViolation(
    violations,
    state.bounds.columns === room.spec.dimensions.columns &&
      state.bounds.rows === room.spec.dimensions.rows,
    'dimension_mismatch',
    `seed ${String(room.spec.seed)} spec and encounter bounds differ`,
  );
  addViolation(
117:export function generatedRoomIntegrityViolations(
          difficulty === 'hard';
        if (usesLegacyProjection) {
          comparisonFixtureBytes = legacyCommittedProjection(parsed, fixture.seed, violations);
        } else {
          addViolation(
            violations,
            !Object.hasOwn(encounterStateRecord(parsed), 'foggedCells'),
            'unexpected_legacy_fogged_cells',
            `seed ${String(fixture.seed)} new v2 brutal fixture contains encounter.state.foggedCells`,
          );
        }
        addViolation(
          violations,
          room.spec.seed === fixture.seed,
          'fixture_seed',
          `seed ${String(fixture.seed)} fixture contains a different seed`,
        );
        violations.push(...generatedRoomIntegrityViolations(room, fixtureBytes));
        if (difficulty === 'hard') {
          violations.push(...hardRoomMembershipViolations(room));
        } else if (manifest.version === 'd569-second-family-manifest-v1') {
          violations.push(...brutalRoomStaticMembershipViolations(room));
        } else {
          violations.push(...brutalRoomMembershipViolations(room));
        }
      } catch (error) {
        violations.push({
          code: 'fixture_parse',
          message: `seed ${String(fixture.seed)} fixture is invalid JSON: ${error instanceof Error ? error.message : String(error)}`,
        });
      }

      const firstGeneration = access.regenerate(fixture.seed, difficulty, 1);
      const secondGeneration = access.regenerate(fixture.seed, difficulty, 2);
      addViolation(
        violations,
        firstGeneration === comparisonFixtureBytes,
        'first_regeneration',
        `seed ${String(fixture.seed)} first canonical regeneration differs from the fixture`,
      );
      addViolation(
        violations,
        secondGeneration === firstGeneration && secondGeneration === comparisonFixtureBytes,
        'independent_regeneration',
        `seed ${String(fixture.seed)} independent canonical regeneration differs`,
      );
    }
  }
  return violations;
}

export function canonicalD569SecondFamilyRegeneration(
  seed: number,
  difficulty: D569SecondFamilyDifficulty,
  _generation: 1 | 2,
): string {
  return `${canonicalJson(generateRoom(seed, { difficulty: difficulty as RoomDifficultyProfile }))}\n`;
}
  });

  it.each(BRUTAL_FIXTURE_PATHS)(
    '%s retains historical integrity plus static budget, caster, and terrain invariants',
    (path) => {
      const frozen = room(path);
      expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
      expect(brutalRoomMembershipViolations(frozen)
        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
    },
  );

  it('second_family_uses_primary_seed: rejects primary or previously used seed namespaces', () => {
    const changed = structuredClone(manifest());
    const hard = changed.cohorts.find((cohort) => cohort.difficulty === 'hard');
    if (hard === undefined) throw new Error('missing hard cohort');
    hard.fixtures[0] = { ...hard.fixtures[0]!, seed: 5_117_001 };
    expect(codes(changed)).toContain('cohort_seed_range');
    expect(codes(changed)).toContain('seed_overlap');
  });

  it('brutal_membership_checks_name_only: rejects structural failures even with the roster names intact', () => {
    const brutal = structuredClone(room(BRUTAL_FIXTURE_PATHS[0]));
    const lastMonster = brutal.encounter.state.combatants.findLast((combatant) =>
      combatant.profile.kind === 'monster');
    if (lastMonster === undefined) throw new Error('missing brutal monster');
    const broken: GeneratedRoom = {
      ...brutal,
      encounter: {
        ...brutal.encounter,
        state: {
          ...brutal.encounter.state,
          combatants: brutal.encounter.state.combatants.map((combatant) =>
            combatant.profile.id === lastMonster.profile.id
              ? { ...combatant, hitPoints: 0, life: 'dead' }
              : combatant),
import { describe, expect, it } from 'vitest';
import { canonicalJson } from '../../../src/commands/canonical-json';
import { sha256 } from '../../../src/crypto/sha256';
import type { GeneratedRoom } from '../../../src/vtt/room-generator';
import {
  D569_SECOND_FAMILY_LEDGER_ENTRY,
  D569_SECOND_FAMILY_SEEDS,
  brutalRoomMembershipViolations,
  canonicalD569SecondFamilyRegeneration,
  generatedRoomIntegrityViolations,
  hardRoomMembershipViolations,
  validateD569SecondFamilyManifest,
  type D569SecondFamilyManifest,
  type D569SecondFamilyValidationAccess,
} from '../../../tools/d569-second-family-manifest';
import { declareTestInputs } from '../../helpers/test-inputs';

const MANIFEST_PATH = 'tests/fixtures/d569-second-family-manifest.json' as const;
const HARD_FIXTURE_PATHS = [
  'tests/fixtures/arena-basis-hard-2/seed-5118001.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118002.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118003.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118004.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118005.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118006.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118007.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118008.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118009.json',
  'tests/fixtures/arena-basis-hard-2/seed-5118010.json',
] as const;
const BRUTAL_FIXTURE_PATHS = [
  'tests/fixtures/arena-basis-brutal-2/seed-6207001.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207002.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207003.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207004.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207005.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207006.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207007.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207008.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207009.json',
  'tests/fixtures/arena-basis-brutal-2/seed-6207010.json',
] as const;
const ALL_FIXTURE_PATHS = [...HARD_FIXTURE_PATHS, ...BRUTAL_FIXTURE_PATHS] as const;
const inputs = declareTestInputs({ fixtures: [MANIFEST_PATH, ...ALL_FIXTURE_PATHS] });
const fixtureBytes = new Map<string, string>(ALL_FIXTURE_PATHS.map((path) => [
  path,
  inputs.fixtures.readText(path),
]));

function manifest(): D569SecondFamilyManifest {
  return JSON.parse(inputs.fixtures.readText(MANIFEST_PATH)) as D569SecondFamilyManifest;
}

function room(path: (typeof ALL_FIXTURE_PATHS)[number]): GeneratedRoom {
  return JSON.parse(inputs.fixtures.readText(path)) as GeneratedRoom;
}

function structuralDifferencePaths(left: unknown, right: unknown, path = '$'): readonly string[] {
  if (Object.is(left, right)) return [];
  if (Array.isArray(left) && Array.isArray(right)) {

exec
/bin/bash -lc "sed -n '110,255p' tools/d569-second-family-manifest.ts
sed -n '130,190p' tests/unit/vtt/engine-query-port.test.ts
sed -n '160,205p' src/combat/statblocks/wild-beasts.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
  return cells.every((cell) =>
    Number.isInteger(cell.column) && Number.isInteger(cell.row) &&
    cell.column >= 0 && cell.column < bounds.columns &&
    cell.row >= 0 && cell.row < bounds.rows);
}

/** Shared state/reducer invariants for the existing generator suite and D569 freeze. */
export function generatedRoomIntegrityViolations(
  room: GeneratedRoom,
  fixtureBytes: string = `${canonicalJson(room)}\n`,
): readonly D569SecondFamilyViolation[] {
  const violations: D569SecondFamilyViolation[] = [];
  const state = room.encounter.state;
  addViolation(
    violations,
    state.bounds.columns === room.spec.dimensions.columns &&
      state.bounds.rows === room.spec.dimensions.rows,
    'dimension_mismatch',
    `seed ${String(room.spec.seed)} spec and encounter bounds differ`,
  );
  addViolation(
    violations,
    cellsInBounds(room.spec.blockedCells, state.bounds),
    'blocked_cell_invalid',
    `seed ${String(room.spec.seed)} has an out-of-bounds blocked cell`,
  );

  const combatantIds = new Set(state.combatants.map((combatant) => String(combatant.profile.id)));
  const tokenIds = state.tokens.map((token) => String(token.id));
  addViolation(
    violations,
    new Set(tokenIds).size === tokenIds.length && state.tokens.length === state.combatants.length,
    'token_set_invalid',
    `seed ${String(room.spec.seed)} has missing or duplicate tokens`,
  );
  addViolation(
    violations,
    state.tokens.every((token) =>
      combatantIds.has(String(token.combatantId)) && cellsInBounds([token.position], state.bounds)),
    'token_position_invalid',
    `seed ${String(room.spec.seed)} has an invalid token position or combatant reference`,
  );
  try {
    const spaces = state.tokens.map((token) => ({
      token,
      space: combatantSpace(state, token.combatantId),
    }));
    const blocked = new Set(state.blockedCells.map((cell) => `${String(cell.column)},${String(cell.row)}`));
    addViolation(
      violations,
      spaces.every(({ space }) => cellsInBounds(space.cells, state.bounds) &&
        space.cells.every((cell) => !blocked.has(`${String(cell.column)},${String(cell.row)}`))),
      'token_footprint_invalid',
      `seed ${String(room.spec.seed)} has an out-of-bounds or blocked creature footprint`,
    );
    addViolation(
      violations,
      spaces.every((left, leftIndex) => spaces.slice(leftIndex + 1).every((right) =>
        !spacesIntersect(left.space, right.space))),
      'token_footprint_overlap',
      `seed ${String(room.spec.seed)} has overlapping creature footprints`,
    );
  } catch (error) {
    violations.push({
      code: 'token_footprint_invalid',
      message: `seed ${String(room.spec.seed)} has an undecodable creature footprint: ${error instanceof Error ? error.message : String(error)}`,
    });
  }

  const environmentRegions = [
    ...state.environment.difficultTerrainRegions,
    ...state.environment.lightRegions,
    ...state.environment.obscurementRegions,
    ...(state.environment.movementRegions ?? []),
    ...(state.environment.narrowOpeningRegions ?? []),
  ];
  const regionIds = environmentRegions.map((region) => region.id);
  addViolation(
    violations,
    new Set(regionIds).size === regionIds.length && environmentRegions.every((region) =>
      region.id.trim().length > 0 && region.cells.length > 0 && cellsInBounds(region.cells, state.bounds)),
    'environment_region_invalid',
    `seed ${String(room.spec.seed)} has an empty, duplicate, or out-of-bounds environment region`,
  );

  try {
    const loaded = decodeArenaFixtureText(fixtureBytes);
    const reduced = reduceEncounter(loaded, { type: 'roll_initiative' }, mulberry32(901));
    addViolation(
      violations,
      reduced.state.revision === loaded.revision + 1 &&
        reduced.state.initiative.length === loaded.combatants.length,
      'reducer_load_invalid',
      `seed ${String(room.spec.seed)} did not initialize through the encounter reducer`,
    );
  } catch (error) {
    violations.push({
      code: 'reducer_load_invalid',
      message: `seed ${String(room.spec.seed)} failed reducer loading: ${error instanceof Error ? error.message : String(error)}`,
    });
  }
  return violations;
}

function executableControlCaster(room: GeneratedRoom): boolean {
  return room.spec.monsterRoster.some((entry) => {
    const row = STARTER_MONSTER_ROSTER.find((candidate) => candidate.id === entry.statblockId);
    const actions = row?.statblock.sourceDetails.actions;
    return actions?.kind === 'present' && actions.value.some((action) =>
      action.kind === 'spellcasting' && action.execution?.kind !== 'absent' &&
      action.spells.some((spell) => {
        const definition = spell.manifestStatus === 'implemented' ? spellDefinition(spell.id) : undefined;
        return definition?.operation.kind === 'persistent_area' &&
          definition.operation.hooks.some((hook) => hook.effect.kind === 'save_gated') &&
          definition.operation.hooks.some((hook) =>
            hook.effect.kind === 'automatic' && hook.effect.payload.kind === 'effect' &&
            hook.effect.payload.payload.kind === 'movement_modifier');
      }));
  });
}

/** State-derived hard membership; hashes and seed names are deliberately not inputs. */
export function hardRoomMembershipViolations(
  room: GeneratedRoom,
): readonly D569SecondFamilyViolation[] {
  const violations: D569SecondFamilyViolation[] = [];
  const roster = room.spec.monsterRoster;
  const rosterIds = new Set(roster.map((entry) => entry.statblockId));
  const budgetGap = room.spec.challengeBudgetEighths - room.spec.challengeSpentEighths;
  addViolation(violations, room.spec.difficultyProfile === 'hard', 'hard_difficulty', 'room is not tagged hard');
  addViolation(
    violations,
    roster.length >= 4 && roster.length <= 7 && budgetGap >= 0 && budgetGap <= 8,
    'hard_roster_budget',
    'hard roster must contain 4-7 monsters and spend within its generated budget band',
  );
  addViolation(
    violations,
    executableControlCaster(room) && rosterIds.has('statblock:guard') && rosterIds.has('statblock:scout'),
    'hard_required_roles',
    'hard roster must contain an executable control caster plus Guard and Scout',
  );

  const difficultRegions = room.spec.terrain.filter((feature) =>
    feature.kind === 'difficult-terrain-patch');
  const hardFeatures = room.spec.hardFeatures;
      tokens: [placedToken(actor, 14, 5), placedToken(target, 1, 6)],
      blockedCells: openLowerFlank
        ? [{ column: 8, row: 5 }]
        : [{ column: 8, row: 5 }, { column: 8, row: 6 }],
    })),
    actorId: actor.id,
    targetId: target.id,
  };
}

function blockedSaveState(blocked: boolean): {
  readonly state: EncounterState;
  readonly actorId: CombatantId;
  readonly targetId: CombatantId;
} {
  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
    combatantId: 'combatant:query-blocked-save-spider',
    tokenId: 'token:query-blocked-save-spider',
  });
  const actor = {
    ...actorBase,
    rules: { ...actorBase.rules, speed: feet(0) },
  };
  const target = playerProfile('query-blocked-save-target');
  return {
    state: freshMonsterPlanningState(createEncounter({
      bounds: { columns: 6, rows: 3 },
      combatants: [actor, target],
      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
      blockedCells: blocked
        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
        : [],
    })),
    actorId: actor.id,
    targetId: target.id,
  };
}

function stateAtResolvedOrigin(
  state: EncounterState,
  actorId: CombatantId,
  position: GridCell,
): EncounterState {
  return {
    ...state,
    activeCombatant: actorId,
    activeInitiativeIndex: 0,
    tokens: state.tokens.map((token) => token.combatantId === actorId
      ? { ...token, position }
      : token),
  };
}

describe('canonical engine query port', () => {
  it('ENGINE_QUERY_VISIBILITY_EQUALS_CANONICAL_DETECTOR including located outcomes', () => {
    const observerBase = monsterProfile('query-visibility-observer');
    const observer = {
      ...observerBase,
      rules: {
        ...observerBase.rules,
        senses: [
  sourceDetails: {
    ...baseDetails([{ path: SRD_PATH, lineStart: 22872, lineEnd: 22899 }], { sizes: ['Large'], type: 'Beast', subtype: null, alignment: 'Unaligned' }, { rating: '1/2', experiencePoints: 100, proficiencyBonus: 2 }, { count: 2, sides: 10, modifier: 2 }, 20, crocodileAbilities, [{ kind: 'swim', feet: 30, hover: false }]),
    skills: present([{ name: 'Stealth', bonus: 2 }]), gear: notListed('gear'), senses: present([]), passivePerception: 10, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'hold_breath', minutes: 60 }]),
    actions: [melee('bite', 'Bite', 4, [damage(6, 1, 8, 2, 'Piercing')], [conditionOnHit('Grappled', 'Medium', { escapeDc: 12, duration: 'until_escape' }), conditionOnHit('Restrained', 'Medium', { duration: 'until_escape' })])],
    bonusActions: notListed('bonus actions'), reactions: notListed('reactions'),
  },
});

const giantSpiderAbilities = abilityLines([14, 2, 2], [16, 3, 3], [12, 1, 1], [2, -4, -4], [11, 0, 0], [4, -3, -3]);
export const GIANT_SPIDER = monsterStatblock({
  id: 'statblock:giant-spider', name: 'Giant Spider', armorClass: 14, hitPointMaximum: 26, speedFeet: 30, initiativeBonus: 3,
  savingThrowBonuses: savingThrowBonuses(giantSpiderAbilities), usesDeathSaves: false,
  sourceDetails: {
    ...baseDetails([{ path: SRD_PATH, lineStart: 23313, lineEnd: 23350 }], { sizes: ['Large'], type: 'Beast', subtype: null, alignment: 'Unaligned' }, { rating: 1, experiencePoints: 200, proficiencyBonus: 2 }, { count: 4, sides: 10, modifier: 4 }, 30, giantSpiderAbilities, [{ kind: 'climb', feet: 30, hover: false }]),
    skills: present([{ name: 'Perception', bonus: 4 }, { name: 'Stealth', bonus: 7 }]), gear: notListed('gear'), senses: present([{ kind: 'darkvision', rangeFeet: 60 }]), passivePerception: 14, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'spider_climb' }, { kind: 'web_walker' }]),
    actions: [
      melee('bite', 'Bite', 5, [damage(7, 1, 8, 3, 'Piercing'), damage(7, 2, 6, 0, 'Poison')]),
      { kind: 'saving_throw', id: 'web', name: 'Web', savingThrow: { ability: 'dexterity', dc: 13 }, target: { rangeFeet: 60, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Restrained', null, { duration: 'until_escape' })] }, success: { kind: 'none' } },
    ], bonusActions: notListed('bonus actions'), reactions: notListed('reactions'),
  },
});

const giantConstrictorAbilities = abilityLines([19, 4, 4], [14, 2, 2], [12, 1, 1], [1, -5, -5], [10, 0, 0], [3, -4, -4]);
export const GIANT_CONSTRICTOR_SNAKE = monsterStatblock({
  id: 'statblock:giant-constrictor-snake', name: 'Giant Constrictor Snake', armorClass: 12, hitPointMaximum: 60, speedFeet: 30, initiativeBonus: 2,
  savingThrowBonuses: savingThrowBonuses(giantConstrictorAbilities), attacksPerAction: 2, reachFeet: 10, usesDeathSaves: false,
  sourceDetails: {
    ...baseDetails([{ path: SRD_PATH, lineStart: 23066, lineEnd: 23094 }], { sizes: ['Huge'], type: 'Beast', subtype: null, alignment: 'Unaligned' }, { rating: 2, experiencePoints: 450, proficiencyBonus: 2 }, { count: 8, sides: 12, modifier: 8 }, 30, giantConstrictorAbilities, [{ kind: 'swim', feet: 30, hover: false }]),
    skills: present([{ name: 'Perception', bonus: 2 }]), gear: notListed('gear'), senses: present([{ kind: 'blindsight', rangeFeet: 10 }]), passivePerception: 12, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: notListed('traits'),
    actions: [
      { kind: 'multiattack', id: 'multiattack', count: 2, actionIds: ['bite', 'constrict'], combination: 'fixed' },
      melee('bite', 'Bite', 6, [damage(11, 2, 6, 4, 'Piercing')], [], 10),
      { kind: 'saving_throw', id: 'constrict', name: 'Constrict', savingThrow: { ability: 'strength', dc: 14 }, target: { rangeFeet: 10, maximumSize: 'Large', excludedKinds: [] }, failure: { damage: [damage(13, 2, 8, 4, 'Bludgeoning')], effects: [conditionOnHit('Grappled', 'Large', { escapeDc: 14, duration: 'until_escape' })] }, success: { kind: 'none' } },
    ], bonusActions: notListed('bonus actions'), reactions: notListed('reactions'),
  },
});

const killerWhaleAbilities = abilityLines([19, 4, 4], [14, 2, 2], [13, 1, 1], [3, -4, -4], [12, 1, 1], [7, -2, -2]);
export const KILLER_WHALE = monsterStatblock({
  id: 'statblock:killer-whale', name: 'Killer Whale', armorClass: 12, hitPointMaximum: 90, speedFeet: 5, initiativeBonus: 2,
  savingThrowBonuses: savingThrowBonuses(killerWhaleAbilities), usesDeathSaves: false,
  sourceDetails: {
    ...baseDetails([{ path: SRD_PATH, lineStart: 23491, lineEnd: 23515 }], { sizes: ['Huge'], type: 'Beast', subtype: null, alignment: 'Unaligned' }, { rating: 3, experiencePoints: 700, proficiencyBonus: 2 }, { count: 12, sides: 12, modifier: 12 }, 5, killerWhaleAbilities, [{ kind: 'swim', feet: 60, hover: false }]),
    skills: present([{ name: 'Perception', bonus: 3 }, { name: 'Stealth', bonus: 4 }]), gear: notListed('gear'), senses: present([{ kind: 'blindsight', rangeFeet: 120 }]), passivePerception: 13, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'hold_breath', minutes: 30 }]),
    actions: [melee('bite', 'Bite', 6, [damage(21, 5, 6, 4, 'Piercing')])], bonusActions: notListed('bonus actions'), reactions: notListed('reactions'),
  },

exec
/bin/bash -lc "sed -n '520,590p' tests/unit/vtt/engine-query-port.test.ts
sed -n '220,310p' src/vtt/intent-resolver.ts
sed -n '140,185p' src/vtt/intent-resolver.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
      .toThrow('Total Cover or is outside line of sight');
    expect(projectEncounterBoard(projectDmView(walled)).blockedCells).toEqual(walledCells);
  });

  it('OFFER_IGNORES_BLOCKS_SIGHT refuses a half-cover two-blocker seam and opens one flank', () => {
    const closed = twoBlockerSeamState(false);
    const opened = twoBlockerSeamState(true);
    // The four formerly clear rays are (14,6)/(15,6) -> (1,6)/(2,6). Each follows row line 6
    // between wall cells (8,5)/(8,6); the other twelve corner rays enter one of those cells.
    expect(traceCombatantLine(closed.state, closed.actorId, closed.targetId)).toMatchObject({
      tier: 'half',
      blocksSight: true,
      firstBlockingCell: { column: 8, row: 5 },
    });
    expect(OFFER_ENVIRONMENT.queries.reach(closed.state, {
      actorId: closed.actorId,
      targetId: closed.targetId,
      actionId: 'shortbow',
    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
    expect(availableEngineActorOptions(closed.state, closed.actorId, OFFER_ENVIRONMENT).some((option) =>
      option.actionSlots.some((slot) => slot.use.kind === 'attack' &&
        slot.use.target.kind === 'combatant' && slot.use.target.combatantId === closed.targetId))).toBe(false);

    // Removing (8,6) leaves the row-line-6 rays with a blocker on only one flank.
    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
      tier: 'none',
      blocksSight: false,
    });
    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
      actorId: opened.actorId,
      targetId: opened.targetId,
      actionId: 'shortbow',
    })).toMatchObject({ legal: true });
    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
      option.actionSlots.some((slot) => slot.use.kind === 'attack' &&
        slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
  });

  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
    const blocked = blockedSaveState(true);
    const opened = blockedSaveState(false);
    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
      tier: 'half',
      blocksSight: true,
    });
    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
      actorId: blocked.actorId,
      targetId: blocked.targetId,
      actionId: 'web',
    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
        slot.use.target.combatantId === blocked.targetId))).toBe(false);

    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
      tier: 'none',
      blocksSight: false,
    });
    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
      actorId: opened.actorId,
      targetId: opened.targetId,
      actionId: 'web',
    })).toMatchObject({ legal: true });
    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
        slot.use.target.combatantId === opened.targetId))).toBe(true);
  });

                targetId: resolved.targetId,
                source: { kind: 'action', actionId: resolved.action.id },
                rangeFeet: resolved.action.target.rangeFeet,
              });
              break;
            }
          }
        }
        break;
      case 'saving_throw': {
        const targetId = resolveSelector(state, actorId, use.target, queries);
        const action = actions.find(
          (candidate): candidate is MonsterSavingThrowAction => candidate.kind === 'saving_throw' && candidate.id === use.actionId,
        );
        if (targetId === null || action === undefined) return null;
        constraints.push({ targetId, source: { kind: 'action', actionId: action.id }, rangeFeet: action.target.rangeFeet });
        break;
      }
      case 'cast_spell': {
        const definition = spellDefinition(use.spellId);
        const targetIds = useTargets(state, actorId, use, queries);
        if (definition === null || targetIds === null) return null;
        const rangeFeet = 'rangeFeet' in definition.targeting ? definition.targeting.rangeFeet : 0;
        for (const targetId of targetIds) {
          const targeting = definition.targeting;
          const requiresSight = targeting.kind !== 'self' && targeting.kind !== 'area' &&
            targeting.kind !== 'area_selected' && targeting.kind !== 'all_in_range' &&
            targeting.kind !== 'remote' && targeting.kind !== 'utility' && targeting.requiresSight === true;
          constraints.push({ targetId, source: { kind: 'spell', requiresSight }, rangeFeet });
        }
        break;
      }
      case 'use_world_object':
      case 'dodge':
      case 'disengage':
      case 'dash':
      case 'hide':
      case 'end_turn': break;
    }
  }
  return constraints;
}

function positionFits(
  state: EncounterState,
  actorId: CombatantId,
  position: GridCell,
  constraints: readonly {
    readonly targetId: CombatantId;
    readonly rangeFeet: number;
    readonly source: { readonly kind: 'action'; readonly actionId: string } | { readonly kind: 'spell'; readonly requiresSight: boolean };
  }[],
  movement: EngineMovementObjective,
  queries: EngineQueryPort,
): boolean {
  for (const constraint of constraints) {
    if (constraint.source.kind === 'spell') {
      const distance = queries.spaceDistance(state, actorId, constraint.targetId, position);
      if (distance === null || distance > constraint.rangeFeet ||
        (constraint.source.requiresSight && traceCombatantLine(
          state, actorId, constraint.targetId, { sourceAnchor: position },
        ).blocksSight)) return false;
      continue;
    }
    const reach = queries.reach(state, {
      actorId,
      targetId: constraint.targetId,
      actionId: constraint.source.actionId,
      origin: position,
    });
    if (!reach.legal || reach.distanceFeet > constraint.rangeFeet) return false;
  }
  const anchor = movement.engagement.anchor;
  if (movement.engagement.stance === 'maintain_range' && anchor !== undefined && anchor !== null) {
    const anchorId = resolveSelector(state, actorId, anchor, queries);
    const anchorReach = anchorId === null ? null : queries.combatant(state, anchorId)?.profile.rules.reach;
    const separation = anchorId === null ? null : queries.spaceDistance(state, actorId, anchorId, position);
    if (separation === null || anchorReach === null || anchorReach === undefined || separation <= anchorReach) return false;
  }
  return true;
}

interface MovementResolution {
  readonly costFeet: number;
  readonly path: readonly GridCell[];
  readonly finalPosition: GridCell;
}

function movementResolution(
  state: EncounterState,
  actorId: CombatantId,
    }
    case 'multiattack': {
      const targets = use.components.map((component) => component.kind === 'attack'
        ? targetUse(state, actorId, component, queries)
        : savingThrowTargetUse(state, actorId, component, queries));
      return targets.some((entry) => entry === null)
        ? null
        : targets.flatMap((entry) => entry === null ? [] : [entry.targetId]);
    }
    case 'saving_throw': {
      const targetId = resolveSelector(state, actorId, use.target, queries);
      return targetId === null ? null : [targetId];
    }
    case 'cast_spell': {
      const targets = use.targets.map((selector) => resolveSelector(state, actorId, selector, queries));
      return targets.some((targetId) => targetId === null)
        ? null
        : targets.flatMap((targetId) => targetId === null ? [] : [targetId]);
    }
    case 'use_world_object':
    case 'dodge':
    case 'disengage':
    case 'dash':
    case 'hide':
    case 'end_turn': return [];
  }
}

function targetConstraints(
  state: EncounterState,
  actorId: CombatantId,
  slots: readonly EngineActionSlotUse[],
  queries: EngineQueryPort,
): readonly {
  readonly targetId: CombatantId;
  readonly rangeFeet: number;
  readonly source: { readonly kind: 'action'; readonly actionId: string } | { readonly kind: 'spell'; readonly requiresSight: boolean };
}[] | null {
  const actions = monsterActions(state, actorId);
  const constraints: {
    readonly targetId: CombatantId;
    readonly rangeFeet: number;
    readonly source: { readonly kind: 'action'; readonly actionId: string } | { readonly kind: 'spell'; readonly requiresSight: boolean };
  }[] = [];
  for (const slot of slots) {
    const use = slot.use;

exec
/bin/bash -lc "rg -n \"export type .*Resolution|export interface .*Resolution|function resolveEngineActorOption\" src/vtt/intent-resolver.ts src/vtt/turn-proposal.ts
sed -n '520,590p' src/vtt/intent-resolver.ts
sed -n '215,260p' src/vtt/turn-proposal.ts
rg -n \"decodeEncounterStateV1\" src/vtt/encounter-state-codec.ts | head" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
src/vtt/turn-proposal.ts:235:export type EngineProposalResolution =
src/vtt/intent-resolver.ts:532:export function resolveEngineActorOption(
        omittedRiders: omittedRiders.filter((rider) => rider.sourceActionId === use.actionId),
      }); break;
      case 'dodge':
      case 'disengage':
      case 'dash':
      case 'hide':
      case 'end_turn': resolved.push({ slot: slot.slot, kind: use.kind, actionId: engineActionId(use.kind), spellId: null, targetIds: [], objectId: null, omittedRiders: [] }); break;
    }
  }
  return resolved;
}

export function resolveEngineActorOption(
  state: EncounterState,
  option: EngineOfferableOption,
  environment: EngineOptionEnvironment,
): OptionResolution {
  const boundDigest = optionEnvironmentDigests.get(option);
  if (boundDigest === undefined || boundDigest !== environment.digest) {
    return refused(
      'OFFER_ENVIRONMENT_MISMATCH',
      `${option.actorId}: option was not created by the bound offer environment`,
    );
  }
  const queries = environment.queries;
  const actor = queries.combatant(state, option.actorId);
  if (actor?.profile.kind !== 'monster' || actor.life !== 'living') {
    return refused('ACTOR_NOT_LIVING_MONSTER', `${option.actorId}: actor is not a living monster`);
  }
  const main = option.actionSlots.filter((slot) => slot.slot === 'main');
  const bonus = option.actionSlots.filter((slot) => slot.slot === 'bonus');
  if (main.length !== 1 || bonus.length > 1) {
    return refused('ACTION_SLOT_COMBINATION_ILLEGAL', `${option.actorId}: option must use exactly one main and at most one bonus slot`);
  }
  if (actor.turn.action.kind !== 'available') return refused('MAIN_ACTION_SPENT', `${option.actorId}: main action is spent`);
  if (bonus.length > 0 && !actor.turn.bonusActionAvailable) return refused('BONUS_ACTION_SPENT', `${option.actorId}: bonus action is spent`);
  for (const slot of option.actionSlots) {
    const code = validateUse(state, option.actorId, slot.slot, slot.use, queries);
    if (code !== null) return refused(code, `${option.actorId}: ${option.label} is unavailable`);
  }
  const movement = movementResolution(state, option.actorId, option.movement, option.actionSlots, queries);
  if (movement === null) return refused('OPTION_UNREACHABLE', `${option.actorId}: ${option.label} has no legal movement expansion`);
  const actionSlots = resolvedUses(state, option.actorId, option.actionSlots, queries, option.omittedRiders);
  if (actionSlots === null) return refused('OPTION_TARGET_UNRESOLVED', `${option.actorId}: ${option.label} target did not resolve`);
  return {
    valid: true,
    mechanics: {
      actorId: option.actorId,
      optionId: option.optionId,
      movementCostFeet: movement.costFeet,
      path: movement.path,
      finalPosition: movement.finalPosition,
      actionSlots,
      omittedRiders: option.omittedRiders,
    },
  };
}

function choiceFitsOption(
  option: EngineOfferableOption,
  choice: EngineActivationChoice | null | undefined,
): boolean {
  const slot = option.activationChoice;
  if (slot === undefined || slot === null) return choice === null || choice === undefined;
  if (choice === null || choice === undefined || choice.kind !== slot.kind) return false;
  switch (slot.kind) {
    case 'command_word':
      return choice.kind === 'command_word' && slot.values.some((value) => value === choice.value);
    case 'unicorns_blessing_spell':
      return choice.kind === 'unicorns_blessing_spell' && slot.values.some((value) => value === choice.value);
    case 'dispel_evil_and_good_mode':
  readonly objectId: WorldObjectId | null;
  readonly omittedRiders: readonly EngineOmittedRider[];
  /** Engine-bound condition encoded by a revision-bound offered option. */
  readonly selectedCondition?: LesserRestorationCondition;
  readonly activationChoice?: EngineActivationChoice;
  readonly multiattackComponent?: true;
  /** Present only for a placed-area spell. */
  readonly area?: AreaTemplate;
}

export interface ResolvedTurnMechanics {
  readonly actorId: CombatantId;
  readonly optionId: EngineOptionId;
  readonly movementCostFeet: number;
  readonly path: readonly GridCell[];
  readonly finalPosition: GridCell;
  readonly actionSlots: readonly ResolvedActionSlotUse[];
  readonly omittedRiders: readonly EngineOmittedRider[];
}

export type EngineProposalResolution =
  | {
      readonly valid: true;
      readonly selectedBranch: 'primary' | 'fallback';
      readonly resolutionDigest: string;
      readonly summary: string;
      readonly refusals: readonly {
        readonly branch: 'primary';
        readonly code: string;
        readonly summary: string;
      }[];
      readonly mechanics: ResolvedTurnMechanics;
      readonly option: EngineOfferableOption;
      readonly primaryOption: EngineOfferableOption;
      readonly fallbackOption: EngineOfferableOption | null;
    }
  | {
      readonly valid: false;
      readonly selectedBranch: 'none';
      readonly refusals: readonly {
        readonly branch: 'primary' | 'fallback';
        readonly code: string;
        readonly summary: string;
      }[];
    };

378:export function decodeEncounterStateV1(value: unknown, mode: EncounterStateDecodeMode): EncounterState {

exec
/bin/bash -lc "sed -n '1,90p' src/vtt/intent-resolver.ts
rg -n \"type OptionResolution|interface OptionResolution\" src/vtt/intent-resolver.ts" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
import { canonicalJson } from '../commands/canonical-json';
import { combatantSpace } from '../combat/combat-rules';
import { minimumSpaceDistanceToCells, minimumSpaceLine } from '../combat/creature-space';
import { traceCombatantLine } from '../combat/cover';
import type { EncounterState } from '../combat/encounter';
import { gridDistance, type GridCell } from '../combat/grid';
import type {
  MonsterAttackAction,
  MonsterSavingThrowAction,
  MonsterSpellcastingAction,
} from '../combat/statblock';
import { monsterSpellResourcePoolId } from '../combat/statblock';
import { spellDefinition } from '../combat/spells/definitions';
import type { CombatantId } from '../combat/values';
import { worldObjectClassAction, worldObjectActionWasUsed } from '../combat/world-object-actions';
import { sha256 } from '../crypto/sha256';
import {
  monsterActions,
  monsterBonusActions,
  type EngineQueryPort,
  type EngineTargetSelector,
} from './engine-query-port';
import { engineActorOptions } from './turn-option-registry';
import type { EngineActorOptionPartition } from './turn-option-registry';
import {
  engineActionId,
  engineSpellId,
  type EngineActionSlotUse,
  type EngineActivationChoice,
  type EngineOfferableOption,
  type EngineBonusActionUse,
  type EngineMainActionUse,
  type EngineMovementObjective,
  type EngineMultiattackComponentUse,
  type EngineProposalResolution,
  type EngineTargetedAttackUse,
  type EngineTurnProposal,
  type PureTurnProposalResolver,
  type ResolvedActionSlotUse,
  type ResolvedTurnMechanics,
} from './turn-proposal';
import type { EngineOmittedRider } from './option-modeling';
import { legalMultiattackCombinations } from './offers/offer-declarations';
import type { EngineOptionEnvironment } from './offers/build-offer-environment';

export type {
  EngineActionId,
  EngineActionSlotUse,
  EngineOfferableOption,
  EngineBonusActionUse,
  EngineEngagement,
  EngineMainActionUse,
  EngineMovementObjective,
  EngineMovementPreference,
  EngineOptionId,
  EngineProposalResolution,
  EngineSpellId,
  EngineTurnProposal,
  PureTurnProposalResolver,
  ResolvedActionSlotUse,
  ResolvedTurnMechanics,
} from './turn-proposal';

type OptionResolution =
  | { readonly valid: true; readonly mechanics: ResolvedTurnMechanics }
  | { readonly valid: false; readonly code: string; readonly summary: string };

function refused(code: string, summary: string): OptionResolution {
  return { valid: false, code, summary };
}

const optionEnvironmentDigests = new WeakMap<EngineOfferableOption, string>();

function bindOptionEnvironment(
  option: EngineOfferableOption,
  environment: EngineOptionEnvironment,
): EngineOfferableOption {
  optionEnvironmentDigests.set(option, environment.digest);
  return option;
}

export function engineActorOptionsForEnvironment(
  state: EncounterState,
  actorId: CombatantId,
  environment: EngineOptionEnvironment,
  revision = state.revision,
): EngineActorOptionPartition {
  const partition = engineActorOptions(state, actorId, revision);
  for (const option of partition.offerable) bindOptionEnvironment(option, environment);
  return partition;
64:type OptionResolution =

codex
The active predicate’s exact shape is now pinned: valid movement remains independently productive; attack/save/cast/world-object slots remain productive; only targeted offensive slots add the `!blocksSight && tier !== 'total'` requirement from the resolved origin. I’m probing minimal decoded states so the three mutant witnesses exercise real engine options rather than fabricated payloads.
apply patch
patch: completed
/tmp/cohort-b2-probe.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}

exec
/bin/bash -lc 'npx vite-node /tmp/cohort-b2-probe.ts' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 172ms:
failed to load config from /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/vite.config.ts
node:internal/fs/promises:637
  return new FileHandle(await PromisePrototypeThen(
                        ^

Error: EROFS: read-only file system, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/node_modules/.vite-temp/vite.config.ts.timestamp-1789697468264-2c54658c6273a.mjs'
    at async open (node:internal/fs/promises:637:25)
    at async Object.writeFile (node:internal/fs/promises:1239:14)
    at async loadConfigFromBundledFile (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/vite/dist/node/chunks/config.js:35994:3)
    at async bundleAndLoadConfigFile (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/vite/dist/node/chunks/config.js:35884:17)
    at async loadConfigFromFile (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/vite/dist/node/chunks/config.js:35851:42)
    at async resolveConfig (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/vite/dist/node/chunks/config.js:35500:22)
    at async _createServer (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/vite/dist/node/chunks/config.js:25441:67)
    at async CAC.run (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/vite-node/dist/cli.mjs:65:17) {
  errno: -30,
  code: 'EROFS',
  syscall: 'open',
  path: '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/node_modules/.vite-temp/vite.config.ts.timestamp-1789697468264-2c54658c6273a.mjs'
}

Node.js v24.13.0

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}

exec
/bin/bash -lc "sed -n '1,160p' vite.config.ts
readlink -f node_modules
npx vite-node --help | sed -n '1,160p'" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 135ms:
vite-node/3.2.4

Usage:
  $ vite-node [...files]

Commands:
  [...files]  

For more info, run any command with the `--help` flag:
  $ vite-node --help

Options:
  -r, --root <path>    Use specified root directory 
  -c, --config <path>  Use specified config file 
  -m, --mode <mode>    Set env mode 
  -w, --watch          Restart on file changes, similar to "nodemon" 
  --script             Use vite-node as a script runner 
  --options <options>  Use specified Vite server options 
  -v, --version        Output the version number 
  -h, --help           Display help for command 
  -v, --version        Display version number 

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}

apply patch
patch: completed
/tmp/cohort-vite.config.mjs
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };

exec
/bin/bash -lc 'npx vite-node --config /tmp/cohort-vite.config.mjs /tmp/cohort-b2-probe.ts' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 2732ms:
0 [{"label":"Dodge","resolution":{"valid":true,"mechanics":{"actorId":"combatant:probe-0","optionId":"option:0:0fa965d48cddeb60e0d8fc384f991d86a34785af12bbbaae","movementCostFeet":0,"path":[],"finalPosition":{"column":0,"row":1},"actionSlots":[{"slot":"main","kind":"dodge","actionId":"dodge","spellId":null,"targetIds":[],"objectId":null,"omittedRiders":[]}],"omittedRiders":[]}}},{"label":"End Turn","resolution":{"valid":true,"mechanics":{"actorId":"combatant:probe-0","optionId":"option:0:4f82d514fe345c063a52d2cb548a304812e0bc3fedab01de","movementCostFeet":0,"path":[],"finalPosition":{"column":0,"row":1},"actionSlots":[{"slot":"main","kind":"end_turn","actionId":"end_turn","spellId":null,"targetIds":[],"objectId":null,"omittedRiders":[]}],"omittedRiders":[]}}}]
30 [{"label":"Dash","resolution":{"valid":true,"mechanics":{"actorId":"combatant:probe-30","optionId":"option:0:7162f26ca124e53a1e5c915b5881bdd77992006db1557a10","movementCostFeet":60,"path":[{"column":1,"row":0},{"column":2,"row":0},{"column":3,"row":0},{"column":4,"row":0},{"column":5,"row":0},{"column":6,"row":0},{"column":7,"row":0},{"column":8,"row":0},{"column":9,"row":0},{"column":10,"row":0},{"column":11,"row":0},{"column":12,"row":0}],"finalPosition":{"column":12,"row":0},"actionSlots":[{"slot":"main","kind":"dash","actionId":"dash","spellId":null,"targetIds":[],"objectId":null,"omittedRiders":[]}],"omittedRiders":[]}}},{"label":"Dodge","resolution":{"valid":true,"mechanics":{"actorId":"combatant:probe-30","optionId":"option:0:4222fda4b7c473a23581662a4b0b235324da3d8bc2495659","movementCostFeet":0,"path":[],"finalPosition":{"column":0,"row":1},"actionSlots":[{"slot":"main","kind":"dodge","actionId":"dodge","spellId":null,"targetIds":[],"objectId":null,"omittedRiders":[]}],"omittedRiders":[]}}},{"label":"End Turn","resolution":{"valid":true,"mechanics":{"actorId":"combatant:probe-30","optionId":"option:0:42e9199b01be1af3facf14098cf85f0ea1955617fc829aa7","movementCostFeet":0,"path":[],"finalPosition":{"column":0,"row":1},"actionSlots":[{"slot":"main","kind":"end_turn","actionId":"end_turn","spellId":null,"targetIds":[],"objectId":null,"omittedRiders":[]}],"omittedRiders":[]}}}]
spider Dodge {"valid":true,"mechanics":{"actorId":"combatant:probe-spider","optionId":"option:0:681fd3facbf89affa220e27547cd546d1f8ad609c5adeeb7","movementCostFeet":0,"path":[],"finalPosition":{"column":0,"row":1},"actionSlots":[{"slot":"main","kind":"dodge","actionId":"dodge","spellId":null,"targetIds":[],"objectId":null,"omittedRiders":[]}],"omittedRiders":[]}}
spider End Turn {"valid":true,"mechanics":{"actorId":"combatant:probe-spider","optionId":"option:0:bcabdd20c6886bb0498d18612c69f07d7097392ba5cb6971","movementCostFeet":0,"path":[],"finalPosition":{"column":0,"row":1},"actionSlots":[{"slot":"main","kind":"end_turn","actionId":"end_turn","spellId":null,"targetIds":[],"objectId":null,"omittedRiders":[]}],"omittedRiders":[]}}
spider Web -> combatant:probe-web-target {"valid":true,"mechanics":{"actorId":"combatant:probe-spider","optionId":"option:0:1e93602b93c0e7a98a795ea3458145d58b05279f19412597","movementCostFeet":0,"path":[],"finalPosition":{"column":0,"row":1},"actionSlots":[{"slot":"main","kind":"saving_throw","actionId":"web","spellId":null,"targetIds":["combatant:probe-web-target"],"objectId":null,"omittedRiders":[]}],"omittedRiders":[]}}

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };

codex
The probe confirmed the intended witnesses with real resolutions: zero-speed monster → `Dodge, End Turn`; normal-speed actionless monster → `Dash` at 60 ft plus nonproductive options; zero-speed Giant Spider → a resolved targeted `Web`. I’m adding only the new active suite now, then I’ll run it before any retirement edit to demonstrate the required 30 missing-cohort reds.
apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-v2.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});

exec
/bin/bash -lc 'npx vitest --help | rg -n "config|pool|parallel|worker|cache"' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 67ms:
32:  -c, --config <path>                                        Path to config file 
50:  --pool <pool>                                              Specify pool, if not running in the browser (default: forks) 
51:  --execArgv <option>                                        Pass additional arguments to node process when spawning worker_threads or child_process. 
52:  --vmMemoryLimit <limit>                                    Memory limit for VM pools. If you see memory leaks, try to tinker this value. 
53:  --fileParallelism                                          Should all test files run in parallel. Use --no-file-parallelism to disable (default: true) 
54:  --maxWorkers <workers>                                     Maximum number or percentage of workers to run tests in 
78:  --cache                                                    Enable cache. Use '--help --cache' for more info. 
87:  --configLoader <loader>                                    Use bundle to bundle the config with esbuild or runner (experimental) to process it on the fly. This is only available in vite version 6.1.0 and above. (default: bundle) 
91:  --clearCache                                               Delete all Vitest caches, including experimental.fsModuleCache, without running any tests. This will reduce the performance in the subsequent test run. 
93:  --strictTags                                               Should Vitest throw an error if test has a tag that is not defined in the config. (default: true) 

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});

exec
/bin/bash -lc 'npx vitest run tests/unit/vtt/arena-basis-brutal-v2.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 3582ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts (34 tests | 30 failed) 29ms
     × 'brutal-v2' seed 6209001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 2ms
     × 'brutal-v2' seed 6209002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-b-v2' seed 6210001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-2-v2' seed 6211001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms

⎯⎯⎯⎯⎯⎯ Failed Tests 30 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209001.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209002.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209003.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209004.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209005.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209006.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209007.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209008.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209009.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[9/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209010.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[10/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[11/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[12/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[13/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[14/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[15/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[16/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[17/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[18/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[19/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[20/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[21/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[22/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[23/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[24/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[25/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[26/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[27/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[28/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[29/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[30/30]⎯


 Test Files  1 failed (1)
      Tests  30 failed | 4 passed (34)
   Start at  22:13:11
   Duration  3.27s (transform 1.22s, setup 0ms, import 1.79s, tests 29ms, environment 0ms)


diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});

codex
RED-first is proven: the new suite collected cleanly, all four predicate/mutant witnesses passed, and exactly 30 membership cases failed—each on `ENOENT` for its declared v2 fixture. I’m now retiring only the two frozen productivity assertions and adding the required historical-contract comments, leaving every integrity, static, digest, and round-execution check intact.
apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-b.test.ts
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/room-generator.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..fe677f077046ad2c609fe9ecf51f8589613e7416
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -23,8 +23,6 @@
   hardRoomMembershipViolations,
 } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
 import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
@@ -75,11 +73,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +578,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..fe677f077046ad2c609fe9ecf51f8589613e7416
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -23,8 +23,6 @@
   hardRoomMembershipViolations,
 } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
 import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
@@ -75,11 +73,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +578,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc 'rg -n "brutalRoomMembershipViolations|OFFER_ENVIRONMENT|freshMonsterPlanningState|resolveEngineActorOption" tests/unit/vtt/room-generator.test.ts tests/unit/vtt/arena-basis-brutal-b.test.ts
find /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01 -maxdepth 2 -type f -print | LC_ALL=C sort' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
tests/unit/vtt/arena-basis-brutal-b.test.ts:15:  resolveEngineActorOption,
tests/unit/vtt/arena-basis-brutal-b.test.ts:25:import { freshMonsterPlanningState, projectFutureMonsterTurns } from '../../../src/vtt/monster-planning-state';
tests/unit/vtt/arena-basis-brutal-b.test.ts:112:const OFFER_ENVIRONMENT = buildOfferEnvironment({
tests/unit/vtt/arena-basis-brutal-b.test.ts:116:const TURN_PROPOSAL_RESOLVER = createPureTurnProposalResolver(OFFER_ENVIRONMENT);
tests/unit/vtt/arena-basis-brutal-b.test.ts:170:      OFFER_ENVIRONMENT,
tests/unit/vtt/arena-basis-brutal-b.test.ts:177:      OFFER_ENVIRONMENT,
tests/unit/vtt/arena-basis-brutal-b.test.ts:215:    OFFER_ENVIRONMENT,
tests/unit/vtt/arena-basis-brutal-b.test.ts:478:    const dodge = availableEngineActorOptions(planningState, activeMonster.id, OFFER_ENVIRONMENT)
tests/unit/vtt/arena-basis-brutal-b.test.ts:496:      OFFER_ENVIRONMENT,
tests/unit/vtt/room-generator.test.ts:22:  brutalRoomMembershipViolations,
tests/unit/vtt/room-generator.test.ts:29:const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01/impl-b1-fix-r1-sol.md
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01/impl-b1-sol.md
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01/loop-log.md
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/cohort-01/review-b1-astra-r1.md

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..fe677f077046ad2c609fe9ecf51f8589613e7416
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -23,8 +23,6 @@
   hardRoomMembershipViolations,
 } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
 import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
@@ -75,11 +73,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +578,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/room-generator.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
The retirement edits are complete: 6203 retains its byte/digest pins, 6206 retains structural/reducer/SHA/round execution, and 6207 retains integrity/static validation. I’ve also added the requested B1-F1 rationale comment. Next I’m running both mandated TypeScript gates before the focused suite matrix.
exec
/bin/bash -lc 'npx tsc -p tsconfig.app.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
exec
/bin/bash -lc 'npx tsc -p tsconfig.node.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 14247ms:
 succeeded in 27405ms:
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
Both TypeScript gates pass (`0/0`). I’m running the frozen suites serially now, followed by the active suite; the acceptance criterion here is not blanket green—it is that frozen failures contain no retired productivity assertion, while the active suite remains exactly the 30 missing fixtures.
exec
/bin/bash -lc 'set +e
npx vitest run tests/unit/tools/d569-second-family-manifest.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner
s1=$?
printf '"'SECOND_FAMILY_STATUS=%s\\n' \""'$s1"
npx vitest run tests/unit/vtt/room-generator.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner
s2=$?
printf '"'ROOM_GENERATOR_STATUS=%s\\n' \""'$s2"
npx vitest run tests/unit/vtt/arena-basis-brutal-b.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner
s3=$?
printf '"'BRUTAL_B_STATUS=%s\\n' \""'$s3"
exit 0' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

 succeeded in 38972ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json

 Test Files  1 passed (1)
      Tests  50 passed (50)
   Start at  22:14:44
   Duration  6.78s (transform 1.21s, setup 0ms, import 1.77s, tests 3.58s, environment 0ms)

SECOND_FAMILY_STATUS=0

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json

 Test Files  1 passed (1)
      Tests  56 passed (56)
   Start at  22:14:51
   Duration  4.28s (transform 1.22s, setup 0ms, import 1.77s, tests 1.01s, environment 0ms)

ROOM_GENERATOR_STATUS=0

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json

 Test Files  1 passed (1)
      Tests  47 passed (47)
   Start at  22:14:56
   Duration  26.71s (transform 1.32s, setup 0ms, import 1.94s, tests 23.33s, environment 0ms)

BRUTAL_B_STATUS=0

codex
The first two frozen suites are green: second-family `50/50`, room-generator `56/56`. The long-running brutal-b command outlived the initial output window, so I’m checking its completion and captured result before proceeding.
exec
/bin/bash -lc "ps -eo pid=,stat=,cmd= | rg 'vitest run tests/unit/vtt/arena-basis-brutal-b|npm exec vitest' || true" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
      1 Ss   /home/vagrant/.codex/tmp/arg0/codex-arg0b2dQc6/codex-linux-sandbox --sandbox-policy-cwd /home/vagrant/PhpstormProjects/dnd-wt-cohort-01 --command-cwd /home/vagrant/PhpstormProjects/dnd-wt-cohort-01 --permission-profile {"type":"managed","file_system":{"type":"restricted","entries":[{"path":{"type":"special","value":{"kind":"root"}},"access":"read"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-cohort-01"},"access":"write"},{"path":{"type":"special","value":{"kind":"slash_tmp"}},"access":"write"},{"path":{"type":"special","value":{"kind":"tmpdir"}},"access":"write"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/.git"},"access":"read","missing_path_behavior":"skip"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/.agents"},"access":"read","missing_path_behavior":"skip"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/.codex"},"access":"read","missing_path_behavior":"skip"}]},"network":"enabled"} --apply-seccomp-then-exec -- /bin/bash -c __CODEX_SNAPSHOT_OVERRIDE_SET_0="${CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS+x}" __CODEX_SNAPSHOT_OVERRIDE_0="${CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS-}" __CODEX_SNAPSHOT_OVERRIDE_SET_1="${CODEX_PERMISSION_PROFILE+x}" __CODEX_SNAPSHOT_OVERRIDE_1="${CODEX_PERMISSION_PROFILE-}" __CODEX_SNAPSHOT_OVERRIDE_SET_2="${CODEX_PLUGIN_METRICS_OUTPUT+x}" __CODEX_SNAPSHOT_OVERRIDE_2="${CODEX_PLUGIN_METRICS_OUTPUT-}" __CODEX_SNAPSHOT_OVERRIDE_SET_3="${CODEX_SESSION_ID+x}" __CODEX_SNAPSHOT_OVERRIDE_3="${CODEX_SESSION_ID-}" __CODEX_SNAPSHOT_OVERRIDE_SET_4="${CODEX_THREAD_ID+x}" __CODEX_SNAPSHOT_OVERRIDE_4="${CODEX_THREAD_ID-}" __CODEX_SNAPSHOT_OVERRIDE_SET_5="${CODEX_VERSION+x}" __CODEX_SNAPSHOT_OVERRIDE_5="${CODEX_VERSION-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_0="${ALL_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_0="${ALL_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_1="${BUNDLE_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_1="${BUNDLE_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_2="${BUNDLE_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_2="${BUNDLE_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_3="${BUNDLE_NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_3="${BUNDLE_NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_4="${BUNDLE_SSL_CA_CERT+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_4="${BUNDLE_SSL_CA_CERT-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_5="${CARGO_HTTP_CAINFO+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_5="${CARGO_HTTP_CAINFO-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_6="${CODEX_CA_CERTIFICATE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_6="${CODEX_CA_CERTIFICATE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_7="${CODEX_NETWORK_ALLOW_LOCAL_BINDING+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_7="${CODEX_NETWORK_ALLOW_LOCAL_BINDING-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_8="${CODEX_NETWORK_PROXY_ACTIVE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_8="${CODEX_NETWORK_PROXY_ACTIVE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_9="${CODEX_NETWORK_PROXY_ATTRIBUTION+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_9="${CODEX_NETWORK_PROXY_ATTRIBUTION-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_10="${CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_10="${CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_11="${CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_11="${CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_12="${CURL_CA_BUNDLE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_12="${CURL_CA_BUNDLE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_13="${DOCKER_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_13="${DOCKER_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_14="${DOCKER_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_14="${DOCKER_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_15="${ELECTRON_GET_USE_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_15="${ELECTRON_GET_USE_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_16="${FTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_16="${FTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_17="${GIT_SSL_CAINFO+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_17="${GIT_SSL_CAINFO-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_18="${HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_18="${HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_19="${HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_19="${HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_20="${NODE_EXTRA_CA_CERTS+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_20="${NODE_EXTRA_CA_CERTS-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_21="${NODE_USE_ENV_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_21="${NODE_USE_ENV_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_22="${NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_22="${NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_23="${NPM_CONFIG_CAFILE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_23="${NPM_CONFIG_CAFILE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_24="${NPM_CONFIG_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_24="${NPM_CONFIG_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_25="${NPM_CONFIG_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_25="${NPM_CONFIG_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_26="${NPM_CONFIG_NOPROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_26="${NPM_CONFIG_NOPROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_27="${NPM_CONFIG_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_27="${NPM_CONFIG_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_28="${PIP_CERT+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_28="${PIP_CERT-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_29="${PIP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_29="${PIP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_30="${REQUESTS_CA_BUNDLE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_30="${REQUESTS_CA_BUNDLE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_31="${SSL_CERT_FILE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_31="${SSL_CERT_FILE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_32="${WSS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_32="${WSS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_33="${WS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_33="${WS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_34="${YARN_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_34="${YARN_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_35="${YARN_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_35="${YARN_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_36="${YARN_NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_36="${YARN_NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_37="${all_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_37="${all_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_38="${ftp_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_38="${ftp_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_39="${http_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_39="${http_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_40="${https_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_40="${https_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_41="${no_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_41="${no_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_42="${npm_config_cafile+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_42="${npm_config_cafile-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_43="${npm_config_http_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_43="${npm_config_http_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_44="${npm_config_https_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_44="${npm_config_https_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_45="${npm_config_noproxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_45="${npm_config_noproxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_46="${npm_config_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_46="${npm_config_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_47="${ws_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_47="${ws_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_48="${wss_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_48="${wss_proxy-}" __CODEX_SNAPSHOT_PROXY_ENV_SET="${CODEX_NETWORK_PROXY_ACTIVE+x}"  if . '/home/vagrant/.codex/shell_snapshots/01a0b245-5a53-7990-9cf5-5abf61f029d7.1789697284695867342.sh' >/dev/null 2>&1; then :; fi  if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_0}" ]; then export CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS="${__CODEX_SNAPSHOT_OVERRIDE_0}"; else unset CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_1}" ]; then export CODEX_PERMISSION_PROFILE="${__CODEX_SNAPSHOT_OVERRIDE_1}"; else unset CODEX_PERMISSION_PROFILE; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_2}" ]; then export CODEX_PLUGIN_METRICS_OUTPUT="${__CODEX_SNAPSHOT_OVERRIDE_2}"; else unset CODEX_PLUGIN_METRICS_OUTPUT; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_3}" ]; then export CODEX_SESSION_ID="${__CODEX_SNAPSHOT_OVERRIDE_3}"; else unset CODEX_SESSION_ID; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_4}" ]; then export CODEX_THREAD_ID="${__CODEX_SNAPSHOT_OVERRIDE_4}"; else unset CODEX_THREAD_ID; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_5}" ]; then export CODEX_VERSION="${__CODEX_SNAPSHOT_OVERRIDE_5}"; else unset CODEX_VERSION; fi if [ -n "$__CODEX_SNAPSHOT_PROXY_ENV_SET" ] || [ -n "${CODEX_NETWORK_PROXY_ACTIVE+x}" ]; then if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_0}" ]; then export ALL_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_0}"; else unset ALL_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_1}" ]; then export BUNDLE_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_1}"; else unset BUNDLE_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_2}" ]; then export BUNDLE_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_2}"; else unset BUNDLE_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_3}" ]; then export BUNDLE_NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_3}"; else unset BUNDLE_NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_4}" ]; then export BUNDLE_SSL_CA_CERT="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_4}"; else unset BUNDLE_SSL_CA_CERT; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_5}" ]; then export CARGO_HTTP_CAINFO="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_5}"; else unset CARGO_HTTP_CAINFO; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_6}" ]; then export CODEX_CA_CERTIFICATE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_6}"; else unset CODEX_CA_CERTIFICATE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_7}" ]; then export CODEX_NETWORK_ALLOW_LOCAL_BINDING="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_7}"; else unset CODEX_NETWORK_ALLOW_LOCAL_BINDING; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_8}" ]; then export CODEX_NETWORK_PROXY_ACTIVE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_8}"; else unset CODEX_NETWORK_PROXY_ACTIVE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_9}" ]; then export CODEX_NETWORK_PROXY_ATTRIBUTION="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_9}"; else unset CODEX_NETWORK_PROXY_ATTRIBUTION; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_10}" ]; then export CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_10}"; else unset CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_11}" ]; then export CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_11}"; else unset CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_12}" ]; then export CURL_CA_BUNDLE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_12}"; else unset CURL_CA_BUNDLE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_13}" ]; then export DOCKER_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_13}"; else unset DOCKER_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_14}" ]; then export DOCKER_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_14}"; else unset DOCKER_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_15}" ]; then export ELECTRON_GET_USE_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_15}"; else unset ELECTRON_GET_USE_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_16}" ]; then export FTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_16}"; else unset FTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_17}" ]; then export GIT_SSL_CAINFO="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_17}"; else unset GIT_SSL_CAINFO; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_18}" ]; then export HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_18}"; else unset HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_19}" ]; then export HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_19}"; else unset HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_20}" ]; then export NODE_EXTRA_CA_CERTS="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_20}"; else unset NODE_EXTRA_CA_CERTS; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_21}" ]; then export NODE_USE_ENV_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_21}"; else unset NODE_USE_ENV_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_22}" ]; then export NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_22}"; else unset NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_23}" ]; then export NPM_CONFIG_CAFILE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_23}"; else unset NPM_CONFIG_CAFILE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_24}" ]; then export NPM_CONFIG_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_24}"; else unset NPM_CONFIG_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_25}" ]; then export NPM_CONFIG_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_25}"; else unset NPM_CONFIG_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_26}" ]; then export NPM_CONFIG_NOPROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_26}"; else unset NPM_CONFIG_NOPROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_27}" ]; then export NPM_CONFIG_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_27}"; else unset NPM_CONFIG_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_28}" ]; then export PIP_CERT="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_28}"; else unset PIP_CERT; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_29}" ]; then export PIP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_29}"; else unset PIP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_30}" ]; then export REQUESTS_CA_BUNDLE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_30}"; else unset REQUESTS_CA_BUNDLE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_31}" ]; then export SSL_CERT_FILE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_31}"; else unset SSL_CERT_FILE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_32}" ]; then export WSS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_32}"; else unset WSS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_33}" ]; then export WS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_33}"; else unset WS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_34}" ]; then export YARN_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_34}"; else unset YARN_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_35}" ]; then export YARN_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_35}"; else unset YARN_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_36}" ]; then export YARN_NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_36}"; else unset YARN_NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_37}" ]; then export all_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_37}"; else unset all_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_38}" ]; then export ftp_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_38}"; else unset ftp_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_39}" ]; then export http_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_39}"; else unset http_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_40}" ]; then export https_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_40}"; else unset https_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_41}" ]; then export no_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_41}"; else unset no_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_42}" ]; then export npm_config_cafile="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_42}"; else unset npm_config_cafile; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_43}" ]; then export npm_config_http_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_43}"; else unset npm_config_http_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_44}" ]; then export npm_config_https_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_44}"; else unset npm_config_https_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_45}" ]; then export npm_config_noproxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_45}"; else unset npm_config_noproxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_46}" ]; then export npm_config_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_46}"; else unset npm_config_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_47}" ]; then export ws_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_47}"; else unset ws_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_48}" ]; then export wss_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_48}"; else unset wss_proxy; fi fi if [ -n "${PATH:-}" ]; then export PATH='/home/vagrant/.nvm/versions/node/v24.13.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/codex-path':"$PATH"; else export PATH='/home/vagrant/.nvm/versions/node/v24.13.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/codex-path'; fi  exec '/bin/bash' -c 'ps -eo pid=,stat=,cmd= | rg '"'"'vitest run tests/unit/vtt/arena-basis-brutal-b|npm exec vitest'"'"' || true'
      2 S    /bin/bash -c ps -eo pid=,stat=,cmd= | rg 'vitest run tests/unit/vtt/arena-basis-brutal-b|npm exec vitest' || true
      4 S    rg vitest run tests/unit/vtt/arena-basis-brutal-b|npm exec vitest

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc 'npx vitest run tests/unit/vtt/arena-basis-brutal-b.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 27069ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json

 Test Files  1 passed (1)
      Tests  47 passed (47)
   Start at  22:15:35
   Duration  26.75s (transform 1.31s, setup 0ms, import 1.91s, tests 23.37s, environment 0ms)


diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc 'npx vitest run tests/unit/vtt/arena-basis-brutal-v2.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 3553ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts (34 tests | 30 failed) 29ms
     × 'brutal-v2' seed 6209001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 2ms
     × 'brutal-v2' seed 6209002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-b-v2' seed 6210001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-2-v2' seed 6211001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms

⎯⎯⎯⎯⎯⎯ Failed Tests 30 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209001.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209002.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209003.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209004.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209005.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209006.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209007.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209008.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209009.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[9/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209010.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[10/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[11/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[12/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[13/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[14/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[15/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[16/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[17/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[18/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[19/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[20/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[21/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[22/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[23/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[24/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[25/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[26/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[27/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[28/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[29/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[30/30]⎯


 Test Files  1 failed (1)
      Tests  30 failed | 4 passed (34)
   Start at  22:16:08
   Duration  3.23s (transform 1.22s, setup 0ms, import 1.73s, tests 29ms, environment 0ms)


diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
The post-retirement focused results are as intended: second-family `50/50`, room-generator `56/56`, brutal-b `47/47`; active v2 remains `30 failed / 4 passed`, all 30 failures being missing files. I’m now running the plan’s existing targeted-suite inventory serially so the handoff can name every other pre-existing RED title and distinguish it from Batch 2 regressions.
exec
/bin/bash -lc "rg --files tests/unit | rg '(d569-blind-experiment|d569-v5|ai-dm-arena|ai-dm-rerun-packet-protocol|los-cover-era-audit|blind-turn-context|composite-turn-proposals|engine-opportunity-movement-intel|speculative-planning)\\.test\\.ts"'$'"' | LC_ALL=C sort" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
tests/unit/tools/ai-dm-arena.test.ts
tests/unit/tools/ai-dm-rerun-packet-protocol.test.ts
tests/unit/tools/d569-blind-experiment.test.ts
tests/unit/tools/d569-v5.test.ts
tests/unit/tools/los-cover-era-audit.test.ts
tests/unit/vtt/blind-turn-context.test.ts
tests/unit/vtt/composite-turn-proposals.test.ts
tests/unit/vtt/engine-opportunity-movement-intel.test.ts
tests/unit/vtt/speculative-planning.test.ts

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc 'npx vitest run tests/unit/tools/d569-blind-experiment.test.ts tests/unit/tools/d569-v5.test.ts tests/unit/tools/ai-dm-arena.test.ts tests/unit/tools/ai-dm-rerun-packet-protocol.test.ts tests/unit/tools/los-cover-era-audit.test.ts tests/unit/vtt/blind-turn-context.test.ts tests/unit/vtt/composite-turn-proposals.test.ts tests/unit/vtt/engine-opportunity-movement-intel.test.ts tests/unit/vtt/speculative-planning.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner --reporter=json --outputFile=/tmp/cohort-b2-targeted.json' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
The targeted inventory is still running serially; no unexpected early crash has appeared. I’m letting the current suite finish, then I’ll extract only failed titles and their first causal message from the JSON report.
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
The inventory has progressed through the arena’s synthetic scheduling cases and is continuing without external model calls. It is intentionally the narrow nine-suite matrix from the plan, not a full Vitest run.
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
The serial matrix is still active with no timeout or process error; these D569/arena tests are the slow portion. I’m continuing the same run so its failure inventory remains one coherent result rather than mixing partial reruns.
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
The process remains quiet longer than the fast suites, so I’m checking its elapsed time and CPU state non-destructively; I will only stop it if it has actually stalled or exceeded the suite’s own timeout.
exec
/bin/bash -lc "ps -eo pid=,ppid=,etime=,stat=,%cpu=,cmd= | rg 'vitest run tests/unit/tools/d569-blind-experiment|node .*vitest' | head -20" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
      1       0       00:00 Ss    0.0 /home/vagrant/.codex/tmp/arg0/codex-arg0b2dQc6/codex-linux-sandbox --sandbox-policy-cwd /home/vagrant/PhpstormProjects/dnd-wt-cohort-01 --command-cwd /home/vagrant/PhpstormProjects/dnd-wt-cohort-01 --permission-profile {"type":"managed","file_system":{"type":"restricted","entries":[{"path":{"type":"special","value":{"kind":"root"}},"access":"read"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-cohort-01"},"access":"write"},{"path":{"type":"special","value":{"kind":"slash_tmp"}},"access":"write"},{"path":{"type":"special","value":{"kind":"tmpdir"}},"access":"write"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/.git"},"access":"read","missing_path_behavior":"skip"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/.agents"},"access":"read","missing_path_behavior":"skip"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/.codex"},"access":"read","missing_path_behavior":"skip"}]},"network":"enabled"} --apply-seccomp-then-exec -- /bin/bash -c __CODEX_SNAPSHOT_OVERRIDE_SET_0="${CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS+x}" __CODEX_SNAPSHOT_OVERRIDE_0="${CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS-}" __CODEX_SNAPSHOT_OVERRIDE_SET_1="${CODEX_PERMISSION_PROFILE+x}" __CODEX_SNAPSHOT_OVERRIDE_1="${CODEX_PERMISSION_PROFILE-}" __CODEX_SNAPSHOT_OVERRIDE_SET_2="${CODEX_PLUGIN_METRICS_OUTPUT+x}" __CODEX_SNAPSHOT_OVERRIDE_2="${CODEX_PLUGIN_METRICS_OUTPUT-}" __CODEX_SNAPSHOT_OVERRIDE_SET_3="${CODEX_SESSION_ID+x}" __CODEX_SNAPSHOT_OVERRIDE_3="${CODEX_SESSION_ID-}" __CODEX_SNAPSHOT_OVERRIDE_SET_4="${CODEX_THREAD_ID+x}" __CODEX_SNAPSHOT_OVERRIDE_4="${CODEX_THREAD_ID-}" __CODEX_SNAPSHOT_OVERRIDE_SET_5="${CODEX_VERSION+x}" __CODEX_SNAPSHOT_OVERRIDE_5="${CODEX_VERSION-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_0="${ALL_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_0="${ALL_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_1="${BUNDLE_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_1="${BUNDLE_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_2="${BUNDLE_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_2="${BUNDLE_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_3="${BUNDLE_NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_3="${BUNDLE_NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_4="${BUNDLE_SSL_CA_CERT+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_4="${BUNDLE_SSL_CA_CERT-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_5="${CARGO_HTTP_CAINFO+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_5="${CARGO_HTTP_CAINFO-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_6="${CODEX_CA_CERTIFICATE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_6="${CODEX_CA_CERTIFICATE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_7="${CODEX_NETWORK_ALLOW_LOCAL_BINDING+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_7="${CODEX_NETWORK_ALLOW_LOCAL_BINDING-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_8="${CODEX_NETWORK_PROXY_ACTIVE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_8="${CODEX_NETWORK_PROXY_ACTIVE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_9="${CODEX_NETWORK_PROXY_ATTRIBUTION+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_9="${CODEX_NETWORK_PROXY_ATTRIBUTION-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_10="${CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_10="${CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_11="${CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_11="${CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_12="${CURL_CA_BUNDLE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_12="${CURL_CA_BUNDLE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_13="${DOCKER_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_13="${DOCKER_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_14="${DOCKER_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_14="${DOCKER_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_15="${ELECTRON_GET_USE_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_15="${ELECTRON_GET_USE_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_16="${FTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_16="${FTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_17="${GIT_SSL_CAINFO+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_17="${GIT_SSL_CAINFO-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_18="${HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_18="${HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_19="${HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_19="${HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_20="${NODE_EXTRA_CA_CERTS+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_20="${NODE_EXTRA_CA_CERTS-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_21="${NODE_USE_ENV_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_21="${NODE_USE_ENV_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_22="${NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_22="${NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_23="${NPM_CONFIG_CAFILE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_23="${NPM_CONFIG_CAFILE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_24="${NPM_CONFIG_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_24="${NPM_CONFIG_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_25="${NPM_CONFIG_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_25="${NPM_CONFIG_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_26="${NPM_CONFIG_NOPROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_26="${NPM_CONFIG_NOPROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_27="${NPM_CONFIG_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_27="${NPM_CONFIG_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_28="${PIP_CERT+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_28="${PIP_CERT-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_29="${PIP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_29="${PIP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_30="${REQUESTS_CA_BUNDLE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_30="${REQUESTS_CA_BUNDLE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_31="${SSL_CERT_FILE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_31="${SSL_CERT_FILE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_32="${WSS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_32="${WSS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_33="${WS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_33="${WS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_34="${YARN_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_34="${YARN_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_35="${YARN_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_35="${YARN_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_36="${YARN_NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_36="${YARN_NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_37="${all_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_37="${all_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_38="${ftp_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_38="${ftp_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_39="${http_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_39="${http_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_40="${https_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_40="${https_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_41="${no_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_41="${no_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_42="${npm_config_cafile+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_42="${npm_config_cafile-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_43="${npm_config_http_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_43="${npm_config_http_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_44="${npm_config_https_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_44="${npm_config_https_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_45="${npm_config_noproxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_45="${npm_config_noproxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_46="${npm_config_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_46="${npm_config_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_47="${ws_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_47="${ws_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_48="${wss_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_48="${wss_proxy-}" __CODEX_SNAPSHOT_PROXY_ENV_SET="${CODEX_NETWORK_PROXY_ACTIVE+x}"  if . '/home/vagrant/.codex/shell_snapshots/01a0b245-5a53-7990-9cf5-5abf61f029d7.1789697284695867342.sh' >/dev/null 2>&1; then :; fi  if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_0}" ]; then export CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS="${__CODEX_SNAPSHOT_OVERRIDE_0}"; else unset CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_1}" ]; then export CODEX_PERMISSION_PROFILE="${__CODEX_SNAPSHOT_OVERRIDE_1}"; else unset CODEX_PERMISSION_PROFILE; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_2}" ]; then export CODEX_PLUGIN_METRICS_OUTPUT="${__CODEX_SNAPSHOT_OVERRIDE_2}"; else unset CODEX_PLUGIN_METRICS_OUTPUT; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_3}" ]; then export CODEX_SESSION_ID="${__CODEX_SNAPSHOT_OVERRIDE_3}"; else unset CODEX_SESSION_ID; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_4}" ]; then export CODEX_THREAD_ID="${__CODEX_SNAPSHOT_OVERRIDE_4}"; else unset CODEX_THREAD_ID; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_5}" ]; then export CODEX_VERSION="${__CODEX_SNAPSHOT_OVERRIDE_5}"; else unset CODEX_VERSION; fi if [ -n "$__CODEX_SNAPSHOT_PROXY_ENV_SET" ] || [ -n "${CODEX_NETWORK_PROXY_ACTIVE+x}" ]; then if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_0}" ]; then export ALL_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_0}"; else unset ALL_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_1}" ]; then export BUNDLE_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_1}"; else unset BUNDLE_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_2}" ]; then export BUNDLE_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_2}"; else unset BUNDLE_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_3}" ]; then export BUNDLE_NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_3}"; else unset BUNDLE_NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_4}" ]; then export BUNDLE_SSL_CA_CERT="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_4}"; else unset BUNDLE_SSL_CA_CERT; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_5}" ]; then export CARGO_HTTP_CAINFO="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_5}"; else unset CARGO_HTTP_CAINFO; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_6}" ]; then export CODEX_CA_CERTIFICATE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_6}"; else unset CODEX_CA_CERTIFICATE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_7}" ]; then export CODEX_NETWORK_ALLOW_LOCAL_BINDING="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_7}"; else unset CODEX_NETWORK_ALLOW_LOCAL_BINDING; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_8}" ]; then export CODEX_NETWORK_PROXY_ACTIVE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_8}"; else unset CODEX_NETWORK_PROXY_ACTIVE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_9}" ]; then export CODEX_NETWORK_PROXY_ATTRIBUTION="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_9}"; else unset CODEX_NETWORK_PROXY_ATTRIBUTION; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_10}" ]; then export CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_10}"; else unset CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_11}" ]; then export CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_11}"; else unset CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_12}" ]; then export CURL_CA_BUNDLE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_12}"; else unset CURL_CA_BUNDLE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_13}" ]; then export DOCKER_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_13}"; else unset DOCKER_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_14}" ]; then export DOCKER_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_14}"; else unset DOCKER_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_15}" ]; then export ELECTRON_GET_USE_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_15}"; else unset ELECTRON_GET_USE_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_16}" ]; then export FTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_16}"; else unset FTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_17}" ]; then export GIT_SSL_CAINFO="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_17}"; else unset GIT_SSL_CAINFO; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_18}" ]; then export HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_18}"; else unset HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_19}" ]; then export HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_19}"; else unset HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_20}" ]; then export NODE_EXTRA_CA_CERTS="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_20}"; else unset NODE_EXTRA_CA_CERTS; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_21}" ]; then export NODE_USE_ENV_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_21}"; else unset NODE_USE_ENV_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_22}" ]; then export NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_22}"; else unset NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_23}" ]; then export NPM_CONFIG_CAFILE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_23}"; else unset NPM_CONFIG_CAFILE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_24}" ]; then export NPM_CONFIG_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_24}"; else unset NPM_CONFIG_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_25}" ]; then export NPM_CONFIG_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_25}"; else unset NPM_CONFIG_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_26}" ]; then export NPM_CONFIG_NOPROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_26}"; else unset NPM_CONFIG_NOPROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_27}" ]; then export NPM_CONFIG_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_27}"; else unset NPM_CONFIG_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_28}" ]; then export PIP_CERT="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_28}"; else unset PIP_CERT; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_29}" ]; then export PIP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_29}"; else unset PIP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_30}" ]; then export REQUESTS_CA_BUNDLE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_30}"; else unset REQUESTS_CA_BUNDLE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_31}" ]; then export SSL_CERT_FILE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_31}"; else unset SSL_CERT_FILE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_32}" ]; then export WSS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_32}"; else unset WSS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_33}" ]; then export WS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_33}"; else unset WS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_34}" ]; then export YARN_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_34}"; else unset YARN_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_35}" ]; then export YARN_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_35}"; else unset YARN_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_36}" ]; then export YARN_NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_36}"; else unset YARN_NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_37}" ]; then export all_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_37}"; else unset all_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_38}" ]; then export ftp_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_38}"; else unset ftp_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_39}" ]; then export http_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_39}"; else unset http_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_40}" ]; then export https_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_40}"; else unset https_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_41}" ]; then export no_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_41}"; else unset no_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_42}" ]; then export npm_config_cafile="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_42}"; else unset npm_config_cafile; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_43}" ]; then export npm_config_http_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_43}"; else unset npm_config_http_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_44}" ]; then export npm_config_https_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_44}"; else unset npm_config_https_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_45}" ]; then export npm_config_noproxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_45}"; else unset npm_config_noproxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_46}" ]; then export npm_config_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_46}"; else unset npm_config_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_47}" ]; then export ws_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_47}"; else unset ws_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_48}" ]; then export wss_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_48}"; else unset wss_proxy; fi fi if [ -n "${PATH:-}" ]; then export PATH='/home/vagrant/.nvm/versions/node/v24.13.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/codex-path':"$PATH"; else export PATH='/home/vagrant/.nvm/versions/node/v24.13.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/codex-path'; fi  exec '/bin/bash' -c 'ps -eo pid=,ppid=,etime=,stat=,%cpu=,cmd= | rg '"'"'vitest run tests/unit/tools/d569-blind-experiment|node .*vitest'"'"' | head -20'
      2       1       00:00 S     0.0 /bin/bash -c ps -eo pid=,ppid=,etime=,stat=,%cpu=,cmd= | rg 'vitest run tests/unit/tools/d569-blind-experiment|node .*vitest' | head -20
      4       2       00:00 R     0.0 rg vitest run tests/unit/tools/d569-blind-experiment|node .*vitest

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc 'pgrep -a node || true
ls -l /tmp/cohort-b2-targeted.json 2>/dev/null || true' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

 exited 1 in 400412ms:
[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
[arena] interleave scheduling started arms=2 rooms=1 reps=1
[arena] dispatch room=1 rep=1 arm=S model=gpt-5.6-luna effort=low
[arena] dispatch room=1 rep=1 arm=V model=gpt-5.6-luna effort=low
[arena] interleave scheduling started arms=2 rooms=2 reps=2
[arena] dispatch room=1 rep=1 arm=control model=model-control effort=low
[arena] dispatch room=1 rep=1 arm=candidate model=model-candidate effort=high
[arena] dispatch room=1 rep=2 arm=control model=model-control effort=low
[arena] dispatch room=1 rep=2 arm=candidate model=model-candidate effort=high
[arena] dispatch room=2 rep=1 arm=control model=model-control effort=low
[arena] dispatch room=2 rep=1 arm=candidate model=model-candidate effort=high
[arena] dispatch room=2 rep=2 arm=control model=model-control effort=low
[arena] dispatch room=2 rep=2 arm=candidate model=model-candidate effort=high
[arena] interleave scheduling started arms=2 rooms=1 reps=1
[arena] dispatch room=1 rep=1 arm=control model=model-control effort=low
[arena] dispatch room=1 rep=1 arm=candidate model=model-candidate effort=high
[arena] interleave scheduling started arms=2 rooms=1 reps=1
[arena] dispatch room=1 rep=1 arm=plain model=model-plain effort=low
[arena] dispatch room=1 rep=1 arm=tiered model=model-tiered effort=high
JSON report written to /tmp/cohort-b2-targeted.json

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc "jq '{numTotalTestSuites,numPassedTestSuites,numFailedTestSuites,numTotalTests,numPassedTests,numFailedTests,numPendingTests,testResults: [.testResults[] | {name,status,failures: [.assertionResults[] | select(.status == \"failed\") | {title: .fullName, message: (.failureMessages[0] // \"\" | split(\"\\n\")[0])}]}]}' /tmp/cohort-b2-targeted.json" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
{
  "numTotalTestSuites": 22,
  "numPassedTestSuites": 12,
  "numFailedTestSuites": 10,
  "numTotalTests": 230,
  "numPassedTests": 206,
  "numFailedTests": 24,
  "numPendingTests": 0,
  "testResults": [
    {
      "name": "/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/ai-dm-arena.test.ts",
      "status": "failed",
      "failures": [
        {
          "title": "AI-DM arena adjustment_runs_in_blind_arm and blind_arm_reuses_semantic_board_by_default: keeps screenshot-first parity",
          "message": "Error: Blind row lacks typed turn-context delivery evidence."
        },
        {
          "title": "AI-DM arena blind_row_omits_context_cap_evidence: records the separately capped semantic diagnostic",
          "message": "Error: Blind row lacks typed turn-context delivery evidence."
        },
        {
          "title": "AI-DM arena resolver_latency_included_as_model_wall and first_token_uses_first_stdout_line: separates code-only retries",
          "message": "Error: Blind row lacks typed turn-context delivery evidence."
        },
        {
          "title": "AI-DM arena blind_row_exposes_option_order: audits retries in the separately labelled minimal-hint arm",
          "message": "Error: Blind row lacks typed turn-context delivery evidence."
        },
        {
          "title": "AI-DM arena plumbs the cap into real hard-fixture context construction and arena evidence",
          "message": "AssertionError: expected 51 to be greater than 32768"
        },
        {
          "title": "AI-DM arena runs the frozen control probe through the arena and emits a deterministic mechanical verdict",
          "message": "AssertionError: expected { …(3) } to deeply equal { …(3) }"
        },
        {
          "title": "AI-DM arena threads inline renderer-profile JSON through dry-run rows while keeping arena metadata",
          "message": "AssertionError: expected undefined to deeply equal { Object (policy_version) }"
        },
        {
          "title": "AI-DM arena suppresses every intel context surface while attributing off rows and preserving full bytes",
          "message": "AssertionError: expected undefined to deeply equal ObjectContaining{…}"
        },
        {
          "title": "AI-DM arena renders and validates a multi-round dry run without spawning a model",
          "message": "AssertionError: expected false to be true // Object.is equality"
        },
        {
          "title": "AI-DM arena keeps configured escalation dormant after one invalid submission and attributes arm-base correction",
          "message": "AssertionError: expected { …(106) } to deeply equal ObjectContaining{…}"
        },
        {
          "title": "AI-DM arena classifies exact, edited, and ignored responses to the inline suggestion from structural bookkeeping",
          "message": "AssertionError: expected { …(106) } to match object { outcome: 'authorized', …(2) }"
        },
        {
          "title": "AI-DM arena adopts the frozen room-two focus draft as complete legal options",
          "message": "AssertionError: expected { …(106) } to match object { outcome: 'authorized', …(2) }"
        },
        {
          "title": "AI-DM arena runs as a vite-node --dry-run CLI without contacting the model binary",
          "message": "AssertionError: expected 'failed to load config from /home/vagr…' to be '' // Object.is equality"
        },
        {
          "title": "AI-DM arena runs through vite-node with a retained -- separator instead of exiting silently",
          "message": "AssertionError: expected 'failed to load config from /home/vagr…' to contain '--out is required.'"
        },
        {
          "title": "AI-DM arena records the engine actual unavailable-option rejection strings in chain evidence",
          "message": "AssertionError: expected { …(106) } to deeply equal ObjectContaining{…}"
        }
      ]
    },
    {
      "name": "/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/ai-dm-rerun-packet-protocol.test.ts",
      "status": "passed",
      "failures": []
    },
    {
      "name": "/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-blind-experiment.test.ts",
      "status": "failed",
      "failures": [
        {
          "title": "D569 preregistered experiment manifest and dry runner validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment",
          "message": "AssertionError: expected [ …(40) ] to deeply equal []"
        },
        {
          "title": "D569 preregistered experiment manifest and dry runner dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows",
          "message": "TypeError: Invalid D569 experiment manifest: second_family_first_regeneration"
        },
        {
          "title": "D569 preregistered experiment manifest and dry runner adds exactly 180 separately labeled advice-assisted hint cells, 660 total",
          "message": "TypeError: Invalid D569 experiment manifest: second_family_first_regeneration"
        },
        {
          "title": "D569 preregistered experiment manifest and dry runner consumes the frozen second encounter family for all 480 core cells",
          "message": "TypeError: Invalid D569 experiment manifest: second_family_first_regeneration"
        },
        {
          "title": "D569 preregistered experiment manifest and dry runner records exact model effort cli and cliVersion for every observed row",
          "message": "TypeError: Invalid D569 experiment manifest: second_family_first_regeneration"
        },
        {
          "title": "D569 preregistered experiment manifest and dry runner rejects either decisive integrity field alone while preserving rows with neither field",
          "message": "TypeError: Invalid D569 experiment manifest: second_family_first_regeneration"
        }
      ]
    },
    {
      "name": "/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-v5.test.ts",
      "status": "passed",
      "failures": []
    },
    {
      "name": "/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/los-cover-era-audit.test.ts",
      "status": "passed",
      "failures": []
    },
    {
      "name": "/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/blind-turn-context.test.ts",
      "status": "passed",
      "failures": []
    },
    {
      "name": "/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/composite-turn-proposals.test.ts",
      "status": "failed",
      "failures": [
        {
          "title": "complete action economy and composite turn proposals projects fresh options for every requested generated-room monster turn",
          "message": "AssertionError: expected [ 'Dodge', 'End Turn' ] to include 'Longbow + Longbow -> combatant:fighter'"
        }
      ]
    },
    {
      "name": "/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/engine-opportunity-movement-intel.test.ts",
      "status": "failed",
      "failures": [
        {
          "title": "engine movement and opportunity-cost intel does not repeat the frozen Room-8 four-actor zero-feet Dash plan",
          "message": "AssertionError: expected [ { …(4) }, { …(4) }, { …(4) }, …(1) ] to deeply equal [ { …(4) }, { …(4) }, { …(4) }, …(1) ]"
        }
      ]
    },
    {
      "name": "/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/speculative-planning.test.ts",
      "status": "failed",
      "failures": [
        {
          "title": "speculative host fact system forms a bounded volatility-ranked menu from baseline dependencies and canonical movement proofs",
          "message": "AssertionError: expected 0 to be greater than 0"
        }
      ]
    }
  ]
}

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc "if [ -f /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.tmp/runs/cohort-01/impl-cohort-01-b2.md ]; then sed -n '1,260p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.tmp/runs/cohort-01/impl-cohort-01-b2.md; else printf 'NO_BRIEF\\n'; fi" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
COMMON RULES (binding, restated in full): You are the IMPLEMENTER (gpt-5.6-sol, high, FRESH session) for VIS-FIELD-01 batch group COHORT-01, Batch 2 ONLY. Worktree /home/vagrant/PhpstormProjects/dnd-wt-cohort-01 (branch claude/cohort-01 at e92ebc93 = WALL-01 candidate 6887755f + Batch 1 (ACCEPTED by astra); clean; node_modules symlinked). Sandbox workspace-write in that worktree ONLY (+ /tmp). No git write commands (the supervisor commits). No edits under .claude/** or docs/**. Do NOT invoke claude, `claude -p`, any reviewer session, any consensus workflow or any other agent; skills under ~/.codex do NOT apply. No Playwright, no full vitest, no npm run build; never port 4173. Model calls WILL fail. NEVER regenerate an expectation from our own output; never delete or weaken a test (retirements only where the plan names them, with the D635/D642/D643 rationale comment); never fresh hashes without a printed derivation; NO byte of tests/fixtures/d569-second-family-manifest.json, tests/fixtures/d569-blind-experiment-manifest.json, tests/fixtures/arena-basis-brutal*, -brutal-b, -brutal-2, -hard-2 may change (V-freeze before and after). Report exact commands and numbers; end with the named marker. The MAIN repo's .claude/decisions.md (D643, D648–D653) and .claude/consensus/cohort-01/*.md are readable.

THE PLAN (binding, FROZEN): .tmp-plans/2026-09-17-cohort-01-plan.md in your worktree — sha256 4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69, 403 lines incl. binding amendments N1 and N2 (verify with sha256sum first; STOP if it differs). Read it fully. Implement EXACTLY "### Batch 2 — RED active-cohort membership witnesses and frozen-contract retirement": its Allowed files, its RED-first witnesses for the v2 families (write them FIRST and show them red — the v2 directories do not exist yet, so the witnesses must fail for the right reason: missing cohort, not a syntax error), the retirement of the frozen families' PRODUCTIVITY assertions ONLY (byte pins, integrity, static checks, hashes and round-execution tests stay) with the plan's rationale comment, the productivity predicate PRESERVED per N2's sibling ruling PLAN-F6 (positive movement stays productive; the sight/cover checks apply only to targeted offensive components; the Dash-only witness and the movement-arm mutant), the mutants the plan lists, and its T/V commands with expected numbers. ALSO (astra B1-F1, P3, one line): at tests/unit/tools/d569-second-family-manifest.test.ts:~147 where brutal_productivity is filtered for the frozen 6207 family, add the comment that 6207 keeps historical integrity/static checks while 6211 (v2) owns live productivity, citing D635/D642/D643. Do NOT generate any cohort (Batches 3–5 are gated on WALL-01's review) and do NOT start Batch 6. If the plan's Batch 2 text is ambiguous or infeasible at e92ebc93, STOP and report BLOCKED with the exact gap.

VERIFICATION: the batch's T and V exactly as the plan states; npx tsc -p tsconfig.app.json --noEmit; npx tsc -p tsconfig.node.json --noEmit; git diff --check; no line > 120; git status --short = the Allowed files (+ the one-line B1-F1 comment file); V-freeze before/after; list every remaining red by title with its one-line cause (expected: the v2 witnesses red for "cohort missing", the six blind-experiment helper reds, brutal-b/brutal/behaviour reds only where the plan says they persist until later batches). OUTPUT: report, then "COHORT-01 B2 DONE" (or BLOCKED with the gap).

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
The scoped RED inventory is now confirmed: the six D569 helper failures are all `second_family_first_regeneration`, and the three Batch-9 behavior witnesses remain stale as planned. I’m finishing the required mutation proof next—one reversible mutation at a time, each run against its named witness, restoring the source immediately after each run.
apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-v2.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..52ac379d1f82ecb1eb91134f7e22a609c306e4fb
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,221 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return true;
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..52ac379d1f82ecb1eb91134f7e22a609c306e4fb
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,221 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return true;
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc "npx vitest run tests/unit/vtt/arena-basis-brutal-v2.test.ts -t 'rejects a living monster whose only valid options are nonproductive' --maxWorkers=1 --no-file-parallelism --configLoader runner" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 3525ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts (34 tests | 1 failed | 33 skipped) 17ms
     × rejects a living monster whose only valid options are nonproductive 16ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > rejects a living monster whose only valid options are nonproductive
AssertionError: expected true to be false // Object.is equality

- Expected
+ Received

- false
+ true

 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:183:72
    181|     expect(availableEngineActorOptions(fixture.state, fixture.actorId,…
    182|       .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
    183|     expect(monsterHasProductiveOption(fixture.state, fixture.actorId))…
       |                                                                        ^
    184|   });
    185|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 33 skipped (34)
   Start at  22:23:56
   Duration  3.21s (transform 1.22s, setup 0ms, import 1.74s, tests 17ms, environment 0ms)


diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..52ac379d1f82ecb1eb91134f7e22a609c306e4fb
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,221 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return true;
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-v2.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-v2.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec95e6a57654c15c4751322361b204239f2c2926
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,219 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec95e6a57654c15c4751322361b204239f2c2926
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,219 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc "npx vitest run tests/unit/vtt/arena-basis-brutal-v2.test.ts -t 'keeps a positive-movement-only Dash monster productive' --maxWorkers=1 --no-file-parallelism --configLoader runner" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 3545ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts (34 tests | 1 failed | 33 skipped) 19ms
     × keeps a positive-movement-only Dash monster productive 18ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > keeps a positive-movement-only Dash monster productive
AssertionError: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:197:72
    195|     expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan…
    196|     expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kin…
    197|     expect(monsterHasProductiveOption(fixture.state, fixture.actorId))…
       |                                                                        ^
    198|   });
    199|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 33 skipped (34)
   Start at  22:24:11
   Duration  3.23s (transform 1.23s, setup 0ms, import 1.74s, tests 19ms, environment 0ms)


diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec95e6a57654c15c4751322361b204239f2c2926
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,219 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-v2.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-v2.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..e29254551d5cdedf0635d99995d384c63b0cb68a
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..e29254551d5cdedf0635d99995d384c63b0cb68a
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc "npx vitest run tests/unit/vtt/arena-basis-brutal-v2.test.ts -t 'rejects targeted offense whose resolved origin has an optically blocked trace' --maxWorkers=1 --no-file-parallelism --configLoader runner" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 3533ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts (34 tests | 1 failed | 33 skipped) 20ms
     × rejects targeted offense whose resolved origin has an optically blocked trace 20ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > rejects targeted offense whose resolved origin has an optically blocked trace
AssertionError: expected true to be false // Object.is equality

- Expected
+ Received

- false
+ true

 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:218:74
    216|     })).toMatchObject({ tier: 'half', blocksSight: true });
    217|     expect(resolvedMechanicsIsProductive(fixture.state, resolution.mec…
    218|     expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics…
       |                                                                          ^
    219|   });
    220| });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 33 skipped (34)
   Start at  22:24:26
   Duration  3.22s (transform 1.23s, setup 0ms, import 1.75s, tests 20ms, environment 0ms)


diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..e29254551d5cdedf0635d99995d384c63b0cb68a
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-v2.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
All three mutants were killed exactly once by their named witnesses: force-true, deleted movement arm, and ignored `blocksSight`. The production test source is restored. I’m checking line length and the final diff now, then I’ll rerun the final TypeScript/freeze gates from the restored state.
exec
/bin/bash -lc "git diff --check
awk 'length("'$0) > 120 { printf "%s:%d:%d:%s'"\\n\", FILENAME, FNR, length("'$0), $0 }'"' tests/unit/vtt/arena-basis-brutal-v2.test.ts tests/unit/vtt/room-generator.test.ts tests/unit/vtt/arena-basis-brutal-b.test.ts tests/unit/tools/d569-second-family-manifest.test.ts
git status --short
git diff --stat
git diff -- tests/unit/vtt/room-generator.test.ts tests/unit/vtt/arena-basis-brutal-b.test.ts tests/unit/tools/d569-second-family-manifest.test.ts | sed -n '1,260p'" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
tests/unit/vtt/arena-basis-brutal-b.test.ts:193:128:      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
 M tests/unit/tools/d569-second-family-manifest.test.ts
 M tests/unit/vtt/arena-basis-brutal-b.test.ts
 M tests/unit/vtt/room-generator.test.ts
?? tests/unit/vtt/arena-basis-brutal-v2.test.ts
 .../unit/tools/d569-second-family-manifest.test.ts |  1 +
 tests/unit/vtt/arena-basis-brutal-b.test.ts        | 18 ++-----------
 tests/unit/vtt/room-generator.test.ts              | 31 +++-------------------
 3 files changed, 6 insertions(+), 44 deletions(-)
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172..8c92f313 100644
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@ describe('D569 independent second-family manifest', () => {
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb..d87a6410 100644
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@ function assertBrutalMembership(
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad..267bbde7 100644
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@ import {
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@ const BRUTAL_BASIS_FIXTURE_DIGESTS = {
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@ describe('seeded room generator', () => {
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..d87a641019c31386a69881f4ad67d950f58834be
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -375,22 +375,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-b.test.ts
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc "jq -r '.testResults[] | select(.name | endswith(\"d569-blind-experiment.test.ts\")) | .assertionResults[] | select(.status == \"failed\") | \"TITLE: \\(.fullName)\\n\\(.failureMessages[0])\\n\"' /tmp/cohort-b2-targeted.json | sed -n '1,260p'
printf '%s\\n' '--- BEHAVIOUR ---'
jq -r '.testResults[] | select(.name | test(\"(composite-turn-proposals|engine-opportunity-movement-intel|speculative-planning)\\\\.test\\\\.ts"'$")) | .assertionResults[] | select(.status == "failed") | "TITLE: '"\\(.fullName)\\n\\(.failureMessages[0])\\n\"' /tmp/cohort-b2-targeted.json | sed -n '1,220p'" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
TITLE: D569 preregistered experiment manifest and dry runner validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment
AssertionError: expected [ …(40) ] to deeply equal []
    at /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-blind-experiment.test.ts:409:27
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:302:11
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:1903:26
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2326:20
    at new Promise (<anonymous>)
    at runWithCancel (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2323:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2305:20
    at new Promise (<anonymous>)
    at runWithTimeout (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2272:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2955:64

TITLE: D569 preregistered experiment manifest and dry runner dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
    at dryRunD569Experiment (/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-blind-experiment.ts:677:11)
    at /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-blind-experiment.test.ts:473:19
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:302:11
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:1903:26
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2326:20
    at new Promise (<anonymous>)
    at runWithCancel (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2323:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2305:20
    at new Promise (<anonymous>)
    at runWithTimeout (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2272:10)

TITLE: D569 preregistered experiment manifest and dry runner adds exactly 180 separately labeled advice-assisted hint cells, 660 total
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
    at dryRunD569Experiment (/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-blind-experiment.ts:677:11)
    at /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-blind-experiment.test.ts:635:19
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:302:11
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:1903:26
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2326:20
    at new Promise (<anonymous>)
    at runWithCancel (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2323:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2305:20
    at new Promise (<anonymous>)
    at runWithTimeout (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2272:10)

TITLE: D569 preregistered experiment manifest and dry runner consumes the frozen second encounter family for all 480 core cells
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
    at dryRunD569Experiment (/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-blind-experiment.ts:677:11)
    at /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-blind-experiment.test.ts:644:19
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:302:11
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:1903:26
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2326:20
    at new Promise (<anonymous>)
    at runWithCancel (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2323:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2305:20
    at new Promise (<anonymous>)
    at runWithTimeout (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2272:10)

TITLE: D569 preregistered experiment manifest and dry runner records exact model effort cli and cliVersion for every observed row
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
    at dryRunD569Experiment (/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-blind-experiment.ts:677:11)
    at /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-blind-experiment.test.ts:653:19
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:302:11
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:1903:26
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2326:20
    at new Promise (<anonymous>)
    at runWithCancel (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2323:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2305:20
    at new Promise (<anonymous>)
    at runWithTimeout (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2272:10)

TITLE: D569 preregistered experiment manifest and dry runner rejects either decisive integrity field alone while preserving rows with neither field
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
    at dryRunD569Experiment (/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tools/d569-blind-experiment.ts:677:11)
    at /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-blind-experiment.test.ts:683:18
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:302:11
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:1903:26
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2326:20
    at new Promise (<anonymous>)
    at runWithCancel (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2323:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2305:20
    at new Promise (<anonymous>)
    at runWithTimeout (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2272:10)

--- BEHAVIOUR ---
TITLE: complete action economy and composite turn proposals projects fresh options for every requested generated-room monster turn
AssertionError: expected [ 'Dodge', 'End Turn' ] to include 'Longbow + Longbow -> combatant:fighter'
    at Proxy.<anonymous> (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/expect/dist/index.js:1319:15)
    at Proxy.<anonymous> (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/expect/dist/index.js:1156:15)
    at Proxy.methodWrapper (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/chai/index.js:1700:25)
    at /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/composite-turn-proposals.test.ts:160:20
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:1903:20

TITLE: engine movement and opportunity-cost intel does not repeat the frozen Room-8 four-actor zero-feet Dash plan
AssertionError: expected [ { …(4) }, { …(4) }, { …(4) }, …(1) ] to deeply equal [ { …(4) }, { …(4) }, { …(4) }, …(1) ]
    at /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/engine-opportunity-movement-intel.test.ts:145:22
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:302:11
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:1903:26
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2326:20
    at new Promise (<anonymous>)
    at runWithCancel (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2323:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2305:20
    at new Promise (<anonymous>)
    at runWithTimeout (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2272:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2955:64

TITLE: speculative host fact system forms a bounded volatility-ranked menu from baseline dependencies and canonical movement proofs
AssertionError: expected 0 to be greater than 0
    at /home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/speculative-planning.test.ts:554:40
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:302:11
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:1903:26
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2326:20
    at new Promise (<anonymous>)
    at runWithCancel (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2323:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2305:20
    at new Promise (<anonymous>)
    at runWithTimeout (file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2272:10)
    at file:///home/vagrant/PhpstormProjects/dnd-wt-vis-field/node_modules/@vitest/runner/dist/chunk-artifact.js:2955:64


diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc 'npx vitest run tests/unit/vtt/arena-basis-brutal-v2.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 exited 1 in 3636ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-cohort-01

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts (34 tests | 30 failed) 30ms
     × 'brutal-v2' seed 6209001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 2ms
     × 'brutal-v2' seed 6209002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-v2' seed 6209010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…' 0ms
     × 'brutal-b-v2' seed 6210001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-b-v2' seed 6210010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…' 0ms
     × 'brutal-2-v2' seed 6211001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms
     × 'brutal-2-v2' seed 6211010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…' 0ms

⎯⎯⎯⎯⎯⎯ Failed Tests 30 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209001.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209002.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209003.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209004.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209005.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209006.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209007.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209008.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209009.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[9/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-v2' seed 6209010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-v2/…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-v2/seed-6209010.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[10/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[11/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[12/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[13/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[14/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[15/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[16/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[17/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[18/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[19/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-b-v2' seed 6210010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-b-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[20/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211001 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[21/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211002 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[22/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211003 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[23/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211004 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[24/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211005 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[25/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211006 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[26/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211007 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[27/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211008 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[28/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211009 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[29/30]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-v2.test.ts > active brutal v2 arena membership > 'brutal-2-v2' seed 6211010 satisfies active state-derived membership at 'tests/fixtures/arena-basis-brutal-2-v…'
Error: ENOENT: no such file or directory, open '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json'
 ❯ Object.readText tests/helpers/test-inputs.ts:155:14
    153|     readText<Selected extends Path>(path: Selected): TestInputText<Cat…
    154|       assertAllowed(path);
    155|       return readFileSync(absoluteInputPath(path), 'utf8') as TestInpu…
       |              ^
    156|     },
    157|     readBytes<Selected extends Path>(path: Selected): TestInputBytes<C…
 ❯ tests/unit/vtt/arena-basis-brutal-v2.test.ts:164:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[30/30]⎯


 Test Files  1 failed (1)
      Tests  30 failed | 4 passed (34)
   Start at  22:25:20
   Duration  3.32s (transform 1.28s, setup 0ms, import 1.84s, tests 30ms, environment 0ms)


diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc 'npx tsc -p tsconfig.app.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
exec
/bin/bash -lc 'npx tsc -p tsconfig.node.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 14154ms:
 succeeded in 28303ms:
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
The restored active suite is again exactly `30 failed / 4 passed`, with every failure an absent v2 fixture; both final TypeScript gates pass `0/0`. I’m running the last V gates now: whitespace, line length, exact path set, frozen-byte diff, and repeated 32-file SHA inventory.
exec
/bin/bash -lc 'set +e
git diff --check
diff_status=$?
printf '"'GIT_DIFF_CHECK_STATUS=%s\\n' \""'$diff_status"
overlong=$(awk '"'length("'$0) > 120 { count += 1; print FILENAME ":" FNR ":" length($0) } END { print count + 0 }'"' tests/unit/vtt/arena-basis-brutal-v2.test.ts tests/unit/vtt/room-generator.test.ts tests/unit/vtt/arena-basis-brutal-b.test.ts tests/unit/tools/d569-second-family-manifest.test.ts)
printf 'OVERLONG_RESULT=%s\\n' \""'$overlong"
printf '"'%s\\n' 'STATUS_BEGIN'
git status --short
printf '%s\\n' 'STATUS_END'
git diff --exit-code -- tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2
freeze_status="'$?
printf '"'V_FREEZE_STATUS=%s\\n' \""'$freeze_status"
freeze_files=$(find tests/fixtures/arena-basis-brutal tests/fixtures/arena-basis-brutal-b tests/fixtures/arena-basis-brutal-2 -type f -print | LC_ALL=C sort)
printf '"'%s\\n' tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json "'$freeze_files | xargs sha256sum
printf '"'%s\\n' tests/fixtures/d569-second-family-manifest.json tests/fixtures/d569-blind-experiment-manifest.json "'$freeze_files | wc -l
sha256sum .tmp-plans/2026-09-17-cohort-01-plan.md
exit 0' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
GIT_DIFF_CHECK_STATUS=0
OVERLONG_RESULT=0
STATUS_BEGIN
 M tests/unit/tools/d569-second-family-manifest.test.ts
 M tests/unit/vtt/arena-basis-brutal-b.test.ts
 M tests/unit/vtt/room-generator.test.ts
?? tests/unit/vtt/arena-basis-brutal-v2.test.ts
STATUS_END
V_FREEZE_STATUS=0
83daa7ea3ca89b270e5149368d09fa97074124dfe438cf95b346e996996026c8  tests/fixtures/d569-second-family-manifest.json
8c0bcb3c0afa8358c26129efbef82d0d2882135389d8dd8040fe7e4b6451e13a  tests/fixtures/d569-blind-experiment-manifest.json
6847b5d44b2577aa31a87a2eb83220ca17f54f8a8b160741d61d47019c75e51c  tests/fixtures/arena-basis-brutal-2/seed-6207001.json
9b1b9be44245659fb1a95922c685bbb1f9082b3f0fa591bd822553573a9d5c66  tests/fixtures/arena-basis-brutal-2/seed-6207002.json
afe31b4cb79f606bd56f29d37267814319de161db745747dae1a70a528617d6f  tests/fixtures/arena-basis-brutal-2/seed-6207003.json
96701e3136318b9f4dd8859311e30bb85586d129c8ef4424fcea398f3bbc813e  tests/fixtures/arena-basis-brutal-2/seed-6207004.json
a3a6774081b01b743091115b29ccfbb81d109e2f26867ce74a1c431b9a56a63a  tests/fixtures/arena-basis-brutal-2/seed-6207005.json
28800721eac25312561d04c49389f95a05ae24e81ff13b1ed6191951a7b34082  tests/fixtures/arena-basis-brutal-2/seed-6207006.json
eb9a566eab8c314fd754d85402e280e5f75ee30e114451b9405007a536375f8f  tests/fixtures/arena-basis-brutal-2/seed-6207007.json
5926d642f77ca2cdbf8cbed29408cfbf4165d6a05b453c2203a0aace93827a2b  tests/fixtures/arena-basis-brutal-2/seed-6207008.json
d398b3ad8a24ecae7d3989a1d47be1bedf69b1b989b17d967278be2b2ca7a50c  tests/fixtures/arena-basis-brutal-2/seed-6207009.json
b7efa3c2df793fcbbdba1d453358577a1d4eb7c6b2fe8c9b575d7fe85c3adf04  tests/fixtures/arena-basis-brutal-2/seed-6207010.json
7cc25c0ab9a0e380373a306874290d0f5247e74c0efb71e43ab593b58f0d9d3b  tests/fixtures/arena-basis-brutal-b/seed-6206001.json
3fdfe05812aeb6724789b4ff6f753ce68c523c13ff024d9ee275bd1060adbcaa  tests/fixtures/arena-basis-brutal-b/seed-6206002.json
36828c26a62a8af8c9cc32db7f3a13124d897a8aacf8ce4d4df2c11d2c742d3c  tests/fixtures/arena-basis-brutal-b/seed-6206003.json
35a5980497b2184a4d684e1849117bb70e4887cb40264ea33117e3e14322c60a  tests/fixtures/arena-basis-brutal-b/seed-6206004.json
dc48b6cdd7d4e33dc3c9a0188586ebd1537deac4dec4284c7e9404cec652e2f1  tests/fixtures/arena-basis-brutal-b/seed-6206005.json
d66fc0a9f29d84b5f1bc5a46b86483f91236d05b5cb1b951519cce7db98e2269  tests/fixtures/arena-basis-brutal-b/seed-6206006.json
1c4c337148afad9aff2a86143bb3156cc446ffabd2ccc8cc29eb004d83034bd9  tests/fixtures/arena-basis-brutal-b/seed-6206007.json
11a065b490f6e68effaddb0e919b9f9f9f7ae300f19a97f3359d3d4c86658a32  tests/fixtures/arena-basis-brutal-b/seed-6206008.json
19808aca9fb3ca7e4a77b6222cfae31c4f69a16b9b74ecc0706cb1eee58d4f5e  tests/fixtures/arena-basis-brutal-b/seed-6206009.json
a9efe78460c66972d09c6efd1c7cc04409ba6fff5a0028657d2442a0ec2fbc83  tests/fixtures/arena-basis-brutal-b/seed-6206010.json
3f737f1ddf714b0381abdc0e822b3a07cda4c55287a4bc0b1a97cd4d7d71d63b  tests/fixtures/arena-basis-brutal/seed-6203001.json
8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860  tests/fixtures/arena-basis-brutal/seed-6203002.json
d54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f  tests/fixtures/arena-basis-brutal/seed-6203003.json
6f9465eccac77eb60ffab2be1832f8f1bcd3290c655130c2ac752a38e6d75a20  tests/fixtures/arena-basis-brutal/seed-6203004.json
5b3be6abbdf2fcccff3243639d582bea2fb1098099fa91cf2208a0f143a2364e  tests/fixtures/arena-basis-brutal/seed-6203005.json
3e6818fe70e276e9ee7407d76b01e3afab7254cc1c27e649d5f445a5f6a6480c  tests/fixtures/arena-basis-brutal/seed-6203006.json
e6215e5b6388a79bc850e7ff97867f2618733acbb434526a77133e382cba2d1b  tests/fixtures/arena-basis-brutal/seed-6203007.json
b067b1aa081f368859979415c468d997b6fa504cad6ad70dc6998c90eeb3207c  tests/fixtures/arena-basis-brutal/seed-6203008.json
9780276cdec0ee71c99cc13217d73376990866f5ab3ab471c7a793196665f7ce  tests/fixtures/arena-basis-brutal/seed-6203009.json
2ec7f3c1298e7b6f3519cf765bcb88f36be3426e02cf7572730a77af9e32114d  tests/fixtures/arena-basis-brutal/seed-6203010.json
32
4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69  .tmp-plans/2026-09-17-cohort-01-plan.md

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
All final V gates pass: `git diff --check` 0, overlong lines 0, exact four allowed paths, V-freeze 0, and all 32 repeated hashes match the pre-edit inventory. I’m doing one final scoped RED report run containing only the six deferred D569 helpers and three deferred Batch-9 behavior cases, so the reported failure count is exact and excludes unrelated later-batch suites.
exec
/bin/bash -lc 'npx vitest run tests/unit/tools/d569-blind-experiment.test.ts tests/unit/vtt/composite-turn-proposals.test.ts tests/unit/vtt/engine-opportunity-movement-intel.test.ts tests/unit/vtt/speculative-planning.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner --reporter=json --outputFile=/tmp/cohort-b2-reds.json' in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

 exited 1 in 12156ms:
[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/5198a070dbed34af.json
JSON report written to /tmp/cohort-b2-reds.json

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

exec
/bin/bash -lc "jq '{numTotalTestSuites,numPassedTestSuites,numFailedTestSuites,numTotalTests,numPassedTests,numFailedTests,numPendingTests,failedTitles: [.testResults[].assertionResults[] | select(.status == \"failed\") | .fullName]}' /tmp/cohort-b2-reds.json" in /home/vagrant/PhpstormProjects/dnd-wt-cohort-01
 succeeded in 0ms:
{
  "numTotalTestSuites": 11,
  "numPassedTestSuites": 3,
  "numFailedTestSuites": 8,
  "numTotalTests": 91,
  "numPassedTests": 82,
  "numFailedTests": 9,
  "numPendingTests": 0,
  "failedTitles": [
    "D569 preregistered experiment manifest and dry runner validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment",
    "D569 preregistered experiment manifest and dry runner dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows",
    "D569 preregistered experiment manifest and dry runner adds exactly 180 separately labeled advice-assisted hint cells, 660 total",
    "D569 preregistered experiment manifest and dry runner consumes the frozen second encounter family for all 480 core cells",
    "D569 preregistered experiment manifest and dry runner records exact model effort cli and cliVersion for every observed row",
    "D569 preregistered experiment manifest and dry runner rejects either decisive integrity field alone while preserving rows with neither field",
    "complete action economy and composite turn proposals projects fresh options for every requested generated-room monster turn",
    "engine movement and opportunity-cost intel does not repeat the frozen Room-8 four-actor zero-feet Dash plan",
    "speculative host fact system forms a bounded volatility-ranked menu from baseline dependencies and canonical movement proofs"
  ]
}

diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

codex
Implemented Batch 2 only.

Changed:

- Added [arena-basis-brutal-v2.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-v2.test.ts) with 30 exact v2 fixture witnesses and three productivity mutant witnesses.
- Retired only frozen 6203 productivity assertions in [room-generator.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/room-generator.test.ts).
- Retired only frozen 6206 productivity assertions in [arena-basis-brutal-b.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-b.test.ts).
- Added the requested 6207/6211 D635/D642/D643 rationale comment in [d569-second-family-manifest.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts).
- Generated no fixtures and did not begin Batch 3 or Batch 6.

Verification:

- Frozen plan: SHA-256 `4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69`, 403 lines.
- Namespace audits matched exactly, statuses `0/1/0`.
- RED-first command:
  `npx vitest run tests/unit/vtt/arena-basis-brutal-v2.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner`
  → `30 failed / 4 passed`; all 30 failures were `ENOENT` for the declared missing v2 fixtures.
- Frozen suites:
  - second-family: `50/50` passed.
  - room-generator: `56/56` passed.
  - brutal-b: `47/47` passed.
  - No frozen productivity RED remains.
- Mutants:
  - Force productivity true: nonproductive witness failed `1/1`.
  - Delete movement arm: Dash-only witness failed `1/1`.
  - Treat `blocksSight:true` as open: blocked-target witness failed `1/1`.
- Deferred RED inventory command covered 91 tests: `82 passed / 9 failed`.
- `npx tsc -p tsconfig.app.json --noEmit` → exit 0.
- `npx tsc -p tsconfig.node.json --noEmit` → exit 0.
- `git diff --check` → exit 0.
- Lines over 120 characters → 0.
- V-freeze before and after → exit 0; all 32 SHA-256 values identical.
- Final status contains exactly:
  - `tests/unit/tools/d569-second-family-manifest.test.ts`
  - `tests/unit/vtt/arena-basis-brutal-b.test.ts`
  - `tests/unit/vtt/room-generator.test.ts`
  - `tests/unit/vtt/arena-basis-brutal-v2.test.ts`

Remaining RED titles:

- Active v2 title template: `'<family>' seed <seed> satisfies active state-derived membership at '<path>'`.
  - `brutal-v2`: 6209001–6209010.
  - `brutal-b-v2`: 6210001–6210010.
  - `brutal-2-v2`: 6211001–6211010.
  - Cause for each: declared cohort fixture is missing, as required before Batches 3–5.

- `validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment`
  - Cause: historical helper produces 40 second-family regeneration mismatches.
- `dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows`
  - Cause: `second_family_first_regeneration`.
- `adds exactly 180 separately labeled advice-assisted hint cells, 660 total`
  - Cause: `second_family_first_regeneration`.
- `consumes the frozen second encounter family for all 480 core cells`
  - Cause: `second_family_first_regeneration`.
- `records exact model effort cli and cliVersion for every observed row`
  - Cause: `second_family_first_regeneration`.
- `rejects either decisive integrity field alone while preserving rows with neither field`
  - Cause: `second_family_first_regeneration`.

- `projects fresh options for every requested generated-room monster turn`
  - Cause: D642 now yields `Dodge, End Turn` instead of the stale Longbow expectation; deferred to Batch 9.
- `does not repeat the frozen Room-8 four-actor zero-feet Dash plan`
  - Cause: the frozen Room-8 plan differs under sealed-wall geometry; deferred to Batch 9.
- `forms a bounded volatility-ranked menu from baseline dependencies and canonical movement proofs`
  - Cause: the frozen speculative scene now produces zero candidates; deferred to Batch 9.

COHORT-01 B2 DONE
diff --git a//tmp/cohort-b2-probe.ts b//tmp/cohort-b2-probe.ts
new file mode 100644
index 0000000000000000000000000000000000000000..cd97e9628ae44a48ad9ee866db7c5f799503247d
--- /dev/null
+++ b//tmp/cohort-b2-probe.ts
@@ -0,0 +1,39 @@
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/encounter.ts';
+import { monsterCombatantProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/combatant.ts';
+import { GIANT_SPIDER } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/statblocks/wild-beasts.ts';
+import { feet } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/combat/values.ts';
+import { availableEngineActorOptions, resolveEngineActorOption } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/intent-resolver.ts';
+import { freshMonsterPlanningState } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/monster-planning-state.ts';
+import { buildOfferEnvironment } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/src/vtt/offers/build-offer-environment.ts';
+import { monsterProfile, placedToken, playerProfile } from '/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/combat/fixtures.ts';
+
+const env = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+for (const speed of [0, 30]) {
+  const base = monsterProfile(`probe-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`target-${String(speed)}`);
+  const state = freshMonsterPlanningState(createEncounter({
+    bounds: { columns: 20, rows: 3 },
+    combatants: [actor, target],
+    tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+  }));
+  const rows = availableEngineActorOptions(state, actor.id, env).map((option) => {
+    const resolution = resolveEngineActorOption(state, option, env);
+    return { label: option.label, resolution };
+  });
+  process.stdout.write(`${String(speed)} ${JSON.stringify(rows)}\n`);
+}
+
+const spiderBase = monsterCombatantProfile(GIANT_SPIDER, {
+  combatantId: 'combatant:probe-spider', tokenId: 'token:probe-spider',
+});
+const spider = { ...spiderBase, rules: { ...spiderBase.rules, speed: feet(0) } };
+const target = playerProfile('probe-web-target');
+const open = freshMonsterPlanningState(createEncounter({
+  bounds: { columns: 6, rows: 3 }, combatants: [spider, target],
+  tokens: [placedToken(spider, 0, 1), placedToken(target, 5, 1)],
+}));
+for (const option of availableEngineActorOptions(open, spider.id, env)) {
+  const resolution = resolveEngineActorOption(open, option, env);
+  process.stdout.write(`spider ${option.label} ${JSON.stringify(resolution)}\n`);
+}
diff --git a//tmp/cohort-vite.config.mjs b//tmp/cohort-vite.config.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..7238ec44c58d61f74c1af42dad1dc5726cf84dab
--- /dev/null
+++ b//tmp/cohort-vite.config.mjs
@@ -0,0 +1 @@
+export default { cacheDir: '/tmp/cohort-vite-cache' };
diff --git a/tests/unit/tools/d569-second-family-manifest.test.ts b/tests/unit/tools/d569-second-family-manifest.test.ts
index 93df1172cfc29b979c29b130d3212f9fc884e249..8c92f313bf4ccbcebbcf59e7d5c0dc8464e5b895
--- a/tests/unit/tools/d569-second-family-manifest.test.ts
+++ b/tests/unit/tools/d569-second-family-manifest.test.ts
@@ -144,6 +144,7 @@
     (path) => {
       const frozen = room(path);
       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.readText(path))).toEqual([]);
+      // D635/D642/D643: 6207 keeps historical integrity/static checks; 6211 v2 owns live productivity.
       expect(brutalRoomMembershipViolations(frozen)
         .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
     },
diff --git a/tests/unit/vtt/arena-basis-brutal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index 3613a2eb3c8585746b048672e2aea6e388c0d940..985044985b1f777b22a885bed13b92223ef35068
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -190,7 +190,9 @@
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
     if (!resolution.valid) {
-      throw new Error(`Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`);
+      throw new Error(
+        `Dry-run plan was refused for ${actorId}: ${resolution.refusals.map((entry) => entry.code).join(', ')}.`,
+      );
     }
     return [actorId, {
       proposal,
@@ -375,22 +377,8 @@
   expect(reduced.state.revision, `seed ${String(seed)} reducer load`).toBe(state.revision + 1);
   expect(reduced.state.initiative, `seed ${String(seed)} initiative load`).toHaveLength(state.combatants.length);
 
-  const planningState = freshMonsterPlanningState(state);
-  for (const monster of planningState.combatants.filter((combatant) =>
-    combatant.profile.kind === 'monster')) {
-    const productive = availableEngineActorOptions(
-      planningState,
-      monster.profile.id,
-      OFFER_ENVIRONMENT,
-    ).some((option) => {
-      const resolution = resolveEngineActorOption(planningState, option, OFFER_ENVIRONMENT);
-      return resolution.valid && (resolution.mechanics.movementCostFeet > 0 ||
-        resolution.mechanics.actionSlots.some((slot) =>
-          slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
-          slot.kind === 'use_world_object'));
-    });
-    expect(productive, `${monster.profile.id} has no productive first-turn offer`).toBe(true);
-  }
+  // D635/D642/D643: 6206 keeps structural, reducer, SHA, and round-execution history;
+  // 6209/6210/6211 own the live productivity contract under the sealed-wall geometry.
 
   // The byte pin lives with, and cannot pass instead of, the state-derived membership checks above.
   expect(sha256(fixtureBytes), `seed ${String(seed)} fixture bytes`).toBe(expectedDigest);
diff --git a/tests/unit/vtt/arena-basis-brutal-v2.test.ts b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..eb57122d1334b8da5cd6289cae5ad2fdbd71ed1e
--- /dev/null
+++ b/tests/unit/vtt/arena-basis-brutal-v2.test.ts
@@ -0,0 +1,220 @@
+import { describe, expect, it } from 'vitest';
+import { monsterCombatantProfile } from '../../../src/combat/combatant';
+import { traceCombatantLine } from '../../../src/combat/cover';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
+import { feet, type CombatantId } from '../../../src/combat/values';
+import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import {
+  availableEngineActorOptions,
+  resolveEngineActorOption,
+  type ResolvedActionSlotUse,
+  type ResolvedTurnMechanics,
+} from '../../../src/vtt/intent-resolver';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
+import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
+import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import type { GeneratedRoom } from '../../../src/vtt/room-generator';
+import {
+  brutalRoomMembershipViolations,
+  generatedRoomIntegrityViolations,
+} from '../../../tools/d569-second-family-manifest';
+import { declareTestInputs } from '../../helpers/test-inputs';
+import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
+
+const ACTIVE_FIXTURES = [
+  { family: 'brutal-v2', seed: 6_209_001, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209001.json' },
+  { family: 'brutal-v2', seed: 6_209_002, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209002.json' },
+  { family: 'brutal-v2', seed: 6_209_003, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209003.json' },
+  { family: 'brutal-v2', seed: 6_209_004, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209004.json' },
+  { family: 'brutal-v2', seed: 6_209_005, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209005.json' },
+  { family: 'brutal-v2', seed: 6_209_006, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209006.json' },
+  { family: 'brutal-v2', seed: 6_209_007, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209007.json' },
+  { family: 'brutal-v2', seed: 6_209_008, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209008.json' },
+  { family: 'brutal-v2', seed: 6_209_009, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209009.json' },
+  { family: 'brutal-v2', seed: 6_209_010, path: 'tests/fixtures/arena-basis-brutal-v2/seed-6209010.json' },
+  { family: 'brutal-b-v2', seed: 6_210_001, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210001.json' },
+  { family: 'brutal-b-v2', seed: 6_210_002, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210002.json' },
+  { family: 'brutal-b-v2', seed: 6_210_003, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210003.json' },
+  { family: 'brutal-b-v2', seed: 6_210_004, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210004.json' },
+  { family: 'brutal-b-v2', seed: 6_210_005, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210005.json' },
+  { family: 'brutal-b-v2', seed: 6_210_006, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210006.json' },
+  { family: 'brutal-b-v2', seed: 6_210_007, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210007.json' },
+  { family: 'brutal-b-v2', seed: 6_210_008, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210008.json' },
+  { family: 'brutal-b-v2', seed: 6_210_009, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210009.json' },
+  { family: 'brutal-b-v2', seed: 6_210_010, path: 'tests/fixtures/arena-basis-brutal-b-v2/seed-6210010.json' },
+  { family: 'brutal-2-v2', seed: 6_211_001, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211001.json' },
+  { family: 'brutal-2-v2', seed: 6_211_002, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211002.json' },
+  { family: 'brutal-2-v2', seed: 6_211_003, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211003.json' },
+  { family: 'brutal-2-v2', seed: 6_211_004, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211004.json' },
+  { family: 'brutal-2-v2', seed: 6_211_005, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211005.json' },
+  { family: 'brutal-2-v2', seed: 6_211_006, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211006.json' },
+  { family: 'brutal-2-v2', seed: 6_211_007, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211007.json' },
+  { family: 'brutal-2-v2', seed: 6_211_008, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211008.json' },
+  { family: 'brutal-2-v2', seed: 6_211_009, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211009.json' },
+  { family: 'brutal-2-v2', seed: 6_211_010, path: 'tests/fixtures/arena-basis-brutal-2-v2/seed-6211010.json' },
+] as const;
+
+const inputs = declareTestInputs({ fixtures: ACTIVE_FIXTURES.map(({ path }) => path) });
+const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
+
+function targetedOffenseHasOpenTrace(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+  slot: ResolvedActionSlotUse,
+): boolean {
+  if (slot.kind !== 'attack' && slot.kind !== 'saving_throw' && slot.kind !== 'cast_spell') {
+    return true;
+  }
+  return slot.targetIds.every((targetId) => {
+    const trace = traceCombatantLine(state, mechanics.actorId, targetId, {
+      sourceAnchor: mechanics.finalPosition,
+    });
+    return !trace.blocksSight && trace.tier !== 'total';
+  });
+}
+
+function resolvedMechanicsIsProductive(
+  state: EncounterState,
+  mechanics: ResolvedTurnMechanics,
+): boolean {
+  const hasProductiveComponent = mechanics.movementCostFeet > 0 ||
+    mechanics.actionSlots.some((slot) =>
+      slot.kind === 'attack' || slot.kind === 'saving_throw' || slot.kind === 'cast_spell' ||
+      slot.kind === 'use_world_object');
+  if (!hasProductiveComponent) return false;
+
+  // D635/D642/D643: live cohorts additionally require every targeted offensive component
+  // to remain sight-clear and non-total at its resolved origin. room-generator.ts:1227's
+  // generated-room predicate is intentionally stricter: it requires no cover at all.
+  return mechanics.actionSlots.every((slot) => targetedOffenseHasOpenTrace(state, mechanics, slot));
+}
+
+function monsterHasProductiveOption(state: EncounterState, actorId: CombatantId): boolean {
+  return availableEngineActorOptions(state, actorId, OFFER_ENVIRONMENT).some((option) => {
+    const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+    return resolution.valid && resolvedMechanicsIsProductive(state, resolution.mechanics);
+  });
+}
+
+function everyLivingMonsterIsProductive(state: EncounterState): boolean {
+  const planningState = freshMonsterPlanningState(state);
+  return planningState.combatants
+    .filter((combatant) => combatant.profile.kind === 'monster' && combatant.life === 'living')
+    .every((monster) => monsterHasProductiveOption(planningState, monster.profile.id));
+}
+
+function decodedClone(state: EncounterState): EncounterState {
+  return decodeEncounterStateV1(JSON.parse(JSON.stringify(state)) as unknown, 'session');
+}
+
+function actionlessMonsterState(speed: 0 | 30): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+} {
+  const base = monsterProfile(`active-productivity-${String(speed)}`);
+  const actor = { ...base, rules: { ...base.rules, speed: feet(speed) } };
+  const target = playerProfile(`active-productivity-target-${String(speed)}`);
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 20, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 15, 1)],
+    }))),
+    actorId: actor.id,
+  };
+}
+
+function openWebState(): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const base = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:active-productivity-spider',
+    tokenId: 'token:active-productivity-spider',
+  });
+  const actor = { ...base, rules: { ...base.rules, speed: feet(0) } };
+  const target = playerProfile('active-productivity-web-target');
+  return {
+    state: freshMonsterPlanningState(decodedClone(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+    }))),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+describe('active brutal v2 arena membership', () => {
+  it('reserves exactly three contiguous ten-seed fixture families', () => {
+    expect(ACTIVE_FIXTURES).toHaveLength(30);
+    expect(ACTIVE_FIXTURES.map(({ seed }) => seed)).toEqual([
+      ...Array.from({ length: 10 }, (_unused, index) => 6_209_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_210_001 + index),
+      ...Array.from({ length: 10 }, (_unused, index) => 6_211_001 + index),
+    ]);
+    expect(new Set(ACTIVE_FIXTURES.map(({ path }) => path))).toHaveLength(30);
+  });
+
+  it.each(ACTIVE_FIXTURES)(
+    '$family seed $seed satisfies active state-derived membership at $path',
+    ({ path, seed }) => {
+      const fixtureBytes = inputs.fixtures.readText(path);
+      const room = JSON.parse(fixtureBytes) as GeneratedRoom;
+      const decoded = decodeArenaFixtureText(fixtureBytes);
+
+      expect(room.spec.seed).toBe(seed);
+      expect(generatedRoomIntegrityViolations(room, fixtureBytes)).toEqual([]);
+      expect(brutalRoomMembershipViolations(room)
+        .filter((violation) => violation.code !== 'brutal_productivity')).toEqual([]);
+      expect(everyLivingMonsterIsProductive(decoded)).toBe(true);
+    },
+  );
+
+  it('rejects a living monster whose only valid options are nonproductive', () => {
+    const fixture = actionlessMonsterState(0);
+    expect(fixture.state.combatants.find((entry) => entry.profile.id === fixture.actorId)?.life)
+      .toBe('living');
+    expect(availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map(({ label }) => label)).toEqual(['Dodge', 'End Turn']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(false);
+  });
+
+  it('keeps a positive-movement-only Dash monster productive', () => {
+    const fixture = actionlessMonsterState(30);
+    const resolved = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .map((option) => ({ option, resolution: resolveEngineActorOption(
+        fixture.state,
+        option,
+        OFFER_ENVIRONMENT,
+      ) }));
+    const dash = resolved.find(({ option }) => option.label === 'Dash');
+    if (dash === undefined || !dash.resolution.valid) throw new Error('Dash witness did not resolve.');
+
+    expect(dash.resolution.mechanics.movementCostFeet).toBeGreaterThan(0);
+    expect(dash.resolution.mechanics.actionSlots.map(({ kind }) => kind)).toEqual(['dash']);
+    expect(monsterHasProductiveOption(fixture.state, fixture.actorId)).toBe(true);
+  });
+
+  it('rejects targeted offense whose resolved origin has an optically blocked trace', () => {
+    const fixture = openWebState();
+    const web = availableEngineActorOptions(fixture.state, fixture.actorId, OFFER_ENVIRONMENT)
+      .find((option) => option.actionSlots.some((slot) =>
+        slot.use.kind === 'saving_throw' && slot.use.actionId === 'web'));
+    if (web === undefined) throw new Error('Blocked-target witness omitted Web.');
+    const resolution = resolveEngineActorOption(fixture.state, web, OFFER_ENVIRONMENT);
+    if (!resolution.valid) throw new Error('Blocked-target witness could not resolve Web.');
+
+    const blocked = decodedClone({
+      ...fixture.state,
+      blockedCells: Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row })),
+    });
+    expect(traceCombatantLine(blocked, fixture.actorId, fixture.targetId, {
+      sourceAnchor: resolution.mechanics.finalPosition,
+    })).toMatchObject({ tier: 'half', blocksSight: true });
+    expect(resolvedMechanicsIsProductive(fixture.state, resolution.mechanics)).toBe(true);
+    expect(resolvedMechanicsIsProductive(blocked, resolution.mechanics)).toBe(false);
+  });
+});
diff --git a/tests/unit/vtt/room-generator.test.ts b/tests/unit/vtt/room-generator.test.ts
index b6eae9ad4c14b786c361b76d4407dd4fe167d391..267bbde7bbc0b4214655d27bd3dc35976f8b3f47
--- a/tests/unit/vtt/room-generator.test.ts
+++ b/tests/unit/vtt/room-generator.test.ts
@@ -18,18 +18,10 @@
   ROOM_GRID_DIMENSIONS,
   type GeneratedRoom,
 } from '../../../src/vtt/room-generator';
-import {
-  brutalRoomMembershipViolations,
-  hardRoomMembershipViolations,
-} from '../../../tools/d569-second-family-manifest';
+import { hardRoomMembershipViolations } from '../../../tools/d569-second-family-manifest';
 import { loadExternalPartyPackBytes } from '../../../src/vtt/party-pack';
-import { availableEngineActorOptions, resolveEngineActorOption } from '../../../src/vtt/intent-resolver';
-import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
-import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { declareTestInputs } from '../../helpers/test-inputs';
 
-const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
-
 const BASIS_SEEDS = [
   3_943_001,
   3_943_002,
@@ -75,11 +67,6 @@
   6_203_002: '8a7738bd2606792bae65da8f39f709ffad5b0bb5487f8e5749cecac4f29f9860',
   6_203_003: 'd54761b2864fcdc1a777f645ade96279da751b3247bca1e042fb997326dc060f',
 } as const;
-const BRUTAL_PRODUCTIVITY_SEEDS = [
-  6_203_001, 6_203_002, 6_203_003, 6_203_004, 6_203_005,
-  6_203_006, 6_203_007, 6_203_008, 6_203_009, 6_203_010,
-] as const;
-
 const D466_6204_PRE_OVERRIDE_ROSTER_DIGESTS = {
   6_204_001: 'df493054bdfcbc09c63a9930b9299f41d0c9558be828f2b76bee03ac95d9073c',
   6_204_002: '0b83417d94a2741bf28865ac9b4b09de44e3d82eee718109e09f2fd4cb4c5b14',
@@ -585,20 +572,8 @@
     }
   });
 
-  it.each(BRUTAL_PRODUCTIVITY_SEEDS)(
-    'generates brutal seed %s with executable casters, dense terrain, scaled pressure, and productive monsters',
-    (seed) => {
-      const brutal = generateRoom(seed, { difficulty: 'brutal' });
-      expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
-      const state = freshMonsterPlanningState(brutal.encounter.state);
-      const monsters = state.combatants.filter((candidate) => candidate.profile.kind === 'monster');
-      for (const monster of monsters) {
-        const productive = availableEngineActorOptions(state, monster.profile.id, OFFER_ENVIRONMENT)
-          .some((option) => resolveEngineActorOption(state, option, OFFER_ENVIRONMENT).valid);
-        expect(productive, monster.profile.id).toBe(true);
-      }
-    },
-  );
+  // D635/D642/D643: 6203 keeps its historical byte/digest pins above; 6209/6210/6211
+  // own the live productivity contract under the sealed-wall geometry.
 
   it.each([
     [3, 900],

tokens used
210,503
Implemented Batch 2 only.

Changed:

- Added [arena-basis-brutal-v2.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-v2.test.ts) with 30 exact v2 fixture witnesses and three productivity mutant witnesses.
- Retired only frozen 6203 productivity assertions in [room-generator.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/room-generator.test.ts).
- Retired only frozen 6206 productivity assertions in [arena-basis-brutal-b.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/vtt/arena-basis-brutal-b.test.ts).
- Added the requested 6207/6211 D635/D642/D643 rationale comment in [d569-second-family-manifest.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-cohort-01/tests/unit/tools/d569-second-family-manifest.test.ts).
- Generated no fixtures and did not begin Batch 3 or Batch 6.

Verification:

- Frozen plan: SHA-256 `4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69`, 403 lines.
- Namespace audits matched exactly, statuses `0/1/0`.
- RED-first command:
  `npx vitest run tests/unit/vtt/arena-basis-brutal-v2.test.ts --maxWorkers=1 --no-file-parallelism --configLoader runner`
  → `30 failed / 4 passed`; all 30 failures were `ENOENT` for the declared missing v2 fixtures.
- Frozen suites:
  - second-family: `50/50` passed.
  - room-generator: `56/56` passed.
  - brutal-b: `47/47` passed.
  - No frozen productivity RED remains.
- Mutants:
  - Force productivity true: nonproductive witness failed `1/1`.
  - Delete movement arm: Dash-only witness failed `1/1`.
  - Treat `blocksSight:true` as open: blocked-target witness failed `1/1`.
- Deferred RED inventory command covered 91 tests: `82 passed / 9 failed`.
- `npx tsc -p tsconfig.app.json --noEmit` → exit 0.
- `npx tsc -p tsconfig.node.json --noEmit` → exit 0.
- `git diff --check` → exit 0.
- Lines over 120 characters → 0.
- V-freeze before and after → exit 0; all 32 SHA-256 values identical.
- Final status contains exactly:
  - `tests/unit/tools/d569-second-family-manifest.test.ts`
  - `tests/unit/vtt/arena-basis-brutal-b.test.ts`
  - `tests/unit/vtt/room-generator.test.ts`
  - `tests/unit/vtt/arena-basis-brutal-v2.test.ts`

Remaining RED titles:

- Active v2 title template: `'<family>' seed <seed> satisfies active state-derived membership at '<path>'`.
  - `brutal-v2`: 6209001–6209010.
  - `brutal-b-v2`: 6210001–6210010.
  - `brutal-2-v2`: 6211001–6211010.
  - Cause for each: declared cohort fixture is missing, as required before Batches 3–5.

- `validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment`
  - Cause: historical helper produces 40 second-family regeneration mismatches.
- `dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows`
  - Cause: `second_family_first_regeneration`.
- `adds exactly 180 separately labeled advice-assisted hint cells, 660 total`
  - Cause: `second_family_first_regeneration`.
- `consumes the frozen second encounter family for all 480 core cells`
  - Cause: `second_family_first_regeneration`.
- `records exact model effort cli and cliVersion for every observed row`
  - Cause: `second_family_first_regeneration`.
- `rejects either decisive integrity field alone while preserving rows with neither field`
  - Cause: `second_family_first_regeneration`.

- `projects fresh options for every requested generated-room monster turn`
  - Cause: D642 now yields `Dodge, End Turn` instead of the stale Longbow expectation; deferred to Batch 9.
- `does not repeat the frozen Room-8 four-actor zero-feet Dash plan`
  - Cause: the frozen Room-8 plan differs under sealed-wall geometry; deferred to Batch 9.
- `forms a bounded volatility-ranked menu from baseline dependencies and canonical movement proofs`
  - Cause: the frozen speculative scene now produces zero candidates; deferred to Batch 9.

COHORT-01 B2 DONE
