
OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-vis-field
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR] (network access enabled)
reasoning effort: high
reasoning summaries: none
session id: 01a0b20e-61fa-7e52-897b-3b01b026c2ff
--------
user
RULES (binding, restated): You are the IMPLEMENTER (gpt-5.6-sol, high, RESUMED — WALL-01 fix r2/r3/r4). Worktree /home/vagrant/PhpstormProjects/dnd-wt-vis-field (claude/vis-field at bc6422bf + your uncommitted r4 edits to src/combat/cover.ts and tests/unit/combat/visibility-field.test.ts). Workspace-write in that worktree only; no git writes; no .claude/** or docs/**; no claude / other agents / consensus skills; no Playwright / full vitest / build; never port 4173; every expectation hand-derived with the geometry in a comment; D569 for any scene pin that moves; never fresh hashes. The MAIN repo's .claude/decisions.md D654–D657 is readable.

FINDING WALL-F4 (supervisor, verified on the clean checkout + your r4 patch; it is a GAP IN MY r4 RULE STATEMENT, not your implementation): my probe D — the 6×6 wall (blockers column 1 rows 0–1, column 2 rows 2–5), observer (2,1) [TR of junction (2,2)], target (1,2) [BL of the junction] — returns AGGREGATE blocksSight FALSE (tier none) although the two cells sit in opposite free quadrants of a sealed junction and no interior point of the target is reachable by a straight line from the observer. Your ADJACENT_CORNER_ZERO_LENGTH witness passes because it asserts per-ray results; the aggregate leaks through two EDGE-ALIGNED corner rays that END at the junction: (2,1)→(2,2) (down x=2 between blocker (1,1) and the observer's own free cell — a graze under the shared-edge rule) and (3,2)→(2,2) (left along y=2 between the observer's cell and blocker (2,2)). Each "reaches the target's corner", but that corner is a wall corner: under the solid-wall model a ray that only touches the pinch point from the far side sees nothing of the target. My r4 rule sent edge-aligned endpoint rays to the shared-edge rule; that was wrong for rays ENDING (or starting) at a sealed junction.

THE RULE, REFINED (D657): at an endpoint vertex v with a sealed diagonal pair, let Q be the (free) quadrant of the endpoint's own cell. The ray is CLEAR at that endpoint iff its OTHER endpoint lies in the CLOSED quadrant Q (Q's interior or either of Q's two boundary half-lines from v); otherwise the ray passes the pinch and is BLOCKED (its far end is in the opposite free quadrant or on the wall's far faces). This subsumes r4's diagonal test (a far endpoint strictly in the opposite quadrant is outside closed Q) and adds the edge-aligned arrivals/departures. Check it keeps the same-side cases clear: (2,1)→(5,1): rays from (2,2) to (5,1),(6,1),(5,2),(6,2) — all in closed TR → clear; a ray from v up along x=2 to (2,0) is on TR's boundary → clear (graze along the own cell's edge); a ray from v to (0,2) along y=2 heading left is on TL/BL boundary, not in closed TR → blocked (it runs along the wall's far face into the opposite side) — state and pin that too. Interior-vertex, shared-edge, cell-interior rules unchanged; physical cover unchanged; same path in coverTierBetweenObjects.

WITNESSES: ADJACENT_CORNER_AGGREGATE_BLOCKED = probe D through traceTerrainLine (Medium tokens): blocksSight true (first = the pair pick); EDGE_ARRIVAL_AT_JUNCTION_BLOCKED = the per-ray (2,1)→(2,2) and (3,2)→(2,2) cases: blocked; OWN_EDGE_DEPARTURE_CLEAR = (2,2)→(2,0) style graze along the own cell's edge: clear; FAR_FACE_DEPARTURE_BLOCKED = (2,2)→(0,2): blocked; keep every r4 witness. MUTANTS: the refined test replaced by r4's diagonal-only test → ADJACENT_CORNER_AGGREGATE_BLOCKED and EDGE_ARRIVAL red; endpoint rule blocking everything → OWN_EDGE_DEPARTURE_CLEAR and SAME_SIDE red; physical tier raised → PHYSICAL_COVER pin red. Then re-run: visibility-field, the r3 scene suites (scene-snapshot, ai-dm-conversation, dm-tactical-intel, tactical-evaluator-r02), two-room-fixture, engine-query-port, room-generator, brutal-b, the three behaviour suites, d569 ×2, screenshot-probe, los-cover, replay+detection (pair ≤41 s); any scene pin that moves → derive (which junction, which edge-aligned ray) and normalize under D569 or stop; REDO the frozen-family counts (before bc6422bf / after) and the 42-seed generation byte check — STOP if generation bytes change. VERIFICATION: tsc app/node; git diff --check; no line > 120; git status --short. OUTPUT: the rule as implemented, per-witness derivations, mutants, counts, byte check, red set by title; then "WALL-01-FIX-R5 DONE" (or BLOCKED).

codex
I’ll refine only the endpoint-direction predicate, add the aggregate and edge-aligned witnesses red-first, and preserve the existing endpoint, interior, physical-cover, and r3 scene pins. I’ll stop immediately if the 42 generated-room bytes change.
exec
/bin/bash -lc "sed -n '/"'^## D654'"\\b/,/"'^## D658'"\\b/p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
sed -n '390,570p' src/combat/cover.ts
sed -n '1090,1390p' tests/unit/combat/visibility-field.test.ts
git status --short" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
## D654 — SUPERVISOR (2026-09-17 22:23): WALL-01 REJECTED by astra r1 (medium; 1 P1: WALL-F3; saved review-impl-wall-01-astra-r1.md) — CONFIRMED BY ME; the defect is MY SPEC: the WALL-01 brief dictated "endpoint vertices are NOT vertex-checked"; fix r4 dispatched
ASTRA (2.49 M tokens): geometry otherwise sound (single-blocker touch and outer-face graze clear; shared edges block; interior diagonal pairs block including TL+BR with a TR→BL ray; three/four-blocker vertices covered; Fraction enumeration matched the gcd enumeration for 600 ordered integer endpoint pairs; ordering/tie-break explicit; physical tier independent — its "total whenever firstBlockingCell" mutant killed); r3 derivations reproduced (fog (4,3) → (4,3),(8,3),(9,3); fighter +(8,2),(9,2),(8,3),(9,3); all three hash transitions reproduced by substituting baseline vs candidate cover.ts in memory; only visibleState.cells/concealedCells changed; fingerprint dd802783…; the three replacement blockers have zero diagonal neighbours; five opened pairs / Half + Three-Quarters / both 90-ft shots asserted); six files 387+/49−; tsc 0/0; 66/66 across the suites it could run; perf: the productive-option los-cover test 4,935 ms baseline → 5,740 ms candidate (gcd + up to four lookups per interior vertex) — deferred perf work, not blocking. WALL-F3 (P1): a COMPLETE stepped wall (6×6; blockers col 1 rows 0–1, col 2 rows 2–5; junction (2,2) between (1,1) and (2,2)) leaks: of the 16 corner rays from observer (2,1) to target (0,3), 10 hit interiors, 2 hit the sealed vertex strictly inside, and 4 START at the sealed junction (the observer's bottom-left corner) and pass through the pinch — the endpoint exclusion lets them through; production says visible/seen. VERIFIED BY ME on seam-ab at bc6422bf: (2,1)→(0,3) tier none / blocksSight FALSE; reversed half / FALSE; same-side (2,1)→(5,1) clear (correct). ROOT CAUSE = my WALL-01 brief: "Endpoint vertices (t = 0 or 1) are NOT vertex-checked — the cell-interior rule governs there"; sol implemented what I specified. Finding against my own work, at full length: I specified a geometric rule from intuition without enumerating the endpoint case; astra's rational enumeration did. RULE (D654): at an endpoint vertex with a sealed pair, the ray blocks iff it passes through the pinch — leaves the source corner into the open quadrant diagonally opposite the source cell's quadrant (or, mirrored, arrives at the target corner from the opposite quadrant); edge-aligned departures fall to the shared-edge rule; same-side departures stay clear; zero-length corner contact across a sealed junction = blocked. Fix r4 dispatched (resume 01a0b20e-61fa…; brief impl-vis-field-wall-01-fix-r4.md): witnesses for source/target endpoint, same-side control, adjacent-corner zero-length, three mutants; re-run of every scene suite with D569 handling if pins move; counts and generation bytes redone — if generation bytes change, STOP (it would alter the cohort plan's premise).

## D655 — SUPERVISOR (2026-09-17 22:51): COHORT-01 B2 VERIFIED BY ME and committed 8284a91c on claude/cohort-01; astra medium review dispatched (resumed B1 reviewer)
VERIFIED (verify-b2.log, clean checkout at e92ebc93 + patch): tsc 0/0; diff-check 0; 0 long lines; V-freeze 0 and no fixture files touched; the new v2 suite 30 red, every failure ENOENT on the declared missing fixture; room-generator 56/56; brutal-b 47/47; second-family 50/50. Read by me: the removed code in both frozen suites is exactly the per-monster productivity loop, replaced by the D635/D642/D643 rationale. Generation (Batches 3–5) still gated on WALL-01 (fix r4 in flight).

## D656 — SUPERVISOR (2026-09-17 23:07): COHORT-01 B2 REJECTED by astra r1 (medium; 0 P1, 1 P2: B2-F1; saved review-b2-astra-r1.md) → fix r1 dispatched; WALL-01 fix r4 harvested (sol; saved impl-wall-01-fix-r4-sol.md) → my verification running
B2-F1 (P2): the 6203 retirement removed the whole brutalRoomMembershipViolations test (difficulty, scaled budget/spend, caster, terrain AND productivity) instead of only the productivity loop; astra's in-memory mutant (budget +1 on 6203010) leaves the suite 56/56 green; the fix restores the ten-seed static assertion filtering only brutal_productivity. Everything else accepted: 6206 retirement exact; 30 v2 declarations through declareTestInputs; PLAN-F6 predicate with the query's own traceCombatantLine at mechanics.finalPosition; membership after decoding with no swallow (an in-memory bad-budget fixture fails with brutal_budget); three mutants killed; tsc 0/0; freeze 0; 229 added lines, 0 over 120. Fix r1 dispatched (resume 01a0b245…).
WALL-01 fix r4 (sol, resume 01a0b20e-61fa…; 2 files 286+/26−): endpoint-junction rule — at a sealed vertex a source ray blocks only when leaving diagonally into the quadrant opposite the source cell (TL→BR, TR→BL, BL→TR, BR→TL ownership), target rays mirrored, axis-aligned rays to the shared-edge rule, zero-length corner contact across opposite free quadrants blocked, progress 0/1, physical cover untouched, same path in coverTierBetweenObjects; witnesses SOURCE/TARGET_ENDPOINT_JUNCTION_SEALED (astra's scene), SAME_SIDE_ENDPOINT_GRAZE_CLEAR, ADJACENT_CORNER_ZERO_LENGTH; DIAGONAL_CORNER_TOUCH_SEALED_D642 re-derived (its source corner (1,1) had been an excluded endpoint — now aggregate blocked, first (1,0), tier none); three mutants killed; visibility-field 50/50; r3 scenes 5/5; ten-suite audit 246 passed + exactly the 35 known frozen reds; counts unchanged (hard 142/35, brutal 57/34, brutal-b 15/41, brutal-2 58/36); 42 generated rooms byte-identical (7b884bf9…); pair 39.4 s; los-cover budget tests 5.6 s isolated (perf). MY VERIFICATION (verify-wall-01-r4.log, seam-ab at bc6422bf + patch): tsc, six-case probe (A/B endpoint must block; C same-side clear; D adjacent corner blocked; E interior; F stepped wall), endpoint-rule-null mutant, 15 suites, los-cover, pair.
    dividend = divisor;
    divisor = remainder;
  }
  return dividend;
}

function sharedVertexBlockingCell(
  column: number,
  row: number,
  sightBlockingAt: (cell: GridCell) => boolean,
): GridCell | null {
  const topLeft = { column: column - 1, row: row - 1 };
  const topRight = { column, row: row - 1 };
  // Check TL/BR first so lower row then lower column wins when both pairs seal.
  if (sightBlockingAt(topLeft) && sightBlockingAt({ column, row })) return topLeft;
  if (sightBlockingAt(topRight) && sightBlockingAt({ column: column - 1, row })) return topRight;
  return null;
}

function endpointSharedVertexBlockingCell(
  column: number,
  row: number,
  ownCellQuadrant: CornerQuadrant,
  sightBlockingAt: (cell: GridCell) => boolean,
): GridCell | null {
  // The endpoint's own cell is excluded from blocking, so only the other diagonal pair can seal.
  if (ownCellQuadrant.column === ownCellQuadrant.row) {
    const topRight = { column, row: row - 1 };
    return sightBlockingAt(topRight) && sightBlockingAt({ column: column - 1, row })
      ? topRight
      : null;
  }
  const topLeft = { column: column - 1, row: row - 1 };
  return sightBlockingAt(topLeft) && sightBlockingAt({ column, row }) ? topLeft : null;
}

function resolveCornerEndpoints(
  endpoints: readonly CornerEndpoint[],
  sightBlockingAt: (cell: GridCell) => boolean,
): readonly ResolvedCornerEndpoint[] {
  return endpoints.map((endpoint) => ({
    ...endpoint,
    blockingCell: endpointSharedVertexBlockingCell(
      endpoint.point.column,
      endpoint.point.row,
      endpoint.cellQuadrant,
      sightBlockingAt,
    ),
  }));
}

function firstSharedVertexBlockingHit(
  from: CornerPoint,
  to: CornerPoint,
  sightBlockingAt: (cell: GridCell) => boolean,
): SightBlockingHit | null {
  if (![from.column, from.row, to.column, to.row].every(Number.isInteger)) return null;
  const columnDelta = to.column - from.column;
  const rowDelta = to.row - from.row;
  const latticeSteps = greatestCommonDivisor(columnDelta, rowDelta);
  if (latticeSteps <= 1) return null;
  const columnStep = columnDelta / latticeSteps;
  const rowStep = rowDelta / latticeSteps;
  // Strict-interior vertices exclude t=0 and t=1; the direction-aware endpoint rule handles those separately.
  for (let index = 1; index < latticeSteps; index += 1) {
    const column = from.column + columnStep * index;
    const row = from.row + rowStep * index;
    const cell = sharedVertexBlockingCell(column, row, sightBlockingAt);
    if (cell !== null) return { cell, progress: index / latticeSteps };
  }
  return null;
}

function entersOppositeQuadrant(
  columnDelta: number,
  rowDelta: number,
  ownCellQuadrant: CornerQuadrant,
): boolean {
  if (columnDelta === 0 || rowDelta === 0) return false;
  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
    Math.sign(rowDelta) === -ownCellQuadrant.row;
}

function endpointSharedVertexBlockingHit(
  from: CornerPoint,
  to: CornerPoint,
  sourceCellQuadrant: CornerQuadrant,
  targetCellQuadrant: CornerQuadrant,
  sourceBlockingCell: GridCell | null,
  targetBlockingCell: GridCell | null,
): SightBlockingHit | null {
  if (![from.column, from.row, to.column, to.row].every(Number.isInteger)) return null;
  const columnDelta = to.column - from.column;
  const rowDelta = to.row - from.row;
  if (columnDelta === 0 && rowDelta === 0) {
    const oppositeSpaces = sourceCellQuadrant.column === -targetCellQuadrant.column &&
      sourceCellQuadrant.row === -targetCellQuadrant.row;
    return sourceBlockingCell !== null && oppositeSpaces
      ? { cell: sourceBlockingCell, progress: 0 }
      : null;
  }
  if (sourceBlockingCell !== null &&
    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
    return { cell: sourceBlockingCell, progress: 0 };
  }
  if (targetBlockingCell !== null &&
    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
    return { cell: targetBlockingCell, progress: 1 };
  }
  return null;
}

function cellInteriorEntryProgress(from: CornerPoint, to: CornerPoint, cell: GridCell): number {
  let entry = 0;
  const columnDelta = to.column - from.column;
  if (columnDelta !== 0) {
    entry = Math.max(entry, Math.min(
      (cell.column - from.column) / columnDelta,
      (cell.column + 1 - from.column) / columnDelta,
    ));
  }
  const rowDelta = to.row - from.row;
  if (rowDelta !== 0) {
    entry = Math.max(entry, Math.min(
      (cell.row - from.row) / rowDelta,
      (cell.row + 1 - from.row) / rowDelta,
    ));
  }
  return entry;
}

function firstSightBlockingCell(
  from: CornerPoint,
  to: CornerPoint,
  sourceCellQuadrant: CornerQuadrant,
  targetCellQuadrant: CornerQuadrant,
  sourceEndpointBlockingCell: GridCell | null,
  targetEndpointBlockingCell: GridCell | null,
  interveningCells: readonly GridCell[],
  sightBlockingAt: (cell: GridCell) => boolean,
): GridCell | null {
  let first = firstSharedEdgeBlockingHit(from, to, sightBlockingAt);
  const endpoint = endpointSharedVertexBlockingHit(
    from,
    to,
    sourceCellQuadrant,
    targetCellQuadrant,
    sourceEndpointBlockingCell,
    targetEndpointBlockingCell,
  );
  if (endpoint !== null && (first === null || endpoint.progress < first.progress ||
    (endpoint.progress === first.progress && compareCells(endpoint.cell, first.cell) < 0))) first = endpoint;
  const vertex = firstSharedVertexBlockingHit(from, to, sightBlockingAt);
  if (vertex !== null && (first === null || vertex.progress < first.progress ||
    (vertex.progress === first.progress && compareCells(vertex.cell, first.cell) < 0))) first = vertex;
  for (const cell of interveningCells) {
    if (!sightBlockingAt(cell)) continue;
    const hit = { cell, progress: cellInteriorEntryProgress(from, to, cell) };
    if (first === null || hit.progress < first.progress ||
      (hit.progress === first.progress && compareCells(hit.cell, first.cell) < 0)) first = hit;
  }
  // One traversal merges cell-interior, shared-edge, endpoint and strict-interior-vertex events by progress.
  // Simultaneous events use lower row, then lower column.
  return first === null ? null : { ...first.cell };
}

function traceCornerLine(
  from: CornerPoint,
  to: CornerPoint,
  sourceCellQuadrant: CornerQuadrant,
  targetCellQuadrant: CornerQuadrant,
  sourceEndpointBlockingCell: GridCell | null,
  targetEndpointBlockingCell: GridCell | null,
  candidates: readonly GridCell[],
  sourcesAt: (cell: GridCell) => readonly TerrainLineSource[],
  sightBlockingAt: (cell: GridCell) => boolean,
): TerrainCornerLineTrace {
  const interveningCells = rasterizeCornerLine(from, to, candidates);
  let tier: CoverTier = 'none';
  let strongest: TerrainLineSource[] = [];
  const firstBlockingCell = firstSightBlockingCell(
      line.targetCorner.column === 4 && line.targetCorner.row === 3);

    // Selected ray (0,1)->(4,3) reaches vertex (2,2): TR (2,1) blocks but BL (1,2) is open.
    expect(trace).toMatchObject({ tier: 'none', blocksSight: false, firstBlockingCell: null });
    expect(oneSidedCorner).toMatchObject({ blocksSight: false, firstBlockingCell: null });
    expect(sightToCreature(state, observer.id, subject.id)).toEqual({
      kind: 'visible',
      grade: 'seen',
      sense: 'normal_sight',
    });
  });

  it('SOURCE_ENDPOINT_JUNCTION_SEALED', () => {
    const observer = normalProfile('source-endpoint-observer');
    const blockers = [
      { column: 1, row: 0 }, { column: 1, row: 1 },
      { column: 2, row: 2 }, { column: 2, row: 3 },
      { column: 2, row: 4 }, { column: 2, row: 5 },
    ];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 0, row: 3 });

    // The observer's bottom-left corner is vertex (2,2); its cell lies TR of that vertex.
    // Ray (2,2)->(0,4) leaves into the opposite BL quadrant through the pinch sealed by
    // TL (1,1) + BR (2,2). Lower-row cell (1,1) wins; physical cover remains None.
    expect(trace).toMatchObject({
      tier: 'none',
      blocksSight: true,
      firstBlockingCell: { column: 1, row: 1 },
    });
  });

  it('TARGET_ENDPOINT_JUNCTION_SEALED', () => {
    const observer = normalProfile('target-endpoint-observer');
    const blockers = [
      { column: 1, row: 0 }, { column: 1, row: 1 },
      { column: 2, row: 2 }, { column: 2, row: 3 },
      { column: 2, row: 4 }, { column: 2, row: 5 },
    ];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
    const trace = traceTerrainLine(state, { column: 0, row: 3 }, { column: 2, row: 1 });

    // Mirrored ray (0,4)->(2,2) arrives from the BL quadrant opposite the target cell's
    // TR quadrant. TL (1,1) + BR (2,2) seal vertex (2,2); physical Half Cover is unchanged.
    expect(trace).toMatchObject({
      tier: 'half',
      blocksSight: true,
      firstBlockingCell: { column: 1, row: 1 },
    });
  });

  it('SAME_SIDE_ENDPOINT_GRAZE_CLEAR', () => {
    const observer = normalProfile('same-side-endpoint-observer');
    const blockers = [
      { column: 1, row: 0 }, { column: 1, row: 1 },
      { column: 2, row: 2 }, { column: 2, row: 3 },
      { column: 2, row: 4 }, { column: 2, row: 5 },
    ];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 5, row: 1 });
    const reverseTrace = traceTerrainLine(state, { column: 5, row: 1 }, { column: 2, row: 1 });
    const sameSideEndpoint = reverseTrace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);

    // Source corner (2,2) belongs to a cell in its TR quadrant. Rays toward (5,1) stay
    // on that same side or leave along a cell edge; neither crosses the sealed pinch.
    expect(trace).toMatchObject({ tier: 'none', blocksSight: false, firstBlockingCell: null });
    // Mirrored ray (5,1)->(2,2) arrives from the target cell's own TR quadrant, not the
    // opposite BL quadrant. Pin it directly so an indiscriminate sealed-endpoint rule fails.
    expect(sameSideEndpoint).toMatchObject({ blocksSight: false, firstBlockingCell: null });
  });

  it('ADJACENT_CORNER_ZERO_LENGTH', () => {
    const observer = normalProfile('zero-length-endpoint-observer');
    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
    const state = fieldState(observer, { bounds: { columns: 4, rows: 4 }, blockedCells: blockers });
    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
    const zeroLength = trace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);
    const diagonal = trace.lines.find((line) =>
      line.targetCorner.column === 1 && line.targetCorner.row === 3);

    // At shared corner (2,2), the source cell is TR and target cell BL: opposite free
    // quadrants around the TL (1,1) + BR (2,2) pair. Their zero-length corner ray is
    // blocked at the solid-wall pinch, as is the ray leaving diagonally into target-side BL.
    expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
    expect(zeroLength).toMatchObject({
      tier: 'none', blocksSight: true, firstBlockingCell: { column: 1, row: 1 },
    });
    expect(diagonal).toMatchObject({
      tier: 'none', blocksSight: true, firstBlockingCell: { column: 1, row: 1 },
    });
    expect(trace.tier).toBe('none');

    const open = { ...state, blockedCells: [{ column: 1, row: 1 }] };
    const openTrace = traceTerrainLine(open, { column: 2, row: 1 }, { column: 1, row: 2 });
    const openZeroLength = openTrace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);
    // Without BR (2,2), no sealed pair exists: the identical zero-length contact is clear.
    expect(openTrace.sourceCorner).toEqual({ column: 2, row: 2 });
    expect(openZeroLength).toMatchObject({ blocksSight: false, firstBlockingCell: null });
    expect(openTrace.blocksSight).toBe(false);
  });

  it('STAIRCASE_WALL_SEALED', () => {
    const observer = normalProfile('staircase-wall-observer');
    const subject = normalProfile('staircase-wall-subject');
    const blockers = [{ column: 7, row: 3 }, { column: 8, row: 4 }];
    const state = fieldState(observer, {
      bounds: { columns: 15, rows: 8 },
      combatants: [observer, subject],
      tokens: [placedToken(observer, 13, 4), placedToken(subject, 1, 4)],
      blockedCells: blockers,
    });
    const trace = traceTerrainLine(state, { column: 13, row: 4 }, { column: 1, row: 4 });
    const stepRay = trace.lines.find((line) =>
      line.targetCorner.column === 1 && line.targetCorner.row === 4);

    // (13,4)->(1,4) touches upper cell (7,3) and lower cell (8,4) only at vertex (8,4).
    // No unit edge has two blocking flanks and neither interior is crossed; lower-row (7,3) wins.
    expect(rasterizeCornerLine({ column: 13, row: 4 }, { column: 1, row: 4 }, blockers)).toEqual([]);
    expect(stepRay).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 7, row: 3 } });
    // Pre-fix aggregate physical pin: two diagonal cells obstruct two corner rays, yielding Half Cover.
    expect(trace.tier).toBe('half');
  });

  it('STAIRCASE_MIRRORED_SEALED', () => {
    const observer = normalProfile('mirrored-staircase-observer');
    const subject = normalProfile('mirrored-staircase-subject');
    const blockers = [{ column: 7, row: 4 }, { column: 8, row: 3 }];
    const state = fieldState(observer, {
      bounds: { columns: 15, rows: 8 },
      combatants: [observer, subject],
      tokens: [placedToken(observer, 13, 4), placedToken(subject, 1, 4)],
      blockedCells: blockers,
    });
    const trace = traceTerrainLine(state, { column: 13, row: 4 }, { column: 1, row: 4 });
    const stepRay = trace.lines.find((line) =>
      line.targetCorner.column === 1 && line.targetCorner.row === 4);

    // (13,4)->(1,4) reaches vertex (8,4) between TR (8,3) and BL (7,4).
    // Neither cell interior nor a doubly flanked edge is crossed; lower-row (8,3) wins.
    expect(rasterizeCornerLine({ column: 13, row: 4 }, { column: 1, row: 4 }, blockers)).toEqual([]);
    expect(stepRay).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 8, row: 3 } });
    expect(trace.tier).toBe('half');
  });

  it('VERTICAL_STEP_SEALED', () => {
    const observer = normalProfile('vertical-step-observer');
    const subject = normalProfile('vertical-step-subject');
    const blockers = [{ column: 4, row: 2 }, { column: 5, row: 3 }];
    const state = fieldState(observer, {
      bounds: { columns: 15, rows: 8 },
      combatants: [observer, subject],
      tokens: [placedToken(observer, 5, 0), placedToken(subject, 5, 6)],
      blockedCells: blockers,
    });
    const trace = traceTerrainLine(state, { column: 5, row: 0 }, { column: 5, row: 6 });
    const stepRay = trace.lines.find((line) =>
      line.targetCorner.column === 5 && line.targetCorner.row === 6);

    // (5,0)->(5,6) reaches vertex (5,3) between TL (4,2) and BR (5,3).
    // It crosses no cell interior or doubly flanked edge; lower-row (4,2) wins the pair.
    expect(rasterizeCornerLine({ column: 5, row: 0 }, { column: 5, row: 6 }, blockers)).toEqual([]);
    expect(stepRay).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 4, row: 2 } });
    expect(trace.tier).toBe('half');
  });

  it('STAIRCASE_SPLIT_BY_OPEN_CELL_STAYS_CLEAR', () => {
    const observer = normalProfile('split-staircase-observer');
    const subject = normalProfile('split-staircase-subject');
    const blockers = [{ column: 7, row: 3 }, { column: 9, row: 4 }];
    const state = fieldState(observer, {
      bounds: { columns: 15, rows: 8 },
      combatants: [observer, subject],
      tokens: [placedToken(observer, 13, 4), placedToken(subject, 1, 4)],
      blockedCells: blockers,
    });
    const trace = traceTerrainLine(state, { column: 13, row: 4 }, { column: 1, row: 4 });
    const splitRay = trace.lines.find((line) =>
      line.targetCorner.column === 1 && line.targetCorner.row === 4);

    // (7,3) and (9,4) leave (8,4) open, so no diagonal pair meets at either lattice vertex.
    // The y=4 ray only grazes one outer face at a time and therefore remains clear.
    expect(rasterizeCornerLine({ column: 13, row: 4 }, { column: 1, row: 4 }, blockers)).toEqual([]);
    expect(splitRay).toMatchObject({ blocksSight: false, firstBlockingCell: null });
    expect(trace.tier).toBe('half');
  });

  it('OUTER_FACE_GRAZE_STILL_CLEAR', () => {
    const actor = normalProfile('outer-face-graze-actor');
    const blockers = [{ column: 2, row: 2 }, { column: 3, row: 2 }];
    const state = fieldState(actor, {
      bounds: { columns: 6, rows: 4 },
      position: { column: 5, row: 3 },
      blockedCells: blockers,
    });
    const trace = traceTerrainLine(state, { column: 0, row: 2 }, { column: 4, row: 2 });
    const outerFace = trace.lines.find((line) =>
      line.targetCorner.column === 4 && line.targetCorner.row === 2);

    // (0,2)->(4,2) follows the north faces of (2,2),(3,2); every cell above y=2 is open.
    expect(rasterizeCornerLine({ column: 0, row: 2 }, { column: 4, row: 2 }, blockers)).toEqual([]);
    expect(outerFace).toMatchObject({ blocksSight: false, firstBlockingCell: null });
    expect(trace.blocksSight).toBe(false);
  });

  it('SOLID_2x2_BLOCK', () => {
    const actor = normalProfile('solid-block-actor');
    const state = fieldState(actor, {
      bounds: { columns: 5, rows: 5 },
      position: { column: 4, row: 4 },
      blockedCells: [
        { column: 1, row: 1 }, { column: 2, row: 1 },
        { column: 1, row: 2 }, { column: 2, row: 2 },
      ],
    });
    const trace = traceTerrainLine(state, { column: 0, row: 0 }, { column: 3, row: 3 });

    // Every corner ray crosses the 2x2 closed region; the existing physical result is Total Cover.
    expect(trace).toMatchObject({
      tier: 'total',
      blocksSight: true,
      firstBlockingCell: { column: 1, row: 1 },
    });
  });

  it('DIAGONAL_CORNER_TOUCH_SEALED_D642', () => {
    const observer = normalProfile('diagonal-corner-observer');
    const blockers = [{ column: 1, row: 0 }, { column: 0, row: 1 }];
    const state = fieldState(observer, {
      bounds: { columns: 4, rows: 4 },
    });
    const trace = traceTerrainLine(
      state,
      { column: 0, row: 0 },
      { column: 2, row: 2 },
      { additionalSightBlockingCells: blockers },
    );
    const sealedRay = trace.lines.find((line) =>
      line.targetCorner.column === 3 && line.targetCorner.row === 3);

    // Original pin: (0,0)->(3,3) reaches vertex (1,1) between TR (1,0) and BL (0,1).
    // D642 seals that exact sight-only ray and picks lower-row cell (1,0); physical tier stays None.
    expect(rasterizeCornerLine({ column: 0, row: 0 }, { column: 3, row: 3 }, blockers)).toEqual([]);
    expect(sealedRay).toMatchObject({
      tier: 'none',
      blocksSight: true,
      firstBlockingCell: { column: 1, row: 0 },
    });
    // D569 endpoint normalization: before D654 the aggregate was clear/null because endpoint vertex
    // (1,1) was skipped. It is the source cell's bottom-right corner, so that cell lies TL of the vertex;
    // all four rays from (1,1) to target corners (2,2)..(3,3) leave into opposite BR through the
    // TR (1,0) + BL (0,1) pinch. That last clear source-corner route is now blocked at lower-row (1,0).
    expect(trace).toMatchObject({ tier: 'none', blocksSight: true, firstBlockingCell: { column: 1, row: 0 } });
  });

  it('PHYSICAL_COVER_UNCHANGED_BY_VERTEX', () => {
    const actor = normalProfile('diagonal-cover-actor');
    const state = fieldState(actor, {
      bounds: { columns: 5, rows: 5 },
      position: { column: 4, row: 4 },
      blockedCells: [{ column: 2, row: 1 }, { column: 1, row: 2 }],
    });
    const trace = traceTerrainLine(state, { column: 0, row: 0 }, { column: 3, row: 3 });

    // Pre-D642 physical pin: interior crossings obstruct two target-corner rays, yielding Half Cover.
    expect(trace.tier).toBe('half');

    const endpointState = fieldState(actor, {
      bounds: { columns: 6, rows: 6 },
      blockedCells: [
        { column: 1, row: 0 }, { column: 1, row: 1 },
        { column: 2, row: 2 }, { column: 2, row: 3 },
        { column: 2, row: 4 }, { column: 2, row: 5 },
      ],
    });
    const endpointTrace = traceTerrainLine(endpointState, { column: 0, row: 3 }, { column: 2, row: 1 });
    const endpointLine = endpointTrace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);
    // Ray (0,4)->(2,2) is optically stopped at the TL (1,1) + BR (2,2) endpoint pinch,
    // but it crosses no cell interior; the new sight hit therefore leaves that line at None.
    expect(endpointLine).toMatchObject({ tier: 'none', blocksSight: true });
  });

  it('PHYSICAL_COVER_UNCHANGED_BY_SEAM', () => {
    const { state } = seamState(true, true);
    const trace = traceTerrainLine(state, { column: 3, row: 4 }, { column: 7, row: 4 });

    // Pre-change pin: two of four target-corner rays cross total-cover cells, producing half cover.
    expect(trace.tier).toBe('half');
  });

  it('EMPTY_SIGHT_BLOCKERS_PRESERVE_FULL_TRACE including caches and a multi-cell footprint', () => {
    const actor = normalProfile('empty-blocker-actor');
    const coverCases = [
      {
        bounds: { columns: 6, rows: 6 },
        to: { column: 4, row: 2 },
        blockedCells: [] as readonly GridCell[],
        expected: 'none',
 M src/combat/cover.ts
 M tests/unit/combat/visibility-field.test.ts

exec
/bin/bash -lc 'rg -a -n -C 12 "D657" /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md | tail -80' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
exec
/bin/bash -lc 'tail -120 /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md | LC_ALL=C strings | tail -100' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
 filter-repo rewrote refs in place, branch-attached worktrees follow their branches (identical trees), and the 16 detached worktrees were remapped through .git/filter-repo/commit-map with update-ref --no-deref HEAD; nothing uncommitted anywhere was touched. Preflight: no codex/vitest readers; dry run 4634 commits OK; refs (174) and detached HEADs snapshotted; the WALL-01 r1 candidate committed on claude/vis-field (7acb936d 
 6887755f after rewrite) and the records committed on main (0847602b 
 3be26bd8) so nothing sat uncommitted. RESULT: main tree 524f110d
 identical before and after; refs 174/174; blob occurrences 1 
 0; pack 67.6 MiB (one pack, 56,225 objects); `git push --force mirror main`: 9f148666...3be26bd8 (forced update); ls-remote main = local main. KEY SHA TRANSLATIONS (old 
 new; the full 4635-line map is committed at .claude/consensus/purge-2026-09-17-commit-map.txt): 9f148666 -> 29a7d2e2; 0847602b -> 3be26bd8; 807d2e3c -> e86a4d26; 9a220fce -> 213f9a7e; 61dd454e -> 8352aed5; ebfcacaa -> 34c564f0; 10cc9f5e -> 30403378; 5492f782 -> e143e899; 9a3235da -> 33aa146c; 78ee31b0 -> bf85fb89; dcca84fd -> a46eea99; 1d58ef72 -> 9407dfa8; 7acb936d -> 6887755f; 4a99570d -> 495d9eb2; 75c2f44a -> df694f93. Every decision written before this entry cites OLD ids; translate through the map.
## D650 
 SUPERVISOR (2026-09-17 21:08): COHORT-01 plan ACCEPTED WITH AMENDMENT by astra r3 (HIGH, final round) 
 FROZEN at sha256 2b931ed857e6019fa515b387cb7c2262e8e04a4bd3c7b8690472a7adf4f6e802 (399 lines incl. binding note N1); Batch 1 dispatched on a new worktree; WALL-01 fix r2 re-dispatched after a sol capacity outage
ASTRA r3 (saved review-plan-cohort-01-astra-r3.md): F2 closed (all three namespace commands re-run verbatim, statuses 0/1/0, 1,000/1,000 plain and underscore seeds per namespace + the xxx form); F5 closed (ten-namespace union, own-cohort exceptions, remove-any-row tests, the 6204 mutant); F6 closed (positive movement stays productive; Dash-only witness + movement-arm mutant; sight/cover only on targeted offensive components); F1/F4 closed; counts 3/3/10/10/10/8/4/4/3/1 = 52 paths. F3 resolved by AMENDMENT: with the typed zero-speed effect alone the real planner still offers A a 25-ft move to (8,3) + Longbow (attack declarations read subject.profile.rules.speed, offer-declarations.ts:431
465); with the profile's rules.speed also feet(0) the real planner + an in-memory D642 reference gives base = Longbow 
2 + Dodge with adjacency/visibility keys scoring 60/60 and, with (8,4) added, Dodge/Dodge and menu [] 
 and removing corner sealing restores the Longbow and the two-entry menu. N1 (appended verbatim to the plan, all copies): construct A with rules.speed feet(0), keep the effect, assert declared and effective speed zero, preserve the exact labels/keys/scores/menus, two mutants (speed 30 restored; sealing disabled), re-verify against accepted WALL-01; test-file only. Plan copies: .tmp-plans (main), ../dnd-wt-seam-verify, ../dnd-wt-cohort-01, ../dnd-wt-vis-field.
OUTAGE: the first WALL-01 fix r2 lane died with "Selected model is at capacity" + "thread 
 not found" before editing (worktree clean); probes SOL-OK / TERRA-OK at 21:10; re-dispatched FRESH on sol (log impl-wall-01-fix-r2b.log). Per the routing rule the next sol failure on this scope goes to Terra xhigh.
COHORT-01 B1 (validator normalization; not gated on WALL-01): new worktree ../dnd-wt-cohort-01 = branch claude/cohort-01 from 6887755f (node_modules symlinked, plan copied); fresh sol; brief .tmp/runs/cohort-01/impl-cohort-01-b1.md; log impl-b1.log; consensus dir .claude/consensus/cohort-01/. Generation batches (3
5) stay gated on WALL-01's acceptance; the cohort branch merges into claude/vis-field once both are accepted.
## D651 
 SUPERVISOR (2026-09-17 21:25): two correct BLOCKED stops ruled 
 WALL-01 fix r2's four scene subjects (test-scene edits approved) and COHORT-01 B1's v5 cap discontinuity (amendment N2; plan re-frozen at sha256 4bff2145b445cbb96771333bfdceb5cfdae8781e494cfa48622619cae0a2ac69, 403 lines)
WALL-01 fix r2 (sol fresh 01a0b20e-61fa
; saved impl-wall-01-fix-r2-sol.md; no files changed): all five reds are correct D642 consequences 
 (1) scene-snapshot: the wall cell (7,2) and the door (6,3) meet diagonally at vertex (7,3); the fighter's and wizard's last-clear row-3 rays (3,3)
(8,3), (2,3)
(9,3) become blocked; DM fog {(4,3)} 
 {(4,3),(8,3),(9,3)}; hashes derived (pendingRequestHash 78f31379
 0ec0fbbc
, coordinatorHash f8fa0780
 79b076c9
, revisionChecksum 35c96ecc
 52305455
, fingerprint unchanged dd802783
 since encounter-state bytes are unchanged); (2)(3) the conversation helper's re-authored blockers (9,1)/(9,5) now seal with (10,2)/(10,6) and the five opened pairs vanish; (4)(5) the R02 helpers' re-authored blocker (9,2) seals with (10,3) at vertex (10,3): the R02 row becomes HIDDEN and the first Scout's 90-ft shot becomes illegal. Sol stopped per the brief and proposed minimal scene edits: (9,1)/(9,5) 
 (8,1)/(8,5) and (9,2) 
 (8,2), temporarily proven (4/4) and restored. RULING: apply the scene-snapshot normalization under D569 (as B4/B2) and the two scene edits with derivations; test files only. Dispatched fix r3 (resume 01a0b20e-61fa
; brief impl-vis-field-wall-01-fix-r3.md).
COHORT-01 B1 (sol 01a0b20e-61fb
; saved impl-b1-sol.md; three allowed files, 176+/13
): second-family validator now a strict v1/v2 union with raw-byte hashing before a legacy projection that drops only the committed own foggedCells key; 20/20 single-key structural diffs; 50/50 second-family (47 historical violations 
 0); five mutants killed; V-freeze 32/32 hashes identical; tsc 0/0. BLOCKED correctly: d569-v5.test.ts still fails at collection 
 now on "Registered context caps mismatch" (tools/d569-v5/validate-first-arm.ts:369): the v5 manifest registers caps.semanticBytes 8192 (engine-server SEMANTIC_BOARD_MAX_BYTES, unchanged by B6) while rows produced by today's live blind runner carry 32,768 (D643). VERIFIED BY ME (test run + guard read). RULING N2 (appended to the plan, all copies): manifest-declared caps are the cell caps, version-aware (v5 = 8192 history; v6 = 32768); the v5 test stays historical and PINS the refusal of live rows under today's runner (no skip, no relaxed cap); v6's active test validates at 32,768; mutants both ways. Dispatched B1 fix r1 (resume 01a0b20e-61fb
). PARKED for the owner (design): making the blind runner take its semantic cap from the experiment manifest end-to-end would let historical experiments dry-run as registered; not in COHORT-01.
## D652 
 SUPERVISOR (2026-09-17 21:51): COHORT-01 B1 (+fix r1) VERIFIED BY ME and committed e92ebc93 on claude/cohort-01; astra medium review dispatched
VERIFIED (verify-b1.log, clean checkout at 6887755f + patch): tsc 0/0; diff-check 0; 0 long lines; V-freeze exit 0 (v1/v5 manifests, brutal/brutal-b/brutal-2/hard-2 fixtures byte-identical); MY MUTANT 
 the legacy projection also drops `round` 
 2 red; second-family 50/50; d569-v5 27/27; blind-experiment 46 + the six frozen-family titles (now the helper-fed-committed-bytes artefact assigned to Batch 6). Committed e92ebc93. Astra (fresh, medium; brief review-b1-astra.md; log review-b1-astra-r1.log) on the detached checkout at e92ebc93.
## D653 
 SUPERVISOR (2026-09-17 22:08): COHORT-01 B1 ACCEPTED by astra r1 (medium; 1 P3; saved review-b1-astra-r1.md); WALL-01 r3 VERIFIED BY ME and committed bc6422bf; astra review of the whole WALL-01 candidate dispatched; COHORT-01 B2 dispatched
ASTRA B1: real generator in memory 
 20/20 fixtures 
 both generations differ only at \$.encounter.state.foggedCells, 20/20 raw hashes match, v1 validation 0 violations; projection deletes exactly the committed own key, generated bytes untouched; rebound `round` and root changes still fail both regenerations; missing historical keys and v2 legacy keys reject; only frozen brutal productivity retired (integrity/reducer loading, static budget/caster/terrain, hashes, ledger, namespaces, both regenerations enforced); the strict v1/v2 union sound; N2 is a real enforcement witness (runArena rows with synthetic adapters rejected at 32,768 vs v5's 8,192; guard mutant 
 2 red); v6 seam correctly deferred; tsc 0/0; freeze 0; 191 added lines, 0 over 120; its runs: second-family 50/50, v5 27/27 (in-memory I/O), ignore-second-generation mutant 49/1. B1-F1 (P3): the brutal_productivity filter at second-family-manifest.test.ts:147 lacks the D635/D642/D643 rationale comment 
 folded into Batch 2. NOTE for Batch 6 (astra): substituting canonical regeneration in the blind-experiment helper clears the six reds but exposes a whitespace-only expectation at d569-blind-experiment.test.ts:727.
WALL-01 r3 VERIFIED BY ME (verify-wall-01-r3.log; seam-ab at 6887755f + patch): tsc 0/0; 0 production files in the diff; vertex-rule-null mutant 
 exactly 1 re-pinned scene red (scene-snapshot; the other four scenes were re-authored to NOT depend on sealing, by design); 176/176 across the five scene suites + visibility-field + two-room-fixture; los-cover 110/112 (the two 5 s budget tests at 5.14/5.40 s 
 perf, D635.53); pair 36/36 in 39 s. Committed bc6422bf on claude/vis-field (6887755f candidate + r3). Astra (resume 01a0a642
, medium; brief review-impl-wall-01-astra.md; log review-wall-01-astra-r1.log) reviews the whole candidate on seam-ab at bc6422bf, including the geometry enumeration (endpoint vertices, three/four-blocker vertices, direction of travel), the gcd/progress ordering, physical-cover invariance, the added per-ray cost, and the r3 derivations.
B2 (RED v2 membership witnesses + frozen-contract retirement; predicate preserved per PLAN-F6) dispatched fresh sol on claude/cohort-01 at e92ebc93 (brief impl-cohort-01-b2.md; log impl-b2.log). Generation (Batches 3
5) remains gated on WALL-01's acceptance.
## D654 
 SUPERVISOR (2026-09-17 22:23): WALL-01 REJECTED by astra r1 (medium; 1 P1: WALL-F3; saved review-impl-wall-01-astra-r1.md) 
 CONFIRMED BY ME; the defect is MY SPEC: the WALL-01 brief dictated "endpoint vertices are NOT vertex-checked"; fix r4 dispatched
ASTRA (2.49 M tokens): geometry otherwise sound (single-blocker touch and outer-face graze clear; shared edges block; interior diagonal pairs block including TL+BR with a TR
BL ray; three/four-blocker vertices covered; Fraction enumeration matched the gcd enumeration for 600 ordered integer endpoint pairs; ordering/tie-break explicit; physical tier independent 
 its "total whenever firstBlockingCell" mutant killed); r3 derivations reproduced (fog (4,3) 
 (4,3),(8,3),(9,3); fighter +(8,2),(9,2),(8,3),(9,3); all three hash transitions reproduced by substituting baseline vs candidate cover.ts in memory; only visibleState.cells/concealedCells changed; fingerprint dd802783
; the three replacement blockers have zero diagonal neighbours; five opened pairs / Half + Three-Quarters / both 90-ft shots asserted); six files 387+/49
; tsc 0/0; 66/66 across the suites it could run; perf: the productive-option los-cover test 4,935 ms baseline 
 5,740 ms candidate (gcd + up to four lookups per interior vertex) 
 deferred perf work, not blocking. WALL-F3 (P1): a COMPLETE stepped wall (6
6; blockers col 1 rows 0
1, col 2 rows 2
5; junction (2,2) between (1,1) and (2,2)) leaks: of the 16 corner rays from observer (2,1) to target (0,3), 10 hit interiors, 2 hit the sealed vertex strictly inside, and 4 START at the sealed junction (the observer's bottom-left corner) and pass through the pinch 
 the endpoint exclusion lets them through; production says visible/seen. VERIFIED BY ME on seam-ab at bc6422bf: (2,1)
(0,3) tier none / blocksSight FALSE; reversed half / FALSE; same-side (2,1)
(5,1) clear (correct). ROOT CAUSE = my WALL-01 brief: "Endpoint vertices (t = 0 or 1) are NOT vertex-checked 
 the cell-interior rule governs there"; sol implemented what I specified. Finding against my own work, at full length: I specified a geometric rule from intuition without enumerating the endpoint case; astra's rational enumeration did. RULE (D654): at an endpoint vertex with a sealed pair, the ray blocks iff it passes through the pinch 
 leaves the source corner into the open quadrant diagonally opposite the source cell's quadrant (or, mirrored, arrives at the target corner from the opposite quadrant); edge-aligned departures fall to the shared-edge rule; same-side departures stay clear; zero-length corner contact across a sealed junction = blocked. Fix r4 dispatched (resume 01a0b20e-61fa
; brief impl-vis-field-wall-01-fix-r4.md): witnesses for source/target endpoint, same-side control, adjacent-corner zero-length, three mutants; re-run of every scene suite with D569 handling if pins move; counts and generation bytes redone 
 if generation bytes change, STOP (it would alter the cohort plan's premise).
## D655 
 SUPERVISOR (2026-09-17 22:51): COHORT-01 B2 VERIFIED BY ME and committed 8284a91c on claude/cohort-01; astra medium review dispatched (resumed B1 reviewer)
VERIFIED (verify-b2.log, clean checkout at e92ebc93 + patch): tsc 0/0; diff-check 0; 0 long lines; V-freeze 0 and no fixture files touched; the new v2 suite 30 red, every failure ENOENT on the declared missing fixture; room-generator 56/56; brutal-b 47/47; second-family 50/50. Read by me: the removed code in both frozen suites is exactly the per-monster productivity loop, replaced by the D635/D642/D643 rationale. Generation (Batches 3
5) still gated on WALL-01 (fix r4 in flight).
## D656 
 SUPERVISOR (2026-09-17 23:07): COHORT-01 B2 REJECTED by astra r1 (medium; 0 P1, 1 P2: B2-F1; saved review-b2-astra-r1.md) 
 fix r1 dispatched; WALL-01 fix r4 harvested (sol; saved impl-wall-01-fix-r4-sol.md) 
 my verification running
B2-F1 (P2): the 6203 retirement removed the whole brutalRoomMembershipViolations test (difficulty, scaled budget/spend, caster, terrain AND productivity) instead of only the productivity loop; astra's in-memory mutant (budget +1 on 6203010) leaves the suite 56/56 green; the fix restores the ten-seed static assertion filtering only brutal_productivity. Everything else accepted: 6206 retirement exact; 30 v2 declarations through declareTestInputs; PLAN-F6 predicate with the query's own traceCombatantLine at mechanics.finalPosition; membership after decoding with no swallow (an in-memory bad-budget fixture fails with brutal_budget); three mutants killed; tsc 0/0; freeze 0; 229 added lines, 0 over 120. Fix r1 dispatched (resume 01a0b245
WALL-01 fix r4 (sol, resume 01a0b20e-61fa
; 2 files 286+/26
): endpoint-junction rule 
 at a sealed vertex a source ray blocks only when leaving diagonally into the quadrant opposite the source cell (TL
BR, TR
BL, BL
TR, BR
TL ownership), target rays mirrored, axis-aligned rays to the shared-edge rule, zero-length corner contact across opposite free quadrants blocked, progress 0/1, physical cover untouched, same path in coverTierBetweenObjects; witnesses SOURCE/TARGET_ENDPOINT_JUNCTION_SEALED (astra's scene), SAME_SIDE_ENDPOINT_GRAZE_CLEAR, ADJACENT_CORNER_ZERO_LENGTH; DIAGONAL_CORNER_TOUCH_SEALED_D642 re-derived (its source corner (1,1) had been an excluded endpoint 
 now aggregate blocked, first (1,0), tier none); three mutants killed; visibility-field 50/50; r3 scenes 5/5; ten-suite audit 246 passed + exactly the 35 known frozen reds; counts unchanged (hard 142/35, brutal 57/34, brutal-b 15/41, brutal-2 58/36); 42 generated rooms byte-identical (7b884bf9
); pair 39.4 s; los-cover budget tests 5.6 s isolated (perf). MY VERIFICATION (verify-wall-01-r4.log, seam-ab at bc6422bf + patch): tsc, six-case probe (A/B endpoint must block; C same-side clear; D adjacent corner blocked; E interior; F stepped wall), endpoint-rule-null mutant, 15 suites, los-cover, pair.

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/combat/visibility-field.test.ts
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..bbf19fe5e6d8ac87eeee0af3c39e01bebb015d24
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,79 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 2, row: 0 });
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 0);
+
+    // Source corner (2,2) has own quadrant TR. Ray (2,2)->(2,0) runs upward on the
+    // vertical boundary of closed TR, grazing the source cell's own edge, so it stays clear.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 0, row: 1 });
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 0 && line.targetCorner.row === 2);
+
+    // Source corner (2,2) has own quadrant TR. Ray (2,2)->(0,2) heads left along
+    // the TL/BL boundary, outside closed TR and down the far face of the sealed wall.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..bbf19fe5e6d8ac87eeee0af3c39e01bebb015d24
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,79 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 2, row: 0 });
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 0);
+
+    // Source corner (2,2) has own quadrant TR. Ray (2,2)->(2,0) runs upward on the
+    // vertical boundary of closed TR, grazing the source cell's own edge, so it stays clear.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 0, row: 1 });
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 0 && line.targetCorner.row === 2);
+
+    // Source corner (2,2) has own quadrant TR. Ray (2,2)->(0,2) heads left along
+    // the TL/BL boundary, outside closed TR and down the far face of the sealed wall.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=verbose --testNamePattern='ADJACENT_CORNER_AGGREGATE_BLOCKED|EDGE_ARRIVAL_AT_JUNCTION_BLOCKED|OWN_EDGE_DEPARTURE_CLEAR|FAR_FACE_DEPARTURE_BLOCKED'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 3036ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > DIM_LIGHT_RAW_GRADES keeps dim visible and darkness unseen for normal sight
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > LIGHT_OBSCUREMENT_FORCES_SEEN_DIM without making the cell invisible
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > DEVILS_SIGHT_RETAINS_OVERLAPPING_LIGHT_OBSCUREMENT
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > LIT_TARGET_ACROSS_ORDINARY_DARKNESS_IS_VISIBLE
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > UNLIT_TARGET_WITHOUT_SENSE_IS_UNSEEN
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > WALL_BLOCKS_TARGET_CELL using the existing corner tracer
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > FOG_CLOUD_BLOCKS_BEYOND_TARGET through a concrete obscured-area effect
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > MAGICAL_DARKNESS_SENSE_MATRIX distinguishes normal, darkvision, Devil's Sight, and truesight
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > HEAVY_FOG_BLOCKS_TRUESIGHT at the target and on intervening rays
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > SPECIAL_SENSE_RANGE_EDGES checks darkvision 60/65 and Devil's Sight 120/125 feet
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > LARGE_OBSERVER_USES_BEST_CELL for range and target-cell tracing
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > BLINDED_FIELD_IS_UNSEEN while in-range blindsight sees heavy fog
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > BLINDSIGHT_SEES_THROUGH_HEAVY_FOG_IN_RANGE
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > BLINDED_TREMORSENSE_REMAINS_LOCATED
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > ANY_FOOTPRINT_CELL_VISIBLE makes a Large subject visible
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > INVISIBLE_SUBJECT_RETAINS_TRANSPOSE_ELIGIBILITY
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > DETECT_FIELD_ALL_PAIRS_AGREE on every ordered overlay-free fixture pair
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > ALLY_CAST_DARKVISION_APPLIES and SENSE_EFFECT_MAX_RANGE_WINS
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > SENSE_EFFECT_MERGE_IS_ORDER_INDEPENDENT for two darkvision grants
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > INNATE_SENSE_NOT_SHORTENED_BY_EFFECT
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > DEVILS_SIGHT_EFFECT_MERGE_IS_ORDER_INDEPENDENT
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > ALWAYS_ON_DEVILS_SIGHT_FIXTURE_PC initializes a permanent 120-foot grant
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > CACHE_IS_STATE_SCOPED_AFTER_MOVEMENT and keeps the prior state unchanged
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > CACHE_IS_STATE_SCOPED_AFTER_LIGHT_CHANGE
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > CACHE_IS_STATE_SCOPED_AFTER_OBSCUREMENT_END
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 visibility field > CACHE_IS_STATE_SCOPED_AFTER_SENSE_EFFECT_REMOVAL and EFFECT_EXPIRY_REMOVAL
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > SEAM_BETWEEN_TWO_BLOCKERS_BLOCKS
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > SEAM_WITH_ONE_OPEN_SIDE_STAYS_CLEAR
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > SEAM_BLOCKED_CELL_PAIR
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > DIAGONAL_CRACK_SEALED
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > DIAGONAL_CRACK_CONTROL_ONE_BLOCKER
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > SOURCE_ENDPOINT_JUNCTION_SEALED
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > TARGET_ENDPOINT_JUNCTION_SEALED
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > SAME_SIDE_ENDPOINT_GRAZE_CLEAR
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > ADJACENT_CORNER_ZERO_LENGTH
 × tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > ADJACENT_CORNER_AGGREGATE_BLOCKED 14ms
   → expected { …(10) } to match object { tier: 'none', …(2) }
(13 matching properties omitted from actual)
 × tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > EDGE_ARRIVAL_AT_JUNCTION_BLOCKED 1ms
   → expected { …(7) } to match object { blocksSight: true, …(1) }
(7 matching properties omitted from actual)
 × tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > OWN_EDGE_DEPARTURE_CLEAR 1ms
   → expected { column: 2, row: 1 } to deeply equal { column: 2, row: 2 }
 × tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > FAR_FACE_DEPARTURE_BLOCKED 1ms
   → expected { column: 2, row: 1 } to deeply equal { column: 2, row: 2 }
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > STAIRCASE_WALL_SEALED
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > STAIRCASE_MIRRORED_SEALED
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > VERTICAL_STEP_SEALED
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > STAIRCASE_SPLIT_BY_OPEN_CELL_STAYS_CLEAR
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > OUTER_FACE_GRAZE_STILL_CLEAR
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > SOLID_2x2_BLOCK
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > DIAGONAL_CORNER_TOUCH_SEALED_D642
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > PHYSICAL_COVER_UNCHANGED_BY_VERTEX
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > PHYSICAL_COVER_UNCHANGED_BY_SEAM
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > EMPTY_SIGHT_BLOCKERS_PRESERVE_FULL_TRACE including caches and a multi-cell footprint
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > SIGHT_ONLY_BLOCKER_PRESERVES_COVER even when fog flips the preferred optical corner
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 shared evaluator boundary > PERCEPTION_GRADES_DRIVE_PASSIVE_BEHAVIOR
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 shared evaluator boundary > UNSEEN_SIGHT_SEARCH_AUTO_FAILS_WITHOUT_ROLLING
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 shared evaluator boundary > SIGHT_TRACER_HAS_SINGLE_OWNER at the final migration stage
 ↓ tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 shared evaluator boundary > PERCEPTION_MODE_CONSUMES_SHARED_SUBJECT_GRADE

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 4 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > ADJACENT_CORNER_AGGREGATE_BLOCKED
AssertionError: expected { …(10) } to match object { tier: 'none', …(2) }
(13 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": true,
-   "firstBlockingCell": {
-     "column": 1,
-     "row": 1,
-   },
+   "blocksSight": false,
+   "firstBlockingCell": null,
    "tier": "none",
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1207:19
    1205|     // and BR (2,2). The last r4-clear rays end at the pinch along its…
    1206|     // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the ag…
    1207|     expect(trace).toMatchObject({
       |                   ^
    1208|       tier: 'none',
    1209|       blocksSight: true,

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/4]⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > EDGE_ARRIVAL_AT_JUNCTION_BLOCKED
AssertionError: expected { …(7) } to match object { blocksSight: true, …(1) }
(7 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": true,
-   "firstBlockingCell": {
-     "column": 1,
-     "row": 1,
-   },
+   "blocksSight": false,
+   "firstBlockingCell": null,
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1231:29
    1229|     expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
    1230|     expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 }…
    1231|     expect(verticalArrival).toMatchObject({ blocksSight: true, firstBl…
       |                             ^
    1232|     expect(horizontalArrival).toMatchObject({ blocksSight: true, first…
    1233|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/4]⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > OWN_EDGE_DEPARTURE_CLEAR
AssertionError: expected { column: 2, row: 1 } to deeply equal { column: 2, row: 2 }

- Expected
+ Received

  {
    "column": 2,
-   "row": 2,
+   "row": 1,
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1245:32
    1243|     // Source corner (2,2) has own quadrant TR. Ray (2,2)->(2,0) runs …
    1244|     // vertical boundary of closed TR, grazing the source cell's own e…
    1245|     expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
       |                                ^
    1246|     expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingC…
    1247|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/4]⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > FAR_FACE_DEPARTURE_BLOCKED
AssertionError: expected { column: 2, row: 1 } to deeply equal { column: 2, row: 2 }

- Expected
+ Received

  {
    "column": 2,
-   "row": 2,
+   "row": 1,
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1263:32
    1261|     // Source corner (2,2) has own quadrant TR. Ray (2,2)->(0,2) heads…
    1262|     // the TL/BL boundary, outside closed TR and down the far face of …
    1263|     expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
       |                                ^
    1264|     expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCe…
    1265|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/4]⎯


 Test Files  1 failed (1)
      Tests  4 failed | 50 skipped (54)
   Start at  23:24:06
   Duration  2.73s (transform 863ms, setup 0ms, import 1.26s, tests 18ms, environment 0ms)


diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..bbf19fe5e6d8ac87eeee0af3c39e01bebb015d24
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,79 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 2, row: 0 });
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 0);
+
+    // Source corner (2,2) has own quadrant TR. Ray (2,2)->(2,0) runs upward on the
+    // vertical boundary of closed TR, grazing the source cell's own edge, so it stays clear.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 0, row: 1 });
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 0 && line.targetCorner.row === 2);
+
+    // Source corner (2,2) has own quadrant TR. Ray (2,2)->(0,2) heads left along
+    // the TL/BL boundary, outside closed TR and down the far face of the sealed wall.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/combat/visibility-field.test.ts
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=dot --testNamePattern='ADJACENT_CORNER_AGGREGATE_BLOCKED|EDGE_ARRIVAL_AT_JUNCTION_BLOCKED|OWN_EDGE_DEPARTURE_CLEAR|FAR_FACE_DEPARTURE_BLOCKED'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 3052ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
-----------------------------------xx·x---------------

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 3 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > ADJACENT_CORNER_AGGREGATE_BLOCKED
AssertionError: expected { …(10) } to match object { tier: 'none', …(2) }
(13 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": true,
-   "firstBlockingCell": {
-     "column": 1,
-     "row": 1,
-   },
+   "blocksSight": false,
+   "firstBlockingCell": null,
    "tier": "none",
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1207:19
    1205|     // and BR (2,2). The last r4-clear rays end at the pinch along its…
    1206|     // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the ag…
    1207|     expect(trace).toMatchObject({
       |                   ^
    1208|       tier: 'none',
    1209|       blocksSight: true,

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/3]⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > EDGE_ARRIVAL_AT_JUNCTION_BLOCKED
AssertionError: expected { …(7) } to match object { blocksSight: true, …(1) }
(7 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": true,
-   "firstBlockingCell": {
-     "column": 1,
-     "row": 1,
-   },
+   "blocksSight": false,
+   "firstBlockingCell": null,
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1231:29
    1229|     expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
    1230|     expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 }…
    1231|     expect(verticalArrival).toMatchObject({ blocksSight: true, firstBl…
       |                             ^
    1232|     expect(horizontalArrival).toMatchObject({ blocksSight: true, first…
    1233|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/3]⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > FAR_FACE_DEPARTURE_BLOCKED
AssertionError: expected { …(7) } to match object { blocksSight: true, …(1) }
(7 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": true,
-   "firstBlockingCell": {
-     "column": 1,
-     "row": 1,
-   },
+   "blocksSight": false,
+   "firstBlockingCell": null,
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1270:21
    1268|     // to departure (2,2)->(0,2), lies left of closed TR on the wall's…
    1269|     expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
    1270|     expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCe…
       |                     ^
    1271|   });
    1272|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/3]⎯


 Test Files  1 failed (1)
      Tests  3 failed | 1 passed | 50 skipped (54)
   Start at  23:24:38
   Duration  2.77s (transform 888ms, setup 0ms, import 1.30s, tests 18ms, environment 0ms)


diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=dot --testNamePattern='ADJACENT_CORNER_AGGREGATE_BLOCKED|EDGE_ARRIVAL_AT_JUNCTION_BLOCKED|OWN_EDGE_DEPARTURE_CLEAR|FAR_FACE_DEPARTURE_BLOCKED'
npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=dot" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 6251ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
-----------------------------------····---------------

 Test Files  1 passed (1)
      Tests  4 passed | 50 skipped (54)
   Start at  23:25:00
   Duration  2.75s (transform 898ms, setup 0ms, import 1.33s, tests 12ms, environment 0ms)


 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
······················································

 Test Files  1 passed (1)
      Tests  54 passed (54)
   Start at  23:25:03
   Duration  2.80s (transform 879ms, setup 0ms, import 1.28s, tests 66ms, environment 0ms)


diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

codex
The refined closed-quadrant predicate now makes all four new witnesses pass, and the complete visibility suite is 54/54. I’m running the generation-byte gate next; if any of the 42 hashes moved, I’ll stop before normalizing anything.
exec
/bin/bash -lc "npx vite-node /tmp/wall01-r4-generation.ts | sed -E 's/,\"rows\":.*"'$/}/'"'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 3960ms:
{"count":42,"aggregateSha256":"7b884bf91213ddaba626ec77b0dbf874c99d5de6cb8e6e190c0be5b7e31c56fc"}

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "npx vitest run --configLoader runner tests/unit/vtt/scene-snapshot.test.ts tests/unit/tools/ai-dm-conversation.test.ts tests/unit/vtt/dm-tactical-intel.test.ts tests/unit/vtt/tactical-evaluator-r02.test.ts --reporter=verbose --testNamePattern=\"preserves full persisted request/coordinator bytes and the actual revision checksum|records typed fallbackReason for 'blocked auto-submit' from the stub adapter|uses sim_controller and records a typed block when an unresolved frontier prevents auto-submit|renders target-specific non-nearest R02 rows in always-on context with exact reasons|reports both 90-foot Scout shots as move-0 normal range and straight at the dying fighter\"" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

 exited 1 in 32102ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > pins ground centres and inverse anchors for every controlled span
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > enriches filtered player combatants with renderer-only ids and omits environment lights
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > derives real Large, Huge, Gargantuan, and squeezed footprints and ground centres through the adapter
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > removes canonical token identity on a visible-to-hidden player transition
 × tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > preserves full persisted request/coordinator bytes and the actual revision checksum 33ms
   → expected [ { column: 4, row: 3 }, …(3) ] to deeply equal [ { column: 4, row: 3 }, …(2) ]
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > emits DM environment lights, exclusive door companions and no door cell wall union
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > uses the object-position north edge for adjacent and multicell doors, open and closed
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > reports deterministic role-aware fallbacks
 ✓ tests/unit/vtt/tactical-evaluator-r02.test.ts > R02-like canonical tactical query > reports both 90-foot Scout shots as move-0 normal range and straight at the dying fighter 20ms
 ↓ tests/unit/vtt/tactical-evaluator-r02.test.ts > R02-like canonical tactical query > compares ordered registry allocations with exact post-grant Bless conditioning
 ✓ tests/unit/vtt/dm-tactical-intel.test.ts > versioned DM tactical intel > renders target-specific non-nearest R02 rows in always-on context with exact reasons 796ms
 ↓ tests/unit/vtt/dm-tactical-intel.test.ts > versioned DM tactical intel > keeps exact probability internally while both context and query render coarse values
 ↓ tests/unit/vtt/dm-tactical-intel.test.ts > versioned DM tactical intel > paginates the full matrix with a state-bound cursor and advertises the read tool to the DM
 ↓ tests/unit/vtt/dm-tactical-intel.test.ts > versioned DM tactical intel > emits a short neutral death-save window only when the top actor precedes the target
 ↓ tests/unit/vtt/dm-tactical-intel.test.ts > versioned DM tactical intel > renders the failure manifest only in the DM profile and retains the 32 KiB trim
 ↓ tests/unit/vtt/dm-tactical-intel.test.ts > versioned DM tactical intel > attaches full-precision offered-set and all intel policy versions to accepted capture
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > omits DM-only intel_mode from blind planned context arguments
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists indeterminate artifact and forensic row before raising the typed integrity STOP
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > classifies a timeout after catalog observation but before a tool call without an integrity STOP
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists primary D569 timeout evidence before applying the expired round deadline
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists forensic evidence and STOPs on a timed_out dispatch with a contradictory catalog
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists forensic evidence and STOPs on a cancelled dispatch with a contradictory catalog
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists forensic evidence and STOPs on a timed_out dispatch with an invalid malformed catalog
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists forensic evidence and STOPs on a timed_out dispatch with an invalid wrong_profile catalog
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists forensic evidence and STOPs on a cancelled dispatch with an invalid malformed catalog
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists forensic evidence and STOPs on a cancelled dispatch with an invalid wrong_profile catalog
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists a runner forensic row before propagating conflicting startup evidence
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > persists delivered context and process evidence before propagating conflicting startup evidence
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > rejects a request after the MCP child closes stdin without an unhandled EPIPE
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > routes a readline input error through typed rejection and complete teardown
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > rejects an in-flight MCP request specifically on child exit
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > rejects on stdout closure and terminates a child that remains alive
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > refuses an MCP request issued after the child has exited
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > preserves a request failure through the caller try/finally cleanup path
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > reports standalone abnormal MCP termination as a typed error
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > authorizes a serialized revision-bound Dodge proposal
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > authorizes targeted Web and non-targeted Dodge through composite options
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > round-trips the End Turn option emitted by the tactical context
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > wall begins before initial without speculation
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > speculation uses arm base until trigger
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > restricted wall excludes engine work and includes later speculation
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > fallback computation of 1000 ms remains outside the restricted wall
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > fallback computation of 200000 ms remains outside the restricted wall
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > expired speculative adoption validation cannot adopt or execute that branch
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > expired recalculation authorization cannot retain or execute recalculated entries on the real path
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > one rejected submission uses arm-base correction before D474 escalation
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > empty structured correction is not D474 validation evidence on the real path
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > correction refusal triggers a fresh D474 escalation on the real path
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > actual refusal is the only single-attempt D474 trigger
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > keeps tiered primary argv/config byte-identical and isolates escalation from the base session
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > constructs no escalation adapter or bootstrap for the exact post-shift no-flag control shape (mutation: implicit escalation default)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > keeps one sitting across rooms, rolls only at threshold, and explicit end prevents resume
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > routes a 'tiered' correction using actual dispatch bookkeeping
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > routes a 'untiered' correction using actual dispatch bookkeeping
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > authorizes a serialized MCP attack proposal
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > requests full context for round one and a delta for a resumed later round
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > preserves accepted suggested composite proposals in the authorized SIMULATED row
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > re-resolves independently specified stored mechanics before reporting divergence
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > divergence refuses a stored resolution produced under a different offer environment
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > rejects stored proposal resolutions with missing or non-64-hex environment digests
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > rejects plan adjustment envelopes whose stored updates carry malformed digests
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > authorizes generated room 3943004 through the real host after resolving boundary decisions
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > authorizes generated room 3943007 through the real host after resolving boundary decisions
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > auto-resolves an unattended ask Reaction with the decline policy and authorizes the SIMULATED arena round
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > auto-resolves an unattended ask Reaction with the take policy and authorizes the SIMULATED arena round
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > restores sticky side-wide guidance across a room transition and resolves a pending offer before dispatch
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > falls back to unattended toggle policy when sticky guidance does not cover the pending trigger
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > keeps one SIMULATED session across two rooms, one correction, and a browser restore
 ✓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > uses sim_controller and records a typed block when an unresolved frontier prevents auto-submit 14286ms
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records typed fallbackReason for 'no proposal' from the stub adapter
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records typed fallbackReason for 'validation exhaustion' from the stub adapter
 ✓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records typed fallbackReason for 'blocked auto-submit' from the stub adapter 13940ms
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records typed fallbackReason for 'timeout' from the stub adapter
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > does not depend on turn-context rendering to resolve a brutal exhaustion frontier
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > hidden_options_logged_once: records every hidden option once with engine-state knowledge and stationary Disengage reasons
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > runs a model-free stdio MCP dry-run smoke (mutation: count tool calls as KB reads)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > runs a three-room three-round model-free brutal smoke with the stub adapter
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > retries one SIMULATED service flap and uses the first healthy primary turn
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > marks three consecutive SIMULATED service flaps as service_null without exhausting the turn
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > does not retry a SIMULATED primary turn that called engine tools before rejection
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records when the configured turn-context trimmer fired
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > injects the root and tactics pair byte-for-byte only on cold start and attributes its hash
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > threads a selected skill through every invocation and hashes the exact fixture bytes
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > runs a model-free none instruction-source smoke through a SIMULATED adapter
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > runs a model-free kb instruction-source smoke through a SIMULATED adapter
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > runs a model-free skill instruction-source smoke through a SIMULATED adapter
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > starts the persistent SIMULATED session with the first round plan in one model dispatch
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > queues a schema-final indexed decision through the same engine round path (mutation: parse valid final output but do not queue it)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > rejects audited boilerplate at structured-final ingress before accepting a substantive correction
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > censors a cancelled structured-final turn as decision_timeout (mutation: classify cancelled final output as decision_missing)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records the actor chosen index for recommendation-anchor measurement (mutation: omit chosen index from final row)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > passes a persisted final-index row through packet validation
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records no chosen indices when no final-index decision is accepted
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > rejects speculative structured-final dispatch at the typed phase boundary (mutation: pass speculative through as a decision phase)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records a missing final decision explicitly and repairs it with the same indexed correction shape (mutation: classify final absence as service_null)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > uses the same indexed final contract for a G2-preflighted adjustment (mutation: send an adjustment through MCP)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > repairs an indexed adjustment through the same correction ingress (mutation: use a tool-driven adjustment correction)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > directs K6 to consume an inline suggestion without fetching the same play again
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > admits the active process and local OpenAI conversation adapters
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > parses the D569 mode, repair, attempt, fact, cap, timeout, and judge-model combinations
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > derives per-combatant initiative for standalone fixtures and rejects an explicit legacy profile
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > walks an alternating initiative fixture without waking the DM for HP-only drift
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > ends a three-round room as party_defeated when all PCs are dead entering the party segment
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > runs three rounds with a one-round party program by recording typed default turns
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > dispatches when a superior option appears and records adjustment choice keep
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > dispatches when a superior option appears and records adjustment choice change
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > skips the D443-style adjustment when material bookkeeping changed but the retained plan stayed legal and non-dominated (mutation: always dispatch)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > dispatches an adjustment when the retained primary becomes illegal
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > rejects a speculative context actor mismatch before model dispatch
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > never authorizes a row when execution throws before its first completed turn (mutation: authorized before the loop)
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records partial_execution when execution throws after a completed PC turn
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > discards speculation when the actual board invalidates the planned option structure
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > aborts the cell when speculation recalculation infrastructure fails after staging output
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > retains diagnosed speculative infrastructure at the end-of-round join
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > retains diagnosed speculative infrastructure at the exception-cleanup join
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > terminates incomplete speculation with a failed rebase without repeating the initiative loop
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > aborts speculative planning at the D407 pacing deadline
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > discards a speculative proposal staged before timeout without authorizing it
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > does not spend a second adjustment flap budget when preflight finds no material plan change
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > runs one adjustment correction and retains the corrected replacement
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > stamps speculation recalculation launchers with the speculative dispatch phase
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > keeps the baseline after three adjustment service nulls and continues the round
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > ignores a staged adjustment proposal after timeout and retains the authorized baseline
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > records adjustment timeout evidence before applying the expired round deadline
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > ignores an adjustment correction proposal staged before timeout and retains the authorized baseline
 ↓ tests/unit/tools/ai-dm-conversation.test.ts > AI-DM engine MCP conversation runner > treats adjustment correction no-response as protocol exhaustion, not a flap

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > preserves full persisted request/coordinator bytes and the actual revision checksum
AssertionError: expected [ { column: 4, row: 3 }, …(3) ] to deeply equal [ { column: 4, row: 3 }, …(2) ]

- Expected
+ Received

@@ -2,10 +2,14 @@
    {
      "column": 4,
      "row": 3,
    },
    {
+     "column": 7,
+     "row": 3,
+   },
+   {
      "column": 8,
      "row": 3,
    },
    {
      "column": 9,

 ❯ tests/unit/vtt/scene-snapshot.test.ts:253:52
    251|     // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains vi…
    252|     // sealing vertex is its target endpoint, which is excluded by the…
    253|     expect(projectDmView(state).state.foggedCells).toEqual([
       |                                                    ^
    254|       { column: 4, row: 3 },
    255|       { column: 8, row: 3 },

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed | 3 passed (4)
      Tests  1 failed | 4 passed | 121 skipped (126)
   Start at  23:25:23
   Duration  31.81s (transform 3.84s, setup 0ms, import 7.08s, tests 29.08s, environment 0ms)


context compacted
codex
The refined endpoint rule and all four new witnesses are in place; the 42-seed byte gate is unchanged. One re-authored snapshot has moved again under edge-aligned sealing, so I’m isolating that exact junction/ray before touching any pin.
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "sed -n '180,330p' tests/unit/vtt/scene-snapshot.test.ts && rg -n \"foggedCells|visibility|traceTerrainLine|debug|terrain\" tests/unit/vtt/scene-snapshot.test.ts src/combat/cover.ts | head -120" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:

  it('derives real Large, Huge, Gargantuan, and squeezed footprints and ground centres through the adapter', () => {
    const cases = [
      { token: sizedSnapshot('Large'), expected: { footprint: { w: 2, h: 2 }, x: 1.5, y: 1.5, z: 0 } },
      { token: sizedSnapshot('Huge'), expected: { footprint: { w: 3, h: 3 }, x: 2, y: 2, z: 0 } },
      { token: sizedSnapshot('Gargantuan'), expected: { footprint: { w: 4, h: 4 }, x: 2.5, y: 2.5, z: 0 } },
      { token: sizedSnapshot('Large', true), expected: { footprint: { w: 1, h: 1 }, x: 1, y: 1, z: 0 } },
    ];
    for (const { token, expected } of cases) {
      expect(token).toMatchObject(expected);
      if (token === undefined) throw new Error('Missing sized token.');
      expect(groundAnchor({ x: token.x, y: token.y, z: token.z }, token.footprint))
        .toEqual({ column: 1, row: 1 });
    }
  });

  it('removes canonical token identity on a visible-to-hidden player transition', () => {
    const state = fixtureState();
    const binding = { seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID };
    const identities = canonicalTokenIdentityIndex(state.tokens);
    const before = sceneSnapshot({
      sceneId: 'scene:hidden', projection: projectPlayerBoard(projectPlayerView(state, binding), IDLE),
      art: REFERENCE_ENCOUNTER_ART, tokenIdentities: identities,
    }).snapshot;
    const monsterTokenId = String(state.tokens.find((token) => token.combatantId === REFERENCE_MONSTER_ID)?.id);
    expect(before.tokens.map((token) => token.id)).toContain(monsterTokenId);
    const hidden = {
      ...state,
      hiddenCombatants: [{
        combatant: REFERENCE_MONSTER_ID, stealthTotal: 20, edition: '2024' as const,
      }],
    };
    const after = sceneSnapshot({
      sceneId: 'scene:hidden', projection: projectPlayerBoard(projectPlayerView(hidden, binding), IDLE),
      art: REFERENCE_ENCOUNTER_ART, tokenIdentities: identities,
    }).snapshot;
    expect(after.tokens.map((token) => token.id)).not.toContain(monsterTokenId);
  });

  it('preserves full persisted request/coordinator bytes and the actual revision checksum', () => {
    const state = fixtureState();
    const view = projectPlayerView(state, { seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID });
    const coordinator: PersistedCoordinatorState = {
      ...IDLE,
      pendingRequest: {
        kind: 'turn' as const,
        requestId: 'request:handoff-baseline',
        encounterRevision: state.revision,
        actorId: REFERENCE_FIGHTER_ID,
        visibleState: view,
        legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
      },
    };
    const encounterStateBytes = canonicalJson(state);
    const before = persistedByteBaseline(state, coordinator);
    const projection = projectPlayerBoard(view, coordinator);
    sceneSnapshot({
      sceneId: 'scene:pending', projection, art: REFERENCE_ENCOUNTER_ART,
      tokenIdentities: canonicalTokenIdentityIndex(state.tokens),
    });
    const after = persistedByteBaseline(state, coordinator);
    // Fighter (2,3) has corners (2,3),(3,3),(2,4),(3,4). For each target (c,4), c=7,8,9:
    // rays from either row-3 source corner to (c,4)/(c+1,4) enter Oak Door (6,3), while rays
    // to (c,5)/(c+1,5) enter Stone Wall (6,4) (the (3,3)->(10,5) ray enters both). From either
    // row-4 source corner, rays to (c,4)/(c+1,4) follow row line 4 between those two blockers,
    // and rays to (c,5)/(c+1,5) enter the wall. Thus every corner ray is blocked for all three.
    // D642 additionally seals lattice vertex (7,3), where blocked cell (7,2) and Oak Door
    // (6,3) touch diagonally. The last clear row-3 rays include (3,3)->(8,3) and
    // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
    // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
    // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
    expect(projectDmView(state).state.foggedCells).toEqual([
      { column: 4, row: 3 },
      { column: 8, row: 3 },
      { column: 9, row: 3 },
    ]);
    expect(view.concealedCells).toEqual([
      { column: 8, row: 2 },
      { column: 9, row: 2 },
      { column: 4, row: 3 },
      { column: 8, row: 3 },
      { column: 9, row: 3 },
      { column: 7, row: 4 },
      { column: 8, row: 4 },
      { column: 9, row: 4 },
    ]);
    // Pre-D635 concealed={(4,3)} and hashes were:
    // pendingRequest=fdaf8775fe7c2cd87ffc3ab6cdafff23dcbf2c3f30c1bf41b9dd9d546902fa6e;
    // coordinator=c31c026432dd2d523e582058ff432ffc4dd2d172dc01fcfd8fe4cbedaa328b0d;
    // revision=15ba352a6817d7d0722e8135a00a33fa026f109a3ec3026819b9081b119e41dc.
    // Pre-B4 revision=cd99e1c872f75ec7167c396e27088715990287458d39981b73b376d9e6284767;
    // Pre-B2 revision=df6f2cceaa19f66115b0bcd387212064b02ae288fc63c47b4db8f71a835fa4ee;
    // branch RNG fingerprint=163cf20067bebf7e70bb76d69b6f45a6123e51fdc409ba32a1b985b3f60b6a2b.
    // B2 removes the empty migration key and recomputes the fingerprint as
    // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
    // Pre-D642 -> D642, derived directly from the hand-derived visibility sets above.
    // pendingRequest:
    // 78f31379db8355bb977b5a87cac2349137ff0aa606d32ba4080277856e874049 ->
    // 0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0.
    // coordinator:
    // f8fa07803660b6ba53f256d885955af174f921fcaf58de46b1a7f0e697de9c66 ->
    // 79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd.
    // revision:
    // 35c96ecc6772f0cd518b5de7beeb79e4e92af4b270a5e709cbc3077d8f900385 ->
    // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
    // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
    // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
    expect(before).toEqual({
      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
      branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
    });
    expect(canonicalJson(state)).toBe(encounterStateBytes);
    expect(coordinator.pendingRequest).toMatchObject({
      kind: 'turn',
      requestId: 'request:handoff-baseline',
      encounterRevision: 0,
      actorId: 'combatant:fighter',
      legalActions: { actions: [{ type: 'end_turn', actor: 'combatant:fighter' }] },
    });
    expect(coordinator.pendingRequest?.visibleState).toBe(view);
    expect(coordinator.pendingRequest?.visibleState !== undefined &&
      'tokenIdentityByCombatant' in coordinator.pendingRequest.visibleState).toBe(false);
    expect(after).toEqual(before);
    assertPersistedHandoffBytesUnchanged(before, state, coordinator);

    if (coordinator.pendingRequest === null) throw new Error('PERSISTENCE_CONTROL_REQUEST_MISSING');
    const enrichedRequest = {
      ...coordinator.pendingRequest,
      visibleState: {
        ...coordinator.pendingRequest.visibleState,
        tokenIdentityByCombatant: { [String(REFERENCE_FIGHTER_ID)]: 'token:fighter' },
      },
    };
    const enrichedCoordinator: PersistedCoordinatorState = {
      ...coordinator,
      pendingRequest: enrichedRequest,
    };
    expect(() => assertPersistedHandoffBytesUnchanged(before, state, enrichedCoordinator))
      .toThrow('PERSISTED_HANDOFF_BYTES_CHANGED');
    expect(persistedRevisionBaseline(state, enrichedCoordinator).revisionChecksum)
      .not.toBe(before.revisionChecksum);
  });

  it('emits DM environment lights, exclusive door companions and no door cell wall union', () => {
    const state = fixtureState();
    const projection = projectDmBoard({
      view: projectDmView(state),
src/combat/cover.ts:4:import { coverRank, type CoverTier } from './terrain';
src/combat/cover.ts:241:  readonly terrain: ReadonlyMap<string, TerrainSourceCell>;
src/combat/cover.ts:245:const terrainSourceIndexes = new WeakMap<EncounterState, TerrainSourceIndexes>();
src/combat/cover.ts:246:const terrainLineTraces = new WeakMap<EncounterState, Map<string, TerrainLineTrace>>();
src/combat/cover.ts:248:const terrainSourceSignatures = new WeakMap<EncounterState, Map<boolean, string>>();
src/combat/cover.ts:252:function terrainSourceSignature(
src/combat/cover.ts:257:  const stateSignatures = terrainSourceSignatures.get(state) ?? new Map<boolean, string>();
src/combat/cover.ts:264:  if (!terrainSourceSignatures.has(state)) terrainSourceSignatures.set(state, stateSignatures);
src/combat/cover.ts:278:  terrain?: ReadonlyMap<string, TerrainSourceCell>,
src/combat/cover.ts:280:  const sourceCells: Map<string, TerrainSourceCell> = terrain === undefined
src/combat/cover.ts:282:    : new Map([...terrain].map(([key, entry]) => [key, {
src/combat/cover.ts:286:  if (terrain === undefined) {
src/combat/cover.ts:317:  const cached = terrainSourceIndexes.get(state);
src/combat/cover.ts:319:    if (!includeCreatures) return cached.terrain;
src/combat/cover.ts:320:    const withCreatures = cached.withCreatures ?? buildTerrainSourceIndex(state, true, cached.terrain);
src/combat/cover.ts:324:  const terrain = buildTerrainSourceIndex(state, false);
src/combat/cover.ts:325:  const entry: TerrainSourceIndexes = { terrain };
src/combat/cover.ts:326:  terrainSourceIndexes.set(state, entry);
src/combat/cover.ts:327:  if (!includeCreatures) return terrain;
src/combat/cover.ts:328:  const withCreatures = buildTerrainSourceIndex(state, true, terrain);
src/combat/cover.ts:703:  const traceKey = `${options.includeCreatures === true ? 'creatures' : 'terrain'}:${sourceCells
src/combat/cover.ts:705:  const stateTraces = terrainLineTraces.get(state) ?? new Map<string, TerrainLineTrace>();
src/combat/cover.ts:708:  if (!terrainLineTraces.has(state)) terrainLineTraces.set(state, stateTraces);
src/combat/cover.ts:712:  const equivalentTraceKey = `${terrainSourceSignature(
src/combat/cover.ts:795:export function traceTerrainLine(
tests/unit/vtt/scene-snapshot.test.ts:6:import { projectDmView, projectPlayerView } from '../../../src/combat/visibility';
tests/unit/vtt/scene-snapshot.test.ts:253:    expect(projectDmView(state).state.foggedCells).toEqual([
tests/unit/vtt/scene-snapshot.test.ts:277:    // Pre-D642 -> D642, derived directly from the hand-derived visibility sets above.

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "sed -n '1,180p' tests/unit/vtt/scene-snapshot.test.ts && rg -n \"function fixtureState|const fixtureState|REFERENCE_.*ID|Oak Door|Stone Wall\" tests src | head -100" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
import { describe, expect, it } from 'vitest';
import { combatToken } from '../../../src/combat/combatant';
import type { PersistedCoordinatorState } from '../../../src/combat/coordinator';
import { createEncounter } from '../../../src/combat/encounter';
import { mulberry32 } from '../../../src/combat/random';
import { projectDmView, projectPlayerView } from '../../../src/combat/visibility';
import {
  armorClass, encounterBranchId, encounterSessionId, worldObjectId,
} from '../../../src/combat/values';
import { canonicalJson } from '../../../src/commands/canonical-json';
import { sha256 } from '../../../src/crypto/sha256';
import { projectDmBoard, projectPlayerBoard } from '../../../src/vtt/encounter-projections';
import { REFERENCE_ENCOUNTER_ART } from '../../../src/vtt/reference-encounter-art';
import {
  REFERENCE_FIGHTER_ID, REFERENCE_MONSTER_ID, referenceEncounterSetup,
} from '../../../src/vtt/reference-encounter';
import {
  canonicalTokenIdentityIndex, groundAnchor, groundCenter, sceneSnapshot,
} from '../../../src/vtt/handoff/scene-snapshot';
import { logicalAssetId } from '../../../src/vtt/handoff/asset-id-map';
import {
  EncounterSessionJournal, MemoryBrowserSessionStore, MemoryMirrorSink,
} from '../../../src/vtt/session-persistence';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';

const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });

const IDLE = {
  requestSequence: 1,
  pendingRequest: null,
  pendingCommand: null,
  continuation: { kind: 'idle' as const },
  pause: null,
};

function fixtureState() {
  const setup = referenceEncounterSetup();
  return createEncounter({
    ...setup,
    worldObjects: [
      {
        id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
        position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
        durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
        blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
      },
      {
        id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
        position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
        durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
        blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
      },
      {
        id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
        position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
        durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
        blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
      },
    ],
  });
}

function sizedSnapshot(size: 'Large' | 'Huge' | 'Gargantuan', squeezed = false) {
  const setup = referenceEncounterSetup();
  const base = setup.combatants.find((profile) => profile.id === REFERENCE_FIGHTER_ID);
  if (base?.kind !== 'player_character') throw new Error('Missing reference fighter.');
  const profile = { ...base, rules: { ...base.rules, sizeCategory: size } };
  const created = createEncounter({
    bounds: setup.bounds, combatants: [profile],
    tokens: [combatToken(profile, { column: 1, row: 1 })],
  });
  if (squeezed && size !== 'Large') throw new Error('Squeezed fixture supports Large only.');
  const state = squeezed
    ? {
        ...created,
        tokens: created.tokens.map((token) => ({
          ...token, placementMode: { kind: 'squeezed' as const, actual: 'Large' as const, sizedFor: 'Medium' as const },
        })),
      }
    : created;
  const projection = projectDmBoard({
    view: projectDmView(state),
    coordinator: IDLE,
    controllers: [],
    history: [],
    offerEnvironment: OFFER_ENVIRONMENT,
  });
  return sceneSnapshot({
    sceneId: 'scene:sizes', projection, art: REFERENCE_ENCOUNTER_ART,
    tokenIdentities: canonicalTokenIdentityIndex(state.tokens),
  }).snapshot.tokens[0];
}

interface PersistedByteBaseline {
  readonly pendingRequestHash: string;
  readonly coordinatorHash: string;
  readonly revisionChecksum: string;
  readonly branchRngStateFingerprint: string;
}

function persistedRevisionBaseline(
  state: ReturnType<typeof fixtureState>,
  coordinatorState: PersistedCoordinatorState,
): Pick<PersistedByteBaseline, 'revisionChecksum' | 'branchRngStateFingerprint'> {
  const store = new MemoryBrowserSessionStore();
  const sessionId = encounterSessionId('session:handoff-persistence-baseline');
  EncounterSessionJournal.create({
    sessionId,
    branchId: encounterBranchId('branch:handoff-persistence-baseline'),
    encounterState: state,
    coordinatorState,
    controllers: [],
    rng: mulberry32(16_603),
    store,
    mirror: new MemoryMirrorSink(),
  });
  const revision = store.revisions(sessionId)[0];
  if (revision === undefined) throw new Error('PERSISTENCE_BASELINE_REVISION_MISSING');
  return {
    revisionChecksum: revision.checksum,
    branchRngStateFingerprint: revision.branchRngStateFingerprint,
  };
}

function persistedByteBaseline(
  state: ReturnType<typeof fixtureState>,
  coordinatorState: PersistedCoordinatorState,
): PersistedByteBaseline {
  if (coordinatorState.pendingRequest === null) throw new Error('PERSISTENCE_BASELINE_REQUEST_MISSING');
  return {
    pendingRequestHash: sha256(canonicalJson(coordinatorState.pendingRequest)),
    coordinatorHash: sha256(canonicalJson(coordinatorState)),
    ...persistedRevisionBaseline(state, coordinatorState),
  };
}

function assertPersistedHandoffBytesUnchanged(
  expected: PersistedByteBaseline,
  state: ReturnType<typeof fixtureState>,
  coordinatorState: PersistedCoordinatorState,
): void {
  const actual = persistedByteBaseline(state, coordinatorState);
  if (canonicalJson(actual) !== canonicalJson(expected)) {
    throw new Error('PERSISTED_HANDOFF_BYTES_CHANGED');
  }
}

describe('renderer-neutral scene snapshot', () => {
  it('pins ground centres and inverse anchors for every controlled span', () => {
    const anchor = { column: 2, row: 3 };
    expect([1, 2, 3, 4].map((span) => groundCenter(anchor, { w: span, h: span }))).toEqual([
      { x: 2, y: 3, z: 0 }, { x: 2.5, y: 3.5, z: 0 },
      { x: 3, y: 4, z: 0 }, { x: 3.5, y: 4.5, z: 0 },
    ]);
    expect(groundCenter(anchor, { w: 1, h: 1 })).toEqual({ x: 2, y: 3, z: 0 });
    expect(groundAnchor({ x: 2.5, y: 3.5, z: 0 }, { w: 2, h: 2 })).toEqual(anchor);
    expect(groundAnchor({ x: 2.25, y: 3.5, z: 0 }, { w: 2, h: 2 })).toBeNull();
    expect(groundAnchor({ x: 2.5, y: 3.5, z: 1 }, { w: 2, h: 2 })).toBeNull();
  });

  it('enriches filtered player combatants with renderer-only ids and omits environment lights', () => {
    const state = fixtureState();
    const view = projectPlayerView(state, { seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID });
    const projection = projectPlayerBoard(view, IDLE);
    const result = sceneSnapshot({
      sceneId: 'scene:two-room', projection, art: REFERENCE_ENCOUNTER_ART,
      tokenIdentities: canonicalTokenIdentityIndex(state.tokens),
    });
    expect(projection.combatants.every((combatant) => !('tokenId' in combatant))).toBe(true);
    const canonicalId = String(state.tokens.find((token) => token.combatantId === REFERENCE_FIGHTER_ID)?.id);
    expect(result.snapshot.tokens.map((token) => token.id)).toContain(canonicalId);
    expect(result.snapshot.tokens.some((token) => token.id === String(REFERENCE_FIGHTER_ID))).toBe(false);
    const hiddenCanonicalIds = state.tokens
      .filter((token) => !projection.combatants.some((combatant) => combatant.id === token.combatantId))
      .map((token) => String(token.id));
    expect(result.snapshot.tokens.every((token) => !hiddenCanonicalIds.includes(token.id))).toBe(true);
    expect(result.snapshot.vision.visible).toEqual(projection.visibleCells.map((cell) => [cell.column, cell.row]));
    expect(result.snapshot.lights.map((light) => light.id)).toEqual(['light:object:object:torch']);
  });

src/ui/ai-chat/mount.ts:47:import { AGENT_REFERENCE_SCRIPT_ID } from '../screens/planner/agent-reference';
src/ui/ai-chat/mount.ts:101:  const island = root.getElementById(AGENT_REFERENCE_SCRIPT_ID);
src/vtt/encounter-app.ts:109:import { REFERENCE_PLAYER_IDS } from './reference-encounter';
src/vtt/encounter-app.ts:1468:    const playerIds = encounter?.playerIds ?? REFERENCE_PLAYER_IDS;
src/vtt/generated-encounter-fixtures.ts:43:  REFERENCE_PLAYER_IDS,
src/vtt/generated-encounter-fixtures.ts:402:    ...REFERENCE_PLAYER_IDS,
src/ui/screens/planner/agent-reference.ts:89:export const AGENT_REFERENCE_SCRIPT_ID = 'planner-build-reference';
src/ui/screens/planner/agent-reference-panel.ts:16:  AGENT_REFERENCE_SCRIPT_ID,
src/ui/screens/planner/agent-reference-panel.ts:90:  data.id = AGENT_REFERENCE_SCRIPT_ID;
tests/unit/vtt/generated-encounter-fixtures.test.ts:38:  REFERENCE_FIGHTER_ID,
tests/unit/vtt/generated-encounter-fixtures.test.ts:377:      combatantId: REFERENCE_FIGHTER_ID,
src/vtt/dm-encounter-host.ts:89:  REFERENCE_PLAYER_IDS,
src/vtt/dm-encounter-host.ts:191:  return [...REFERENCE_PLAYER_IDS, encounterSessionMonsterId()].map((combatantId) => ({
src/vtt/dm-encounter-host.ts:396:    this.#playerIds = options.playerIds ?? REFERENCE_PLAYER_IDS;
tests/unit/vtt/refusal-handling.test.ts:20:  REFERENCE_FIGHTER_ID,
tests/unit/vtt/refusal-handling.test.ts:21:  REFERENCE_MONSTER_ID,
tests/unit/vtt/refusal-handling.test.ts:22:  REFERENCE_PLAYER_IDS,
tests/unit/vtt/refusal-handling.test.ts:37:    characters: REFERENCE_PLAYER_IDS.map((combatantId, index) => ({
tests/unit/vtt/refusal-handling.test.ts:53:    reactionPolicies: REFERENCE_PLAYER_IDS.flatMap((combatant) =>
tests/unit/vtt/refusal-handling.test.ts:66:    command: { type: 'end_turn', actor: REFERENCE_FIGHTER_ID },
tests/unit/vtt/refusal-handling.test.ts:67:    combatant: REFERENCE_FIGHTER_ID,
tests/unit/vtt/refusal-handling.test.ts:74:    actor: REFERENCE_FIGHTER_ID,
tests/unit/vtt/refusal-handling.test.ts:82:    targets: [REFERENCE_MONSTER_ID],
tests/unit/vtt/refusal-handling.test.ts:244:        combatant: REFERENCE_MONSTER_ID,
tests/unit/vtt/refusal-handling.test.ts:245:        boundary: { activeCombatant: REFERENCE_FIGHTER_ID, round: state.round },
tests/unit/vtt/refusal-handling.test.ts:249:          mover: REFERENCE_FIGHTER_ID,
tests/unit/vtt/refusal-handling.test.ts:254:            actor: REFERENCE_MONSTER_ID,
tests/unit/vtt/refusal-handling.test.ts:255:            target: REFERENCE_FIGHTER_ID,
tests/unit/vtt/refusal-handling.test.ts:273:    const action = { type: 'end_turn', actor: REFERENCE_FIGHTER_ID } as const;
tests/unit/assets/board-glyphs.test.ts:579:    doorObject('open-door', 'Open Oak Door', OPEN_DOOR, true),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:136:      object('open-door', 'Open Oak Door', 'door', 0, 2, false),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:271:      { column: 0, row: 2, name: 'Open Oak Door' },
src/vtt/handoff/fixtures/two-room.ts:102:      object('object:two-room-door', 'Oak Door', 'door', { column: 5, row: 4 }, { movement: true, lineOfSight: true, cover: 'total' }),
tests/unit/bridge/narration.test.ts:13:import { referenceEncounterSetup, REFERENCE_MONSTER_ID } from '../../../src/vtt/reference-encounter';
tests/unit/bridge/narration.test.ts:82:      damage: { target: REFERENCE_MONSTER_ID, amount: 9 },
tests/unit/bridge/narration.test.ts:91:      target: REFERENCE_MONSTER_ID,
tests/unit/bridge/narration.test.ts:99:      target: REFERENCE_MONSTER_ID,
tests/unit/bridge/narration.test.ts:106:      target: REFERENCE_MONSTER_ID,
tests/unit/tools/local-openai-conversation.SIMULATED.test.ts:270:      const fixtureState = await loadArenaFixture('tests/fixtures/arena-basis/seed-3943001.json');
src/vtt/reference-encounter.ts:71:export const REFERENCE_FIGHTER_ID = combatantId('combatant:fighter');
src/vtt/reference-encounter.ts:72:export const REFERENCE_CLERIC_ID = combatantId('combatant:cleric');
src/vtt/reference-encounter.ts:73:export const REFERENCE_WIZARD_ID = combatantId('combatant:wizard');
src/vtt/reference-encounter.ts:74:export const REFERENCE_MONSTER_ID = combatantId('combatant:training-brute');
src/vtt/reference-encounter.ts:75:export const REFERENCE_PLAYER_IDS: readonly CombatantId[] = [
src/vtt/reference-encounter.ts:76:  REFERENCE_FIGHTER_ID,
src/vtt/reference-encounter.ts:77:  REFERENCE_CLERIC_ID,
src/vtt/reference-encounter.ts:78:  REFERENCE_WIZARD_ID,
src/vtt/reference-encounter.ts:240:    targets: [REFERENCE_MONSTER_ID],
src/vtt/reference-encounter.ts:254:    if (actor === REFERENCE_MONSTER_ID) {
src/vtt/reference-encounter.ts:257:      if (minimumSpaceDistance(combatantSpace(state, actor), combatantSpace(state, REFERENCE_MONSTER_ID)) <= 5) {
src/vtt/reference-encounter.ts:258:        actions.push(weaponAttack(actor, REFERENCE_MONSTER_ID));
src/vtt/reference-encounter.ts:265:      if (actor === REFERENCE_CLERIC_ID) actions.push(sacredFlame(actor));
src/vtt/reference-encounter.ts:266:      if (actor === REFERENCE_WIZARD_ID) actions.push(...shatterActions(state, actor));
src/vtt/reference-encounter.ts:279:    reactor !== REFERENCE_FIGHTER_ID ||
src/vtt/reference-encounter.ts:280:    mover !== REFERENCE_MONSTER_ID ||
tests/unit/bridge/client.test.ts:19:  REFERENCE_CLERIC_ID,
tests/unit/bridge/client.test.ts:20:  REFERENCE_FIGHTER_ID,
tests/unit/bridge/client.test.ts:21:  REFERENCE_MONSTER_ID,
tests/unit/bridge/client.test.ts:22:  REFERENCE_WIZARD_ID,
tests/unit/bridge/client.test.ts:49:    for (const actor of [REFERENCE_FIGHTER_ID, REFERENCE_CLERIC_ID, REFERENCE_WIZARD_ID]) {
tests/unit/bridge/client.test.ts:52:    expect(state.activeCombatant).toBe(REFERENCE_MONSTER_ID);
tests/unit/bridge/client.test.ts:69:            monsterId: REFERENCE_MONSTER_ID,
tests/unit/bridge/client.test.ts:88:      livingMonsterIds: [REFERENCE_MONSTER_ID],
tests/unit/bridge/client.test.ts:90:    expect(host.snapshot().dm.encounter.activeCombatant).toBe(REFERENCE_FIGHTER_ID);
tests/unit/bridge/client.test.ts:101:    for (const actor of [REFERENCE_FIGHTER_ID, REFERENCE_CLERIC_ID, REFERENCE_WIZARD_ID]) {
tests/unit/bridge/client.test.ts:114:          commands: [{ type: 'end_turn', actor: REFERENCE_MONSTER_ID }],
tests/unit/vtt/summons.test.ts:104:    const fixtureState = started(content, [{ level: 3, maximum: 1 }]);
tests/unit/vtt/summons.test.ts:130:    const fixtureState = started(content);
tests/unit/vtt/summons.test.ts:146:    const fixtureState = started(loadedPack());
tests/unit/vtt/summons.test.ts:211:    const fixtureState = started(loadedPack());
tests/unit/vtt/summons.test.ts:291:    const fixtureState = started(loadedPack());
tests/unit/vtt/engine-host-integration.test.ts:9:  REFERENCE_CLERIC_ID,
tests/unit/vtt/engine-host-integration.test.ts:10:  REFERENCE_FIGHTER_ID,
tests/unit/vtt/engine-host-integration.test.ts:11:  REFERENCE_MONSTER_ID,
tests/unit/vtt/engine-host-integration.test.ts:12:  REFERENCE_PLAYER_IDS,
tests/unit/vtt/engine-host-integration.test.ts:13:  REFERENCE_WIZARD_ID,
tests/unit/vtt/engine-host-integration.test.ts:25:  return [...REFERENCE_PLAYER_IDS, REFERENCE_MONSTER_ID].map((combatantId) => ({
tests/unit/vtt/engine-host-integration.test.ts:27:    controllerId: `${combatantId}:${combatantId === REFERENCE_MONSTER_ID ? 'agent' : 'human'}:test`,
tests/unit/vtt/engine-host-integration.test.ts:28:    kind: combatantId === REFERENCE_MONSTER_ID ? 'agent' as const : 'human' as const,
tests/unit/vtt/engine-host-integration.test.ts:52:    const before = snapshot.dm.encounter.combatants.find((entry) => entry.id === REFERENCE_MONSTER_ID)?.hitPoints;
tests/unit/vtt/engine-host-integration.test.ts:62:      actorId: REFERENCE_MONSTER_ID,
tests/unit/vtt/engine-host-integration.test.ts:95:    expect(after.dm.encounter.combatants.find((entry) => entry.id === REFERENCE_MONSTER_ID)?.hitPoints)
tests/unit/vtt/engine-host-integration.test.ts:116:    for (const actor of [REFERENCE_FIGHTER_ID, REFERENCE_CLERIC_ID, REFERENCE_WIZARD_ID]) {
tests/unit/vtt/engine-host-integration.test.ts:145:    host.takeOverController(REFERENCE_MONSTER_ID);
tests/unit/vtt/engine-host-integration.test.ts:146:    await settleUntil(() => host.snapshot().dm.pendingRequest?.actorId === REFERENCE_MONSTER_ID);
tests/unit/vtt/engine-host-integration.test.ts:149:      combatantId: REFERENCE_MONSTER_ID,
tests/unit/vtt/engine-host-integration.test.ts:157:    host.handBackController(REFERENCE_MONSTER_ID);
tests/unit/vtt/engine-host-integration.test.ts:159:      combatantId: REFERENCE_MONSTER_ID,
tests/unit/vtt/engine-host-integration.test.ts:166:    host.submitHumanDecision(REFERENCE_MONSTER_ID, {
tests/unit/vtt/engine-host-integration.test.ts:169:      action: { type: 'end_turn', actor: REFERENCE_MONSTER_ID },
tests/unit/vtt/engine-host-integration.test.ts:172:      entry.combatantId === REFERENCE_MONSTER_ID && entry.kind === 'agent'));
tests/unit/tools/ai-dm-arena.test.ts:1327:    const fixtureState = createEncounter({
tests/unit/tools/ai-dm-arena.test.ts:1786:      const fixtureState = {
tests/unit/vtt/encounter-projections.test.ts:28:  REFERENCE_CLERIC_ID,
tests/unit/vtt/encounter-projections.test.ts:29:  REFERENCE_FIGHTER_ID,
tests/unit/vtt/encounter-projections.test.ts:30:  REFERENCE_MONSTER_ID,
tests/unit/vtt/encounter-projections.test.ts:31:  REFERENCE_PLAYER_IDS,
tests/unit/vtt/encounter-projections.test.ts:51:  const viewerId = REFERENCE_PLAYER_IDS.find((id) => id === state.activeCombatant) ?? REFERENCE_PLAYER_IDS[0];
tests/unit/vtt/encounter-projections.test.ts:56:    ownedCombatantIds: REFERENCE_PLAYER_IDS,
tests/unit/vtt/encounter-projections.test.ts:66:    target: REFERENCE_FIGHTER_ID,

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "sed -n '60,130p' src/vtt/reference-encounter.ts && rg -n \"function visible|traceTerrainLine|concealedCells|foggedCells\" src/combat/visibility.ts src/combat/cover.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
      contactMedium: 'surface',
      sizeCategory: 'Medium',
      ...(key === 'training-brute' ? { skillBonuses: { stealth: 20 } } : {}),
      spellSlots: options.spellSlots ?? [],
    },
  } as const;
  return kind === 'player_character'
    ? { ...common, kind, characterId: key === 'fighter' ? 1 : key === 'cleric' ? 2 : 3, wildShape: null }
    : { ...common, kind, statblockId: statblockId('statblock:training-brute') };
}

export const REFERENCE_FIGHTER_ID = combatantId('combatant:fighter');
export const REFERENCE_CLERIC_ID = combatantId('combatant:cleric');
export const REFERENCE_WIZARD_ID = combatantId('combatant:wizard');
export const REFERENCE_MONSTER_ID = combatantId('combatant:training-brute');
export const REFERENCE_PLAYER_IDS: readonly CombatantId[] = [
  REFERENCE_FIGHTER_ID,
  REFERENCE_CLERIC_ID,
  REFERENCE_WIZARD_ID,
];

export function referenceEncounterSetup(): {
  readonly bounds: { readonly columns: number; readonly rows: number };
  readonly combatants: readonly CombatantProfile[];
  readonly tokens: readonly CombatToken[];
  readonly blockedCells: readonly GridCell[];
  readonly environment: EncounterState['environment'];
  readonly dmNotes: readonly string[];
} {
  const fighter = profile('fighter', 'Reference Fighter', 'player_character', {
    maximumHp: 67,
    armor: 18,
    initiative: 100,
  });
  const cleric = profile('cleric', 'Reference Cleric', 'player_character', {
    maximumHp: 52,
    armor: 18,
    initiative: 70,
    spellSlots: referencePartySpellSlots('Cleric'),
  });
  const wizard = profile('wizard', 'Reference Wizard', 'player_character', {
    maximumHp: 38,
    armor: 15,
    initiative: 40,
    spellSlots: referencePartySpellSlots('Wizard'),
  });
  const monster = profile('training-brute', 'Training Brute', 'monster', {
    maximumHp: 80,
    armor: 12,
    initiative: 0,
  });
  const positions: readonly GridCell[] = [
    { column: 2, row: 3 },
    { column: 1, row: 4 },
    { column: 1, row: 2 },
    { column: 3, row: 3 },
  ];
  const combatants = [fighter, cleric, wizard, monster];
  return {
    bounds: { columns: 10, rows: 7 },
    combatants,
    tokens: combatants.map((combatant, index) => combatToken(combatant, positions[index] as GridCell)),
    blockedCells: [{ column: 7, row: 2 }],
    environment: {
      lightRegions: [{ id: 'reference-hiding-shadow', cells: [{ column: 4, row: 3 }], level: 'darkness' }],
      obscurementRegions: [],
      difficultTerrainRegions: [],
      movementRegions: [],
      narrowOpeningRegions: [],
    },
    dmNotes: ['Training Brute retreats after the three reference PCs act.'],
src/combat/cover.ts:795:export function traceTerrainLine(
src/combat/visibility.ts:93:  readonly foggedCells: readonly GridCell[];
src/combat/visibility.ts:288:  readonly concealedCells: readonly GridCell[];
src/combat/visibility.ts:315:    readonly foggedCells: readonly GridCell[];
src/combat/visibility.ts:524:      foggedCells: derivedFogCells(state, visibilityObserverIds),
src/combat/visibility.ts:650:    concealedCells: allCells(state.bounds).filter((cell) => concealed.has(cellKey(cell))),
src/combat/visibility.ts:735:      foggedCells: state.foggedCells.map((cell) => ({ ...cell })),

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "sed -n '480,680p' src/combat/visibility.ts && sed -n '620,840p' src/combat/cover.ts && sed -n '1,100p' /tmp/wall01-r4-generation.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
    }
    if (isDmOnlyEvent(event)) return [];
    if (
      event.type === 'effect_ended' ||
      event.type === 'effect_clock_ticked' ||
      event.type === 'effect_duration_extended'
    ) return [];
    if (event.type === 'initiative_ordered') {
      return [{
        ...event,
        order: event.order.filter((id) => visibleIds.has(id)),
        slots: event.slots
          .map((slot) => slot.filter((id) => visibleIds.has(id)))
          .filter((slot) => slot.length > 0),
      }];
    }
    if (event.type === 'adjudicated') {
      return visibleIds.has(event.target)
        ? [{ sequence: event.sequence, type: 'adjudicated', target: event.target, consequence: event.consequence }]
        : [];
    }
    return eventCombatants(event).every((id) => visibleIds.has(id)) ? [event] : [];
  });
}

/** The only canonical EncounterState -> DM projection function. */
export function projectDmView(state: EncounterState): DmView {
  const visibilityObserverIds = eligibleVisibilityObserverIds(state, { kind: 'dm_party' });
  return {
    audience: 'dm',
    state: {
      config: structuredClone(state.config),
      phase: structuredClone(state.phase),
      rulesEdition: state.rulesEdition,
      hiddenRolls: [...state.hiddenRolls],
      revision: state.revision,
      nextEventSequence: state.nextEventSequence,
      nextDecisionSequence: state.nextDecisionSequence,
      nextEffectSequence: state.nextEffectSequence,
      bounds: structuredClone(state.bounds),
      blockedCells: structuredClone(state.blockedCells),
      worldObjects: structuredClone(state.worldObjects),
      nextWorldObjectSequence: state.nextWorldObjectSequence,
      environment: structuredClone(state.environment),
      foggedCells: derivedFogCells(state, visibilityObserverIds),
      dmNotes: [...state.dmNotes],
      combatants: structuredClone(state.combatants),
      tokens: structuredClone(state.tokens),
      sharedSpaceRelations: structuredClone(state.sharedSpaceRelations),
      adjudicationPending: structuredClone(state.adjudicationPending),
      ...(state.absentTokens === undefined ? {} : { absentTokens: structuredClone(state.absentTokens) }),
      initiative: structuredClone(state.initiative),
      activeCombatant: state.activeCombatant,
      activeInitiativeIndex: state.activeInitiativeIndex,
      round: state.round,
      effects: structuredClone(state.effects),
      persistentAreas: structuredClone(state.persistentAreas),
      nextPersistentAreaSequence: state.nextPersistentAreaSequence,
      ...(state.reevaluatedBranches === undefined ? {} : { reevaluatedBranches: structuredClone(state.reevaluatedBranches) }),
      eventLog: structuredClone(state.eventLog),
      hiddenCombatants: structuredClone(state.hiddenCombatants),
      observationHistory: structuredClone(state.observationHistory),
      ...(state.searchMemories === undefined ? {} : { searchMemories: structuredClone(state.searchMemories) }),
      ...(state.alerting === undefined ? {} : { alerting: structuredClone(state.alerting) }),
      pendingDecisions: structuredClone(state.pendingDecisions),
      reactionPolicies: structuredClone(state.reactionPolicies),
      ...(state.contentPacks === undefined ? {} : { contentPacks: structuredClone(state.contentPacks) }),
      ...(state.equipment === undefined ? {} : { equipment: structuredClone(state.equipment) }),
      ...(state.groundItems === undefined ? {} : { groundItems: structuredClone(state.groundItems) }),
      ...(state.itemContacts === undefined ? {} : { itemContacts: structuredClone(state.itemContacts) }),
    },
  };
}

/** The only canonical EncounterState -> player projection function. */
export function projectPlayerView(state: EncounterState, binding: PlayerSeatBinding): PlayerView {
  if (!state.combatants.some((subject) => subject.profile.id === binding.combatantId)) {
    throw new UnknownPlayerSeatCombatantError(binding.seatId, binding.combatantId);
  }
  const ownedIds = new Set(binding.ownedCombatantIds ?? [binding.combatantId]);
  if (!ownedIds.has(binding.combatantId)) {
    throw new Error(`Player seat ${binding.seatId} does not own its visibility combatant.`);
  }
  const tokensByCombatant = new Map(state.tokens.map((token) => [token.combatantId, token] as const));
  const availableObservers = eligibleVisibilityObserverIds(state, {
    kind: 'player_seat',
    ownedIds,
  });
  const fog = new Set(derivedFogCells(state, availableObservers).map(cellKey));
  const hidden = new Set(state.hiddenCombatants.map((entry) => entry.combatant));
  // Hidden creature geometry is never read into a player-safe projection.
  const concealed = new Set(fog);
  const initiativeOrder = new Map(state.initiative.map((entry, index) =>
    [entry.combatant, index] as const));
  const combatants = state.combatants.flatMap((subject): readonly PlayerVisibleCombatant[] => {
    const pending = state.adjudicationPending.find((entry) =>
      entry.combatant === subject.profile.id);
    if (pending !== undefined) {
      if (!ownedIds.has(subject.profile.id)) return [];
      return [{
        id: subject.profile.id,
        name: subject.profile.name,
        kind: combatantSide(state, subject.profile.id),
        life: subject.life,
        placementStatus: 'placement_pending',
        pendingReason: pending.kind,
        active: state.activeCombatant === subject.profile.id,
        formName: subject.wildShape?.formName ?? subject.form?.formName ?? null,
        conditions: playerVisibleConditions(state, subject.profile.id, true),
      }];
    }
    const token = tokensByCombatant.get(subject.profile.id);
    if (token === undefined) return [];
    const owned = ownedIds.has(subject.profile.id);
    if (hidden.has(subject.profile.id) ||
      (!owned && !availableObservers.some((observer) =>
        detectCombatant(state, observer, subject.profile.id).kind === 'seen'))) return [];
    const space = combatantSpace(state, subject.profile.id);
    return [{
      id: subject.profile.id,
      name: subject.profile.name,
      kind: combatantSide(state, subject.profile.id),
      life: subject.life,
      placementStatus: 'placed',
      position: { ...token.position },
      effectiveSize: effectiveCreatureSize(state, subject.profile.id),
      placementMode: structuredClone(token.placementMode),
      footprint: space.cells.map((cell) => ({ ...cell })) as [GridCell, ...GridCell[]],
      active: state.activeCombatant === subject.profile.id,
      formName: subject.wildShape?.formName ?? subject.form?.formName ?? null,
      conditions: playerVisibleConditions(state, subject.profile.id, owned),
    }];
  }).sort((left, right) =>
    (initiativeOrder.get(left.id) ?? Number.MAX_SAFE_INTEGER) -
      (initiativeOrder.get(right.id) ?? Number.MAX_SAFE_INTEGER) ||
    String(left.id).localeCompare(String(right.id)));
  const visibleIds = new Set(combatants.map((subject) => subject.id));
  const lastSeen = state.combatants.flatMap((subject): readonly PlayerLastSeenCombatant[] => {
    if (subject.life === 'dead' || visibleIds.has(subject.profile.id) ||
      !tokensByCombatant.has(subject.profile.id)) return [];
    const latest = state.observationHistory
      .filter((entry) => ownedIds.has(entry.observer) && entry.subject === subject.profile.id)
      .sort((left, right) => right.revision - left.revision ||
        String(left.observer).localeCompare(String(right.observer)))[0];
    if (latest === undefined) return [];
    return [{
      id: subject.profile.id,
      name: subject.profile.name,
      kind: combatantSide(state, subject.profile.id),
      cell: { ...latest.cell },
      round: latest.round,
    }];
  }).sort((left, right) =>
    (initiativeOrder.get(left.id) ?? Number.MAX_SAFE_INTEGER) -
      (initiativeOrder.get(right.id) ?? Number.MAX_SAFE_INTEGER) ||
    String(left.id).localeCompare(String(right.id)));
  const monsterIds = new Set(state.combatants.flatMap((subject) =>
    subject.profile.kind === 'monster' ? [subject.profile.id] : []));
  return {
    audience: 'player',
    seat: {
      seatId: binding.seatId,
      combatantId: binding.combatantId,
      ...(binding.ownedCombatantIds === undefined ? {} : { ownedCombatantIds: [...binding.ownedCombatantIds] }),
    },
    revision: state.revision,
    round: state.round,
    activeCombatant: state.activeCombatant,
    bounds: { ...state.bounds },
    cells: allCells(state.bounds).filter((cell) => !concealed.has(cellKey(cell))),
    concealedCells: allCells(state.bounds).filter((cell) => concealed.has(cellKey(cell))),
    blockedCells: state.blockedCells.filter((cell) => !concealed.has(cellKey(cell))).map((cell) => ({ ...cell })),
    worldObjects: state.worldObjects
      .filter((object) => object.footprint.every((cell) => !concealed.has(cellKey(cell))))
      .map((object) => structuredClone(object)),
    combatants,
    lastSeen,
    ownedCombatants: state.combatants
      .filter((subject) => ownedIds.has(subject.profile.id))
      .map((subject) => ({
        id: subject.profile.id,
        hitPoints: subject.hitPoints,
        hitPointMaximum: effectiveCombatRules(state, subject.profile.id).hitPointMaximum,
        turn: structuredClone(subject.turn),
        wildShapeUses: structuredClone(subject.wildShapeUses),
      })),
    recentEvents: playerEvents(state.eventLog, visibleIds, new Set(state.hiddenRolls), monsterIds),
  };
}

/** Surface mapper; its input is already a DmView, never canonical state. */
export function dmVisibleEncounter(view: DmView): DmVisibleEncounterState {
  const state = view.state;
  const tokensByCombatant = new Map(state.tokens.map((token) => [token.combatantId, token] as const));
  return {
    viewer: 'dm',
    phase: structuredClone(state.phase),
    revision: state.revision,
    round: state.round,
    activeCombatant: state.activeCombatant,
    hiddenRolls: [...state.hiddenRolls],
  const physicalBlocksCover = lines.length === 4 && lines.every((line) => line.tier === 'total');
  const obstructed = lines.filter((line) => line.tier !== 'none');
  const strongestLineTier = obstructed.reduce<CoverTier>((strongest, line) =>
    coverRank(line.tier) > coverRank(strongest) ? line.tier : strongest, 'none');
  const tier = physicalBlocksCover
    ? 'total'
    : COVER_TIER_BY_RANK[Math.min(
        coverRank(countTier(obstructed.length)),
        coverRank(strongestLineTier),
        coverRank('three_quarters'),
      )] ?? 'none';
  const sources = tier === 'none'
    ? []
    : uniqueSources(obstructed
        .filter((line) => line.tier === strongestLineTier)
        .flatMap((line) => line.sources));
  const interveningCells = [...new Map(lines
    .flatMap((line) => line.interveningCells)
    .map((cell) => [cellKey(cell), cell] as const)).values()];
  const firstSightBlockingLine = blocksSight ? lines.find((line) => line.blocksSight) : undefined;
  return {
    sourceCell: { ...sourceCell },
    targetCell: { ...targetCell },
    sourceCorner: { ...sourceCorner },
    lines,
    interveningCells,
    tier,
    blocksSight,
    sourceIds: sources.map(taggedSourceId),
    sources,
    firstBlockingCell: firstSightBlockingLine?.firstBlockingCell ?? null,
  };
}

function comparePhysicalCornerTraces(left: TerrainLineTrace, right: TerrainLineTrace): number {
  return coverRank(left.tier) - coverRank(right.tier) ||
    left.sourceCorner.row - right.sourceCorner.row ||
    left.sourceCorner.column - right.sourceCorner.column;
}

function compareOpticalCornerTraces(left: TerrainLineTrace, right: TerrainLineTrace): number {
  return left.sourceCorner.row - right.sourceCorner.row ||
    left.sourceCorner.column - right.sourceCorner.column;
}

function nearestCells(
  sourceCells: readonly GridCell[],
  targetCells: readonly GridCell[],
): { readonly sourceCell: GridCell; readonly targetCell: GridCell } {
  const sources = [...sourceCells].sort(compareCells);
  const targets = [...targetCells].sort(compareCells);
  const firstSource = sources[0];
  const firstTarget = targets[0];
  if (firstSource === undefined || firstTarget === undefined) {
    throw new RangeError('A line query requires non-empty source and target spaces.');
  }
  let selectedSource = firstSource;
  let selectedTarget = firstTarget;
  let selectedDistance = Number.POSITIVE_INFINITY;
  for (const sourceCell of sources) {
    for (const targetCell of targets) {
      const distance = Math.max(
        Math.abs(sourceCell.column - targetCell.column),
        Math.abs(sourceCell.row - targetCell.row),
      );
      if (distance < selectedDistance) {
        selectedSource = sourceCell;
        selectedTarget = targetCell;
        selectedDistance = distance;
      }
    }
  }
  return { sourceCell: selectedSource, targetCell: selectedTarget };
}

function traceSpaces(
  state: EncounterState,
  sourceCells: readonly GridCell[],
  targetCells: readonly GridCell[],
  options: TerrainLineOptions,
): TerrainLineTrace {
  const sightBlockingCells = normalizedSightBlockingCells(options.additionalSightBlockingCells);
  const blockerSignature = sightBlockingSignature(sightBlockingCells);
  const traceKey = `${options.includeCreatures === true ? 'creatures' : 'terrain'}:${sourceCells
    .map(cellKey).join(';')}->${targetCells.map(cellKey).join(';')}#sight:${blockerSignature}`;
  const stateTraces = terrainLineTraces.get(state) ?? new Map<string, TerrainLineTrace>();
  const cached = stateTraces.get(traceKey);
  if (cached !== undefined) return cached;
  if (!terrainLineTraces.has(state)) terrainLineTraces.set(state, stateTraces);
  const nearest = nearestCells(sourceCells, targetCells);
  const exclusions = new Set([...sourceCells, ...targetCells].map(cellKey));
  const indexedSources = indexedTerrainSources(state, options.includeCreatures === true);
  const equivalentTraceKey = `${terrainSourceSignature(
    state,
    options.includeCreatures === true,
    indexedSources,
  )}\0${traceKey}`;
  const equivalent = equivalentTerrainLineTraces.get(equivalentTraceKey);
  if (equivalent !== undefined) {
    stateTraces.set(traceKey, equivalent);
    return equivalent;
  }
  const sourceEndpoints = outerCornerEndpoints(sourceCells);
  const targetEndpoints = outerCornerEndpoints(targetCells);
  const sourceCorners = sourceEndpoints.map((endpoint) => endpoint.point);
  const targetCorners = targetEndpoints.map((endpoint) => endpoint.point);
  const cornerColumns = [...sourceCorners, ...targetCorners].map((corner) => corner.column);
  const cornerRows = [...sourceCorners, ...targetCorners].map((corner) => corner.row);
  const minimumColumn = Math.min(...cornerColumns);
  const maximumColumn = Math.max(...cornerColumns);
  const minimumRow = Math.min(...cornerRows);
  const maximumRow = Math.max(...cornerRows);
  const candidateEntries = sightBlockingCells.length === 0
    ? [...indexedSources.values()]
    : [...new Map([
        ...indexedSources.values(),
        ...sightBlockingCells.map((cell) => ({ cell, sources: [] })),
      ].map((entry) => [cellKey(entry.cell), entry] as const)).values()];
  const candidates = candidateEntries
    .filter((entry) => !exclusions.has(cellKey(entry.cell)) &&
      entry.cell.column < maximumColumn && entry.cell.column + 1 > minimumColumn &&
      entry.cell.row < maximumRow && entry.cell.row + 1 > minimumRow)
    .map((entry) => entry.cell);
  const sightBlockingKeys = new Set([
    ...sightBlockingCells.map(cellKey),
    ...[...indexedSources]
      .filter(([, entry]) => entry.sources.some((source) => source.tier === 'total'))
      .map(([key]) => key),
  ]);
  const sightBlockingAt = (cell: GridCell): boolean => {
    const key = cellKey(cell);
    return !exclusions.has(key) && sightBlockingKeys.has(key);
  };
  const resolvedSourceEndpoints = resolveCornerEndpoints(sourceEndpoints, sightBlockingAt);
  const resolvedTargetEndpoints = resolveCornerEndpoints(targetEndpoints, sightBlockingAt);
  const traces = resolvedSourceEndpoints.map((sourceEndpoint) => aggregateCornerLines(
    sourceEndpoint.point,
    resolvedTargetEndpoints.map((targetEndpoint) => traceCornerLine(
      sourceEndpoint.point,
      targetEndpoint.point,
      sourceEndpoint.cellQuadrant,
      targetEndpoint.cellQuadrant,
      sourceEndpoint.blockingCell,
      targetEndpoint.blockingCell,
      candidates,
      (cell) => indexedSources.get(cellKey(cell))?.sources ?? [],
      sightBlockingAt,
    )),
    nearest.sourceCell,
    nearest.targetCell,
  ));
  const first = traces[0];
  if (first === undefined) throw new RangeError('A line query requires non-empty source and target spaces.');
  const physicalSelected = traces.reduce(
    (best, candidate) => comparePhysicalCornerTraces(candidate, best) < 0 ? candidate : best,
    first,
  );
  const blocksSight = traces.every((trace) => trace.blocksSight);
  const opticalSelected = blocksSight
    ? traces.reduce(
        (best, candidate) => compareOpticalCornerTraces(candidate, best) < 0 ? candidate : best,
        first,
      )
    : null;
  const selected: TerrainLineTrace = {
    ...physicalSelected,
    blocksSight,
    firstBlockingCell: opticalSelected?.firstBlockingCell ?? null,
  };
  stateTraces.set(traceKey, selected);
  retainEquivalentTrace(equivalentTraceKey, selected);
  return selected;
}

/** The canonical 2014 corner-to-corner trace for two bare grid cells. */
export function traceTerrainLine(
  state: EncounterState,
  sourceCell: GridCell,
  targetCell: GridCell,
  options: TerrainLineOptions = {},
): TerrainLineTrace {
  return traceSpaces(state, [sourceCell], [targetCell], options);
}

export interface CreatureLineOptions {
  readonly sourceAnchor?: GridCell;
  readonly additionalSightBlockingCells?: readonly GridCell[];
}

export function traceCombatantLine(
  state: EncounterState,
  sourceId: CombatantId,
  targetId: CombatantId,
  options: CreatureLineOptions = {},
): TerrainLineTrace {
  const blockerSignature = sightBlockingSignature(
    normalizedSightBlockingCells(options.additionalSightBlockingCells),
  );
  const traceKey = `${String(sourceId)}->${String(targetId)}@${options.sourceAnchor === undefined
    ? 'current'
    : cellKey(options.sourceAnchor)}#sight:${blockerSignature}`;
  const cached = combatantLineTraces.get(state)?.get(traceKey);
  if (cached !== undefined) return cached;
  const source = options.sourceAnchor === undefined
    ? combatantSpace(state, sourceId)
    : combatantSpaceAt(state, sourceId, options.sourceAnchor);
  const trace = traceSpaces(state, source.cells, combatantSpace(state, targetId).cells, {
    sourceId,
    targetId,
    includeCreatures: true,
    ...(options.additionalSightBlockingCells === undefined
      ? {}
      : { additionalSightBlockingCells: options.additionalSightBlockingCells }),
  });
  const stateTraces = combatantLineTraces.get(state) ?? new Map<string, TerrainLineTrace>();
  stateTraces.set(traceKey, trace);
  if (!combatantLineTraces.has(state)) combatantLineTraces.set(state, stateTraces);
  return trace;
}

export function traceCombatantLineToCells(
import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
import { generateRoom } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/room-generator';

const cases = [
  ...Array.from({ length: 12 }, (_unused, index) => ({
    seed: 3_943_001 + index,
    difficulty: 'standard' as const,
  })),
  ...Array.from({ length: 10 }, (_unused, index) => ({
    seed: 5_117_001 + index,
    difficulty: 'hard' as const,
  })),
  ...Array.from({ length: 10 }, (_unused, index) => ({
    seed: 6_203_001 + index,
    difficulty: 'brutal' as const,
  })),
  ...Array.from({ length: 10 }, (_unused, index) => ({
    seed: 6_206_001 + index,
    difficulty: 'brutal' as const,
  })),
];

const rows = cases.map(({ seed, difficulty }) => {
  const room = difficulty === 'standard' ? generateRoom(seed) : generateRoom(seed, { difficulty });
  return { seed, difficulty, sha256: sha256(canonicalJson(room)) };
});
console.log(JSON.stringify({ count: rows.length, aggregateSha256: sha256(canonicalJson(rows)), rows }));

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "rg -n \"function derivedFogCells|derivedFogCells\" src/combat/visibility.ts && sed -n '360,470p' src/combat/visibility.ts && rg -n \"function .*Visible|traceTerrainLine\" src/combat/visibility.ts | head -50" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
24:  derivedFogCells,
524:      foggedCells: derivedFogCells(state, visibilityObserverIds),
568:  const fog = new Set(derivedFogCells(state, availableObservers).map(cellKey));
    case 'combatant_returned_to_board':
    case 'combatant_summoned':
    case 'summoned_combatant_despawned':
    case 'object_interaction_spent':
    case 'item_dropped':
    case 'item_picked_up':
    case 'item_equipped':
    case 'item_stowed':
    case 'wild_shape_assumed':
    case 'wild_shape_reverted':
    case 'hide_resolved':
    case 'search_resolved':
    case 'pending_decision_queued':
    case 'pending_decision_resolved':
    case 'reaction_policy_auto_resolved': return [event.combatant];
    case 'legendary_action_used':
    case 'legendary_action_pool_refreshed':
    case 'legendary_action_window_closed':
    case 'legendary_resistance_used': return [event.combatant];
    case 'initiative_block_rolled': return event.combatants;
    case 'spell_component_consumed':
    case 'slow_spellcasting_checked': return [event.caster];
    case 'spell_cast':
    case 'sustained_effect_activated':
    case 'sustained_effect_triggered': return [event.caster, ...event.targets];
    case 'spell_utility_resolved':
    case 'composition_step_resolved': return [event.caster];
    case 'shared_outcome_resolved': return [event.caster, event.target];
    case 'reaction_declined': return [event.combatant, event.mover];
    case 'reaction_offered':
    case 'reaction_refused':
    case 'reaction_resolved': return [event.combatant];
    case 'spell_cast_intercepted': return [event.caster, event.reactor];
    case 'initiative_ordered': return event.order;
    case 'attack_resolved': return [event.actor, event.target];
    case 'ability_check_resolved': return [event.actor];
    case 'save_resolved': return [event.source, event.target];
    case 'damage_applied':
    case 'healing_applied': return [event.source, event.target];
    case 'effect_applied': return [event.source, ...event.targets];
    case 'condition_application_refused': return [event.source, event.target];
    case 'effect_target_removed': return [event.target];
    case 'effect_ended':
    case 'effect_clock_ticked':
    case 'effect_duration_extended':
    case 'persistent_area_ended':
    case 'world_object_created':
    case 'world_object_modified':
    case 'world_object_damaged':
    case 'world_object_removed':
    case 'environment_terrain_changed':
    case 'environment_light_changed':
      return 'actor' in event && event.actor !== null ? [event.actor] : [];
    case 'world_object_used': return [event.actor];
    case 'reinforcement_wave_deployed': return [event.calledBy, ...event.combatants];
    case 'conditional_joiners_deployed': return [event.leader, ...event.combatants];
    case 'persistent_area_created':
    case 'persistent_area_moved': return [event.owner];
    case 'persistent_area_membership_changed': return [...event.entered, ...event.exited];
    case 'persistent_area_triggered': return [event.target];
    case 'hidden_ended': return [event.combatant];
  }
}

function isDmOnlyEvent(
  event: EncounterEvent,
): event is Extract<EncounterEvent, { readonly visibility: 'dm_only' }> {
  return 'visibility' in event && event.visibility === 'dm_only';
}

function playerEvents(
  events: readonly EncounterEvent[],
  visibleIds: ReadonlySet<CombatantId>,
  hiddenRolls: ReadonlySet<HiddenRollCategory>,
  monsterIds: ReadonlySet<CombatantId>,
): readonly PlayerVisibleEncounterEvent[] {
  return events.flatMap((event): readonly PlayerVisibleEncounterEvent[] => {
    if (event.type === 'death_save_resolved') {
      if (!visibleIds.has(event.combatant)) return [];
      return hiddenRolls.has('death_saves')
        ? [{
            sequence: event.sequence,
            type: event.type,
            combatant: event.combatant,
            outcome: event.outcome,
            successes: event.successes,
            failures: event.failures,
            lifeState: event.lifeState,
            stableRecovery: event.stableRecovery,
            rollVisibility: 'dm_only',
          }]
        : [{ ...event, rollVisibility: 'player_visible' }];
    }
    if (event.type === 'attack_resolved') {
      if (!eventCombatants(event).every((id) => visibleIds.has(id))) return [];
      return hiddenRolls.has('monster_attack_rolls') && monsterIds.has(event.actor)
        ? [{
            sequence: event.sequence,
            type: event.type,
            actor: event.actor,
            target: event.target,
            outcome: event.attack.outcome,
            rollVisibility: 'dm_only',
          }]
        : [{ ...event, rollVisibility: 'player_visible' }];
    }
    if (event.type === 'save_resolved') {
      if (!eventCombatants(event).every((id) => visibleIds.has(id))) return [];
      return hiddenRolls.has('monster_saving_throws') && monsterIds.has(event.target)
        ? [{
            sequence: event.sequence,
131:function playerVisibleConditions(
179:export function isPlacedVisibleCombatant<Combatant extends PlayerVisibleCombatant>(
671:export function dmVisibleEncounter(view: DmView): DmVisibleEncounterState {

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "sed -n '1,45p' src/combat/visibility.ts && rg -n \"export function derivedFogCells|derivedFogCells\" src/combat" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
import type { CombatRulesProfile } from './combatant';
import type {
  DeathSaveState,
  EncounterCombatantState,
  EncounterState,
  LifeState,
  SpellSlotState,
  TurnResources,
} from './encounter';
import { combatantSide } from './allies';
import {
  combatantConditions,
  combatantSpace,
  effectiveCombatRules,
  effectiveCreatureSize,
} from './combat-rules';
import type { KnownCreatureSize, SerializedPlacementMode } from './creature-space';
import type { HiddenRollCategory } from './roll-visibility';
import type { EncounterEvent } from './events';
import type { GridCell } from './grid';
import type { CombatantId } from './values';
import {
  detectCombatant,
  derivedFogCells,
  eligibleVisibilityObserverIds,
} from './visibility-field';

export type EncounterViewClassification = 'dm_only' | 'player_visible' | 'per_seat';

/** D359's exhaustive projection-decision inventory. */
export const ENCOUNTER_VIEW_CLASSIFICATION = {
  config: 'player_visible',
  phase: 'dm_only',
  rulesEdition: 'player_visible',
  hiddenRolls: 'dm_only',
  revision: 'player_visible',
  nextEventSequence: 'dm_only',
  nextDecisionSequence: 'dm_only',
  nextEffectSequence: 'dm_only',
  bounds: 'player_visible',
  blockedCells: 'per_seat',
  worldObjects: 'per_seat',
  nextWorldObjectSequence: 'dm_only',
  environment: 'dm_only',
  dmNotes: 'dm_only',
src/combat/visibility.ts:24:  derivedFogCells,
src/combat/visibility.ts:524:      foggedCells: derivedFogCells(state, visibilityObserverIds),
src/combat/visibility.ts:568:  const fog = new Set(derivedFogCells(state, availableObservers).map(cellKey));
src/combat/visibility-field.ts:542:export function derivedFogCells(

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "sed -n '500,575p' src/combat/visibility-field.ts && sed -n '1,80p' src/combat/visibility-field.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:

  const observerBlinded = combatantConditions(state, observer)
    .some(({ name }) => name === 'Blinded');
  if (observerBlinded) return { kind: 'undetected', reason: 'obscured' };
  const subjectInvisible = combatantConditions(state, subject)
    .some(({ name }) => name === 'Invisible');
  const hidden = combatantIsHidden(state, subject);
  if (hidden && !hasTruesight) return { kind: 'undetected', reason: 'hidden' };
  if (subjectInvisible && !hasTruesight) return { kind: 'undetected', reason: 'invisible' };
  return sight.kind === 'visible'
    ? { kind: 'seen', sense: sight.sense }
    : { kind: 'undetected', reason: sight.reason };
}

export function cellsThatCanSee(
  state: EncounterState,
  subjectId: CombatantId,
): readonly GridCell[] {
  const subjectCells = combatantSpace(state, subjectId).cells;
  return Object.freeze(allCells(state).filter((origin) => {
    const context = virtualEvaluationContext(state, origin);
    return subjectCells.some((target) => evaluateCell(context, target).kind === 'visible');
  }).map((cell) => Object.freeze({ ...cell })));
}

export function eligibleVisibilityObserverIds(
  state: EncounterState,
  scope: VisibilityObserverScope,
): readonly CombatantId[] {
  return Object.freeze(state.combatants.flatMap((combatant) => {
    const id = combatant.profile.id;
    if (combatant.life !== 'living') return [];
    if (!state.tokens.some((token) => token.combatantId === id)) return [];
    if (state.adjudicationPending.some((entry) => entry.combatant === id)) return [];
    if (combatantConditions(state, id).some(({ name }) => name === 'Unconscious')) return [];
    if (scope.kind === 'dm_party') {
      return combatant.profile.kind === 'player_character' ? [id] : [];
    }
    return scope.ownedIds.has(id) ? [id] : [];
  }));
}

export function derivedFogCells(
  state: EncounterState,
  observerIds: readonly CombatantId[],
): readonly GridCell[] {
  const stateFog = derivedFogByState.get(state) ?? new Map<string, readonly GridCell[]>();
  const observerKey = observerIds.map(String).join('\u0000');
  const cached = stateFog.get(observerKey);
  if (cached !== undefined) return cached;
  const fog = Object.freeze(allCells(state)
    .filter((cell) => !observerIds.some((observerId) => {
      const outcome = evaluateCell(actualObserverContext(state, observerId), cell);
      return outcome.kind === 'visible';
    }))
    .sort(compareCells)
    .map((cell) => Object.freeze({ ...cell })));
  stateFog.set(observerKey, fog);
  if (!derivedFogByState.has(state)) derivedFogByState.set(state, stateFog);
  return fog;
}
import type { CombatSense } from './statblock';
import {
  combatantConditions,
  combatantSpace,
  effectiveCombatRules,
} from './combat-rules';
import {
  traceCombatantLineToCells,
  traceTerrainLine,
} from './cover';
import {
  minimumSpaceDistance,
  minimumSpaceDistanceToCells,
  type CreatureSpace,
  type KnownCreatureSize,
} from './creature-space';
import type { EncounterState } from './encounter';
import type { GridCell } from './grid';
import { affectedCells } from './templates';
import { terrainWallCells } from './terrain';
import type { CombatantId } from './values';
import { environmentLightAt } from './world-objects';

export type VisibilityGrade = 'seen' | 'seen_dim' | 'unseen';
export type SightSense =
  | 'normal_sight'
  | 'darkvision'
  | 'blindsight'
  | 'truesight'
  | 'devils_sight';

export type SightOutcome =
  | {
      readonly kind: 'visible';
      readonly grade: 'seen' | 'seen_dim';
      readonly sense: SightSense;
    }
  | {
      readonly kind: 'unseen';
      readonly reason: 'blocked' | 'obscured' | 'darkness' | 'out_of_range';
    };

export type DetectionResult =
  | {
      readonly kind: 'seen';
      readonly sense: 'normal_sight' | 'darkvision' | 'blindsight' | 'truesight' | 'devils_sight';
    }
  | {
      readonly kind: 'located';
      readonly sense: 'tremorsense' | 'web_sense';
    }
  | {
      readonly kind: 'undetected';
      readonly reason: 'blocked' | 'hidden' | 'invisible' | 'obscured' | 'darkness' | 'out_of_range';
    };

export interface VisibilityFieldCell {
  readonly cell: GridCell;
  readonly grade: VisibilityGrade;
}

export type VisibilityObserverScope =
  | { readonly kind: 'dm_party' }
  | {
      readonly kind: 'player_seat';
      readonly ownedIds: ReadonlySet<CombatantId>;
    };

interface ActualObserver {
  readonly kind: 'actual';
  readonly id: CombatantId;
  readonly space: CreatureSpace<KnownCreatureSize>;
  readonly senses: readonly CombatSense[];
  readonly blinded: boolean;
}

interface VirtualObserver {
  readonly kind: 'virtual';
  readonly cell: GridCell;
}

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "sed -n '840,900p' src/combat/cover.ts && sed -n '100,180p' src/combat/cover.ts && rg -n \"export interface TerrainLineTrace|interface TerrainLineTrace|lines:\" src/combat/cover.ts | head -40" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
export function traceCombatantLineToCells(
  state: EncounterState,
  sourceId: CombatantId,
  targetCells: readonly GridCell[],
  options: CreatureLineOptions = {},
): TerrainLineTrace {
  const blockerSignature = sightBlockingSignature(
    normalizedSightBlockingCells(options.additionalSightBlockingCells),
  );
  const targetSignature = targetCells.map(cellKey).join(';');
  const anchorSignature = options.sourceAnchor === undefined ? 'current' : cellKey(options.sourceAnchor);
  const traceKey = `${String(sourceId)}->cells:${targetSignature}@${anchorSignature}#sight:${blockerSignature}`;
  const cached = combatantLineTraces.get(state)?.get(traceKey);
  if (cached !== undefined) return cached;
  const source = options.sourceAnchor === undefined
    ? combatantSpace(state, sourceId)
    : combatantSpaceAt(state, sourceId, options.sourceAnchor);
  const trace = traceSpaces(state, source.cells, targetCells, {
    sourceId,
    includeCreatures: true,
    ...(options.additionalSightBlockingCells === undefined
      ? {}
      : { additionalSightBlockingCells: options.additionalSightBlockingCells }),
  });
  const stateTraces = combatantLineTraces.get(state) ?? new Map<string, TerrainLineTrace>();
  stateTraces.set(traceKey, trace);
  if (!combatantLineTraces.has(state)) combatantLineTraces.set(state, stateTraces);
  return trace;
}

export function coverTierBetweenObjects(
  objects: readonly WorldObject[],
  from: GridCell,
  to: GridCell,
): CoverTier {
  const exclusions = new Set([cellKey(from), cellKey(to)]);
  const candidates = [...new Map(objects
    .flatMap((object) => object.footprint)
    .filter((cell) => !exclusions.has(cellKey(cell)))
    .map((cell) => [cellKey(cell), cell] as const)).values()];
  const sourceCell = { ...from };
  const targetCell = { ...to };
  const sourceEndpoints = outerCornerEndpoints([from]);
  const targetEndpoints = outerCornerEndpoints([to]);
  const sourcesAt = (cell: GridCell): readonly TerrainLineSource[] =>
    objects.flatMap((object): readonly TerrainLineSource[] =>
      objectOccupiesCell(object, cell) && object.blocking.cover !== 'none'
        ? [{ kind: 'world_object', id: String(object.id), tier: object.blocking.cover }]
        : []);
  const sightBlockingAt = (cell: GridCell): boolean =>
    !exclusions.has(cellKey(cell)) && sourcesAt(cell).some((source) => source.tier === 'total');
  const resolvedSourceEndpoints = resolveCornerEndpoints(sourceEndpoints, sightBlockingAt);
  const resolvedTargetEndpoints = resolveCornerEndpoints(targetEndpoints, sightBlockingAt);
  const traces = resolvedSourceEndpoints.map((sourceEndpoint) => aggregateCornerLines(
    sourceEndpoint.point,
    resolvedTargetEndpoints.map((targetEndpoint) => traceCornerLine(
      sourceEndpoint.point,
      targetEndpoint.point,
      sourceEndpoint.cellQuadrant,
      targetEndpoint.cellQuadrant,
      sourceEndpoint.blockingCell,
    { point: topLeft, cellQuadrant: { column: 1, row: 1 } },
    { point: topRight, cellQuadrant: { column: -1, row: 1 } },
    { point: bottomLeft, cellQuadrant: { column: 1, row: -1 } },
    { point: bottomRight, cellQuadrant: { column: -1, row: -1 } },
  ];
}

/** A corner ray crosses only a cell interior; a boundary graze is not a crossing. */
export function cornerLineCrossesCell(from: CornerPoint, to: CornerPoint, cell: GridCell): boolean {
  const epsilon = 1e-9;
  let entry = 0;
  let exit = 1;
  const columnDelta = to.column - from.column;
  const minimumColumn = cell.column + epsilon;
  const maximumColumn = cell.column + 1 - epsilon;
  if (columnDelta === 0) {
    if (from.column <= minimumColumn || from.column >= maximumColumn) return false;
  } else {
    const first = (minimumColumn - from.column) / columnDelta;
    const second = (maximumColumn - from.column) / columnDelta;
    entry = Math.max(entry, Math.min(first, second));
    exit = Math.min(exit, Math.max(first, second));
  }
  const rowDelta = to.row - from.row;
  const minimumRow = cell.row + epsilon;
  const maximumRow = cell.row + 1 - epsilon;
  if (rowDelta === 0) {
    if (from.row <= minimumRow || from.row >= maximumRow) return false;
  } else {
    const first = (minimumRow - from.row) / rowDelta;
    const second = (maximumRow - from.row) / rowDelta;
    entry = Math.max(entry, Math.min(first, second));
    exit = Math.min(exit, Math.max(first, second));
  }
  return entry <= exit && exit > 0 && entry < 1;
}

function lineProgress(from: CornerPoint, to: CornerPoint, cell: GridCell): number {
  const columnDelta = to.column - from.column;
  const rowDelta = to.row - from.row;
  const magnitudeSquared = columnDelta * columnDelta + rowDelta * rowDelta;
  if (magnitudeSquared === 0) return 0;
  return (
    (cell.column + 0.5 - from.column) * columnDelta +
    (cell.row + 0.5 - from.row) * rowDelta
  ) / magnitudeSquared;
}

/** Rasterizes a corner ray in source-to-target traversal order. */
export function rasterizeCornerLine(
  from: CornerPoint,
  to: CornerPoint,
  candidates: readonly GridCell[],
): readonly GridCell[] {
  const crossed: GridCell[] = [];
  for (const cell of candidates) {
    if (cornerLineCrossesCell(from, to, cell)) crossed.push(cell);
  }
  if (crossed.length > 1) {
    crossed.sort((left, right) =>
      lineProgress(from, to, left) - lineProgress(from, to, right) || compareCells(left, right));
  }
  return crossed;
}

export interface TerrainLineSource {
  readonly kind: 'blocked_cell' | 'world_object' | 'creature';
  readonly id: string;
  readonly tier: Exclude<CoverTier, 'none'>;
}

export interface TerrainCornerLineTrace {
  readonly targetCorner: CornerPoint;
  readonly interveningCells: readonly GridCell[];
  readonly tier: CoverTier;
  readonly blocksSight: boolean;
  readonly sourceIds: readonly string[];
  readonly sources: readonly TerrainLineSource[];
  readonly firstBlockingCell: GridCell | null;
}

181:export interface TerrainLineTrace {
186:  readonly lines: readonly TerrainCornerLineTrace[];
615:  lines: readonly TerrainCornerLineTrace[],

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc "sed -n '175,205p' src/combat/cover.ts && sed -n '590,655p' src/combat/cover.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
  readonly blocksSight: boolean;
  readonly sourceIds: readonly string[];
  readonly sources: readonly TerrainLineSource[];
  readonly firstBlockingCell: GridCell | null;
}

export interface TerrainLineTrace {
  /** Stable nearest occupied cells retained for attack-position consumers, not cover geometry. */
  readonly sourceCell: GridCell;
  readonly targetCell: GridCell;
  readonly sourceCorner: CornerPoint;
  readonly lines: readonly TerrainCornerLineTrace[];
  readonly interveningCells: readonly GridCell[];
  readonly tier: CoverTier;
  readonly blocksSight: boolean;
  readonly sourceIds: readonly string[];
  readonly sources: readonly TerrainLineSource[];
  readonly firstBlockingCell: GridCell | null;
}

export interface TerrainLineOptions {
  readonly sourceId?: CombatantId;
  readonly targetId?: CombatantId;
  readonly includeCreatures?: boolean;
  readonly additionalSightBlockingCells?: readonly GridCell[];
}

function normalizedSightBlockingCells(cells: readonly GridCell[] = []): readonly GridCell[] {
  return [...new Map(cells.map((cell) => [cellKey(cell), { ...cell }] as const)).values()]
    .sort(compareCells);
}
    }
  }
  const sources = uniqueSources(strongest);
  return {
    targetCorner: { ...to },
    interveningCells,
    tier,
    blocksSight: firstBlockingCell !== null,
    sourceIds: sources.map(taggedSourceId),
    sources,
    firstBlockingCell,
  };
}

const COVER_TIER_BY_RANK = ['none', 'half', 'three_quarters', 'total'] as const satisfies readonly CoverTier[];

function countTier(obstructedLineCount: number): CoverTier {
  if (obstructedLineCount === 0) return 'none';
  if (obstructedLineCount <= 2) return 'half';
  if (obstructedLineCount === 3) return 'three_quarters';
  return 'total';
}

function aggregateCornerLines(
  sourceCorner: CornerPoint,
  lines: readonly TerrainCornerLineTrace[],
  sourceCell: GridCell,
  targetCell: GridCell,
): TerrainLineTrace {
  const blocksSight = lines.length === 4 && lines.every((line) => line.blocksSight);
  const physicalBlocksCover = lines.length === 4 && lines.every((line) => line.tier === 'total');
  const obstructed = lines.filter((line) => line.tier !== 'none');
  const strongestLineTier = obstructed.reduce<CoverTier>((strongest, line) =>
    coverRank(line.tier) > coverRank(strongest) ? line.tier : strongest, 'none');
  const tier = physicalBlocksCover
    ? 'total'
    : COVER_TIER_BY_RANK[Math.min(
        coverRank(countTier(obstructed.length)),
        coverRank(strongestLineTier),
        coverRank('three_quarters'),
      )] ?? 'none';
  const sources = tier === 'none'
    ? []
    : uniqueSources(obstructed
        .filter((line) => line.tier === strongestLineTier)
        .flatMap((line) => line.sources));
  const interveningCells = [...new Map(lines
    .flatMap((line) => line.interveningCells)
    .map((cell) => [cellKey(cell), cell] as const)).values()];
  const firstSightBlockingLine = blocksSight ? lines.find((line) => line.blocksSight) : undefined;
  return {
    sourceCell: { ...sourceCell },
    targetCell: { ...targetCell },
    sourceCorner: { ...sourceCorner },
    lines,
    interveningCells,
    tier,
    blocksSight,
    sourceIds: sources.map(taggedSourceId),
    sources,
    firstBlockingCell: firstSightBlockingLine?.firstBlockingCell ?? null,
  };
}

function comparePhysicalCornerTraces(left: TerrainLineTrace, right: TerrainLineTrace): number {
  return coverRank(left.tier) - coverRank(right.tier) ||

diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

apply patch
patch: completed
/tmp/wall01-r5-scene-proof.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..00106721b3c4cf4606a503392d6bda5ef35cf358
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,60 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..00106721b3c4cf4606a503392d6bda5ef35cf358
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,60 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc 'npx vite-node /tmp/wall01-r5-scene-proof.ts' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 3974ms:
{"observer":"combatant:fighter","sourceCell":{"column":2,"row":3},"sourceCorner":{"column":2,"row":3},"tier":"half","blocksSight":true,"first":{"column":7,"row":2},"rays":[{"target":{"column":7,"row":3},"tier":"none","blocksSight":true,"first":{"column":7,"row":2}},{"target":{"column":8,"row":3},"tier":"none","blocksSight":true,"first":{"column":7,"row":2}},{"target":{"column":7,"row":4},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":8,"row":4},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}}]}
{"observer":"combatant:cleric","sourceCell":{"column":1,"row":4},"sourceCorner":{"column":1,"row":4},"tier":"half","blocksSight":true,"first":{"column":6,"row":3},"rays":[{"target":{"column":7,"row":3},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":8,"row":3},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":7,"row":4},"tier":"none","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":8,"row":4},"tier":"none","blocksSight":true,"first":{"column":6,"row":3}}]}
{"observer":"combatant:wizard","sourceCell":{"column":1,"row":2},"sourceCorner":{"column":1,"row":3},"tier":"half","blocksSight":true,"first":{"column":7,"row":2},"rays":[{"target":{"column":7,"row":3},"tier":"none","blocksSight":true,"first":{"column":7,"row":2}},{"target":{"column":8,"row":3},"tier":"none","blocksSight":true,"first":{"column":7,"row":2}},{"target":{"column":7,"row":4},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":8,"row":4},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}}]}
{"dmFog":[{"column":4,"row":3},{"column":7,"row":3},{"column":8,"row":3},{"column":9,"row":3}],"fighterConcealed":[{"column":8,"row":2},{"column":9,"row":2},{"column":4,"row":3},{"column":7,"row":3},{"column":8,"row":3},{"column":9,"row":3},{"column":7,"row":4},{"column":8,"row":4},{"column":9,"row":4}]}

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..00106721b3c4cf4606a503392d6bda5ef35cf358
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,60 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc 'rg -n "function liesInClosedQuadrant|D657" -A15 -B3 src/combat/cover.ts' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
460-  return null;
461-}
462-
463:function liesInClosedQuadrant(
464-  columnDelta: number,
465-  rowDelta: number,
466-  ownCellQuadrant: CornerQuadrant,
467-): boolean {
468-  return columnDelta * ownCellQuadrant.column >= 0 &&
469-    rowDelta * ownCellQuadrant.row >= 0;
470-}
471-
472-function endpointSharedVertexBlockingHit(
473-  from: CornerPoint,
474-  to: CornerPoint,
475-  sourceCellQuadrant: CornerQuadrant,
476-  targetCellQuadrant: CornerQuadrant,
477-  sourceBlockingCell: GridCell | null,
478-  targetBlockingCell: GridCell | null,
--
487-      ? { cell: sourceBlockingCell, progress: 0 }
488-      : null;
489-  }
490:  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
491-  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
492-  if (sourceBlockingCell !== null &&
493-    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
494-    return { cell: sourceBlockingCell, progress: 0 };
495-  }
496-  if (targetBlockingCell !== null &&
497-    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
498-    return { cell: targetBlockingCell, progress: 1 };
499-  }
500-  return null;
501-}
502-
503-function cellInteriorEntryProgress(from: CornerPoint, to: CornerPoint, cell: GridCell): number {
504-  let entry = 0;
505-  const columnDelta = to.column - from.column;

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..00106721b3c4cf4606a503392d6bda5ef35cf358
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,60 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..00106721b3c4cf4606a503392d6bda5ef35cf358
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,60 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..48e5c6889f6e31ce8a1bbe2a48a78f6be2cf95e9
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,14 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  if (columnDelta === 0 || rowDelta === 0) return true;
+  return columnDelta * ownCellQuadrant.column >= 0 ||
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +488,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..00106721b3c4cf4606a503392d6bda5ef35cf358
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,60 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..48e5c6889f6e31ce8a1bbe2a48a78f6be2cf95e9
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,14 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  if (columnDelta === 0 || rowDelta === 0) return true;
+  return columnDelta * ownCellQuadrant.column >= 0 ||
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +488,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc 'npx vite-node /tmp/wall01-r5-scene-proof.ts' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 3997ms:
{"observer":"combatant:fighter","sourceCell":{"column":2,"row":3},"sourceCorner":{"column":2,"row":3},"tier":"half","blocksSight":false,"first":null,"rays":[{"target":{"column":7,"row":3},"tier":"none","blocksSight":false,"first":null},{"target":{"column":8,"row":3},"tier":"none","blocksSight":true,"first":{"column":7,"row":2}},{"target":{"column":7,"row":4},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":8,"row":4},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}}]}
{"observer":"combatant:cleric","sourceCell":{"column":1,"row":4},"sourceCorner":{"column":1,"row":4},"tier":"half","blocksSight":true,"first":{"column":6,"row":3},"rays":[{"target":{"column":7,"row":3},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":8,"row":3},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":7,"row":4},"tier":"none","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":8,"row":4},"tier":"none","blocksSight":true,"first":{"column":6,"row":3}}]}
{"observer":"combatant:wizard","sourceCell":{"column":1,"row":2},"sourceCorner":{"column":1,"row":3},"tier":"half","blocksSight":false,"first":null,"rays":[{"target":{"column":7,"row":3},"tier":"none","blocksSight":false,"first":null},{"target":{"column":8,"row":3},"tier":"none","blocksSight":true,"first":{"column":7,"row":2}},{"target":{"column":7,"row":4},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}},{"target":{"column":8,"row":4},"tier":"total","blocksSight":true,"first":{"column":6,"row":3}}]}
{"dmFog":[{"column":4,"row":3},{"column":8,"row":3},{"column":9,"row":3}],"fighterConcealed":[{"column":8,"row":2},{"column":9,"row":2},{"column":4,"row":3},{"column":8,"row":3},{"column":9,"row":3},{"column":7,"row":4},{"column":8,"row":4},{"column":9,"row":4}]}

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..00106721b3c4cf4606a503392d6bda5ef35cf358
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,60 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..48e5c6889f6e31ce8a1bbe2a48a78f6be2cf95e9
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,14 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  if (columnDelta === 0 || rowDelta === 0) return true;
+  return columnDelta * ownCellQuadrant.column >= 0 ||
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +488,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..00106721b3c4cf4606a503392d6bda5ef35cf358
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,60 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..00106721b3c4cf4606a503392d6bda5ef35cf358
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,60 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

codex
The moved cell is legitimate, not a defect: cell `(7,3)` was previously visible only through edge-aligned rays ending at sealed vertex `(7,3)`. For fighter `(2,3)` and wizard `(1,2)`, the decisive `(2,3)→(7,3)` / `(1,3)→(7,3)` rays changed from `tier none / false / null` to `tier none / true / first (7,2)`; the cleric was already blocked by the door. I’m applying only that hand-derived fog/concealment delta, then recomputing the persisted hashes from those exact bytes.
apply patch
patch: completed
/tmp/wall01-r5-scene-proof.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

exec
/bin/bash -lc 'npx vite-node /tmp/wall01-r5-scene-proof.ts | tail -2' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 4014ms:
{"dmFog":[{"column":4,"row":3},{"column":7,"row":3},{"column":8,"row":3},{"column":9,"row":3}],"fighterConcealed":[{"column":8,"row":2},{"column":9,"row":2},{"column":4,"row":3},{"column":7,"row":3},{"column":8,"row":3},{"column":9,"row":3},{"column":7,"row":4},{"column":8,"row":4},{"column":9,"row":4}]}
{"derivation":"fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged","pendingRequestHash":"28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda","coordinatorHash":"87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f","revisionChecksum":"9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a","branchRngStateFingerprint":"dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a"}

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/vtt/scene-snapshot.test.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "npx vitest run --configLoader runner tests/unit/vtt/scene-snapshot.test.ts --reporter=verbose --testNamePattern='preserves full persisted request/coordinator bytes and the actual revision checksum'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 3389ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > pins ground centres and inverse anchors for every controlled span
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > enriches filtered player combatants with renderer-only ids and omits environment lights
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > derives real Large, Huge, Gargantuan, and squeezed footprints and ground centres through the adapter
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > removes canonical token identity on a visible-to-hidden player transition
 ✓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > preserves full persisted request/coordinator bytes and the actual revision checksum 31ms
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > emits DM environment lights, exclusive door companions and no door cell wall union
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > uses the object-position north edge for adjacent and multicell doors, open and closed
 ↓ tests/unit/vtt/scene-snapshot.test.ts > renderer-neutral scene snapshot > reports deterministic role-aware fallbacks

 Test Files  1 passed (1)
      Tests  1 passed | 7 skipped (8)
   Start at  23:29:07
   Duration  3.09s (transform 1.12s, setup 0ms, import 1.63s, tests 32ms, environment 0ms)


diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "git diff -- src/combat/cover.ts tests/unit/combat/visibility-field.test.ts tests/unit/vtt/scene-snapshot.test.ts | sed -n '1,260p'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 52e68ffc..29a577f1 100644
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -44,6 +44,22 @@ export interface CornerPoint {
   readonly row: number;
 }
 
+type QuadrantDirection = -1 | 1;
+
+interface CornerQuadrant {
+  readonly column: QuadrantDirection;
+  readonly row: QuadrantDirection;
+}
+
+interface CornerEndpoint {
+  readonly point: CornerPoint;
+  readonly cellQuadrant: CornerQuadrant;
+}
+
+interface ResolvedCornerEndpoint extends CornerEndpoint {
+  readonly blockingCell: GridCell | null;
+}
+
 /** Row-major outer corners: top-left, top-right, bottom-left, bottom-right. */
 export function outerCorners(cells: readonly GridCell[]): readonly CornerPoint[] {
   const first = cells[0];
@@ -68,6 +84,26 @@ export function outerCorners(cells: readonly GridCell[]): readonly CornerPoint[]
   ];
 }
 
+function outerCornerEndpoints(cells: readonly GridCell[]): readonly CornerEndpoint[] {
+  const corners = outerCorners(cells);
+  const topLeft = corners[0];
+  const topRight = corners[1];
+  const bottomLeft = corners[2];
+  const bottomRight = corners[3];
+  if (topLeft === undefined || topRight === undefined ||
+    bottomLeft === undefined || bottomRight === undefined) {
+    throw new RangeError('A line query requires four outer corners.');
+  }
+  // The quadrant names the occupied space relative to its outer corner:
+  // top-left -> BR, top-right -> BL, bottom-left -> TR, bottom-right -> TL.
+  return [
+    { point: topLeft, cellQuadrant: { column: 1, row: 1 } },
+    { point: topRight, cellQuadrant: { column: -1, row: 1 } },
+    { point: bottomLeft, cellQuadrant: { column: 1, row: -1 } },
+    { point: bottomRight, cellQuadrant: { column: -1, row: -1 } },
+  ];
+}
+
 /** A corner ray crosses only a cell interior; a boundary graze is not a crossing. */
 export function cornerLineCrossesCell(from: CornerPoint, to: CornerPoint, cell: GridCell): boolean {
   const epsilon = 1e-9;
@@ -357,6 +393,51 @@ function greatestCommonDivisor(left: number, right: number): number {
   return dividend;
 }
 
+function sharedVertexBlockingCell(
+  column: number,
+  row: number,
+  sightBlockingAt: (cell: GridCell) => boolean,
+): GridCell | null {
+  const topLeft = { column: column - 1, row: row - 1 };
+  const topRight = { column, row: row - 1 };
+  // Check TL/BR first so lower row then lower column wins when both pairs seal.
+  if (sightBlockingAt(topLeft) && sightBlockingAt({ column, row })) return topLeft;
+  if (sightBlockingAt(topRight) && sightBlockingAt({ column: column - 1, row })) return topRight;
+  return null;
+}
+
+function endpointSharedVertexBlockingCell(
+  column: number,
+  row: number,
+  ownCellQuadrant: CornerQuadrant,
+  sightBlockingAt: (cell: GridCell) => boolean,
+): GridCell | null {
+  // The endpoint's own cell is excluded from blocking, so only the other diagonal pair can seal.
+  if (ownCellQuadrant.column === ownCellQuadrant.row) {
+    const topRight = { column, row: row - 1 };
+    return sightBlockingAt(topRight) && sightBlockingAt({ column: column - 1, row })
+      ? topRight
+      : null;
+  }
+  const topLeft = { column: column - 1, row: row - 1 };
+  return sightBlockingAt(topLeft) && sightBlockingAt({ column, row }) ? topLeft : null;
+}
+
+function resolveCornerEndpoints(
+  endpoints: readonly CornerEndpoint[],
+  sightBlockingAt: (cell: GridCell) => boolean,
+): readonly ResolvedCornerEndpoint[] {
+  return endpoints.map((endpoint) => ({
+    ...endpoint,
+    blockingCell: endpointSharedVertexBlockingCell(
+      endpoint.point.column,
+      endpoint.point.row,
+      endpoint.cellQuadrant,
+      sightBlockingAt,
+    ),
+  }));
+}
+
 function firstSharedVertexBlockingHit(
   from: CornerPoint,
   to: CornerPoint,
@@ -369,19 +450,52 @@ function firstSharedVertexBlockingHit(
   if (latticeSteps <= 1) return null;
   const columnStep = columnDelta / latticeSteps;
   const rowStep = rowDelta / latticeSteps;
-  // Indices exclude t=0 and t=1: endpoint vertices are governed only by cell-interior crossing.
+  // Strict-interior vertices exclude t=0 and t=1; the direction-aware endpoint rule handles those separately.
   for (let index = 1; index < latticeSteps; index += 1) {
     const column = from.column + columnStep * index;
     const row = from.row + rowStep * index;
-    const topLeft = { column: column - 1, row: row - 1 };
-    const topRight = { column, row: row - 1 };
-    // Check TL/BR first so lower row then lower column wins when both pairs seal.
-    if (sightBlockingAt(topLeft) && sightBlockingAt({ column, row })) {
-      return { cell: topLeft, progress: index / latticeSteps };
-    }
-    if (sightBlockingAt(topRight) && sightBlockingAt({ column: column - 1, row })) {
-      return { cell: topRight, progress: index / latticeSteps };
-    }
+    const cell = sharedVertexBlockingCell(column, row, sightBlockingAt);
+    if (cell !== null) return { cell, progress: index / latticeSteps };
+  }
+  return null;
+}
+
+function liesInClosedQuadrant(
+  columnDelta: number,
+  rowDelta: number,
+  ownCellQuadrant: CornerQuadrant,
+): boolean {
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
+}
+
+function endpointSharedVertexBlockingHit(
+  from: CornerPoint,
+  to: CornerPoint,
+  sourceCellQuadrant: CornerQuadrant,
+  targetCellQuadrant: CornerQuadrant,
+  sourceBlockingCell: GridCell | null,
+  targetBlockingCell: GridCell | null,
+): SightBlockingHit | null {
+  if (![from.column, from.row, to.column, to.row].every(Number.isInteger)) return null;
+  const columnDelta = to.column - from.column;
+  const rowDelta = to.row - from.row;
+  if (columnDelta === 0 && rowDelta === 0) {
+    const oppositeSpaces = sourceCellQuadrant.column === -targetCellQuadrant.column &&
+      sourceCellQuadrant.row === -targetCellQuadrant.row;
+    return sourceBlockingCell !== null && oppositeSpaces
+      ? { cell: sourceBlockingCell, progress: 0 }
+      : null;
+  }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
+  if (sourceBlockingCell !== null &&
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    return { cell: sourceBlockingCell, progress: 0 };
+  }
+  if (targetBlockingCell !== null &&
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
 }
@@ -408,10 +522,24 @@ function cellInteriorEntryProgress(from: CornerPoint, to: CornerPoint, cell: Gri
 function firstSightBlockingCell(
   from: CornerPoint,
   to: CornerPoint,
+  sourceCellQuadrant: CornerQuadrant,
+  targetCellQuadrant: CornerQuadrant,
+  sourceEndpointBlockingCell: GridCell | null,
+  targetEndpointBlockingCell: GridCell | null,
   interveningCells: readonly GridCell[],
   sightBlockingAt: (cell: GridCell) => boolean,
 ): GridCell | null {
   let first = firstSharedEdgeBlockingHit(from, to, sightBlockingAt);
+  const endpoint = endpointSharedVertexBlockingHit(
+    from,
+    to,
+    sourceCellQuadrant,
+    targetCellQuadrant,
+    sourceEndpointBlockingCell,
+    targetEndpointBlockingCell,
+  );
+  if (endpoint !== null && (first === null || endpoint.progress < first.progress ||
+    (endpoint.progress === first.progress && compareCells(endpoint.cell, first.cell) < 0))) first = endpoint;
   const vertex = firstSharedVertexBlockingHit(from, to, sightBlockingAt);
   if (vertex !== null && (first === null || vertex.progress < first.progress ||
     (vertex.progress === first.progress && compareCells(vertex.cell, first.cell) < 0))) first = vertex;
@@ -421,7 +549,7 @@ function firstSightBlockingCell(
     if (first === null || hit.progress < first.progress ||
       (hit.progress === first.progress && compareCells(hit.cell, first.cell) < 0)) first = hit;
   }
-  // One traversal merges cell-interior, shared-edge and strict-interior-vertex events by segment progress.
+  // One traversal merges cell-interior, shared-edge, endpoint and strict-interior-vertex events by progress.
   // Simultaneous events use lower row, then lower column.
   return first === null ? null : { ...first.cell };
 }
@@ -429,6 +557,10 @@ function firstSightBlockingCell(
 function traceCornerLine(
   from: CornerPoint,
   to: CornerPoint,
+  sourceCellQuadrant: CornerQuadrant,
+  targetCellQuadrant: CornerQuadrant,
+  sourceEndpointBlockingCell: GridCell | null,
+  targetEndpointBlockingCell: GridCell | null,
   candidates: readonly GridCell[],
   sourcesAt: (cell: GridCell) => readonly TerrainLineSource[],
   sightBlockingAt: (cell: GridCell) => boolean,
@@ -439,6 +571,10 @@ function traceCornerLine(
   const firstBlockingCell = firstSightBlockingCell(
     from,
     to,
+    sourceCellQuadrant,
+    targetCellQuadrant,
+    sourceEndpointBlockingCell,
+    targetEndpointBlockingCell,
     interveningCells,
     sightBlockingAt,
   );
@@ -583,8 +719,10 @@ function traceSpaces(
     stateTraces.set(traceKey, equivalent);
     return equivalent;
   }
-  const sourceCorners = outerCorners(sourceCells);
-  const targetCorners = outerCorners(targetCells);
+  const sourceEndpoints = outerCornerEndpoints(sourceCells);
+  const targetEndpoints = outerCornerEndpoints(targetCells);
+  const sourceCorners = sourceEndpoints.map((endpoint) => endpoint.point);
+  const targetCorners = targetEndpoints.map((endpoint) => endpoint.point);
   const cornerColumns = [...sourceCorners, ...targetCorners].map((corner) => corner.column);
   const cornerRows = [...sourceCorners, ...targetCorners].map((corner) => corner.row);
   const minimumColumn = Math.min(...cornerColumns);
@@ -612,11 +750,17 @@ function traceSpaces(
     const key = cellKey(cell);
     return !exclusions.has(key) && sightBlockingKeys.has(key);
   };
-  const traces = sourceCorners.map((sourceCorner) => aggregateCornerLines(
-    sourceCorner,
-    targetCorners.map((targetCorner) => traceCornerLine(
-      sourceCorner,
-      targetCorner,
+  const resolvedSourceEndpoints = resolveCornerEndpoints(sourceEndpoints, sightBlockingAt);
+  const resolvedTargetEndpoints = resolveCornerEndpoints(targetEndpoints, sightBlockingAt);
+  const traces = resolvedSourceEndpoints.map((sourceEndpoint) => aggregateCornerLines(
+    sourceEndpoint.point,
+    resolvedTargetEndpoints.map((targetEndpoint) => traceCornerLine(
+      sourceEndpoint.point,
+      targetEndpoint.point,
+      sourceEndpoint.cellQuadrant,
+      targetEndpoint.cellQuadrant,
+      sourceEndpoint.blockingCell,
+      targetEndpoint.blockingCell,
       candidates,
       (cell) => indexedSources.get(cellKey(cell))?.sources ?? [],

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..48e5c6889f6e31ce8a1bbe2a48a78f6be2cf95e9
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,14 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  if (columnDelta === 0 || rowDelta === 0) return true;
+  return columnDelta * ownCellQuadrant.column >= 0 ||
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +488,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..48e5c6889f6e31ce8a1bbe2a48a78f6be2cf95e9
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,14 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  if (columnDelta === 0 || rowDelta === 0) return true;
+  return columnDelta * ownCellQuadrant.column >= 0 ||
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +488,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=dot --testNamePattern='ADJACENT_CORNER_AGGREGATE_BLOCKED|EDGE_ARRIVAL_AT_JUNCTION_BLOCKED|FAR_FACE_DEPARTURE_BLOCKED'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 3062ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
-----------------------------------xx-x---------------

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 3 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > ADJACENT_CORNER_AGGREGATE_BLOCKED
AssertionError: expected { …(10) } to match object { tier: 'none', …(2) }
(13 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": true,
-   "firstBlockingCell": {
-     "column": 1,
-     "row": 1,
-   },
+   "blocksSight": false,
+   "firstBlockingCell": null,
    "tier": "none",
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1207:19
    1205|     // and BR (2,2). The last r4-clear rays end at the pinch along its…
    1206|     // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the ag…
    1207|     expect(trace).toMatchObject({
       |                   ^
    1208|       tier: 'none',
    1209|       blocksSight: true,

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/3]⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > EDGE_ARRIVAL_AT_JUNCTION_BLOCKED
AssertionError: expected { …(7) } to match object { blocksSight: true, …(1) }
(7 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": true,
-   "firstBlockingCell": {
-     "column": 1,
-     "row": 1,
-   },
+   "blocksSight": false,
+   "firstBlockingCell": null,
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1231:29
    1229|     expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
    1230|     expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 }…
    1231|     expect(verticalArrival).toMatchObject({ blocksSight: true, firstBl…
       |                             ^
    1232|     expect(horizontalArrival).toMatchObject({ blocksSight: true, first…
    1233|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/3]⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > FAR_FACE_DEPARTURE_BLOCKED
AssertionError: expected { …(7) } to match object { blocksSight: true, …(1) }
(7 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": true,
-   "firstBlockingCell": {
-     "column": 1,
-     "row": 1,
-   },
+   "blocksSight": false,
+   "firstBlockingCell": null,
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1270:21
    1268|     // to departure (2,2)->(0,2), lies left of closed TR on the wall's…
    1269|     expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
    1270|     expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCe…
       |                     ^
    1271|   });
    1272|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/3]⎯


 Test Files  1 failed (1)
      Tests  3 failed | 51 skipped (54)
   Start at  23:29:21
   Duration  2.76s (transform 872ms, setup 0ms, import 1.31s, tests 18ms, environment 0ms)


diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..48e5c6889f6e31ce8a1bbe2a48a78f6be2cf95e9
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,14 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  if (columnDelta === 0 || rowDelta === 0) return true;
+  return columnDelta * ownCellQuadrant.column >= 0 ||
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +488,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc 'rg -n "SAME_SIDE|OWN_EDGE|PHYSICAL_COVER" tests/unit/combat/visibility-field.test.ts' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
1141:  it('SAME_SIDE_ENDPOINT_GRAZE_CLEAR', () => {
1235:  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
1426:  it('PHYSICAL_COVER_UNCHANGED_BY_VERTEX', () => {
1454:  it('PHYSICAL_COVER_UNCHANGED_BY_SEAM', () => {

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..653393e4bd06c5b85bed6cb4cb8de591da73a703
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,12 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return false;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +486,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..653393e4bd06c5b85bed6cb4cb8de591da73a703
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,12 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return false;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +486,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=dot --testNamePattern='OWN_EDGE_DEPARTURE_CLEAR|SAME_SIDE_ENDPOINT_GRAZE_CLEAR'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 3066ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
---------------------------------x---x----------------

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > SAME_SIDE_ENDPOINT_GRAZE_CLEAR
AssertionError: expected { …(7) } to match object { blocksSight: false, …(1) }
(7 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": false,
-   "firstBlockingCell": null,
+   "blocksSight": true,
+   "firstBlockingCell": {
+     "column": 1,
+     "row": 1,
+   },
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1159:30
    1157|     // Mirrored ray (5,1)->(2,2) arrives from the target cell's own TR…
    1158|     // opposite BL quadrant. Pin it directly so an indiscriminate seal…
    1159|     expect(sameSideEndpoint).toMatchObject({ blocksSight: false, first…
       |                              ^
    1160|   });
    1161|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > OWN_EDGE_DEPARTURE_CLEAR
AssertionError: expected { …(7) } to match object { blocksSight: false, …(1) }
(7 matching properties omitted from actual)

- Expected
+ Received

  {
-   "blocksSight": false,
-   "firstBlockingCell": null,
+   "blocksSight": true,
+   "firstBlockingCell": {
+     "column": 1,
+     "row": 1,
+   },
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1251:21
    1249|     // to departure (2,2)->(2,0), lies on closed TR's vertical own-edg…
    1250|     expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
    1251|     expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingC…
       |                     ^
    1252|   });
    1253|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


 Test Files  1 failed (1)
      Tests  2 failed | 52 skipped (54)
   Start at  23:29:39
   Duration  2.77s (transform 878ms, setup 0ms, import 1.31s, tests 12ms, environment 0ms)


diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..653393e4bd06c5b85bed6cb4cb8de591da73a703
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,12 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return false;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +486,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "sed -n '550,615p' src/combat/cover.ts && sed -n '1400,1450p' tests/unit/combat/visibility-field.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
      (hit.progress === first.progress && compareCells(hit.cell, first.cell) < 0)) first = hit;
  }
  // One traversal merges cell-interior, shared-edge, endpoint and strict-interior-vertex events by progress.
  // Simultaneous events use lower row, then lower column.
  return first === null ? null : { ...first.cell };
}

function traceCornerLine(
  from: CornerPoint,
  to: CornerPoint,
  sourceCellQuadrant: CornerQuadrant,
  targetCellQuadrant: CornerQuadrant,
  sourceEndpointBlockingCell: GridCell | null,
  targetEndpointBlockingCell: GridCell | null,
  candidates: readonly GridCell[],
  sourcesAt: (cell: GridCell) => readonly TerrainLineSource[],
  sightBlockingAt: (cell: GridCell) => boolean,
): TerrainCornerLineTrace {
  const interveningCells = rasterizeCornerLine(from, to, candidates);
  let tier: CoverTier = 'none';
  let strongest: TerrainLineSource[] = [];
  const firstBlockingCell = firstSightBlockingCell(
    from,
    to,
    sourceCellQuadrant,
    targetCellQuadrant,
    sourceEndpointBlockingCell,
    targetEndpointBlockingCell,
    interveningCells,
    sightBlockingAt,
  );
  for (const cell of interveningCells) {
    for (const source of sourcesAt(cell)) {
      const comparison = coverRank(source.tier) - coverRank(tier);
      if (comparison > 0) {
        tier = source.tier;
        strongest = [source];
      } else if (comparison === 0) {
        strongest.push(source);
      }
    }
  }
  const sources = uniqueSources(strongest);
  return {
    targetCorner: { ...to },
    interveningCells,
    tier,
    blocksSight: firstBlockingCell !== null,
    sourceIds: sources.map(taggedSourceId),
    sources,
    firstBlockingCell,
  };
}

const COVER_TIER_BY_RANK = ['none', 'half', 'three_quarters', 'total'] as const satisfies readonly CoverTier[];

function countTier(obstructedLineCount: number): CoverTier {
  if (obstructedLineCount === 0) return 'none';
  if (obstructedLineCount <= 2) return 'half';
  if (obstructedLineCount === 3) return 'three_quarters';
  return 'total';
}

function aggregateCornerLines(
  sourceCorner: CornerPoint,
  lines: readonly TerrainCornerLineTrace[],
      bounds: { columns: 4, rows: 4 },
    });
    const trace = traceTerrainLine(
      state,
      { column: 0, row: 0 },
      { column: 2, row: 2 },
      { additionalSightBlockingCells: blockers },
    );
    const sealedRay = trace.lines.find((line) =>
      line.targetCorner.column === 3 && line.targetCorner.row === 3);

    // Original pin: (0,0)->(3,3) reaches vertex (1,1) between TR (1,0) and BL (0,1).
    // D642 seals that exact sight-only ray and picks lower-row cell (1,0); physical tier stays None.
    expect(rasterizeCornerLine({ column: 0, row: 0 }, { column: 3, row: 3 }, blockers)).toEqual([]);
    expect(sealedRay).toMatchObject({
      tier: 'none',
      blocksSight: true,
      firstBlockingCell: { column: 1, row: 0 },
    });
    // D569 endpoint normalization: before D654 the aggregate was clear/null because endpoint vertex
    // (1,1) was skipped. It is the source cell's bottom-right corner, so that cell lies TL of the vertex;
    // all four rays from (1,1) to target corners (2,2)..(3,3) leave into opposite BR through the
    // TR (1,0) + BL (0,1) pinch. That last clear source-corner route is now blocked at lower-row (1,0).
    expect(trace).toMatchObject({ tier: 'none', blocksSight: true, firstBlockingCell: { column: 1, row: 0 } });
  });

  it('PHYSICAL_COVER_UNCHANGED_BY_VERTEX', () => {
    const actor = normalProfile('diagonal-cover-actor');
    const state = fieldState(actor, {
      bounds: { columns: 5, rows: 5 },
      position: { column: 4, row: 4 },
      blockedCells: [{ column: 2, row: 1 }, { column: 1, row: 2 }],
    });
    const trace = traceTerrainLine(state, { column: 0, row: 0 }, { column: 3, row: 3 });

    // Pre-D642 physical pin: interior crossings obstruct two target-corner rays, yielding Half Cover.
    expect(trace.tier).toBe('half');

    const endpointState = fieldState(actor, {
      bounds: { columns: 6, rows: 6 },
      blockedCells: [
        { column: 1, row: 0 }, { column: 1, row: 1 },
        { column: 2, row: 2 }, { column: 2, row: 3 },
        { column: 2, row: 4 }, { column: 2, row: 5 },
      ],
    });
    const endpointTrace = traceTerrainLine(endpointState, { column: 0, row: 3 }, { column: 2, row: 1 });
    const endpointLine = endpointTrace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);
    // Ray (0,4)->(2,2) is optically stopped at the TL (1,1) + BR (2,2) endpoint pinch,
    // but it crosses no cell interior; the new sight hit therefore leaves that line at None.

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "sed -n '1450,1475p' tests/unit/combat/visibility-field.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
    // but it crosses no cell interior; the new sight hit therefore leaves that line at None.
    expect(endpointLine).toMatchObject({ tier: 'none', blocksSight: true });
  });

  it('PHYSICAL_COVER_UNCHANGED_BY_SEAM', () => {
    const { state } = seamState(true, true);
    const trace = traceTerrainLine(state, { column: 3, row: 4 }, { column: 7, row: 4 });

    // Pre-change pin: two of four target-corner rays cross total-cover cells, producing half cover.
    expect(trace.tier).toBe('half');
  });

  it('EMPTY_SIGHT_BLOCKERS_PRESERVE_FULL_TRACE including caches and a multi-cell footprint', () => {
    const actor = normalProfile('empty-blocker-actor');
    const coverCases = [
      {
        bounds: { columns: 6, rows: 6 },
        to: { column: 4, row: 2 },
        blockedCells: [] as readonly GridCell[],
        expected: 'none',
      },
      {
        bounds: { columns: 6, rows: 2 },
        to: { column: 4, row: 0 },
        blockedCells: [{ column: 2, row: 0 }],
        expected: 'half',

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..e4516b1cfa1e8a1161a1a14efcdbd83d91a259bb
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
@@ -588,6 +589,14 @@
       }
     }
   }
+  if (endpointSharedVertexBlockingHit(
+    from,
+    to,
+    sourceCellQuadrant,
+    targetCellQuadrant,
+    sourceEndpointBlockingCell,
+    targetEndpointBlockingCell,
+  ) !== null) tier = 'total';
   const sources = uniqueSources(strongest);
   return {
     targetCorner: { ...to },
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..e4516b1cfa1e8a1161a1a14efcdbd83d91a259bb
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
@@ -588,6 +589,14 @@
       }
     }
   }
+  if (endpointSharedVertexBlockingHit(
+    from,
+    to,
+    sourceCellQuadrant,
+    targetCellQuadrant,
+    sourceEndpointBlockingCell,
+    targetEndpointBlockingCell,
+  ) !== null) tier = 'total';
   const sources = uniqueSources(strongest);
   return {
     targetCorner: { ...to },
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=dot --testNamePattern='PHYSICAL_COVER_UNCHANGED_BY_VERTEX'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 3033ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
----------------------------------------------x-------

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/combat/visibility-field.test.ts > VIS-FIELD-01 optical tracer separation > PHYSICAL_COVER_UNCHANGED_BY_VERTEX
AssertionError: expected { …(7) } to match object { tier: 'none', blocksSight: true }
(9 matching properties omitted from actual)

- Expected
+ Received

  {
    "blocksSight": true,
-   "tier": "none",
+   "tier": "total",
  }

 ❯ tests/unit/combat/visibility-field.test.ts:1451:26
    1449|     // Ray (0,4)->(2,2) is optically stopped at the TL (1,1) + BR (2,2…
    1450|     // but it crosses no cell interior; the new sight hit therefore le…
    1451|     expect(endpointLine).toMatchObject({ tier: 'none', blocksSight: tr…
       |                          ^
    1452|   });
    1453|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 53 skipped (54)
   Start at  23:30:04
   Duration  2.74s (transform 877ms, setup 0ms, import 1.30s, tests 15ms, environment 0ms)


diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..e4516b1cfa1e8a1161a1a14efcdbd83d91a259bb
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
@@ -588,6 +589,14 @@
       }
     }
   }
+  if (endpointSharedVertexBlockingHit(
+    from,
+    to,
+    sourceCellQuadrant,
+    targetCellQuadrant,
+    sourceEndpointBlockingCell,
+    targetEndpointBlockingCell,
+  ) !== null) tier = 'total';
   const sources = uniqueSources(strongest);
   return {
     targetCorner: { ...to },
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

codex
Mutation checks are complete: diagonal-only produced exactly the aggregate, edge-arrival, and far-face reds; indiscriminate endpoint blocking broke both own-edge and same-side controls; coupling endpoint sight hits to physical cover broke the physical-cover pin (`none → total`). All mutants have been removed.
exec
/bin/bash -lc "/usr/bin/time -f 'WALL_SECONDS=%e' npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts tests/unit/vtt/room-generator.test.ts tests/unit/vtt/arena-basis-brutal-b.test.ts tests/unit/vtt/composite-turn-proposals.test.ts tests/unit/vtt/engine-opportunity-movement-intel.test.ts tests/unit/vtt/speculative-planning.test.ts tests/unit/tools/d569-blind-experiment.test.ts tests/unit/tools/d569-second-family-manifest.test.ts tests/unit/tools/ai-dm-screenshot-probe.test.ts tests/unit/vtt/two-room-fixture.test.ts --reporter=dot" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

 exited 1 in 34510ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
···················xxx··x····x····x·····x······xx··············································xxxx·x············x·x·x···x·xx·x·x·xx···xx··x····x·····x······x···x··················································x···x··xx····························································

⎯⎯⎯⎯⎯⎯ Failed Tests 35 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment
AssertionError: expected [ …(7) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+ ]

 ❯ tests/unit/tools/d569-blind-experiment.test.ts:409:27
    407|   it('validates the exact v5 Opus 4.8 arms and notes-only Fable Astra …
    408|     const frozen = manifest();
    409|     expect(codes(frozen)).toEqual([]);
       |                           ^
    410|     expect(frozen.version).toBe('d569-blind-experiment-v5');
    411|     expect(frozen.preregistrationAmendments).toEqual(EXPECTED_V5_AMEND…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/35]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:473:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/35]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > adds exactly 180 separately labeled advice-assisted hint cells, 660 total
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:635:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/35]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > consumes the frozen second encounter family for all 480 core cells
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:644:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/35]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > records exact model effort cli and cliVersion for every observed row
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:653:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/35]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects either decisive integrity field alone while preserving rows with neither field
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:683:18

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/35]⎯

 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > pins the amended exact contiguous ranges and verbatim independent ledger evidence
AssertionError: expected [ 'first_regeneration', …(46) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+ ]

 ❯ tests/unit/tools/d569-second-family-manifest.test.ts:89:27
     87|     ]);
     88|     expect(frozen.ledgerEntry).toBe(D569_SECOND_FAMILY_LEDGER_ENTRY);
     89|     expect(codes(frozen)).toEqual([]);
       |                           ^
     90|   });
     91|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/35]⎯

 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207002.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207004.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207005.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207006.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207007.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207008.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207009.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
AssertionError: expected [ { …(2) } ] to deeply equal []

- Expected
+ Received

- []
+ [
+   {
+     "code": "brutal_productivity",
+     "message": "every brutal monster must have a productive first-turn offer",
+   },
+ ]

 ❯ tests/unit/tools/d569-second-family-manifest.test.ts:103:54
    101|       const frozen = room(path);
    102|       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.…
    103|       expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
       |                                                      ^
    104|     },
    105|   );

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206001 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206001-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[9/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206002 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206002-monster-1 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[10/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206003 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206003-monster-1 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[11/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206004 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206004-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[12/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206005 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206005-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[13/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206006 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206006-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[14/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206007 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206007-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[15/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206008 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206008-monster-1 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[16/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206009 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206009-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[17/35]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206010 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206010-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[18/35]⎯

 FAIL  tests/unit/vtt/composite-turn-proposals.test.ts > complete action economy and composite turn proposals > projects fresh options for every requested generated-room monster turn
AssertionError: expected [ 'Dodge', 'End Turn' ] to include 'Longbow + Longbow -> combatant:fighter'
 ❯ tests/unit/vtt/composite-turn-proposals.test.ts:160:20
    158|     const labels = scout?.options.map((option) => option.label) ?? [];
    159|
    160|     expect(labels).toContain('Longbow + Longbow -> combatant:fighter');
       |                    ^
    161|     expect(labels).toContain('Longbow + Longbow -> combatant:cleric');
    162|     expect(labels).toContain('Longbow + Longbow -> combatant:wizard');

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[19/35]⎯

 FAIL  tests/unit/vtt/engine-opportunity-movement-intel.test.ts > engine movement and opportunity-cost intel > does not repeat the frozen Room-8 four-actor zero-feet Dash plan
AssertionError: expected [ { …(4) }, { …(4) }, { …(4) }, …(1) ] to deeply equal [ { …(4) }, { …(4) }, { …(4) }, …(1) ]

- Expected
+ Received

@@ -1,38 +1,38 @@
  [
    {
      "actions": [
-       "dash",
+       "dodge",
      ],
      "actorId": "combatant:generated-5117008-monster-1",
      "finalPosition": {
-       "column": 12,
-       "row": 6,
+       "column": 22,
+       "row": 1,
      },
-     "movementFeet": 55,
+     "movementFeet": 0,
    },
    {
      "actions": [
-       "dash",
+       "dodge",
      ],
      "actorId": "combatant:generated-5117008-monster-2",
      "finalPosition": {
-       "column": 11,
-       "row": 5,
+       "column": 21,
+       "row": 1,
      },
-     "movementFeet": 60,
+     "movementFeet": 0,
    },
    {
      "actions": [
-       "dash",
+       "dodge",
      ],
      "actorId": "combatant:generated-5117008-monster-4",
      "finalPosition": {
-       "column": 11,
-       "row": 5,
+       "column": 21,
+       "row": 3,
      },
-     "movementFeet": 60,
+     "movementFeet": 0,
    },
    {
      "actions": [
        "dash",
      ],

 ❯ tests/unit/vtt/engine-opportunity-movement-intel.test.ts:145:22
    143|     // None can attack this turn, but the wall opening at (12,6) makes…
    144|     // closest-reachable Dash productive rather than a zero-foot waste…
    145|     expect(selected).toEqual([
       |                      ^
    146|       {
    147|         actorId: combatantId('combatant:generated-5117008-monster-1'),

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[20/35]⎯

 FAIL  tests/unit/vtt/room-generator.test.ts > seeded room generator > generates brutal seed 6203001 with executable casters, dense terrain, scaled pressure, and productive monsters
 FAIL  tests/unit/vtt/room-generator.test.ts > seeded room generator > generates brutal seed 6203002 with executable casters, dense terrain, scaled pressure, and productive monsters
 FAIL  tests/unit/vtt/room-generator.test.ts > seeded room generator > generates brutal seed 6203003 with executable casters, dense terrain, scaled pressure, and productive monsters
 FAIL  tests/unit/vtt/room-generator.test.ts > seeded room generator > generates brutal seed 6203004 with executable casters, dense terrain, scaled pressure, and productive monsters
 FAIL  tests/unit/vtt/room-generator.test.ts > seeded room generator > generates brutal seed 6203006 with executable casters, dense terrain, scaled pressure, and productive monsters
 FAIL  tests/unit/vtt/room-generator.test.ts > seeded room generator > generates brutal seed 6203008 with executable casters, dense terrain, scaled pressure, and productive monsters
 FAIL  tests/unit/vtt/room-generator.test.ts > seeded room generator > generates brutal seed 6203009 with executable casters, dense terrain, scaled pressure, and productive monsters
 FAIL  tests/unit/vtt/room-generator.test.ts > seeded room generator > generates brutal seed 6203010 with executable casters, dense terrain, scaled pressure, and productive monsters
AssertionError: expected [ { …(2) } ] to deeply equal []

- Expected
+ Received

- []
+ [
+   {
+     "code": "brutal_productivity",
+     "message": "every brutal monster must have a productive first-turn offer",
+   },
+ ]

 ❯ tests/unit/vtt/room-generator.test.ts:592:54
    590|     (seed) => {
    591|       const brutal = generateRoom(seed, { difficulty: 'brutal' });
    592|       expect(brutalRoomMembershipViolations(brutal)).toEqual([]);
       |                                                      ^
    593|       const state = freshMonsterPlanningState(brutal.encounter.state);
    594|       const monsters = state.combatants.filter((candidate) => candidat…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[21/35]⎯

 FAIL  tests/unit/vtt/speculative-planning.test.ts > speculative host fact system > forms a bounded volatility-ranked menu from baseline dependencies and canonical movement proofs
AssertionError: expected 0 to be greater than 0
 ❯ tests/unit/vtt/speculative-planning.test.ts:554:40
    552|     const result = buildHostScenarioMenu(state, actors, players, OFFER…
    553|     expect(result.baselineProposals.map((entry) => entry.actorId)).toE…
    554|     expect(result.scenarioMenu.length).toBeGreaterThan(0);
       |                                        ^
    555|     expect(result.scenarioMenu.length).toBeLessThanOrEqual(8);
    556|     expect(result.scenarioMenu.map((entry) => entry.rank)).toEqual(

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[22/35]⎯


 Test Files  7 failed | 3 passed (10)
      Tests  35 failed | 246 passed (281)
   Start at  23:30:19
   Duration  34.14s (transform 7.56s, setup 0ms, import 15.36s, tests 94.22s, environment 1ms)

Command exited with non-zero status 1
WALL_SECONDS=34.63

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc 'npx vite-node /tmp/wall01-r4-counts.ts' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 14148ms:
{"family":"hard","monsters":75,"offers":142,"shotless":35}
{"family":"brutal","monsters":44,"offers":57,"shotless":34}
{"family":"brutal-b","monsters":42,"offers":15,"shotless":41}
{"family":"brutal-2","monsters":44,"offers":58,"shotless":36}

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "/usr/bin/time -f 'WALL_SECONDS=%e' npx vitest run --configLoader runner tests/unit/combat/los-cover.test.ts --reporter=verbose" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 254ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

No test files found, exiting with code 1

filter: tests/unit/combat/los-cover.test.ts
include: tests/**/*.test.ts
exclude:  **/node_modules/**, **/.git/**, tests/integration-supervisor/**

Command exited with non-zero status 1
WALL_SECONDS=0.37

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "rg --files tests | rg 'los-cover|cover.*los|los.*cover'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
tests/fixtures/arena-basis-los-cover-v1/seed-5762101.json
tests/fixtures/arena-basis-los-cover-v1/seed-5762203.json
tests/fixtures/arena-basis-los-cover-v1/seed-5762001.json
tests/fixtures/arena-basis-los-cover-v1/seed-5762003.json
tests/fixtures/arena-basis-los-cover-v1/seed-5762201.json
tests/fixtures/arena-basis-los-cover-v1/seed-5762002.json
tests/fixtures/arena-basis-los-cover-v1/seed-5762103.json
tests/fixtures/arena-basis-los-cover-v1/seed-5762102.json
tests/fixtures/arena-basis-los-cover-v1/seed-5762202.json
tests/unit/vtt/room-generator-los-cover.test.ts
tests/unit/tools/los-cover-era-audit.test.ts
tests/unit/tools/los-cover-call-sites.test.ts

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "/usr/bin/time -f 'WALL_SECONDS=%e' npx vitest run --configLoader runner tests/unit/vtt/room-generator-los-cover.test.ts --reporter=verbose" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

 exited 1 in 116642ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > OPEN_RAY_IGNORES_BLOCKS_SIGHT rejects the only opposing pair across a two-blocker seam 10ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > generated and authored arena fixtures share one strict decoder 514ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'standard' seed 5762001 457ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'standard' seed 5762002 487ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'standard' seed 5762003 186ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'hard' seed 5762101 89ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'hard' seed 5762102 605ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'hard' seed 5762103 754ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'brutal' seed 5762201 1265ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'brutal' seed 5762202 646ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'brutal' seed 5762203 503ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-COVER-OVERLAPS-SPAWN across every versioned fixture 4879ms
 × tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-WALL-SEALS-ROOM for every living footprint size 5365ms
   → Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 × tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > retains a productive first-turn option for every living monster in the versioned basis 5593ms
   → Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 × tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-ONE-SIDE-CANNOT-CHANGE-COVER with a legal destination per side 5001ms
   → Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed +0 332ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 1 620ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 2 1295ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 3 725ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 4 852ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 5 306ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 6 377ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 7 160ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 8 550ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 9 688ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 10 1184ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 11 538ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 12 619ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 13 569ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 14 894ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 15 294ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 16 1491ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 17 64ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 18 560ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 19 274ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 20 944ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 21 78ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 22 754ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 23 326ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 24 464ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 25 1148ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 26 815ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 27 311ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 28 1101ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 29 48ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 30 2783ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 31 774ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed +0 331ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 1 1055ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 2 220ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 3 1015ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 4 1603ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 5 1335ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 6 780ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 7 295ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 8 460ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 9 447ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 10 1401ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 11 1127ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 12 461ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 13 857ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 14 898ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 15 508ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 16 1264ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 17 239ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 18 1013ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 19 276ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 20 1615ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 21 745ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 22 934ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 23 340ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 24 781ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 25 1385ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 26 1279ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 27 486ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 28 1091ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 29 320ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 30 2350ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 31 1820ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed +0 434ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 1 2095ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 2 953ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 3 1507ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 4 1929ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 5 3414ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 6 803ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 7 83ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 8 983ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 9 1091ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 10 1887ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 11 908ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 12 755ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 13 1155ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 14 739ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 15 67ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 16 1308ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 17 1508ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 18 2158ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 19 165ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 20 1314ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 21 641ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 22 608ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 23 241ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 24 1069ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 25 988ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 26 1072ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 27 1101ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 28 996ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 29 848ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 30 1020ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 31 710ms
 ✓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-LARGE-CREATURE-NO-PRODUCTIVE-REGION 1555ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 3 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-WALL-SEALS-ROOM for every living footprint size
Error: Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ❯ tests/unit/vtt/room-generator-los-cover.test.ts:328:3
    326|   });
    327|
    328|   it('kills M576-E2-WALL-SEALS-ROOM for every living footprint size', …
       |   ^
    329|     for (const fixture of FIXTURE_CASES) {
    330|       const room = generateRoom(fixture.seed, {

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/3]⎯

 FAIL  tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > retains a productive first-turn option for every living monster in the versioned basis
Error: Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ❯ tests/unit/vtt/room-generator-los-cover.test.ts:342:3
    340|   });
    341|
    342|   it('retains a productive first-turn option for every living monster …
       |   ^
    343|     for (const fixture of FIXTURE_CASES) {
    344|       expectProductiveMonsters(generateRoom(fixture.seed, {

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/3]⎯

 FAIL  tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-ONE-SIDE-CANNOT-CHANGE-COVER with a legal destination per side
Error: Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ❯ tests/unit/vtt/room-generator-los-cover.test.ts:351:3
    349|   });
    350|
    351|   it('kills M576-E2-ONE-SIDE-CANNOT-CHANGE-COVER with a legal destinat…
       |   ^
    352|     for (const fixture of FIXTURE_CASES) {
    353|       const room = generateRoom(fixture.seed, {

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/3]⎯


 Test Files  1 failed (1)
      Tests  3 failed | 109 passed (112)
   Start at  23:31:24
   Duration  116.32s (transform 1.22s, setup 0ms, import 1.74s, tests 113.14s, environment 0ms)

Command exited with non-zero status 1
WALL_SECONDS=116.76

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

codex
The broad prescribed set is stable: 246 pass / 35 frozen-family reds, exactly the prior titles. The los-cover full file likewise remains 109 pass with only the three known 5-second load-sensitive timeouts; I’m measuring the two explicitly requested budget tests in isolation rather than changing their budgets.
exec
/bin/bash -lc "/usr/bin/time -f 'WALL_SECONDS=%e' npx vitest run --configLoader runner tests/unit/vtt/room-generator-los-cover.test.ts --reporter=verbose --testNamePattern='kills M576-E2-WALL-SEALS-ROOM for every living footprint size'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 9097ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > OPEN_RAY_IGNORES_BLOCKS_SIGHT rejects the only opposing pair across a two-blocker seam
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > generated and authored arena fixtures share one strict decoder
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'standard' seed 5762001
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'standard' seed 5762002
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'standard' seed 5762003
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'hard' seed 5762101
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'hard' seed 5762102
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'hard' seed 5762103
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'brutal' seed 5762201
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'brutal' seed 5762202
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'brutal' seed 5762203
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-COVER-OVERLAPS-SPAWN across every versioned fixture
 × tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-WALL-SEALS-ROOM for every living footprint size 5560ms
   → Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > retains a productive first-turn option for every living monster in the versioned basis
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-ONE-SIDE-CANNOT-CHANGE-COVER with a legal destination per side
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed +0
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 1
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 2
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 3
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 4
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 5
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 6
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 7
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 8
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 9
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 10
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 11
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 12
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 13
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 14
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 15
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 16
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 17
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 18
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 19
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 20
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 21
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 22
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 23
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 24
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 25
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 26
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 27
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 28
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 29
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 30
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 31
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed +0
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 1
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 2
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 3
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 4
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 5
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 6
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 7
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 8
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 9
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 10
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 11
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 12
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 13
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 14
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 15
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 16
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 17
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 18
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 19
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 20
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 21
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 22
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 23
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 24
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 25
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 26
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 27
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 28
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 29
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 30
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 31
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed +0
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 1
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 2
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 3
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 4
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 5
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 6
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 7
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 8
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 9
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 10
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 11
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 12
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 13
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 14
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 15
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 16
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 17
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 18
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 19
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 20
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 21
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 22
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 23
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 24
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 25
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 26
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 27
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 28
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 29
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 30
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 31
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-LARGE-CREATURE-NO-PRODUCTIVE-REGION

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-WALL-SEALS-ROOM for every living footprint size
Error: Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ❯ tests/unit/vtt/room-generator-los-cover.test.ts:328:3
    326|   });
    327|
    328|   it('kills M576-E2-WALL-SEALS-ROOM for every living footprint size', …
       |   ^
    329|     for (const fixture of FIXTURE_CASES) {
    330|       const room = generateRoom(fixture.seed, {

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 111 skipped (112)
   Start at  23:33:31
   Duration  8.76s (transform 1.24s, setup 0ms, import 1.75s, tests 5.56s, environment 0ms)

Command exited with non-zero status 1
WALL_SECONDS=9.22

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "/usr/bin/time -f 'WALL_SECONDS=%e' npx vitest run --configLoader runner tests/unit/vtt/room-generator-los-cover.test.ts --reporter=verbose --testNamePattern='retains a productive first-turn option for every living monster in the versioned basis'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 9353ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > OPEN_RAY_IGNORES_BLOCKS_SIGHT rejects the only opposing pair across a two-blocker seam
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > generated and authored arena fixtures share one strict decoder
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'standard' seed 5762001
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'standard' seed 5762002
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'standard' seed 5762003
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'hard' seed 5762101
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'hard' seed 5762102
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'hard' seed 5762103
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'brutal' seed 5762201
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'brutal' seed 5762202
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-TIER-PRESENT-BUT-NOT-EXERCISED for 'brutal' seed 5762203
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-COVER-OVERLAPS-SPAWN across every versioned fixture
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-WALL-SEALS-ROOM for every living footprint size
 × tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > retains a productive first-turn option for every living monster in the versioned basis 5835ms
   → Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-ONE-SIDE-CANNOT-CHANGE-COVER with a legal destination per side
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed +0
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 1
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 2
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 3
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 4
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 5
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 6
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 7
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 8
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 9
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 10
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 11
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 12
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 13
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 14
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 15
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 16
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 17
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 18
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 19
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 20
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 21
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 22
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 23
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 24
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 25
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 26
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 27
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 28
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 29
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 30
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'standard' seed 31
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed +0
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 1
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 2
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 3
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 4
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 5
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 6
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 7
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 8
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 9
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 10
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 11
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 12
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 13
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 14
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 15
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 16
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 17
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 18
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 19
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 20
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 21
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 22
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 23
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 24
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 25
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 26
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 27
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 28
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 29
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 30
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'hard' seed 31
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed +0
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 1
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 2
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 3
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 4
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 5
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 6
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 7
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 8
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 9
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 10
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 11
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 12
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 13
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 14
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 15
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 16
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 17
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 18
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 19
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 20
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 21
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 22
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 23
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 24
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 25
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 26
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 27
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 28
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 29
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 30
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-SEED-SPECIFIC-CORRECTION for 'brutal' seed 31
 ↓ tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > kills M576-E2-LARGE-CREATURE-NO-PRODUCTIVE-REGION

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/room-generator-los-cover.test.ts > D576 los_cover_v1 generator membership > retains a productive first-turn option for every living monster in the versioned basis
Error: Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ❯ tests/unit/vtt/room-generator-los-cover.test.ts:342:3
    340|   });
    341|
    342|   it('retains a productive first-turn option for every living monster …
       |   ^
    343|     for (const fixture of FIXTURE_CASES) {
    344|       expectProductiveMonsters(generateRoom(fixture.seed, {

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 111 skipped (112)
   Start at  23:33:44
   Duration  9.01s (transform 1.21s, setup 0ms, import 1.73s, tests 5.84s, environment 0ms)

Command exited with non-zero status 1
WALL_SECONDS=9.47

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "/usr/bin/time -f 'WALL_SECONDS=%e' npx vitest run --configLoader runner tests/unit/vtt/replay.test.ts tests/unit/vtt/detection-reactions.test.ts --reporter=dot" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

 succeeded in 38643ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
····································

 Test Files  2 passed (2)
      Tests  36 passed (36)
   Start at  23:33:57
   Duration  38.32s (transform 1.96s, setup 0ms, import 2.87s, tests 35.39s, environment 0ms)

WALL_SECONDS=38.76

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "npx vitest run --configLoader runner tests/unit/vtt/scene-snapshot.test.ts tests/unit/tools/ai-dm-conversation.test.ts tests/unit/vtt/dm-tactical-intel.test.ts tests/unit/vtt/tactical-evaluator-r02.test.ts --reporter=dot --testNamePattern=\"preserves full persisted request/coordinator bytes and the actual revision checksum|records typed fallbackReason for 'blocked auto-submit' from the stub adapter|uses sim_controller and records a typed block when an unresolved frontier prevents auto-submit|renders target-specific non-nearest R02 rows in always-on context with exact reasons|reports both 90-foot Scout shots as move-0 normal range and straight at the dying fighter\"" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

 succeeded in 32214ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
----·---·-·---------------------------------------------------------·--·------------------------------------------------------

 Test Files  4 passed (4)
      Tests  5 passed | 121 skipped (126)
   Start at  23:34:42
   Duration  31.90s (transform 3.74s, setup 0ms, import 6.97s, tests 29.20s, environment 0ms)


diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

codex
All five r3 consequence tests are now green after the single D657 normalization. Replay+detection is 36/36 in 38.76 seconds, under the 41-second ceiling. I’m on the final compile, formatting, line-length, and clean-scope checks now.
exec
/bin/bash -lc 'npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=dot' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
exec
/bin/bash -lc 'npx tsc -p tsconfig.app.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
exec
/bin/bash -lc 'npx tsc -p tsconfig.node.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 3424ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
······················································

 Test Files  1 passed (1)
      Tests  54 passed (54)
   Start at  23:35:23
   Duration  3.12s (transform 981ms, setup 0ms, import 1.44s, tests 70ms, environment 0ms)


 succeeded in 14534ms:
 succeeded in 27748ms:
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "git diff --check
git status --short
awk 'length("'$0) > 120 { print FNR ":" length($0) ":" $0 }'"' src/combat/cover.ts tests/unit/combat/visibility-field.test.ts tests/unit/vtt/scene-snapshot.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
 M src/combat/cover.ts
 M tests/unit/combat/visibility-field.test.ts
 M tests/unit/vtt/scene-snapshot.test.ts

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "sed -n '1050,1290p' tests/unit/combat/visibility-field.test.ts && git diff --stat && git diff --check" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
      firstBlockingCell: { column: 5, row: 3 },
    });
  });

  it('DIAGONAL_CRACK_SEALED', () => {
    const observer = normalProfile('diagonal-crack-observer');
    const subject = normalProfile('diagonal-crack-subject');
    const blockers = [{ column: 2, row: 1 }, { column: 1, row: 2 }];
    const state = fieldState(observer, {
      bounds: { columns: 5, rows: 5 },
      combatants: [observer, subject],
      tokens: [placedToken(observer, 0, 0), placedToken(subject, 3, 3)],
      blockedCells: blockers,
    });
    const trace = traceTerrainLine(state, { column: 0, row: 0 }, { column: 3, row: 3 });

    // The (0,0)->(4,4) ray reaches lattice vertex (2,2) between diagonal blockers TR (2,1)
    // and BL (1,2). D642 seals that crack and deterministically chooses lower-row cell (2,1).
    expect(trace).toMatchObject({
      tier: 'half',
      blocksSight: true,
      firstBlockingCell: { column: 2, row: 1 },
    });
    expect(sightToCreature(state, observer.id, subject.id)).toEqual({
      kind: 'unseen',
      reason: 'blocked',
    });
  });

  it('DIAGONAL_CRACK_CONTROL_ONE_BLOCKER', () => {
    const observer = normalProfile('diagonal-crack-control-observer');
    const subject = normalProfile('diagonal-crack-control-subject');
    const state = fieldState(observer, {
      bounds: { columns: 5, rows: 5 },
      combatants: [observer, subject],
      tokens: [placedToken(observer, 0, 0), placedToken(subject, 3, 3)],
      blockedCells: [{ column: 2, row: 1 }],
    });
    const trace = traceTerrainLine(state, { column: 0, row: 0 }, { column: 3, row: 3 });
    const oneSidedCorner = trace.lines.find((line) =>
      line.targetCorner.column === 4 && line.targetCorner.row === 3);

    // Selected ray (0,1)->(4,3) reaches vertex (2,2): TR (2,1) blocks but BL (1,2) is open.
    expect(trace).toMatchObject({ tier: 'none', blocksSight: false, firstBlockingCell: null });
    expect(oneSidedCorner).toMatchObject({ blocksSight: false, firstBlockingCell: null });
    expect(sightToCreature(state, observer.id, subject.id)).toEqual({
      kind: 'visible',
      grade: 'seen',
      sense: 'normal_sight',
    });
  });

  it('SOURCE_ENDPOINT_JUNCTION_SEALED', () => {
    const observer = normalProfile('source-endpoint-observer');
    const blockers = [
      { column: 1, row: 0 }, { column: 1, row: 1 },
      { column: 2, row: 2 }, { column: 2, row: 3 },
      { column: 2, row: 4 }, { column: 2, row: 5 },
    ];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 0, row: 3 });

    // The observer's bottom-left corner is vertex (2,2); its cell lies TR of that vertex.
    // Ray (2,2)->(0,4) leaves into the opposite BL quadrant through the pinch sealed by
    // TL (1,1) + BR (2,2). Lower-row cell (1,1) wins; physical cover remains None.
    expect(trace).toMatchObject({
      tier: 'none',
      blocksSight: true,
      firstBlockingCell: { column: 1, row: 1 },
    });
  });

  it('TARGET_ENDPOINT_JUNCTION_SEALED', () => {
    const observer = normalProfile('target-endpoint-observer');
    const blockers = [
      { column: 1, row: 0 }, { column: 1, row: 1 },
      { column: 2, row: 2 }, { column: 2, row: 3 },
      { column: 2, row: 4 }, { column: 2, row: 5 },
    ];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
    const trace = traceTerrainLine(state, { column: 0, row: 3 }, { column: 2, row: 1 });

    // Mirrored ray (0,4)->(2,2) arrives from the BL quadrant opposite the target cell's
    // TR quadrant. TL (1,1) + BR (2,2) seal vertex (2,2); physical Half Cover is unchanged.
    expect(trace).toMatchObject({
      tier: 'half',
      blocksSight: true,
      firstBlockingCell: { column: 1, row: 1 },
    });
  });

  it('SAME_SIDE_ENDPOINT_GRAZE_CLEAR', () => {
    const observer = normalProfile('same-side-endpoint-observer');
    const blockers = [
      { column: 1, row: 0 }, { column: 1, row: 1 },
      { column: 2, row: 2 }, { column: 2, row: 3 },
      { column: 2, row: 4 }, { column: 2, row: 5 },
    ];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 5, row: 1 });
    const reverseTrace = traceTerrainLine(state, { column: 5, row: 1 }, { column: 2, row: 1 });
    const sameSideEndpoint = reverseTrace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);

    // Source corner (2,2) belongs to a cell in its TR quadrant. Rays toward (5,1) stay
    // on that same side or leave along a cell edge; neither crosses the sealed pinch.
    expect(trace).toMatchObject({ tier: 'none', blocksSight: false, firstBlockingCell: null });
    // Mirrored ray (5,1)->(2,2) arrives from the target cell's own TR quadrant, not the
    // opposite BL quadrant. Pin it directly so an indiscriminate sealed-endpoint rule fails.
    expect(sameSideEndpoint).toMatchObject({ blocksSight: false, firstBlockingCell: null });
  });

  it('ADJACENT_CORNER_ZERO_LENGTH', () => {
    const observer = normalProfile('zero-length-endpoint-observer');
    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
    const state = fieldState(observer, { bounds: { columns: 4, rows: 4 }, blockedCells: blockers });
    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
    const zeroLength = trace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);
    const diagonal = trace.lines.find((line) =>
      line.targetCorner.column === 1 && line.targetCorner.row === 3);

    // At shared corner (2,2), the source cell is TR and target cell BL: opposite free
    // quadrants around the TL (1,1) + BR (2,2) pair. Their zero-length corner ray is
    // blocked at the solid-wall pinch, as is the ray leaving diagonally into target-side BL.
    expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
    expect(zeroLength).toMatchObject({
      tier: 'none', blocksSight: true, firstBlockingCell: { column: 1, row: 1 },
    });
    expect(diagonal).toMatchObject({
      tier: 'none', blocksSight: true, firstBlockingCell: { column: 1, row: 1 },
    });
    expect(trace.tier).toBe('none');

    const open = { ...state, blockedCells: [{ column: 1, row: 1 }] };
    const openTrace = traceTerrainLine(open, { column: 2, row: 1 }, { column: 1, row: 2 });
    const openZeroLength = openTrace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);
    // Without BR (2,2), no sealed pair exists: the identical zero-length contact is clear.
    expect(openTrace.sourceCorner).toEqual({ column: 2, row: 2 });
    expect(openZeroLength).toMatchObject({ blocksSight: false, firstBlockingCell: null });
    expect(openTrace.blocksSight).toBe(false);
  });

  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
    const observer = normalProfile('adjacent-junction-observer');
    const blockers = [
      { column: 1, row: 0 }, { column: 1, row: 1 },
      { column: 2, row: 2 }, { column: 2, row: 3 },
      { column: 2, row: 4 }, { column: 2, row: 5 },
    ];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });

    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
    expect(trace).toMatchObject({
      tier: 'none',
      blocksSight: true,
      firstBlockingCell: { column: 1, row: 1 },
    });
  });

  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
    const observer = normalProfile('edge-arrival-observer');
    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
    const options = { additionalSightBlockingCells: blockers } as const;
    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
    const verticalArrival = verticalTrace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);
    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
    const horizontalArrival = horizontalTrace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);

    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
  });

  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
    const observer = normalProfile('own-edge-departure-observer');
    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
    const trace = traceTerrainLine(
      state,
      { column: 2, row: 0 },
      { column: 2, row: 1 },
      { additionalSightBlockingCells: blockers },
    );
    const ownEdge = trace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);

    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
  });

  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
    const observer = normalProfile('far-face-departure-observer');
    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
    const trace = traceTerrainLine(
      state,
      { column: 0, row: 2 },
      { column: 2, row: 1 },
      { additionalSightBlockingCells: blockers },
    );
    const farFace = trace.lines.find((line) =>
      line.targetCorner.column === 2 && line.targetCorner.row === 2);

    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
  });

  it('STAIRCASE_WALL_SEALED', () => {
    const observer = normalProfile('staircase-wall-observer');
    const subject = normalProfile('staircase-wall-subject');
    const blockers = [{ column: 7, row: 3 }, { column: 8, row: 4 }];
    const state = fieldState(observer, {
      bounds: { columns: 15, rows: 8 },
      combatants: [observer, subject],
      tokens: [placedToken(observer, 13, 4), placedToken(subject, 1, 4)],
      blockedCells: blockers,
    });
    const trace = traceTerrainLine(state, { column: 13, row: 4 }, { column: 1, row: 4 });
    const stepRay = trace.lines.find((line) =>
      line.targetCorner.column === 1 && line.targetCorner.row === 4);

    // (13,4)->(1,4) touches upper cell (7,3) and lower cell (8,4) only at vertex (8,4).
    // No unit edge has two blocking flanks and neither interior is crossed; lower-row (7,3) wins.
    expect(rasterizeCornerLine({ column: 13, row: 4 }, { column: 1, row: 4 }, blockers)).toEqual([]);
    expect(stepRay).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 7, row: 3 } });
 src/combat/cover.ts                        | 199 +++++++++++++++++++++++++----
 tests/unit/combat/visibility-field.test.ts | 193 +++++++++++++++++++++++++++-
 tests/unit/vtt/scene-snapshot.test.ts      |  19 ++-
 3 files changed, 380 insertions(+), 31 deletions(-)

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc 'rg -n "D656|hard.*142|shotless|7b884" /home/vagrant/PhpstormProjects/dnd/.claude/decisions.md /home/vagrant/PhpstormProjects/dnd-wt-vis-field/.claude/decisions.md | tail -40' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
rg: /home/vagrant/PhpstormProjects/dnd/.claude/decisions.md: No such file or directory (os error 2)

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc 'ls -d /home/vagrant/PhpstormProjects/dnd* | head -30 && rg -n "D656|7b884bf|shotless" .claude/decisions.md | tail -30' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
/home/vagrant/PhpstormProjects/dnd-d4-builds-code-test
/home/vagrant/PhpstormProjects/dnd-d4-wt-mutt
/home/vagrant/PhpstormProjects/dnd-lane-discord
/home/vagrant/PhpstormProjects/dnd-lane-dspike
/home/vagrant/PhpstormProjects/dnd-lane-imports
/home/vagrant/PhpstormProjects/dnd-lane-inc4
/home/vagrant/PhpstormProjects/dnd-lane-inc6
/home/vagrant/PhpstormProjects/dnd-lane-kill2b
/home/vagrant/PhpstormProjects/dnd-lane-kill2c
/home/vagrant/PhpstormProjects/dnd-lane-m2
/home/vagrant/PhpstormProjects/dnd-lane-m7
/home/vagrant/PhpstormProjects/dnd-lane-mergefix
/home/vagrant/PhpstormProjects/dnd-lane-packet
/home/vagrant/PhpstormProjects/dnd-lane-prose
/home/vagrant/PhpstormProjects/dnd-lane-rollout
/home/vagrant/PhpstormProjects/dnd-lane-statblocks
/home/vagrant/PhpstormProjects/dnd-lane-trial-idx5
/home/vagrant/PhpstormProjects/dnd-lane-vtt3a
/home/vagrant/PhpstormProjects/dnd-lane-vtt3b
/home/vagrant/PhpstormProjects/dnd-lane-vtt3c
/home/vagrant/PhpstormProjects/dnd-lane-vttmove
/home/vagrant/PhpstormProjects/dnd-lane-vttres
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static
/home/vagrant/PhpstormProjects/dnd-rerun-baseline
/home/vagrant/PhpstormProjects/dnd-rerun-current
/home/vagrant/PhpstormProjects/dnd-rerun2-current
/home/vagrant/PhpstormProjects/dnd-research-private
/home/vagrant/PhpstormProjects/dnd-static-v2
/home/vagrant/PhpstormProjects/dnd-wt-a10-unknown
/home/vagrant/PhpstormProjects/dnd-wt-a11y

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc 'rg -l "D656" /home/vagrant/PhpstormProjects/dnd-*/.claude/decisions.md 2>/dev/null | head -20' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
/home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc 'rg -n "''^### D65[4-7]" /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 0ms:
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc 'rg -n "D656|WALL-01-FIX-R4|7b884bf" /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md | tail -20' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
23074:SOL fix r1: vertex lookup now queries sightBlockingAt directly around each GCD-enumerated interior lattice vertex (no candidate bbox), also on the coverTierBetweenObjects path; the three axis witnesses (my geometries: first cells (7,3) / (8,3) / (4,2)) + split control; 46/46; four mutants incl. the bbox-regression one (exactly the three axis witnesses red); counts table unchanged before/after (hard 142/35, brutal 57/34, brutal-b 15/41, brutal-2 58/36 — now with a working rule); 42 seeds byte-identical (aggregate 7b884bf9…); pair 39.1 s; the 35 frozen/behaviour reds unchanged; four los-cover 5 s budget timeouts under the combined audit (110/112 isolated with two) — perf, not re-pinned. MY VERIFICATION (verify-wall-01-r1.log; ../dnd-wt-seam-ab reset to 807d2e3c + patch): tsc, my seven-case probe (A/B/C/D must block, E split pair / F outer-face graze / G single corner must stay clear), a vertex-lookup-null mutant, the suites, los-cover alone, pair.
23109:## D656 — SUPERVISOR (2026-09-17 23:07): COHORT-01 B2 REJECTED by astra r1 (medium; 0 P1, 1 P2: B2-F1; saved review-b2-astra-r1.md) → fix r1 dispatched; WALL-01 fix r4 harvested (sol; saved impl-wall-01-fix-r4-sol.md) → my verification running
23111:WALL-01 fix r4 (sol, resume 01a0b20e-61fa…; 2 files 286+/26−): endpoint-junction rule — at a sealed vertex a source ray blocks only when leaving diagonally into the quadrant opposite the source cell (TL→BR, TR→BL, BL→TR, BR→TL ownership), target rays mirrored, axis-aligned rays to the shared-edge rule, zero-length corner contact across opposite free quadrants blocked, progress 0/1, physical cover untouched, same path in coverTierBetweenObjects; witnesses SOURCE/TARGET_ENDPOINT_JUNCTION_SEALED (astra's scene), SAME_SIDE_ENDPOINT_GRAZE_CLEAR, ADJACENT_CORNER_ZERO_LENGTH; DIAGONAL_CORNER_TOUCH_SEALED_D642 re-derived (its source corner (1,1) had been an excluded endpoint — now aggregate blocked, first (1,0), tier none); three mutants killed; visibility-field 50/50; r3 scenes 5/5; ten-suite audit 246 passed + exactly the 35 known frozen reds; counts unchanged (hard 142/35, brutal 57/34, brutal-b 15/41, brutal-2 58/36); 42 generated rooms byte-identical (7b884bf9…); pair 39.4 s; los-cover budget tests 5.6 s isolated (perf). MY VERIFICATION (verify-wall-01-r4.log, seam-ab at bc6422bf + patch): tsc, six-case probe (A/B endpoint must block; C same-side clear; D adjacent corner blocked; E interior; F stepped wall), endpoint-rule-null mutant, 15 suites, los-cover, pair.

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "sed -n '23105,23135p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:

## D655 — SUPERVISOR (2026-09-17 22:51): COHORT-01 B2 VERIFIED BY ME and committed 8284a91c on claude/cohort-01; astra medium review dispatched (resumed B1 reviewer)
VERIFIED (verify-b2.log, clean checkout at e92ebc93 + patch): tsc 0/0; diff-check 0; 0 long lines; V-freeze 0 and no fixture files touched; the new v2 suite 30 red, every failure ENOENT on the declared missing fixture; room-generator 56/56; brutal-b 47/47; second-family 50/50. Read by me: the removed code in both frozen suites is exactly the per-monster productivity loop, replaced by the D635/D642/D643 rationale. Generation (Batches 3–5) still gated on WALL-01 (fix r4 in flight).

## D656 — SUPERVISOR (2026-09-17 23:07): COHORT-01 B2 REJECTED by astra r1 (medium; 0 P1, 1 P2: B2-F1; saved review-b2-astra-r1.md) → fix r1 dispatched; WALL-01 fix r4 harvested (sol; saved impl-wall-01-fix-r4-sol.md) → my verification running
B2-F1 (P2): the 6203 retirement removed the whole brutalRoomMembershipViolations test (difficulty, scaled budget/spend, caster, terrain AND productivity) instead of only the productivity loop; astra's in-memory mutant (budget +1 on 6203010) leaves the suite 56/56 green; the fix restores the ten-seed static assertion filtering only brutal_productivity. Everything else accepted: 6206 retirement exact; 30 v2 declarations through declareTestInputs; PLAN-F6 predicate with the query's own traceCombatantLine at mechanics.finalPosition; membership after decoding with no swallow (an in-memory bad-budget fixture fails with brutal_budget); three mutants killed; tsc 0/0; freeze 0; 229 added lines, 0 over 120. Fix r1 dispatched (resume 01a0b245…).
WALL-01 fix r4 (sol, resume 01a0b20e-61fa…; 2 files 286+/26−): endpoint-junction rule — at a sealed vertex a source ray blocks only when leaving diagonally into the quadrant opposite the source cell (TL→BR, TR→BL, BL→TR, BR→TL ownership), target rays mirrored, axis-aligned rays to the shared-edge rule, zero-length corner contact across opposite free quadrants blocked, progress 0/1, physical cover untouched, same path in coverTierBetweenObjects; witnesses SOURCE/TARGET_ENDPOINT_JUNCTION_SEALED (astra's scene), SAME_SIDE_ENDPOINT_GRAZE_CLEAR, ADJACENT_CORNER_ZERO_LENGTH; DIAGONAL_CORNER_TOUCH_SEALED_D642 re-derived (its source corner (1,1) had been an excluded endpoint — now aggregate blocked, first (1,0), tier none); three mutants killed; visibility-field 50/50; r3 scenes 5/5; ten-suite audit 246 passed + exactly the 35 known frozen reds; counts unchanged (hard 142/35, brutal 57/34, brutal-b 15/41, brutal-2 58/36); 42 generated rooms byte-identical (7b884bf9…); pair 39.4 s; los-cover budget tests 5.6 s isolated (perf). MY VERIFICATION (verify-wall-01-r4.log, seam-ab at bc6422bf + patch): tsc, six-case probe (A/B endpoint must block; C same-side clear; D adjacent corner blocked; E interior; F stepped wall), endpoint-rule-null mutant, 15 suites, los-cover, pair.

## D657 — SUPERVISOR (2026-09-17 23:22): WALL-01 fix r4 verified except for ONE aggregate leak that is a gap in MY r4 rule statement (WALL-F4) → fix r5 dispatched; B2 fix r1 harvested → verification running
MY VERIFICATION of r4 (verify-wall-01-r4.log; seam-ab at bc6422bf + patch): tsc 0/0; diff-check 0; probes A (source endpoint) and B (target endpoint) now BLOCK (first (1,1)); C same-side clear; E interior blocked; F the stepped wall blocked (first (7,3)); 15 suites 2273/2308 = exactly the 35 known frozen/behaviour reds; los-cover 109/112 (three 5 s budget timeouts: 5.41/5.64/5.02 s — perf); pair 36/36 in 39 s. My "endpoint rule disabled" mutant hit the wrong function (a python name-grep picked the pre-existing outerCornerEndpoints → the whole suite red, 48/50) — a void mutant, not evidence; the intended one (endpointSharedVertexBlockingHit → null) is re-run in the r5 verification. WALL-F4 (found by my probe D, verified): observer (2,1) and target (1,2) in opposite free quadrants of the sealed junction (2,2) → AGGREGATE blocksSight FALSE. Cause: two EDGE-ALIGNED corner rays that END at the junction — (2,1)→(2,2) down x=2 between blocker (1,1) and the observer's own cell, and (3,2)→(2,2) along y=2 — count as grazes under the shared-edge rule and "reach the target's corner", but that corner is the wall's corner; no interior point of the target is reachable by a straight line. My r4 rule statement explicitly sent edge-aligned endpoint rays to the shared-edge rule: my error again, now stated fully. REFINED RULE (D657): at an endpoint vertex with a sealed pair, with Q the endpoint cell's own free quadrant, the ray is clear iff its other endpoint lies in the CLOSED quadrant Q (interior or Q's two boundary half-lines); otherwise it passes the pinch and is blocked. Same-side grazes stay clear (the far endpoint on Q's boundary); a departure along the wall's far face is blocked. Fix r5 dispatched (resume 01a0b20e-61fa…; brief impl-vis-field-wall-01-fix-r5.md) with aggregate and per-ray witnesses, three mutants, counts and generation bytes redone.
B2 fix r1 (sol resumed): restored the ten-seed static membership assertion for 6203 filtering only brutal_productivity; harvest saved impl-b2-fix-r1-sol.md; my verification (verify-b2-r1.log) on seam-verify at 8284a91c + patch.

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

exec
/bin/bash -lc "nl -ba src/combat/cover.ts | sed -n '80,120p;390,510p;735,785p;860,930p'
nl -ba tests/unit/combat/visibility-field.test.ts | sed -n '1110,1280p'
nl -ba tests/unit/vtt/scene-snapshot.test.ts | sed -n '235,315p'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
    80	    { column: minimumColumn, row: minimumRow },
    81	    { column: maximumColumn, row: minimumRow },
    82	    { column: minimumColumn, row: maximumRow },
    83	    { column: maximumColumn, row: maximumRow },
    84	  ];
    85	}
    86	
    87	function outerCornerEndpoints(cells: readonly GridCell[]): readonly CornerEndpoint[] {
    88	  const corners = outerCorners(cells);
    89	  const topLeft = corners[0];
    90	  const topRight = corners[1];
    91	  const bottomLeft = corners[2];
    92	  const bottomRight = corners[3];
    93	  if (topLeft === undefined || topRight === undefined ||
    94	    bottomLeft === undefined || bottomRight === undefined) {
    95	    throw new RangeError('A line query requires four outer corners.');
    96	  }
    97	  // The quadrant names the occupied space relative to its outer corner:
    98	  // top-left -> BR, top-right -> BL, bottom-left -> TR, bottom-right -> TL.
    99	  return [
   100	    { point: topLeft, cellQuadrant: { column: 1, row: 1 } },
   101	    { point: topRight, cellQuadrant: { column: -1, row: 1 } },
   102	    { point: bottomLeft, cellQuadrant: { column: 1, row: -1 } },
   103	    { point: bottomRight, cellQuadrant: { column: -1, row: -1 } },
   104	  ];
   105	}
   106	
   107	/** A corner ray crosses only a cell interior; a boundary graze is not a crossing. */
   108	export function cornerLineCrossesCell(from: CornerPoint, to: CornerPoint, cell: GridCell): boolean {
   109	  const epsilon = 1e-9;
   110	  let entry = 0;
   111	  let exit = 1;
   112	  const columnDelta = to.column - from.column;
   113	  const minimumColumn = cell.column + epsilon;
   114	  const maximumColumn = cell.column + 1 - epsilon;
   115	  if (columnDelta === 0) {
   116	    if (from.column <= minimumColumn || from.column >= maximumColumn) return false;
   117	  } else {
   118	    const first = (minimumColumn - from.column) / columnDelta;
   119	    const second = (maximumColumn - from.column) / columnDelta;
   120	    entry = Math.max(entry, Math.min(first, second));
   390	    dividend = divisor;
   391	    divisor = remainder;
   392	  }
   393	  return dividend;
   394	}
   395	
   396	function sharedVertexBlockingCell(
   397	  column: number,
   398	  row: number,
   399	  sightBlockingAt: (cell: GridCell) => boolean,
   400	): GridCell | null {
   401	  const topLeft = { column: column - 1, row: row - 1 };
   402	  const topRight = { column, row: row - 1 };
   403	  // Check TL/BR first so lower row then lower column wins when both pairs seal.
   404	  if (sightBlockingAt(topLeft) && sightBlockingAt({ column, row })) return topLeft;
   405	  if (sightBlockingAt(topRight) && sightBlockingAt({ column: column - 1, row })) return topRight;
   406	  return null;
   407	}
   408	
   409	function endpointSharedVertexBlockingCell(
   410	  column: number,
   411	  row: number,
   412	  ownCellQuadrant: CornerQuadrant,
   413	  sightBlockingAt: (cell: GridCell) => boolean,
   414	): GridCell | null {
   415	  // The endpoint's own cell is excluded from blocking, so only the other diagonal pair can seal.
   416	  if (ownCellQuadrant.column === ownCellQuadrant.row) {
   417	    const topRight = { column, row: row - 1 };
   418	    return sightBlockingAt(topRight) && sightBlockingAt({ column: column - 1, row })
   419	      ? topRight
   420	      : null;
   421	  }
   422	  const topLeft = { column: column - 1, row: row - 1 };
   423	  return sightBlockingAt(topLeft) && sightBlockingAt({ column, row }) ? topLeft : null;
   424	}
   425	
   426	function resolveCornerEndpoints(
   427	  endpoints: readonly CornerEndpoint[],
   428	  sightBlockingAt: (cell: GridCell) => boolean,
   429	): readonly ResolvedCornerEndpoint[] {
   430	  return endpoints.map((endpoint) => ({
   431	    ...endpoint,
   432	    blockingCell: endpointSharedVertexBlockingCell(
   433	      endpoint.point.column,
   434	      endpoint.point.row,
   435	      endpoint.cellQuadrant,
   436	      sightBlockingAt,
   437	    ),
   438	  }));
   439	}
   440	
   441	function firstSharedVertexBlockingHit(
   442	  from: CornerPoint,
   443	  to: CornerPoint,
   444	  sightBlockingAt: (cell: GridCell) => boolean,
   445	): SightBlockingHit | null {
   446	  if (![from.column, from.row, to.column, to.row].every(Number.isInteger)) return null;
   447	  const columnDelta = to.column - from.column;
   448	  const rowDelta = to.row - from.row;
   449	  const latticeSteps = greatestCommonDivisor(columnDelta, rowDelta);
   450	  if (latticeSteps <= 1) return null;
   451	  const columnStep = columnDelta / latticeSteps;
   452	  const rowStep = rowDelta / latticeSteps;
   453	  // Strict-interior vertices exclude t=0 and t=1; the direction-aware endpoint rule handles those separately.
   454	  for (let index = 1; index < latticeSteps; index += 1) {
   455	    const column = from.column + columnStep * index;
   456	    const row = from.row + rowStep * index;
   457	    const cell = sharedVertexBlockingCell(column, row, sightBlockingAt);
   458	    if (cell !== null) return { cell, progress: index / latticeSteps };
   459	  }
   460	  return null;
   461	}
   462	
   463	function liesInClosedQuadrant(
   464	  columnDelta: number,
   465	  rowDelta: number,
   466	  ownCellQuadrant: CornerQuadrant,
   467	): boolean {
   468	  return columnDelta * ownCellQuadrant.column >= 0 &&
   469	    rowDelta * ownCellQuadrant.row >= 0;
   470	}
   471	
   472	function endpointSharedVertexBlockingHit(
   473	  from: CornerPoint,
   474	  to: CornerPoint,
   475	  sourceCellQuadrant: CornerQuadrant,
   476	  targetCellQuadrant: CornerQuadrant,
   477	  sourceBlockingCell: GridCell | null,
   478	  targetBlockingCell: GridCell | null,
   479	): SightBlockingHit | null {
   480	  if (![from.column, from.row, to.column, to.row].every(Number.isInteger)) return null;
   481	  const columnDelta = to.column - from.column;
   482	  const rowDelta = to.row - from.row;
   483	  if (columnDelta === 0 && rowDelta === 0) {
   484	    const oppositeSpaces = sourceCellQuadrant.column === -targetCellQuadrant.column &&
   485	      sourceCellQuadrant.row === -targetCellQuadrant.row;
   486	    return sourceBlockingCell !== null && oppositeSpaces
   487	      ? { cell: sourceBlockingCell, progress: 0 }
   488	      : null;
   489	  }
   490	  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
   491	  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   492	  if (sourceBlockingCell !== null &&
   493	    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
   494	    return { cell: sourceBlockingCell, progress: 0 };
   495	  }
   496	  if (targetBlockingCell !== null &&
   497	    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
   498	    return { cell: targetBlockingCell, progress: 1 };
   499	  }
   500	  return null;
   501	}
   502	
   503	function cellInteriorEntryProgress(from: CornerPoint, to: CornerPoint, cell: GridCell): number {
   504	  let entry = 0;
   505	  const columnDelta = to.column - from.column;
   506	  if (columnDelta !== 0) {
   507	    entry = Math.max(entry, Math.min(
   508	      (cell.column - from.column) / columnDelta,
   509	      (cell.column + 1 - from.column) / columnDelta,
   510	    ));
   735	        ...indexedSources.values(),
   736	        ...sightBlockingCells.map((cell) => ({ cell, sources: [] })),
   737	      ].map((entry) => [cellKey(entry.cell), entry] as const)).values()];
   738	  const candidates = candidateEntries
   739	    .filter((entry) => !exclusions.has(cellKey(entry.cell)) &&
   740	      entry.cell.column < maximumColumn && entry.cell.column + 1 > minimumColumn &&
   741	      entry.cell.row < maximumRow && entry.cell.row + 1 > minimumRow)
   742	    .map((entry) => entry.cell);
   743	  const sightBlockingKeys = new Set([
   744	    ...sightBlockingCells.map(cellKey),
   745	    ...[...indexedSources]
   746	      .filter(([, entry]) => entry.sources.some((source) => source.tier === 'total'))
   747	      .map(([key]) => key),
   748	  ]);
   749	  const sightBlockingAt = (cell: GridCell): boolean => {
   750	    const key = cellKey(cell);
   751	    return !exclusions.has(key) && sightBlockingKeys.has(key);
   752	  };
   753	  const resolvedSourceEndpoints = resolveCornerEndpoints(sourceEndpoints, sightBlockingAt);
   754	  const resolvedTargetEndpoints = resolveCornerEndpoints(targetEndpoints, sightBlockingAt);
   755	  const traces = resolvedSourceEndpoints.map((sourceEndpoint) => aggregateCornerLines(
   756	    sourceEndpoint.point,
   757	    resolvedTargetEndpoints.map((targetEndpoint) => traceCornerLine(
   758	      sourceEndpoint.point,
   759	      targetEndpoint.point,
   760	      sourceEndpoint.cellQuadrant,
   761	      targetEndpoint.cellQuadrant,
   762	      sourceEndpoint.blockingCell,
   763	      targetEndpoint.blockingCell,
   764	      candidates,
   765	      (cell) => indexedSources.get(cellKey(cell))?.sources ?? [],
   766	      sightBlockingAt,
   767	    )),
   768	    nearest.sourceCell,
   769	    nearest.targetCell,
   770	  ));
   771	  const first = traces[0];
   772	  if (first === undefined) throw new RangeError('A line query requires non-empty source and target spaces.');
   773	  const physicalSelected = traces.reduce(
   774	    (best, candidate) => comparePhysicalCornerTraces(candidate, best) < 0 ? candidate : best,
   775	    first,
   776	  );
   777	  const blocksSight = traces.every((trace) => trace.blocksSight);
   778	  const opticalSelected = blocksSight
   779	    ? traces.reduce(
   780	        (best, candidate) => compareOpticalCornerTraces(candidate, best) < 0 ? candidate : best,
   781	        first,
   782	      )
   783	    : null;
   784	  const selected: TerrainLineTrace = {
   785	    ...physicalSelected,
   860	    ...(options.additionalSightBlockingCells === undefined
   861	      ? {}
   862	      : { additionalSightBlockingCells: options.additionalSightBlockingCells }),
   863	  });
   864	  const stateTraces = combatantLineTraces.get(state) ?? new Map<string, TerrainLineTrace>();
   865	  stateTraces.set(traceKey, trace);
   866	  if (!combatantLineTraces.has(state)) combatantLineTraces.set(state, stateTraces);
   867	  return trace;
   868	}
   869	
   870	export function coverTierBetweenObjects(
   871	  objects: readonly WorldObject[],
   872	  from: GridCell,
   873	  to: GridCell,
   874	): CoverTier {
   875	  const exclusions = new Set([cellKey(from), cellKey(to)]);
   876	  const candidates = [...new Map(objects
   877	    .flatMap((object) => object.footprint)
   878	    .filter((cell) => !exclusions.has(cellKey(cell)))
   879	    .map((cell) => [cellKey(cell), cell] as const)).values()];
   880	  const sourceCell = { ...from };
   881	  const targetCell = { ...to };
   882	  const sourceEndpoints = outerCornerEndpoints([from]);
   883	  const targetEndpoints = outerCornerEndpoints([to]);
   884	  const sourcesAt = (cell: GridCell): readonly TerrainLineSource[] =>
   885	    objects.flatMap((object): readonly TerrainLineSource[] =>
   886	      objectOccupiesCell(object, cell) && object.blocking.cover !== 'none'
   887	        ? [{ kind: 'world_object', id: String(object.id), tier: object.blocking.cover }]
   888	        : []);
   889	  const sightBlockingAt = (cell: GridCell): boolean =>
   890	    !exclusions.has(cellKey(cell)) && sourcesAt(cell).some((source) => source.tier === 'total');
   891	  const resolvedSourceEndpoints = resolveCornerEndpoints(sourceEndpoints, sightBlockingAt);
   892	  const resolvedTargetEndpoints = resolveCornerEndpoints(targetEndpoints, sightBlockingAt);
   893	  const traces = resolvedSourceEndpoints.map((sourceEndpoint) => aggregateCornerLines(
   894	    sourceEndpoint.point,
   895	    resolvedTargetEndpoints.map((targetEndpoint) => traceCornerLine(
   896	      sourceEndpoint.point,
   897	      targetEndpoint.point,
   898	      sourceEndpoint.cellQuadrant,
   899	      targetEndpoint.cellQuadrant,
   900	      sourceEndpoint.blockingCell,
   901	      targetEndpoint.blockingCell,
   902	      candidates,
   903	      sourcesAt,
   904	      sightBlockingAt,
   905	    )),
   906	    sourceCell,
   907	    targetCell,
   908	  ));
   909	  const first = traces[0];
   910	  if (first === undefined) throw new RangeError('A line query requires non-empty source and target spaces.');
   911	  return traces.reduce((best, candidate) =>
   912	    comparePhysicalCornerTraces(candidate, best) < 0 ? candidate : best, first).tier;
   913	}
   914	
   915	export type CreatureCoverOptions = CreatureLineOptions;
   916	
   917	export interface CombatantCoverResult {
   918	  readonly tier: CoverTier;
   919	  readonly sourceIds: readonly string[];
   920	}
   921	
   922	export function coverBetweenCombatants(
   923	  state: EncounterState,
   924	  sourceId: CombatantId,
   925	  targetId: CombatantId,
   926	  options: CreatureCoverOptions = {},
   927	): CombatantCoverResult {
   928	  const trace = traceCombatantLine(state, sourceId, targetId, options);
   929	  return { tier: trace.tier, sourceIds: trace.sourceIds };
   930	}
  1110	    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 0, row: 3 });
  1111	
  1112	    // The observer's bottom-left corner is vertex (2,2); its cell lies TR of that vertex.
  1113	    // Ray (2,2)->(0,4) leaves into the opposite BL quadrant through the pinch sealed by
  1114	    // TL (1,1) + BR (2,2). Lower-row cell (1,1) wins; physical cover remains None.
  1115	    expect(trace).toMatchObject({
  1116	      tier: 'none',
  1117	      blocksSight: true,
  1118	      firstBlockingCell: { column: 1, row: 1 },
  1119	    });
  1120	  });
  1121	
  1122	  it('TARGET_ENDPOINT_JUNCTION_SEALED', () => {
  1123	    const observer = normalProfile('target-endpoint-observer');
  1124	    const blockers = [
  1125	      { column: 1, row: 0 }, { column: 1, row: 1 },
  1126	      { column: 2, row: 2 }, { column: 2, row: 3 },
  1127	      { column: 2, row: 4 }, { column: 2, row: 5 },
  1128	    ];
  1129	    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
  1130	    const trace = traceTerrainLine(state, { column: 0, row: 3 }, { column: 2, row: 1 });
  1131	
  1132	    // Mirrored ray (0,4)->(2,2) arrives from the BL quadrant opposite the target cell's
  1133	    // TR quadrant. TL (1,1) + BR (2,2) seal vertex (2,2); physical Half Cover is unchanged.
  1134	    expect(trace).toMatchObject({
  1135	      tier: 'half',
  1136	      blocksSight: true,
  1137	      firstBlockingCell: { column: 1, row: 1 },
  1138	    });
  1139	  });
  1140	
  1141	  it('SAME_SIDE_ENDPOINT_GRAZE_CLEAR', () => {
  1142	    const observer = normalProfile('same-side-endpoint-observer');
  1143	    const blockers = [
  1144	      { column: 1, row: 0 }, { column: 1, row: 1 },
  1145	      { column: 2, row: 2 }, { column: 2, row: 3 },
  1146	      { column: 2, row: 4 }, { column: 2, row: 5 },
  1147	    ];
  1148	    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
  1149	    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 5, row: 1 });
  1150	    const reverseTrace = traceTerrainLine(state, { column: 5, row: 1 }, { column: 2, row: 1 });
  1151	    const sameSideEndpoint = reverseTrace.lines.find((line) =>
  1152	      line.targetCorner.column === 2 && line.targetCorner.row === 2);
  1153	
  1154	    // Source corner (2,2) belongs to a cell in its TR quadrant. Rays toward (5,1) stay
  1155	    // on that same side or leave along a cell edge; neither crosses the sealed pinch.
  1156	    expect(trace).toMatchObject({ tier: 'none', blocksSight: false, firstBlockingCell: null });
  1157	    // Mirrored ray (5,1)->(2,2) arrives from the target cell's own TR quadrant, not the
  1158	    // opposite BL quadrant. Pin it directly so an indiscriminate sealed-endpoint rule fails.
  1159	    expect(sameSideEndpoint).toMatchObject({ blocksSight: false, firstBlockingCell: null });
  1160	  });
  1161	
  1162	  it('ADJACENT_CORNER_ZERO_LENGTH', () => {
  1163	    const observer = normalProfile('zero-length-endpoint-observer');
  1164	    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
  1165	    const state = fieldState(observer, { bounds: { columns: 4, rows: 4 }, blockedCells: blockers });
  1166	    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
  1167	    const zeroLength = trace.lines.find((line) =>
  1168	      line.targetCorner.column === 2 && line.targetCorner.row === 2);
  1169	    const diagonal = trace.lines.find((line) =>
  1170	      line.targetCorner.column === 1 && line.targetCorner.row === 3);
  1171	
  1172	    // At shared corner (2,2), the source cell is TR and target cell BL: opposite free
  1173	    // quadrants around the TL (1,1) + BR (2,2) pair. Their zero-length corner ray is
  1174	    // blocked at the solid-wall pinch, as is the ray leaving diagonally into target-side BL.
  1175	    expect(trace.sourceCorner).toEqual({ column: 2, row: 2 });
  1176	    expect(zeroLength).toMatchObject({
  1177	      tier: 'none', blocksSight: true, firstBlockingCell: { column: 1, row: 1 },
  1178	    });
  1179	    expect(diagonal).toMatchObject({
  1180	      tier: 'none', blocksSight: true, firstBlockingCell: { column: 1, row: 1 },
  1181	    });
  1182	    expect(trace.tier).toBe('none');
  1183	
  1184	    const open = { ...state, blockedCells: [{ column: 1, row: 1 }] };
  1185	    const openTrace = traceTerrainLine(open, { column: 2, row: 1 }, { column: 1, row: 2 });
  1186	    const openZeroLength = openTrace.lines.find((line) =>
  1187	      line.targetCorner.column === 2 && line.targetCorner.row === 2);
  1188	    // Without BR (2,2), no sealed pair exists: the identical zero-length contact is clear.
  1189	    expect(openTrace.sourceCorner).toEqual({ column: 2, row: 2 });
  1190	    expect(openZeroLength).toMatchObject({ blocksSight: false, firstBlockingCell: null });
  1191	    expect(openTrace.blocksSight).toBe(false);
  1192	  });
  1193	
  1194	  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
  1195	    const observer = normalProfile('adjacent-junction-observer');
  1196	    const blockers = [
  1197	      { column: 1, row: 0 }, { column: 1, row: 1 },
  1198	      { column: 2, row: 2 }, { column: 2, row: 3 },
  1199	      { column: 2, row: 4 }, { column: 2, row: 5 },
  1200	    ];
  1201	    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
  1202	    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
  1203	
  1204	    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
  1205	    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
  1206	    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
  1207	    expect(trace).toMatchObject({
  1208	      tier: 'none',
  1209	      blocksSight: true,
  1210	      firstBlockingCell: { column: 1, row: 1 },
  1211	    });
  1212	  });
  1213	
  1214	  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
  1215	    const observer = normalProfile('edge-arrival-observer');
  1216	    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
  1217	    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
  1218	    const options = { additionalSightBlockingCells: blockers } as const;
  1219	    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
  1220	    const verticalArrival = verticalTrace.lines.find((line) =>
  1221	      line.targetCorner.column === 2 && line.targetCorner.row === 2);
  1222	    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
  1223	    const horizontalArrival = horizontalTrace.lines.find((line) =>
  1224	      line.targetCorner.column === 2 && line.targetCorner.row === 2);
  1225	
  1226	    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
  1227	    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
  1228	    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
  1229	    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
  1230	    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
  1231	    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
  1232	    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
  1233	  });
  1234	
  1235	  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
  1236	    const observer = normalProfile('own-edge-departure-observer');
  1237	    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
  1238	    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
  1239	    const trace = traceTerrainLine(
  1240	      state,
  1241	      { column: 2, row: 0 },
  1242	      { column: 2, row: 1 },
  1243	      { additionalSightBlockingCells: blockers },
  1244	    );
  1245	    const ownEdge = trace.lines.find((line) =>
  1246	      line.targetCorner.column === 2 && line.targetCorner.row === 2);
  1247	
  1248	    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
  1249	    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
  1250	    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
  1251	    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
  1252	  });
  1253	
  1254	  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
  1255	    const observer = normalProfile('far-face-departure-observer');
  1256	    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
  1257	    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
  1258	    const trace = traceTerrainLine(
  1259	      state,
  1260	      { column: 0, row: 2 },
  1261	      { column: 2, row: 1 },
  1262	      { additionalSightBlockingCells: blockers },
  1263	    );
  1264	    const farFace = trace.lines.find((line) =>
  1265	      line.targetCorner.column === 2 && line.targetCorner.row === 2);
  1266	
  1267	    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
  1268	    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
  1269	    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
  1270	    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
  1271	  });
  1272	
  1273	  it('STAIRCASE_WALL_SEALED', () => {
  1274	    const observer = normalProfile('staircase-wall-observer');
  1275	    const subject = normalProfile('staircase-wall-subject');
  1276	    const blockers = [{ column: 7, row: 3 }, { column: 8, row: 4 }];
  1277	    const state = fieldState(observer, {
  1278	      bounds: { columns: 15, rows: 8 },
  1279	      combatants: [observer, subject],
  1280	      tokens: [placedToken(observer, 13, 4), placedToken(subject, 1, 4)],
   235	    const projection = projectPlayerBoard(view, coordinator);
   236	    sceneSnapshot({
   237	      sceneId: 'scene:pending', projection, art: REFERENCE_ENCOUNTER_ART,
   238	      tokenIdentities: canonicalTokenIdentityIndex(state.tokens),
   239	    });
   240	    const after = persistedByteBaseline(state, coordinator);
   241	    // Fighter (2,3) has corners (2,3),(3,3),(2,4),(3,4). For each target (c,4), c=7,8,9:
   242	    // rays from either row-3 source corner to (c,4)/(c+1,4) enter Oak Door (6,3), while rays
   243	    // to (c,5)/(c+1,5) enter Stone Wall (6,4) (the (3,3)->(10,5) ray enters both). From either
   244	    // row-4 source corner, rays to (c,4)/(c+1,4) follow row line 4 between those two blockers,
   245	    // and rays to (c,5)/(c+1,5) enter the wall. Thus every corner ray is blocked for all three.
   246	    // D642 additionally seals lattice vertex (7,3), where blocked cell (7,2) and Oak Door
   247	    // (6,3) touch diagonally. The last clear row-3 rays include (3,3)->(8,3) and
   248	    // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
   249	    // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
   250	    // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
   251	    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
   252	    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
   253	    // were tier none / blocksSight false / first null under the diagonal-only endpoint
   254	    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
   255	    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
   256	    // fighter's concealment each gain exactly (7,3).
   257	    expect(projectDmView(state).state.foggedCells).toEqual([
   258	      { column: 4, row: 3 },
   259	      { column: 7, row: 3 },
   260	      { column: 8, row: 3 },
   261	      { column: 9, row: 3 },
   262	    ]);
   263	    expect(view.concealedCells).toEqual([
   264	      { column: 8, row: 2 },
   265	      { column: 9, row: 2 },
   266	      { column: 4, row: 3 },
   267	      { column: 7, row: 3 },
   268	      { column: 8, row: 3 },
   269	      { column: 9, row: 3 },
   270	      { column: 7, row: 4 },
   271	      { column: 8, row: 4 },
   272	      { column: 9, row: 4 },
   273	    ]);
   274	    // Pre-D635 concealed={(4,3)} and hashes were:
   275	    // pendingRequest=fdaf8775fe7c2cd87ffc3ab6cdafff23dcbf2c3f30c1bf41b9dd9d546902fa6e;
   276	    // coordinator=c31c026432dd2d523e582058ff432ffc4dd2d172dc01fcfd8fe4cbedaa328b0d;
   277	    // revision=15ba352a6817d7d0722e8135a00a33fa026f109a3ec3026819b9081b119e41dc.
   278	    // Pre-B4 revision=cd99e1c872f75ec7167c396e27088715990287458d39981b73b376d9e6284767;
   279	    // Pre-B2 revision=df6f2cceaa19f66115b0bcd387212064b02ae288fc63c47b4db8f71a835fa4ee;
   280	    // branch RNG fingerprint=163cf20067bebf7e70bb76d69b6f45a6123e51fdc409ba32a1b985b3f60b6a2b.
   281	    // B2 removes the empty migration key and recomputes the fingerprint as
   282	    // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
   283	    // Pre-D642 -> D642, derived directly from the hand-derived visibility sets above.
   284	    // pendingRequest:
   285	    // 78f31379db8355bb977b5a87cac2349137ff0aa606d32ba4080277856e874049 ->
   286	    // 0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0.
   287	    // coordinator:
   288	    // f8fa07803660b6ba53f256d885955af174f921fcaf58de46b1a7f0e697de9c66 ->
   289	    // 79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd.
   290	    // revision:
   291	    // 35c96ecc6772f0cd518b5de7beeb79e4e92af4b270a5e709cbc3077d8f900385 ->
   292	    // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
   293	    // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
   294	    // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
   295	    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
   296	    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
   297	    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
   298	    expect(before).toEqual({
   299	      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
   300	      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
   301	      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
   302	      branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
   303	    });
   304	    expect(canonicalJson(state)).toBe(encounterStateBytes);
   305	    expect(coordinator.pendingRequest).toMatchObject({
   306	      kind: 'turn',
   307	      requestId: 'request:handoff-baseline',
   308	      encounterRevision: 0,
   309	      actorId: 'combatant:fighter',
   310	      legalActions: { actions: [{ type: 'end_turn', actor: 'combatant:fighter' }] },
   311	    });
   312	    expect(coordinator.pendingRequest?.visibleState).toBe(view);
   313	    expect(coordinator.pendingRequest?.visibleState !== undefined &&
   314	      'tokenIdentityByCombatant' in coordinator.pendingRequest.visibleState).toBe(false);
   315	    expect(after).toEqual(before);

diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

codex
Implemented WALL-01 fix r5.

Files changed:

- [cover.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts:87)
- [visibility-field.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/combat/visibility-field.test.ts:1110)
- [scene-snapshot.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/vtt/scene-snapshot.test.ts:241)

### Implemented rule

At a sealed endpoint vertex, the ray is clear only when its other endpoint lies in the closed quadrant occupied by the endpoint’s own cell, including its two boundary half-lines. Every other direction crosses the pinch and blocks sight.

Source hits retain progress `0`; target hits retain progress `1`. Lower-row/lower-column selection, interior vertices, shared edges, cell interiors, and physical cover are unchanged. `coverTierBetweenObjects` uses the same endpoint resolution path.

### Witness derivations

| Witness | Geometry and result |
|---|---|
| `SOURCE_ENDPOINT_JUNCTION_SEALED` | At `(2,2)`, source cell `(2,1)` is TR; ray `(2,2)→(0,4)` leaves into opposite BL through TL `(1,1)` + BR `(2,2)`. Blocked, first `(1,1)`, tier `none`. |
| `TARGET_ENDPOINT_JUNCTION_SEALED` | Reverse ray `(0,4)→(2,2)` arrives from BL, outside target cell’s closed TR quadrant. Blocked, first `(1,1)`, tier remains `half`. |
| `SAME_SIDE_ENDPOINT_GRAZE_CLEAR` | Rays from `(2,2)` toward `(5,1)` remain in closed TR or on its boundaries. Clear, `first=null`. |
| `ADJACENT_CORNER_ZERO_LENGTH` | Source TR and target BL share `(2,2)` across TL `(1,1)` + BR `(2,2)`. Zero-length contact and the diagonal ray are blocked; removing BR restores clear sight. |
| `ADJACENT_CORNER_AGGREGATE_BLOCKED` | The r4 leaks `(2,1)→(2,2)` and `(3,2)→(2,2)` arrive along the sealed junction’s far faces. Both now block; aggregate is `tier none / blocksSight true / first (1,1)`. |
| `EDGE_ARRIVAL_AT_JUNCTION_BLOCKED` | Exact vertical and horizontal arrivals above/right of target cell’s closed BL quadrant both block at `(1,1)`. |
| `OWN_EDGE_DEPARTURE_CLEAR` | Mirrored `(2,2)→(2,0)` lies on closed TR’s own-edge boundary. Clear. |
| `FAR_FACE_DEPARTURE_BLOCKED` | Mirrored `(2,2)→(0,2)` lies left of closed TR, on the wall’s far face. Blocked, first `(1,1)`. |
| `PHYSICAL_COVER_UNCHANGED_BY_VERTEX` | Endpoint optical hit remains `tier none`; physical cover is not raised. |

### D569 scene normalization

Cell `(7,3)` was previously visible only through endpoint-edge rays:

- Fighter: `(2,3)→(7,3)`
- Wizard: `(1,3)→(7,3)`

At sealed vertex `(7,3)`, blocked cell `(7,2)` and Oak Door `(6,3)` form the diagonal pair. Both rays changed from `tier none / false / null` to `tier none / true / first (7,2)`. The cleric was already blocked by the door.

Therefore:

- DM fog gained exactly `(7,3)`.
- Fighter concealment gained exactly `(7,3)`.
- `pendingRequestHash`: `0ec0fbbc… → 28a1eb7e…`
- `coordinatorHash`: `79b076c9… → 87fc04ad…`
- `revisionChecksum`: `52305455… → 9ee96377…`
- Encounter bytes and branch RNG fingerprint remained unchanged: `dd802783…`.

All five r3 consequence tests passed: `5 passed`, `121 skipped`.

### Mutants

- r4 diagonal-only endpoint rule: `ADJACENT_CORNER_AGGREGATE_BLOCKED`, `EDGE_ARRIVAL_AT_JUNCTION_BLOCKED`, and `FAR_FACE_DEPARTURE_BLOCKED` red.
- Block every sealed endpoint: `OWN_EDGE_DEPARTURE_CLEAR` and `SAME_SIDE_ENDPOINT_GRAZE_CLEAR` red.
- Raise physical tier on endpoint hits: `PHYSICAL_COVER_UNCHANGED_BY_VERTEX` red, showing `none → total`.

All mutants were removed.

### Frozen-family counts

| Family | Before `bc6422bf` offers/shotless | After offers/shotless |
|---|---:|---:|
| hard, 75 monsters | 142 / 35 | 142 / 35 |
| brutal, 44 monsters | 57 / 34 | 57 / 34 |
| brutal-b, 42 monsters | 15 / 41 | 15 / 41 |
| brutal-2, 44 monsters | 58 / 36 | 58 / 36 |

The 42-seed generation byte check remained identical:

`count=42`, aggregate SHA-256 `7b884bf91213ddaba626ec77b0dbf874c99d5de6cb8e6e190c0be5b7e31c56fc`.

### Verification

- `npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=dot`  
  `54/54 passed`.
- Prescribed ten-suite audit: `246 passed`, exactly `35` known frozen/behaviour reds, `34.63s`.
- Replay+detection: `36/36 passed`, `38.76s`, below the 41-second ceiling.
- Los-cover: `109 passed`, three known load-sensitive 5-second timeouts:
  - `kills M576-E2-WALL-SEALS-ROOM for every living footprint size`: `5365ms`; isolated `5560ms`.
  - `retains a productive first-turn option for every living monster in the versioned basis`: `5593ms`; isolated `5835ms`.
  - `kills M576-E2-ONE-SIDE-CANNOT-CHANGE-COVER with a legal destination per side`: `5001ms`.
  No budgets or pins changed.
- `npx tsc -p tsconfig.app.json --noEmit`: passed.
- `npx tsc -p tsconfig.node.json --noEmit`: passed.
- `git diff --check`: passed.
- Lines over 120 characters: `0`.

The exact 35 expected reds remained:

- D569 blind experiment: six titles beginning `validates the exact v5…`, `dry-runs versioned…`, `adds exactly 180…`, `consumes the frozen second encounter family…`, `records exact model effort…`, and `rejects either decisive integrity field…`.
- Second-family manifest: the amended-range pin plus seed titles `6207002`, `6207004`–`6207009`.
- Brutal-b pins: seeds `6206001`–`6206010`.
- Room-generator brutal titles: seeds `6203001`–`6203004`, `6203006`, `6203008`–`6203010`.
- Behaviour titles:
  - `projects fresh options for every requested generated-room monster turn`
  - `does not repeat the frozen Room-8 four-actor zero-feet Dash plan`
  - `forms a bounded volatility-ranked menu from baseline dependencies and canonical movement proofs`

Final `git status --short`:

```text
 M src/combat/cover.ts
 M tests/unit/combat/visibility-field.test.ts
 M tests/unit/vtt/scene-snapshot.test.ts
```

WALL-01-FIX-R5 DONE
diff --git a//tmp/wall01-r5-scene-proof.ts b//tmp/wall01-r5-scene-proof.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f32728e3b851774a5b561d73cffde8c99d60415
--- /dev/null
+++ b//tmp/wall01-r5-scene-proof.ts
@@ -0,0 +1,108 @@
+import { combatToken } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/combatant';
+import { createEncounter } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/encounter';
+import { traceCombatantLineToCells } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover';
+import { projectDmView, projectPlayerView } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/visibility';
+import { armorClass, worldObjectId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { encounterBranchId, encounterSessionId } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/values';
+import { canonicalJson } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/commands/canonical-json';
+import { sha256 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/crypto/sha256';
+import { mulberry32 } from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/random';
+import {
+  EncounterSessionJournal,
+  MemoryBrowserSessionStore,
+  MemoryMirrorSink,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/session-persistence';
+import {
+  REFERENCE_FIGHTER_ID,
+  REFERENCE_PLAYER_IDS,
+  referenceEncounterSetup,
+} from '/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/reference-encounter';
+
+const setup = referenceEncounterSetup();
+const state = createEncounter({
+  ...setup,
+  worldObjects: [
+    {
+      id: worldObjectId('object:door'), name: 'Oak Door', kind: 'door',
+      position: { column: 6, row: 3 }, footprint: [{ column: 6, row: 3 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:wall'), name: 'Stone Wall', kind: 'barrier',
+      position: { column: 6, row: 4 }, footprint: [{ column: 6, row: 4 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: true, lineOfSight: true, cover: 'total' }, createdRevision: 0,
+    },
+    {
+      id: worldObjectId('object:torch'), name: 'Torch', kind: 'light-source',
+      position: { column: 4, row: 1 }, footprint: [{ column: 4, row: 1 }],
+      durability: { kind: 'indestructible' }, armorClass: armorClass(10), damageResponses: [],
+      blocking: { movement: false, lineOfSight: false, cover: 'none' }, createdRevision: 0,
+    },
+  ],
+});
+
+const target = [{ column: 7, row: 3 }];
+for (const observer of REFERENCE_PLAYER_IDS) {
+  const trace = traceCombatantLineToCells(state, observer, target);
+  console.log(JSON.stringify({
+    observer,
+    sourceCell: trace.sourceCell,
+    sourceCorner: trace.sourceCorner,
+    tier: trace.tier,
+    blocksSight: trace.blocksSight,
+    first: trace.firstBlockingCell,
+    rays: trace.lines.map((line) => ({
+      target: line.targetCorner,
+      tier: line.tier,
+      blocksSight: line.blocksSight,
+      first: line.firstBlockingCell,
+    })),
+  }));
+}
+console.log(JSON.stringify({
+  dmFog: projectDmView(state).state.foggedCells,
+  fighterConcealed: projectPlayerView(state, {
+    seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+  }).concealedCells,
+}));
+
+const visibleState = projectPlayerView(state, {
+  seatId: 'player:one', combatantId: REFERENCE_FIGHTER_ID,
+});
+const coordinator = {
+  requestSequence: 1,
+  pendingRequest: {
+    kind: 'turn' as const,
+    requestId: 'request:handoff-baseline',
+    encounterRevision: state.revision,
+    actorId: REFERENCE_FIGHTER_ID,
+    visibleState,
+    legalActions: { actions: [{ type: 'end_turn' as const, actor: REFERENCE_FIGHTER_ID }] },
+  },
+  pendingCommand: null,
+  continuation: { kind: 'idle' as const },
+  pause: null,
+};
+const store = new MemoryBrowserSessionStore();
+const sessionId = encounterSessionId('session:handoff-persistence-baseline');
+EncounterSessionJournal.create({
+  sessionId,
+  branchId: encounterBranchId('branch:handoff-persistence-baseline'),
+  encounterState: state,
+  coordinatorState: coordinator,
+  controllers: [],
+  rng: mulberry32(16_603),
+  store,
+  mirror: new MemoryMirrorSink(),
+});
+const revision = store.revisions(sessionId)[0];
+if (revision === undefined) throw new Error('missing revision');
+console.log(JSON.stringify({
+  derivation: 'fog +(7,3); fighter concealment +(7,3); encounter bytes unchanged',
+  pendingRequestHash: sha256(canonicalJson(coordinator.pendingRequest)),
+  coordinatorHash: sha256(canonicalJson(coordinator)),
+  revisionChecksum: revision.checksum,
+  branchRngStateFingerprint: revision.branchRngStateFingerprint,
+}));
diff --git a/src/combat/cover.ts b/src/combat/cover.ts
index 73a8b753accdc2eadb36cdea432cebfebb6c4806..29a577f1c49e578912cc329e5c77f846db8c304b
--- a/src/combat/cover.ts
+++ b/src/combat/cover.ts
@@ -460,14 +460,13 @@
   return null;
 }
 
-function entersOppositeQuadrant(
+function liesInClosedQuadrant(
   columnDelta: number,
   rowDelta: number,
   ownCellQuadrant: CornerQuadrant,
 ): boolean {
-  if (columnDelta === 0 || rowDelta === 0) return false;
-  return Math.sign(columnDelta) === -ownCellQuadrant.column &&
-    Math.sign(rowDelta) === -ownCellQuadrant.row;
+  return columnDelta * ownCellQuadrant.column >= 0 &&
+    rowDelta * ownCellQuadrant.row >= 0;
 }
 
 function endpointSharedVertexBlockingHit(
@@ -488,12 +487,14 @@
       ? { cell: sourceBlockingCell, progress: 0 }
       : null;
   }
+  // D657: the other endpoint must remain inside the endpoint cell's closed quadrant;
+  // either boundary half-line is an own-edge graze, while every other direction crosses the pinch.
   if (sourceBlockingCell !== null &&
-    entersOppositeQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
+    !liesInClosedQuadrant(columnDelta, rowDelta, sourceCellQuadrant)) {
     return { cell: sourceBlockingCell, progress: 0 };
   }
   if (targetBlockingCell !== null &&
-    entersOppositeQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
+    !liesInClosedQuadrant(-columnDelta, -rowDelta, targetCellQuadrant)) {
     return { cell: targetBlockingCell, progress: 1 };
   }
   return null;
diff --git a/tests/unit/combat/visibility-field.test.ts b/tests/unit/combat/visibility-field.test.ts
index cd59a17e0674a92556f88d044ab35a3939c9a3f4..db2515b844c9f2ebda644275ddad380748d133e9
--- a/tests/unit/combat/visibility-field.test.ts
+++ b/tests/unit/combat/visibility-field.test.ts
@@ -1191,6 +1191,85 @@
     expect(openTrace.blocksSight).toBe(false);
   });
 
+  it('ADJACENT_CORNER_AGGREGATE_BLOCKED', () => {
+    const observer = normalProfile('adjacent-junction-observer');
+    const blockers = [
+      { column: 1, row: 0 }, { column: 1, row: 1 },
+      { column: 2, row: 2 }, { column: 2, row: 3 },
+      { column: 2, row: 4 }, { column: 2, row: 5 },
+    ];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 }, blockedCells: blockers });
+    const trace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 });
+
+    // The observer is TR and the target BL of sealed vertex (2,2), between TL (1,1)
+    // and BR (2,2). The last r4-clear rays end at the pinch along its far faces:
+    // (2,1)->(2,2) and (3,2)->(2,2). D657 blocks both, sealing the aggregate at (1,1).
+    expect(trace).toMatchObject({
+      tier: 'none',
+      blocksSight: true,
+      firstBlockingCell: { column: 1, row: 1 },
+    });
+  });
+
+  it('EDGE_ARRIVAL_AT_JUNCTION_BLOCKED', () => {
+    const observer = normalProfile('edge-arrival-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const options = { additionalSightBlockingCells: blockers } as const;
+    const verticalTrace = traceTerrainLine(state, { column: 2, row: 1 }, { column: 1, row: 2 }, options);
+    const verticalArrival = verticalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+    const horizontalTrace = traceTerrainLine(state, { column: 3, row: 2 }, { column: 1, row: 2 }, options);
+    const horizontalArrival = horizontalTrace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies BL. Other endpoints (2,1) and (3,2)
+    // are respectively above and right of the closed BL quadrant, on the wall's far faces.
+    // Thus rays (2,1)->(2,2) and (3,2)->(2,2) both stop at TL (1,1) + BR (2,2).
+    expect(verticalTrace.sourceCorner).toEqual({ column: 2, row: 1 });
+    expect(horizontalTrace.sourceCorner).toEqual({ column: 3, row: 2 });
+    expect(verticalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+    expect(horizontalArrival).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
+  it('OWN_EDGE_DEPARTURE_CLEAR', () => {
+    const observer = normalProfile('own-edge-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 2, row: 0 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const ownEdge = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (2,0)->(2,2), equivalent
+    // to departure (2,2)->(2,0), lies on closed TR's vertical own-edge boundary.
+    expect(trace.sourceCorner).toEqual({ column: 2, row: 0 });
+    expect(ownEdge).toMatchObject({ blocksSight: false, firstBlockingCell: null });
+  });
+
+  it('FAR_FACE_DEPARTURE_BLOCKED', () => {
+    const observer = normalProfile('far-face-departure-observer');
+    const blockers = [{ column: 1, row: 1 }, { column: 2, row: 2 }];
+    const state = fieldState(observer, { bounds: { columns: 6, rows: 6 } });
+    const trace = traceTerrainLine(
+      state,
+      { column: 0, row: 2 },
+      { column: 2, row: 1 },
+      { additionalSightBlockingCells: blockers },
+    );
+    const farFace = trace.lines.find((line) =>
+      line.targetCorner.column === 2 && line.targetCorner.row === 2);
+
+    // At target vertex (2,2), its cell lies TR. Mirrored ray (0,2)->(2,2), equivalent
+    // to departure (2,2)->(0,2), lies left of closed TR on the wall's far face.
+    expect(trace.sourceCorner).toEqual({ column: 0, row: 2 });
+    expect(farFace).toMatchObject({ blocksSight: true, firstBlockingCell: { column: 1, row: 1 } });
+  });
+
   it('STAIRCASE_WALL_SEALED', () => {
     const observer = normalProfile('staircase-wall-observer');
     const subject = normalProfile('staircase-wall-subject');
diff --git a/tests/unit/vtt/scene-snapshot.test.ts b/tests/unit/vtt/scene-snapshot.test.ts
index c7a8b7229a75ea41929a955407043ba62a5d262d..d1bc92916fc0bd1fd95f96d2dec2b3c42e14f05b
--- a/tests/unit/vtt/scene-snapshot.test.ts
+++ b/tests/unit/vtt/scene-snapshot.test.ts
@@ -248,10 +248,15 @@
     // (2,3)->(9,3): each was tier none / blocksSight false / first null, and is now tier
     // none / blocksSight true / first (7,2). Thus party-derived DM fog gains (8,3),(9,3).
     // Fighter-only concealment also gains (8,2),(9,2), whose bottom-corner rays are the
-    // same sealed row-3 rays, plus (8,3),(9,3). Cell (7,3) remains visible because the
-    // sealing vertex is its target endpoint, which is excluded by the strict-interior rule.
+    // same sealed row-3 rays, plus (8,3),(9,3). D657 then seals edge-aligned arrivals at
+    // endpoint vertex (7,3): the fighter's (2,3)->(7,3) and wizard's (1,3)->(7,3) rays
+    // were tier none / blocksSight false / first null under the diagonal-only endpoint
+    // rule, and are tier none / blocksSight true / first (7,2) under the closed-quadrant
+    // rule. The cleric's ray was already blocked by Oak Door (6,3), so DM fog and the
+    // fighter's concealment each gain exactly (7,3).
     expect(projectDmView(state).state.foggedCells).toEqual([
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
     ]);
@@ -259,6 +264,7 @@
       { column: 8, row: 2 },
       { column: 9, row: 2 },
       { column: 4, row: 3 },
+      { column: 7, row: 3 },
       { column: 8, row: 3 },
       { column: 9, row: 3 },
       { column: 7, row: 4 },
@@ -286,10 +292,13 @@
     // 52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8.
     // Encounter-state bytes do not change, so the branch RNG state fingerprint remains
     // dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a.
+    // D642 -> D657 adds only hand-derived cell (7,3) to DM fog and fighter concealment:
+    // pendingRequest 0ec0fbbc... -> 28a1eb7e..., coordinator 79b076c9... -> 87fc04ad...,
+    // and revision 52305455... -> 9ee96377...; encounter-state bytes remain unchanged.
     expect(before).toEqual({
-      pendingRequestHash: '0ec0fbbc308e5b744b61bd609c7107c1cdda97a6d561d2787b8b3bf4f194b8e0',
-      coordinatorHash: '79b076c9c5cbc8312f72a917753f2aeabc3f63dc8ec2f8a259e530418c3b36cd',
-      revisionChecksum: '52305455f1650a70231e6c7d2d322ca344bda7192a37632c050a3bc4f75fb3a8',
+      pendingRequestHash: '28a1eb7efdd80c746bc0bee68beffcc41dd88628f3d9023abc707585cd1deeda',
+      coordinatorHash: '87fc04ad58800bb678aa6ee2e18ec452c043662ec252713f3b7898c719d4824f',
+      revisionChecksum: '9ee9637765265f35eef5dc6f72331ea87405927c78523074079ca6308fdd4b0a',
       branchRngStateFingerprint: 'dd802783e2936f2c144e487bb48fa8e6a17e05dbaefb181d40c74647bb66b46a',
     });
     expect(canonicalJson(state)).toBe(encounterStateBytes);

tokens used
646,842
Implemented WALL-01 fix r5.

Files changed:

- [cover.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/combat/cover.ts:87)
- [visibility-field.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/combat/visibility-field.test.ts:1110)
- [scene-snapshot.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/vtt/scene-snapshot.test.ts:241)

### Implemented rule

At a sealed endpoint vertex, the ray is clear only when its other endpoint lies in the closed quadrant occupied by the endpoint’s own cell, including its two boundary half-lines. Every other direction crosses the pinch and blocks sight.

Source hits retain progress `0`; target hits retain progress `1`. Lower-row/lower-column selection, interior vertices, shared edges, cell interiors, and physical cover are unchanged. `coverTierBetweenObjects` uses the same endpoint resolution path.

### Witness derivations

| Witness | Geometry and result |
|---|---|
| `SOURCE_ENDPOINT_JUNCTION_SEALED` | At `(2,2)`, source cell `(2,1)` is TR; ray `(2,2)→(0,4)` leaves into opposite BL through TL `(1,1)` + BR `(2,2)`. Blocked, first `(1,1)`, tier `none`. |
| `TARGET_ENDPOINT_JUNCTION_SEALED` | Reverse ray `(0,4)→(2,2)` arrives from BL, outside target cell’s closed TR quadrant. Blocked, first `(1,1)`, tier remains `half`. |
| `SAME_SIDE_ENDPOINT_GRAZE_CLEAR` | Rays from `(2,2)` toward `(5,1)` remain in closed TR or on its boundaries. Clear, `first=null`. |
| `ADJACENT_CORNER_ZERO_LENGTH` | Source TR and target BL share `(2,2)` across TL `(1,1)` + BR `(2,2)`. Zero-length contact and the diagonal ray are blocked; removing BR restores clear sight. |
| `ADJACENT_CORNER_AGGREGATE_BLOCKED` | The r4 leaks `(2,1)→(2,2)` and `(3,2)→(2,2)` arrive along the sealed junction’s far faces. Both now block; aggregate is `tier none / blocksSight true / first (1,1)`. |
| `EDGE_ARRIVAL_AT_JUNCTION_BLOCKED` | Exact vertical and horizontal arrivals above/right of target cell’s closed BL quadrant both block at `(1,1)`. |
| `OWN_EDGE_DEPARTURE_CLEAR` | Mirrored `(2,2)→(2,0)` lies on closed TR’s own-edge boundary. Clear. |
| `FAR_FACE_DEPARTURE_BLOCKED` | Mirrored `(2,2)→(0,2)` lies left of closed TR, on the wall’s far face. Blocked, first `(1,1)`. |
| `PHYSICAL_COVER_UNCHANGED_BY_VERTEX` | Endpoint optical hit remains `tier none`; physical cover is not raised. |

### D569 scene normalization

Cell `(7,3)` was previously visible only through endpoint-edge rays:

- Fighter: `(2,3)→(7,3)`
- Wizard: `(1,3)→(7,3)`

At sealed vertex `(7,3)`, blocked cell `(7,2)` and Oak Door `(6,3)` form the diagonal pair. Both rays changed from `tier none / false / null` to `tier none / true / first (7,2)`. The cleric was already blocked by the door.

Therefore:

- DM fog gained exactly `(7,3)`.
- Fighter concealment gained exactly `(7,3)`.
- `pendingRequestHash`: `0ec0fbbc… → 28a1eb7e…`
- `coordinatorHash`: `79b076c9… → 87fc04ad…`
- `revisionChecksum`: `52305455… → 9ee96377…`
- Encounter bytes and branch RNG fingerprint remained unchanged: `dd802783…`.

All five r3 consequence tests passed: `5 passed`, `121 skipped`.

### Mutants

- r4 diagonal-only endpoint rule: `ADJACENT_CORNER_AGGREGATE_BLOCKED`, `EDGE_ARRIVAL_AT_JUNCTION_BLOCKED`, and `FAR_FACE_DEPARTURE_BLOCKED` red.
- Block every sealed endpoint: `OWN_EDGE_DEPARTURE_CLEAR` and `SAME_SIDE_ENDPOINT_GRAZE_CLEAR` red.
- Raise physical tier on endpoint hits: `PHYSICAL_COVER_UNCHANGED_BY_VERTEX` red, showing `none → total`.

All mutants were removed.

### Frozen-family counts

| Family | Before `bc6422bf` offers/shotless | After offers/shotless |
|---|---:|---:|
| hard, 75 monsters | 142 / 35 | 142 / 35 |
| brutal, 44 monsters | 57 / 34 | 57 / 34 |
| brutal-b, 42 monsters | 15 / 41 | 15 / 41 |
| brutal-2, 44 monsters | 58 / 36 | 58 / 36 |

The 42-seed generation byte check remained identical:

`count=42`, aggregate SHA-256 `7b884bf91213ddaba626ec77b0dbf874c99d5de6cb8e6e190c0be5b7e31c56fc`.

### Verification

- `npx vitest run tests/unit/combat/visibility-field.test.ts --reporter=dot`  
  `54/54 passed`.
- Prescribed ten-suite audit: `246 passed`, exactly `35` known frozen/behaviour reds, `34.63s`.
- Replay+detection: `36/36 passed`, `38.76s`, below the 41-second ceiling.
- Los-cover: `109 passed`, three known load-sensitive 5-second timeouts:
  - `kills M576-E2-WALL-SEALS-ROOM for every living footprint size`: `5365ms`; isolated `5560ms`.
  - `retains a productive first-turn option for every living monster in the versioned basis`: `5593ms`; isolated `5835ms`.
  - `kills M576-E2-ONE-SIDE-CANNOT-CHANGE-COVER with a legal destination per side`: `5001ms`.
  No budgets or pins changed.
- `npx tsc -p tsconfig.app.json --noEmit`: passed.
- `npx tsc -p tsconfig.node.json --noEmit`: passed.
- `git diff --check`: passed.
- Lines over 120 characters: `0`.

The exact 35 expected reds remained:

- D569 blind experiment: six titles beginning `validates the exact v5…`, `dry-runs versioned…`, `adds exactly 180…`, `consumes the frozen second encounter family…`, `records exact model effort…`, and `rejects either decisive integrity field…`.
- Second-family manifest: the amended-range pin plus seed titles `6207002`, `6207004`–`6207009`.
- Brutal-b pins: seeds `6206001`–`6206010`.
- Room-generator brutal titles: seeds `6203001`–`6203004`, `6203006`, `6203008`–`6203010`.
- Behaviour titles:
  - `projects fresh options for every requested generated-room monster turn`
  - `does not repeat the frozen Room-8 four-actor zero-feet Dash plan`
  - `forms a bounded volatility-ranked menu from baseline dependencies and canonical movement proofs`

Final `git status --short`:

```text
 M src/combat/cover.ts
 M tests/unit/combat/visibility-field.test.ts
 M tests/unit/vtt/scene-snapshot.test.ts
```

WALL-01-FIX-R5 DONE
