
OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-vis-field
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR] (network access enabled)
reasoning effort: high
reasoning summaries: none
session id: 01a0ae59-86c2-7cb1-8b2e-bbf96f72fd73
--------
user
COMMON RULES (binding, restated in full): You are the IMPLEMENTER (gpt-5.6-sol, high, RESUMED — the session that built LAND-01) for VIS-FIELD-01's LANDING RECONCILIATION. Worktree /home/vagrant/PhpstormProjects/dnd-wt-vis-field (branch claude/vis-field at ebfcacaa = the complete frozen plan + LAND-01 as you left it, committed by the supervisor; own node_modules). The worktree carries ONE uncommitted file, tests/browser/vtt-encounter.spec.ts (the LAND-02 re-pin, under supervisor verification): do NOT touch it and do NOT revert it. Sandbox workspace-write in that worktree ONLY (+ /tmp). No git write commands (the supervisor commits). No edits under .claude/** or docs/**. Do NOT invoke claude, `claude -p`, any reviewer session, any consensus workflow or any other agent; skills under ~/.codex do NOT apply. Playwright ONLY on port 4470 (PLAYWRIGHT_PORT=4470 PLAYWRIGHT_WORKERS=1), never 4173, never the full browser suite, never full vitest, never npm run build. Model calls WILL fail. Never regenerate an expectation from our own output; never delete a test to reach green; never fresh hashes. Report exact commands and numbers; end with the named marker. The main repo's .claude/decisions.md D635.35–D635.42 and .claude/consensus/vis-field/{review-impl-land-01-astra-r1.md,impl-land-02-sol.md} are readable.

TASK A — LAND-01 fix r1 (astra LAND-F1, P2, verified by the supervisor: resolveTargetSave at encounter.ts:3577 and the force_save branch at :12451 trace no sight line, so executing a save through reduceEncounter cannot demonstrate sightline agreement). In tests/unit/vtt/engine-query-port.test.ts (the agreement property at ~:574 and the LAND-01 witnesses): (1) for EVERY resolved attack and save component the property must assert directly that the trace from the resolved origin to the target — traceCombatantLine with the SAME inputs the query uses (actor, target, sourceAnchor = origin, footprints, exclusions, default optical blockers; cite the query line) — has blocksSight === false and tier !== 'total'; keep reducer execution as an additional check and word its description accurately (attacks: the reducer enforces sight; saves: it does not). (2) Add a blocked-save query witness: a state where a save option's only origins have a sight-blocking trace to the target (e.g. Web or a monster breath save behind a full blocking column) → the option is not offered / the query refuses with target_outside_line_of_sight, with a control where the column is removed and the save IS offered. (3) Prove the property now catches astra's mutant: insert a full blocking column into each save execution state (astra's in-memory probe; the property must go RED), restore, report both runs. No production code changes in Task A.

TASK B — LAND-03 (supervisor ruling D635.42, a manifest addition under landing reconciliation): the browser spec tests/browser/ai-dm-board-snapshot.spec.ts:258 times out in #waitForBoardVisible (tools/ai-dm-board-snapshot.ts:992-1030) because the service binds to boardStateDigest(state) = sha256(canonicalJson(state)) (tools/ai-dm-board-snapshot.ts:201) while the app renders projection.stateDigest = sha256(canonicalJson(projectDmView(state).state)) (src/vtt/encounter-projections.ts:488, written at encounter-app.ts:2308). Since B3 the DM view state carries the DERIVED fog (visibility.ts:~524) that canonical state no longer holds (B2), so the two digests differ for every state with a non-empty derived field — every live blind capture (ai-dm-conversation.ts:4487, glyph captures :118, snapshot-check :20, blind-board-snapshot-check :25/:55, screenshot probe) would wait 30 s and fail, not just the spec. RULING: the binding digest IS the digest of the DM view state, as the app defines it. Change boardStateDigest(state) to return sha256(canonicalJson(projectDmView(state).state)) (import already present at :22; reuse the app's exact canonicalisation so the two are the same function of the same input) so every service caller binds to the value the app renders. Do not change encounter-projections.ts or encounter-app.ts. WITNESS in tests/unit/tools/ai-dm-board-snapshot.test.ts: a state whose derived field is non-empty (a party member with normal sight, a blocker, dark cells beyond) where (a) boardStateDigest(state) === the DM projection's stateDigest (built through the real projection path, as :214 does) and (b) as a NEGATIVE CONTROL boardStateDigest(state) !== sha256(canonicalJson(state)) (this pins that the defect is real: with the old definition (a) fails); state in the test title that the derived field is what separates them. Then PLAYWRIGHT_PORT=4470 PLAYWRIGHT_WORKERS=1 npx playwright test tests/browser/ai-dm-board-snapshot.spec.ts --project=chromium must PASS (it passed on main and failed on 10cc9f5e). PINS: any test that recomputes the digest independently as sha256(canonicalJson(state)) (e.g. tests/unit/tools/ai-dm-board-delivery.test.ts:770) is re-derived to the projected form ONLY where it models "the digest of the state the renderer shows", with a one-line comment; where a pin protects historical bytes, apply D569's demonstrate-then-normalize (show the old value, the new value, and WHY exactly one field changed) — never a fresh hash without derivation. Run: tests/unit/tools/{ai-dm-board-snapshot,ai-dm-board-delivery,ai-dm-board-glyph-captures,ai-dm-blind-board-snapshot-check,ai-dm-screenshot-probe,d569-blind-experiment,d569-second-family-manifest,d569-v5}.test.ts (use the names that exist; list them) and tests/unit/tools/ai-dm-conversation.test.ts. The six D569 second-family manifest reds and the ten brutal-b productivity reds are EXPECTED (the frozen-family consequence awaiting the owner): report that the red SET is unchanged (same titles). ANY new red: report the derivation; if a manifest or fixture embeds stateDigest values that would need regeneration, STOP and report BLOCKED with the file:line rather than regenerating.

VERIFICATION (report each command and number): npx tsc -p tsconfig.app.json --noEmit; npx tsc -p tsconfig.node.json --noEmit; git diff --check; the suites above; tests/unit/vtt/engine-query-port.test.ts; the mutant run (Task A step 3) and, for Task B, the reverse mutant (restore the old boardStateDigest → the new witness must go RED, the browser spec need not be rerun); git status --short (expected: tests/browser/vtt-encounter.spec.ts unchanged by you + your files); no line over 120 columns. OUTPUT: a report listing files, the witness titles, the mutant results, the red set before/after, then "LAND-01-FIX-R1+LAND-03 DONE" (or BLOCKED with the reason).

codex
I’ll preserve the supervisor-owned browser re-pin untouched. First I’m reading D635.35–D635.42 plus the two reconciliation reports and confirming the `ebfcacaa`/one-dirty-file baseline; then I’ll build Task A’s direct trace oracle before changing the digest path for LAND-03.
exec
/bin/bash -lc "git rev-parse --short=8 HEAD
git branch --show-current
git status --short
sed -n '22945,23030p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
sed -n '1,260p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/review-impl-land-01-astra-r1.md
sed -n '1,320p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-land-02-sol.md" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
ebfcacaa
claude/vis-field
 M tests/browser/vtt-encounter.spec.ts
## D635.32 — SUPERVISOR (2026-09-17 01:24): B6 r2 complete on 11 files; its only red is a pre-existing D524 probe test now deterministically over its 5 s budget because of derived visibility — A/B measured by me; NAMED RE-BUDGET ruled (5 s → 10 s for that one test); the raised blind semantic cap flagged for astra and the owner; r3 dispatched
Sol (impl-b6-sol.md, 381 k tokens): v2 visibility block (observers + subjects transposes, strict schemas, generator-only JSON schema), player output omits it (with a forbidden-field control), serializer sentinel seam, effect obscurement union, Invisible subject keeps visible_from, truncation ordered light → adjacency → objects → visibility with the 8,192-byte calibration re-derived by hand (16,120 → 15,167 → 13,168 → 12,259 → 5,492 bytes), truncation recorded, the final single-owner stage, source-binding witnesses (standard-case string leaves 493 → 550, paths 131 → 143, explained), the semantic partition pin hand-derived [[4,0],[3,1]]; six plan mutants killed with sha-restored files; semantic 11/11, engine-mcp-handler 131/131, source-binding 6/6, support 227/227; tsc 0/0; OFFERS checks 0/0; D569 E1C/FOOTPRINTS unchanged; pair 38.85 → 38.72 s; 36 compiled foggedCells references, none a runtime state consumer; the final aggregate of all 30 plan-named suites 759/760 in 1,172 s — the sole red tests/unit/tools/ai-dm-screenshot-probe.test.ts:464 "is versioned, uses generic classic-board conventions, and leaks no non-rendered probe identity" at 5.33 s (isolated 5.18 / 5.28 s) against its D524 5 s budget; sol stopped rather than raise a timeout (correct). ALSO CHANGED BY SOL (flagged): the blind semantic cap SEMANTIC_BOARD_MAX_BYTES (or its blind-context counterpart) raised 8,192 → 32,768 because the retained hard fixtures' semantic boards now measure 13,406–17,597 bytes with the visibility block; that is a blind-DM context-size decision the plan left open ("cap is explicit") — astra reviews it; the owner is told. A/B BY ME (ab-probe.log, isolated, alternating, load ≈1): main 9f148666 passes at 4,205 / 4,210 ms; vis-field (B6 dirty) fails at 5,243 / 5,094 / 5,246 ms — +21–25% on this single test, the derived-visibility cost of the primer's board projections, at the edge of D630's 25% and over a fixed 5 s budget. RULING: a NAMED re-budget for this one test — timeout 5,000 → 10,000 ms — recorded here with the numbers as the justification (the plan's "raise timeouts only for D544-named flaky tests" is a lane rule; this is not flakiness but a measured cost of the plan's own behaviour, and the supervisor may name it); manifest +1 for that timeout line only (12 files); the cost itself is queued for TEST-PERF-01 / the owner's engine-cache decision (D630 brainstorm rank 6) with sol's mechanism note (how many projections the primer render computes derived fog for). r3 on the resumed session: apply the re-budget, state the mechanism, justify the cap change with per-fixture bytes and what the 8,192 cap would have dropped, then the forced build and the second aggregate.

## D635.33 — SUPERVISOR (2026-09-17 02:21): VIS-FIELD B6 (the last batch) verified by me and COMMITTED 10cc9f5e; astra medium review dispatched with the blind-cap decision in its brief
Sol r3 (impl-b6-sol.md): re-budget applied exactly as ruled; mechanism traced (44 fact sheets → 88 projection calls → 73 fresh derived-fog evaluations, because each encounter-board clone is a distinct state root — ai-dm-screenshot-probe.ts:829, visibility.ts:524, encounter-board.ts:716, visibility-field.ts:550); cap justified per fixture (with/without visibility bytes and the classes the 8,192 cap would drop from each: all four; 16,384 rejects 5117001/5117002/5117007; 32,768 = half the 65,536 blind base cap); D569 blind fixtures unchanged (10 files, inventory sha 3cb2a649…); E1C/FOOTPRINTS unchanged checks 2/2; six mutants killed; tsc 0/0; tsc -b --force 0; OFFERS checks 0/0; pair 38.7 s; second final aggregate of all 30 plan suites 760/760 in 1,171 s. VERIFIED BY ME (verify-b6.log; dirty tree 11 files 516+/32−): tsc app/node 0; tsc -b --force 0; both OFFERS checks 0; 0 forbidden, 0 long lines; schema generator idempotent (a08a7359… → a08a7359…); tests/fixtures diff empty; MY MUTANT PLAYER_OUTPUT_KEEPS_VISIBILITY (the audience === 'dm' guard dropped at semantic-board-payload.ts:601) → the sentinel/player-omission test RED, restored 77cfdb2d…; 11 B6 suites 328/328 (semantic, blind-turn-context, source-binding, engine-mcp-handler, board-delivery, visibility-field, screenshot-probe, engine-mcp-server, engine-mcp-boundary, stable-dom-render, accessible-board); pair 36/36 in 39 s. Committed 10cc9f5e. vis-field = 10cc9f5e: B1, B3, SEAM, B4, B5, REBASE, B2, B2-F1, B6 — every batch of the frozen plan. Astra (resume 01a0a642…, medium; brief review-impl-b6-astra.md; log review-b6-astra-r1.log) reviews the last batch and is asked to state whether the 32,768-byte blind semantic cap is within B6's authority or a product decision; the owner has been told and can rule before landing. Next after ACCEPT: the VIS-FIELD LANDING GATE — merge main into the branch (main has not moved since the rebase: 9f148666), gate-wt2 (tsc -b, sg, full vitest, Playwright), OFFERS checks, D630 pair and one arena round on the merged tip, then "Land VIS-FIELD-01" on main and the mirror push (D640); then ELEVATION-02 B0.

## D635.34 — SUPERVISOR (2026-09-17 02:36): VIS-FIELD B6 ACCEPTED at 10cc9f5e — the frozen plan is fully implemented on claude/vis-field; LANDING GATE launched
Astra r1 (review-impl-b6-astra-r1.md, 1.72 M tokens): 11 files 516+/32−; schema regenerated in memory byte-identical (159,453 bytes); the strict v2 objects reject unknown keys (schemas.ts:671-683) with the blind validator mirroring them; no DM-semantic-to-player delivery path (projectPlayerBoard builds from PlayerView; serialization requires the DM audience at :601; turn-context delivery rejects player projections at :673; MCP attachment paths DM-only at engine-server.ts:1568-1579); one sight service (visibilityField / cellsThatCanSee at :334/:344; the serializer consumes supplied rows at :604-612); the truncation calibration reproduced exactly (16,120 → 15,167 → 13,168 → 12,259 → 5,492); the semantic partition oracle re-enumerated by rational arithmetic (18 visible, only [(4,0),(3,1)] concealed); the effect-line and Invisible-subject oracles confirmed; the source-binding counts recounted (57 = 3 + 6×5 + 6×4; 12 paths) and all three mutations rejected; the 10 D569 fixtures and the 31,995 / 32,000-byte raw-context pins byte-identical; three own mutants killed; tsc 0/0; pair 39.2 s. Its noted limits: the final single-owner test is a lexical check (no alias bypass found); its sandbox could not execute the D569 pins (sol 2/2 and my board-delivery 17/17 stand as the evidence). BLIND CAP: astra judges 32,768 bytes within B6's explicitly open cap choice, with the owner notification retained; two corrections to sol's account, accepted: blind delivery REJECTS an oversized protected semantic block rather than truncating it (blind-turn-context.ts:1304-1316) — so under the old 8,192 cap every retained hard fixture's blind context would have been refused outright, not thinned — and the semantic allowance is ADDITIONAL to the 65,536-byte base allowance, not within it. The owner may still rule the cap smaller before the landing merge; absent a ruling it lands at 32,768. VIS-FIELD-01 = 10cc9f5e: B1, B3, SEAM, B4, B5, REBASE, B2, B2-F1, B6. LANDING GATE launched now (gate-vis-field-land.log): gate-wt2 wt-vis-field (tsc -b --force, sg, full vitest, Playwright chromium) + the two OFFERS checks + the D630 pair twice + one arena round; main 9f148666 is already an ancestor, so the landing is a "Land VIS-FIELD-01" no-ff merge once the gate reads green (timeouts, if any, rerun isolated per the offers precedent D617.50); then the mirror push (D640); then ELEVATION-02 B0 on a fresh worktree from the landed main.

## D635.35 — SUPERVISOR (2026-09-17 02:54): VIS-FIELD LANDING GATE vitest phase RED with DETERMINISTIC reds outside every plan manifest — the frozen arena families lose their productivity guarantee under the seam rule; read-only investigation before any ruling
gate-wt-vis-field.log: tsc -b --force 0; sg 0; vitest RED; Playwright running. Detail runs by me (gate-fail-detail.log, gate-fail-detail2.log, both while Playwright loaded the box): tests/unit/vtt/arena-basis-brutal-b.test.ts 34 failed / 13 passed — ten "pins seed 62060xx only after proving every brutal membership property from state" fail at :413 with "combatant:generated-62060xx-monster-N has no productive first-turn offer" (one named monster per seed: 6206001 m2, 6206002 m1, 6206003 m1, 6206004 m2, 6206005 m2, 6206006 m2, 6206007 m2, 6206008 m1, 6206009 m5, 6206010 m3), twenty-three "executes round 1 … under the unattended scripted-party policy" fail at :421 with 'execution_failed' instead of 'executed' (hard 5117001-007, 009-013; brutal 6203002-006, 008, 009; brutal-b 6206001, 004, 009, 010), and "rejects rather than skips an execution_failed row" at :517 (expected >0); tests/unit/tools/d569-blind-experiment.test.ts 6 failed / 46 passed — five throw "Invalid D569 experiment manifest: second_family_brutal_productivity" and one expects [] at :409 (seven entries received); replay's OWN-BUNDLE-VERSION-OUTSIDE-WINDOW and los-cover's two were 5 s timeouts in the gate (los-cover 111/111 isolated earlier). READING: these are consequences of the owner's seam rule (D635) on the FROZEN arena families — the generator certified "every living monster retains a productive first-turn option" (M576-E2) and the unattended scripted party assumed shooting lanes that ran along two-blocker edges; the D569 experiment manifest re-validates the second family's productivity against the CURRENT engine, so the frozen second family is now an invalid manifest. None of this was in any batch's manifest because the plan's final aggregate named only its own suites; the landing gate is where it had to surface. NOT YET PROVEN: that every failure is the seam (some could be derived-fog / actor-knowledge / query-port consolidation effects). A READ-ONLY investigation lane (fresh sol) traces three representatives (one productivity pin, one scripted round-1 execution_failed, the d569 manifest validity) to their mechanism with rays and cells and classifies every failure as seam-consequence, derived-fog consequence, or VIS-FIELD defect; the owner then decides between regenerating the frozen families under the seam-aware tracer (which invalidates the D569 second-family experiment rows), amending the productivity contract, or revisiting D635. No landing until then.

## D635.36 — SUPERVISOR (2026-09-17 03:22): landing investigation harvested — TWO VIS-FIELD DEFECTS (category c) found, plus a legitimate seam consequence on the frozen brutal families that needs the OWNER's decision
Sol (impl-landing-investigation-sol.md, read-only, 387 k tokens), classification of every deterministic red: (c) DEFECT 1 — src/vtt/engine-query-port.ts:1163 refuses an attack/save offer only when the physical trace's tier === 'total', while execution (src/combat/encounter.ts:7183) refuses when attackLine.blocksSight || tier === 'total'; D635 made optical blocking independent of physical cover, so a seam-blocked ray with tier 'half' is OFFERED and then REFUSED at execution ("The target has Total Cover or is outside line of sight.") — 19 of the 23 scripted round-1 failures (hard 5117001-007, 009-013; brutal 6203002, 003, 005, 006, 008, 009; brutal-b 6206004) are exactly this offer/execution disagreement (e.g. hard 5117001 m6 Heavy Crossbow at the wizard along row line 6 between (8,5)/(8,6); brutal 6203002 m1 Radiant Flame after its move, along row line 5 between (8,4)/(8,5)). This violates the offers boundary (an offered option must execute) and is VIS-FIELD's to fix before landing. (c) DEFECT 2 — src/vtt/room-generator.ts:1223 hasOpenOpposingRay() calls a ray open when tier === 'none' without requiring blocksSight === false, so the generator's M576-E2 wall placement no longer certifies a sight lane. (a) LEGITIMATE CONSEQUENCE — 25 monsters across the ten frozen brutal-b rooms and 18 across seven of the ten frozen brutal-2 rooms (the D569 second family: 6207002, 004-009) lose every productive first-turn option because their only lanes were two-blocker seams (per-seed flanking cells listed; mechanism count 25 seam / 0 derived-fog / 0 other; the B2 consolidation shown non-causal); the D569 second-family validator re-checks productivity against the current engine, so the frozen 480-cell second family is now an invalid manifest. (a′) LATENT TEST-HELPER DEFECT — four scripted rows (brutal 6203004; brutal-b 6206001, 009, 010) fail because dryRunMonsterPlan() in arena-basis-brutal-b.test.ts:145 selects dispel-evil-and-good without its required activationChoice (ACTIVATION_CHOICE_INVALID at intent-resolver.ts:578); D635 changed the ranking and exposed it. (b) derived-fog/detection consequences: none. All three suites pass under main's tracer (in-memory replay). VERIFIED BY ME by reading: engine-query-port.ts:1163 tests only `tier === 'total'`; encounter.ts:7183 tests `attackLine.blocksSight || attackLine.tier === 'total'` — the disagreement is real. RULING: (1) a VIS-FIELD LANDING RECONCILIATION batch (LAND-01) fixes the two production defects and the helper now: the query port refuses to offer an attack/save whose trace blocksSight (agreement witness: for a seam-blocked pair the query offers nothing the reducer would refuse — a hand-authored fixture plus a property over the frozen families that every offered attack executes); the generator requires tier 'none' AND blocksSight false for an open opposing ray (witness + mutant); dryRunMonsterPlan materializes a legal activation choice or skips choice-requiring options; nothing else. (2) The frozen brutal-b and brutal-2 families and the D569 second-family manifest are the OWNER's decision, asked now (regenerate under the seam-aware generator, invalidating the second-family experiment identity: 240 cell state hashes and a new manifest version; or amend the productivity contract — sol and I advise against; or revisit D635). Landing waits for both.

## D635.37 — SUPERVISOR (2026-09-17 03:52): VIS-FIELD landing gate complete on 10cc9f5e — tsc 0, sg 0, vitest RED (D635.36), Playwright 184 passed / 2 FAILED, OFFERS checks 0/0, D630 pair 39.3 / 39.4 s (main 32.8 s, +20%, ceiling 41 s), arena round 38 s (main 51 s); LAND-01 dispatched
Playwright (1.0 h, port 4450): tests/browser/ai-dm-board-snapshot.spec.ts:258 "captures the full production DM board deterministically through durable save upload/load" (0 ms — "Target page, context or browser has been closed", four times) and tests/browser/vtt-encounter.spec.ts:173 "M38-PLAYER-NO-DM-CONTROLS and two local windows complete the resumable reference flow" (15.0 s — expect(locator).toHaveCount failed). The second is plausibly a legitimate consequence of B4 on the REFERENCE scene (authored fog (8,1),(8,2) replaced by derived fog [(4,3)]) that no batch's manifest covered because the plan named no browser spec; the first may be a crash chain from the same run. Both are being read from the gate's JSON report before any ruling; browser specs that pin the reference scene's fog need a hand-derived re-pin under the same rule as every other pin. LAND-01 (fresh sol 01a0ae59…, brief impl-vis-field-land-01.md, log impl-land-01.log) fixes the two production defects and the helper now.

## D635.38 — SUPERVISOR (2026-09-17 04:07): the board-snapshot browser spec's failure REPRODUCES alone on the clean 10cc9f5e checkout — a beforeAll timeout inside the blind board-snapshot service's board-visible wait; LAND-02 diagnoses it as a candidate VIS-FIELD defect
pw-snapshot-alone.log (detached checkout, port 4460, single worker): tests/browser/ai-dm-board-snapshot.spec.ts fails in its FIRST beforeAll (:186) with "beforeAll hook timeout of 30000ms exceeded" — the blind warmup's captureSynchronized → #prepareBoard → #waitForBoardVisible (tools/ai-dm-board-snapshot.ts:992-1030) never resolves; the three dependent beforeAlls then throw their "did not complete" guards and the test reports 0 ms with the closed-browser errors. The wait predicate requires the rendered board element to carry data-board-audience = dm/player, data-source-revision, data-source-round, data-source-state-digest === the service's expected stateDigest, and (D525) the requested glyph mode; a pageerror would have rejected immediately, so one of those attribute equalities never holds. HYPOTHESIS (unproven): a state-digest disagreement between the service and the app now that the V1 decoder validates-and-discards the legacy fog key (B2) — one side may digest the raw fixture bytes or the pre-discard state while the other digests the decoded state — or the derived fog changing what the blind capture app renders for the hard fixture. The same spec passed in the OFFERS landing gate on 1cacd8f0 (187/187, D617.50); a control run on main (port 4480) is in progress. Either way it is VIS-FIELD's to resolve before landing: LAND-02's item (2) is now "diagnose the predicate attribute that fails (instrument in the browser context on a copy), name the producing code on both sides, and fix the VIS-FIELD side or STOP with the mechanism" — a production change there needs a manifest ruling from me. vtt-encounter.spec.ts:237 remains the hand re-pin of the reference scene's derived fog at (4,3).

## D635.38.1 — SUPERVISOR (2026-09-17 04:21): control run confirms the board-snapshot browser spec passes on main 9f148666 (2 passed, 3.4 min, port 4480) and fails alone on the VIS-FIELD tree — VIS-FIELD-caused; LAND-02 diagnoses per D635.38.

## D635.39 — SUPERVISOR (2026-09-17 04:37): LAND-01 complete on its five files (both defects fixed with witnesses and mutants; 214 frozen-family offers all execute; generator bytes 42/42 equal); one out-of-manifest hand pin moves as a consequence — manifest +1; r2 dispatched
Sol (impl-land-01-sol.md): engine-query-port.ts:1167 refuses attack/save offers whose trace blocksSight with the new exact code target_outside_line_of_sight, tracing from the proposed origin (execution's sourceAnchor semantics); the targetability audit found no other authorization gate (query-port :1234/:1547 are tactical-scoring cover inputs and :1830 reports physical cover — left alone, and changing them broke valid intel expectations); room-generator.ts:1227 generatedLineIsOpen = tier 'none' && !blocksSight for both opposing-pair and creature-to-cell predicates; the brutal-b helper materializes the first declared legal activation choice (keeps the engine-ranked option). Witnesses: the two-blocker seam fixture (rays (14,6)/(15,6)→(1,6)/(2,6) along row line 6 between (8,5)/(8,6): closed → half/blocksSight true, no attack offered, LOS code; one flank removed → offered); the AGREEMENT PROPERTY over hard 5117001-013, brutal 6203001-010, brutal-b 6206001-010: exactly 214 offered attack/save uses, every one executes through the reducer without the LOS/Total-Cover error; mutant OFFER_IGNORES_BLOCKS_SIGHT → both red (the cardinality would be 451); OPEN_RAY_IGNORES_BLOCKS_SIGHT → red. GENERATOR BYTES: in-memory regeneration of all 42 checked seeds is byte-equal before/after the fix — so the fixed generator does not cure the frozen families; restoring productivity needs a NEW cohort (reselected seeds), which sharpens question 5's option A. Suites: engine-query-port 19/19; los-cover 112/112; brutal-b 37 passed / 10 failed — the ten productivity pins only (the 19 c1 rows and the four helper rows now execute; "rejects rather than skips" is GREEN, contrary to my brief's prediction — the attended pending decision still forms); d569-blind-experiment 46 / 6 retained (frozen-family, owner's decision); OFFERS 11 files 329/329; dm-tactical-intel + challenge-room 23/23; tsc 0/0; OFFERS checks 0/0; pair 38.6 s; 5 files 260+/9−. ONE CONSEQUENCE outside the manifest: tests/unit/vtt/tactical-evaluator-r02.test.ts:262 pins a hand-authored expected-value probability 0.380769180625 that scored three attacks from origins the reducer would have REFUSED (priest m1, scout m3, bandit m4 at {half, blocksSight true}); with the corrected query the offers come from executable origins (priest 30-ft move to (13,4) none/false; scout 5 ft to (18,4) half/false; bandit 5 ft to (17,4) half/false) and the evaluator scores from each option's finalPosition, giving 0.4323649600000002. Legitimate: the old number rated offers that could not execute. RULING: manifest +1 — tests/unit/vtt/tactical-evaluator-r02.test.ts, that ONE expectation, re-derived BY HAND in its comment (each monster's new origin, line tier/sight, hit chance and damage terms, the sum; the old value kept as pre-LAND-01), never copied from the run; r2 on the resumed session; then my verification, astra medium, and LAND-02.

## D635.40 — SUPERVISOR (2026-09-17 04:51): LAND-01 r2 — the no-bless pin re-derived by hand (spot-checked by me); the same mechanism moves the Bless-variant pins in the same test; allowance widened to every expectation in that test that scores the three moved monsters; r3 dispatched
Sol (resumed): :262 re-derived with the full arithmetic in the comment — priest m1 now offered after a 30-ft move to (13,4) (none / sight clear): +5 vs AC 18 → miss 12/20, normal 7/20, critical 1/20, two 2d10 attacks → 2·(7/20·11 + 1/20·22) = 9.9; scout m3 after a 5-ft move to (18,4) (half): AC 20, +4 → 15/20, 4/20, 1/20, two 1d8+2 → 2·(4/20·6.5 + 1/20·11) = 3.7; bandit m4 after 5 ft to (17,4) (half): +3 → 16/20, 3/20, 1/20, 1d8+1 → 1.325; unchanged scout m5 1.75 and bandit m6 1.875; damage sum 18.55; the dying-target death-save fold (tactical-evaluator.ts:786-814, miss/normal/critical → +0/+1/+2 failures capped at three) in initiative order gives 11068542976/25600000000 = 2702281/6250000 = 0.43236496; the evaluator's float order yields 0.4323649600000002; the file's existing 12-place closeness kept; the old 0.380769180625 recorded as pre-LAND-01. SPOT-CHECKED BY ME: the three to-hit splits and the three damage expectations are right (e.g. 2·(3.85+1.1) = 9.9; 2·(1.3+0.55) = 3.7; 0.825+0.5 = 1.325). The stop: the next assertion, bless-scout-scout-priest at :280 (expected 0.5791028347540284, actual 0.6320368904718017), scores the SAME moved priest/scout origins under Bless — the same legitimate mechanism; my one-expectation ruling was too narrow. RULING: the allowance covers EVERY expectation in tests/unit/vtt/tactical-evaluator-r02.test.ts whose allocation includes the moved priest m1 / scout m3 / bandit m4 (the Bless variants included), each re-derived by hand in the same form (Bless adds 1d4 = +2.5 expected to the attack roll → state the new hit/normal/critical splits per monster and the fold), old values kept as pre-LAND-01; any expectation NOT involving those three monsters must stay byte-identical (a change there is a STOP). r3 on the resumed session.

## D635.41 — SUPERVISOR (2026-09-17 05:22): LAND-01 verified by me and COMMITTED ebfcacaa; astra medium review dispatched; LAND-02 (browser specs) dispatched in parallel
VERIFIED BY ME (verify-land-01.log): tsc 0/0; both OFFERS checks 0; 0 forbidden, 0 long lines; tests/fixtures untouched; MY MUTANTS — the query port's sight refusal disabled → both witnesses RED (the seam fixture and the 214-offer agreement property), restored 1a552fba…; the generator's predicate relaxed to tier-only → OPEN_RAY_IGNORES_BLOCKS_SIGHT RED, restored 8d85ae64…; 16 suites 299/299 (engine-query-port, r02, dm-tactical-intel, challenge-room, the three offer-environment suites, engine-mcp-handler/server/golden, board-delivery, board-snapshot, SIMULATED, dm-encounter-host-live-path, replay, detection); los-cover 112/112; EXACTLY the 16 expected reds (ten brutal-b productivity pins, six D569 second-family manifest) — the frozen-family consequence that is the owner's question 5; pair 36/36 in 38 s. Committed ebfcacaa (6 files 325+/22−). vis-field = ebfcacaa. Astra (resume 01a0a642…, medium; brief review-impl-land-01-astra.md; log review-land-01-astra-r1.log) reviews LAND-01 on the detached checkout; LAND-02 (fresh sol; brief impl-vis-field-land-02.md; log impl-land-02.log) re-pins vtt-encounter.spec.ts:237 by hand and diagnoses the board-snapshot spec's board-visible wait (D635.38) on the worktree, its Playwright runs on port 4470 only.

## D635.42 — SUPERVISOR (2026-09-17 05:40): LAND-01 astra r1 = REJECT (0 P1, 1 P2: LAND-F1); LAND-02 re-pinned the spec and stopped BLOCKED on a manifest question; RULING: LAND-03 realigns the snapshot binding digest to the DM view state
ASTRA r1 (medium, resume 01a0a642…; saved review-impl-land-01-astra-r1.md): production fix correct (query :1167-1174 and execution :7183-7203 same inputs via cover.ts:584-594; :1234/:1547/:1830 correctly untouched); 214 options = 316 components recounted (hard 142 / brutal 57 / brutal-b 15; 204 multiattack + 108 ordinary + 4 saves), no option skipped; seam geometry re-enumerated in rational arithmetic (12/16 rays enter a blocker, 4/16 run the row-6 seam, shared edge t in [5/13, 6/13]); 10/10 brutal-b seeds byte-identical pre/post predicate (the fix does not cure the frozen cohort — confirms option A of question 5); both R02 allocations reproduced by Fraction enumeration (2702281/6250000; 1035529241349/1638400000000); both controls byte-identical; its own mutants: sight refusal disabled → both witnesses red (451 options offered), predicate relaxed → witness red. LAND-F1 (P2): the agreement property "checks" saves by executing force_save, whose reducer branch never traces sight — astra's blocking-column probe survived. VERIFIED BY ME: resolveTargetSave (encounter.ts:3577) computes only coverSavingThrowBonus and the force_save branch (:12451) validates the declaration, spends the cost and resolves — no sight trace; the property's save half proves nothing about sight. Accepted; test-only fix (direct trace assertion per component, a blocked-save query witness, astra's mutant as the proof).
PARKED (owner, pre-existing, not a VIS-FIELD defect): force_save enforces no clear-path/total-cover rule at execution while the query refuses blocksSight/total saves; query stricter than execution is safe for the offers boundary (nothing offered fails) but the reducer accepts a save through a wall if a client bypasses the offers.
LAND-02 (sol 01a0aeac…, saved impl-land-02-sol.md): vtt-encounter.spec.ts:237 re-pinned by hand — fighter (2,3), cleric (1,4), wizard (1,2) normal sight only, dark target cell (4,3) unseen by all three, so DM fog and the owned-party complement are exactly {(4,3)}: DM fog count 1, DM token visible + hidden-from-players, player floor visible + fog count 1 + zero monster tokens; sol reports 3/3 passed on port 4470, tsc 0/0. MY VERIFICATION of the re-pin (patch applied to the detached checkout, port 4471, plus a reverse mutant) is in flight (verify-land-02-spec.log). Board-snapshot diagnosis (sol; VERIFIED BY ME in source): the service binds to boardStateDigest(state) = sha256(canonicalJson(state)) (tools/ai-dm-board-snapshot.ts:201) while the app renders sha256(canonicalJson(projectDmView(state).state)) (encounter-projections.ts:488 → encounter-app.ts:2308); since B3 the DM view state carries the derived fog that canonical state no longer holds (B2), so #waitForBoardVisible (:992-1030) never matches for any state with a non-empty derived field — 176 derived cells in the spec's fixture. This is not spec-only: every live capture (ai-dm-conversation.ts:4487, glyph captures, the two snapshot checks, the screenshot probe) binds through the same function and would time out. RULING (manifest addition under landing reconciliation, D635.36 pattern): the binding digest is the digest of the DM view state as the app defines it; boardStateDigest(state) becomes sha256(canonicalJson(projectDmView(state).state)) so all service callers bind to the rendered value; witness with a negative control (raw digest differs), the browser spec must pass, independent recomputations re-derived only where they model the rendered state, D569 demonstrate-then-normalize elsewhere, any embedded manifest digest = BLOCKED not regenerated. Dispatched as ONE sol lane (resume 01a0ae59…, brief impl-vis-field-land-03.md, log impl-land-03.log): Task A = LAND-01 fix r1, Task B = LAND-03. The uncommitted spec re-pin stays in the worktree, off-limits to the lane.

OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
model: gpt-6-astra
provider: openai
approval: never
sandbox: read-only
reasoning effort: medium
reasoning summaries: none
session id: 01a0a642-35a1-7082-aba2-eda56ea291c1
--------
user
RULES (binding): You are the REVIEWER (gpt-6-astra, MEDIUM per D627, resumed — the session that reviewed every VIS-FIELD batch). Worktree /home/vagrant/PhpstormProjects/dnd-wt-seam-verify — a DETACHED clean checkout at ebfcacaa (= 10cc9f5e (B6 ACCEPTED, the complete frozen plan) + LAND-01; node_modules symlinked). Sandbox read-only; in-memory when /tmp is read-only; do NOT edit files, no git writes, no claude / `claude -p` / other agents / consensus skills; skills under ~/.codex do NOT apply; never port 4173; no full gate, full vitest, Playwright, npm run build. Do not read sibling worktrees or /home/vagrant/dnd-slim-runs/. MAIN repo /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static's .claude/decisions.md and .claude/consensus/vis-field/** readable: read D635.35–D635.41, impl-landing-investigation-sol.md and impl-land-01-sol.md first. Model calls WILL fail. Report exact commands and numbers; end with the named marker.

TASK: Review `git diff 10cc9f5e ebfcacaa` (6 files, 325+/22−: src/vtt/engine-query-port.ts, src/vtt/room-generator.ts, tests/unit/vtt/{engine-query-port,room-generator-los-cover,arena-basis-brutal-b,tactical-evaluator-r02}.test.ts) = LAND-01, the landing-reconciliation batch for the two VIS-FIELD defects the full-suite landing gate exposed (D635.36): (c1) the query port offered attacks/saves whose corner trace blocks sight (tier-only check) while execution refuses them — an offers-boundary violation; (c2) the generator's open-ray predicate ignored blocksSight; plus the brutal-b helper's missing activation choice, and six tactical-evaluator kill-probability pins that scored shots from origins the reducer would refuse. Supervisor verification (verify-land-01.log): tsc 0/0; OFFERS checks 0/0; my mutants (sight check disabled → both query witnesses red incl. the 214-offer agreement property; generator predicate relaxed → its witness red); 16 suites 299/299 (query port, r02, dm-tactical-intel, challenge-room, the OFFERS set, replay, detection); los-cover 112/112; exactly the 16 expected reds remain (ten brutal-b productivity pins + six D569 manifest — the frozen-family consequence awaiting the owner, D635.36); pair 38 s.

CHECK (each with commands and numbers): (1) c1: the refusal at engine-query-port.ts:~1171 uses the SAME trace inputs as execution (encounter.ts:~7183: actor, target, sourceAnchor = the proposed origin) — could the two still disagree (a different anchor, footprint, exclusion set, or a path where execution traces after a move the query did not model)? Enumerate every production site that decides attack/save targetability and confirm each is now sight-aware or is not an authorization gate (sol left :1234/:1547 tactical-scoring and :1830 cover reporting alone — agree or not). (2) The AGREEMENT PROPERTY ("offered attacks and saves in every frozen landing family pass the reducer LOS rule"): read it — is executing each offer through reduceEncounter at the resolved finalPosition a faithful model of real execution (initiative/active-combatant setup, the () => 0.5 roll, multiattack components, bonus-action slot)? 214 offers checked — recount from the three families; a property that silently skips options is a P1. (3) c2: generatedLineIsOpen = tier 'none' && !blocksSight at both call sites; the witness's geometry (14,6)→(1,6) along row line 6 between (8,5)/(8,6) — verify; sol's in-memory regeneration of 42 seeds byte-equal — reproduce for at least the ten brutal-b seeds (so the frozen families are NOT cured by the fix; relevant to the owner's question). (4) The six R02 pins: recompute at least two allocations independently (no-bless 2702281/6250000 and one Bless variant; Bless enumerated over the d4 faces; the death-save fold in initiative order per tactical-evaluator.ts:786-814) and confirm the two unchanged controls are byte-identical to 10cc9f5e. (5) The brutal-b helper: materializing the first declared legal activation choice keeps the engine-ranked option — confirm it does not mask a resolver defect and that dispel-evil-and-good's choice contract is what the helper reads. (6) No fixture changed; the remaining reds are exactly the frozen-family ones; tsc 0/0; suites you can run; pair (ceiling ≈41 s).

OUTPUT: findings LAND-Fn with severity P1/P2/P3, file:line, evidence, fix; then exactly "VERDICT: ACCEPT" or "VERDICT: REJECT (n P1, m P2)"; end with REVIEW DONE.

codex
I’ll read the landing rulings and reports first, then check the targeting agreement, generator behavior, and probability pins against the code and targeted tests.

exec
/bin/bash -lc "rg -n '"'^## D635'"\\.(35|36|37|38|39|40|41)' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md; cat /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-land-01-sol.md; cat /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-landing-investigation-sol.md" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
22954:## D635.35 — SUPERVISOR (2026-09-17 02:54): VIS-FIELD LANDING GATE vitest phase RED with DETERMINISTIC reds outside every plan manifest — the frozen arena families lose their productivity guarantee under the seam rule; read-only investigation before any ruling
22957:## D635.36 — SUPERVISOR (2026-09-17 03:22): landing investigation harvested — TWO VIS-FIELD DEFECTS (category c) found, plus a legitimate seam consequence on the frozen brutal families that needs the OWNER's decision
22960:## D635.37 — SUPERVISOR (2026-09-17 03:52): VIS-FIELD landing gate complete on 10cc9f5e — tsc 0, sg 0, vitest RED (D635.36), Playwright 184 passed / 2 FAILED, OFFERS checks 0/0, D630 pair 39.3 / 39.4 s (main 32.8 s, +20%, ceiling 41 s), arena round 38 s (main 51 s); LAND-01 dispatched
22963:## D635.38 — SUPERVISOR (2026-09-17 04:07): the board-snapshot browser spec's failure REPRODUCES alone on the clean 10cc9f5e checkout — a beforeAll timeout inside the blind board-snapshot service's board-visible wait; LAND-02 diagnoses it as a candidate VIS-FIELD defect
22966:## D635.38.1 — SUPERVISOR (2026-09-17 04:21): control run confirms the board-snapshot browser spec passes on main 9f148666 (2 passed, 3.4 min, port 4480) and fails alone on the VIS-FIELD tree — VIS-FIELD-caused; LAND-02 diagnoses per D635.38.
22968:## D635.39 — SUPERVISOR (2026-09-17 04:37): LAND-01 complete on its five files (both defects fixed with witnesses and mutants; 214 frozen-family offers all execute; generator bytes 42/42 equal); one out-of-manifest hand pin moves as a consequence — manifest +1; r2 dispatched
22971:## D635.40 — SUPERVISOR (2026-09-17 04:51): LAND-01 r2 — the no-bless pin re-derived by hand (spot-checked by me); the same mechanism moves the Bless-variant pins in the same test; allowance widened to every expectation in that test that scores the three moved monsters; r3 dispatched
22974:## D635.41 — SUPERVISOR (2026-09-17 05:22): LAND-01 verified by me and COMMITTED ebfcacaa; astra medium review dispatched; LAND-02 (browser specs) dispatched in parallel
# VIS-FIELD LAND-01 — sol report (fresh 01a0ae59…, 389 k tokens; BLOCKED only on one out-of-manifest hand pin)

utal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index d76fdc233b9b4def2bc1d4a141cc81f083564e5f..3613a2eb3c8585746b048672e2aea6e388c0d940
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -15,6 +15,10 @@
   resolveEngineActorOption,
   type EngineTurnProposal,
 } from '../../../src/vtt/intent-resolver';
+import type {
+  EngineActivationChoice,
+  EngineOfferableOption,
+} from '../../../src/vtt/turn-proposal';
 import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { actorOpportunityReport } from '../../../src/vtt/intel/opportunity-cost';
 import { decodeArenaFixture } from '../../../src/vtt/mcp/entrypoint';
@@ -142,6 +146,20 @@
   return actors;
 }
 
+function deterministicActivationChoice(option: EngineOfferableOption): EngineActivationChoice | undefined {
+  const slot = option.activationChoice;
+  if (slot === undefined || slot === null) return undefined;
+  switch (slot.kind) {
+    case 'command_word': return { kind: slot.kind, value: slot.values[0] };
+    case 'unicorns_blessing_spell': return { kind: slot.kind, value: slot.values[0] };
+    case 'dispel_evil_and_good_mode': return { kind: slot.kind, value: slot.values[0] };
+    case 'calm_emotions_per_target': return {
+      kind: slot.kind,
+      selections: slot.targetIds.map((targetId) => ({ targetId, mode: slot.values[0] })),
+    };
+  }
+}
+
 function dryRunMonsterPlan(state: EncounterState): ReadonlyMap<CombatantId, AuthorizedEngineTurnProposal> {
   const actorIds = [...livingMonsterIds(state)].sort((left, right) => left.localeCompare(right));
   const planningState = projectFutureMonsterTurns(state, actorIds);
@@ -160,12 +178,14 @@
       state.revision,
     ).find((option) => option.optionId !== primary.optionId);
     if (fallback === undefined) throw new Error(`Dry-run plan has no independent fallback for ${actorId}.`);
+    const activationChoice = deterministicActivationChoice(primary);
     const proposal: EngineTurnProposal = {
       actorId,
       expectedRevision: state.revision,
       primaryOptionId: primary.optionId,
       fallbackOptionId: fallback.optionId,
       reason: 'Use the engine-ranked option to maximize immediate tactical value.',
+      ...(activationChoice === undefined ? {} : { activationChoice }),
       overrideJustification: null,
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 66fe8cfa5e7742935be2fd8a47e42d07a131f38b..62acd4131b7a3469070f9f41ce9c246e4904c1e2
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -9,24 +9,35 @@
 } from '../../../src/combat/encounter';
 import { traceCombatantLine } from '../../../src/combat/cover';
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
-import { monsterAttackCommand } from '../../../src/combat/monster-commands';
+import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
-import { armorClass, combatantId, statblockId, worldObjectId, type CombatantId } from '../../../src/combat/values';
+import {
+  armorClass,
+  combatantId,
+  feet,
+  statblockId,
+  worldObjectId,
+  type CombatantId,
+} from '../../../src/combat/values';
 import { projectDmView } from '../../../src/combat/visibility';
 import { ENCOUNTER_VIEW_CLASSIFICATION } from '../../../src/combat/visibility';
 import { projectEncounterBoard } from '../../../src/vtt/encounter-board';
 import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import { monsterActions, monsterBonusActions } from '../../../src/vtt/engine-query-port';
 import {
   availableEngineActorOptions,
   createPureTurnProposalResolver,
   engineActorOptionsForEnvironment,
+  resolveEngineActorOption,
 } from '../../../src/vtt/intent-resolver';
 import { engineOptionId } from '../../../src/vtt/turn-proposal';
 import { generateRoom } from '../../../src/vtt/room-generator';
 import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
 import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
 import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
 import { readFileSync } from '../../helpers/test-filesystem';
 
@@ -36,6 +47,21 @@
 const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
 const TURN_PROPOSAL_RESOLVER = createPureTurnProposalResolver(OFFER_ENVIRONMENT);
 
+const AGREEMENT_FIXTURE_PATHS = [
+  ...Array.from(
+    { length: 13 },
+    (_unused, index) => `tests/fixtures/arena-basis-hard/seed-${String(5_117_001 + index)}.json`,
+  ),
+  ...Array.from(
+    { length: 10 },
+    (_unused, index) => `tests/fixtures/arena-basis-brutal/seed-${String(6_203_001 + index)}.json`,
+  ),
+  ...Array.from(
+    { length: 10 },
+    (_unused, index) => `tests/fixtures/arena-basis-brutal-b/seed-${String(6_206_001 + index)}.json`,
+  ),
+] as const;
+
 function legacyDocument(
   state: EncounterState,
   foggedCells: unknown,
@@ -82,6 +108,49 @@
   });
 }
 
+function twoBlockerSeamState(openLowerFlank: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GOBLIN_WARRIOR, {
+    combatantId: 'combatant:query-seam-shooter',
+    tokenId: 'token:query-seam-shooter',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-seam-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 16, rows: 8 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 14, 5), placedToken(target, 1, 6)],
+      blockedCells: openLowerFlank
+        ? [{ column: 8, row: 5 }]
+        : [{ column: 8, row: 5 }, { column: 8, row: 6 }],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+function stateAtResolvedOrigin(
+  state: EncounterState,
+  actorId: CombatantId,
+  position: GridCell,
+): EncounterState {
+  return {
+    ...state,
+    activeCombatant: actorId,
+    activeInitiativeIndex: 0,
+    tokens: state.tokens.map((token) => token.combatantId === actorId
+      ? { ...token, position }
+      : token),
+  };
+}
+
 describe('canonical engine query port', () => {
   it('ENGINE_QUERY_VISIBILITY_EQUALS_CANONICAL_DETECTOR including located outcomes', () => {
     const observerBase = monsterProfile('query-visibility-observer');
@@ -423,6 +492,110 @@
     expect(projectEncounterBoard(projectDmView(walled)).blockedCells).toEqual(walledCells);
   });
 
+  it('OFFER_IGNORES_BLOCKS_SIGHT refuses a half-cover two-blocker seam and opens one flank', () => {
+    const closed = twoBlockerSeamState(false);
+    const opened = twoBlockerSeamState(true);
+    // The four formerly clear rays are (14,6)/(15,6) -> (1,6)/(2,6). Each follows row line 6
+    // between wall cells (8,5)/(8,6); the other twelve corner rays enter one of those cells.
+    expect(traceCombatantLine(closed.state, closed.actorId, closed.targetId)).toMatchObject({
+      tier: 'half',
+      blocksSight: true,
+      firstBlockingCell: { column: 8, row: 5 },
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(closed.state, {
+      actorId: closed.actorId,
+      targetId: closed.targetId,
+      actionId: 'shortbow',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(closed.state, closed.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'attack' &&
+        slot.use.target.kind === 'combatant' && slot.use.target.combatantId === closed.targetId))).toBe(false);
+
+    // Removing (8,6) leaves the row-line-6 rays with a blocker on only one flank.
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'shortbow',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'attack' &&
+        slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+    const losFailures: string[] = [];
+    let checkedOffers = 0;
+    for (const path of AGREEMENT_FIXTURE_PATHS) {
+      const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
+      const monsters = state.combatants.filter((combatant) =>
+        combatant.profile.kind === 'monster' && combatant.life === 'living');
+      for (const monster of monsters) {
+        for (const option of availableEngineActorOptions(
+          state,
+          monster.profile.id,
+          OFFER_ENVIRONMENT,
+        )) {
+          const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
+          if (!resolution.valid) throw new Error(`${path}: offered option ${option.optionId} did not resolve.`);
+          const uses = resolution.mechanics.actionSlots.filter((use) =>
+            use.kind === 'attack' || use.kind === 'saving_throw');
+          if (uses.length === 0) continue;
+          checkedOffers += 1;
+          for (const use of uses) {

OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-vis-field
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR] (network access enabled)
reasoning effort: high
reasoning summaries: none
session id: 01a0aeac-47dc-7361-830f-5bcc1251fc36
--------
user
COMMON RULES (binding, restated in full): You are the IMPLEMENTER (gpt-5.6-sol, high, FRESH session) for VIS-FIELD-01's LANDING RECONCILIATION batch LAND-02 (browser specs). Worktree /home/vagrant/PhpstormProjects/dnd-wt-vis-field (branch claude/vis-field at ebfcacaa = the complete frozen plan + LAND-01 (accepted pending astra); clean; own node_modules), sandbox workspace-write in that worktree ONLY (+ /tmp). No git write commands. No edits under .claude/** or docs/**. Do NOT invoke claude, `claude -p`, any reviewer session, any consensus workflow or any other agent; skills under ~/.codex do NOT apply. NEVER touch port 4173 (the owner's live app); if you must run a browser spec, use PLAYWRIGHT_PORT=4470 PLAYWRIGHT_WORKERS=1 and ONLY the spec you changed, once; no full Playwright run, no full gate, no full vitest, no `npm run build` beyond what the spec's webServer performs. No `any`, @ts-ignore, @ts-expect-error, .skip, .todo, no conditional test branches; NO WEAKENED ASSERTIONS; no deleted tests; every re-pinned expectation hand-derived from the geometry and the plan's fog rules (§3.3: DM fog = complement of the union of eligible DM-party fields; player concealedCells = complement of eligible owned fields; ordinary darkness is target-cell only, D626.3), never copied from a run. No fixture under tests/fixtures changes. Other lanes run in sibling worktrees: do not read or touch them; do not read /home/vagrant/dnd-slim-runs/. MAIN repo /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static's .claude/decisions.md and .claude/consensus/vis-field/** readable (read-only): read D626.3, D626.10, D635.36, D635.37, D635.38 first. No model calls. Report exact commands and numbers; end with the named marker or "BLOCKED: <reason>".

TASK — LAND-02: the landing gate's browser suite (Playwright chromium, 184 passed / 2 failed on 10cc9f5e) exposed browser specs no plan batch named. (1) tests/browser/vtt-encounter.spec.ts:173 "M38-PLAYER-NO-DM-CONTROLS and two local windows complete the resumable reference flow": at :237 it expects `dm.locator('[data-cell="4,3"] .encounter-art-fog')` toHaveCount(0) after the Training Brute moves 3,3 → 4,3 and hides. Reference scene (src/vtt/reference-encounter.ts): 10×7, fighter (2,3), cleric (1,4), wizard (1,2), blocker (7,2), ordinary darkness at (4,3) only, no authored fog any more (B4). Under the plan's rules the DM board's fog is derived: HAND-DERIVE which cells are unseen by all three PCs after the brute stands in (4,3) (the brute's body does not change light; (4,3) is a dark target cell — is it seen by any PC without darkvision? state each PC's sense list from the setup) and re-pin :237 (and :238 the DM token visibility, :242 the player's floor art at (4,3), :243-245 the player's monster count) accordingly, with the derivation in a comment; check EVERY other assertion in that flow and in the file that depends on the reference scene's fog or concealment (grep encounter-art-fog, data-cell, concealed) and re-pin each the same way; assertions about undo/adjudication/controls stay byte-identical. (2) [D635.38: it DID reproduce alone on a clean checkout — first beforeAll (:186) times out at 30 s inside BoardSnapshotService.#waitForBoardVisible (tools/ai-dm-board-snapshot.ts:992-1030), whose predicate requires the rendered board's data-board-audience, data-source-revision, data-source-round, data-source-state-digest and glyph mode to equal the service's expected source; find WHICH attribute never matches by evaluating the predicate's inputs in the page (a copy under /tmp, or a temporary console probe you remove), name the code producing each side (the service's stateDigest vs the app's data-source-state-digest — check whether B2's validate-and-discard of the legacy fog key or B3/B4's derived fog changed one side), and report the mechanism; if the fix is in production code, STOP with the file:line and the proposed change (manifest ruling); if it is the spec's own fixture/expectation, re-pin by hand] tests/browser/ai-dm-board-snapshot.spec.ts:258 "captures the full production DM board deterministically through durable save upload/load" failed in the gate at 0 ms with "Target page, context or browser has been closed" ×4 — the supervisor is re-running it alone on a clean checkout; D635.38 states whether it reproduced: if it did NOT reproduce, leave that file untouched and say so; if it DID, diagnose (its beforeAll starts a blind service; a fixture whose derived fog or semantic v2 bytes changed?) and re-pin by hand or STOP with the mechanism. (3) tests/browser/** other specs that pin fog/concealment of the reference, Vane, two-room or d365 scenes: grep and list; if any would fail under derived fog, re-pin by hand in this batch and name them; if none, say so. ALLOWED FILES: tests/browser/vtt-encounter.spec.ts, tests/browser/ai-dm-board-snapshot.spec.ts (only if (2) reproduced), plus any spec you list under (3) — STOP and report if a production file would need to change. VERIFY: run each changed spec ONCE on port 4470 (PLAYWRIGHT_PORT=4470 PLAYWRIGHT_WORKERS=1 npx playwright test <spec> --project=chromium) and paste the result; both tsc; git diff --check; status = exactly the changed specs. End with `VIS-FIELD LAND-02 DONE` or `BLOCKED: <reason>`.

codex
I’ll first read the five binding decisions and inspect the two browser specs plus the snapshot predicate. I’ll hand-derive every fog/concealment expectation before editing, then run only each changed Chromium spec once on port 4470, followed by both TypeScript checks, `git diff --check`, and status.
exec
/bin/bash -lc "sed -n '/"'^## D626'"\\.3\\b/,/"'^## D/p; /''^## D626'"\\.10\\b/,/"'^## D/p; /''^## D635'"\\.36\\b/,/"'^## D/p; /''^## D635'"\\.37\\b/,/"'^## D/p; /''^## D635'"\\.38\\b/,/"'^## D/p'"' .claude/decisions.md" in /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static
 succeeded in 0ms:
## D626.3 — OWNER (2026-09-15 16:03): ordinary darkness = "target only" (reading 1); copy the Foundry / Roll20 / Owlbear Rodeo vision model
Owner: "you see a campfire from a mile away on a dark night. that supports reading 1. try to copy what owlbear rodeo and foundry and roll20 have done". Ruling: unlit cells between observer and target never block sight; ordinary darkness is evaluated at the target cell only (the VIS-FIELD-01 plan's frozen default, §1/§3.2, stands; §7's flip is closed — no amendment needed). Model to copy, as those three products share it: (1) walls/terrain block sight; (2) light sources give bright/dim radii, a target is visible when its cell is lit or within the observer's darkvision (Foundry "vision mode" / Roll20 "night vision") range, with unlimited range for lit targets; (3) darkness never blocks line of sight; (4) magical darkness is a darkness region that defeats ordinary darkvision, with Devil's Sight as a vision mode that ignores it (Foundry); (5) obscuring smoke/fog regions block sight through them (Owlbear "smoke & spectre", Foundry fog-cloud practice); (6) fog of war is the union of the party's vision, per seat for players. The plan already implements (1)-(6). One deliberate gap versus Foundry and Roll20 Explorer mode: explored-but-not-currently-visible memory (greyed explored areas). VIS-FIELD-01 keeps that out of scope (plan §1 non-goals); recorded as follow-up FOG-MEMORY-01 (per-seat explored-cell set derived from past fields, greyed on the board) to be planned after VIS-FIELD-01 lands, unless the owner wants it inside this unit.

## D626.4 — SUPERVISOR (2026-09-15 16:05): VIS-FIELD-01 Batch 1 BLOCKED by sol on a second exhaustive inventory outside the manifest; bounded amendment — src/vtt/intel/option-outcome.ts joins Batch 1 (11 files, one-record delta); re-dispatched
## D626.10 — SUPERVISOR (2026-09-15 19:36): VIS-FIELD B3 r4 candidate — batch green (154/154, 5/5 supervisor mutants killed, pair 38.3 s = +14.7%, within D630) but the full tests/unit/combat + tests/unit/vtt sweep shows 7 deterministic reds in five consumer suites; dispositions
(1) DEFECT in B3 (fix r5): tests/unit/combat/visibility.test.ts — sol DELETED the D359 ledger-pinned control "omits a fogged edge cell and its contents while retaining the adjacent visible boundary cell" (old :339); tests/unit/vtt/mutation-ledger.test.ts:375 pins that title as one of four restored controls with its killing check. The plan names no removal; COMMON RULES forbid deleting a test the plan does not name. Required: restore the control under its EXACT title with derived-fog geometry (an edge cell made unseen for every eligible observer by darkness/wall, its contents omitted, the adjacent visible boundary cell retained) so the ledger's killing check still kills. (2) INTERMEDIATE REDS owned by later batches, accepted as the plan's sequencing (B3 stops consumption; B4 removes the authored lists that fixtures still carry; B5/B6 migrate the semantic source): tests/unit/vtt/generated-encounter-fixtures.test.ts (B4 manifest), tests/unit/vtt/semantic-board-payload.test.ts ×2 (B5/B6 manifests: semantic source still copies authored fog while the DM board projection is derived), tests/unit/tools/ai-dm-screenshot-probe.test.ts "derives every fact class…" (plan :197 excludes the Q9 suite from B3's gate; tools/ai-dm-screenshot-probe.ts is B5's). (3) PLAN GAP → B4 manifest amendment +2 (plan :266 analogue): tests/unit/vtt/two-room-fixture.test.ts and tests/unit/vtt/handoff-examples.test.ts pin the two-room handoff fixture's projections and tracked scene bytes (fixtures/scenes/two-room.v1.json / two-room.snapshots.v1.json) whose authored foggedCells (src/vtt/handoff/fixtures/two-room.ts:112) B4 removes; they can only go green in B4 and belong to no manifest. Landing rule unchanged: every suite green at landing; the intermediate reds are listed by owner in the vis-field loop-log and re-checked after each batch.

## D626.11 — SUPERVISOR (2026-09-15 19:52): VIS-FIELD-01 Batch 3 ACCEPTED at 78ee31b0 — astra r1 ACCEPT (medium, 7 mutations killed incl. three of its own; pin demonstration reproduced independently: old hashes from the pre-B3 module, only cells + concealedCells changed, concealed {(4,3)}, edge-touch rule clears the blocker rays); next B4 with the D626.10 manifest amendment (+2)
## D635.36 — SUPERVISOR (2026-09-17 03:22): landing investigation harvested — TWO VIS-FIELD DEFECTS (category c) found, plus a legitimate seam consequence on the frozen brutal families that needs the OWNER's decision
Sol (impl-landing-investigation-sol.md, read-only, 387 k tokens), classification of every deterministic red: (c) DEFECT 1 — src/vtt/engine-query-port.ts:1163 refuses an attack/save offer only when the physical trace's tier === 'total', while execution (src/combat/encounter.ts:7183) refuses when attackLine.blocksSight || tier === 'total'; D635 made optical blocking independent of physical cover, so a seam-blocked ray with tier 'half' is OFFERED and then REFUSED at execution ("The target has Total Cover or is outside line of sight.") — 19 of the 23 scripted round-1 failures (hard 5117001-007, 009-013; brutal 6203002, 003, 005, 006, 008, 009; brutal-b 6206004) are exactly this offer/execution disagreement (e.g. hard 5117001 m6 Heavy Crossbow at the wizard along row line 6 between (8,5)/(8,6); brutal 6203002 m1 Radiant Flame after its move, along row line 5 between (8,4)/(8,5)). This violates the offers boundary (an offered option must execute) and is VIS-FIELD's to fix before landing. (c) DEFECT 2 — src/vtt/room-generator.ts:1223 hasOpenOpposingRay() calls a ray open when tier === 'none' without requiring blocksSight === false, so the generator's M576-E2 wall placement no longer certifies a sight lane. (a) LEGITIMATE CONSEQUENCE — 25 monsters across the ten frozen brutal-b rooms and 18 across seven of the ten frozen brutal-2 rooms (the D569 second family: 6207002, 004-009) lose every productive first-turn option because their only lanes were two-blocker seams (per-seed flanking cells listed; mechanism count 25 seam / 0 derived-fog / 0 other; the B2 consolidation shown non-causal); the D569 second-family validator re-checks productivity against the current engine, so the frozen 480-cell second family is now an invalid manifest. (a′) LATENT TEST-HELPER DEFECT — four scripted rows (brutal 6203004; brutal-b 6206001, 009, 010) fail because dryRunMonsterPlan() in arena-basis-brutal-b.test.ts:145 selects dispel-evil-and-good without its required activationChoice (ACTIVATION_CHOICE_INVALID at intent-resolver.ts:578); D635 changed the ranking and exposed it. (b) derived-fog/detection consequences: none. All three suites pass under main's tracer (in-memory replay). VERIFIED BY ME by reading: engine-query-port.ts:1163 tests only `tier === 'total'`; encounter.ts:7183 tests `attackLine.blocksSight || attackLine.tier === 'total'` — the disagreement is real. RULING: (1) a VIS-FIELD LANDING RECONCILIATION batch (LAND-01) fixes the two production defects and the helper now: the query port refuses to offer an attack/save whose trace blocksSight (agreement witness: for a seam-blocked pair the query offers nothing the reducer would refuse — a hand-authored fixture plus a property over the frozen families that every offered attack executes); the generator requires tier 'none' AND blocksSight false for an open opposing ray (witness + mutant); dryRunMonsterPlan materializes a legal activation choice or skips choice-requiring options; nothing else. (2) The frozen brutal-b and brutal-2 families and the D569 second-family manifest are the OWNER's decision, asked now (regenerate under the seam-aware generator, invalidating the second-family experiment identity: 240 cell state hashes and a new manifest version; or amend the productivity contract — sol and I advise against; or revisit D635). Landing waits for both.

## D635.37 — SUPERVISOR (2026-09-17 03:52): VIS-FIELD landing gate complete on 10cc9f5e — tsc 0, sg 0, vitest RED (D635.36), Playwright 184 passed / 2 FAILED, OFFERS checks 0/0, D630 pair 39.3 / 39.4 s (main 32.8 s, +20%, ceiling 41 s), arena round 38 s (main 51 s); LAND-01 dispatched
## D635.37 — SUPERVISOR (2026-09-17 03:52): VIS-FIELD landing gate complete on 10cc9f5e — tsc 0, sg 0, vitest RED (D635.36), Playwright 184 passed / 2 FAILED, OFFERS checks 0/0, D630 pair 39.3 / 39.4 s (main 32.8 s, +20%, ceiling 41 s), arena round 38 s (main 51 s); LAND-01 dispatched
Playwright (1.0 h, port 4450): tests/browser/ai-dm-board-snapshot.spec.ts:258 "captures the full production DM board deterministically through durable save upload/load" (0 ms — "Target page, context or browser has been closed", four times) and tests/browser/vtt-encounter.spec.ts:173 "M38-PLAYER-NO-DM-CONTROLS and two local windows complete the resumable reference flow" (15.0 s — expect(locator).toHaveCount failed). The second is plausibly a legitimate consequence of B4 on the REFERENCE scene (authored fog (8,1),(8,2) replaced by derived fog [(4,3)]) that no batch's manifest covered because the plan named no browser spec; the first may be a crash chain from the same run. Both are being read from the gate's JSON report before any ruling; browser specs that pin the reference scene's fog need a hand-derived re-pin under the same rule as every other pin. LAND-01 (fresh sol 01a0ae59…, brief impl-vis-field-land-01.md, log impl-land-01.log) fixes the two production defects and the helper now.

## D635.38 — SUPERVISOR (2026-09-17 04:07): the board-snapshot browser spec's failure REPRODUCES alone on the clean 10cc9f5e checkout — a beforeAll timeout inside the blind board-snapshot service's board-visible wait; LAND-02 diagnoses it as a candidate VIS-FIELD defect
## D635.38 — SUPERVISOR (2026-09-17 04:07): the board-snapshot browser spec's failure REPRODUCES alone on the clean 10cc9f5e checkout — a beforeAll timeout inside the blind board-snapshot service's board-visible wait; LAND-02 diagnoses it as a candidate VIS-FIELD defect
pw-snapshot-alone.log (detached checkout, port 4460, single worker): tests/browser/ai-dm-board-snapshot.spec.ts fails in its FIRST beforeAll (:186) with "beforeAll hook timeout of 30000ms exceeded" — the blind warmup's captureSynchronized → #prepareBoard → #waitForBoardVisible (tools/ai-dm-board-snapshot.ts:992-1030) never resolves; the three dependent beforeAlls then throw their "did not complete" guards and the test reports 0 ms with the closed-browser errors. The wait predicate requires the rendered board element to carry data-board-audience = dm/player, data-source-revision, data-source-round, data-source-state-digest === the service's expected stateDigest, and (D525) the requested glyph mode; a pageerror would have rejected immediately, so one of those attribute equalities never holds. HYPOTHESIS (unproven): a state-digest disagreement between the service and the app now that the V1 decoder validates-and-discards the legacy fog key (B2) — one side may digest the raw fixture bytes or the pre-discard state while the other digests the decoded state — or the derived fog changing what the blind capture app renders for the hard fixture. The same spec passed in the OFFERS landing gate on 1cacd8f0 (187/187, D617.50); a control run on main (port 4480) is in progress. Either way it is VIS-FIELD's to resolve before landing: LAND-02's item (2) is now "diagnose the predicate attribute that fails (instrument in the browser context on a copy), name the producing code on both sides, and fix the VIS-FIELD side or STOP with the mechanism" — a production change there needs a manifest ruling from me. vtt-encounter.spec.ts:237 remains the hand re-pin of the reference scene's derived fog at (4,3).

## D635.38.1 — SUPERVISOR (2026-09-17 04:21): control run confirms the board-snapshot browser spec passes on main 9f148666 (2 passed, 3.4 min, port 4480) and fails alone on the VIS-FIELD tree — VIS-FIELD-caused; LAND-02 diagnoses per D635.38.

exec
/bin/bash -lc "git status --short && git rev-parse --short HEAD && rg -n \"encounter-art-fog|data-cell|concealed|darkvision|senses|Training Brute|reference\" tests/browser/vtt-encounter.spec.ts tests/browser/ai-dm-board-snapshot.spec.ts src/vtt/reference-encounter.ts && sed -n '1,330p' tests/browser/vtt-encounter.spec.ts && sed -n '1,360p' tests/browser/ai-dm-board-snapshot.spec.ts && sed -n '940,1050p' tools/ai-dm-board-snapshot.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
ebfcacaa
src/vtt/reference-encounter.ts:10:import { referencePartySpellSlots } from '../combat/spells/resources';
src/vtt/reference-encounter.ts:57:      senses: [{ kind: 'normal_sight' }],
src/vtt/reference-encounter.ts:81:export function referenceEncounterSetup(): {
src/vtt/reference-encounter.ts:98:    spellSlots: referencePartySpellSlots('Cleric'),
src/vtt/reference-encounter.ts:104:    spellSlots: referencePartySpellSlots('Wizard'),
src/vtt/reference-encounter.ts:106:  const monster = profile('training-brute', 'Training Brute', 'monster', {
src/vtt/reference-encounter.ts:124:      lightRegions: [{ id: 'reference-hiding-shadow', cells: [{ column: 4, row: 3 }], level: 'darkness' }],
src/vtt/reference-encounter.ts:130:    dmNotes: ['Training Brute retreats after the three reference PCs act.'],
src/vtt/reference-encounter.ts:136:  if (found === undefined) throw new Error(`Unknown reference combatant ${id}.`);
src/vtt/reference-encounter.ts:247:export function referenceTurnLegalActions(
src/vtt/reference-encounter.ts:273:export function referenceReactionLegalActions(
tests/browser/vtt-encounter.spec.ts:173:test('M38-PLAYER-NO-DM-CONTROLS and two local windows complete the resumable reference flow', async ({
tests/browser/vtt-encounter.spec.ts:177:  await page.goto('/vtt?encounter=reference&view=player&session=increment-6-browser');
tests/browser/vtt-encounter.spec.ts:234:  await expect(tray).toContainText('Training Brute moved from 3,3 to 4,3');
tests/browser/vtt-encounter.spec.ts:235:  await expect(dm.locator('.dm-pending-request')).toContainText('turn: Training Brute');
tests/browser/vtt-encounter.spec.ts:237:  await expect(dm.locator('[data-cell="4,3"] .encounter-art-fog')).toHaveCount(0);
tests/browser/vtt-encounter.spec.ts:238:  await expect(dm.locator('[data-cell="4,3"] .encounter-token[data-kind="monster"]')).toBeVisible();
tests/browser/vtt-encounter.spec.ts:242:  await expect(page.locator('[data-cell="4,3"] .encounter-art-floor')).toBeVisible();
import type { Page } from '@playwright/test';
import { expect, test } from './fixtures/parallel-test';

async function confirmChoice(page: Page, label: string): Promise<void> {
  await page.getByRole('button', { name: label, exact: true }).click();
  await page.getByRole('button', { name: 'Confirm choice' }).click();
}

async function expectActive(page: Page, name: string): Promise<void> {
  await expect(page.getByRole('heading', { name: `Active PC: ${name}` })).toBeVisible();
  await expect(page.locator('.encounter-token[data-active="true"]')).toHaveText(name);
}

async function resetHome(page: Page): Promise<void> {
  await page.goto('/');
  await expect(page.locator('#status')).toHaveAttribute('data-ready', 'true', {
    timeout: 65_000,
  });
  await page.evaluate(() => window.staticApp.reset());
  await page.reload();
  await expect(page.locator('#status')).toHaveAttribute('data-ready', 'true', {
    timeout: 65_000,
  });
}

test('DM loads the bundled D365 dungeon and RPC-authored party into room 1', async ({
  page,
}) => {
  test.setTimeout(120_000);
  await resetHome(page);
  await page.goto('/vtt?encounter=d365');
  await expect(page.getByRole('heading', { name: 'D365 sample dungeon' })).toBeVisible({
    timeout: 65_000,
  });
  await page.getByRole('button', { name: 'Load bundled dungeon and party' }).click();

  await expect(page.getByRole('heading', { name: 'DM controls' })).toBeVisible({
    timeout: 65_000,
  });
  await expect(page.locator('.adventuring-day-status')).toHaveText(
    'Adventuring day — room 1 of 4 · 2024 rules',
  );
  for (const name of ['Mirel Ash', 'Orin Reed', 'Brann Vale', 'Sera Dawn']) {
    await expect(
      page.locator('.encounter-token[data-kind="player_character"]', { hasText: name }),
    ).toBeVisible();
  }
  await expect(
    page.locator('.encounter-token[data-kind="monster"]', { hasText: 'Goblin Warrior' }),
  ).toHaveCount(2);
  await expect(
    page.locator('.encounter-token[data-kind="monster"]', { hasText: 'Wolf' }),
  ).toHaveCount(2);
  const board = page.locator('.encounter-board');
  await expect(board).toBeVisible();
  await expect(board.locator('.encounter-nameplate')).toHaveCount(0);
  await expect(board.locator('.encounter-creature-badge')).toHaveCount(9);
  await expect(board.locator('.encounter-roster-entry')).toHaveCount(9);
  const rosterContract = await board.evaluate((element) => {
    const badges = Array.from(element.querySelectorAll<HTMLElement>('.encounter-creature-badge'));
    const roster = Array.from(element.querySelectorAll<HTMLElement>('.encounter-roster-entry'));
    const names = roster.map((row) => row.dataset.fullName ?? '');
    return {
      badgeNumbers: badges.map((badge) => badge.dataset.badgeNumber ?? ''),
      badgeColors: badges.map((badge) => badge.dataset.badgeColor ?? ''),
      badgeSources: badges.map((badge) => badge instanceof HTMLImageElement ? badge.src : ''),
      rosterNumbers: roster.map((row) => row.dataset.badgeNumber ?? ''),
      rosterCombatantIds: roster.map((row) => row.dataset.combatantId ?? ''),
      rosterNames: names,
      rosterOrders: roster.map((row) => row.dataset.rosterOrder ?? ''),
      rosterCoordinates: roster.map((row) => row.querySelector<HTMLElement>('.encounter-roster-coordinate')?.dataset.coordinate ?? ''),
      rosterBadgeSources: roster.map((row) => row.querySelector<HTMLImageElement>('.encounter-roster-badge')?.src ?? ''),
      rosterNameBitmapWidths: roster.map((row) => {
        const image = row.querySelector<HTMLImageElement>('.encounter-roster-name');
        return image === null ? null : [image.width, image.naturalWidth];
      }),
      bustBitmapBoxes: Array.from(element.querySelectorAll<HTMLImageElement>('.encounter-token-sprite'))
        .map((image) => [image.getBoundingClientRect().width, image.naturalWidth]),
    };
  });
  expect(rosterContract.badgeNumbers).toEqual(['1', '2', '3', '4', '5', '6', '7', '8', '9']);
  expect(rosterContract.badgeNumbers).toEqual(rosterContract.rosterNumbers);
  expect(rosterContract.badgeSources).toEqual(rosterContract.rosterBadgeSources);
  expect(rosterContract.rosterOrders).toEqual(rosterContract.rosterNumbers);
  expect(new Set(rosterContract.badgeColors).size).toBe(9);
  expect([...rosterContract.rosterNames].sort()).toEqual([
    'Mirel Ash', 'Orin Reed', 'Brann Vale', 'Sera Dawn', 'Tamsin Quill',
    'Goblin Warrior', 'Goblin Warrior', 'Wolf', 'Wolf',
  ].sort());
  const initiativeEntries = page.locator('.dm-initiative > ol > li');
  await expect(initiativeEntries).toHaveCount(9);
  const initiativeCombatantIds = await initiativeEntries.evaluateAll((items) =>
    items.map((item) => item instanceof HTMLElement ? item.dataset.combatantId ?? '' : ''),
  );
  expect(rosterContract.rosterCombatantIds).toEqual(initiativeCombatantIds);
  expect(rosterContract.rosterCoordinates.every((coordinate) => /^\([0-9]+,[0-9]+\)$/u.test(coordinate))).toBe(true);
  for (const dimensions of rosterContract.rosterNameBitmapWidths) {
    expect(dimensions).not.toBeNull();
    expect(dimensions?.[0]).toBe(dimensions?.[1]);
  }
  expect(rosterContract.bustBitmapBoxes).toHaveLength(9);
  expect(rosterContract.bustBitmapBoxes.every(([rendered, bitmap]) => rendered === 128 && bitmap === 128)).toBe(true);
});

test('DM composes stored builder characters and each PC defaults to human control', async ({
  page,
}) => {
  test.setTimeout(120_000);
  await resetHome(page);
  const names = await page.evaluate(async () => {
    const classes = await window.appRpc.call<
      Record<string, never>,
      readonly { readonly content_key: string; readonly name: string }[]
    >('queries.characters.guidedClassOptions', {});
    const fighter = classes.find((candidate) => candidate.name === 'Fighter');
    if (fighter === undefined) throw new Error('Bundled Fighter is missing.');
    const origins = await window.appRpc.call<
      { readonly kind: 'species' },
      readonly { readonly content_key: string; readonly name: string }[]
    >('queries.characters.originOptions', { kind: 'species' });
    const human = origins.find((candidate) => candidate.name === 'Human');
    if (human === undefined) throw new Error('Bundled Human is missing.');
    const createdNames = ['Stored Rowan', 'Stored Sable', 'Stored Tamsin'];
    for (const name of createdNames) {
      const character = await window.appRpc.call<
        { readonly name: string; readonly class_content_key: string },
        { readonly id: number }
      >('queries.characters.createGuided', {
        name,
        class_content_key: fighter.content_key,
      });
      await window.appRpc.call('queries.characters.applyOrigin', {
        character_id: character.id,
        kind: 'species',
        content_key: human.content_key,
      });
    }
    return createdNames;
  });

  await page.goto('/vtt?compose=stored');
  await expect(
    page.getByRole('heading', { name: 'Build an encounter from stored characters' }),
  ).toBeVisible({ timeout: 65_000 });
  for (const name of names) {
    await page.getByRole('checkbox', { name: new RegExp(`^${name} —`, 'u') }).check();
  }
  await expect(page.getByRole('button', { name: 'Start DM encounter' })).toBeEnabled();
  await page.getByRole('button', { name: 'Start DM encounter' }).click();

  await expect(page.getByRole('heading', { name: 'DM controls' })).toBeVisible();
  await expect(page.locator('.player-encounter')).toHaveCount(0);
  for (const name of names) {
    await expect(page.getByLabel(`${name} controller`)).toHaveValue('human');
    await expect(page.getByLabel(`${name} controller`)).toBeEnabled();
    await expect(
      page.locator('.encounter-token[data-kind="player_character"]', { hasText: name }),
    ).toBeVisible();
  }
  await expect(page.locator('.adventuring-day-status')).toHaveText(
    'Adventuring day — room 1 of 4 · 2024 rules',
  );
  await expect(page.getByRole('heading', { name: 'Short Rest before next room' })).toBeVisible();
  await expect(page.getByRole('button', { name: 'Take Short Rest and enter next room' })).toBeVisible();
  await page.getByRole('button', { name: 'End room and enter next room' }).click();
  await expect(page.locator('.adventuring-day-status')).toHaveAttribute('data-room', '2');
  for (const name of names) {
    await expect(page.getByLabel(`${name} controller`)).toHaveValue('human');
    await expect(page.getByLabel(`${name} controller`)).toBeEnabled();
  }
});

test('M38-PLAYER-NO-DM-CONTROLS and two local windows complete the resumable reference flow', async ({
  page,
}) => {
  test.setTimeout(120_000);
  await page.goto('/vtt?encounter=reference&view=player&session=increment-6-browser');
  await expect(page.getByRole('heading', { name: 'Reference encounter' })).toBeVisible({
    timeout: 60_000,
  });

  const popup = page.waitForEvent('popup');
  await page.getByRole('button', { name: 'Open local DM window' }).click();
  const dm = await popup;
  await expect(dm.getByRole('heading', { name: 'DM controls' })).toBeVisible({
    timeout: 60_000,
  });
  await expect(page.locator('.encounter-authority-status')).toHaveAttribute(
    'data-state',
    'connected',
  );
  for (const boardPage of [dm, page]) {
    const tokens = boardPage.locator('.encounter-token');
    await expect(tokens).toHaveCount(4);
    await expect(boardPage.locator('.encounter-cell .encounter-token')).toHaveCount(0);
    for (let index = 0; index < 4; index += 1) {
      await expect(tokens.nth(index)).toHaveAttribute('data-column-span', '1');
      await expect(tokens.nth(index)).toHaveAttribute('data-row-span', '1');
    }
  }

  await expect(page.getByRole('button', { name: 'Undo last' })).toHaveCount(0);
  await expect(page.getByRole('button', { name: 'Apply ADJUDICATED override' })).toHaveCount(0);

  await expectActive(page, 'Reference Fighter');
  await confirmChoice(page, 'Attack');
  await confirmChoice(page, 'End turn');

  await expectActive(page, 'Reference Cleric');
  await confirmChoice(page, 'Sacred Flame');
  await confirmChoice(page, 'End turn');

  await expectActive(page, 'Reference Wizard');
  await page.getByRole('button', { name: 'Shatter', exact: true }).click();
  const board = page.locator('.encounter-board');
  const box = await board.boundingBox();
  if (box === null) throw new Error('Player encounter board has no layout box.');
  await page.mouse.move(box.x + box.width * 0.15, box.y + box.height * 0.35);
  await page.mouse.down();
  await page.mouse.move(box.x + box.width * 0.4, box.y + box.height * (3 / 7), {
    steps: 6,
  });
  await page.mouse.up();
  await expect(page.locator('.encounter-cell[data-preview="true"]')).not.toHaveCount(0);
  await page.getByRole('button', { name: 'Confirm choice' }).click();
  await confirmChoice(page, 'End turn');

  await expect(dm.getByRole('button', { name: 'Move to 4,3', exact: true })).toBeVisible();
  await dm.getByRole('button', { name: 'Move to 4,3', exact: true }).click();
  const tray = dm.locator('.dm-decision-tray');
  await expect(tray.locator('[data-entry-kind="pending"]')).toContainText(
    'Reference Fighter — opportunity attack',
  );
  await expect(tray).toContainText('Training Brute moved from 3,3 to 4,3');
  await expect(dm.locator('.dm-pending-request')).toContainText('turn: Training Brute');
  await dm.getByRole('button', { name: 'Hide', exact: true }).click();
  await expect(dm.locator('[data-cell="4,3"] .encounter-art-fog')).toHaveCount(0);
  await expect(dm.locator('[data-cell="4,3"] .encounter-token[data-kind="monster"]')).toBeVisible();
  await expect(dm.locator(
    '.encounter-token[data-kind="monster"][data-column="4"][data-row="3"]',
  )).toHaveAttribute('data-hidden-from-players', 'true');
  await expect(page.locator('[data-cell="4,3"] .encounter-art-floor')).toBeVisible();
  await expect(page.locator(
    '.encounter-token[data-kind="monster"][data-column="4"][data-row="3"]',
  )).toHaveCount(0);
  await expect(dm.getByRole('button', { name: 'End turn', exact: true })).toBeVisible();

  await dm.getByLabel('Adjudication target').selectOption({ label: 'Reference Fighter' });
  await dm.getByLabel('Hit Point delta').fill('-999');
  await dm.getByLabel('DM reasoning').fill('DM-only collapse reasoning sentinel');
  await dm.getByRole('button', { name: 'Apply ADJUDICATED override' }).click();
  await expect(dm.locator('[data-pause="adjudicated"]')).toBeVisible();
  await expect(dm.locator('.dm-log [data-event-type="adjudicated"]')).toContainText(
    'DM-only collapse reasoning sentinel',
  );

exec
/bin/bash -lc "rg -n '"'^## D635'"\\.(35|36|37|38|39|40|41|42)' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
sed -n '22965,23010p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md
sed -n '1,220p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/review-impl-land-01-astra-r1.md
sed -n '1,260p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-land-02-sol.md" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
22954:## D635.35 — SUPERVISOR (2026-09-17 02:54): VIS-FIELD LANDING GATE vitest phase RED with DETERMINISTIC reds outside every plan manifest — the frozen arena families lose their productivity guarantee under the seam rule; read-only investigation before any ruling
22957:## D635.36 — SUPERVISOR (2026-09-17 03:22): landing investigation harvested — TWO VIS-FIELD DEFECTS (category c) found, plus a legitimate seam consequence on the frozen brutal families that needs the OWNER's decision
22960:## D635.37 — SUPERVISOR (2026-09-17 03:52): VIS-FIELD landing gate complete on 10cc9f5e — tsc 0, sg 0, vitest RED (D635.36), Playwright 184 passed / 2 FAILED, OFFERS checks 0/0, D630 pair 39.3 / 39.4 s (main 32.8 s, +20%, ceiling 41 s), arena round 38 s (main 51 s); LAND-01 dispatched
22963:## D635.38 — SUPERVISOR (2026-09-17 04:07): the board-snapshot browser spec's failure REPRODUCES alone on the clean 10cc9f5e checkout — a beforeAll timeout inside the blind board-snapshot service's board-visible wait; LAND-02 diagnoses it as a candidate VIS-FIELD defect
22966:## D635.38.1 — SUPERVISOR (2026-09-17 04:21): control run confirms the board-snapshot browser spec passes on main 9f148666 (2 passed, 3.4 min, port 4480) and fails alone on the VIS-FIELD tree — VIS-FIELD-caused; LAND-02 diagnoses per D635.38.
22968:## D635.39 — SUPERVISOR (2026-09-17 04:37): LAND-01 complete on its five files (both defects fixed with witnesses and mutants; 214 frozen-family offers all execute; generator bytes 42/42 equal); one out-of-manifest hand pin moves as a consequence — manifest +1; r2 dispatched
22971:## D635.40 — SUPERVISOR (2026-09-17 04:51): LAND-01 r2 — the no-bless pin re-derived by hand (spot-checked by me); the same mechanism moves the Bless-variant pins in the same test; allowance widened to every expectation in that test that scores the three moved monsters; r3 dispatched
22974:## D635.41 — SUPERVISOR (2026-09-17 05:22): LAND-01 verified by me and COMMITTED ebfcacaa; astra medium review dispatched; LAND-02 (browser specs) dispatched in parallel
22977:## D635.42 — SUPERVISOR (2026-09-17 05:40): LAND-01 astra r1 = REJECT (0 P1, 1 P2: LAND-F1); LAND-02 re-pinned the spec and stopped BLOCKED on a manifest question; RULING: LAND-03 realigns the snapshot binding digest to the DM view state

## D635.38.1 — SUPERVISOR (2026-09-17 04:21): control run confirms the board-snapshot browser spec passes on main 9f148666 (2 passed, 3.4 min, port 4480) and fails alone on the VIS-FIELD tree — VIS-FIELD-caused; LAND-02 diagnoses per D635.38.

## D635.39 — SUPERVISOR (2026-09-17 04:37): LAND-01 complete on its five files (both defects fixed with witnesses and mutants; 214 frozen-family offers all execute; generator bytes 42/42 equal); one out-of-manifest hand pin moves as a consequence — manifest +1; r2 dispatched
Sol (impl-land-01-sol.md): engine-query-port.ts:1167 refuses attack/save offers whose trace blocksSight with the new exact code target_outside_line_of_sight, tracing from the proposed origin (execution's sourceAnchor semantics); the targetability audit found no other authorization gate (query-port :1234/:1547 are tactical-scoring cover inputs and :1830 reports physical cover — left alone, and changing them broke valid intel expectations); room-generator.ts:1227 generatedLineIsOpen = tier 'none' && !blocksSight for both opposing-pair and creature-to-cell predicates; the brutal-b helper materializes the first declared legal activation choice (keeps the engine-ranked option). Witnesses: the two-blocker seam fixture (rays (14,6)/(15,6)→(1,6)/(2,6) along row line 6 between (8,5)/(8,6): closed → half/blocksSight true, no attack offered, LOS code; one flank removed → offered); the AGREEMENT PROPERTY over hard 5117001-013, brutal 6203001-010, brutal-b 6206001-010: exactly 214 offered attack/save uses, every one executes through the reducer without the LOS/Total-Cover error; mutant OFFER_IGNORES_BLOCKS_SIGHT → both red (the cardinality would be 451); OPEN_RAY_IGNORES_BLOCKS_SIGHT → red. GENERATOR BYTES: in-memory regeneration of all 42 checked seeds is byte-equal before/after the fix — so the fixed generator does not cure the frozen families; restoring productivity needs a NEW cohort (reselected seeds), which sharpens question 5's option A. Suites: engine-query-port 19/19; los-cover 112/112; brutal-b 37 passed / 10 failed — the ten productivity pins only (the 19 c1 rows and the four helper rows now execute; "rejects rather than skips" is GREEN, contrary to my brief's prediction — the attended pending decision still forms); d569-blind-experiment 46 / 6 retained (frozen-family, owner's decision); OFFERS 11 files 329/329; dm-tactical-intel + challenge-room 23/23; tsc 0/0; OFFERS checks 0/0; pair 38.6 s; 5 files 260+/9−. ONE CONSEQUENCE outside the manifest: tests/unit/vtt/tactical-evaluator-r02.test.ts:262 pins a hand-authored expected-value probability 0.380769180625 that scored three attacks from origins the reducer would have REFUSED (priest m1, scout m3, bandit m4 at {half, blocksSight true}); with the corrected query the offers come from executable origins (priest 30-ft move to (13,4) none/false; scout 5 ft to (18,4) half/false; bandit 5 ft to (17,4) half/false) and the evaluator scores from each option's finalPosition, giving 0.4323649600000002. Legitimate: the old number rated offers that could not execute. RULING: manifest +1 — tests/unit/vtt/tactical-evaluator-r02.test.ts, that ONE expectation, re-derived BY HAND in its comment (each monster's new origin, line tier/sight, hit chance and damage terms, the sum; the old value kept as pre-LAND-01), never copied from the run; r2 on the resumed session; then my verification, astra medium, and LAND-02.

## D635.40 — SUPERVISOR (2026-09-17 04:51): LAND-01 r2 — the no-bless pin re-derived by hand (spot-checked by me); the same mechanism moves the Bless-variant pins in the same test; allowance widened to every expectation in that test that scores the three moved monsters; r3 dispatched
Sol (resumed): :262 re-derived with the full arithmetic in the comment — priest m1 now offered after a 30-ft move to (13,4) (none / sight clear): +5 vs AC 18 → miss 12/20, normal 7/20, critical 1/20, two 2d10 attacks → 2·(7/20·11 + 1/20·22) = 9.9; scout m3 after a 5-ft move to (18,4) (half): AC 20, +4 → 15/20, 4/20, 1/20, two 1d8+2 → 2·(4/20·6.5 + 1/20·11) = 3.7; bandit m4 after 5 ft to (17,4) (half): +3 → 16/20, 3/20, 1/20, 1d8+1 → 1.325; unchanged scout m5 1.75 and bandit m6 1.875; damage sum 18.55; the dying-target death-save fold (tactical-evaluator.ts:786-814, miss/normal/critical → +0/+1/+2 failures capped at three) in initiative order gives 11068542976/25600000000 = 2702281/6250000 = 0.43236496; the evaluator's float order yields 0.4323649600000002; the file's existing 12-place closeness kept; the old 0.380769180625 recorded as pre-LAND-01. SPOT-CHECKED BY ME: the three to-hit splits and the three damage expectations are right (e.g. 2·(3.85+1.1) = 9.9; 2·(1.3+0.55) = 3.7; 0.825+0.5 = 1.325). The stop: the next assertion, bless-scout-scout-priest at :280 (expected 0.5791028347540284, actual 0.6320368904718017), scores the SAME moved priest/scout origins under Bless — the same legitimate mechanism; my one-expectation ruling was too narrow. RULING: the allowance covers EVERY expectation in tests/unit/vtt/tactical-evaluator-r02.test.ts whose allocation includes the moved priest m1 / scout m3 / bandit m4 (the Bless variants included), each re-derived by hand in the same form (Bless adds 1d4 = +2.5 expected to the attack roll → state the new hit/normal/critical splits per monster and the fold), old values kept as pre-LAND-01; any expectation NOT involving those three monsters must stay byte-identical (a change there is a STOP). r3 on the resumed session.

## D635.41 — SUPERVISOR (2026-09-17 05:22): LAND-01 verified by me and COMMITTED ebfcacaa; astra medium review dispatched; LAND-02 (browser specs) dispatched in parallel
VERIFIED BY ME (verify-land-01.log): tsc 0/0; both OFFERS checks 0; 0 forbidden, 0 long lines; tests/fixtures untouched; MY MUTANTS — the query port's sight refusal disabled → both witnesses RED (the seam fixture and the 214-offer agreement property), restored 1a552fba…; the generator's predicate relaxed to tier-only → OPEN_RAY_IGNORES_BLOCKS_SIGHT RED, restored 8d85ae64…; 16 suites 299/299 (engine-query-port, r02, dm-tactical-intel, challenge-room, the three offer-environment suites, engine-mcp-handler/server/golden, board-delivery, board-snapshot, SIMULATED, dm-encounter-host-live-path, replay, detection); los-cover 112/112; EXACTLY the 16 expected reds (ten brutal-b productivity pins, six D569 second-family manifest) — the frozen-family consequence that is the owner's question 5; pair 36/36 in 38 s. Committed ebfcacaa (6 files 325+/22−). vis-field = ebfcacaa. Astra (resume 01a0a642…, medium; brief review-impl-land-01-astra.md; log review-land-01-astra-r1.log) reviews LAND-01 on the detached checkout; LAND-02 (fresh sol; brief impl-vis-field-land-02.md; log impl-land-02.log) re-pins vtt-encounter.spec.ts:237 by hand and diagnoses the board-snapshot spec's board-visible wait (D635.38) on the worktree, its Playwright runs on port 4470 only.

## D635.42 — SUPERVISOR (2026-09-17 05:40): LAND-01 astra r1 = REJECT (0 P1, 1 P2: LAND-F1); LAND-02 re-pinned the spec and stopped BLOCKED on a manifest question; RULING: LAND-03 realigns the snapshot binding digest to the DM view state
ASTRA r1 (medium, resume 01a0a642…; saved review-impl-land-01-astra-r1.md): production fix correct (query :1167-1174 and execution :7183-7203 same inputs via cover.ts:584-594; :1234/:1547/:1830 correctly untouched); 214 options = 316 components recounted (hard 142 / brutal 57 / brutal-b 15; 204 multiattack + 108 ordinary + 4 saves), no option skipped; seam geometry re-enumerated in rational arithmetic (12/16 rays enter a blocker, 4/16 run the row-6 seam, shared edge t in [5/13, 6/13]); 10/10 brutal-b seeds byte-identical pre/post predicate (the fix does not cure the frozen cohort — confirms option A of question 5); both R02 allocations reproduced by Fraction enumeration (2702281/6250000; 1035529241349/1638400000000); both controls byte-identical; its own mutants: sight refusal disabled → both witnesses red (451 options offered), predicate relaxed → witness red. LAND-F1 (P2): the agreement property "checks" saves by executing force_save, whose reducer branch never traces sight — astra's blocking-column probe survived. VERIFIED BY ME: resolveTargetSave (encounter.ts:3577) computes only coverSavingThrowBonus and the force_save branch (:12451) validates the declaration, spends the cost and resolves — no sight trace; the property's save half proves nothing about sight. Accepted; test-only fix (direct trace assertion per component, a blocked-save query witness, astra's mutant as the proof).
PARKED (owner, pre-existing, not a VIS-FIELD defect): force_save enforces no clear-path/total-cover rule at execution while the query refuses blocksSight/total saves; query stricter than execution is safe for the offers boundary (nothing offered fails) but the reducer accepts a save through a wall if a client bypasses the offers.
LAND-02 (sol 01a0aeac…, saved impl-land-02-sol.md): vtt-encounter.spec.ts:237 re-pinned by hand — fighter (2,3), cleric (1,4), wizard (1,2) normal sight only, dark target cell (4,3) unseen by all three, so DM fog and the owned-party complement are exactly {(4,3)}: DM fog count 1, DM token visible + hidden-from-players, player floor visible + fog count 1 + zero monster tokens; sol reports 3/3 passed on port 4470, tsc 0/0. MY VERIFICATION of the re-pin (patch applied to the detached checkout, port 4471, plus a reverse mutant) is in flight (verify-land-02-spec.log). Board-snapshot diagnosis (sol; VERIFIED BY ME in source): the service binds to boardStateDigest(state) = sha256(canonicalJson(state)) (tools/ai-dm-board-snapshot.ts:201) while the app renders sha256(canonicalJson(projectDmView(state).state)) (encounter-projections.ts:488 → encounter-app.ts:2308); since B3 the DM view state carries the derived fog that canonical state no longer holds (B2), so #waitForBoardVisible (:992-1030) never matches for any state with a non-empty derived field — 176 derived cells in the spec's fixture. This is not spec-only: every live capture (ai-dm-conversation.ts:4487, glyph captures, the two snapshot checks, the screenshot probe) binds through the same function and would time out. RULING (manifest addition under landing reconciliation, D635.36 pattern): the binding digest is the digest of the DM view state as the app defines it; boardStateDigest(state) becomes sha256(canonicalJson(projectDmView(state).state)) so all service callers bind to the rendered value; witness with a negative control (raw digest differs), the browser spec must pass, independent recomputations re-derived only where they model the rendered state, D569 demonstrate-then-normalize elsewhere, any embedded manifest digest = BLOCKED not regenerated. Dispatched as ONE sol lane (resume 01a0ae59…, brief impl-vis-field-land-03.md, log impl-land-03.log): Task A = LAND-01 fix r1, Task B = LAND-03. The uncommitted spec re-pin stays in the worktree, off-limits to the lane.

OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
model: gpt-6-astra
provider: openai
approval: never
sandbox: read-only
reasoning effort: medium
reasoning summaries: none
session id: 01a0a642-35a1-7082-aba2-eda56ea291c1
--------
user
RULES (binding): You are the REVIEWER (gpt-6-astra, MEDIUM per D627, resumed — the session that reviewed every VIS-FIELD batch). Worktree /home/vagrant/PhpstormProjects/dnd-wt-seam-verify — a DETACHED clean checkout at ebfcacaa (= 10cc9f5e (B6 ACCEPTED, the complete frozen plan) + LAND-01; node_modules symlinked). Sandbox read-only; in-memory when /tmp is read-only; do NOT edit files, no git writes, no claude / `claude -p` / other agents / consensus skills; skills under ~/.codex do NOT apply; never port 4173; no full gate, full vitest, Playwright, npm run build. Do not read sibling worktrees or /home/vagrant/dnd-slim-runs/. MAIN repo /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static's .claude/decisions.md and .claude/consensus/vis-field/** readable: read D635.35–D635.41, impl-landing-investigation-sol.md and impl-land-01-sol.md first. Model calls WILL fail. Report exact commands and numbers; end with the named marker.

TASK: Review `git diff 10cc9f5e ebfcacaa` (6 files, 325+/22−: src/vtt/engine-query-port.ts, src/vtt/room-generator.ts, tests/unit/vtt/{engine-query-port,room-generator-los-cover,arena-basis-brutal-b,tactical-evaluator-r02}.test.ts) = LAND-01, the landing-reconciliation batch for the two VIS-FIELD defects the full-suite landing gate exposed (D635.36): (c1) the query port offered attacks/saves whose corner trace blocks sight (tier-only check) while execution refuses them — an offers-boundary violation; (c2) the generator's open-ray predicate ignored blocksSight; plus the brutal-b helper's missing activation choice, and six tactical-evaluator kill-probability pins that scored shots from origins the reducer would refuse. Supervisor verification (verify-land-01.log): tsc 0/0; OFFERS checks 0/0; my mutants (sight check disabled → both query witnesses red incl. the 214-offer agreement property; generator predicate relaxed → its witness red); 16 suites 299/299 (query port, r02, dm-tactical-intel, challenge-room, the OFFERS set, replay, detection); los-cover 112/112; exactly the 16 expected reds remain (ten brutal-b productivity pins + six D569 manifest — the frozen-family consequence awaiting the owner, D635.36); pair 38 s.

CHECK (each with commands and numbers): (1) c1: the refusal at engine-query-port.ts:~1171 uses the SAME trace inputs as execution (encounter.ts:~7183: actor, target, sourceAnchor = the proposed origin) — could the two still disagree (a different anchor, footprint, exclusion set, or a path where execution traces after a move the query did not model)? Enumerate every production site that decides attack/save targetability and confirm each is now sight-aware or is not an authorization gate (sol left :1234/:1547 tactical-scoring and :1830 cover reporting alone — agree or not). (2) The AGREEMENT PROPERTY ("offered attacks and saves in every frozen landing family pass the reducer LOS rule"): read it — is executing each offer through reduceEncounter at the resolved finalPosition a faithful model of real execution (initiative/active-combatant setup, the () => 0.5 roll, multiattack components, bonus-action slot)? 214 offers checked — recount from the three families; a property that silently skips options is a P1. (3) c2: generatedLineIsOpen = tier 'none' && !blocksSight at both call sites; the witness's geometry (14,6)→(1,6) along row line 6 between (8,5)/(8,6) — verify; sol's in-memory regeneration of 42 seeds byte-equal — reproduce for at least the ten brutal-b seeds (so the frozen families are NOT cured by the fix; relevant to the owner's question). (4) The six R02 pins: recompute at least two allocations independently (no-bless 2702281/6250000 and one Bless variant; Bless enumerated over the d4 faces; the death-save fold in initiative order per tactical-evaluator.ts:786-814) and confirm the two unchanged controls are byte-identical to 10cc9f5e. (5) The brutal-b helper: materializing the first declared legal activation choice keeps the engine-ranked option — confirm it does not mask a resolver defect and that dispel-evil-and-good's choice contract is what the helper reads. (6) No fixture changed; the remaining reds are exactly the frozen-family ones; tsc 0/0; suites you can run; pair (ceiling ≈41 s).

OUTPUT: findings LAND-Fn with severity P1/P2/P3, file:line, evidence, fix; then exactly "VERDICT: ACCEPT" or "VERDICT: REJECT (n P1, m P2)"; end with REVIEW DONE.

codex
I’ll read the landing rulings and reports first, then check the targeting agreement, generator behavior, and probability pins against the code and targeted tests.

exec
/bin/bash -lc "rg -n '"'^## D635'"\\.(35|36|37|38|39|40|41)' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md; cat /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-land-01-sol.md; cat /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-landing-investigation-sol.md" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
22954:## D635.35 — SUPERVISOR (2026-09-17 02:54): VIS-FIELD LANDING GATE vitest phase RED with DETERMINISTIC reds outside every plan manifest — the frozen arena families lose their productivity guarantee under the seam rule; read-only investigation before any ruling
22957:## D635.36 — SUPERVISOR (2026-09-17 03:22): landing investigation harvested — TWO VIS-FIELD DEFECTS (category c) found, plus a legitimate seam consequence on the frozen brutal families that needs the OWNER's decision
22960:## D635.37 — SUPERVISOR (2026-09-17 03:52): VIS-FIELD landing gate complete on 10cc9f5e — tsc 0, sg 0, vitest RED (D635.36), Playwright 184 passed / 2 FAILED, OFFERS checks 0/0, D630 pair 39.3 / 39.4 s (main 32.8 s, +20%, ceiling 41 s), arena round 38 s (main 51 s); LAND-01 dispatched
22963:## D635.38 — SUPERVISOR (2026-09-17 04:07): the board-snapshot browser spec's failure REPRODUCES alone on the clean 10cc9f5e checkout — a beforeAll timeout inside the blind board-snapshot service's board-visible wait; LAND-02 diagnoses it as a candidate VIS-FIELD defect
22966:## D635.38.1 — SUPERVISOR (2026-09-17 04:21): control run confirms the board-snapshot browser spec passes on main 9f148666 (2 passed, 3.4 min, port 4480) and fails alone on the VIS-FIELD tree — VIS-FIELD-caused; LAND-02 diagnoses per D635.38.
22968:## D635.39 — SUPERVISOR (2026-09-17 04:37): LAND-01 complete on its five files (both defects fixed with witnesses and mutants; 214 frozen-family offers all execute; generator bytes 42/42 equal); one out-of-manifest hand pin moves as a consequence — manifest +1; r2 dispatched
22971:## D635.40 — SUPERVISOR (2026-09-17 04:51): LAND-01 r2 — the no-bless pin re-derived by hand (spot-checked by me); the same mechanism moves the Bless-variant pins in the same test; allowance widened to every expectation in that test that scores the three moved monsters; r3 dispatched
22974:## D635.41 — SUPERVISOR (2026-09-17 05:22): LAND-01 verified by me and COMMITTED ebfcacaa; astra medium review dispatched; LAND-02 (browser specs) dispatched in parallel
# VIS-FIELD LAND-01 — sol report (fresh 01a0ae59…, 389 k tokens; BLOCKED only on one out-of-manifest hand pin)

utal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index d76fdc233b9b4def2bc1d4a141cc81f083564e5f..3613a2eb3c8585746b048672e2aea6e388c0d940
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -15,6 +15,10 @@
   resolveEngineActorOption,
   type EngineTurnProposal,
 } from '../../../src/vtt/intent-resolver';
+import type {
+  EngineActivationChoice,
+  EngineOfferableOption,
+} from '../../../src/vtt/turn-proposal';
 import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { actorOpportunityReport } from '../../../src/vtt/intel/opportunity-cost';
 import { decodeArenaFixture } from '../../../src/vtt/mcp/entrypoint';
@@ -142,6 +146,20 @@
   return actors;
 }
 
+function deterministicActivationChoice(option: EngineOfferableOption): EngineActivationChoice | undefined {
+  const slot = option.activationChoice;
+  if (slot === undefined || slot === null) return undefined;
+  switch (slot.kind) {
+    case 'command_word': return { kind: slot.kind, value: slot.values[0] };
+    case 'unicorns_blessing_spell': return { kind: slot.kind, value: slot.values[0] };
+    case 'dispel_evil_and_good_mode': return { kind: slot.kind, value: slot.values[0] };
+    case 'calm_emotions_per_target': return {
+      kind: slot.kind,
+      selections: slot.targetIds.map((targetId) => ({ targetId, mode: slot.values[0] })),
+    };
+  }
+}
+
 function dryRunMonsterPlan(state: EncounterState): ReadonlyMap<CombatantId, AuthorizedEngineTurnProposal> {
   const actorIds = [...livingMonsterIds(state)].sort((left, right) => left.localeCompare(right));
   const planningState = projectFutureMonsterTurns(state, actorIds);
@@ -160,12 +178,14 @@
       state.revision,
     ).find((option) => option.optionId !== primary.optionId);
     if (fallback === undefined) throw new Error(`Dry-run plan has no independent fallback for ${actorId}.`);
+    const activationChoice = deterministicActivationChoice(primary);
     const proposal: EngineTurnProposal = {
       actorId,
       expectedRevision: state.revision,
       primaryOptionId: primary.optionId,
       fallbackOptionId: fallback.optionId,
       reason: 'Use the engine-ranked option to maximize immediate tactical value.',
+      ...(activationChoice === undefined ? {} : { activationChoice }),
       overrideJustification: null,
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 66fe8cfa5e7742935be2fd8a47e42d07a131f38b..62acd4131b7a3469070f9f41ce9c246e4904c1e2
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -9,24 +9,35 @@
 } from '../../../src/combat/encounter';
 import { traceCombatantLine } from '../../../src/combat/cover';
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
-import { monsterAttackCommand } from '../../../src/combat/monster-commands';
+import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
-import { armorClass, combatantId, statblockId, worldObjectId, type CombatantId } from '../../../src/combat/values';
+import {
+  armorClass,
+  combatantId,
+  feet,
+  statblockId,
+  worldObjectId,
+  type CombatantId,
+} from '../../../src/combat/values';
 import { projectDmView } from '../../../src/combat/visibility';
 import { ENCOUNTER_VIEW_CLASSIFICATION } from '../../../src/combat/visibility';
 import { projectEncounterBoard } from '../../../src/vtt/encounter-board';
 import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import { monsterActions, monsterBonusActions } from '../../../src/vtt/engine-query-port';
 import {
   availableEngineActorOptions,
   createPureTurnProposalResolver,
   engineActorOptionsForEnvironment,
+  resolveEngineActorOption,
 } from '../../../src/vtt/intent-resolver';
 import { engineOptionId } from '../../../src/vtt/turn-proposal';
 import { generateRoom } from '../../../src/vtt/room-generator';
 import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
 import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
 import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
 import { readFileSync } from '../../helpers/test-filesystem';
 
@@ -36,6 +47,21 @@
 const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
 const TURN_PROPOSAL_RESOLVER = createPureTurnProposalResolver(OFFER_ENVIRONMENT);
 
+const AGREEMENT_FIXTURE_PATHS = [
+  ...Array.from(
+    { length: 13 },
+    (_unused, index) => `tests/fixtures/arena-basis-hard/seed-${String(5_117_001 + index)}.json`,
+  ),
+  ...Array.from(
+    { length: 10 },
+    (_unused, index) => `tests/fixtures/arena-basis-brutal/seed-${String(6_203_001 + index)}.json`,
+  ),
+  ...Array.from(
+    { length: 10 },
+    (_unused, index) => `tests/fixtures/arena-basis-brutal-b/seed-${String(6_206_001 + index)}.json`,
+  ),
+] as const;
+
 function legacyDocument(
   state: EncounterState,
   foggedCells: unknown,
@@ -82,6 +108,49 @@
   });
 }
 
+function twoBlockerSeamState(openLowerFlank: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GOBLIN_WARRIOR, {
+    combatantId: 'combatant:query-seam-shooter',
+    tokenId: 'token:query-seam-shooter',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-seam-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 16, rows: 8 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 14, 5), placedToken(target, 1, 6)],
+      blockedCells: openLowerFlank
+        ? [{ column: 8, row: 5 }]
+        : [{ column: 8, row: 5 }, { column: 8, row: 6 }],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+function stateAtResolvedOrigin(
+  state: EncounterState,
+  actorId: CombatantId,
+  position: GridCell,
+): EncounterState {
+  return {
+    ...state,
+    activeCombatant: actorId,
+    activeInitiativeIndex: 0,
+    tokens: state.tokens.map((token) => token.combatantId === actorId
+      ? { ...token, position }
+      : token),
+  };
+}
+
 describe('canonical engine query port', () => {
   it('ENGINE_QUERY_VISIBILITY_EQUALS_CANONICAL_DETECTOR including located outcomes', () => {
     const observerBase = monsterProfile('query-visibility-observer');
@@ -423,6 +492,110 @@
     expect(projectEncounterBoard(projectDmView(walled)).blockedCells).toEqual(walledCells);
   });
 
+  it('OFFER_IGNORES_BLOCKS_SIGHT refuses a half-cover two-blocker seam and opens one flank', () => {
+    const closed = twoBlockerSeamState(false);
+    const opened = twoBlockerSeamState(true);
+    // The four formerly clear rays are (14,6)/(15,6) -> (1,6)/(2,6). Each follows row line 6
+    // between wall cells (8,5)/(8,6); the other twelve corner rays enter one of those cells.
+    expect(traceCombatantLine(closed.state, closed.actorId, closed.targetId)).toMatchObject({
+      tier: 'half',
+      blocksSight: true,
+      firstBlockingCell: { column: 8, row: 5 },
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(closed.state, {
+      actorId: closed.actorId,
+      targetId: closed.targetId,
+      actionId: 'shortbow',

OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-vis-field
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR] (network access enabled)
reasoning effort: high
reasoning summaries: none
session id: 01a0aeac-47dc-7361-830f-5bcc1251fc36
--------
user
COMMON RULES (binding, restated in full): You are the IMPLEMENTER (gpt-5.6-sol, high, FRESH session) for VIS-FIELD-01's LANDING RECONCILIATION batch LAND-02 (browser specs). Worktree /home/vagrant/PhpstormProjects/dnd-wt-vis-field (branch claude/vis-field at ebfcacaa = the complete frozen plan + LAND-01 (accepted pending astra); clean; own node_modules), sandbox workspace-write in that worktree ONLY (+ /tmp). No git write commands. No edits under .claude/** or docs/**. Do NOT invoke claude, `claude -p`, any reviewer session, any consensus workflow or any other agent; skills under ~/.codex do NOT apply. NEVER touch port 4173 (the owner's live app); if you must run a browser spec, use PLAYWRIGHT_PORT=4470 PLAYWRIGHT_WORKERS=1 and ONLY the spec you changed, once; no full Playwright run, no full gate, no full vitest, no `npm run build` beyond what the spec's webServer performs. No `any`, @ts-ignore, @ts-expect-error, .skip, .todo, no conditional test branches; NO WEAKENED ASSERTIONS; no deleted tests; every re-pinned expectation hand-derived from the geometry and the plan's fog rules (§3.3: DM fog = complement of the union of eligible DM-party fields; player concealedCells = complement of eligible owned fields; ordinary darkness is target-cell only, D626.3), never copied from a run. No fixture under tests/fixtures changes. Other lanes run in sibling worktrees: do not read or touch them; do not read /home/vagrant/dnd-slim-runs/. MAIN repo /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static's .claude/decisions.md and .claude/consensus/vis-field/** readable (read-only): read D626.3, D626.10, D635.36, D635.37, D635.38 first. No model calls. Report exact commands and numbers; end with the named marker or "BLOCKED: <reason>".

TASK — LAND-02: the landing gate's browser suite (Playwright chromium, 184 passed / 2 failed on 10cc9f5e) exposed browser specs no plan batch named. (1) tests/browser/vtt-encounter.spec.ts:173 "M38-PLAYER-NO-DM-CONTROLS and two local windows complete the resumable reference flow": at :237 it expects `dm.locator('[data-cell="4,3"] .encounter-art-fog')` toHaveCount(0) after the Training Brute moves 3,3 → 4,3 and hides. Reference scene (src/vtt/reference-encounter.ts): 10×7, fighter (2,3), cleric (1,4), wizard (1,2), blocker (7,2), ordinary darkness at (4,3) only, no authored fog any more (B4). Under the plan's rules the DM board's fog is derived: HAND-DERIVE which cells are unseen by all three PCs after the brute stands in (4,3) (the brute's body does not change light; (4,3) is a dark target cell — is it seen by any PC without darkvision? state each PC's sense list from the setup) and re-pin :237 (and :238 the DM token visibility, :242 the player's floor art at (4,3), :243-245 the player's monster count) accordingly, with the derivation in a comment; check EVERY other assertion in that flow and in the file that depends on the reference scene's fog or concealment (grep encounter-art-fog, data-cell, concealed) and re-pin each the same way; assertions about undo/adjudication/controls stay byte-identical. (2) [D635.38: it DID reproduce alone on a clean checkout — first beforeAll (:186) times out at 30 s inside BoardSnapshotService.#waitForBoardVisible (tools/ai-dm-board-snapshot.ts:992-1030), whose predicate requires the rendered board's data-board-audience, data-source-revision, data-source-round, data-source-state-digest and glyph mode to equal the service's expected source; find WHICH attribute never matches by evaluating the predicate's inputs in the page (a copy under /tmp, or a temporary console probe you remove), name the code producing each side (the service's stateDigest vs the app's data-source-state-digest — check whether B2's validate-and-discard of the legacy fog key or B3/B4's derived fog changed one side), and report the mechanism; if the fix is in production code, STOP with the file:line and the proposed change (manifest ruling); if it is the spec's own fixture/expectation, re-pin by hand] tests/browser/ai-dm-board-snapshot.spec.ts:258 "captures the full production DM board deterministically through durable save upload/load" failed in the gate at 0 ms with "Target page, context or browser has been closed" ×4 — the supervisor is re-running it alone on a clean checkout; D635.38 states whether it reproduced: if it did NOT reproduce, leave that file untouched and say so; if it DID, diagnose (its beforeAll starts a blind service; a fixture whose derived fog or semantic v2 bytes changed?) and re-pin by hand or STOP with the mechanism. (3) tests/browser/** other specs that pin fog/concealment of the reference, Vane, two-room or d365 scenes: grep and list; if any would fail under derived fog, re-pin by hand in this batch and name them; if none, say so. ALLOWED FILES: tests/browser/vtt-encounter.spec.ts, tests/browser/ai-dm-board-snapshot.spec.ts (only if (2) reproduced), plus any spec you list under (3) — STOP and report if a production file would need to change. VERIFY: run each changed spec ONCE on port 4470 (PLAYWRIGHT_PORT=4470 PLAYWRIGHT_WORKERS=1 npx playwright test <spec> --project=chromium) and paste the result; both tsc; git diff --check; status = exactly the changed specs. End with `VIS-FIELD LAND-02 DONE` or `BLOCKED: <reason>`.

codex
I’ll first read the five binding decisions and inspect the two browser specs plus the snapshot predicate. I’ll hand-derive every fog/concealment expectation before editing, then run only each changed Chromium spec once on port 4470, followed by both TypeScript checks, `git diff --check`, and status.
exec
/bin/bash -lc "sed -n '/"'^## D626'"\\.3\\b/,/"'^## D/p; /''^## D626'"\\.10\\b/,/"'^## D/p; /''^## D635'"\\.36\\b/,/"'^## D/p; /''^## D635'"\\.37\\b/,/"'^## D/p; /''^## D635'"\\.38\\b/,/"'^## D/p'"' .claude/decisions.md" in /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static
 succeeded in 0ms:
## D626.3 — OWNER (2026-09-15 16:03): ordinary darkness = "target only" (reading 1); copy the Foundry / Roll20 / Owlbear Rodeo vision model
Owner: "you see a campfire from a mile away on a dark night. that supports reading 1. try to copy what owlbear rodeo and foundry and roll20 have done". Ruling: unlit cells between observer and target never block sight; ordinary darkness is evaluated at the target cell only (the VIS-FIELD-01 plan's frozen default, §1/§3.2, stands; §7's flip is closed — no amendment needed). Model to copy, as those three products share it: (1) walls/terrain block sight; (2) light sources give bright/dim radii, a target is visible when its cell is lit or within the observer's darkvision (Foundry "vision mode" / Roll20 "night vision") range, with unlimited range for lit targets; (3) darkness never blocks line of sight; (4) magical darkness is a darkness region that defeats ordinary darkvision, with Devil's Sight as a vision mode that ignores it (Foundry); (5) obscuring smoke/fog regions block sight through them (Owlbear "smoke & spectre", Foundry fog-cloud practice); (6) fog of war is the union of the party's vision, per seat for players. The plan already implements (1)-(6). One deliberate gap versus Foundry and Roll20 Explorer mode: explored-but-not-currently-visible memory (greyed explored areas). VIS-FIELD-01 keeps that out of scope (plan §1 non-goals); recorded as follow-up FOG-MEMORY-01 (per-seat explored-cell set derived from past fields, greyed on the board) to be planned after VIS-FIELD-01 lands, unless the owner wants it inside this unit.

## D626.4 — SUPERVISOR (2026-09-15 16:05): VIS-FIELD-01 Batch 1 BLOCKED by sol on a second exhaustive inventory outside the manifest; bounded amendment — src/vtt/intel/option-outcome.ts joins Batch 1 (11 files, one-record delta); re-dispatched
## D626.10 — SUPERVISOR (2026-09-15 19:36): VIS-FIELD B3 r4 candidate — batch green (154/154, 5/5 supervisor mutants killed, pair 38.3 s = +14.7%, within D630) but the full tests/unit/combat + tests/unit/vtt sweep shows 7 deterministic reds in five consumer suites; dispositions
(1) DEFECT in B3 (fix r5): tests/unit/combat/visibility.test.ts — sol DELETED the D359 ledger-pinned control "omits a fogged edge cell and its contents while retaining the adjacent visible boundary cell" (old :339); tests/unit/vtt/mutation-ledger.test.ts:375 pins that title as one of four restored controls with its killing check. The plan names no removal; COMMON RULES forbid deleting a test the plan does not name. Required: restore the control under its EXACT title with derived-fog geometry (an edge cell made unseen for every eligible observer by darkness/wall, its contents omitted, the adjacent visible boundary cell retained) so the ledger's killing check still kills. (2) INTERMEDIATE REDS owned by later batches, accepted as the plan's sequencing (B3 stops consumption; B4 removes the authored lists that fixtures still carry; B5/B6 migrate the semantic source): tests/unit/vtt/generated-encounter-fixtures.test.ts (B4 manifest), tests/unit/vtt/semantic-board-payload.test.ts ×2 (B5/B6 manifests: semantic source still copies authored fog while the DM board projection is derived), tests/unit/tools/ai-dm-screenshot-probe.test.ts "derives every fact class…" (plan :197 excludes the Q9 suite from B3's gate; tools/ai-dm-screenshot-probe.ts is B5's). (3) PLAN GAP → B4 manifest amendment +2 (plan :266 analogue): tests/unit/vtt/two-room-fixture.test.ts and tests/unit/vtt/handoff-examples.test.ts pin the two-room handoff fixture's projections and tracked scene bytes (fixtures/scenes/two-room.v1.json / two-room.snapshots.v1.json) whose authored foggedCells (src/vtt/handoff/fixtures/two-room.ts:112) B4 removes; they can only go green in B4 and belong to no manifest. Landing rule unchanged: every suite green at landing; the intermediate reds are listed by owner in the vis-field loop-log and re-checked after each batch.

## D626.11 — SUPERVISOR (2026-09-15 19:52): VIS-FIELD-01 Batch 3 ACCEPTED at 78ee31b0 — astra r1 ACCEPT (medium, 7 mutations killed incl. three of its own; pin demonstration reproduced independently: old hashes from the pre-B3 module, only cells + concealedCells changed, concealed {(4,3)}, edge-touch rule clears the blocker rays); next B4 with the D626.10 manifest amendment (+2)
## D635.36 — SUPERVISOR (2026-09-17 03:22): landing investigation harvested — TWO VIS-FIELD DEFECTS (category c) found, plus a legitimate seam consequence on the frozen brutal families that needs the OWNER's decision
Sol (impl-landing-investigation-sol.md, read-only, 387 k tokens), classification of every deterministic red: (c) DEFECT 1 — src/vtt/engine-query-port.ts:1163 refuses an attack/save offer only when the physical trace's tier === 'total', while execution (src/combat/encounter.ts:7183) refuses when attackLine.blocksSight || tier === 'total'; D635 made optical blocking independent of physical cover, so a seam-blocked ray with tier 'half' is OFFERED and then REFUSED at execution ("The target has Total Cover or is outside line of sight.") — 19 of the 23 scripted round-1 failures (hard 5117001-007, 009-013; brutal 6203002, 003, 005, 006, 008, 009; brutal-b 6206004) are exactly this offer/execution disagreement (e.g. hard 5117001 m6 Heavy Crossbow at the wizard along row line 6 between (8,5)/(8,6); brutal 6203002 m1 Radiant Flame after its move, along row line 5 between (8,4)/(8,5)). This violates the offers boundary (an offered option must execute) and is VIS-FIELD's to fix before landing. (c) DEFECT 2 — src/vtt/room-generator.ts:1223 hasOpenOpposingRay() calls a ray open when tier === 'none' without requiring blocksSight === false, so the generator's M576-E2 wall placement no longer certifies a sight lane. (a) LEGITIMATE CONSEQUENCE — 25 monsters across the ten frozen brutal-b rooms and 18 across seven of the ten frozen brutal-2 rooms (the D569 second family: 6207002, 004-009) lose every productive first-turn option because their only lanes were two-blocker seams (per-seed flanking cells listed; mechanism count 25 seam / 0 derived-fog / 0 other; the B2 consolidation shown non-causal); the D569 second-family validator re-checks productivity against the current engine, so the frozen 480-cell second family is now an invalid manifest. (a′) LATENT TEST-HELPER DEFECT — four scripted rows (brutal 6203004; brutal-b 6206001, 009, 010) fail because dryRunMonsterPlan() in arena-basis-brutal-b.test.ts:145 selects dispel-evil-and-good without its required activationChoice (ACTIVATION_CHOICE_INVALID at intent-resolver.ts:578); D635 changed the ranking and exposed it. (b) derived-fog/detection consequences: none. All three suites pass under main's tracer (in-memory replay). VERIFIED BY ME by reading: engine-query-port.ts:1163 tests only `tier === 'total'`; encounter.ts:7183 tests `attackLine.blocksSight || attackLine.tier === 'total'` — the disagreement is real. RULING: (1) a VIS-FIELD LANDING RECONCILIATION batch (LAND-01) fixes the two production defects and the helper now: the query port refuses to offer an attack/save whose trace blocksSight (agreement witness: for a seam-blocked pair the query offers nothing the reducer would refuse — a hand-authored fixture plus a property over the frozen families that every offered attack executes); the generator requires tier 'none' AND blocksSight false for an open opposing ray (witness + mutant); dryRunMonsterPlan materializes a legal activation choice or skips choice-requiring options; nothing else. (2) The frozen brutal-b and brutal-2 families and the D569 second-family manifest are the OWNER's decision, asked now (regenerate under the seam-aware generator, invalidating the second-family experiment identity: 240 cell state hashes and a new manifest version; or amend the productivity contract — sol and I advise against; or revisit D635). Landing waits for both.

## D635.37 — SUPERVISOR (2026-09-17 03:52): VIS-FIELD landing gate complete on 10cc9f5e — tsc 0, sg 0, vitest RED (D635.36), Playwright 184 passed / 2 FAILED, OFFERS checks 0/0, D630 pair 39.3 / 39.4 s (main 32.8 s, +20%, ceiling 41 s), arena round 38 s (main 51 s); LAND-01 dispatched
## D635.37 — SUPERVISOR (2026-09-17 03:52): VIS-FIELD landing gate complete on 10cc9f5e — tsc 0, sg 0, vitest RED (D635.36), Playwright 184 passed / 2 FAILED, OFFERS checks 0/0, D630 pair 39.3 / 39.4 s (main 32.8 s, +20%, ceiling 41 s), arena round 38 s (main 51 s); LAND-01 dispatched
Playwright (1.0 h, port 4450): tests/browser/ai-dm-board-snapshot.spec.ts:258 "captures the full production DM board deterministically through durable save upload/load" (0 ms — "Target page, context or browser has been closed", four times) and tests/browser/vtt-encounter.spec.ts:173 "M38-PLAYER-NO-DM-CONTROLS and two local windows complete the resumable reference flow" (15.0 s — expect(locator).toHaveCount failed). The second is plausibly a legitimate consequence of B4 on the REFERENCE scene (authored fog (8,1),(8,2) replaced by derived fog [(4,3)]) that no batch's manifest covered because the plan named no browser spec; the first may be a crash chain from the same run. Both are being read from the gate's JSON report before any ruling; browser specs that pin the reference scene's fog need a hand-derived re-pin under the same rule as every other pin. LAND-01 (fresh sol 01a0ae59…, brief impl-vis-field-land-01.md, log impl-land-01.log) fixes the two production defects and the helper now.

## D635.38 — SUPERVISOR (2026-09-17 04:07): the board-snapshot browser spec's failure REPRODUCES alone on the clean 10cc9f5e checkout — a beforeAll timeout inside the blind board-snapshot service's board-visible wait; LAND-02 diagnoses it as a candidate VIS-FIELD defect
## D635.38 — SUPERVISOR (2026-09-17 04:07): the board-snapshot browser spec's failure REPRODUCES alone on the clean 10cc9f5e checkout — a beforeAll timeout inside the blind board-snapshot service's board-visible wait; LAND-02 diagnoses it as a candidate VIS-FIELD defect
pw-snapshot-alone.log (detached checkout, port 4460, single worker): tests/browser/ai-dm-board-snapshot.spec.ts fails in its FIRST beforeAll (:186) with "beforeAll hook timeout of 30000ms exceeded" — the blind warmup's captureSynchronized → #prepareBoard → #waitForBoardVisible (tools/ai-dm-board-snapshot.ts:992-1030) never resolves; the three dependent beforeAlls then throw their "did not complete" guards and the test reports 0 ms with the closed-browser errors. The wait predicate requires the rendered board element to carry data-board-audience = dm/player, data-source-revision, data-source-round, data-source-state-digest === the service's expected stateDigest, and (D525) the requested glyph mode; a pageerror would have rejected immediately, so one of those attribute equalities never holds. HYPOTHESIS (unproven): a state-digest disagreement between the service and the app now that the V1 decoder validates-and-discards the legacy fog key (B2) — one side may digest the raw fixture bytes or the pre-discard state while the other digests the decoded state — or the derived fog changing what the blind capture app renders for the hard fixture. The same spec passed in the OFFERS landing gate on 1cacd8f0 (187/187, D617.50); a control run on main (port 4480) is in progress. Either way it is VIS-FIELD's to resolve before landing: LAND-02's item (2) is now "diagnose the predicate attribute that fails (instrument in the browser context on a copy), name the producing code on both sides, and fix the VIS-FIELD side or STOP with the mechanism" — a production change there needs a manifest ruling from me. vtt-encounter.spec.ts:237 remains the hand re-pin of the reference scene's derived fog at (4,3).

## D635.38.1 — SUPERVISOR (2026-09-17 04:21): control run confirms the board-snapshot browser spec passes on main 9f148666 (2 passed, 3.4 min, port 4480) and fails alone on the VIS-FIELD tree — VIS-FIELD-caused; LAND-02 diagnoses per D635.38.

exec
/bin/bash -lc "git status --short && git rev-parse --short HEAD && rg -n \"encounter-art-fog|data-cell|concealed|darkvision|senses|Training Brute|reference\" tests/browser/vtt-encounter.spec.ts tests/browser/ai-dm-board-snapshot.spec.ts src/vtt/reference-encounter.ts && sed -n '1,330p' tests/browser/vtt-encounter.spec.ts && sed -n '1,360p' tests/browser/ai-dm-board-snapshot.spec.ts && sed -n '940,1050p' tools/ai-dm-board-snapshot.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
ebfcacaa
src/vtt/reference-encounter.ts:10:import { referencePartySpellSlots } from '../combat/spells/resources';
src/vtt/reference-encounter.ts:57:      senses: [{ kind: 'normal_sight' }],
src/vtt/reference-encounter.ts:81:export function referenceEncounterSetup(): {
src/vtt/reference-encounter.ts:98:    spellSlots: referencePartySpellSlots('Cleric'),
src/vtt/reference-encounter.ts:104:    spellSlots: referencePartySpellSlots('Wizard'),
src/vtt/reference-encounter.ts:106:  const monster = profile('training-brute', 'Training Brute', 'monster', {
src/vtt/reference-encounter.ts:124:      lightRegions: [{ id: 'reference-hiding-shadow', cells: [{ column: 4, row: 3 }], level: 'darkness' }],
src/vtt/reference-encounter.ts:130:    dmNotes: ['Training Brute retreats after the three reference PCs act.'],
src/vtt/reference-encounter.ts:136:  if (found === undefined) throw new Error(`Unknown reference combatant ${id}.`);
src/vtt/reference-encounter.ts:247:export function referenceTurnLegalActions(
src/vtt/reference-encounter.ts:273:export function referenceReactionLegalActions(
tests/browser/vtt-encounter.spec.ts:173:test('M38-PLAYER-NO-DM-CONTROLS and two local windows complete the resumable reference flow', async ({
tests/browser/vtt-encounter.spec.ts:177:  await page.goto('/vtt?encounter=reference&view=player&session=increment-6-browser');
tests/browser/vtt-encounter.spec.ts:234:  await expect(tray).toContainText('Training Brute moved from 3,3 to 4,3');
tests/browser/vtt-encounter.spec.ts:235:  await expect(dm.locator('.dm-pending-request')).toContainText('turn: Training Brute');
tests/browser/vtt-encounter.spec.ts:237:  await expect(dm.locator('[data-cell="4,3"] .encounter-art-fog')).toHaveCount(0);
tests/browser/vtt-encounter.spec.ts:238:  await expect(dm.locator('[data-cell="4,3"] .encounter-token[data-kind="monster"]')).toBeVisible();
tests/browser/vtt-encounter.spec.ts:242:  await expect(page.locator('[data-cell="4,3"] .encounter-art-floor')).toBeVisible();
import type { Page } from '@playwright/test';
import { expect, test } from './fixtures/parallel-test';

async function confirmChoice(page: Page, label: string): Promise<void> {
  await page.getByRole('button', { name: label, exact: true }).click();
  await page.getByRole('button', { name: 'Confirm choice' }).click();
}

async function expectActive(page: Page, name: string): Promise<void> {
  await expect(page.getByRole('heading', { name: `Active PC: ${name}` })).toBeVisible();
  await expect(page.locator('.encounter-token[data-active="true"]')).toHaveText(name);
}

async function resetHome(page: Page): Promise<void> {
  await page.goto('/');
  await expect(page.locator('#status')).toHaveAttribute('data-ready', 'true', {
    timeout: 65_000,
  });
  await page.evaluate(() => window.staticApp.reset());
  await page.reload();
  await expect(page.locator('#status')).toHaveAttribute('data-ready', 'true', {
    timeout: 65_000,
  });
}

test('DM loads the bundled D365 dungeon and RPC-authored party into room 1', async ({
  page,
}) => {
  test.setTimeout(120_000);
  await resetHome(page);
  await page.goto('/vtt?encounter=d365');
  await expect(page.getByRole('heading', { name: 'D365 sample dungeon' })).toBeVisible({
    timeout: 65_000,
  });
  await page.getByRole('button', { name: 'Load bundled dungeon and party' }).click();

  await expect(page.getByRole('heading', { name: 'DM controls' })).toBeVisible({
    timeout: 65_000,
  });
  await expect(page.locator('.adventuring-day-status')).toHaveText(
    'Adventuring day — room 1 of 4 · 2024 rules',
  );
  for (const name of ['Mirel Ash', 'Orin Reed', 'Brann Vale', 'Sera Dawn']) {
    await expect(
      page.locator('.encounter-token[data-kind="player_character"]', { hasText: name }),
    ).toBeVisible();
  }
  await expect(
    page.locator('.encounter-token[data-kind="monster"]', { hasText: 'Goblin Warrior' }),
  ).toHaveCount(2);
  await expect(
    page.locator('.encounter-token[data-kind="monster"]', { hasText: 'Wolf' }),
  ).toHaveCount(2);
  const board = page.locator('.encounter-board');
  await expect(board).toBeVisible();
  await expect(board.locator('.encounter-nameplate')).toHaveCount(0);
  await expect(board.locator('.encounter-creature-badge')).toHaveCount(9);
  await expect(board.locator('.encounter-roster-entry')).toHaveCount(9);
  const rosterContract = await board.evaluate((element) => {
    const badges = Array.from(element.querySelectorAll<HTMLElement>('.encounter-creature-badge'));
    const roster = Array.from(element.querySelectorAll<HTMLElement>('.encounter-roster-entry'));
    const names = roster.map((row) => row.dataset.fullName ?? '');
    return {
      badgeNumbers: badges.map((badge) => badge.dataset.badgeNumber ?? ''),
      badgeColors: badges.map((badge) => badge.dataset.badgeColor ?? ''),
      badgeSources: badges.map((badge) => badge instanceof HTMLImageElement ? badge.src : ''),
      rosterNumbers: roster.map((row) => row.dataset.badgeNumber ?? ''),
      rosterCombatantIds: roster.map((row) => row.dataset.combatantId ?? ''),
      rosterNames: names,
      rosterOrders: roster.map((row) => row.dataset.rosterOrder ?? ''),
      rosterCoordinates: roster.map((row) => row.querySelector<HTMLElement>('.encounter-roster-coordinate')?.dataset.coordinate ?? ''),
      rosterBadgeSources: roster.map((row) => row.querySelector<HTMLImageElement>('.encounter-roster-badge')?.src ?? ''),
      rosterNameBitmapWidths: roster.map((row) => {
        const image = row.querySelector<HTMLImageElement>('.encounter-roster-name');
        return image === null ? null : [image.width, image.naturalWidth];
      }),
      bustBitmapBoxes: Array.from(element.querySelectorAll<HTMLImageElement>('.encounter-token-sprite'))
        .map((image) => [image.getBoundingClientRect().width, image.naturalWidth]),
    };
  });
  expect(rosterContract.badgeNumbers).toEqual(['1', '2', '3', '4', '5', '6', '7', '8', '9']);
  expect(rosterContract.badgeNumbers).toEqual(rosterContract.rosterNumbers);
  expect(rosterContract.badgeSources).toEqual(rosterContract.rosterBadgeSources);
  expect(rosterContract.rosterOrders).toEqual(rosterContract.rosterNumbers);
  expect(new Set(rosterContract.badgeColors).size).toBe(9);
  expect([...rosterContract.rosterNames].sort()).toEqual([
    'Mirel Ash', 'Orin Reed', 'Brann Vale', 'Sera Dawn', 'Tamsin Quill',
    'Goblin Warrior', 'Goblin Warrior', 'Wolf', 'Wolf',
  ].sort());
  const initiativeEntries = page.locator('.dm-initiative > ol > li');
  await expect(initiativeEntries).toHaveCount(9);
  const initiativeCombatantIds = await initiativeEntries.evaluateAll((items) =>
    items.map((item) => item instanceof HTMLElement ? item.dataset.combatantId ?? '' : ''),
  );
  expect(rosterContract.rosterCombatantIds).toEqual(initiativeCombatantIds);
  expect(rosterContract.rosterCoordinates.every((coordinate) => /^\([0-9]+,[0-9]+\)$/u.test(coordinate))).toBe(true);
  for (const dimensions of rosterContract.rosterNameBitmapWidths) {
    expect(dimensions).not.toBeNull();
    expect(dimensions?.[0]).toBe(dimensions?.[1]);
  }
  expect(rosterContract.bustBitmapBoxes).toHaveLength(9);
  expect(rosterContract.bustBitmapBoxes.every(([rendered, bitmap]) => rendered === 128 && bitmap === 128)).toBe(true);
});

test('DM composes stored builder characters and each PC defaults to human control', async ({
  page,
}) => {
  test.setTimeout(120_000);
  await resetHome(page);
  const names = await page.evaluate(async () => {
    const classes = await window.appRpc.call<
      Record<string, never>,
      readonly { readonly content_key: string; readonly name: string }[]
    >('queries.characters.guidedClassOptions', {});
    const fighter = classes.find((candidate) => candidate.name === 'Fighter');
    if (fighter === undefined) throw new Error('Bundled Fighter is missing.');
    const origins = await window.appRpc.call<
      { readonly kind: 'species' },
      readonly { readonly content_key: string; readonly name: string }[]
    >('queries.characters.originOptions', { kind: 'species' });
    const human = origins.find((candidate) => candidate.name === 'Human');
    if (human === undefined) throw new Error('Bundled Human is missing.');
    const createdNames = ['Stored Rowan', 'Stored Sable', 'Stored Tamsin'];
    for (const name of createdNames) {
      const character = await window.appRpc.call<
        { readonly name: string; readonly class_content_key: string },
        { readonly id: number }
      >('queries.characters.createGuided', {
        name,
        class_content_key: fighter.content_key,
      });
      await window.appRpc.call('queries.characters.applyOrigin', {
        character_id: character.id,
        kind: 'species',
        content_key: human.content_key,
      });
    }
    return createdNames;
  });

  await page.goto('/vtt?compose=stored');
  await expect(
    page.getByRole('heading', { name: 'Build an encounter from stored characters' }),
  ).toBeVisible({ timeout: 65_000 });
  for (const name of names) {
    await page.getByRole('checkbox', { name: new RegExp(`^${name} —`, 'u') }).check();
  }
  await expect(page.getByRole('button', { name: 'Start DM encounter' })).toBeEnabled();
  await page.getByRole('button', { name: 'Start DM encounter' }).click();

  await expect(page.getByRole('heading', { name: 'DM controls' })).toBeVisible();
  await expect(page.locator('.player-encounter')).toHaveCount(0);
  for (const name of names) {
    await expect(page.getByLabel(`${name} controller`)).toHaveValue('human');
    await expect(page.getByLabel(`${name} controller`)).toBeEnabled();
    await expect(
      page.locator('.encounter-token[data-kind="player_character"]', { hasText: name }),
    ).toBeVisible();
  }
  await expect(page.locator('.adventuring-day-status')).toHaveText(
    'Adventuring day — room 1 of 4 · 2024 rules',
  );
  await expect(page.getByRole('heading', { name: 'Short Rest before next room' })).toBeVisible();
  await expect(page.getByRole('button', { name: 'Take Short Rest and enter next room' })).toBeVisible();
  await page.getByRole('button', { name: 'End room and enter next room' }).click();
  await expect(page.locator('.adventuring-day-status')).toHaveAttribute('data-room', '2');
  for (const name of names) {
    await expect(page.getByLabel(`${name} controller`)).toHaveValue('human');
    await expect(page.getByLabel(`${name} controller`)).toBeEnabled();
  }
});

test('M38-PLAYER-NO-DM-CONTROLS and two local windows complete the resumable reference flow', async ({
  page,
}) => {
  test.setTimeout(120_000);
  await page.goto('/vtt?encounter=reference&view=player&session=increment-6-browser');
  await expect(page.getByRole('heading', { name: 'Reference encounter' })).toBeVisible({
    timeout: 60_000,
  });

  const popup = page.waitForEvent('popup');
  await page.getByRole('button', { name: 'Open local DM window' }).click();
  const dm = await popup;
  await expect(dm.getByRole('heading', { name: 'DM controls' })).toBeVisible({
    timeout: 60_000,
  });
  await expect(page.locator('.encounter-authority-status')).toHaveAttribute(
    'data-state',
    'connected',
  );
  for (const boardPage of [dm, page]) {
    const tokens = boardPage.locator('.encounter-token');
    await expect(tokens).toHaveCount(4);
    await expect(boardPage.locator('.encounter-cell .encounter-token')).toHaveCount(0);

exec
/bin/bash -lc "wc -l /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/review-impl-land-01-astra-r1.md /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-land-02-sol.md
sed -n '1,240p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/review-impl-land-01-astra-r1.md" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
  3918 /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/review-impl-land-01-astra-r1.md
  7740 /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-land-02-sol.md
 11658 total

OpenAI Codex v0.154.0
--------
workdir: /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
model: gpt-6-astra
provider: openai
approval: never
sandbox: read-only
reasoning effort: medium
reasoning summaries: none
session id: 01a0a642-35a1-7082-aba2-eda56ea291c1
--------
user
RULES (binding): You are the REVIEWER (gpt-6-astra, MEDIUM per D627, resumed — the session that reviewed every VIS-FIELD batch). Worktree /home/vagrant/PhpstormProjects/dnd-wt-seam-verify — a DETACHED clean checkout at ebfcacaa (= 10cc9f5e (B6 ACCEPTED, the complete frozen plan) + LAND-01; node_modules symlinked). Sandbox read-only; in-memory when /tmp is read-only; do NOT edit files, no git writes, no claude / `claude -p` / other agents / consensus skills; skills under ~/.codex do NOT apply; never port 4173; no full gate, full vitest, Playwright, npm run build. Do not read sibling worktrees or /home/vagrant/dnd-slim-runs/. MAIN repo /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static's .claude/decisions.md and .claude/consensus/vis-field/** readable: read D635.35–D635.41, impl-landing-investigation-sol.md and impl-land-01-sol.md first. Model calls WILL fail. Report exact commands and numbers; end with the named marker.

TASK: Review `git diff 10cc9f5e ebfcacaa` (6 files, 325+/22−: src/vtt/engine-query-port.ts, src/vtt/room-generator.ts, tests/unit/vtt/{engine-query-port,room-generator-los-cover,arena-basis-brutal-b,tactical-evaluator-r02}.test.ts) = LAND-01, the landing-reconciliation batch for the two VIS-FIELD defects the full-suite landing gate exposed (D635.36): (c1) the query port offered attacks/saves whose corner trace blocks sight (tier-only check) while execution refuses them — an offers-boundary violation; (c2) the generator's open-ray predicate ignored blocksSight; plus the brutal-b helper's missing activation choice, and six tactical-evaluator kill-probability pins that scored shots from origins the reducer would refuse. Supervisor verification (verify-land-01.log): tsc 0/0; OFFERS checks 0/0; my mutants (sight check disabled → both query witnesses red incl. the 214-offer agreement property; generator predicate relaxed → its witness red); 16 suites 299/299 (query port, r02, dm-tactical-intel, challenge-room, the OFFERS set, replay, detection); los-cover 112/112; exactly the 16 expected reds remain (ten brutal-b productivity pins + six D569 manifest — the frozen-family consequence awaiting the owner, D635.36); pair 38 s.

CHECK (each with commands and numbers): (1) c1: the refusal at engine-query-port.ts:~1171 uses the SAME trace inputs as execution (encounter.ts:~7183: actor, target, sourceAnchor = the proposed origin) — could the two still disagree (a different anchor, footprint, exclusion set, or a path where execution traces after a move the query did not model)? Enumerate every production site that decides attack/save targetability and confirm each is now sight-aware or is not an authorization gate (sol left :1234/:1547 tactical-scoring and :1830 cover reporting alone — agree or not). (2) The AGREEMENT PROPERTY ("offered attacks and saves in every frozen landing family pass the reducer LOS rule"): read it — is executing each offer through reduceEncounter at the resolved finalPosition a faithful model of real execution (initiative/active-combatant setup, the () => 0.5 roll, multiattack components, bonus-action slot)? 214 offers checked — recount from the three families; a property that silently skips options is a P1. (3) c2: generatedLineIsOpen = tier 'none' && !blocksSight at both call sites; the witness's geometry (14,6)→(1,6) along row line 6 between (8,5)/(8,6) — verify; sol's in-memory regeneration of 42 seeds byte-equal — reproduce for at least the ten brutal-b seeds (so the frozen families are NOT cured by the fix; relevant to the owner's question). (4) The six R02 pins: recompute at least two allocations independently (no-bless 2702281/6250000 and one Bless variant; Bless enumerated over the d4 faces; the death-save fold in initiative order per tactical-evaluator.ts:786-814) and confirm the two unchanged controls are byte-identical to 10cc9f5e. (5) The brutal-b helper: materializing the first declared legal activation choice keeps the engine-ranked option — confirm it does not mask a resolver defect and that dispel-evil-and-good's choice contract is what the helper reads. (6) No fixture changed; the remaining reds are exactly the frozen-family ones; tsc 0/0; suites you can run; pair (ceiling ≈41 s).

OUTPUT: findings LAND-Fn with severity P1/P2/P3, file:line, evidence, fix; then exactly "VERDICT: ACCEPT" or "VERDICT: REJECT (n P1, m P2)"; end with REVIEW DONE.

codex
I’ll read the landing rulings and reports first, then check the targeting agreement, generator behavior, and probability pins against the code and targeted tests.

exec
/bin/bash -lc "rg -n '"'^## D635'"\\.(35|36|37|38|39|40|41)' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md; cat /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-land-01-sol.md; cat /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/impl-landing-investigation-sol.md" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
22954:## D635.35 — SUPERVISOR (2026-09-17 02:54): VIS-FIELD LANDING GATE vitest phase RED with DETERMINISTIC reds outside every plan manifest — the frozen arena families lose their productivity guarantee under the seam rule; read-only investigation before any ruling
22957:## D635.36 — SUPERVISOR (2026-09-17 03:22): landing investigation harvested — TWO VIS-FIELD DEFECTS (category c) found, plus a legitimate seam consequence on the frozen brutal families that needs the OWNER's decision
22960:## D635.37 — SUPERVISOR (2026-09-17 03:52): VIS-FIELD landing gate complete on 10cc9f5e — tsc 0, sg 0, vitest RED (D635.36), Playwright 184 passed / 2 FAILED, OFFERS checks 0/0, D630 pair 39.3 / 39.4 s (main 32.8 s, +20%, ceiling 41 s), arena round 38 s (main 51 s); LAND-01 dispatched
22963:## D635.38 — SUPERVISOR (2026-09-17 04:07): the board-snapshot browser spec's failure REPRODUCES alone on the clean 10cc9f5e checkout — a beforeAll timeout inside the blind board-snapshot service's board-visible wait; LAND-02 diagnoses it as a candidate VIS-FIELD defect
22966:## D635.38.1 — SUPERVISOR (2026-09-17 04:21): control run confirms the board-snapshot browser spec passes on main 9f148666 (2 passed, 3.4 min, port 4480) and fails alone on the VIS-FIELD tree — VIS-FIELD-caused; LAND-02 diagnoses per D635.38.
22968:## D635.39 — SUPERVISOR (2026-09-17 04:37): LAND-01 complete on its five files (both defects fixed with witnesses and mutants; 214 frozen-family offers all execute; generator bytes 42/42 equal); one out-of-manifest hand pin moves as a consequence — manifest +1; r2 dispatched
22971:## D635.40 — SUPERVISOR (2026-09-17 04:51): LAND-01 r2 — the no-bless pin re-derived by hand (spot-checked by me); the same mechanism moves the Bless-variant pins in the same test; allowance widened to every expectation in that test that scores the three moved monsters; r3 dispatched
22974:## D635.41 — SUPERVISOR (2026-09-17 05:22): LAND-01 verified by me and COMMITTED ebfcacaa; astra medium review dispatched; LAND-02 (browser specs) dispatched in parallel
# VIS-FIELD LAND-01 — sol report (fresh 01a0ae59…, 389 k tokens; BLOCKED only on one out-of-manifest hand pin)

utal-b.test.ts b/tests/unit/vtt/arena-basis-brutal-b.test.ts
index d76fdc233b9b4def2bc1d4a141cc81f083564e5f..3613a2eb3c8585746b048672e2aea6e388c0d940
--- a/tests/unit/vtt/arena-basis-brutal-b.test.ts
+++ b/tests/unit/vtt/arena-basis-brutal-b.test.ts
@@ -15,6 +15,10 @@
   resolveEngineActorOption,
   type EngineTurnProposal,
 } from '../../../src/vtt/intent-resolver';
+import type {
+  EngineActivationChoice,
+  EngineOfferableOption,
+} from '../../../src/vtt/turn-proposal';
 import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
 import { actorOpportunityReport } from '../../../src/vtt/intel/opportunity-cost';
 import { decodeArenaFixture } from '../../../src/vtt/mcp/entrypoint';
@@ -142,6 +146,20 @@
   return actors;
 }
 
+function deterministicActivationChoice(option: EngineOfferableOption): EngineActivationChoice | undefined {
+  const slot = option.activationChoice;
+  if (slot === undefined || slot === null) return undefined;
+  switch (slot.kind) {
+    case 'command_word': return { kind: slot.kind, value: slot.values[0] };
+    case 'unicorns_blessing_spell': return { kind: slot.kind, value: slot.values[0] };
+    case 'dispel_evil_and_good_mode': return { kind: slot.kind, value: slot.values[0] };
+    case 'calm_emotions_per_target': return {
+      kind: slot.kind,
+      selections: slot.targetIds.map((targetId) => ({ targetId, mode: slot.values[0] })),
+    };
+  }
+}
+
 function dryRunMonsterPlan(state: EncounterState): ReadonlyMap<CombatantId, AuthorizedEngineTurnProposal> {
   const actorIds = [...livingMonsterIds(state)].sort((left, right) => left.localeCompare(right));
   const planningState = projectFutureMonsterTurns(state, actorIds);
@@ -160,12 +178,14 @@
       state.revision,
     ).find((option) => option.optionId !== primary.optionId);
     if (fallback === undefined) throw new Error(`Dry-run plan has no independent fallback for ${actorId}.`);
+    const activationChoice = deterministicActivationChoice(primary);
     const proposal: EngineTurnProposal = {
       actorId,
       expectedRevision: state.revision,
       primaryOptionId: primary.optionId,
       fallbackOptionId: fallback.optionId,
       reason: 'Use the engine-ranked option to maximize immediate tactical value.',
+      ...(activationChoice === undefined ? {} : { activationChoice }),
       overrideJustification: null,
     };
     const resolution = TURN_PROPOSAL_RESOLVER.resolve(planningState, proposal);
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 66fe8cfa5e7742935be2fd8a47e42d07a131f38b..62acd4131b7a3469070f9f41ce9c246e4904c1e2
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -9,24 +9,35 @@
 } from '../../../src/combat/encounter';
 import { traceCombatantLine } from '../../../src/combat/cover';
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
-import { monsterAttackCommand } from '../../../src/combat/monster-commands';
+import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
-import { armorClass, combatantId, statblockId, worldObjectId, type CombatantId } from '../../../src/combat/values';
+import {
+  armorClass,
+  combatantId,
+  feet,
+  statblockId,
+  worldObjectId,
+  type CombatantId,
+} from '../../../src/combat/values';
 import { projectDmView } from '../../../src/combat/visibility';
 import { ENCOUNTER_VIEW_CLASSIFICATION } from '../../../src/combat/visibility';
 import { projectEncounterBoard } from '../../../src/vtt/encounter-board';
 import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
+import { monsterActions, monsterBonusActions } from '../../../src/vtt/engine-query-port';
 import {
   availableEngineActorOptions,
   createPureTurnProposalResolver,
   engineActorOptionsForEnvironment,
+  resolveEngineActorOption,
 } from '../../../src/vtt/intent-resolver';
 import { engineOptionId } from '../../../src/vtt/turn-proposal';
 import { generateRoom } from '../../../src/vtt/room-generator';
 import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
 import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
+import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
 import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
 import { readFileSync } from '../../helpers/test-filesystem';
 
@@ -36,6 +47,21 @@
 const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
 const TURN_PROPOSAL_RESOLVER = createPureTurnProposalResolver(OFFER_ENVIRONMENT);
 
+const AGREEMENT_FIXTURE_PATHS = [
+  ...Array.from(
+    { length: 13 },
+    (_unused, index) => `tests/fixtures/arena-basis-hard/seed-${String(5_117_001 + index)}.json`,
+  ),
+  ...Array.from(
+    { length: 10 },
+    (_unused, index) => `tests/fixtures/arena-basis-brutal/seed-${String(6_203_001 + index)}.json`,
+  ),
+  ...Array.from(
+    { length: 10 },
+    (_unused, index) => `tests/fixtures/arena-basis-brutal-b/seed-${String(6_206_001 + index)}.json`,
+  ),
+] as const;
+
 function legacyDocument(
   state: EncounterState,
   foggedCells: unknown,
@@ -82,6 +108,49 @@
   });
 }
 
+function twoBlockerSeamState(openLowerFlank: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GOBLIN_WARRIOR, {
+    combatantId: 'combatant:query-seam-shooter',
+    tokenId: 'token:query-seam-shooter',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-seam-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 16, rows: 8 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 14, 5), placedToken(target, 1, 6)],
+      blockedCells: openLowerFlank
+        ? [{ column: 8, row: 5 }]
+        : [{ column: 8, row: 5 }, { column: 8, row: 6 }],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
+function stateAtResolvedOrigin(
+  state: EncounterState,
+  actorId: CombatantId,
+  position: GridCell,
+): EncounterState {
+  return {
+    ...state,
+    activeCombatant: actorId,
+    activeInitiativeIndex: 0,
+    tokens: state.tokens.map((token) => token.combatantId === actorId
+      ? { ...token, position }
+      : token),
+  };
+}
+
 describe('canonical engine query port', () => {
   it('ENGINE_QUERY_VISIBILITY_EQUALS_CANONICAL_DETECTOR including located outcomes', () => {
     const observerBase = monsterProfile('query-visibility-observer');
@@ -423,6 +492,110 @@
     expect(projectEncounterBoard(projectDmView(walled)).blockedCells).toEqual(walledCells);
   });
 
+  it('OFFER_IGNORES_BLOCKS_SIGHT refuses a half-cover two-blocker seam and opens one flank', () => {
+    const closed = twoBlockerSeamState(false);
+    const opened = twoBlockerSeamState(true);
+    // The four formerly clear rays are (14,6)/(15,6) -> (1,6)/(2,6). Each follows row line 6
+    // between wall cells (8,5)/(8,6); the other twelve corner rays enter one of those cells.
+    expect(traceCombatantLine(closed.state, closed.actorId, closed.targetId)).toMatchObject({
+      tier: 'half',
+      blocksSight: true,
+      firstBlockingCell: { column: 8, row: 5 },
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(closed.state, {
+      actorId: closed.actorId,
+      targetId: closed.targetId,
+      actionId: 'shortbow',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(closed.state, closed.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'attack' &&
+        slot.use.target.kind === 'combatant' && slot.use.target.combatantId === closed.targetId))).toBe(false);
+
+    // Removing (8,6) leaves the row-line-6 rays with a blocker on only one flank.
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'shortbow',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'attack' &&
+        slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+

exec
/bin/bash -lc "sha256sum tests/browser/vtt-encounter.spec.ts
nl -ba tests/unit/vtt/engine-query-port.test.ts | sed -n '1,180p;470,640p'
nl -ba src/vtt/engine-query-port.ts | sed -n '1158,1178p'
nl -ba tools/ai-dm-board-snapshot.ts | sed -n '1,45p;185,220p;970,1040p'
nl -ba src/vtt/encounter-projections.ts | sed -n '475,500p'
nl -ba src/vtt/encounter-app.ts | sed -n '2296,2315p'
rg --files tests/unit/tools | rg 'ai-dm-(board-snapshot|board-delivery|board-glyph-captures|blind-board-snapshot-check|screenshot-probe|conversation)|d569-(blind-experiment|second-family-manifest|v5)' | sort" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
cb89f2ddc5a8b7af2f564b496a88f2d568b456518ccda4623083162f9d079227  tests/browser/vtt-encounter.spec.ts
     1	import ts from 'typescript';
     2	import { describe, expect, it } from 'vitest';
     3	import {
     4	  canCombatantSee,
     5	  createEncounter,
     6	  detectCombatant,
     7	  reduceEncounter,
     8	  type EncounterState,
     9	} from '../../../src/combat/encounter';
    10	import { traceCombatantLine } from '../../../src/combat/cover';
    11	import { monsterCombatantProfile } from '../../../src/combat/combatant';
    12	import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
    13	import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
    14	import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
    15	import type { GridCell } from '../../../src/combat/grid';
    16	import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
    17	import {
    18	  armorClass,
    19	  combatantId,
    20	  feet,
    21	  statblockId,
    22	  worldObjectId,
    23	  type CombatantId,
    24	} from '../../../src/combat/values';
    25	import { projectDmView } from '../../../src/combat/visibility';
    26	import { ENCOUNTER_VIEW_CLASSIFICATION } from '../../../src/combat/visibility';
    27	import { projectEncounterBoard } from '../../../src/vtt/encounter-board';
    28	import { decodeEncounterStateV1 } from '../../../src/vtt/encounter-state-codec';
    29	import { monsterActions, monsterBonusActions } from '../../../src/vtt/engine-query-port';
    30	import {
    31	  availableEngineActorOptions,
    32	  createPureTurnProposalResolver,
    33	  engineActorOptionsForEnvironment,
    34	  resolveEngineActorOption,
    35	} from '../../../src/vtt/intent-resolver';
    36	import { engineOptionId } from '../../../src/vtt/turn-proposal';
    37	import { generateRoom } from '../../../src/vtt/room-generator';
    38	import { freshMonsterPlanningState } from '../../../src/vtt/monster-planning-state';
    39	import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
    40	import { decodeArenaFixtureText } from '../../../src/vtt/mcp/entrypoint';
    41	import { monsterProfile, placedToken, playerProfile } from '../combat/fixtures';
    42	import { readFileSync } from '../../helpers/test-filesystem';
    43	
    44	const SEED = 3_943_001;
    45	const ACTOR_ID = combatantId('combatant:generated-3943001-monster-2');
    46	const TARGET_ID = combatantId('combatant:fighter');
    47	const OFFER_ENVIRONMENT = buildOfferEnvironment({ kind: 'configuration', mode: 'legacy_standard' });
    48	const TURN_PROPOSAL_RESOLVER = createPureTurnProposalResolver(OFFER_ENVIRONMENT);
    49	
    50	const AGREEMENT_FIXTURE_PATHS = [
    51	  ...Array.from(
    52	    { length: 13 },
    53	    (_unused, index) => `tests/fixtures/arena-basis-hard/seed-${String(5_117_001 + index)}.json`,
    54	  ),
    55	  ...Array.from(
    56	    { length: 10 },
    57	    (_unused, index) => `tests/fixtures/arena-basis-brutal/seed-${String(6_203_001 + index)}.json`,
    58	  ),
    59	  ...Array.from(
    60	    { length: 10 },
    61	    (_unused, index) => `tests/fixtures/arena-basis-brutal-b/seed-${String(6_206_001 + index)}.json`,
    62	  ),
    63	] as const;
    64	
    65	function legacyDocument(
    66	  state: EncounterState,
    67	  foggedCells: unknown,
    68	): Record<string, unknown> {
    69	  return {
    70	    ...(JSON.parse(JSON.stringify(state)) as Record<string, unknown>),
    71	    foggedCells,
    72	  };
    73	}
    74	
    75	function minimalState(key: string, bounds: EncounterState['bounds']): EncounterState {
    76	  const actor = monsterProfile(key);
    77	  return createEncounter({
    78	    bounds,
    79	    combatants: [actor],
    80	    tokens: [placedToken(actor, 0)],
    81	  });
    82	}
    83	
    84	function placedState(
    85	  seed: number,
    86	  positions: ReadonlyMap<CombatantId, GridCell>,
    87	  difficultCells: readonly GridCell[] = [],
    88	  rows?: number,
    89	): EncounterState {
    90	  const state = generateRoom(seed).encounter.state;
    91	  return freshMonsterPlanningState({
    92	    ...state,
    93	    bounds: rows === undefined ? state.bounds : { ...state.bounds, rows },
    94	    blockedCells: [],
    95	    worldObjects: [],
    96	    persistentAreas: [],
    97	    environment: {
    98	      ...state.environment,
    99	      difficultTerrainRegions: difficultCells.length === 0
   100	        ? []
   101	        : [{ id: 'engine-query-difficult', cells: difficultCells }],
   102	      movementRegions: [],
   103	    },
   104	    tokens: state.tokens.flatMap((token) => {
   105	      const position = positions.get(token.combatantId);
   106	      return position === undefined ? [] : [{ ...token, position }];
   107	    }),
   108	  });
   109	}
   110	
   111	function twoBlockerSeamState(openLowerFlank: boolean): {
   112	  readonly state: EncounterState;
   113	  readonly actorId: CombatantId;
   114	  readonly targetId: CombatantId;
   115	} {
   116	  const actorBase = monsterCombatantProfile(GOBLIN_WARRIOR, {
   117	    combatantId: 'combatant:query-seam-shooter',
   118	    tokenId: 'token:query-seam-shooter',
   119	  });
   120	  const actor = {
   121	    ...actorBase,
   122	    rules: { ...actorBase.rules, speed: feet(0) },
   123	  };
   124	  const target = playerProfile('query-seam-target');
   125	  return {
   126	    state: freshMonsterPlanningState(createEncounter({
   127	      bounds: { columns: 16, rows: 8 },
   128	      combatants: [actor, target],
   129	      tokens: [placedToken(actor, 14, 5), placedToken(target, 1, 6)],
   130	      blockedCells: openLowerFlank
   131	        ? [{ column: 8, row: 5 }]
   132	        : [{ column: 8, row: 5 }, { column: 8, row: 6 }],
   133	    })),
   134	    actorId: actor.id,
   135	    targetId: target.id,
   136	  };
   137	}
   138	
   139	function stateAtResolvedOrigin(
   140	  state: EncounterState,
   141	  actorId: CombatantId,
   142	  position: GridCell,
   143	): EncounterState {
   144	  return {
   145	    ...state,
   146	    activeCombatant: actorId,
   147	    activeInitiativeIndex: 0,
   148	    tokens: state.tokens.map((token) => token.combatantId === actorId
   149	      ? { ...token, position }
   150	      : token),
   151	  };
   152	}
   153	
   154	describe('canonical engine query port', () => {
   155	  it('ENGINE_QUERY_VISIBILITY_EQUALS_CANONICAL_DETECTOR including located outcomes', () => {
   156	    const observerBase = monsterProfile('query-visibility-observer');
   157	    const observer = {
   158	      ...observerBase,
   159	      rules: {
   160	        ...observerBase.rules,
   161	        senses: [
   162	          { kind: 'normal_sight' as const },
   163	          { kind: 'tremorsense' as const, rangeFeet: 5 },
   164	        ],
   165	      },
   166	    };
   167	    const located = playerProfile('query-visibility-located');
   168	    const seen = playerProfile('query-visibility-seen');
   169	    const unseen = playerProfile('query-visibility-unseen');
   170	    const hidden = playerProfile('query-visibility-hidden');
   171	    const created = createEncounter({
   172	      bounds: { columns: 5, rows: 1 },
   173	      combatants: [observer, located, seen, unseen, hidden],
   174	      tokens: [
   175	        placedToken(observer, 0),
   176	        placedToken(located, 1),
   177	        placedToken(seen, 2),
   178	        placedToken(unseen, 3),
   179	        placedToken(hidden, 4),
   180	      ],
   470	    const attack = OFFER_ENVIRONMENT.queries.actions(partial, actor.id)
   471	      .find((action) => action.kind === 'attack' && action.id === 'shortbow');
   472	    if (attack === undefined || attack.kind !== 'attack') throw new Error('Parity fixture omitted Shortbow.');
   473	    expect(() => reduceEncounter(partial, monsterAttackCommand(attack, actor.id, target.id), () => 0.5)).not.toThrow();
   474	
   475	    const walledCells = [
   476	      ...[0, 1, 2, 3].map((row) => ({ column: 2, row })),
   477	      // Keep the actor at the tested origin so an option cannot move to a boundary-grazing line.
   478	      { column: 1, row: 0 }, { column: 0, row: 1 }, { column: 1, row: 1 },
   479	    ];
   480	    const walled = setup([], walledCells);
   481	    expect(traceCombatantLine(walled, actor.id, target.id)).toMatchObject({ tier: 'total', blocksSight: true });
   482	    expect(canCombatantSee(walled, actor.id, target.id)).toBe(false);
   483	    expect(OFFER_ENVIRONMENT.queries.cover(walled, actor.id, target.id)).toMatchObject({ tier: 'total' });
   484	    expect(OFFER_ENVIRONMENT.queries.visibility(walled, actor.id, target.id)).toMatchObject({
   485	      visible: false, reason: 'blocked',
   486	    });
   487	    expect(availableEngineActorOptions(walled, actor.id, OFFER_ENVIRONMENT).some((option) =>
   488	      option.actionSlots.some((slot) =>
   489	      slot.use.kind === 'attack' && slot.use.target.kind === 'combatant' && slot.use.target.combatantId === target.id))).toBe(false);
   490	    expect(() => reduceEncounter(walled, monsterAttackCommand(attack, actor.id, target.id), () => 0.5))
   491	      .toThrow('Total Cover or is outside line of sight');
   492	    expect(projectEncounterBoard(projectDmView(walled)).blockedCells).toEqual(walledCells);
   493	  });
   494	
   495	  it('OFFER_IGNORES_BLOCKS_SIGHT refuses a half-cover two-blocker seam and opens one flank', () => {
   496	    const closed = twoBlockerSeamState(false);
   497	    const opened = twoBlockerSeamState(true);
   498	    // The four formerly clear rays are (14,6)/(15,6) -> (1,6)/(2,6). Each follows row line 6
   499	    // between wall cells (8,5)/(8,6); the other twelve corner rays enter one of those cells.
   500	    expect(traceCombatantLine(closed.state, closed.actorId, closed.targetId)).toMatchObject({
   501	      tier: 'half',
   502	      blocksSight: true,
   503	      firstBlockingCell: { column: 8, row: 5 },
   504	    });
   505	    expect(OFFER_ENVIRONMENT.queries.reach(closed.state, {
   506	      actorId: closed.actorId,
   507	      targetId: closed.targetId,
   508	      actionId: 'shortbow',
   509	    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
   510	    expect(availableEngineActorOptions(closed.state, closed.actorId, OFFER_ENVIRONMENT).some((option) =>
   511	      option.actionSlots.some((slot) => slot.use.kind === 'attack' &&
   512	        slot.use.target.kind === 'combatant' && slot.use.target.combatantId === closed.targetId))).toBe(false);
   513	
   514	    // Removing (8,6) leaves the row-line-6 rays with a blocker on only one flank.
   515	    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
   516	      tier: 'none',
   517	      blocksSight: false,
   518	    });
   519	    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
   520	      actorId: opened.actorId,
   521	      targetId: opened.targetId,
   522	      actionId: 'shortbow',
   523	    })).toMatchObject({ legal: true });
   524	    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
   525	      option.actionSlots.some((slot) => slot.use.kind === 'attack' &&
   526	        slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   527	  });
   528	
   529	  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
   530	    const losFailures: string[] = [];
   531	    let checkedOffers = 0;
   532	    for (const path of AGREEMENT_FIXTURE_PATHS) {
   533	      const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
   534	      const monsters = state.combatants.filter((combatant) =>
   535	        combatant.profile.kind === 'monster' && combatant.life === 'living');
   536	      for (const monster of monsters) {
   537	        for (const option of availableEngineActorOptions(
   538	          state,
   539	          monster.profile.id,
   540	          OFFER_ENVIRONMENT,
   541	        )) {
   542	          const resolution = resolveEngineActorOption(state, option, OFFER_ENVIRONMENT);
   543	          if (!resolution.valid) throw new Error(`${path}: offered option ${option.optionId} did not resolve.`);
   544	          const uses = resolution.mechanics.actionSlots.filter((use) =>
   545	            use.kind === 'attack' || use.kind === 'saving_throw');
   546	          if (uses.length === 0) continue;
   547	          checkedOffers += 1;
   548	          for (const use of uses) {
   549	            const targetId = use.targetIds[0];
   550	            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
   551	            const executionState = stateAtResolvedOrigin(
   552	              state,
   553	              monster.profile.id,
   554	              resolution.mechanics.finalPosition,
   555	            );
   556	            const sources = [
   557	              ...monsterActions(executionState, monster.profile.id),
   558	              ...monsterBonusActions(executionState, monster.profile.id),
   559	            ];
   560	            const action = sources.find(
   561	              (candidate): candidate is MonsterAttackAction | MonsterSavingThrowAction =>
   562	                (candidate.kind === 'attack' || candidate.kind === 'saving_throw') &&
   563	                candidate.id === use.actionId && candidate.kind === use.kind,
   564	            );
   565	            if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
   566	            try {
   567	              if (action.kind === 'attack') {
   568	                reduceEncounter(
   569	                  executionState,
   570	                  monsterAttackCommand(action, monster.profile.id, targetId),
   571	                  () => 0.5,
   572	                );
   573	              } else {
   574	                reduceEncounter(
   575	                  executionState,
   576	                  monsterSavingThrowCommand(
   577	                    action,
   578	                    monster.profile.id,
   579	                    targetId,
   580	                    use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
   581	                  ),
   582	                  () => 0.5,
   583	                );
   584	              }
   585	            } catch (error) {
   586	              const message = error instanceof Error ? error.message : String(error);
   587	              if (!message.includes('Total Cover or is outside line of sight')) throw error;
   588	              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
   589	            }
   590	          }
   591	        }
   592	      }
   593	    }
   594	
   595	    expect(checkedOffers).toBe(214);
   596	    expect(losFailures).toEqual([]);
   597	  }, 30_000);
   598	
   599	  it('returns independently hand-computed melee reach and thrown normal range', () => {
   600	    const calls = [
   601	      { case: 'melee_reach', actionId: 'grab', actor: { column: 0, row: 0 }, target: { column: 2, row: 0 } },
   602	      { case: 'thrown_range', actionId: 'light-hammer', actor: { column: 0, row: 0 }, target: { column: 4, row: 0 } },
   603	    ] as const;
   604	    for (const call of calls) {
   605	      const state = placedState(SEED, new Map<CombatantId, GridCell>([
   606	        [ACTOR_ID, call.actor],
   607	        [TARGET_ID, call.target],
   608	      ]));
   609	      const canonical = OFFER_ENVIRONMENT.queries.reach(state, {
   610	        actorId: ACTOR_ID,
   611	        targetId: TARGET_ID,
   612	        actionId: call.actionId,
   613	      });
   614	      if (call.case === 'melee_reach') {
   615	        // Two grid intervals are 10 feet, exactly the Bugbear Warrior's Grab reach.
   616	        expect(canonical).toEqual({
   617	          legal: true,
   618	          distanceFeet: 10,
   619	          rangeFeet: 10,
   620	          rangeBand: 'melee',
   621	          normalRangeFeet: 10,
   622	          longRangeFeet: null,
   623	        });
   624	      } else {
   625	        // Four grid intervals are 20 feet, exactly Light Hammer's normal thrown range.
   626	        expect(canonical).toEqual({
   627	          legal: true,
   628	          distanceFeet: 20,
   629	          rangeFeet: 20,
   630	          rangeBand: 'normal',
   631	          normalRangeFeet: 20,
   632	          longRangeFeet: 60,
   633	        });
   634	      }
   635	    }
   636	  });
   637	
   638	  it('keeps normal and long range distinct in the authoritative reach query', () => {
   639	    const state = placedState(SEED, new Map<CombatantId, GridCell>([
   640	      [ACTOR_ID, { column: 0, row: 0 }],
  1158	    origin === undefined ||
  1159	    targetPosition === undefined ||
  1160	    rangeFeet === null
  1161	  ) {
  1162	    return { legal: false, codes };
  1163	  }
  1164	  const actorSpace = queryCombatantSpaceAt(state, request.actorId, origin);
  1165	  const targetSpace = queryCombatantSpace(state, request.targetId);
  1166	  const distanceFeet = minimumSpaceDistance(actorSpace, targetSpace);
  1167	  if (action.kind === 'attack' || action.kind === 'saving_throw') {
  1168	    const trace = traceCombatantLine(state, request.actorId, request.targetId, {
  1169	      sourceAnchor: origin,
  1170	    });
  1171	    if (trace.blocksSight) {
  1172	      return { legal: false, codes: ['target_outside_line_of_sight'] };
  1173	    }
  1174	    if (trace.tier === 'total') return { legal: false, codes: ['target_has_total_cover'] };
  1175	  }
  1176	  if (action.kind === 'attack') {
  1177	    const verdict = tacticalRangeVerdictAtDistance(distanceFeet, monsterAttackRange(action));
  1178	    if (verdict.status === 'unresolved') {
     1	import { spawn } from 'node:child_process';
     2	import { createHash } from 'node:crypto';
     3	import {
     4	  mkdir,
     5	  mkdtemp,
     6	  readFile,
     7	  realpath,
     8	  rename,
     9	  rm,
    10	  stat,
    11	  writeFile,
    12	} from 'node:fs/promises';
    13	import { arch, platform, release, tmpdir } from 'node:os';
    14	import { basename, isAbsolute, join, relative, resolve, sep } from 'node:path';
    15	import { fileURLToPath } from 'node:url';
    16	import { chromium, type Browser, type BrowserContext, type Page } from '@playwright/test';
    17	import { preview, type PreviewServer } from 'vite';
    18	import { canonicalJson } from '../src/commands/canonical-json';
    19	import type { ControllerIdentity } from '../src/combat/controllers';
    20	import type { PersistedCoordinatorState } from '../src/combat/coordinator';
    21	import type { EncounterState } from '../src/combat/encounter';
    22	import { projectDmView } from '../src/combat/visibility';
    23	import { mulberry32 } from '../src/combat/random';
    24	import { isBoardGlyphMode, type BoardGlyphMode } from '../src/assets/board-glyphs';
    25	import {
    26	  encounterBranchId,
    27	  encounterSessionId,
    28	  type EncounterSessionId,
    29	} from '../src/combat/values';
    30	import {
    31	  EncounterSessionJournal,
    32	  MemoryBrowserSessionStore,
    33	  MemoryMirrorSink,
    34	  exportSavedSession,
    35	} from '../src/vtt/session-persistence';
    36	import { projectEncounterBoard } from '../src/vtt/encounter-board';
    37	import { serializeAccessibleBoard } from '../src/vtt/accessible-board';
    38	
    39	const repositoryRoot = resolve(fileURLToPath(new URL('../', import.meta.url)));
    40	const PNG_SIGNATURE = Buffer.from([137, 80, 78, 71, 13, 10, 26, 10]);
    41	export const MAX_BOARD_PNG_BYTES = 1_000_000;
    42	const VIEWPORT = Object.freeze({ width: 1_280, height: 1_280 });
    43	const SNAPSHOT_CANARY = 'board-snapshot-element-crop-canary';
    44	const SHA256_PATTERN = /^[a-f0-9]{64}$/u;
    45	
   185	export interface BoardSnapshotServiceOptions {
   186	  readonly outputDirectory: string;
   187	  readonly forbiddenBoardStrings?: readonly string[];
   188	  /** D525: render every capture under this glyph mode; a board that says otherwise is never captured. */
   189	  readonly boardGlyphs?: BoardGlyphMode;
   190	  /** D561: CSS raster scale for the diagnostic capture; native art remains unchanged. */
   191	  readonly captureTilePx?: CaptureTilePx;
   192	  /** D569: advice is deliberately implicit so old capture URLs and artifacts are byte-identical. */
   193	  readonly informationMode?: BoardSnapshotInformationMode;
   194	  readonly primerVersion?: string;
   195	}
   196	
   197	function sha256Bytes(value: Uint8Array): string {
   198	  return createHash('sha256').update(value).digest('hex');
   199	}
   200	
   201	export function boardStateDigest(state: EncounterState): string {
   202	  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
   203	}
   204	
   205	function validateSource(state: EncounterState, source: BoardImageSource): void {
   206	  if (!Number.isSafeInteger(source.room) || source.room < 1) {
   207	    throw new RangeError('Board image room must be a positive safe integer.');
   208	  }
   209	  if (source.round !== state.round) {
   210	    throw new Error(`Board image round ${String(source.round)} does not match state round ${String(state.round)}.`);
   211	  }
   212	  if (source.revision !== state.revision) {
   213	    throw new Error(`Board image revision ${String(source.revision)} does not match state revision ${String(state.revision)}.`);
   214	  }
   215	  if (!SHA256_PATTERN.test(source.stateDigest)) {
   216	    throw new TypeError('Board image state digest must be a lowercase SHA-256 digest.');
   217	  }
   218	  const digest = boardStateDigest(state);
   219	  if (source.stateDigest !== digest) {
   220	    throw new Error(`Board image state digest ${source.stateDigest} does not match canonical state ${digest}.`);
   970	    return this.#page.locator(
   971	      this.#informationMode === 'blind_state'
   972	        ? `[data-blind-snapshot-capture=true][data-blind-snapshot-role=${role}]`
   973	        : '.encounter-board',
   974	    );
   975	  }
   976	
   977	  async #addCanary(captureOrdinal: number): Promise<void> {
   978	    await this.#page.evaluate(({ ordinal, canary, markerHeightCssPx }) => {
   979	      const marker = document.createElement('p');
   980	      marker.id = 'board-snapshot-outside-canary';
   981	      marker.textContent = `${canary}:${String(ordinal)}`;
   982	      marker.style.height = `${String(markerHeightCssPx)}px`;
   983	      marker.style.margin = '0';
   984	      document.body.append(marker);
   985	    }, {
   986	      ordinal: captureOrdinal,
   987	      canary: SNAPSHOT_CANARY,
   988	      markerHeightCssPx: this.#captureGeometry.markerHeightCssPx,
   989	    });
   990	  }
   991	
   992	  async #waitForBoardVisible(
   993	    board: ReturnType<Page['locator']>,
   994	    source: BoardImageSource,
   995	    role: BoardSnapshotImageRole,
   996	  ): Promise<void> {
   997	    await new Promise<void>((resolveBoard, reject) => {
   998	      const onPageError = (error: Error): void => {
   999	        this.#page.off('pageerror', onPageError);
  1000	        reject(new Error(`Snapshot page failed before board render: ${error.message}`, { cause: error }));
  1001	      };
  1002	      this.#page.on('pageerror', onPageError);
  1003	      void this.#page.waitForFunction((expected) => {
  1004	        const selector = expected.informationMode === 'blind_state'
  1005	          ? `[data-blind-snapshot-capture=true][data-blind-snapshot-role=${expected.role}]`
  1006	          : '.encounter-board';
  1007	        const candidate = document.querySelector<HTMLElement>(selector);
  1008	        if (candidate === null) return false;
  1009	        const bounds = candidate.getBoundingClientRect();
  1010	        const style = getComputedStyle(candidate);
  1011	        return bounds.width > 0 && bounds.height > 0 &&
  1012	          style.visibility !== 'hidden' && style.display !== 'none' &&
  1013	          candidate.dataset.boardAudience === (expected.role === 'player_board' ? 'player' : 'dm') &&
  1014	          candidate.dataset.sourceRevision === String(expected.revision) &&
  1015	          candidate.dataset.sourceRound === String(expected.round) &&
  1016	          candidate.dataset.sourceStateDigest === expected.stateDigest &&
  1017	          // D525: a board rendered under another glyph mode than requested is never captured.
  1018	          (expected.boardGlyphs === null || candidate.dataset.boardGlyphs === expected.boardGlyphs);
  1019	      }, {
  1020	        ...source,
  1021	        boardGlyphs: this.#boardGlyphs,
  1022	        informationMode: this.#informationMode,
  1023	        role,
  1024	      }, { polling: 'raf' }).then(() => {
  1025	        this.#page.off('pageerror', onPageError);
  1026	        resolveBoard();
  1027	      }, (error: unknown) => {
  1028	        this.#page.off('pageerror', onPageError);
  1029	        reject(error);
  1030	      });
  1031	    });
  1032	    if (await board.count() !== 1) throw new Error('Snapshot page must render exactly one encounter board.');
  1033	  }
  1034	
  1035	  async #waitForSettledBoard(
  1036	    board: ReturnType<Page['locator']>,
  1037	    source: BoardImageSource,
  1038	    role: BoardSnapshotImageRole,
  1039	  ): Promise<void> {
  1040	    await board.evaluate(async (element, expected) => {
   475	      : [],
   476	  );
   477	  const engineAdjudicationEntries: readonly DmDecisionTrayEntry[] = (input.engineAdjudications ?? [])
   478	    .map((request) => ({
   479	      kind: 'engine_adjudication' as const,
   480	      request: structuredClone(request),
   481	      combatantName: names.get(request.actorId as CombatantId) ?? request.actorId,
   482	      interactive: true as const,
   483	    }));
   484	  const offerEnvironment = input.offerEnvironment;
   485	  const offeredActors = offeredOptionActorsForState(input.view.state, undefined, offerEnvironment);
   486	  return {
   487	    audience: 'dm',
   488	    stateDigest: sha256(canonicalJson(input.view.state)),
   489	    offerEnvironmentDigest: offerEnvironment.digest,
   490	    encounter: dmVisibleEncounter(input.view),
   491	    board: projectEncounterBoard(input.view, input.coordinator.pendingRequest, targets),
   492	    coordinator: input.coordinator,
   493	    pendingRequest: input.coordinator.pendingRequest,
   494	    humanCommandActions,
   495	    humanEngineOptions: projectHumanEngineOptions(input.view.state, undefined, undefined, offerEnvironment),
   496	    movementPreviews: humanCommandActions.flatMap((action): readonly DmMovementPathPreview[] =>
   497	      action.type === 'move'
   498	        ? [{ commandKey: canonicalJson(action), ...previewMovementPathDangers(input.view.state, action) }]
   499	        : []),
   500	    offeredOptionPaths: projectOfferedOptionPaths(input.view.state, offeredActors, offerEnvironment),
  2296	  }
  2297	
  2298	  #markBlindSnapshotCapture(
  2299	    element: HTMLElement,
  2300	    projection: TopDownDmBoardProjection,
  2301	    role: BoardSnapshotRole,
  2302	  ): void {
  2303	    element.dataset.blindSnapshotCapture = 'true';
  2304	    element.dataset.blindSnapshotRole = role;
  2305	    element.dataset.boardAudience = role === 'player_board' ? 'player' : 'dm';
  2306	    element.dataset.sourceRevision = String(projection.encounter.revision);
  2307	    element.dataset.sourceRound = String(projection.board.round);
  2308	    element.dataset.sourceStateDigest = projection.stateDigest;
  2309	    element.dataset.boardGlyphs = encounterArtForBoard(
  2310	      projection.board,
  2311	      this.boardGlyphs,
  2312	    ).boardGlyphs;
  2313	  }
  2314	
  2315	  #renderBlindStateSnapshot(projection: TopDownDmBoardProjection): void {
tests/unit/tools/ai-dm-board-delivery.test.ts
tests/unit/tools/ai-dm-board-snapshot.test.ts
tests/unit/tools/ai-dm-conversation-row-codec.test.ts
tests/unit/tools/ai-dm-conversation.test.ts
tests/unit/tools/ai-dm-screenshot-probe.test.ts
tests/unit/tools/d569-blind-experiment.test.ts
tests/unit/tools/d569-second-family-manifest.test.ts
tests/unit/tools/d569-v5.test.ts

exec
/bin/bash -lc "rg --files tests | rg '(glyph-captures|blind-board-snapshot-check|board-snapshot-check|screenshot-probe|d569)' | sort
rg -n \"boardStateDigest|canonicalJson\\(state\\)|stateDigest\" tests/unit/tools/ai-dm-board-snapshot.test.ts tests/unit/tools/ai-dm-board-delivery.test.ts tests/unit/tools/ai-dm-screenshot-probe.test.ts tests/unit/tools/d569-*.test.ts tests/unit/tools/ai-dm-conversation.test.ts tools/ai-dm-*.ts | head -260" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
tests/fixtures/ai-dm-kb/d569/actions.md
tests/fixtures/ai-dm-kb/d569/ai-dm-core.md
tests/fixtures/ai-dm-kb/d569/conditions.md
tests/fixtures/ai-dm-kb/d569/movement.md
tests/fixtures/ai-dm-kb/d569/protocol.md
tests/fixtures/ai-dm-kb/d569/provenance.json
tests/fixtures/ai-dm-kb/d569/reactions.md
tests/fixtures/ai-dm-kb/d569/spells.md
tests/fixtures/ai-dm-kb/d569/tactics.md
tests/fixtures/ai-dm-kb/d569/targeting.md
tests/fixtures/d569-blind-experiment-manifest.json
tests/fixtures/d569-prepatch-primary-bytes.json
tests/fixtures/d569-second-family-manifest.json
tests/unit/tools/ai-dm-screenshot-probe.test.ts
tests/unit/tools/d569-blind-experiment.test.ts
tests/unit/tools/d569-second-family-manifest.test.ts
tests/unit/tools/d569-v5.test.ts
tools/ai-dm-screenshot-probe.ts:48:  boardStateDigest,
tools/ai-dm-screenshot-probe.ts:388:  readonly stateDigest: string;
tools/ai-dm-screenshot-probe.ts:2622:    stateDigest: boardStateDigest(state),
tools/ai-dm-screenshot-probe.ts:2753:    stateDigest: task.artifact.source.stateDigest,
tools/ai-dm-screenshot-probe.ts:2842:  readonly stateDigest: string;
tools/ai-dm-screenshot-probe.ts:2864:    stateDigest: z.string().min(1),
tools/ai-dm-screenshot-probe.ts:2903:    stateDigest: z.string().min(1),
tools/ai-dm-screenshot-probe.ts:3115:    stateDigest: row.stateDigest,
tools/ai-dm-screenshot-probe.ts:3123:    stateDigest: row.stateDigest,
tools/ai-dm-screenshot-probe.ts:3408:    stateDigest: saved.stateDigest,
tools/ai-dm-screenshot-probe.ts:3509:      saved.stateDigest,
tools/ai-dm-screenshot-probe.ts:3520:      hiddenNamesByState.get(saved.stateDigest) ?? [],
tools/ai-dm-screenshot-probe.ts:3615:        artifact.source.stateDigest !== boardStateDigest(candidate.state)
tests/unit/tools/ai-dm-board-delivery.test.ts:124:    const png = dimensionedPng(input.source.stateDigest);
tests/unit/tools/ai-dm-board-delivery.test.ts:484:    expect(service.captures[1]?.source.stateDigest).not.toBe(service.captures[0]?.source.stateDigest);
tests/unit/tools/ai-dm-board-delivery.test.ts:770:      stateDigest: sha256(canonicalJson(state)),
tests/unit/tools/ai-dm-board-snapshot.test.ts:39:  boardStateDigest,
tests/unit/tools/ai-dm-board-snapshot.test.ts:65:    stateDigest: boardStateDigest(state),
tests/unit/tools/ai-dm-board-snapshot.test.ts:162:        { revision: state.revision, round: state.round, stateDigest: boardStateDigest(state) },
tests/unit/tools/ai-dm-board-snapshot.test.ts:214:    expect(stateOnly.stateDigest).toBe(projection.stateDigest);
tests/unit/tools/ai-dm-board-snapshot.test.ts:265:    const stateBytes = canonicalJson(state);
tests/unit/tools/ai-dm-board-snapshot.test.ts:315:    expect(movedSource.stateDigest).not.toBe(source.stateDigest);
tools/ai-dm-board-snapshot.ts:92:  readonly stateDigest: string;
tools/ai-dm-board-snapshot.ts:201:export function boardStateDigest(state: EncounterState): string {
tools/ai-dm-board-snapshot.ts:202:  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
tools/ai-dm-board-snapshot.ts:215:  if (!SHA256_PATTERN.test(source.stateDigest)) {
tools/ai-dm-board-snapshot.ts:218:  const digest = boardStateDigest(state);
tools/ai-dm-board-snapshot.ts:219:  if (source.stateDigest !== digest) {
tools/ai-dm-board-snapshot.ts:220:    throw new Error(`Board image state digest ${source.stateDigest} does not match canonical state ${digest}.`);
tools/ai-dm-board-snapshot.ts:234:    artifact.source.stateDigest !== source.stateDigest
tools/ai-dm-board-snapshot.ts:288:    `board-snapshot:${source.stateDigest}:${String(captureOrdinal)}`,
tools/ai-dm-board-snapshot.ts:293:    branchId: encounterBranchId(`branch:board-snapshot:${source.stateDigest}`),
tools/ai-dm-board-snapshot.ts:793:      artifact.source.stateDigest !== input.source.stateDigest)) {
tools/ai-dm-board-snapshot.ts:923:    const bundlePath = join(sessions, `${input.source.stateDigest}-${String(captureOrdinal)}.vtt.json`);
tools/ai-dm-board-snapshot.ts:1016:          candidate.dataset.sourceStateDigest === expected.stateDigest &&
tools/ai-dm-board-snapshot.ts:1052:        expected.stateDigest,
tools/ai-dm-board-snapshot-check.ts:10:  boardStateDigest,
tools/ai-dm-board-snapshot-check.ts:20:    stateDigest: boardStateDigest(state),
tools/ai-dm-board-snapshot-check.ts:188:    movedDigestChanged: movedArtifact.source.stateDigest !== captures[1]?.source.stateDigest,
tools/ai-dm-conversation.ts:105:  boardStateDigest,
tools/ai-dm-conversation.ts:335:  readonly stateDigest: string;
tools/ai-dm-conversation.ts:660:    readonly stateDigest: string;
tools/ai-dm-conversation.ts:2135:    stateDigest: proposal.stateDigest,
tools/ai-dm-conversation.ts:3222:    typeof input['expectedRevision'] === 'number' && typeof input['stateDigest'] === 'string' &&
tools/ai-dm-conversation.ts:3261:    typeof input['expectedRevision'] === 'number' && typeof input['stateDigest'] === 'string' &&
tools/ai-dm-conversation.ts:3277:    stateDigest: proposal.stateDigest,
tools/ai-dm-conversation.ts:3818:    stateDigest: snapshot.capsule.digest,
tools/ai-dm-conversation.ts:4487:          stateDigest: boardStateDigest(rendererState),
tools/ai-dm-conversation.ts:5934:              proposal.expectedRevision !== contextRevision || proposal.stateDigest !== expectedCapsule.digest ||
tools/ai-dm-conversation.ts:6541:              stateDigest: correctionCapsule.digest,
tools/ai-dm-conversation.ts:7420:            stateDigest: String(movementProvenance['state_digest']),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:62:  boardStateDigest,
tests/unit/tools/ai-dm-screenshot-probe.test.ts:526:      boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1071:      new Set(first.map((candidate) => boardStateDigest(candidate.state))).size,
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1076:        digest: boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1081:        digest: boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1308:          stateDigest: 'a'.repeat(64),
tools/ai-dm-board-glyph-captures.ts:22:import { BoardSnapshotService, boardStateDigest, type BoardImageArtifact } from './ai-dm-board-snapshot';
tools/ai-dm-board-glyph-captures.ts:118:            stateDigest: boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-conversation.test.ts:679:        .toBe(canonicalJson(state));
tests/unit/tools/ai-dm-conversation.test.ts:2328:      stateDigest: 'state',
tests/unit/tools/ai-dm-conversation.test.ts:2407:      stateDigest: 'state',
tools/ai-dm-blind-board-snapshot-check.ts:14:  boardStateDigest,
tools/ai-dm-blind-board-snapshot-check.ts:25:    stateDigest: boardStateDigest(state),
tools/ai-dm-blind-board-snapshot-check.ts:49:    requireCondition(artifact.source.stateDigest === boardStateDigest(state), 'Image digest binding changed.');
tools/ai-dm-blind-board-snapshot-check.ts:160:    readonly stateDigest: string;
tools/ai-dm-blind-board-snapshot-check.ts:285:      stateDigest: source.stateDigest,
tests/unit/tools/d569-v5.test.ts:117:    startingRoomDigest: sha256(canonicalJson(state)), kbHash: cell.sharedKbHash,
tests/unit/tools/d569-v5.test.ts:244:    Buffer.from(input.source.stateDigest, 'utf8').copy(png, 24, 0, 64);

exec
/bin/bash -lc "rg -l \"ai-dm-board-glyph-captures|ai-dm-blind-board-snapshot-check|ai-dm-board-snapshot-check\" tests | sort
sed -n '1,360p' tests/unit/tools/ai-dm-board-snapshot.test.ts
sed -n '730,795p' tests/unit/tools/ai-dm-board-delivery.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
tests/browser/ai-dm-board-snapshot.spec.ts
tests/unit/vtt/challenge-room-fixtures.test.ts
import { describe, expect, it } from 'vitest';
import { readFile } from '../../helpers/test-filesystem-promises';
import { canonicalJson } from '../../../src/commands/canonical-json';
import type { EncounterState } from '../../../src/combat/encounter';
import { terrainWallCells } from '../../../src/combat/terrain';
import { projectDmView } from '../../../src/combat/visibility';
import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
import { encounterArtForBoard } from '../../../src/vtt/encounter-art-selection';
import { renderBoard } from '../../../src/vtt/encounter-app';
import {
  encounterBoardRenderModel,
  projectEncounterBoard,
  projectEncounterTerrainCells,
} from '../../../src/vtt/encounter-board';
import {
  MemoryBrowserSessionStore,
  MemoryMirrorSink,
  importSavedSession,
  EncounterSessionJournal,
} from '../../../src/vtt/session-persistence';
import {
  loadArenaFixture,
} from '../../../src/vtt/mcp/entrypoint';
import * as engineMcpEntrypoint from '../../../src/vtt/mcp/entrypoint';
import {
  createDisabledEngineOfferFamilyPolicy,
} from '../../../src/vtt/offers/offer-environment';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
import { createUnrepresentedPartyThreatCatalog } from '../../../src/vtt/offers/party-threat-catalog';
import {
  assertBoardImageFresh,
  assertSynchronizedBoardImages,
  BLIND_STATE_PRIMER_VERSION,
  BOARD_SNAPSHOT_IMAGE_ROLES,
  BOARD_SNAPSHOT_TERRAIN_SELECTORS,
  boardSnapshotCaptureGeometry,
  boardSnapshotInformationMode,
  boardSnapshotVisualDescriptor,
  boardStateDigest,
  configuredPreviewPort,
  createBoardSnapshotSessionBundle,
  isBoardSnapshotDomEvidence,
  type BoardImageArtifact,
  type BoardImageSource,
  type BoardSnapshotDomEvidence,
} from '../../../tools/ai-dm-board-snapshot';
import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';

const CONTROL_FIXTURES = Array.from(
  { length: 10 },
  (_unused, index) => `tests/fixtures/arena-basis-brutal/seed-${String(6_203_001 + index)}.json`,
);
const BOUND_OFFER_ENVIRONMENT = buildOfferEnvironment({
  kind: 'configuration',
  mode: 'revision_bound',
  familyPolicy: createDisabledEngineOfferFamilyPolicy(),
  partyThreatCatalog: createUnrepresentedPartyThreatCatalog(),
});

function sourceFor(state: EncounterState, room = 1): BoardImageSource {
  return {
    room,
    round: state.round,
    revision: state.revision,
    stateDigest: boardStateDigest(state),
  };
}

function movedState(state: EncounterState): EncounterState {
  const first = state.tokens[0];
  if (first === undefined) throw new Error('Fixture has no token to move.');
  return {
    ...state,
    tokens: state.tokens.map((token, index) => index === 0
      ? {
          ...token,
          position: {
            column: Math.min(token.position.column + 1, state.bounds.columns - 1),
            row: token.position.row,
          },
        }
      : token),
  };
}

describe('AI DM board snapshot contracts', () => {
  it('isolates its preview port from the Playwright worker pool', () => {
    expect(configuredPreviewPort({ PLAYWRIGHT_PORT: '4650' })).toBe(0);
    expect(configuredPreviewPort({
      PLAYWRIGHT_PORT: '4650',
      BOARD_SNAPSHOT_PREVIEW_PORT: '4750',
    })).toBe(4_750);
  });

  it('derives the CSS tile, marker height, and default from capture scale', () => {
    expect(boardSnapshotCaptureGeometry(64)).toEqual({
      tileSizeCssPx: 64,
      markerHeightCssPx: 640,
    });
    expect(boardSnapshotCaptureGeometry()).toEqual({
      tileSizeCssPx: 128,
      markerHeightCssPx: 1_280,
    });
  });

  it('defaults the information profile to byte-compatible advice and closes state-only roles', () => {
    expect(boardSnapshotInformationMode()).toBe('advice');
    expect(boardSnapshotInformationMode('advice')).toBe('advice');
    expect(boardSnapshotInformationMode('blind_state')).toBe('blind_state');
    expect(BOARD_SNAPSHOT_IMAGE_ROLES).toEqual([
      'dm_board', 'accessible_board_raster', 'player_board',
    ]);
  });

  it('accepts the complete terrain evidence shape and rejects legacy or incomplete shapes', () => {
    const evidence: BoardSnapshotDomEvidence = {
      optionSurfaceAbsent: true,
      nextEventPreviewAbsent: true,
      coordinateLabels: 0,
      creatureBadges: 0,
      rosterEntries: 0,
      hpBars: 0,
      legendEntries: 0,
      wallCells: 0,
      halfCoverCells: 0,
      threeQuartersCoverCells: 0,
      difficultCells: 0,
      obscuredCells: 0,
      illuminatedCells: 0,
      fogMarks: 0,
      doors: 0,
      objects: 0,
      hiddenMarks: 0,
      multiCellFootprints: 0,
    };
    expect(isBoardSnapshotDomEvidence(evidence)).toBe(true);

    const legacyShape: Record<string, unknown> = { ...evidence };
    Reflect.deleteProperty(legacyShape, 'wallCells');
    Reflect.deleteProperty(legacyShape, 'halfCoverCells');
    Reflect.deleteProperty(legacyShape, 'threeQuartersCoverCells');
    legacyShape['blockedCells'] = 0;
    expect(isBoardSnapshotDomEvidence(legacyShape)).toBe(false);

    const missingHalfCover: Record<string, unknown> = { ...evidence };
    Reflect.deleteProperty(missingHalfCover, 'halfCoverCells');
    expect(isBoardSnapshotDomEvidence(missingHalfCover)).toBe(false);
  });

  it.each([
    ['hard', 5_117_001, 'tests/fixtures/arena-basis-hard/seed-5117001.json'],
    ['brutal', 6_203_001, 'tests/fixtures/arena-basis-brutal/seed-6203001.json'],
  ] as const)('renders state-derived terrain evidence for %s seed %i', async (_difficulty, _seed, path) => {
    const restoreDocument = installInteractiveDocument();
    try {
      const state = await loadArenaFixture(path);
      const projection = projectEncounterBoard(projectDmView(state));
      const board = interactiveElement(renderBoard(
        projection,
        new Set(),
        null,
        { revision: state.revision, round: state.round, stateDigest: boardStateDigest(state) },
        'full',
        true,
        128,
      ));
      const projectedTerrain = projectEncounterTerrainCells(state.bounds, state);
      const expected = {
        wallCells: terrainWallCells(state).length,
        halfCoverCells: projectedTerrain.filter((cell) => cell.kind === 'half_cover').length,
        threeQuartersCoverCells: projectedTerrain.filter(
          (cell) => cell.kind === 'three_quarters_cover',
        ).length,
      };

      for (const key of Object.keys(BOARD_SNAPSHOT_TERRAIN_SELECTORS) as readonly (
        keyof typeof BOARD_SNAPSHOT_TERRAIN_SELECTORS
      )[]) {
        expect(
          board.querySelectorAll(BOARD_SNAPSHOT_TERRAIN_SELECTORS[key]),
          `${key} must match its independent state derivation`,
        ).toHaveLength(expected[key]);
      }
    } finally {
      restoreDocument();
    }
  });

  it('removes offered paths at the projection boundary without changing board state', async () => {
    const state = await loadArenaFixture(CONTROL_FIXTURES[0]!);
    const projection = projectDmBoard({
      view: projectDmView(state),
      coordinator: {
        requestSequence: 1,
        pendingRequest: null,
        pendingCommand: null,
        continuation: { kind: 'idle' },
        pause: { kind: 'interrupted' },
      },
      controllers: state.combatants.map((combatant) => ({
        combatantId: combatant.profile.id,
        controllerId: `test:${String(combatant.profile.id)}`,
        kind: 'human' as const,
        generation: 0,
      })),
      history: [],
      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
    });
    const stateOnly = projectStateOnlyDmBoard(projection);

    expect(projection.offeredOptionPaths.length).toBeGreaterThan(0);
    expect(stateOnly.offeredOptionPaths).toEqual([]);
    expect(stateOnly.board).toBe(projection.board);
    expect(stateOnly.stateDigest).toBe(projection.stateDigest);
  });

  it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
    const state = await loadArenaFixture(path);
    const projection = projectEncounterBoard(projectDmView(state));
    const art = encounterArtForBoard(projection);
    const cells = encounterBoardRenderModel(projection, art);

    expect(art.room).toMatchObject(state.bounds);
    expect(art.id).toBe(`encounter-art:generated-${String(state.bounds.columns)}x${String(state.bounds.rows)}:v1`);
    expect(art.terrain).toEqual([]);
    expect(cells).toHaveLength(state.bounds.columns * state.bounds.rows);
    expect(cells.flatMap((cell) => cell.mechanicalLayers).filter(
      (layer) => layer.kind === 'terrain' && layer.terrainKind === 'wall',
    )).toHaveLength(projection.terrainCells.filter((cell) => cell.kind === 'wall').length);

    const expectedDifficult = new Set(state.environment.difficultTerrainRegions.flatMap(
      (region) => region.cells.map((cell) => `${region.id}:${String(cell.column)},${String(cell.row)}`),
    ));
    const actualDifficult = new Set(cells.flatMap((cell) => cell.mechanicalLayers.flatMap((layer) =>
      layer.kind === 'difficult_terrain' ? [`${layer.regionId}:${cell.key}`] : [])));
    expect(actualDifficult).toEqual(expectedDifficult);

    const expectedObscurement = new Set(state.environment.obscurementRegions.flatMap(
      (region) => region.cells.map(
        (cell) => `${region.id}:${region.obscurement}:${String(cell.column)},${String(cell.row)}`,
      ),
    ));
    const actualObscurement = new Set(cells.flatMap((cell) => cell.mechanicalLayers.flatMap(
      (layer) => layer.kind === 'obscurement'
        ? [`${layer.regionId}:${layer.obscurement}:${cell.key}`]
        : [],
    )));
    expect(actualObscurement).toEqual(expectedObscurement);

    const expectedIllumination = new Set(state.environment.lightRegions.flatMap(
      (region) => region.cells.map(
        (cell) => `${region.id}:${region.level}:${String(cell.column)},${String(cell.row)}`,
      ),
    ));
    const actualIllumination = new Set(cells.flatMap((cell) => cell.mechanicalLayers.flatMap(
      (layer) => layer.kind === 'illumination'
        ? [`${layer.regionId}:${layer.level}:${cell.key}`]
        : [],
    )));
    expect(actualIllumination).toEqual(expectedIllumination);
  });

  it('builds a replayable interrupted all-human save without changing encounter bytes', async () => {
    const state = await loadArenaFixture(CONTROL_FIXTURES[0]!);
    const stateBytes = canonicalJson(state);
    const source = sourceFor(state);
    const bundle = createBoardSnapshotSessionBundle(state, source, 1);
    const store = new MemoryBrowserSessionStore();

    expect(importSavedSession(store, bundle.bytes)).toBe(bundle.sessionId);
    const resumed = EncounterSessionJournal.resume(bundle.sessionId, store, new MemoryMirrorSink());
    const runtime = engineMcpEntrypoint.createEngineMcpRuntime(resumed.encounterState, {
      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
    });
    expect(runtime.feed.current().offerEnvironment).toEqual(BOUND_OFFER_ENVIRONMENT.binding);
    expect(canonicalJson(resumed.encounterState)).toBe(stateBytes);
    expect(runtime.feed.current().projection.combatants.map((combatant) => combatant.id).sort()).toEqual(
      resumed.encounterState.combatants
        .filter((combatant) => combatant.life !== 'dead')
        .map((combatant) => combatant.profile.id)
        .sort(),
    );
    expect(resumed.coordinatorState.pause).toEqual({ kind: 'interrupted' });
    expect(resumed.controllers).toHaveLength(state.combatants.length);
    expect(resumed.controllers.every((controller) => controller.kind === 'human')).toBe(true);
  });

  it('board_image_stale_after_move rejects an artifact after canonical state movement', async () => {
    const state = await loadArenaFixture(CONTROL_FIXTURES[1]!);
    const source = sourceFor(state);
    const artifact: BoardImageArtifact = {
      version: 'arena-board-image-v1',
      audience: 'dm',
      mimeType: 'image/png',
      relativePath: `board-images/${'a'.repeat(64)}.png`,
      sha256: 'a'.repeat(64),
      bytes: 100,
      width: 10,
      height: 10,
      capturedAtUnixMs: 1,
      captureMs: 1,
      source,
      chromiumVersion: 'unit',
      html: {
        relativePath: `board-html/${'b'.repeat(64)}/board.html`,
        sha256: 'b'.repeat(64),
        bytes: 1,
      },
    };
    const moved = movedState(state);
    const movedSource = sourceFor(moved);

    expect(() => assertBoardImageFresh(artifact, state, source)).not.toThrow();
    expect(() => assertBoardImageFresh(artifact, moved, movedSource)).toThrow(/stale/u);
    expect(movedSource.stateDigest).not.toBe(source.stateDigest);
  });

  it('records hashes privately while exposing only a neutral role descriptor model-side', async () => {
    const state = await loadArenaFixture(CONTROL_FIXTURES[0]!);
    const artifact: BoardImageArtifact = {
      version: 'arena-board-image-v1',
      audience: 'dm',
      mimeType: 'image/png',
      relativePath: `board-images/${'a'.repeat(64)}.png`,
      sha256: 'a'.repeat(64),
      bytes: 100,
      width: 10,
      height: 10,
      capturedAtUnixMs: 1,
      captureMs: 1,
      source: sourceFor(state),
      chromiumVersion: 'unit',
      html: {
        relativePath: `board-html/${'b'.repeat(64)}/board.html`,
        sha256: 'b'.repeat(64),
        bytes: 1,
      },
      blindState: {
        informationMode: 'blind_state',
        role: 'player_board',
        ordinal: 3,
        primerVersion: BLIND_STATE_PRIMER_VERSION,
        glyphMode: 'full',
        captureTilePx: 128,
        domEvidence: {
          optionSurfaceAbsent: true,
          nextEventPreviewAbsent: true,
          coordinateLabels: 0,
          creatureBadges: 0,
          rosterEntries: 0,
          hpBars: 0,
          legendEntries: 0,
          wallCells: 0,
          halfCoverCells: 0,
          threeQuartersCoverCells: 0,
          difficultCells: 0,
          obscuredCells: 0,
          illuminatedCells: 0,
          fogMarks: 0,
          doors: 0,
    expect(services.get('capture_only')?.closed).toBe(true);
    expect(captureOnlyRow.boardImage.relativePath)
      .toBe(`board-images/${captureOnlyRow.boardImage.sha256}.png`);
    expect(offInvocation.instructions?.split(BOARD_IMAGE_SCALE_STARTUP_INSTRUCTION)).toHaveLength(1);
    expect(captureOnlyInvocation.instructions?.split(BOARD_IMAGE_SCALE_STARTUP_INSTRUCTION)).toHaveLength(1);
  });

  it('ui_feedback_offered_in_off_arm keeps the off-arm tool manifest byte-identical', () => {
    const state = generateRoom(3_943_006).encounter.state;
    const implicitOff = createEngineMcpRuntime(state, { toolProfile: 'dm' });
    const explicitOff = createEngineMcpRuntime(state, { toolProfile: 'dm' });
    const offBytes = canonicalJson(implicitOff.toolSurface.tools);
    expect(canonicalJson(explicitOff.toolSurface.tools)).toBe(offBytes);
    expect(offBytes).not.toContain('engine.submit_ui_feedback');

    const png = dimensionedPng('feedback-tool');
    const image = createEngineMcpRuntime(state, {
      toolProfile: 'dm',
      boardImageContent: { type: 'image', mimeType: 'image/png', data: png.toString('base64') },
    });
    expect(image.toolSurface.tools.map(({ name }) => name)).toContain('engine.submit_ui_feedback');
  });
});

describe('launcher image containment and binding', () => {
  it('validates bytes and rejects a path that is not content-addressed beneath the root', async () => {
    const root = mkdtempSync(join(tmpdir(), 'launcher-board-image-'));
    const state = { ...generateRoom(3_943_006).encounter.state, round: 1, revision: 1 };
    const png = dimensionedPng('launcher');
    const digest = hashBytes(png);
    const relativePath = `board-images/${digest}.png` as const;
    mkdirSync(join(root, 'board-images'), { recursive: true });
    writeFileSync(join(root, relativePath), png);
    const htmlBytes = Buffer.from('<!doctype html><html lang="en"><body><main>Board facts</main></body></html>\n');
    const htmlDigest = hashBytes(htmlBytes);
    const htmlRelativePath = `board-html/${htmlDigest}/board.html` as const;
    mkdirSync(join(root, 'board-html', htmlDigest), { recursive: true });
    writeFileSync(join(root, htmlRelativePath), htmlBytes);
    const source: BoardImageSource = {
      room: 1, round: state.round, revision: state.revision,
      stateDigest: sha256(canonicalJson(state)),
    };
    const fixturePath = join(root, 'fixture.json');
    const proposalSpoolPath = join(root, 'proposals.jsonl');
    writeFileSync(fixturePath, canonicalJson({ encounter: { state } }));
    writeFileSync(proposalSpoolPath, '');
    const manifest = {
      format: 'engine-mcp-launcher-v1', fixturePath,
      proposalSpoolPath, runId: encounterSessionId('encounter:test'),
      branchId: encounterBranchId('branch:test'),
      revision: 2, requestId: 'request:test', phase: 'initial', correctionNumber: 0,
      offerEnvironment: createLegacyEngineOptionEnvironmentBinding(),
      room: 1, historyKind: 'room_ready', requestKind: 'round_plan',
      boardImage: {
        artifactRoot: root,
        primaryDispatchStartedAtUnixMs: 2,
        artifact: {
          version: 'arena-board-image-v1', audience: 'dm', mimeType: 'image/png', relativePath,
          sha256: digest, bytes: png.byteLength, width: 1, height: 1,
          capturedAtUnixMs: 1, captureMs: 1, source, chromiumVersion: 'SIMULATED',
          html: {
            relativePath: htmlRelativePath,
            sha256: htmlDigest,
            bytes: htmlBytes.byteLength,
          },
        },

exec
/bin/bash -lc "rg -n \"kind: 'saving_throw'|savingThrow\\(\" src/combat/statblocks tests/unit/vtt/engine-query-port.test.ts tests/unit/combat/fixtures.ts | head -160
rg -n \"Web|breath\" src/combat/statblocks | head -120
rg -n \"export const .*SPIDER|SPIDER|DRAGON|HELL_HOUND|breath\" src/combat/statblocks/monsters.ts src/combat/statblocks/*.ts | head -160" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
src/combat/statblocks/companions.ts:127:    ? [{ kind: 'saving_throw' as const, id: 'fell-glare', name: 'Fell Glare', savingThrow: { ability: 'wisdom' as const, dc: context.spellSaveDc }, target: { rangeFeet: 60, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Frightened', null, { duration: 'until_end_of_monster_next_turn' })] }, success: { kind: 'none' as const } }]
src/combat/statblocks/companions.ts:156:    ? [{ kind: 'saving_throw' as const, id: 'venomous-spew', name: 'Venomous Spew', savingThrow: { ability: 'constitution' as const, dc: context.spellSaveDc }, target: { rangeFeet: 10, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Poisoned', null, { duration: 'until_start_of_monster_next_turn' })] }, success: { kind: 'none' as const } }]
src/combat/statblocks/goblinoid-warband.ts:80:    bonusActions: present([{ kind: 'saving_throw', id: 'quick-grapple', name: 'Quick Grapple', savingThrow: { ability: 'dexterity', dc: 13 }, target: { rangeFeet: 10, maximumSize: 'Medium', excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Grappled', 'Medium', { escapeDc: 13, duration: 'until_escape' })] }, success: { kind: 'none' } }]),
src/combat/statblocks/undead-crypt.ts:126:      { kind: 'saving_throw', id: 'life-drain', name: 'Life Drain', savingThrow: { ability: 'constitution', dc: 13 }, target: { rangeFeet: 5, maximumSize: null, excludedKinds: [] }, failure: { damage: [damage(6, 1, 8, 2, 'Necrotic')], effects: [{ kind: 'hit_point_maximum_reduction', amount: 'damage_taken' }, { kind: 'raises_as_zombie', targetKind: 'Humanoid', delayHours: 24, controllerLimit: 12, preventedBy: ['restored_to_life', 'body_destroyed'] }] }, success: { kind: 'none' } },
src/combat/statblocks/wild-beasts.ts:74:      { kind: 'saving_throw', id: 'roar', name: 'Roar', savingThrow: { ability: 'wisdom', dc: 11 }, target: { rangeFeet: 15, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Frightened', null, { duration: 'until_start_of_monster_next_turn' })] }, success: { kind: 'none' } },
src/combat/statblocks/wild-beasts.ts:177:      { kind: 'saving_throw', id: 'web', name: 'Web', savingThrow: { ability: 'dexterity', dc: 13 }, target: { rangeFeet: 60, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Restrained', null, { duration: 'until_escape' })] }, success: { kind: 'none' } },
src/combat/statblocks/wild-beasts.ts:192:      { kind: 'saving_throw', id: 'constrict', name: 'Constrict', savingThrow: { ability: 'strength', dc: 14 }, target: { rangeFeet: 10, maximumSize: 'Large', excludedKinds: [] }, failure: { damage: [damage(13, 2, 8, 4, 'Bludgeoning')], effects: [conditionOnHit('Grappled', 'Large', { escapeDc: 14, duration: 'until_escape' })] }, success: { kind: 'none' } },
src/combat/statblocks/wild-beasts.ts:227:    bonusActions: present([{ kind: 'saving_throw', id: 'trample', name: 'Trample', savingThrow: { ability: 'dexterity', dc: 16 }, target: { rangeFeet: 5, maximumSize: null, excludedKinds: [] }, failure: { damage: [damage(17, 2, 10, 6, 'Bludgeoning')], effects: [] }, success: { kind: 'half_damage' } }]), reactions: notListed('reactions'),
src/combat/statblocks/astral-tower.ts:89:        kind: 'saving_throw', id: 'whirlwind', name: 'Whirlwind', savingThrow: { ability: 'strength', dc: 13 },
src/combat/statblocks/astral-tower.ts:203:        kind: 'saving_throw', id: 'unsettling-visage', name: 'Unsettling Visage', savingThrow: { ability: 'wisdom', dc: 12 },
src/combat/statblocks/astral-tower.ts:306:        kind: 'saving_throw', id: 'horrific-visage', name: 'Horrific Visage', savingThrow: { ability: 'wisdom', dc: 13 },
src/combat/statblocks/astral-tower.ts:318:        kind: 'saving_throw', id: 'possession', name: 'Possession', savingThrow: { ability: 'charisma', dc: 13 },
src/combat/statblocks/homebrew-beast-families.ts:181:      const web: MonsterAction = { kind: 'saving_throw', id: 'web', name: 'Web', savingThrow: { ability: 'dexterity', dc }, target: { rangeFeet: 30 + index * 5, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Restrained', null, { escapeDc: dc, duration: 'until_escape' })] }, success: { kind: 'none' } };
src/combat/statblocks/kb/entries.ts:52:  { ruleId: 'R-MONSTER-041', monsterId: 'statblock:giant-spider', srdLocator: 'docs/srd/full/srd-5.2.1.txt:23313-23350', rulingGuidance: 'Choose the damaging Bite or the DC 13 Web that restrains its target until the web is destroyed.' },
src/combat/statblocks/companions.ts:153:    ...(request.form === 'Spider' ? [attack('web-bolt', 'Web Bolt', context.spellAttackBonus, { kind: 'ranged', rangeFeet: 60, longRangeFeet: notListed('a long range for Web Bolt') }, [damage(8 + context.slotLevel, 1, 10, 3 + context.slotLevel, 'Bludgeoning')], [{ kind: 'speed_reduction' as const, feet: 0 as const, duration: 'until_start_of_monster_next_turn' as const }])] : []),
src/combat/statblocks/homebrew-beast-families.ts:93:  arachnid: ['Threadling Weaver', 'Ambush Weaver', 'Gloom Weaver', 'Ironweb Weaver', 'Dreadweb Weaver', 'Cavern Weaver', 'Titan Weaver', 'Elder Web Tyrant'],
src/combat/statblocks/homebrew-beast-families.ts:181:      const web: MonsterAction = { kind: 'saving_throw', id: 'web', name: 'Web', savingThrow: { ability: 'dexterity', dc }, target: { rangeFeet: 30 + index * 5, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Restrained', null, { escapeDc: dc, duration: 'until_escape' })] }, success: { kind: 'none' } };
src/combat/statblocks/homebrew-beast-families.ts:205:        signatures: [{ mechanic: 'swim_speed', magnitude: 30 + index * 5, standingInFor: null }, { mechanic: 'blood_frenzy', magnitude: dc, standingInFor: null }, { mechanic: 'water_breathing', magnitude: 1, standingInFor: null }] };
src/combat/statblocks/homebrew-beast-families.ts:230:    case 'aquatic': return present([{ kind: 'water_breathing' as const, onlyUnderwater: true as const }]);
src/combat/statblocks/homebrew-beast-families.ts:294:  aquatic: ['swim_speed', 'blood_frenzy', 'water_breathing'],
src/combat/statblocks/wild-beasts.ts:162:    skills: present([{ name: 'Stealth', bonus: 2 }]), gear: notListed('gear'), senses: present([]), passivePerception: 10, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'hold_breath', minutes: 60 }]),
src/combat/statblocks/wild-beasts.ts:177:      { kind: 'saving_throw', id: 'web', name: 'Web', savingThrow: { ability: 'dexterity', dc: 13 }, target: { rangeFeet: 60, maximumSize: null, excludedKinds: [] }, failure: { damage: [], effects: [conditionOnHit('Restrained', null, { duration: 'until_escape' })] }, success: { kind: 'none' } },
src/combat/statblocks/wild-beasts.ts:203:    skills: present([{ name: 'Perception', bonus: 3 }, { name: 'Stealth', bonus: 4 }]), gear: notListed('gear'), senses: present([{ kind: 'blindsight', rangeFeet: 120 }]), passivePerception: 13, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'hold_breath', minutes: 30 }]),
src/combat/statblocks/wild-beasts.ts:237:    skills: present([{ name: 'Stealth', bonus: 5 }]), gear: notListed('gear'), senses: present([]), passivePerception: 10, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'hold_breath', minutes: 60 }]),
src/combat/statblocks/wild-beasts.ts:252:    skills: present([{ name: 'Perception', bonus: 3 }]), gear: notListed('gear'), senses: present([{ kind: 'blindsight', rangeFeet: 60 }]), passivePerception: 13, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'water_breathing', onlyUnderwater: true }]),
src/combat/statblocks/wild-beasts.ts:162:    skills: present([{ name: 'Stealth', bonus: 2 }]), gear: notListed('gear'), senses: present([]), passivePerception: 10, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'hold_breath', minutes: 60 }]),
src/combat/statblocks/wild-beasts.ts:169:export const GIANT_SPIDER = monsterStatblock({
src/combat/statblocks/wild-beasts.ts:203:    skills: present([{ name: 'Perception', bonus: 3 }, { name: 'Stealth', bonus: 4 }]), gear: notListed('gear'), senses: present([{ kind: 'blindsight', rangeFeet: 120 }]), passivePerception: 13, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'hold_breath', minutes: 30 }]),
src/combat/statblocks/wild-beasts.ts:237:    skills: present([{ name: 'Stealth', bonus: 5 }]), gear: notListed('gear'), senses: present([]), passivePerception: 10, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'hold_breath', minutes: 60 }]),
src/combat/statblocks/wild-beasts.ts:252:    skills: present([{ name: 'Perception', bonus: 3 }]), gear: notListed('gear'), senses: present([{ kind: 'blindsight', rangeFeet: 60 }]), passivePerception: 13, languages: present([]), damageResponses: notListed('damage vulnerabilities, resistances, or immunities'), conditionImmunities: notListed('condition immunities'), traits: present([{ kind: 'water_breathing', onlyUnderwater: true }]),
src/combat/statblocks/roster.ts:4:  CAMEL, CROCODILE, DIRE_WOLF, ELEPHANT, GHAST, GHOUL, GIANT_CONSTRICTOR_SNAKE, GIANT_CROCODILE, GIANT_SCORPION, GIANT_SHARK, GIANT_SPIDER, GOBLIN_BOSS, GOBLIN_MINION, GOBLIN_WARRIOR, GUARD,
src/combat/statblocks/roster.ts:69:  { id: 'statblock:giant-spider', name: 'Giant Spider', family: 'wild_beasts', challengeRating: 1, source: [{ path: SRD_PATH, lineStart: 23313, lineEnd: 23350 }], selectionNote: null, statblock: GIANT_SPIDER },
src/combat/statblocks/homebrew-beast-families.ts:205:        signatures: [{ mechanic: 'swim_speed', magnitude: 30 + index * 5, standingInFor: null }, { mechanic: 'blood_frenzy', magnitude: dc, standingInFor: null }, { mechanic: 'water_breathing', magnitude: 1, standingInFor: null }] };
src/combat/statblocks/homebrew-beast-families.ts:230:    case 'aquatic': return present([{ kind: 'water_breathing' as const, onlyUnderwater: true as const }]);
src/combat/statblocks/homebrew-beast-families.ts:294:  aquatic: ['swim_speed', 'blood_frenzy', 'water_breathing'],

exec
/bin/bash -lc 'rg -n "blocking column|blocking-column|save execution|LAND-F1|surviv" /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/review-impl-land-01-astra-r1.md | tail -80' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
3443:I recounted 214 options: 142 hard, 57 brutal, and 15 brutal-b. They contain 312 attack components and four saves; none has multiple targets. The save coverage gap is reproducible: I inserted blocking walls into all four save execution states, confirmed `blocksSight === true`, and the agreement test still passed. The generator’s old/new outputs were byte-identical for all ten brutal-b seeds.
3691:## LAND-F1 — P2: the agreement property does not establish save LOS
3695:**Reproduced:** in memory, I inserted a full blocking column into each of the four save execution states and asserted `traceCombatantLine(...).blocksSight === true`. The existing agreement property still passed **1/1**:
3791:- Save execution-state blocker probe: **survived**, supporting LAND-F1.
3806:## LAND-F1 — P2: the agreement property does not establish save LOS
3810:**Reproduced:** in memory, I inserted a full blocking column into each of the four save execution states and asserted `traceCombatantLine(...).blocksSight === true`. The existing agreement property still passed **1/1**:
3906:- Save execution-state blocker probe: **survived**, supporting LAND-F1.

exec
/bin/bash -lc "sed -n '3380,3475p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/review-impl-land-01-astra-r1.md
sed -n '3675,3725p' /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/review-impl-land-01-astra-r1.md" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
    throw new EncounterRuleError('validation', `Combatant ${actor} is Incapacitated.`);
  }
}
2114:export function hasLineOfSight(
4876:    requiresSight: true,
7120:    opportunityTrigger === null && command.requiresSight === true &&
9306:        (!declaration.requiresSight || canCombatantSee(state, reactor, event.source));
9315:        (!declaration.requiresSight || canCombatantSee(state, reactor, event.caster));
9484:    definition.targeting.requiresSight === true
/** The canonical 2014 corner-to-corner trace for two bare grid cells. */
export function traceTerrainLine(
  state: EncounterState,
  sourceCell: GridCell,
  targetCell: GridCell,
  options: TerrainLineOptions = {},
): TerrainLineTrace {
  return traceSpaces(state, [sourceCell], [targetCell], options);
}

export interface CreatureLineOptions {
  readonly sourceAnchor?: GridCell;
  readonly additionalSightBlockingCells?: readonly GridCell[];
}

export function traceCombatantLine(
  state: EncounterState,
  sourceId: CombatantId,
  targetId: CombatantId,
  options: CreatureLineOptions = {},
): TerrainLineTrace {
  const blockerSignature = sightBlockingSignature(
    normalizedSightBlockingCells(options.additionalSightBlockingCells),
  );
  const traceKey = `${String(sourceId)}->${String(targetId)}@${options.sourceAnchor === undefined
    ? 'current'

 succeeded in 3463ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-seam-verify

stdout | tests/unit/vtt/tactical-evaluator-r02.test.ts > R02-like canonical tactical query > compares ordered registry allocations with exact post-grant Bless conditioning
R02_ORIGIN combatant:generated-5117009-monster-1 {"column":13,"row":4} none false
R02_ORIGIN combatant:generated-5117009-monster-3 {"column":18,"row":4} half false
R02_ORIGIN combatant:generated-5117009-monster-5 {"column":19,"row":5} three_quarters false
R02_ORIGIN combatant:generated-5117009-monster-4 {"column":17,"row":4} half false
R02_ORIGIN combatant:generated-5117009-monster-6 {"column":18,"row":5} none false

R02_ORIGIN combatant:generated-5117009-monster-1 {"column":13,"row":4} none false
R02_ORIGIN combatant:generated-5117009-monster-3 {"column":18,"row":4} half false
R02_ORIGIN combatant:generated-5117009-monster-5 {"column":19,"row":5} three_quarters false
R02_ORIGIN combatant:generated-5117009-monster-4 {"column":17,"row":4} half false
R02_ORIGIN combatant:generated-5117009-monster-6 {"column":18,"row":5} none false

 ✓ tests/unit/vtt/tactical-evaluator-r02.test.ts (2 tests | 1 skipped) 394ms
     ✓ compares ordered registry allocations with exact post-grant Bless conditioning  393ms

 Test Files  1 passed (1)
      Tests  1 passed | 1 skipped (2)
   Start at  05:27:33
   Duration  3.37s (transform 1.28s, setup 0ms, import 2.90s, tests 394ms, environment 0ms)


codex
I recounted 214 options: 142 hard, 57 brutal, and 15 brutal-b. They contain 312 attack components and four saves; none has multiple targets. The save coverage gap is reproducible: I inserted blocking walls into all four save execution states, confirmed `blocksSight === true`, and the agreement test still passed. The generator’s old/new outputs were byte-identical for all ten brutal-b seeds.

exec
/bin/bash -lc "sed -n '9465,9500p' src/combat/encounter.ts; sed -n '7100,7138p' src/combat/encounter.ts; nl -ba src/vtt/room-generator.ts | sed -n '1225,1252p'; git diff 10cc9f5e ebfcacaa -- src/vtt/engine-query-port.ts" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
 succeeded in 0ms:
      }
      const roundDelta = context.state.round - subject.deathAt.round;
      const withinWindow = roundDelta >= 0 && (
        roundDelta < definition.operation.maximumDeathAgeRounds ||
        (roundDelta === definition.operation.maximumDeathAgeRounds &&
          (context.state.activeInitiativeIndex ?? 0) <= subject.deathAt.initiativeIndex)
      );
      if (!withinWindow) {
        throw new RevivifyRuleError('target_dead_too_long', REVIVIFY_CITATIONS.deathWindowAndHitPoints);
      }
    }
  }
  if (
    definition.targeting.kind !== 'self' &&
    definition.targeting.kind !== 'area' &&
    definition.targeting.kind !== 'area_selected' &&
    definition.targeting.kind !== 'all_in_range' &&
    definition.targeting.kind !== 'remote' &&
    definition.targeting.kind !== 'utility' &&
    definition.targeting.requiresSight === true
  ) {
    const unseen = targets.find((target) => !canCombatantSee(context.state, command.actor, target));
    if (unseen !== undefined) {
      throw new VisionTargetingRuleError('target_not_seen', command.actor, unseen);
    }
  }
  if (definition.components.verbal) endHidden(context, command.actor, 'verbal_spell');
  spendSpellCastingCost(context, definition, command);
 12473	        context,
 12474	        command.actor,
 12475	        command.target,
 12476	        command.ability,
 12477	        command.dc,
 12478	        command.rollMode,
 12479	        null,
 12480	        'other',
 12481	        command.rollModifierEffectIds,
 12482	      );
 12483	      const damage = resolveTargetDamage(context, command.actor, command.target, command.damage);
 12484	      const amount =
 12485	        save.outcome === 'failure'
 12486	          ? damage.total

codex
## LAND-F1 — P2: the agreement property does not establish save LOS

At [engine-query-port.test.ts:574](/home/vagrant/PhpstormProjects/dnd-wt-seam-verify/tests/unit/vtt/engine-query-port.test.ts:574), saving throws are checked only by executing `force_save`. That reducer branch validates the declaration, spends the action, and resolves the save **without checking LOS** ([encounter.ts:12451](/home/vagrant/PhpstormProjects/dnd-wt-seam-verify/src/combat/encounter.ts:12451)).

**Reproduced:** in memory, I inserted a full blocking column into each of the four save execution states and asserted `traceCombatantLine(...).blocksSight === true`. The existing agreement property still passed **1/1**:

- `6203010`: one Web save.
- `6206007`: three Web saves.

Thus the property’s save portion cannot demonstrate the claimed sightline agreement. This is an existing reducer limitation exposed by the new witness, not a regression introduced by the production change.

**Fix:** explicitly assert the resolved-origin trace is sight-clear and non-total for every attack/save component; add a blocked-save query witness and save-specific mutant. Describe reducer execution as an additional check, without claiming that `force_save` itself enforces LOS.

## Other checks

### Query/execution alignment

The production fix is correct for the stated trace predicate:

- Query: `engine-query-port.ts:1167–1174`, with `sourceAnchor: origin`.
- Attack execution: `encounter.ts:7183–7203`, using the actor’s current position.
- Real proposal execution moves first: `engine-round-session.ts:264–289`.
- Both routes use the same footprint calculation, source/target IDs, creature exclusions, and default optical-blocker inputs through `cover.ts:584–594`.

At the same resulting state, these inputs agree. Movement-triggered state changes can invalidate an earlier proposal; teleporting directly to `finalPosition` does not test those interactions.

Relevant gate audit:

- Resolver action-position constraints delegate to `queries.reach`: `intent-resolver.ts:284`.
- Spell-position sight constraints check `blocksSight`: `:279–281`.
- Ordinary attacks, suspected-square attacks, and spell attacks check sight: `encounter.ts:7201`, `:7491`, `:8568`.
- Sight-required spell targets use canonical detection: `:9484–9488`.
- `force_save` is the exception described above.
- Query-port `:1234/:1547` calculate tactical cover; `:1830` reports cover. They are not authorization gates and correctly remain unchanged.


exec
/bin/bash -lc 'rg -n "blockedCells|MUTANT|save.*column|column =|fullColumn" /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/consensus/vis-field/review-impl-land-01-astra-r1.md | tail -160' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
176:+      blockedCells: openLowerFlank
204:     expect(projectEncounterBoard(projectDmView(walled)).blockedCells).toEqual(walledCells);
350:+      blockedCells: [{ column: 8, row: 5 }, { column: 8, row: 6 }],
354:+      blockedCells: [{ column: 8, row: 5 }],
366:+      line.targetCorner.column === 1 && line.targetCorner.row === 6);
375:+      line.targetCorner.column === 1 && line.targetCorner.row === 6);
788:VERIFIED BY ME (verify-land-01.log): tsc 0/0; both OFFERS checks 0; 0 forbidden, 0 long lines; tests/fixtures untouched; MY MUTANTS — the query port's sight refusal disabled → both witnesses RED (the seam fixture and the 214-offer agreement property), restored 1a552fba…; the generator's predicate relaxed to tier-only → OPEN_RAY_IGNORES_BLOCKS_SIGHT RED, restored 8d85ae64…; 16 suites 299/299 (engine-query-port, r02, dm-tactical-intel, challenge-room, the three offer-environment suites, engine-mcp-handler/server/golden, board-delivery, board-snapshot, SIMULATED, dm-encounter-host-live-path, replay, detection); los-cover 112/112; EXACTLY the 16 expected reds (ten brutal-b productivity pins, six D569 second-family manifest) — the frozen-family consequence that is the owner's question 5; pair 36/36 in 38 s. Committed ebfcacaa (6 files 325+/22−). vis-field = ebfcacaa. Astra (resume 01a0a642…, medium; brief review-impl-land-01-astra.md; log review-land-01-astra-r1.log) reviews LAND-01 on the detached checkout; LAND-02 (fresh sol; brief impl-vis-field-land-02.md; log impl-land-02.log) re-pins vtt-encounter.spec.ts:237 by hand and diagnoses the board-snapshot spec's board-visible wait (D635.38) on the worktree, its Playwright runs on port 4470 only.
855:       for (let column = 0; column < state.bounds.columns; column += 1) {
1321:    blockedCells: state.blockedCells.map((cell) => cell.column === 10 && cell.row === 2
1334:    expect(state.blockedCells).toContainEqual({ column: 9, row: 2 });
1335:    expect(state.blockedCells).not.toContainEqual({ column: 10, row: 2 });
1756:{"encounter":{"provenance":{"licenseTag":"MIT","seed":6206001,"source":"generated"},"schemaVersion":1,"state":{"activeCombatant":null,"activeInitiativeIndex":null,"adjudicationPending":[],"blockedCells":[{"column":11,"row":0},{"column":11,"row":1},{"column":11,"row":2},{"column":11,"row":3},{"column":11,"row":4},{"column":11,"row":5},{"column":11,"row":6},{"column":11,"row":7},{"column":11,"row":8},{"column":11,"row":9},{"column":11,"row":10},{"column":11,"row":11},{"column":11,"row":12},{"column":11,"row":13},{"column":11,"row":15},{"column":11,"row":16},{"column":11,"row":17},{"column":11,"row":18},{"column":11,"row":19},{"column":11,"row":20},{"column":11,"row":21}],"bounds":{"columns":22,"rows":22},"combatants":[{"deathAt":null,"deathSaves":null,"hitPoints":67,"life":"living","profile":{"characterId":1,"id":"combatant:fighter","kind":"player_character","name":"Reference Fighter","rules":{"armorClass":18,"attacksPerAction":2,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":67,"initiativeBonus":100,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[],"usesDeathSaves":true},"tokenId":"token:fighter","wildShape":null},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":39,"life":"living","profile":{"characterId":2,"id":"combatant:cleric","kind":"player_character","name":"Reference Cleric","rules":{"armorClass":18,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":52,"initiativeBonus":70,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:cleric","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":0},{"level":2,"maximum":3,"remaining":1},{"level":3,"maximum":3,"remaining":3},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","profile":{"characterId":3,"id":"combatant:wizard","kind":"player_character","name":"Reference Wizard","rules":{"armorClass":15,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","damageResponses":[],"detectionTraits":[],"hitPointMaximum":38,"initiativeBonus":40,"passivePerception":12,"reach":5,"savingThrowBonuses":{"charisma":0,"constitution":3,"dexterity":2,"intelligence":1,"strength":3,"wisdom":2},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":30,"spellSlots":[{"level":1,"maximum":4},{"level":2,"maximum":3},{"level":3,"maximum":3},{"level":4,"maximum":1}],"usesDeathSaves":true},"tokenId":"token:wizard","wildShape":null},"spellSlots":[{"level":1,"maximum":4,"remaining":3},{"level":2,"maximum":3,"remaining":3},{"level":3,"maximum":3,"remaining":1},{"level":4,"maximum":1,"remaining":1}],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":97,"legendary":{"actionUsesMaximum":3,"actionUsesRemaining":3,"resistanceUsesMaximum":3,"resistanceUsesRemaining":3},"life":"living","limitedResources":[{"id":"monster-spell:spellcasting:calm-emotions","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:spellcasting:dispel-evil-and-good","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:spellcasting:entangle","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:spellcasting:pass-without-trace","maximum":1,"recharge":"long_rest","remaining":1},{"id":"monster-spell:spellcasting:word-of-recall","maximum":1,"recharge":"long_rest","remaining":1}],"profile":{"id":"combatant:generated-6206001-monster-1","kind":"monster","name":"Unicorn","rules":{"abilityScores":{"charisma":16,"constitution":15,"dexterity":14,"intelligence":11,"strength":18,"wisdom":17},"armorClass":12,"attacksPerAction":2,"conditionImmunities":["Charmed","Paralyzed","Poisoned"],"contactMedium":"surface","creatureType":"Celestial","damageResponses":[{"response":"immune","type":"Poison"}],"detectionTraits":[],"hitPointMaximum":97,"initiativeBonus":8,"legendary":{"actionUsesMaximum":3,"actions":[{"attackId":"radiant-horn","avoidsOpportunityAttacks":true,"cost":1,"id":"charging-horn","kind":"move_and_attack","movement":"half_speed","name":"Charging Horn"},{"armorClassBonus":2,"cost":1,"expiresAt":"end_of_monster_next_turn","id":"shimmering-shield","kind":"temporary_defense","name":"Shimmering Shield","rangeFeet":60,"target":"self_or_visible_creature","temporaryHitPoints":{"count":3,"modifier":0,"sides":6},"temporaryHitPointsAverage":10}],"resistanceUsesMaximum":3},"limitedResources":[{"id":"monster-spell:spellcasting:calm-emotions","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:spellcasting:dispel-evil-and-good","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:spellcasting:entangle","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:spellcasting:pass-without-trace","maximum":1,"recharge":"long_rest"},{"id":"monster-spell:spellcasting:word-of-recall","maximum":1,"recharge":"long_rest"}],"magicResistance":true,"passivePerception":13,"proficiencyBonus":3,"reach":5,"savingThrowBonuses":{"charisma":3,"constitution":2,"dexterity":2,"intelligence":0,"strength":4,"wisdom":3},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Large","speed":50,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:unicorn","tokenId":"token:generated-6206001-monster-1"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":45,"life":"living","profile":{"id":"combatant:generated-6206001-monster-2","kind":"monster","name":"Minotaur Skeleton","rules":{"abilityScores":{"charisma":5,"constitution":15,"dexterity":11,"intelligence":6,"strength":18,"wisdom":8},"armorClass":12,"attacksPerAction":1,"conditionImmunities":["Exhaustion","Poisoned"],"contactMedium":"surface","creatureType":"Undead","damageResponses":[{"response":"vulnerable","type":"Bludgeoning"},{"response":"immune","type":"Poison"}],"detectionTraits":[],"hitPointMaximum":45,"initiativeBonus":0,"passivePerception":9,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-3,"constitution":2,"dexterity":0,"intelligence":-2,"strength":4,"wisdom":-1},"senses":[{"kind":"normal_sight"},{"kind":"darkvision","rangeFeet":60}],"sizeCategory":"Large","speed":40,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:minotaur-skeleton","tokenId":"token:generated-6206001-monster-2"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":38,"life":"living","profile":{"id":"combatant:generated-6206001-monster-3","kind":"monster","name":"Ironweb Weaver","rules":{"abilityScores":{"charisma":6,"constitution":15,"dexterity":15,"intelligence":3,"strength":18,"wisdom":12},"armorClass":14,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":["web_sense"],"hitPointMaximum":38,"initiativeBonus":2,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":2,"dexterity":2,"intelligence":-4,"strength":4,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Large","speed":36,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/ironweb-weaver","tokenId":"token:generated-6206001-monster-3"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null},{"deathAt":null,"deathSaves":null,"hitPoints":19,"life":"living","profile":{"id":"combatant:generated-6206001-monster-4","kind":"monster","name":"Crested Charger","rules":{"abilityScores":{"charisma":6,"constitution":13,"dexterity":13,"intelligence":3,"strength":14,"wisdom":12},"armorClass":12,"attacksPerAction":1,"conditionImmunities":[],"contactMedium":"surface","creatureType":"Beast","damageResponses":[],"detectionTraits":[],"hitPointMaximum":19,"initiativeBonus":1,"passivePerception":11,"proficiencyBonus":2,"reach":5,"savingThrowBonuses":{"charisma":-2,"constitution":1,"dexterity":1,"intelligence":-4,"strength":2,"wisdom":1},"senses":[{"kind":"normal_sight"}],"sizeCategory":"Medium","speed":32,"spellSlots":[],"usesDeathSaves":false},"statblockId":"statblock:homebrew-beast/crested-charger","tokenId":"token:generated-6206001-monster-4"},"spellSlots":[],"temporaryHitPoints":0,"turn":{"action":{"kind":"spent"},"bonusActionAvailable":false,"disengaging":false,"dodging":false,"movement":{"remaining":0,"speed":0,"spent":0},"reactionAvailable":false},"wildShapeUses":null}],"config":{"initiativeMode":"shared_enemy"},"dmNotes":["Deterministic generated room seed 6206001."],"effects":[],"environment":{"difficultTerrainRegions":[{"cells":[{"column":9,"row":13},{"column":9,"row":14},{"column":9,"row":15},{"column":10,"row":13},{"column":10,"row":14},{"column":10,"row":15},{"column":12,"row":13},{"column":12,"row":14},{"column":12,"row":15},{"column":13,"row":13},{"column":13,"row":14},{"column":13,"row":15}],"id":"difficult:generated-6206001-approach"},{"cells":[{"column":4,"row":5},{"column":5,"row":5},{"column":6,"row":5},{"column":7,"row":5},{"column":4,"row":6},{"column":5,"row":6},{"column":6,"row":6},{"column":7,"row":6},{"column":4,"row":7},{"column":5,"row":7},{"column":6,"row":7},{"column":7,"row":7}],"id":"difficult:generated-6206001-brutal-1"},{"cells":[{"column":12,"row":9},{"column":13,"row":9},{"column":14,"row":9},{"column":15,"row":9},{"column":12,"row":10},{"column":13,"row":10},{"column":14,"row":10},{"column":15,"row":10},{"column":12,"row":11},{"column":13,"row":11},{"column":14,"row":11},{"column":15,"row":11},{"column":12,"row":12},{"column":13,"row":12},{"column":14,"row":12},{"column":15,"row":12}],"id":"difficult:generated-6206001-brutal-2"},{"cells":[{"column":4,"row":17},{"column":5,"row":17},{"column":6,"row":17},{"column":7,"row":17},{"column":4,"row":18},{"column":5,"row":18},{"column":6,"row":18},{"column":7,"row":18},{"column":4,"row":19},{"column":5,"row":19},{"column":6,"row":19},{"column":7,"row":19},{"column":4,"row":20},{"column":5,"row":20},{"column":6,"row":20},{"column":7,"row":20}],"id":"difficult:generated-6206001-brutal-3"}],"lightRegions":[{"cells":[{"column":15,"row":16},{"column":17,"row":16},{"column":18,"row":16},{"column":15,"row":17},{"column":16,"row":17},{"column":17,"row":17},{"column":18,"row":17},{"column":15,"row":18},{"column":16,"row":18},{"column":17,"row":18},{"column":18,"row":18},{"column":15,"row":19},{"column":16,"row":19},{"column":17,"row":19},{"column":18,"row":19}],"id":"light:world-object:generated-6206001-5","level":"dim"},{"cells":[{"column":4,"row":5},{"column":5,"row":5},{"column":6,"row":5},{"column":7,"row":5},{"column":4,"row":6},{"column":5,"row":6},{"column":6,"row":6},{"column":7,"row":6},{"column":4,"row":7},{"column":5,"row":7},{"column":6,"row":7},{"column":7,"row":7}],"id":"light:world-object:generated-6206001-6","level":"dim"},{"cells":[{"column":14,"row":14},{"column":15,"row":14},{"column":14,"row":15},{"column":15,"row":15}],"id":"light:world-object:generated-6206001-7","level":"bright"}],"movementRegions":[],"narrowOpeningRegions":[],"obscurementRegions":[{"cells":[{"column":3,"row":18},{"column":4,"row":18},{"column":3,"row":19},{"column":4,"row":19},{"column":3,"row":20},{"column":4,"row":20}],"id":"obscurement:generated-6206001-1","obscurement":"light"},{"cells":[{"column":15,"row":17},{"column":16,"row":17},{"column":15,"row":18},{"column":16,"row":18},{"column":15,"row":19},{"column":16,"row":19},{"column":15,"row":20},{"column":16,"row":20}],"id":"obscurement:generated-6206001-2","obscurement":"light"},{"cells":[{"column":6,"row":1},{"column":7,"row":1},{"column":8,"row":1},{"column":9,"row":1},{"column":6,"row":2},{"column":7,"row":2},{"column":8,"row":2},{"column":9,"row":2},{"column":6,"row":3},{"column":7,"row":3},{"column":8,"row":3},{"column":9,"row":3}],"id":"obscurement:generated-6206001-3","obscurement":"heavy"}]},"eventLog":[],"foggedCells":[],"hiddenCombatants":[],"hiddenRolls":[],"initiative":[],"nextDecisionSequence":1,"nextEffectSequence":1,"nextEventSequence":1,"nextPersistentAreaSequence":1,"nextWorldObjectSequence":1,"observationHistory":[],"pendingDecisions":[],"persistentAreas":[],"phase":{"kind":"active"},"reactionPolicies":[],"revision":0,"round":0,"rulesEdition":"2024","sharedSpaceRelations":[],"tokens":[{"combatantId":"combatant:fighter","id":"token:fighter","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":2}},{"combatantId":"combatant:cleric","id":"token:cleric","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":2,"row":4}},{"combatantId":"combatant:wizard","id":"token:wizard","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":1,"row":6}},{"combatantId":"combatant:generated-6206001-monster-1","id":"token:generated-6206001-monster-1","placementMode":{"actual":"Large","kind":"normal"},"position":{"column":20,"row":1}},{"combatantId":"combatant:generated-6206001-monster-2","id":"token:generated-6206001-monster-2","placementMode":{"actual":"Large","kind":"normal"},"position":{"column":18,"row":0}},{"combatantId":"combatant:generated-6206001-monster-3","id":"token:generated-6206001-monster-3","placementMode":{"actual":"Large","kind":"normal"},"position":{"column":20,"row":3}},{"combatantId":"combatant:generated-6206001-monster-4","id":"token:generated-6206001-monster-4","placementMode":{"actual":"Medium","kind":"normal"},"position":{"column":19,"row":3}}],"worldObjects":[{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":12}],"id":"world-object:generated-6206001-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":12}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":14,"row":10}],"id":"world-object:generated-6206001-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":14,"row":10}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":16,"row":16}],"id":"world-object:generated-6206001-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":16,"row":16}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":6,"row":8}],"id":"world-object:generated-6206001-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":6,"row":8}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":2,"row":19}],"id":"world-object:generated-6206001-5","kind":"light-source","name":"Runed Brazier","position":{"column":2,"row":19}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":17,"row":1}],"id":"world-object:generated-6206001-6","kind":"light-source","name":"Runed Brazier","position":{"column":17,"row":1}},{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":4}],"id":"world-object:generated-6206001-7","kind":"light-source","name":"Runed Brazier","position":{"column":1,"row":4}}]}},"spec":{"blockedCells":[{"column":11,"row":0},{"column":11,"row":1},{"column":11,"row":2},{"column":11,"row":3},{"column":11,"row":4},{"column":11,"row":5},{"column":11,"row":6},{"column":11,"row":7},{"column":11,"row":8},{"column":11,"row":9},{"column":11,"row":10},{"column":11,"row":11},{"column":11,"row":12},{"column":11,"row":13},{"column":11,"row":15},{"column":11,"row":16},{"column":11,"row":17},{"column":11,"row":18},{"column":11,"row":19},{"column":11,"row":20},{"column":11,"row":21}],"challengeBudgetEighths":76,"challengeSpentEighths":76,"difficultyProfile":"brutal","dimensions":{"columns":22,"rows":22},"hardFeatures":{"chokepointCells":[{"column":11,"row":14}],"likelyApproachTerrainRegionIds":["difficult:generated-6206001-approach"],"shape":"single-gate"},"monsterRoster":[{"challengeEighths":40,"challengeRating":5,"combatantId":"combatant:generated-6206001-monster-1","statblockId":"statblock:unicorn","tokenId":"token:generated-6206001-monster-1"},{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-6206001-monster-2","statblockId":"statblock:minotaur-skeleton","tokenId":"token:generated-6206001-monster-2"},{"challengeEighths":16,"challengeRating":2,"combatantId":"combatant:generated-6206001-monster-3","statblockId":"statblock:homebrew-beast/ironweb-weaver","tokenId":"token:generated-6206001-monster-3"},{"challengeEighths":4,"challengeRating":"1/2","combatantId":"combatant:generated-6206001-monster-4","statblockId":"statblock:homebrew-beast/crested-charger","tokenId":"token:generated-6206001-monster-4"}],"partyState":[{"combatantId":"combatant:fighter","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":67,"life":"living","spellSlots":[]},{"combatantId":"combatant:cleric","concentrating":false,"deathSaves":null,"hitPointFraction":0.75,"hitPoints":39,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":0,"spent":4},{"level":2,"maximum":3,"remaining":1,"spent":2},{"level":3,"maximum":3,"remaining":3,"spent":0},{"level":4,"maximum":1,"remaining":1,"spent":0}]},{"combatantId":"combatant:wizard","concentrating":false,"deathSaves":null,"hitPointFraction":1,"hitPoints":38,"life":"living","spellSlots":[{"level":1,"maximum":4,"remaining":3,"spent":1},{"level":2,"maximum":3,"remaining":3,"spent":0},{"level":3,"maximum":3,"remaining":1,"spent":2},{"level":4,"maximum":1,"remaining":1,"spent":0}]}],"seed":6206001,"terrain":[{"cells":[{"column":9,"row":13},{"column":9,"row":14},{"column":9,"row":15},{"column":10,"row":13},{"column":10,"row":14},{"column":10,"row":15},{"column":12,"row":13},{"column":12,"row":14},{"column":12,"row":15},{"column":13,"row":13},{"column":13,"row":14},{"column":13,"row":15}],"id":"difficult:generated-6206001-approach","kind":"difficult-terrain-patch"},{"cells":[{"column":4,"row":5},{"column":5,"row":5},{"column":6,"row":5},{"column":7,"row":5},{"column":4,"row":6},{"column":5,"row":6},{"column":6,"row":6},{"column":7,"row":6},{"column":4,"row":7},{"column":5,"row":7},{"column":6,"row":7},{"column":7,"row":7}],"id":"difficult:generated-6206001-brutal-1","kind":"difficult-terrain-patch"},{"cells":[{"column":12,"row":9},{"column":13,"row":9},{"column":14,"row":9},{"column":15,"row":9},{"column":12,"row":10},{"column":13,"row":10},{"column":14,"row":10},{"column":15,"row":10},{"column":12,"row":11},{"column":13,"row":11},{"column":14,"row":11},{"column":15,"row":11},{"column":12,"row":12},{"column":13,"row":12},{"column":14,"row":12},{"column":15,"row":12}],"id":"difficult:generated-6206001-brutal-2","kind":"difficult-terrain-patch"},{"cells":[{"column":4,"row":17},{"column":5,"row":17},{"column":6,"row":17},{"column":7,"row":17},{"column":4,"row":18},{"column":5,"row":18},{"column":6,"row":18},{"column":7,"row":18},{"column":4,"row":19},{"column":5,"row":19},{"column":6,"row":19},{"column":7,"row":19},{"column":4,"row":20},{"column":5,"row":20},{"column":6,"row":20},{"column":7,"row":20}],"id":"difficult:generated-6206001-brutal-3","kind":"difficult-terrain-patch"},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":5,"row":12}],"id":"world-object:generated-6206001-1","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":5,"row":12}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":14,"row":10}],"id":"world-object:generated-6206001-2","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":14,"row":10}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":16,"row":16}],"id":"world-object:generated-6206001-3","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":16,"row":16}}},{"kind":"hazard-object","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":6,"row":8}],"id":"world-object:generated-6206001-4","kind":"hazard","name":"Unstable Arcane Vent","position":{"column":6,"row":8}}},{"cells":[{"column":15,"row":16},{"column":17,"row":16},{"column":18,"row":16},{"column":15,"row":17},{"column":16,"row":17},{"column":17,"row":17},{"column":18,"row":17},{"column":15,"row":18},{"column":16,"row":18},{"column":17,"row":18},{"column":18,"row":18},{"column":15,"row":19},{"column":16,"row":19},{"column":17,"row":19},{"column":18,"row":19}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":2,"row":19}],"id":"world-object:generated-6206001-5","kind":"light-source","name":"Runed Brazier","position":{"column":2,"row":19}}},{"cells":[{"column":4,"row":5},{"column":5,"row":5},{"column":6,"row":5},{"column":7,"row":5},{"column":4,"row":6},{"column":5,"row":6},{"column":6,"row":6},{"column":7,"row":6},{"column":4,"row":7},{"column":5,"row":7},{"column":6,"row":7},{"column":7,"row":7}],"kind":"light-source","level":"dim","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":17,"row":1}],"id":"world-object:generated-6206001-6","kind":"light-source","name":"Runed Brazier","position":{"column":17,"row":1}}},{"cells":[{"column":14,"row":14},{"column":15,"row":14},{"column":14,"row":15},{"column":15,"row":15}],"kind":"light-source","level":"bright","object":{"armorClass":15,"blocking":{"cover":"none","lineOfSight":false,"movement":false},"createdRevision":0,"damageResponses":[],"durability":{"kind":"indestructible"},"footprint":[{"column":1,"row":4}],"id":"world-object:generated-6206001-7","kind":"light-source","name":"Runed Brazier","position":{"column":1,"row":4}}},{"cells":[{"column":3,"row":18},{"column":4,"row":18},{"column":3,"row":19},{"column":4,"row":19},{"column":3,"row":20},{"column":4,"row":20}],"id":"obscurement:generated-6206001-1","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":15,"row":17},{"column":16,"row":17},{"column":15,"row":18},{"column":16,"row":18},{"column":15,"row":19},{"column":16,"row":19},{"column":15,"row":20},{"column":16,"row":20}],"id":"obscurement:generated-6206001-2","kind":"obscurement-patch","obscurement":"light"},{"cells":[{"column":6,"row":1},{"column":7,"row":1},{"column":8,"row":1},{"column":9,"row":1},{"column":6,"row":2},{"column":7,"row":2},{"column":8,"row":2},{"column":9,"row":2},{"column":6,"row":3},{"column":7,"row":3},{"column":8,"row":3},{"column":9,"row":3}],"id":"obscurement:generated-6206001-3","kind":"obscurement-patch","obscurement":"heavy"}]}}
1826:/bin/bash -lc "rg -n 'function processForceSave|processForceSave|force_save' src/combat/encounter.ts | tail -8; rg -n 'monsterAttackCommand|multiattackComponent' src/vtt --glob '*.ts' | head -35; sed -n '570,595p' src/vtt/intent-resolver.ts; sed -n '9030,9060p' src/combat/encounter.ts; rg -n 'difficulty|terrainProfile' tests/fixtures/arena-basis-brutal-b/seed-6206001.json --max-columns 180 --max-columns-preview" in /home/vagrant/PhpstormProjects/dnd-wt-seam-verify
2174:dict_keys(['blockedCells', 'challengeBudgetEighths', 'challengeSpentEighths', 'difficultyProfile', 'dimensions', 'hardFeatures', 'monsterRoster', 'partyState', 'seed', 'terrain'])
2265:      for (let column = 0; column < state.bounds.columns; column += 1) {
2726:const files=[\"tests/unit/vtt/engine-query-port.test.ts\"];const ctx=await startVitest('test',files,{config:false,run:true,pool:'threads',environment:'node',isolate:false,maxWorkers:1,cache:false,experimental:{viteModuleRunner:true},reporters:['default',{onUserConsoleLog(log){process.stdout.write(log.content+'\\n')}}],testNamePattern:\"offered attacks and saves\"},{configFile:false,server:{watch:null},test:{include:files},plugins:[{name:'review-memory',enforce:'pre',transform(code,id){if(id.endsWith('/tests/unit/vtt/engine-query-port.test.ts'))return code.replace(\"const executionState = stateAtResolvedOrigin(\", \"let executionState = stateAtResolvedOrigin(\").replace(\"const sources = [\", \"if(use.kind==='saving_throw'){ const s=executionState.tokens.find(t=>t.combatantId===monster.profile.id).position; const t=executionState.tokens.find(t=>t.combatantId===targetId).position; const x=Math.floor((s.column+t.column)/2);executionState={...executionState,blockedCells:[...executionState.blockedCells,...Array.from({length:executionState.bounds.rows},(_,row)=>({column:x,row}))]};expect(traceCombatantLine(executionState,monster.profile.id,targetId).blocksSight).toBe(true);console.log('BLOCKED_SAVE_STILL_EXECUTES',path,use.actionId); } const sources = [\");}}]});await ctx?.close();
3539:  1241	      for (let column = 0; column < state.bounds.columns; column += 1) {

exec
/bin/bash -lc "rg -n \"function projectDmView|export function projectDmView|concealedCells|foggedCells\" src/combat/visibility.ts src/combat/encounter.ts src/vtt/encounter-projections.ts | head -180
nl -ba src/combat/visibility.ts | sed -n '485,550p'
rg -n \"interface DmView|type DmView\" src/combat/visibility.ts src/combat/*.ts
rg -n \"createEncounter\\(|playerProfile\\(|placedToken\\(\" tests/unit/tools/ai-dm-board-snapshot.test.ts tests/unit/tools/*.test.ts | head -80" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
src/vtt/encounter-projections.ts:118:  readonly concealedCells: readonly GridCell[];
src/vtt/encounter-projections.ts:368:    concealedCells: view.concealedCells.map((cell) => ({ ...cell })),
src/combat/visibility.ts:93:  readonly foggedCells: readonly GridCell[];
src/combat/visibility.ts:288:  readonly concealedCells: readonly GridCell[];
src/combat/visibility.ts:315:    readonly foggedCells: readonly GridCell[];
src/combat/visibility.ts:506:export function projectDmView(state: EncounterState): DmView {
src/combat/visibility.ts:524:      foggedCells: derivedFogCells(state, visibilityObserverIds),
src/combat/visibility.ts:650:    concealedCells: allCells(state.bounds).filter((cell) => concealed.has(cellKey(cell))),
src/combat/visibility.ts:735:      foggedCells: state.foggedCells.map((cell) => ({ ...cell })),
   485	      event.type === 'effect_duration_extended'
   486	    ) return [];
   487	    if (event.type === 'initiative_ordered') {
   488	      return [{
   489	        ...event,
   490	        order: event.order.filter((id) => visibleIds.has(id)),
   491	        slots: event.slots
   492	          .map((slot) => slot.filter((id) => visibleIds.has(id)))
   493	          .filter((slot) => slot.length > 0),
   494	      }];
   495	    }
   496	    if (event.type === 'adjudicated') {
   497	      return visibleIds.has(event.target)
   498	        ? [{ sequence: event.sequence, type: 'adjudicated', target: event.target, consequence: event.consequence }]
   499	        : [];
   500	    }
   501	    return eventCombatants(event).every((id) => visibleIds.has(id)) ? [event] : [];
   502	  });
   503	}
   504	
   505	/** The only canonical EncounterState -> DM projection function. */
   506	export function projectDmView(state: EncounterState): DmView {
   507	  const visibilityObserverIds = eligibleVisibilityObserverIds(state, { kind: 'dm_party' });
   508	  return {
   509	    audience: 'dm',
   510	    state: {
   511	      config: structuredClone(state.config),
   512	      phase: structuredClone(state.phase),
   513	      rulesEdition: state.rulesEdition,
   514	      hiddenRolls: [...state.hiddenRolls],
   515	      revision: state.revision,
   516	      nextEventSequence: state.nextEventSequence,
   517	      nextDecisionSequence: state.nextDecisionSequence,
   518	      nextEffectSequence: state.nextEffectSequence,
   519	      bounds: structuredClone(state.bounds),
   520	      blockedCells: structuredClone(state.blockedCells),
   521	      worldObjects: structuredClone(state.worldObjects),
   522	      nextWorldObjectSequence: state.nextWorldObjectSequence,
   523	      environment: structuredClone(state.environment),
   524	      foggedCells: derivedFogCells(state, visibilityObserverIds),
   525	      dmNotes: [...state.dmNotes],
   526	      combatants: structuredClone(state.combatants),
   527	      tokens: structuredClone(state.tokens),
   528	      sharedSpaceRelations: structuredClone(state.sharedSpaceRelations),
   529	      adjudicationPending: structuredClone(state.adjudicationPending),
   530	      ...(state.absentTokens === undefined ? {} : { absentTokens: structuredClone(state.absentTokens) }),
   531	      initiative: structuredClone(state.initiative),
   532	      activeCombatant: state.activeCombatant,
   533	      activeInitiativeIndex: state.activeInitiativeIndex,
   534	      round: state.round,
   535	      effects: structuredClone(state.effects),
   536	      persistentAreas: structuredClone(state.persistentAreas),
   537	      nextPersistentAreaSequence: state.nextPersistentAreaSequence,
   538	      ...(state.reevaluatedBranches === undefined ? {} : { reevaluatedBranches: structuredClone(state.reevaluatedBranches) }),
   539	      eventLog: structuredClone(state.eventLog),
   540	      hiddenCombatants: structuredClone(state.hiddenCombatants),
   541	      observationHistory: structuredClone(state.observationHistory),
   542	      ...(state.searchMemories === undefined ? {} : { searchMemories: structuredClone(state.searchMemories) }),
   543	      ...(state.alerting === undefined ? {} : { alerting: structuredClone(state.alerting) }),
   544	      pendingDecisions: structuredClone(state.pendingDecisions),
   545	      reactionPolicies: structuredClone(state.reactionPolicies),
   546	      ...(state.contentPacks === undefined ? {} : { contentPacks: structuredClone(state.contentPacks) }),
   547	      ...(state.equipment === undefined ? {} : { equipment: structuredClone(state.equipment) }),
   548	      ...(state.groundItems === undefined ? {} : { groundItems: structuredClone(state.groundItems) }),
   549	      ...(state.itemContacts === undefined ? {} : { itemContacts: structuredClone(state.itemContacts) }),
   550	    },
src/combat/visibility.ts:97:export interface DmView {
src/combat/visibility.ts:97:export interface DmView {
tests/unit/tools/ai-dm-screenshot-probe.test.ts:128:  const hero = playerProfile('screenshot-hero', { hitPoints: 12 });
tests/unit/tools/ai-dm-screenshot-probe.test.ts:130:  const state = createEncounter({
tests/unit/tools/ai-dm-screenshot-probe.test.ts:133:    tokens: [placedToken(hero, 1, 1), placedToken(foe, 2, 2)],
tests/unit/tools/ai-dm-screenshot-probe.test.ts:730:    const fixtureActor = playerProfile('primer-feature-actor');
tests/unit/tools/ai-dm-screenshot-probe.test.ts:731:    const featureState = createEncounter({
tests/unit/tools/ai-dm-screenshot-probe.test.ts:734:      tokens: [placedToken(fixtureActor, 5, 3)],
tests/unit/tools/ai-dm-screenshot-probe.test.ts:805:    const wallState = createEncounter({
tests/unit/tools/ai-dm-screenshot-probe.test.ts:808:      tokens: [placedToken(fixtureActor, 5, 5)],
tests/unit/tools/ai-dm-screenshot-probe.test.ts:841:    const creatureSource = playerProfile('primer-creature-source');
tests/unit/tools/ai-dm-screenshot-probe.test.ts:844:    const creatureState = createEncounter({
tests/unit/tools/ai-dm-screenshot-probe.test.ts:848:        placedToken(creatureSource, 0, 1),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:849:        placedToken(creatureMiddle, 2, 1),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:850:        placedToken(creatureTarget, 4, 1),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:880:    const baseLargeSource = playerProfile('primer-large-source');
tests/unit/tools/ai-dm-screenshot-probe.test.ts:886:    const largeState = createEncounter({
tests/unit/tools/ai-dm-screenshot-probe.test.ts:889:      tokens: [placedToken(largeSource, 0, 0), placedToken(largeTarget, 6, 0)],
tests/unit/tools/ai-dm-arena.test.ts:709:    const player = playerProfile('arena-distance-player');
tests/unit/tools/ai-dm-arena.test.ts:710:    const state = createEncounter({
tests/unit/tools/ai-dm-arena.test.ts:713:      tokens: [placedToken(monster, 0), placedToken(player, 1)],
tests/unit/tools/ai-dm-arena.test.ts:1324:    const character = playerProfile('hidden-option-character', { initiativeBonus: 20 });
tests/unit/tools/ai-dm-arena.test.ts:1325:    const fixtureState = createEncounter({
tests/unit/tools/ai-dm-arena.test.ts:1328:      tokens: [placedToken(monster, 0), placedToken(character, 2)],
tests/unit/tools/ai-dm-arena.test.ts:1783:      const character = playerProfile('full-intel-rep-character', { initiativeBonus: 20 });
tests/unit/tools/ai-dm-arena.test.ts:1785:        ...createEncounter({
tests/unit/tools/ai-dm-arena.test.ts:1788:          tokens: [placedToken(monster, 0), placedToken(character, 2)],
tests/unit/tools/los-cover-era-audit.test.ts:26:    const source = playerProfile('era-source');
tests/unit/tools/los-cover-era-audit.test.ts:30:      tokens: [placedToken(source, 0), placedToken(target, 4)],
tests/unit/tools/los-cover-era-audit.test.ts:32:    const blockedCell = createEncounter({ ...base, blockedCells: [{ column: 2, row: 0 }] });
tests/unit/tools/los-cover-era-audit.test.ts:35:    const objectWall = createEncounter({ ...base, worldObjects: [wallObject()] });
tests/unit/tools/los-cover-era-audit.test.ts:38:    const intervening = playerProfile('era-intervening');
tests/unit/tools/los-cover-era-audit.test.ts:39:    const creatureCover = createEncounter({
tests/unit/tools/los-cover-era-audit.test.ts:42:      tokens: [placedToken(source, 0), placedToken(intervening, 2), placedToken(target, 4)],
tests/unit/tools/los-cover-era-audit.test.ts:48:    const left = playerProfile('era-left');
tests/unit/tools/los-cover-era-audit.test.ts:49:    const middle = playerProfile('era-middle');
tests/unit/tools/los-cover-era-audit.test.ts:51:    const state = createEncounter({
tests/unit/tools/los-cover-era-audit.test.ts:53:      tokens: [placedToken(left, 0, 0), placedToken(middle, 0, 4), placedToken(right, 6, 2)],
tests/unit/tools/los-cover-era-audit.test.ts:85:    const leftFixture = playerProfile('era-unchanged-left');
tests/unit/tools/los-cover-era-audit.test.ts:87:    const unchanged = createEncounter({
tests/unit/tools/los-cover-era-audit.test.ts:89:      tokens: [placedToken(leftFixture, 0), placedToken(rightFixture, 2)],
tests/unit/tools/los-cover-era-audit.test.ts:93:      { family: 'pool', seed: 2, room: 2, fixtureHash: 'b'.repeat(64), state: createEncounter({
tests/unit/tools/los-cover-era-audit.test.ts:95:        tokens: [placedToken(leftFixture, 0), placedToken(rightFixture, 2)],
tests/unit/tools/scrape-content-pack.test.ts:218:    const caster = playerProfile('scrape-pack-caster', {
tests/unit/tools/scrape-content-pack.test.ts:226:    let state = createEncounter({
tests/unit/tools/scrape-content-pack.test.ts:229:      tokens: [placedToken(caster, 0), placedToken(target, 1)],
tests/unit/tools/ai-dm-knowledge-base.test.ts:407:    const initial = createEncounter({
tests/unit/tools/ai-dm-knowledge-base.test.ts:410:      tokens: [placedToken(profile, 0)],
tests/unit/tools/ai-dm-knowledge-base.test.ts:560:    const tracer = playerProfile('guide-feature-tracer');
tests/unit/tools/ai-dm-knowledge-base.test.ts:561:    const featureState = createEncounter({
tests/unit/tools/ai-dm-knowledge-base.test.ts:564:      tokens: [placedToken(tracer, 5, 3)],
tests/unit/tools/ai-dm-knowledge-base.test.ts:571:    const source = playerProfile('guide-source');
tests/unit/tools/ai-dm-knowledge-base.test.ts:572:    const screen = playerProfile('guide-screen');
tests/unit/tools/ai-dm-knowledge-base.test.ts:573:    const target = playerProfile('guide-target');
tests/unit/tools/ai-dm-knowledge-base.test.ts:574:    const creatureState = createEncounter({
tests/unit/tools/ai-dm-knowledge-base.test.ts:577:      tokens: [placedToken(source, 0, 1), placedToken(screen, 2, 1), placedToken(target, 4, 1)],
tests/unit/tools/ai-dm-knowledge-base.test.ts:580:    const largeBase = playerProfile('guide-large-source');
tests/unit/tools/ai-dm-knowledge-base.test.ts:582:    const largeTarget = playerProfile('guide-large-target');
tests/unit/tools/ai-dm-knowledge-base.test.ts:583:    const largeState = createEncounter({
tests/unit/tools/ai-dm-knowledge-base.test.ts:586:      tokens: [placedToken(large, 0, 0), placedToken(largeTarget, 6, 0)],
tests/unit/tools/ai-dm-conversation.test.ts:879:  const mover = playerProfile('arena-reaction-mover', { hitPoints: 100, initiativeBonus: 20 });
tests/unit/tools/ai-dm-conversation.test.ts:881:  const started = reduceEncounter(createEncounter({
tests/unit/tools/ai-dm-conversation.test.ts:884:    tokens: [placedToken(mover, 1), placedToken(reactor, 0)],
tests/unit/tools/ai-dm-conversation.test.ts:900:  const player = playerProfile('context-trim-player', { initiativeBonus: 20 });
tests/unit/tools/ai-dm-conversation.test.ts:903:  return reduceEncounter(createEncounter({
tests/unit/tools/ai-dm-conversation.test.ts:907:      placedToken(player, 49),
tests/unit/tools/ai-dm-conversation.test.ts:908:      ...monsters.map((monster, index) => placedToken(monster, index)),
tests/unit/tools/ai-dm-conversation.test.ts:2930:    const hiddenTarget = playerProfile('hidden-options-target', { initiativeBonus: -20 });
tests/unit/tools/ai-dm-conversation.test.ts:2931:    const state = createEncounter({
tests/unit/tools/ai-dm-conversation.test.ts:2934:      tokens: [placedToken(hiddenActor, 0), placedToken(hiddenTarget, 2)],

exec
/bin/bash -lc "sed -n '110,155p' tests/unit/tools/ai-dm-screenshot-probe.test.ts
sed -n '790,825p' tests/unit/tools/ai-dm-screenshot-probe.test.ts
rg -n \"function createEncounter|export function createEncounter|interface CreateEncounter\" src/combat/encounter.ts | head -30
nl -ba src/combat/encounter.ts | sed -n '3160,3245p'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
    id: worldObjectId(`world-object:${id}`),
    name: id,
    kind: 'cover',
    position,
    footprint: cells,
    durability: { kind: 'indestructible' },
    armorClass: armorClass(15),
    damageResponses: [],
    blocking: {
      movement: cover !== 'half',
      lineOfSight: false,
      cover,
    },
    createdRevision: 0,
  };
}

function everyClassState(): EncounterState {
  const hero = playerProfile('screenshot-hero', { hitPoints: 12 });
  const foe = monsterProfile('screenshot-foe', { hitPoints: 12 });
  const state = createEncounter({
    bounds: { columns: 6, rows: 4 },
    combatants: [hero, foe],
    tokens: [placedToken(hero, 1, 1), placedToken(foe, 2, 2)],
    blockedCells: [{ column: 5, row: 3 }],
    worldObjects: [
      object('open-door', 'Open Oak Door', 'door', 0, 2, false),
      object('closed-door', 'Closed Iron Door', 'door', 5, 2, true),
      object('statue', 'Moon Statue', 'generic', 3, 2, false),
    ],
    environment: {
      difficultTerrainRegions: [{ id: 'mud', cells: [{ column: 0, row: 1 }] }],
      obscurementRegions: [
        { id: 'smoke', obscurement: 'heavy', cells: [{ column: 3, row: 1 }] },
      ],
      lightRegions: [
        { id: 'dim', level: 'dim', cells: [{ column: 1, row: 0 }] },
        { id: 'dark', level: 'darkness', cells: [{ column: 2, row: 0 }] },
      ],
      movementRegions: [],
      narrowOpeningRegions: [],
    },
  });
  return {
    ...state,
    combatants: state.combatants.map((entry) =>
      ],
    };
    const fourHalfTrace = traceTerrainLine(
      fourHalfState,
      { column: 0, row: 0 },
      { column: 4, row: 2 },
    );
    expect(fourHalfTrace.lines.map((line) => line.tier)).toEqual([
      'half',
      'half',
      'half',
      'half',
    ]);
    expect(fourHalfTrace).toMatchObject({ tier: 'half', blocksSight: false });

    const wallState = createEncounter({
      bounds: { columns: 6, rows: 6 },
      combatants: [fixtureActor],
      tokens: [placedToken(fixtureActor, 5, 5)],
      blockedCells: [0, 1, 2, 3, 4, 5].map((row) => ({ column: 1, row })),
    });
    expect(
      traceTerrainLine(
        wallState,
        { column: 0, row: 0 },
        { column: 4, row: 2 },
      ),
    ).toMatchObject({ tier: 'total', blocksSight: true });
    const endpointFeatureState = {
      ...featureState,
      worldObjects: [
        coverObject(
          'primer-source-endpoint',
          [{ column: 0, row: 0 }],
          'three_quarters',
        ),
1147:export function createEncounter(setup: EncounterSetup): EncounterState {
  3160	    sharedSpaceRelations: withCombatant.sharedSpaceRelations.filter((relation) =>
  3161	      relation.first !== target && relation.second !== target),
  3162	  };
  3163	}
  3164	
  3165	function revertWildShape(
  3166	  context: ReductionContext,
  3167	  target: CombatantId,
  3168	  reason: WildShapeReversionReason,
  3169	  excessDamage = 0,
  3170	): void {
  3171	  const subject = combatant(context.state, target);
  3172	  const overlay = subject.wildShape;
  3173	  if (overlay === undefined) return;
  3174	  const { wildShape: _wildShape, ...trueForm } = subject;
  3175	  context.state = stateAfterSizeTransition(context.state, target, trueForm);
  3176	  reevaluatePersistentAreaMembership(context);
  3177	  emit(context, {
  3178	    type: 'wild_shape_reverted',
  3179	    combatant: target,
  3180	    formId: overlay.formId,
  3181	    reason,
  3182	    excessDamage,
  3183	  });
  3184	}
  3185	
  3186	function revertExpiredWildShapes(context: ReductionContext, round: number): void {
  3187	  for (const subject of [...context.state.combatants]) {
  3188	    if (subject.wildShape !== undefined && round >= subject.wildShape.expiresAtRound) {
  3189	      revertWildShape(context, subject.profile.id, 'duration_expired');
  3190	    }
  3191	  }
  3192	}
  3193	
  3194	function assumeWildShape(
  3195	  context: ReductionContext,
  3196	  command: Extract<EncounterCommand, { readonly type: 'assume_wild_shape' }>,
  3197	): void {
  3198	  const subject = assertActiveActor(context, command.actor);
  3199	  if (subject.profile.kind !== 'player_character') {
  3200	    throw new WildShapeRuleError('not_player_character', null, 'Only a player character can use Wild Shape.');
  3201	  }
  3202	  const feature = subject.profile.wildShape;
  3203	  if (feature === null || subject.wildShapeUses === null) {
  3204	    throw new WildShapeRuleError('feature_unavailable', null, `Combatant ${command.actor} has no configured Wild Shape feature.`);
  3205	  }
  3206	  if (subject.form !== undefined) {
  3207	    throw new WildShapeRuleError('feature_unavailable', null, 'Wild Shape cannot overlay another replacement form.');
  3208	  }
  3209	  if (subject.wildShapeUses.remaining < 1) {
  3210	    throw new WildShapeRuleError('no_uses_remaining', null, `Combatant ${command.actor} has no Wild Shape uses remaining.`);
  3211	  }
  3212	  const form = wildShapeForm(command.formId);
  3213	  if (form === null) {
  3214	    throw new WildShapeRuleError('unknown_form', null, `Wild Shape form ${command.formId} is not a bundled Beast.`);
  3215	  }
  3216	  if (!feature.knownForms.includes(command.formId)) {
  3217	    throw new WildShapeRuleError('form_not_known', null, `Wild Shape form ${command.formId} is not on the character sheet.`);
  3218	  }
  3219	  const gate = wildShapeGateFor(feature.druidLevel, form);
  3220	  if (gate !== null) {
  3221	    switch (gate.kind) {
  3222	      case 'challenge_rating':
  3223	        throw new WildShapeRuleError('challenge_rating_gate', gate, `Wild Shape form ${command.formId} exceeds the level-${String(feature.druidLevel)} Challenge Rating gate.`);
  3224	      case 'fly_speed':
  3225	        throw new WildShapeRuleError('fly_speed_gate', gate, `Wild Shape form ${command.formId} requires Druid level 8 for its Fly Speed.`);
  3226	    }
  3227	  }
  3228	  if (subject.profile.rules.abilityScores === undefined) {
  3229	    throw new WildShapeRuleError('feature_unavailable', null, 'Wild Shape requires sourced true-form ability scores.');
  3230	  }
  3231	  spendCost(context, command.actor, 'bonus_action', `Wild Shape: ${form.name}`);
  3232	  const refreshed = combatant(context.state, command.actor);
  3233	  if (refreshed.wildShape !== undefined) {
  3234	    revertWildShape(context, command.actor, 'new_wild_shape');
  3235	  }
  3236	  const beforeOverlay = combatant(context.state, command.actor);
  3237	  const remaining = (beforeOverlay.wildShapeUses?.remaining ?? 0) - 1;
  3238	  const physical = wildShapePhysicalLayer(form);
  3239	  const expiresAtRound = context.state.round + wildShapeDurationRounds(feature.druidLevel);
  3240	  context.state = stateAfterSizeTransition(context.state, command.actor, {
  3241	    ...beforeOverlay,
  3242	    wildShapeUses: {
  3243	      kind: 'wild_shape_uses',
  3244	      maximum: feature.usesMaximum,
  3245	      remaining,

exec
/bin/bash -lc "sed -n '1,95p' tests/unit/tools/ai-dm-board-delivery.test.ts
rg -n \"sha256\\(canonicalJson\\((state|.*\\.state)\\)\\)\" tests tools src --glob '*.ts' | sort
rg -n \"boardStateDigest\\(|stateDigest:\" tests/unit tests/browser --glob '*.test.ts' --glob '*.spec.ts' | head -260" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
import { createHash } from 'node:crypto';
import { join } from 'node:path';
import { tmpdir } from 'node:os';
import { describe, expect, it } from 'vitest';
import {
  agentSessionIdFromCli,
  type AgentInvocation,
  type AgentSessionAdapter,
  type AgentSessionBinding,
  type AgentTurnResult,
} from '../../../src/vtt/agent-session';
import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
import { canonicalJson } from '../../../src/commands/canonical-json';
import { sha256 } from '../../../src/crypto/sha256';
import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
import {
  createDisabledEngineOfferFamilyPolicy,
  createLegacyEngineOptionEnvironmentBinding,
} from '../../../src/vtt/offers/offer-environment';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
import { createUnrepresentedPartyThreatCatalog } from '../../../src/vtt/offers/party-threat-catalog';
import {
  decodeEngineMcpLauncherManifest,
  loadArenaFixture,
  validatedLauncherBoardHtmlReference,
  validatedLauncherBoardImage,
  type EngineMcpLauncherManifest,
} from '../../../src/vtt/mcp/entrypoint';
import * as engineMcpEntrypoint from '../../../src/vtt/mcp/entrypoint';
import { generateRoom } from '../../../src/vtt/room-generator';
import { DEFAULT_RENDERER_PROFILE } from '../../../src/vtt/renderer-profile';
import {
  decodeArenaRowEvidence,
  parseArenaArgs,
  runArena,
} from '../../../tools/ai-dm-arena';
import {
  BOARD_IMAGE_SCALE_STARTUP_INSTRUCTION,
  parseConversationArgs,
  runConversation,
  UI_FEEDBACK_STARTUP_INSTRUCTION,
  type BoardImageMode,
  type ConversationBoardSnapshotService,
} from '../../../tools/ai-dm-conversation';
import type {
  BoardImageArtifact,
  BoardImageSource,
  BoardSnapshotCapture,
} from '../../../tools/ai-dm-board-snapshot';
import {
  mkdirSync,
  mkdtempSync,
  readFileSync,
  writeFileSync,
} from '../../helpers/test-filesystem';

const META = mcpRequestMeta({ name: 'board-delivery-test', version: '1.0.0' });
const BOUND_OFFER_ENVIRONMENT = buildOfferEnvironment({
  kind: 'configuration',
  mode: 'revision_bound',
  familyPolicy: createDisabledEngineOfferFamilyPolicy(),
  partyThreatCatalog: createUnrepresentedPartyThreatCatalog(),
});
function createEngineMcpRuntime(
  state: Parameters<typeof engineMcpEntrypoint.createEngineMcpRuntime>[0],
  options: Omit<NonNullable<Parameters<typeof engineMcpEntrypoint.createEngineMcpRuntime>[1]>,
    'offerEnvironment'> & { readonly offerEnvironment?: typeof BOUND_OFFER_ENVIRONMENT } = {},
): ReturnType<typeof engineMcpEntrypoint.createEngineMcpRuntime> {
  const offerEnvironment = options.offerEnvironment ?? BOUND_OFFER_ENVIRONMENT;
  const runtime = engineMcpEntrypoint.createEngineMcpRuntime(state, { ...options, offerEnvironment });
  expect(runtime.feed.current().offerEnvironment).toEqual(offerEnvironment.binding);
  return runtime;
}

function launcherOfferEnvironment(manifest: EngineMcpLauncherManifest) {
  if (manifest.offerEnvironment === undefined) throw new TypeError('Launcher offer environment is absent.');
  return buildOfferEnvironment({ kind: 'binding', binding: manifest.offerEnvironment });
}
// Pin = the intel-leak lane context (04fd8420: shown-option boundary, size-omission
// declarations) BEFORE the last-seen (D545) merge; the last-seen policy string and
// state handle are normalised back below so the pin stays independent of that merge.
const FOOTPRINTS_RAW_CONTEXT_SHA256 = 'aa841063ad0512d0f6b286318db802526da3efc5265a04d8b67f4d8351909d51';
const FOOTPRINTS_STATE_HANDLE = 'engine-state:c7c7b052bd70a39bf59c83277b7508d8bf52b69100fbde5939c8562ac8686842';
// The post-merge E1c pin is paired with byte identity across implicit defaults,
// explicit image-off, and an explicitly semantic-board-off renderer profile.
const E1C_RAW_CONTEXT_SHA256 = '4071943750fc963ceac5a39fcd1baa71e9a3fd2dcc9bc539a1bb0bec4dc18e6d';

function record(value: unknown, label: string): Readonly<Record<string, unknown>> {
  if (typeof value !== 'object' || value === null || Array.isArray(value)) {
    throw new TypeError(`${label} must be an object.`);
  }
  return value as Readonly<Record<string, unknown>>;
}

function dimensionedPng(identity: string, bytes = 96): Buffer {
src/vtt/encounter-projections.ts:488:    stateDigest: sha256(canonicalJson(input.view.state)),
tests/unit/tools/ai-dm-board-delivery.test.ts:770:      stateDigest: sha256(canonicalJson(state)),
tests/unit/tools/d569-v5.test.ts:117:    startingRoomDigest: sha256(canonicalJson(state)), kbHash: cell.sharedKbHash,
tests/browser/ai-dm-board-snapshot.spec.ts:85:    readonly stateDigest: string;
tests/unit/tools/ai-dm-legacy-invariance.test.ts:793:    stateDigest: proposal.stateDigest,
tests/unit/tools/ai-dm-legacy-invariance.test.ts:915:    stateDigest: submitted.stateDigest,
tests/unit/tools/ai-dm-legacy-invariance.test.ts:938:    .toMatchObject({ expectedRevision: authorization.revision, stateDigest: authorization.digest });
tests/unit/tools/ai-dm-screenshot-probe.test.ts:526:      boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1071:      new Set(first.map((candidate) => boardStateDigest(candidate.state))).size,
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1076:        digest: boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1081:        digest: boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1308:          stateDigest: 'a'.repeat(64),
tests/unit/vtt/board-chrome.test.ts:808:    stateDigest: 'digest-under-test',
tests/unit/vtt/turn-exhaustion-coordinator.test.ts:132:    stateDigest: f.capsule.digest,
tests/unit/vtt/footprint-increment-four.test.ts:248:        { revision: 1, round: 1, stateDigest: `footprint:${String(index)}` },
tests/unit/vtt/footprint-increment-four.test.ts:266:          { revision: 1, round: 1, stateDigest: 'footprint:64' },
tests/unit/tools/ai-dm-board-snapshot.test.ts:65:    stateDigest: boardStateDigest(state),
tests/unit/tools/ai-dm-board-snapshot.test.ts:162:        { revision: state.revision, round: state.round, stateDigest: boardStateDigest(state) },
tests/unit/tools/ai-dm-conversation.test.ts:2328:      stateDigest: 'state',
tests/unit/tools/ai-dm-conversation.test.ts:2407:      stateDigest: 'state',
tests/unit/tools/rl-extract-sft.test.ts:92:      stateDigest: 'a'.repeat(64),
tests/unit/tools/rl-extract-sft.test.ts:272:        stateDigest: 'a'.repeat(64),
tests/unit/tools/rl-extract-sft.test.ts:285:          stateDigest: 'b'.repeat(64),
tests/unit/tools/ai-dm-board-delivery.test.ts:770:      stateDigest: sha256(canonicalJson(state)),
tests/unit/vtt/engine-state-capsule.test.ts:496:      stateDigest: capsule.digest,
tests/unit/vtt/engine-state-capsule.test.ts:615:      stateDigest: fixture.capsule.digest,
tests/unit/assets/light-encoding.test.ts:365:  const provenance = { revision: 1, round: 1, stateDigest: 'light-encoding-under-test' };
tests/unit/vtt/agent-round-decision.test.ts:31:    stateDigest: 'a'.repeat(64),
tests/unit/vtt/agent-round-decision.test.ts:198:      stateDigest: first.stateDigest,
tests/unit/assets/board-glyphs.test.ts:704:  const provenance = { revision: 1, round: 1, stateDigest: 'board-glyphs-under-test' };
tests/unit/vtt/adjustment-exhaustion-coordinator.test.ts:95:    stateDigest: capsule.digest,
tests/unit/vtt/engine-host-integration.test.ts:60:      stateDigest: 'a'.repeat(64),
tests/unit/vtt/offered-option-paths.test.ts:233:      { revision: fixture.state.revision, round: fixture.state.round, stateDigest: 'digest:d512' },

exec
/bin/bash -lc "rg -n \"boardGlyph|glyphCaptures|blindBoard|snapshotCheck|runBoard|board snapshot\" tests/unit/vtt/challenge-room-fixtures.test.ts | head -120
rg -n \"ai-dm-board-glyph-captures|ai-dm-blind-board-snapshot-check|ai-dm-board-snapshot-check\" package.json scripts tests --glob '*.ts' --glob '*.json' | head -120
cat package.json | jq '.scripts | with_entries(select(.key|test(\"board|glyph|snapshot\")))'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
tests/browser/ai-dm-board-snapshot.spec.ts:45:  'tools/ai-dm-blind-board-snapshot-check.ts',
tests/browser/ai-dm-board-snapshot.spec.ts:156:      'tools/ai-dm-board-snapshot-check.ts',
tests/unit/vtt/challenge-room-fixtures.test.ts:137:    const source = readFileSync('tools/ai-dm-blind-board-snapshot-check.ts', 'utf8');
{}

exec
/bin/bash -lc "rg -n \"slot\\.use\\.kind === 'saving_throw'|use.kind === 'saving_throw'\" tests/unit/vtt | head -80
rg -n \"interface.*Saving|kind: 'saving_throw'\" src/vtt/turn-proposal.ts | head -80
sed -n '170,280p' src/vtt/turn-proposal.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
tests/unit/vtt/monster-omitted-riders.test.ts:49:      : slot.use.kind === 'saving_throw'
tests/unit/vtt/engine-query-port.test.ts:545:            use.kind === 'attack' || use.kind === 'saving_throw');
tests/unit/vtt/snippets.test.ts:122:    slot.use.kind === 'attack' || slot.use.kind === 'multiattack' || slot.use.kind === 'saving_throw');
134:      readonly kind: 'saving_throw';
148:      readonly kind: 'saving_throw';
179:      readonly kind: 'saving_throw';
      readonly sourceActionId: EngineActionId;
      readonly spellId: EngineSpellId;
      readonly targets: readonly EngineTargetSelector[];
      /** Engine-bound condition encoded by a revision-bound offered option. */
      readonly selectedCondition?: LesserRestorationCondition;
      /** Exact engine-owned placement; null for spells without a placed area. */
      readonly area: AreaTemplate | null;
    }
  | {
      readonly kind: 'saving_throw';
      readonly actionId: EngineActionId;
      readonly target: EngineTargetSelector;
    }
  | { readonly kind: 'dash' | 'disengage' | 'hide' };

export type EngineActionSlotUse =
  | { readonly slot: 'main'; readonly use: EngineMainActionUse }
  | { readonly slot: 'bonus'; readonly use: EngineBonusActionUse };

export interface EngineTurnProposal {
  readonly actorId: CombatantId;
  readonly expectedRevision: number;
  readonly primaryOptionId: EngineOptionId;
  readonly fallbackOptionId: EngineOptionId | null;
  /** Verbatim, bounded explanation supplied by the decision author. */
  readonly reason: string;
  readonly activationChoice?: EngineActivationChoice | null;
  readonly overrideJustification: EngineOverrideJustification | null;
}

export interface ResolvedActionSlotUse {
  readonly slot: 'main' | 'bonus';
  readonly kind:
    | 'attack'
    | 'saving_throw'
    | 'cast_spell'
    | 'use_world_object'
    | 'dodge'
    | 'disengage'
    | 'dash'
    | 'hide'
    | 'end_turn';
  readonly actionId: EngineActionId;
  readonly spellId: EngineSpellId | null;
  readonly targetIds: readonly CombatantId[];
  readonly objectId: WorldObjectId | null;
  readonly omittedRiders: readonly EngineOmittedRider[];
  /** Engine-bound condition encoded by a revision-bound offered option. */
  readonly selectedCondition?: LesserRestorationCondition;
  readonly activationChoice?: EngineActivationChoice;
  readonly multiattackComponent?: true;
  /** Present only for a placed-area spell. */
  readonly area?: AreaTemplate;
}

export interface ResolvedTurnMechanics {
  readonly actorId: CombatantId;
  readonly optionId: EngineOptionId;
  readonly movementCostFeet: number;
  readonly path: readonly GridCell[];
  readonly finalPosition: GridCell;
  readonly actionSlots: readonly ResolvedActionSlotUse[];
  readonly omittedRiders: readonly EngineOmittedRider[];
}

export type EngineProposalResolution =
  | {
      readonly valid: true;
      readonly selectedBranch: 'primary' | 'fallback';
      readonly resolutionDigest: string;
      readonly summary: string;
      readonly refusals: readonly {
        readonly branch: 'primary';
        readonly code: string;
        readonly summary: string;
      }[];
      readonly mechanics: ResolvedTurnMechanics;
      readonly option: EngineOfferableOption;
      readonly primaryOption: EngineOfferableOption;
      readonly fallbackOption: EngineOfferableOption | null;
    }
  | {
      readonly valid: false;
      readonly selectedBranch: 'none';
      readonly refusals: readonly {
        readonly branch: 'primary' | 'fallback';
        readonly code: string;
        readonly summary: string;
      }[];
    };

export interface PureTurnProposalResolver {
  resolve(state: import('../combat/encounter').EncounterState, proposal: EngineTurnProposal): EngineProposalResolution;
}

codex
The reviewed gap is exactly four save components (`6203010` Web once; `6206007` Web three times) among 316 total components. I’m adding a direct resolved-origin trace assertion for all 316, a speed-zero Giant Spider/Web wall witness, and the derived-fog digest witness. LAND-03’s digest witness will be run red before the production line changes.
apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/tools/ai-dm-board-snapshot.test.ts
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/vtt/engine-query-port.test.ts
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..85b63108e1c9454ed372f7f418ec07cb94aa7334
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,43 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      tier: 'total',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +609,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +627,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +647,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +669,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 

diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..85b63108e1c9454ed372f7f418ec07cb94aa7334
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,43 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      tier: 'total',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +609,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +627,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +647,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +669,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 

exec
/bin/bash -lc "git diff --check
npx vitest run --configLoader runner tests/unit/tools/ai-dm-board-snapshot.test.ts --maxWorkers=1 -t 'binds to the DM projection digest when a non-empty derived fog field separates it from raw state'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 4334ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ❯ tests/unit/tools/ai-dm-board-snapshot.test.ts (22 tests | 1 failed | 21 skipped) 12ms
     × binds to the DM projection digest when a non-empty derived fog field separates it from raw state 11ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/ai-dm-board-snapshot.test.ts > AI DM board snapshot contracts > binds to the DM projection digest when a non-empty derived fog field separates it from raw state
AssertionError: expected '46d6cc958d1a5a8f522fde31d571834f2717e…' to be '92ba94ec3971037de9b61ddabc327e52050ec…' // Object.is equality

Expected: "92ba94ec3971037de9b61ddabc327e52050ece39c79cfb060ae00785e3ffafe8"
Received: "46d6cc958d1a5a8f522fde31d571834f2717e0d102ceda9ff6f480080404214b"

 ❯ tests/unit/tools/ai-dm-board-snapshot.test.ts:260:37
    258|     });
    259|
    260|     expect(boardStateDigest(state)).toBe(projection.stateDigest);
       |                                     ^
    261|     expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(stat…
    262|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 21 skipped (22)
   Start at  05:43:59
   Duration  4.01s (transform 1.61s, setup 0ms, import 2.56s, tests 12ms, environment 0ms)


diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..85b63108e1c9454ed372f7f418ec07cb94aa7334
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,43 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      tier: 'total',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +609,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +627,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +647,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +669,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 

exec
/bin/bash -lc 'npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts --maxWorkers=1' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 12057ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ❯ tests/unit/vtt/engine-query-port.test.ts (20 tests | 1 failed) 8246ms
     × SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed 6ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/engine-query-port.test.ts > canonical engine query port > SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed
AssertionError: expected { …(10) } to match object { tier: 'total', blocksSight: true }
(16 matching properties omitted from actual)

- Expected
+ Received

  {
    "blocksSight": true,
-   "tier": "total",
+   "tier": "half",
  }

 ❯ tests/unit/vtt/engine-query-port.test.ts:561:82
    559|     const blocked = blockedSaveState(true);
    560|     const opened = blockedSaveState(false);
    561|     expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.…
       |                                                                                  ^
    562|       tier: 'total',
    563|       blocksSight: true,

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 19 passed (20)
   Start at  05:44:07
   Duration  11.75s (transform 1.30s, setup 0ms, import 2.08s, tests 8.25s, environment 0ms)


diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..85b63108e1c9454ed372f7f418ec07cb94aa7334
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,43 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      tier: 'total',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +609,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +627,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +647,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +669,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/vtt/engine-query-port.test.ts
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 

diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 

codex
The LAND-03 witness is red under the old raw-state digest, as required. The save witness also exposed the intended seam distinction: the complete wall column is optically blocking while physical cover remains Half because of boundary rays; I’ve pinned that hand geometry and am now applying the single DM-view digest change plus the one renderer-modeling test recomputation.
apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/tools/ai-dm-board-delivery.test.ts
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tools/ai-dm-board-snapshot.ts
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "git diff --check
npx vitest run --configLoader runner tests/unit/tools/ai-dm-board-snapshot.test.ts --maxWorkers=1 -t 'binds to the DM projection digest when a non-empty derived fog field separates it from raw state'
npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts --maxWorkers=1" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 16527ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json

 Test Files  1 passed (1)
      Tests  1 passed | 21 skipped (22)
   Start at  05:44:40
   Duration  3.95s (transform 1.60s, setup 0ms, import 2.53s, tests 9ms, environment 0ms)


 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json

 Test Files  1 passed (1)
      Tests  20 passed (20)
   Start at  05:44:45
   Duration  11.83s (transform 1.34s, setup 0ms, import 2.11s, tests 8.27s, environment 0ms)


diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc 'sha256sum tests/unit/vtt/engine-query-port.test.ts' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
30fd153dddae3c8804c89943c84bbcdeb995b3d891803257e460b9dadedbf95f  tests/unit/vtt/engine-query-port.test.ts

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/vtt/engine-query-port.test.ts
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..2d0b8180ee85eaa9ba7223d032b8b0b120e915b4
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,13 +610,35 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
-            const executionState = stateAtResolvedOrigin(
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
+            let executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
               resolution.mechanics.finalPosition,
             );
+            if (use.kind === 'saving_throw') {
+              const sourcePosition = executionState.tokens.find((token) =>
+                token.combatantId === monster.profile.id)?.position;
+              const firstTargetId = use.targetIds[0];
+              const targetPosition = executionState.tokens.find((token) =>
+                token.combatantId === firstTargetId)?.position;
+              if (sourcePosition === undefined || targetPosition === undefined) {
+                throw new Error('Save blocker mutant omitted a token position.');
+              }
+              const blockingColumn = Math.floor((sourcePosition.column + targetPosition.column) / 2);
+              executionState = {
+                ...executionState,
+                blockedCells: [
+                  ...executionState.blockedCells,
+                  ...Array.from(
+                    { length: executionState.bounds.rows },
+                    (_unused, row) => ({ column: blockingColumn, row }),
+                  ),
+                ],
+              };
+            }
             const sources = [
               ...monsterActions(executionState, monster.profile.id),
               ...monsterBonusActions(executionState, monster.profile.id),
@@ -563,15 +649,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +669,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +691,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..2d0b8180ee85eaa9ba7223d032b8b0b120e915b4
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,13 +610,35 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
-            const executionState = stateAtResolvedOrigin(
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
+            let executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
               resolution.mechanics.finalPosition,
             );
+            if (use.kind === 'saving_throw') {
+              const sourcePosition = executionState.tokens.find((token) =>
+                token.combatantId === monster.profile.id)?.position;
+              const firstTargetId = use.targetIds[0];
+              const targetPosition = executionState.tokens.find((token) =>
+                token.combatantId === firstTargetId)?.position;
+              if (sourcePosition === undefined || targetPosition === undefined) {
+                throw new Error('Save blocker mutant omitted a token position.');
+              }
+              const blockingColumn = Math.floor((sourcePosition.column + targetPosition.column) / 2);
+              executionState = {
+                ...executionState,
+                blockedCells: [
+                  ...executionState.blockedCells,
+                  ...Array.from(
+                    { length: executionState.bounds.rows },
+                    (_unused, row) => ({ column: blockingColumn, row }),
+                  ),
+                ],
+              };
+            }
             const sources = [
               ...monsterActions(executionState, monster.profile.id),
               ...monsterBonusActions(executionState, monster.profile.id),
@@ -563,15 +649,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +669,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +691,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts --maxWorkers=1 -t 'offered attacks and saves have query-clear traces'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 9959ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ❯ tests/unit/vtt/engine-query-port.test.ts (20 tests | 1 failed | 19 skipped) 6046ms
     × offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement 6045ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/engine-query-port.test.ts > canonical engine query port > offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement
AssertionError: tests/fixtures/arena-basis-brutal/seed-6203010.json:option:0:81c272c6cb7e824f0e13b648f741ee573b8c422dc2f814cf:web:blocksSight: expected true to be false // Object.is equality

- Expected
+ Received

- false
+ true

 ❯ tests/unit/vtt/engine-query-port.test.ts:658:99
    656|                 sourceAnchor: resolution.mechanics.finalPosition,
    657|               });
    658|               expect(trace.blocksSight, `${path}:${option.optionId}:${…
       |                                                                                                   ^
    659|               expect(trace.tier, `${path}:${option.optionId}:${use.act…
    660|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 19 skipped (20)
   Start at  05:45:17
   Duration  9.63s (transform 1.35s, setup 0ms, import 2.15s, tests 6.05s, environment 0ms)


diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..2d0b8180ee85eaa9ba7223d032b8b0b120e915b4
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,13 +610,35 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
-            const executionState = stateAtResolvedOrigin(
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
+            let executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
               resolution.mechanics.finalPosition,
             );
+            if (use.kind === 'saving_throw') {
+              const sourcePosition = executionState.tokens.find((token) =>
+                token.combatantId === monster.profile.id)?.position;
+              const firstTargetId = use.targetIds[0];
+              const targetPosition = executionState.tokens.find((token) =>
+                token.combatantId === firstTargetId)?.position;
+              if (sourcePosition === undefined || targetPosition === undefined) {
+                throw new Error('Save blocker mutant omitted a token position.');
+              }
+              const blockingColumn = Math.floor((sourcePosition.column + targetPosition.column) / 2);
+              executionState = {
+                ...executionState,
+                blockedCells: [
+                  ...executionState.blockedCells,
+                  ...Array.from(
+                    { length: executionState.bounds.rows },
+                    (_unused, row) => ({ column: blockingColumn, row }),
+                  ),
+                ],
+              };
+            }
             const sources = [
               ...monsterActions(executionState, monster.profile.id),
               ...monsterBonusActions(executionState, monster.profile.id),
@@ -563,15 +649,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +669,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +691,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/vtt/engine-query-port.test.ts
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "sha256sum tests/unit/vtt/engine-query-port.test.ts
npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts --maxWorkers=1 -t 'offered attacks and saves have query-clear traces'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 11690ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json

 Test Files  1 passed (1)
      Tests  1 passed | 19 skipped (20)
   Start at  05:45:38
   Duration  11.38s (transform 1.31s, setup 0ms, import 2.09s, tests 7.84s, environment 0ms)


diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
Task A’s save-wall mutant is killed: with full blocking columns injected into all save execution states, the direct oracle fails on `6203010` Web (`blocksSight true`), while the unmutated property passes and counts 214 offers, 316 components, and 4 saves. I’m now proving LAND-03’s reverse mutant before the broader unit/browser runs.
exec
/bin/bash -lc 'sha256sum tools/ai-dm-board-snapshot.ts' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
e9398ff7d97d62804920ce6a3ac017917ad932949574bc47415e0eaa4a48e55c  tools/ai-dm-board-snapshot.ts

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tools/ai-dm-board-snapshot.ts
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 

exec
/bin/bash -lc "npx vitest run --configLoader runner tests/unit/tools/ai-dm-board-snapshot.test.ts --maxWorkers=1 -t 'binds to the DM projection digest when a non-empty derived fog field separates it from raw state'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 4259ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ❯ tests/unit/tools/ai-dm-board-snapshot.test.ts (22 tests | 1 failed | 21 skipped) 12ms
     × binds to the DM projection digest when a non-empty derived fog field separates it from raw state 11ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/ai-dm-board-snapshot.test.ts > AI DM board snapshot contracts > binds to the DM projection digest when a non-empty derived fog field separates it from raw state
AssertionError: expected '46d6cc958d1a5a8f522fde31d571834f2717e…' to be '92ba94ec3971037de9b61ddabc327e52050ec…' // Object.is equality

Expected: "92ba94ec3971037de9b61ddabc327e52050ece39c79cfb060ae00785e3ffafe8"
Received: "46d6cc958d1a5a8f522fde31d571834f2717e0d102ceda9ff6f480080404214b"

 ❯ tests/unit/tools/ai-dm-board-snapshot.test.ts:260:37
    258|     });
    259|
    260|     expect(boardStateDigest(state)).toBe(projection.stateDigest);
       |                                     ^
    261|     expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(stat…
    262|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 21 skipped (22)
   Start at  05:46:03
   Duration  3.93s (transform 1.59s, setup 0ms, import 2.49s, tests 12ms, environment 0ms)


diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 

context compacted
codex
I’m resuming at the two deliberately injected mutants. Task A’s mutant has already been restored and proved green; Task B’s reverse mutant is still present, so I’ll first restore the projected-DM digest and confirm the witness passes before running the remaining landing gate.
apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tools/ai-dm-board-snapshot.ts
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "sha256sum tools/ai-dm-board-snapshot.ts && npx vitest run --configLoader runner tests/unit/tools/ai-dm-board-snapshot.test.ts --maxWorkers=1 -t 'binds to the DM projection digest when a non-empty derived fog field separates it from raw state'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 4273ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json

 Test Files  1 passed (1)
      Tests  1 passed | 21 skipped (22)
   Start at  05:47:02
   Duration  3.96s (transform 1.60s, setup 0ms, import 2.51s, tests 9ms, environment 0ms)


diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The production digest is restored byte-for-byte to the previously verified implementation (`e9398f…`), and the new derived-fog witness is green (1 passed, 21 skipped). I’ll now audit the final diff and run the requested unit groups, treating any digest fixture/manifest change as a hard stop.
exec
/bin/bash -lc "git status --short && git diff --check && git diff --stat && rg --files tests/unit/tools | rg '(ai-dm-board-(snapshot|delivery|glyph-captures)|ai-dm-blind-board-snapshot-check|ai-dm-screenshot-probe|d569-(blind-experiment|second-family-manifest|v5)|ai-dm-conversation)\\.test\\.ts"'$'"'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
 M tests/unit/tools/ai-dm-board-delivery.test.ts
 M tests/unit/tools/ai-dm-board-snapshot.test.ts
 M tests/unit/vtt/engine-query-port.test.ts
 M tools/ai-dm-board-snapshot.ts
 tests/unit/tools/ai-dm-board-delivery.test.ts |   4 +-
 tests/unit/tools/ai-dm-board-snapshot.test.ts |  49 +++++++++++-
 tests/unit/vtt/engine-query-port.test.ts      | 111 ++++++++++++++++++++++----
 tools/ai-dm-board-snapshot.ts                 |   2 +-
 4 files changed, 147 insertions(+), 19 deletions(-)
tests/unit/tools/ai-dm-board-snapshot.test.ts
tests/unit/tools/ai-dm-board-delivery.test.ts
tests/unit/tools/d569-v5.test.ts
tests/unit/tools/d569-blind-experiment.test.ts
tests/unit/tools/d569-second-family-manifest.test.ts
tests/unit/tools/ai-dm-screenshot-probe.test.ts
tests/unit/tools/ai-dm-conversation.test.ts

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc 'git rev-parse --short=8 HEAD && sha256sum tests/browser/vtt-encounter.spec.ts && git diff -- tests/unit/vtt/engine-query-port.test.ts tests/unit/tools/ai-dm-board-snapshot.test.ts tests/unit/tools/ai-dm-board-delivery.test.ts tools/ai-dm-board-snapshot.ts' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
61dd454e
cb89f2ddc5a8b7af2f564b496a88f2d568b456518ccda4623083162f9d079227  tests/browser/vtt-encounter.spec.ts
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca..58cffc59 100644
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@ import {
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@ describe('launcher image containment and binding', () => {
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c..384c5008 100644
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@ import {
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@ describe('AI DM board snapshot contracts', () => {
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd413..41e13bea 100644
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@ import { traceCombatantLine } from '../../../src/combat/cover';
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@ function twoBlockerSeamState(openLowerFlank: boolean): {
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@ describe('canonical engine query port', () => {
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@ describe('canonical engine query port', () => {
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@ describe('canonical engine query port', () => {
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@ describe('canonical engine query port', () => {
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@ describe('canonical engine query port', () => {
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e14..35aa2cd1 100644
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@ function sha256Bytes(value: Uint8Array): string {
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "sed -n '1148,1182p' src/vtt/engine-query-port.ts && sed -n '180,220p' src/combat/cover.ts && git log -3 --oneline --decorate" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
  }
  if (targetPosition === undefined) codes.push('target_not_placed');
  const actions = actor?.profile.kind === 'monster' ? monsterActions(state, request.actorId) : [];
  const action = actions.find((candidate) => candidate.id === request.actionId);
  if (action === undefined) codes.push('action_absent');
  const rangeFeet = action === undefined ? null : engineActionRangeFeet(actions, action);
  if (action !== undefined && rangeFeet === null) codes.push('action_range_unresolved');
  if (
    codes.length > 0 ||
    action === undefined ||
    origin === undefined ||
    targetPosition === undefined ||
    rangeFeet === null
  ) {
    return { legal: false, codes };
  }
  const actorSpace = queryCombatantSpaceAt(state, request.actorId, origin);
  const targetSpace = queryCombatantSpace(state, request.targetId);
  const distanceFeet = minimumSpaceDistance(actorSpace, targetSpace);
  if (action.kind === 'attack' || action.kind === 'saving_throw') {
    const trace = traceCombatantLine(state, request.actorId, request.targetId, {
      sourceAnchor: origin,
    });
    if (trace.blocksSight) {
      return { legal: false, codes: ['target_outside_line_of_sight'] };
    }
    if (trace.tier === 'total') return { legal: false, codes: ['target_has_total_cover'] };
  }
  if (action.kind === 'attack') {
    const verdict = tacticalRangeVerdictAtDistance(distanceFeet, monsterAttackRange(action));
    if (verdict.status === 'unresolved') {
      return { legal: false, codes: ['action_range_unresolved'] };
    }
    if (!verdict.legal || verdict.band === 'out') {
      return { legal: false, codes: ['target_out_of_range'] };
  }
}

interface TerrainSourceCell {
  readonly cell: GridCell;
  readonly sources: TerrainLineSource[];
}

function addSource(
  sourceCells: Map<string, TerrainSourceCell>,
  cell: GridCell,
  source: TerrainLineSource,
  exclusions: ReadonlySet<string>,
): void {
  const key = cellKey(cell);
  if (exclusions.has(key)) return;
  const existing = sourceCells.get(key);
  if (existing === undefined) {
    sourceCells.set(key, { cell: { ...cell }, sources: [source] });
  } else {
    existing.sources.push(source);
  }
}

interface TerrainSourceIndexes {
  readonly terrain: ReadonlyMap<string, TerrainSourceCell>;
  withCreatures?: ReadonlyMap<string, TerrainSourceCell>;
}

const terrainSourceIndexes = new WeakMap<EncounterState, TerrainSourceIndexes>();
const terrainLineTraces = new WeakMap<EncounterState, Map<string, TerrainLineTrace>>();
const combatantLineTraces = new WeakMap<EncounterState, Map<string, TerrainLineTrace>>();
const terrainSourceSignatures = new WeakMap<EncounterState, Map<boolean, string>>();
const equivalentTerrainLineTraces = new Map<string, TerrainLineTrace>();
const EQUIVALENT_TRACE_CACHE_LIMIT = 4_096;

function terrainSourceSignature(
  state: EncounterState,
  includeCreatures: boolean,
  indexedSources: ReadonlyMap<string, TerrainSourceCell>,
): string {
61dd454e (HEAD -> claude/vis-field) VIS-FIELD LAND-02: re-pin the reference-flow fog expectation to the derived field
ebfcacaa VIS-FIELD LAND-01: offers agree with execution under the seam rule; generator open rays require sight
10cc9f5e VIS-FIELD-01 B6: optional semantic v2 visibility block, explicit truncation, pins

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The supervisor committed the pre-existing LAND-02 file while this session was active: HEAD is now `61dd454e`, and its content hash remains exactly `cb89f2…`, so I did not modify it. The working tree now correctly contains only the four LAND-01-FIX/LAND-03 files. Of the requested tool-test names, seven exist; the glyph-captures and blind-board-snapshot-check names are browser helpers, not standalone unit files.
exec
/bin/bash -lc 'npx vitest run --configLoader runner tests/unit/tools/ai-dm-board-snapshot.test.ts tests/unit/tools/ai-dm-board-delivery.test.ts tests/unit/tools/ai-dm-screenshot-probe.test.ts --maxWorkers=1' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "pgrep -af 'vitest.*ai-dm-board-snapshot' || true" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
1 /home/vagrant/.codex/tmp/arg0/codex-arg0kHUXDj/codex-linux-sandbox --sandbox-policy-cwd /home/vagrant/PhpstormProjects/dnd-wt-vis-field --command-cwd /home/vagrant/PhpstormProjects/dnd-wt-vis-field --permission-profile {"type":"managed","file_system":{"type":"restricted","entries":[{"path":{"type":"special","value":{"kind":"root"}},"access":"read"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-vis-field"},"access":"write"},{"path":{"type":"special","value":{"kind":"slash_tmp"}},"access":"write"},{"path":{"type":"special","value":{"kind":"tmpdir"}},"access":"write"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-vis-field/.git"},"access":"read","missing_path_behavior":"skip"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-vis-field/.agents"},"access":"read","missing_path_behavior":"skip"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-vis-field/.codex"},"access":"read","missing_path_behavior":"skip"}]},"network":"enabled"} --apply-seccomp-then-exec -- /bin/bash -c __CODEX_SNAPSHOT_OVERRIDE_SET_0="${CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS+x}" __CODEX_SNAPSHOT_OVERRIDE_0="${CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS-}" __CODEX_SNAPSHOT_OVERRIDE_SET_1="${CODEX_PERMISSION_PROFILE+x}" __CODEX_SNAPSHOT_OVERRIDE_1="${CODEX_PERMISSION_PROFILE-}" __CODEX_SNAPSHOT_OVERRIDE_SET_2="${CODEX_PLUGIN_METRICS_OUTPUT+x}" __CODEX_SNAPSHOT_OVERRIDE_2="${CODEX_PLUGIN_METRICS_OUTPUT-}" __CODEX_SNAPSHOT_OVERRIDE_SET_3="${CODEX_SESSION_ID+x}" __CODEX_SNAPSHOT_OVERRIDE_3="${CODEX_SESSION_ID-}" __CODEX_SNAPSHOT_OVERRIDE_SET_4="${CODEX_THREAD_ID+x}" __CODEX_SNAPSHOT_OVERRIDE_4="${CODEX_THREAD_ID-}" __CODEX_SNAPSHOT_OVERRIDE_SET_5="${CODEX_VERSION+x}" __CODEX_SNAPSHOT_OVERRIDE_5="${CODEX_VERSION-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_0="${ALL_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_0="${ALL_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_1="${BUNDLE_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_1="${BUNDLE_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_2="${BUNDLE_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_2="${BUNDLE_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_3="${BUNDLE_NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_3="${BUNDLE_NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_4="${BUNDLE_SSL_CA_CERT+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_4="${BUNDLE_SSL_CA_CERT-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_5="${CARGO_HTTP_CAINFO+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_5="${CARGO_HTTP_CAINFO-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_6="${CODEX_CA_CERTIFICATE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_6="${CODEX_CA_CERTIFICATE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_7="${CODEX_NETWORK_ALLOW_LOCAL_BINDING+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_7="${CODEX_NETWORK_ALLOW_LOCAL_BINDING-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_8="${CODEX_NETWORK_PROXY_ACTIVE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_8="${CODEX_NETWORK_PROXY_ACTIVE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_9="${CODEX_NETWORK_PROXY_ATTRIBUTION+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_9="${CODEX_NETWORK_PROXY_ATTRIBUTION-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_10="${CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_10="${CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_11="${CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_11="${CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_12="${CURL_CA_BUNDLE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_12="${CURL_CA_BUNDLE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_13="${DOCKER_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_13="${DOCKER_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_14="${DOCKER_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_14="${DOCKER_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_15="${ELECTRON_GET_USE_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_15="${ELECTRON_GET_USE_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_16="${FTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_16="${FTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_17="${GIT_SSL_CAINFO+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_17="${GIT_SSL_CAINFO-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_18="${HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_18="${HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_19="${HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_19="${HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_20="${NODE_EXTRA_CA_CERTS+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_20="${NODE_EXTRA_CA_CERTS-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_21="${NODE_USE_ENV_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_21="${NODE_USE_ENV_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_22="${NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_22="${NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_23="${NPM_CONFIG_CAFILE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_23="${NPM_CONFIG_CAFILE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_24="${NPM_CONFIG_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_24="${NPM_CONFIG_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_25="${NPM_CONFIG_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_25="${NPM_CONFIG_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_26="${NPM_CONFIG_NOPROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_26="${NPM_CONFIG_NOPROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_27="${NPM_CONFIG_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_27="${NPM_CONFIG_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_28="${PIP_CERT+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_28="${PIP_CERT-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_29="${PIP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_29="${PIP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_30="${REQUESTS_CA_BUNDLE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_30="${REQUESTS_CA_BUNDLE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_31="${SSL_CERT_FILE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_31="${SSL_CERT_FILE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_32="${WSS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_32="${WSS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_33="${WS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_33="${WS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_34="${YARN_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_34="${YARN_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_35="${YARN_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_35="${YARN_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_36="${YARN_NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_36="${YARN_NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_37="${all_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_37="${all_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_38="${ftp_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_38="${ftp_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_39="${http_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_39="${http_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_40="${https_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_40="${https_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_41="${no_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_41="${no_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_42="${npm_config_cafile+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_42="${npm_config_cafile-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_43="${npm_config_http_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_43="${npm_config_http_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_44="${npm_config_https_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_44="${npm_config_https_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_45="${npm_config_noproxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_45="${npm_config_noproxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_46="${npm_config_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_46="${npm_config_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_47="${ws_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_47="${ws_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_48="${wss_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_48="${wss_proxy-}" __CODEX_SNAPSHOT_PROXY_ENV_SET="${CODEX_NETWORK_PROXY_ACTIVE+x}"  if . '/home/vagrant/.codex/shell_snapshots/01a0ae59-86c2-7cb1-8b2e-bbf96f72fd73.1789638023249753372.sh' >/dev/null 2>&1; then :; fi  if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_0}" ]; then export CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS="${__CODEX_SNAPSHOT_OVERRIDE_0}"; else unset CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_1}" ]; then export CODEX_PERMISSION_PROFILE="${__CODEX_SNAPSHOT_OVERRIDE_1}"; else unset CODEX_PERMISSION_PROFILE; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_2}" ]; then export CODEX_PLUGIN_METRICS_OUTPUT="${__CODEX_SNAPSHOT_OVERRIDE_2}"; else unset CODEX_PLUGIN_METRICS_OUTPUT; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_3}" ]; then export CODEX_SESSION_ID="${__CODEX_SNAPSHOT_OVERRIDE_3}"; else unset CODEX_SESSION_ID; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_4}" ]; then export CODEX_THREAD_ID="${__CODEX_SNAPSHOT_OVERRIDE_4}"; else unset CODEX_THREAD_ID; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_5}" ]; then export CODEX_VERSION="${__CODEX_SNAPSHOT_OVERRIDE_5}"; else unset CODEX_VERSION; fi if [ -n "$__CODEX_SNAPSHOT_PROXY_ENV_SET" ] || [ -n "${CODEX_NETWORK_PROXY_ACTIVE+x}" ]; then if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_0}" ]; then export ALL_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_0}"; else unset ALL_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_1}" ]; then export BUNDLE_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_1}"; else unset BUNDLE_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_2}" ]; then export BUNDLE_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_2}"; else unset BUNDLE_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_3}" ]; then export BUNDLE_NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_3}"; else unset BUNDLE_NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_4}" ]; then export BUNDLE_SSL_CA_CERT="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_4}"; else unset BUNDLE_SSL_CA_CERT; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_5}" ]; then export CARGO_HTTP_CAINFO="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_5}"; else unset CARGO_HTTP_CAINFO; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_6}" ]; then export CODEX_CA_CERTIFICATE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_6}"; else unset CODEX_CA_CERTIFICATE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_7}" ]; then export CODEX_NETWORK_ALLOW_LOCAL_BINDING="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_7}"; else unset CODEX_NETWORK_ALLOW_LOCAL_BINDING; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_8}" ]; then export CODEX_NETWORK_PROXY_ACTIVE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_8}"; else unset CODEX_NETWORK_PROXY_ACTIVE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_9}" ]; then export CODEX_NETWORK_PROXY_ATTRIBUTION="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_9}"; else unset CODEX_NETWORK_PROXY_ATTRIBUTION; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_10}" ]; then export CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_10}"; else unset CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_11}" ]; then export CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_11}"; else unset CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_12}" ]; then export CURL_CA_BUNDLE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_12}"; else unset CURL_CA_BUNDLE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_13}" ]; then export DOCKER_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_13}"; else unset DOCKER_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_14}" ]; then export DOCKER_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_14}"; else unset DOCKER_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_15}" ]; then export ELECTRON_GET_USE_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_15}"; else unset ELECTRON_GET_USE_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_16}" ]; then export FTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_16}"; else unset FTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_17}" ]; then export GIT_SSL_CAINFO="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_17}"; else unset GIT_SSL_CAINFO; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_18}" ]; then export HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_18}"; else unset HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_19}" ]; then export HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_19}"; else unset HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_20}" ]; then export NODE_EXTRA_CA_CERTS="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_20}"; else unset NODE_EXTRA_CA_CERTS; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_21}" ]; then export NODE_USE_ENV_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_21}"; else unset NODE_USE_ENV_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_22}" ]; then export NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_22}"; else unset NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_23}" ]; then export NPM_CONFIG_CAFILE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_23}"; else unset NPM_CONFIG_CAFILE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_24}" ]; then export NPM_CONFIG_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_24}"; else unset NPM_CONFIG_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_25}" ]; then export NPM_CONFIG_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_25}"; else unset NPM_CONFIG_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_26}" ]; then export NPM_CONFIG_NOPROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_26}"; else unset NPM_CONFIG_NOPROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_27}" ]; then export NPM_CONFIG_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_27}"; else unset NPM_CONFIG_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_28}" ]; then export PIP_CERT="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_28}"; else unset PIP_CERT; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_29}" ]; then export PIP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_29}"; else unset PIP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_30}" ]; then export REQUESTS_CA_BUNDLE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_30}"; else unset REQUESTS_CA_BUNDLE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_31}" ]; then export SSL_CERT_FILE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_31}"; else unset SSL_CERT_FILE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_32}" ]; then export WSS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_32}"; else unset WSS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_33}" ]; then export WS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_33}"; else unset WS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_34}" ]; then export YARN_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_34}"; else unset YARN_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_35}" ]; then export YARN_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_35}"; else unset YARN_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_36}" ]; then export YARN_NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_36}"; else unset YARN_NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_37}" ]; then export all_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_37}"; else unset all_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_38}" ]; then export ftp_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_38}"; else unset ftp_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_39}" ]; then export http_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_39}"; else unset http_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_40}" ]; then export https_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_40}"; else unset https_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_41}" ]; then export no_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_41}"; else unset no_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_42}" ]; then export npm_config_cafile="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_42}"; else unset npm_config_cafile; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_43}" ]; then export npm_config_http_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_43}"; else unset npm_config_http_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_44}" ]; then export npm_config_https_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_44}"; else unset npm_config_https_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_45}" ]; then export npm_config_noproxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_45}"; else unset npm_config_noproxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_46}" ]; then export npm_config_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_46}"; else unset npm_config_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_47}" ]; then export ws_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_47}"; else unset ws_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_48}" ]; then export wss_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_48}"; else unset wss_proxy; fi fi if [ -n "${PATH:-}" ]; then export PATH='/home/vagrant/.nvm/versions/node/v24.13.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/codex-path':"$PATH"; else export PATH='/home/vagrant/.nvm/versions/node/v24.13.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/codex-path'; fi  exec '/bin/bash' -c 'pgrep -af '"'"'vitest.*ai-dm-board-snapshot'"'"' || true'
2 /bin/bash -c pgrep -af 'vitest.*ai-dm-board-snapshot' || true

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc 'npx vitest run --configLoader runner tests/unit/tools/ai-dm-board-snapshot.test.ts tests/unit/tools/ai-dm-board-delivery.test.ts tests/unit/tools/ai-dm-screenshot-probe.test.ts --maxWorkers=1' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The first combined run exceeded the short output window without preserving a usable result, so I restarted the same exact three-file command with explicit session polling. It is still running; no failure output has appeared yet.
 exited 1 in 104285ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ❯ tests/unit/tools/ai-dm-board-delivery.test.ts (17 tests | 2 failed) 91948ms
     × persists simulated image feedback and gives judges the feedback beside the image 4835ms
     × validates bytes and rejects a path that is not content-addressed beneath the root 9ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/ai-dm-board-delivery.test.ts > arena capture lifecycle and off-arm invariance > persists simulated image feedback and gives judges the feedback beside the image
AssertionError: expected null to deeply equal { readability: 4, …(4) }

- Expected:
{
  "missing": [],
  "readability": 4,
  "suggestion": "Keep the board picture aligned with the same round context.",
  "what_confused": [],
  "what_helped": [
    "The board picture made positions and nearby obstacles easy to compare.",
  ],
}

+ Received:
null

 ❯ tests/unit/tools/ai-dm-board-delivery.test.ts:600:33
    598|     });
    599|     expect(rows).toHaveLength(1);
    600|     expect(rows[0]?.uiFeedback).toEqual({
       |                                 ^
    601|       readability: 4,
    602|       what_helped: ['The board picture made positions and nearby obsta…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/unit/tools/ai-dm-board-delivery.test.ts > launcher image containment and binding > validates bytes and rejects a path that is not content-addressed beneath the root
Error: Board image binding is stale for the launcher fixture.
 ❯ validatedLauncherBoardImage src/vtt/mcp/entrypoint.ts:886:11
    884|   if (artifact.source.room !== manifest.room || artifact.source.round …
    885|     artifact.source.revision !== state.revision || artifact.source.sta…
    886|     throw new Error('Board image binding is stale for the launcher fix…
       |           ^
    887|   }
    888|   if (binding.primaryDispatchStartedAtUnixMs < artifact.capturedAtUnix…
 ❯ tests/unit/tools/ai-dm-board-delivery.test.ts:800:25

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


 Test Files  1 failed | 2 passed (3)
      Tests  2 failed | 63 passed (65)
   Start at  05:47:31
   Duration  103.92s (transform 1.88s, setup 0ms, import 2.86s, tests 99.61s, environment 0ms)


diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The three-file tool group is taking over a minute, consistent with the screenshot-probe timing tests; it remains live and has emitted no red result. I’m continuing to poll it rather than starting overlapping suites.
 exited 1 in 104190ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ❯ tests/unit/tools/ai-dm-board-delivery.test.ts (17 tests | 2 failed) 92004ms
     × persists simulated image feedback and gives judges the feedback beside the image 4762ms
     × validates bytes and rejects a path that is not content-addressed beneath the root 7ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/ai-dm-board-delivery.test.ts > arena capture lifecycle and off-arm invariance > persists simulated image feedback and gives judges the feedback beside the image
AssertionError: expected null to deeply equal { readability: 4, …(4) }

- Expected:
{
  "missing": [],
  "readability": 4,
  "suggestion": "Keep the board picture aligned with the same round context.",
  "what_confused": [],
  "what_helped": [
    "The board picture made positions and nearby obstacles easy to compare.",
  ],
}

+ Received:
null

 ❯ tests/unit/tools/ai-dm-board-delivery.test.ts:600:33
    598|     });
    599|     expect(rows).toHaveLength(1);
    600|     expect(rows[0]?.uiFeedback).toEqual({
       |                                 ^
    601|       readability: 4,
    602|       what_helped: ['The board picture made positions and nearby obsta…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/unit/tools/ai-dm-board-delivery.test.ts > launcher image containment and binding > validates bytes and rejects a path that is not content-addressed beneath the root
Error: Board image binding is stale for the launcher fixture.
 ❯ validatedLauncherBoardImage src/vtt/mcp/entrypoint.ts:886:11
    884|   if (artifact.source.room !== manifest.room || artifact.source.round …
    885|     artifact.source.revision !== state.revision || artifact.source.sta…
    886|     throw new Error('Board image binding is stale for the launcher fix…
       |           ^
    887|   }
    888|   if (binding.primaryDispatchStartedAtUnixMs < artifact.capturedAtUnix…
 ❯ tests/unit/tools/ai-dm-board-delivery.test.ts:800:25

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


 Test Files  1 failed | 2 passed (3)
      Tests  2 failed | 63 passed (65)
   Start at  05:48:16
   Duration  103.83s (transform 1.95s, setup 0ms, import 2.94s, tests 99.39s, environment 0ms)


diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
This exposed two new reds in `ai-dm-board-delivery` (63 passed, 2 failed): one unrelated simulated-feedback timeout symptom (`uiFeedback` stayed null after a 92-second file run), and one relevant stale launcher binding. I’m tracing the latter before changing anything; if its fixture embeds a historical digest, I will stop per the manifest rule rather than regenerate it.
exec
/bin/bash -lc "sed -n '540,620p' tests/unit/tools/ai-dm-board-delivery.test.ts && sed -n '720,820p' tests/unit/tools/ai-dm-board-delivery.test.ts && sed -n '850,900p' src/vtt/mcp/entrypoint.ts && rg -n \"stateDigest|boardStateDigest\" src/vtt/mcp/entrypoint.ts tests/unit/tools/ai-dm-board-delivery.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
    const directory = mkdtempSync(join(tmpdir(), 'semantic-board-off-invariance-'));
    const invocations = new Map<string, AgentInvocation[]>();
    const run = async (label: 'default' | 'image-off' | 'semantic-absent') => {
      const captured: AgentInvocation[] = [];
      invocations.set(label, captured);
      const args = [
        '--rooms', '1', '--reps', '1', '--seed', '3943001',
        '--out', join(directory, `${label}.jsonl`), '--dry-run', '--effort', 'low',
        '--model', 'gpt-5.6-luna', '--transport', 'mcp_minimal',
        '--initiative-profile', 'derived_v1',
        ...(label === 'image-off' ? ['--board-image', 'off'] : []),
        ...(label === 'semantic-absent'
          ? ['--renderer-profile', JSON.stringify(DEFAULT_RENDERER_PROFILE)]
          : []),
      ];
      return runArena(parseArenaArgs(args), {
        onPrimaryInvocation: (invocation) => { captured.push(structuredClone(invocation)); },
      });
    };
    const defaultResult = await run('default');
    const offResult = await run('image-off');
    const absentResult = await run('semantic-absent');
    expect(absentResult.every((row) =>
      row.semanticBoardBytes === 0 && row.semanticBoardTruncated.length === 0)).toBe(true);
    for (const [label, result] of [['image-off', offResult], ['semantic-absent', absentResult]] as const) {
      expect(result.map((row) => row.rawTurnContext))
        .toEqual(defaultResult.map((row) => row.rawTurnContext));
      expect(invocations.get(label)?.map((entry) => ({
        prompt: entry.prompt, instructions: entry.instructions, output: entry.output,
      }))).toEqual(invocations.get('default')?.map((entry) => ({
        prompt: entry.prompt, instructions: entry.instructions, output: entry.output,
      })));
    }

    const raw = offResult[0]?.rawTurnContext;
    if (raw === undefined || raw === null) throw new TypeError('Semantic-board-off row omitted rawTurnContext.');
    const row = offResult[0];
    if (row === undefined) throw new TypeError('Semantic-board-off run omitted its row.');
    const rawContext = record(JSON.parse(raw) as unknown, 'semantic-board-off raw context');
    expect(record(rawContext['state_ref'], 'semantic-board-off state reference')).toEqual({
      run_id: 'encounter:ai-dm-conversation',
      state_handle: `engine-state:${row.stateBinding.capsule.digest}`,
      expected_revision: row.stateBinding.capsule.revision,
    });
    expect(Buffer.byteLength(raw)).toBe(31_995);
    expect(createHash('sha256').update(raw).digest('hex')).toBe(E1C_RAW_CONTEXT_SHA256);
  });

  it('persists simulated image feedback and gives judges the feedback beside the image', { timeout: 60_000 }, async () => {
    const directory = mkdtempSync(join(tmpdir(), 'board-image-feedback-'));
    const service = new FakeSnapshotService();
    const invocations: AgentInvocation[] = [];
    const rows = await runArena(parseArenaArgs([
      '--rooms', '1', '--reps', '1', '--seed', '3943001', '--out', join(directory, 'rows.jsonl'),
      '--dry-run', '--board-image', 'png', '--effort', 'low', '--initiative-profile', 'derived_v1',
    ]), {
      boardSnapshotServiceFactory: async () => service,
      onPrimaryInvocation: (invocation) => { invocations.push(structuredClone(invocation)); },
    });
    expect(rows).toHaveLength(1);
    expect(rows[0]?.uiFeedback).toEqual({
      readability: 4,
      what_helped: ['The board picture made positions and nearby obstacles easy to compare.'],
      what_confused: [],
      missing: [],
      suggestion: 'Keep the board picture aligned with the same round context.',
    });
    expect(invocations[0]?.instructions?.split(UI_FEEDBACK_STARTUP_INSTRUCTION)).toHaveLength(2);
    expect(invocations[0]?.instructions?.split(BOARD_IMAGE_SCALE_STARTUP_INSTRUCTION)).toHaveLength(2);
    expect(service.closed).toBe(true);
  });

  it('capture_only_leaks_image_block is killed by off-arm byte identity at every model boundary', { timeout: 60_000 }, async () => {
    const directory = mkdtempSync(join(tmpdir(), 'board-image-off-invariance-'));
    const invocations = new Map<string, AgentInvocation[]>();
    const services = new Map<string, FakeSnapshotService>();
    const run = async (label: 'default' | 'off' | 'capture_only') => {
      const captured: AgentInvocation[] = [];
      invocations.set(label, captured);
      const service = label === 'capture_only' ? new FakeSnapshotService() : null;
      if (service !== null) services.set(label, service);
      params: { _meta: META, name: 'engine.get_turn_context', arguments: {} },
    });
    const offMcpResult = record(record(offResponse, 'off MCP response')['result'], 'off MCP result');
    expect(offMcpResult['content']).toEqual([{ type: 'text', text: '{"exact":"structured"}' }]);
    expect(offMcpResult['structuredContent']).toEqual({ exact: 'structured' });

    const captureOnlyRow = captureOnlyResult[0];
    if (captureOnlyRow?.boardImage.mode !== 'capture_only' || captureOnlyRow.boardImageEvidence === null) {
      throw new TypeError('Capture-only fixture omitted persisted board-image evidence.');
    }
    expect(services.get('capture_only')?.captures).toHaveLength(1);
    expect(services.get('capture_only')?.closed).toBe(true);
    expect(captureOnlyRow.boardImage.relativePath)
      .toBe(`board-images/${captureOnlyRow.boardImage.sha256}.png`);
    expect(offInvocation.instructions?.split(BOARD_IMAGE_SCALE_STARTUP_INSTRUCTION)).toHaveLength(1);
    expect(captureOnlyInvocation.instructions?.split(BOARD_IMAGE_SCALE_STARTUP_INSTRUCTION)).toHaveLength(1);
  });

  it('ui_feedback_offered_in_off_arm keeps the off-arm tool manifest byte-identical', () => {
    const state = generateRoom(3_943_006).encounter.state;
    const implicitOff = createEngineMcpRuntime(state, { toolProfile: 'dm' });
    const explicitOff = createEngineMcpRuntime(state, { toolProfile: 'dm' });
    const offBytes = canonicalJson(implicitOff.toolSurface.tools);
    expect(canonicalJson(explicitOff.toolSurface.tools)).toBe(offBytes);
    expect(offBytes).not.toContain('engine.submit_ui_feedback');

    const png = dimensionedPng('feedback-tool');
    const image = createEngineMcpRuntime(state, {
      toolProfile: 'dm',
      boardImageContent: { type: 'image', mimeType: 'image/png', data: png.toString('base64') },
    });
    expect(image.toolSurface.tools.map(({ name }) => name)).toContain('engine.submit_ui_feedback');
  });
});

describe('launcher image containment and binding', () => {
  it('validates bytes and rejects a path that is not content-addressed beneath the root', async () => {
    const root = mkdtempSync(join(tmpdir(), 'launcher-board-image-'));
    const state = { ...generateRoom(3_943_006).encounter.state, round: 1, revision: 1 };
    const png = dimensionedPng('launcher');
    const digest = hashBytes(png);
    const relativePath = `board-images/${digest}.png` as const;
    mkdirSync(join(root, 'board-images'), { recursive: true });
    writeFileSync(join(root, relativePath), png);
    const htmlBytes = Buffer.from('<!doctype html><html lang="en"><body><main>Board facts</main></body></html>\n');
    const htmlDigest = hashBytes(htmlBytes);
    const htmlRelativePath = `board-html/${htmlDigest}/board.html` as const;
    mkdirSync(join(root, 'board-html', htmlDigest), { recursive: true });
    writeFileSync(join(root, htmlRelativePath), htmlBytes);
    const source: BoardImageSource = {
      room: 1, round: state.round, revision: state.revision,
      // The launcher artifact identifies the DM-view state rendered by the board app.
      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
    };
    const fixturePath = join(root, 'fixture.json');
    const proposalSpoolPath = join(root, 'proposals.jsonl');
    writeFileSync(fixturePath, canonicalJson({ encounter: { state } }));
    writeFileSync(proposalSpoolPath, '');
    const manifest = {
      format: 'engine-mcp-launcher-v1', fixturePath,
      proposalSpoolPath, runId: encounterSessionId('encounter:test'),
      branchId: encounterBranchId('branch:test'),
      revision: 2, requestId: 'request:test', phase: 'initial', correctionNumber: 0,
      offerEnvironment: createLegacyEngineOptionEnvironmentBinding(),
      room: 1, historyKind: 'room_ready', requestKind: 'round_plan',
      boardImage: {
        artifactRoot: root,
        primaryDispatchStartedAtUnixMs: 2,
        artifact: {
          version: 'arena-board-image-v1', audience: 'dm', mimeType: 'image/png', relativePath,
          sha256: digest, bytes: png.byteLength, width: 1, height: 1,
          capturedAtUnixMs: 1, captureMs: 1, source, chromiumVersion: 'SIMULATED',
          html: {
            relativePath: htmlRelativePath,
            sha256: htmlDigest,
            bytes: htmlBytes.byteLength,
          },
        },
      },
    } satisfies EngineMcpLauncherManifest;
    const image = await validatedLauncherBoardImage(manifest, state);
    expect(Buffer.from(image?.data ?? '', 'base64')).toEqual(png);
    const htmlReference = await validatedLauncherBoardHtmlReference(manifest, state);
    expect(htmlReference?.text).toContain(htmlRelativePath);
    expect(htmlReference?.text).toContain(htmlDigest);

    const blindManifest: EngineMcpLauncherManifest = {
      ...manifest,
      dmMode: 'blind',
      toolProfile: 'blind',
      blindVisuals: [{
        kind: 'dm_board',
        ordinal: 1,
        primer_version: 'primer-v10',
        glyph_mode: 'full',
        capture_tile_px: 128,
      }],
    };
    expect(decodeEngineMcpLauncherManifest(blindManifest)).not.toBeNull();
    expect(await validatedLauncherBoardHtmlReference(blindManifest, state)).toBeUndefined();

          blind['role'] === 'player_board') && isPositiveSafeInteger(blind['ordinal']) &&
        typeof blind['primerVersion'] === 'string' && blind['primerVersion'].length > 0 &&
        (blind['glyphMode'] === 'none' || blind['glyphMode'] === 'light' || blind['glyphMode'] === 'full') &&
        (blind['captureTilePx'] === 64 || blind['captureTilePx'] === 128) &&
        isBoardSnapshotDomEvidence(blind['domEvidence']);
    })()) &&
    (artifact['html'] === undefined || (() => {
      const value = artifact['html'];
      if (typeof value !== 'object' || value === null || Array.isArray(value)) return false;
      const html = value as Readonly<Record<string, unknown>>;
      const htmlSha = html['sha256'];
      return Object.keys(html).every((key) => ['relativePath', 'sha256', 'bytes'].includes(key)) &&
        typeof htmlSha === 'string' && /^[a-f0-9]{64}$/u.test(htmlSha) &&
        html['relativePath'] === `board-html/${htmlSha}/board.html` &&
        isPositiveSafeInteger(html['bytes']);
    })()) &&
    Object.keys(source).every((key) => ['room', 'round', 'revision', 'stateDigest'].includes(key)) &&
    isPositiveSafeInteger(source['room']) && isPositiveSafeInteger(source['round']) &&
    Number.isSafeInteger(source['revision']) && typeof source['revision'] === 'number' &&
    source['revision'] >= 0 && typeof source['stateDigest'] === 'string' &&
    /^[a-f0-9]{64}$/u.test(source['stateDigest']);
}

export async function validatedLauncherBoardImage(
  manifest: EngineMcpLauncherManifest,
  state: EncounterState,
): Promise<McpImageContentBlock | undefined> {
  const binding = manifest.boardImage;
  if (binding === undefined) return undefined;
  if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
    throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
  }
  const artifact = binding.artifact;
  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
  if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
    artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
    throw new Error('Board image binding is stale for the launcher fixture.');
  }
  if (binding.primaryDispatchStartedAtUnixMs < artifact.capturedAtUnixMs) {
    throw new Error('Board image binding was captured after primary dispatch began.');
  }
  const root = resolve(binding.artifactRoot);
  const path = resolve(root, artifact.relativePath);
  const inside = relative(root, path);
  if (inside === '..' || inside.startsWith(`..${sep}`) || isAbsolute(inside)) {
    throw new Error('Board image path escaped its artifact root.');
  }
  const [realRoot, realPath] = await Promise.all([realpath(root), realpath(path)]);
  const realInside = relative(realRoot, realPath);
  if (realInside === '..' || realInside.startsWith(`..${sep}`) || isAbsolute(realInside)) {
    throw new Error('Board image path resolved outside its artifact root.');
tests/unit/tools/ai-dm-board-delivery.test.ts:125:    const png = dimensionedPng(input.source.stateDigest);
tests/unit/tools/ai-dm-board-delivery.test.ts:485:    expect(service.captures[1]?.source.stateDigest).not.toBe(service.captures[0]?.source.stateDigest);
tests/unit/tools/ai-dm-board-delivery.test.ts:772:      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
src/vtt/mcp/entrypoint.ts:293:    readonly stateDigest: string;
src/vtt/mcp/entrypoint.ts:866:    Object.keys(source).every((key) => ['room', 'round', 'revision', 'stateDigest'].includes(key)) &&
src/vtt/mcp/entrypoint.ts:869:    source['revision'] >= 0 && typeof source['stateDigest'] === 'string' &&
src/vtt/mcp/entrypoint.ts:870:    /^[a-f0-9]{64}$/u.test(source['stateDigest']);
src/vtt/mcp/entrypoint.ts:885:    artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
src/vtt/mcp/entrypoint.ts:928:    binding.artifact.source.stateDigest !== sourceDigest) {

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "sed -n '900,940p' src/vtt/mcp/entrypoint.ts && sed -n '1,35p' src/vtt/mcp/entrypoint.ts && rg -n \"createHash\\('sha256'\\)\\.update\\(canonicalJson\\(state\\)|sha256\\(canonicalJson\\(state\\)\\)|boardStateDigest\\(\" src tools tests/unit --glob '*.ts'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
    throw new Error('Board image path resolved outside its artifact root.');
  }
  const png = await readFile(realPath);
  const signature = Buffer.from([137, 80, 78, 71, 13, 10, 26, 10]);
  if (png.byteLength !== artifact.bytes || png.byteLength > 1_000_000 || png.byteLength < 24 ||
    !png.subarray(0, signature.byteLength).equals(signature) ||
    createHash('sha256').update(png).digest('hex') !== artifact.sha256 ||
    png.readUInt32BE(16) !== artifact.width || png.readUInt32BE(20) !== artifact.height) {
    throw new Error('Board image bytes do not match the launcher metadata.');
  }
  return { type: 'image', mimeType: 'image/png', data: png.toString('base64') };
}

export async function validatedLauncherBoardHtmlReference(
  manifest: EngineMcpLauncherManifest,
  state: EncounterState,
): Promise<McpTextContentBlock | undefined> {
  if (manifest.dmMode === 'blind' || manifest.toolProfile === 'blind') return undefined;
  const binding = manifest.boardImage;
  const html = binding?.artifact.html;
  if (binding === undefined || html === undefined) return undefined;
  if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
    throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
  }
  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
  if (binding.artifact.source.room !== manifest.room ||
    binding.artifact.source.round !== state.round ||
    binding.artifact.source.revision !== state.revision ||
    binding.artifact.source.stateDigest !== sourceDigest) {
    throw new Error('Board HTML binding is stale for the launcher fixture.');
  }
  const root = resolve(binding.artifactRoot);
  const path = resolve(root, html.relativePath);
  const inside = relative(root, path);
  if (inside === '..' || inside.startsWith(`..${sep}`) || isAbsolute(inside)) {
    throw new Error('Board HTML path escaped its artifact root.');
  }
  const [realRoot, realPath] = await Promise.all([realpath(root), realpath(path)]);
  const realInside = relative(realRoot, realPath);
  if (realInside === '..' || realInside.startsWith(`..${sep}`) || isAbsolute(realInside)) {
    throw new Error('Board HTML path resolved outside its artifact root.');
import { createInterface } from 'node:readline';
import { createHash } from 'node:crypto';
import { appendFileSync, readFileSync } from 'node:fs';
import { readFile, realpath } from 'node:fs/promises';
import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
import { canonicalJson } from '../../commands/canonical-json';
import type { EncounterState } from '../../combat/encounter';
import {
  encounterBranchId,
  encounterSessionId,
  type EncounterBranchId,
  type EncounterSessionId,
} from '../../combat/values';
import type { EngineProposalEnvelope, NarrationEnvelope } from '../engine-envelopes';
import type {
  HostScenario,
  HostSplitCandidate,
  QueuedSpeculativePlanEnvelope,
} from '../speculative-plan-types';
import {
  createEngineStateCapsuleForEnvironment,
  projectEngineEncounterState,
  type EngineCapsuleRequest,
  type EngineInitiativeProjection,
  type EngineOrdinaryRequestKind,
  type EnginePlanAdjustmentMetadata,
  type RuleReference,
} from '../engine-state-capsule';
import { engineActionRegistryForEnvironment } from '../engine-query-port';
import {
  decodeEngineOptionEnvironmentBinding,
  type EngineOptionEnvironmentBinding,
} from '../offers/offer-environment';
import {
  buildOfferEnvironment,
tools/ai-dm-board-snapshot-check.ts:20:    stateDigest: boardStateDigest(state),
tools/ai-dm-blind-board-snapshot-check.ts:25:    stateDigest: boardStateDigest(state),
tools/ai-dm-blind-board-snapshot-check.ts:49:    requireCondition(artifact.source.stateDigest === boardStateDigest(state), 'Image digest binding changed.');
tools/ai-dm-screenshot-probe.ts:2622:    stateDigest: boardStateDigest(state),
tools/ai-dm-screenshot-probe.ts:3615:        artifact.source.stateDigest !== boardStateDigest(candidate.state)
tools/ai-dm-board-snapshot.ts:201:export function boardStateDigest(state: EncounterState): string {
tools/ai-dm-board-snapshot.ts:218:  const digest = boardStateDigest(state);
tools/ai-dm-board-glyph-captures.ts:118:            stateDigest: boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:526:      boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1071:      new Set(first.map((candidate) => boardStateDigest(candidate.state))).size,
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1076:        digest: boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-screenshot-probe.test.ts:1081:        digest: boardStateDigest(candidate.state),
tests/unit/tools/ai-dm-board-snapshot.test.ts:67:    stateDigest: boardStateDigest(state),
tests/unit/tools/ai-dm-board-snapshot.test.ts:164:        { revision: state.revision, round: state.round, stateDigest: boardStateDigest(state) },
tests/unit/tools/ai-dm-board-snapshot.test.ts:260:    expect(boardStateDigest(state)).toBe(projection.stateDigest);
tests/unit/tools/ai-dm-board-snapshot.test.ts:261:    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
tests/unit/tools/d569-v5.test.ts:117:    startingRoomDigest: sha256(canonicalJson(state)), kbHash: cell.sharedKbHash,
tools/ai-dm-conversation.ts:4487:          stateDigest: boardStateDigest(rendererState),
src/vtt/mcp/entrypoint.ts:883:  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
src/vtt/mcp/entrypoint.ts:924:  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');

diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The stale binding is not an embedded hash: the test constructs a renderer artifact using the projected digest, while both launcher validators independently recompute the obsolete raw-state digest at `src/vtt/mcp/entrypoint.ts:883` and `:924`. Those validators consume the same renderer artifact, so I’m aligning both to the DM projection; otherwise the explicitly required line-770 re-derivation can never validate. The feedback failure has no digest comparison and will be rerun isolated after this fix.
apply patch
patch: completed
/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/mcp/entrypoint.ts
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc 'npx vitest run --configLoader runner tests/unit/tools/ai-dm-board-delivery.test.ts --maxWorkers=1' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The isolated delivery file is again long-running past 60 seconds. The digest validator fix is in place; I’m waiting for the full isolated result so the simulated-feedback behavior can be classified as deterministic or load-related from evidence.
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

 succeeded in 100547ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json

 Test Files  1 passed (1)
      Tests  17 passed (17)
   Start at  05:50:41
   Duration  100.25s (transform 1.68s, setup 0ms, import 2.56s, tests 96.28s, environment 0ms)


diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The isolated delivery rerun is clean: 17/17 passed in 100.25 seconds. The earlier `uiFeedback: null` was therefore load/timing-related, while the stale-binding failure was deterministic and is fixed by projecting both launcher image and HTML validator digests. No manifest or fixture hash was changed.
exec
/bin/bash -lc 'npx vitest run --configLoader runner tests/unit/tools/ai-dm-board-snapshot.test.ts tests/unit/tools/ai-dm-screenshot-probe.test.ts --maxWorkers=1' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 11776ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json

 Test Files  2 passed (2)
      Tests  48 passed (48)
   Start at  05:52:30
   Duration  11.42s (transform 1.65s, setup 0ms, import 2.53s, tests 7.45s, environment 0ms)


diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc 'npx vitest run --configLoader runner tests/unit/tools/d569-blind-experiment.test.ts tests/unit/tools/d569-second-family-manifest.test.ts tests/unit/tools/d569-v5.test.ts --maxWorkers=1' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

 exited 1 in 42107ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ❯ tests/unit/tools/d569-blind-experiment.test.ts (52 tests | 6 failed) 27425ms
     × validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment 1259ms
     × dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows 1143ms
     × adds exactly 180 separately labeled advice-assisted hint cells, 660 total 1101ms
     × consumes the frozen second encounter family for all 480 core cells 1105ms
     × records exact model effort cli and cliVersion for every observed row 1105ms
     × rejects either decisive integrity field alone while preserving rows with neither field 1098ms
 ❯ tests/unit/tools/d569-v5.test.ts (0 test)
 ❯ tests/unit/tools/d569-second-family-manifest.test.ts (27 tests | 8 failed) 9068ms
     × pins the amended exact contiguous ranges and verbatim independent ledger evidence 1124ms
     × tests/fixtures/arena-basis-brutal-2/seed-6207002.json satisfies budget, caster, terrain, and productive-offer invariants before its pin 36ms
     × tests/fixtures/arena-basis-brutal-2/seed-6207004.json satisfies budget, caster, terrain, and productive-offer invariants before its pin 244ms
     × tests/fixtures/arena-basis-brutal-2/seed-6207005.json satisfies budget, caster, terrain, and productive-offer invariants before its pin 66ms
     × tests/fixtures/arena-basis-brutal-2/seed-6207006.json satisfies budget, caster, terrain, and productive-offer invariants before its pin 60ms
     × tests/fixtures/arena-basis-brutal-2/seed-6207007.json satisfies budget, caster, terrain, and productive-offer invariants before its pin 232ms
     × tests/fixtures/arena-basis-brutal-2/seed-6207008.json satisfies budget, caster, terrain, and productive-offer invariants before its pin 88ms
     × tests/fixtures/arena-basis-brutal-2/seed-6207009.json satisfies budget, caster, terrain, and productive-offer invariants before its pin 62ms

⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-v5.test.ts [ tests/unit/tools/d569-v5.test.ts ]
TypeError: Invalid D569 experiment manifest: second_family_first_regeneration
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-v5.test.ts:68:15

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/15]⎯


⎯⎯⎯⎯⎯⎯ Failed Tests 14 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment
AssertionError: expected [ …(7) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+   "second_family_brutal_productivity",
+ ]

 ❯ tests/unit/tools/d569-blind-experiment.test.ts:409:27
    407|   it('validates the exact v5 Opus 4.8 arms and notes-only Fable Astra …
    408|     const frozen = manifest();
    409|     expect(codes(frozen)).toEqual([]);
       |                           ^
    410|     expect(frozen.version).toBe('d569-blind-experiment-v5');
    411|     expect(frozen.preregistrationAmendments).toEqual(EXPECTED_V5_AMEND…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/15]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:473:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/15]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > adds exactly 180 separately labeled advice-assisted hint cells, 660 total
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:635:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/15]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > consumes the frozen second encounter family for all 480 core cells
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:644:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/15]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > records exact model effort cli and cliVersion for every observed row
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:653:19

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/15]⎯

 FAIL  tests/unit/tools/d569-blind-experiment.test.ts > D569 preregistered experiment manifest and dry runner > rejects either decisive integrity field alone while preserving rows with neither field
TypeError: Invalid D569 experiment manifest: second_family_brutal_productivity
 ❯ dryRunD569Experiment tools/d569-blind-experiment.ts:677:11
    675|   const violations = validateD569ExperimentManifest(input, access);
    676|   if (violations.length > 0) {
    677|     throw new TypeError(`Invalid D569 experiment manifest: ${violation…
       |           ^
    678|   }
    679|   const manifest = d569ExperimentManifestSchema.parse(input);
 ❯ tests/unit/tools/d569-blind-experiment.test.ts:683:18

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/15]⎯

 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > pins the amended exact contiguous ranges and verbatim independent ledger evidence
AssertionError: expected [ 'first_regeneration', …(46) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "brutal_productivity",
+   "first_regeneration",
+   "independent_regeneration",
+   "first_regeneration",
+   "independent_regeneration",
+ ]

 ❯ tests/unit/tools/d569-second-family-manifest.test.ts:89:27
     87|     ]);
     88|     expect(frozen.ledgerEntry).toBe(D569_SECOND_FAMILY_LEDGER_ENTRY);
     89|     expect(codes(frozen)).toEqual([]);
       |                           ^
     90|   });
     91|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/15]⎯

 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207002.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207004.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207005.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207006.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207007.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207008.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
 FAIL  tests/unit/tools/d569-second-family-manifest.test.ts > D569 independent second-family manifest > tests/fixtures/arena-basis-brutal-2/seed-6207009.json satisfies budget, caster, terrain, and productive-offer invariants before its pin
AssertionError: expected [ { …(2) } ] to deeply equal []

- Expected
+ Received

- []
+ [
+   {
+     "code": "brutal_productivity",
+     "message": "every brutal monster must have a productive first-turn offer",
+   },
+ ]

 ❯ tests/unit/tools/d569-second-family-manifest.test.ts:103:54
    101|       const frozen = room(path);
    102|       expect(generatedRoomIntegrityViolations(frozen, inputs.fixtures.…
    103|       expect(brutalRoomMembershipViolations(frozen)).toEqual([]);
       |                                                      ^
    104|     },
    105|   );

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[9/15]⎯


 Test Files  3 failed (3)
      Tests  14 failed | 65 passed (79)
   Start at  05:52:47
   Duration  41.75s (transform 1.81s, setup 0ms, import 2.25s, tests 36.49s, environment 0ms)


diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The three D569 files produced their frozen-family baseline, but it is broader than the shorthand “six reds”: 14 failed tests plus one failed `d569-v5` suite (65 passed). The six `d569-blind-experiment` titles are exactly the known set; `d569-second-family-manifest` additionally has its ledger pin plus seven seed productivity failures, and `d569-v5` stops during manifest validation. None touches or reports `stateDigest`, so LAND-03 introduced no new D569 mechanism. I’m checking D635.42’s exact accounting before the after-run comparison.
exec
/bin/bash -lc "rg -n \"D635\\.42|six D569|second-family manifest|LAND-03\" /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.claude/decisions.md | tail -40 && sed -n '1,130p' tests/unit/tools/d569-v5.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
22958:Sol (impl-landing-investigation-sol.md, read-only, 387 k tokens), classification of every deterministic red: (c) DEFECT 1 — src/vtt/engine-query-port.ts:1163 refuses an attack/save offer only when the physical trace's tier === 'total', while execution (src/combat/encounter.ts:7183) refuses when attackLine.blocksSight || tier === 'total'; D635 made optical blocking independent of physical cover, so a seam-blocked ray with tier 'half' is OFFERED and then REFUSED at execution ("The target has Total Cover or is outside line of sight.") — 19 of the 23 scripted round-1 failures (hard 5117001-007, 009-013; brutal 6203002, 003, 005, 006, 008, 009; brutal-b 6206004) are exactly this offer/execution disagreement (e.g. hard 5117001 m6 Heavy Crossbow at the wizard along row line 6 between (8,5)/(8,6); brutal 6203002 m1 Radiant Flame after its move, along row line 5 between (8,4)/(8,5)). This violates the offers boundary (an offered option must execute) and is VIS-FIELD's to fix before landing. (c) DEFECT 2 — src/vtt/room-generator.ts:1223 hasOpenOpposingRay() calls a ray open when tier === 'none' without requiring blocksSight === false, so the generator's M576-E2 wall placement no longer certifies a sight lane. (a) LEGITIMATE CONSEQUENCE — 25 monsters across the ten frozen brutal-b rooms and 18 across seven of the ten frozen brutal-2 rooms (the D569 second family: 6207002, 004-009) lose every productive first-turn option because their only lanes were two-blocker seams (per-seed flanking cells listed; mechanism count 25 seam / 0 derived-fog / 0 other; the B2 consolidation shown non-causal); the D569 second-family validator re-checks productivity against the current engine, so the frozen 480-cell second family is now an invalid manifest. (a′) LATENT TEST-HELPER DEFECT — four scripted rows (brutal 6203004; brutal-b 6206001, 009, 010) fail because dryRunMonsterPlan() in arena-basis-brutal-b.test.ts:145 selects dispel-evil-and-good without its required activationChoice (ACTIVATION_CHOICE_INVALID at intent-resolver.ts:578); D635 changed the ranking and exposed it. (b) derived-fog/detection consequences: none. All three suites pass under main's tracer (in-memory replay). VERIFIED BY ME by reading: engine-query-port.ts:1163 tests only `tier === 'total'`; encounter.ts:7183 tests `attackLine.blocksSight || attackLine.tier === 'total'` — the disagreement is real. RULING: (1) a VIS-FIELD LANDING RECONCILIATION batch (LAND-01) fixes the two production defects and the helper now: the query port refuses to offer an attack/save whose trace blocksSight (agreement witness: for a seam-blocked pair the query offers nothing the reducer would refuse — a hand-authored fixture plus a property over the frozen families that every offered attack executes); the generator requires tier 'none' AND blocksSight false for an open opposing ray (witness + mutant); dryRunMonsterPlan materializes a legal activation choice or skips choice-requiring options; nothing else. (2) The frozen brutal-b and brutal-2 families and the D569 second-family manifest are the OWNER's decision, asked now (regenerate under the seam-aware generator, invalidating the second-family experiment identity: 240 cell state hashes and a new manifest version; or amend the productivity contract — sol and I advise against; or revisit D635). Landing waits for both.
22975:VERIFIED BY ME (verify-land-01.log): tsc 0/0; both OFFERS checks 0; 0 forbidden, 0 long lines; tests/fixtures untouched; MY MUTANTS — the query port's sight refusal disabled → both witnesses RED (the seam fixture and the 214-offer agreement property), restored 1a552fba…; the generator's predicate relaxed to tier-only → OPEN_RAY_IGNORES_BLOCKS_SIGHT RED, restored 8d85ae64…; 16 suites 299/299 (engine-query-port, r02, dm-tactical-intel, challenge-room, the three offer-environment suites, engine-mcp-handler/server/golden, board-delivery, board-snapshot, SIMULATED, dm-encounter-host-live-path, replay, detection); los-cover 112/112; EXACTLY the 16 expected reds (ten brutal-b productivity pins, six D569 second-family manifest) — the frozen-family consequence that is the owner's question 5; pair 36/36 in 38 s. Committed ebfcacaa (6 files 325+/22−). vis-field = ebfcacaa. Astra (resume 01a0a642…, medium; brief review-impl-land-01-astra.md; log review-land-01-astra-r1.log) reviews LAND-01 on the detached checkout; LAND-02 (fresh sol; brief impl-vis-field-land-02.md; log impl-land-02.log) re-pins vtt-encounter.spec.ts:237 by hand and diagnoses the board-snapshot spec's board-visible wait (D635.38) on the worktree, its Playwright runs on port 4470 only.
22977:## D635.42 — SUPERVISOR (2026-09-17 05:40): LAND-01 astra r1 = REJECT (0 P1, 1 P2: LAND-F1); LAND-02 re-pinned the spec and stopped BLOCKED on a manifest question; RULING: LAND-03 realigns the snapshot binding digest to the DM view state
22980:LAND-02 (sol 01a0aeac…, saved impl-land-02-sol.md): vtt-encounter.spec.ts:237 re-pinned by hand — fighter (2,3), cleric (1,4), wizard (1,2) normal sight only, dark target cell (4,3) unseen by all three, so DM fog and the owned-party complement are exactly {(4,3)}: DM fog count 1, DM token visible + hidden-from-players, player floor visible + fog count 1 + zero monster tokens; sol reports 3/3 passed on port 4470, tsc 0/0. MY VERIFICATION of the re-pin (patch applied to the detached checkout, port 4471, plus a reverse mutant) is in flight (verify-land-02-spec.log). Board-snapshot diagnosis (sol; VERIFIED BY ME in source): the service binds to boardStateDigest(state) = sha256(canonicalJson(state)) (tools/ai-dm-board-snapshot.ts:201) while the app renders sha256(canonicalJson(projectDmView(state).state)) (encounter-projections.ts:488 → encounter-app.ts:2308); since B3 the DM view state carries the derived fog that canonical state no longer holds (B2), so #waitForBoardVisible (:992-1030) never matches for any state with a non-empty derived field — 176 derived cells in the spec's fixture. This is not spec-only: every live capture (ai-dm-conversation.ts:4487, glyph captures, the two snapshot checks, the screenshot probe) binds through the same function and would time out. RULING (manifest addition under landing reconciliation, D635.36 pattern): the binding digest is the digest of the DM view state as the app defines it; boardStateDigest(state) becomes sha256(canonicalJson(projectDmView(state).state)) so all service callers bind to the rendered value; witness with a negative control (raw digest differs), the browser spec must pass, independent recomputations re-derived only where they model the rendered state, D569 demonstrate-then-normalize elsewhere, any embedded manifest digest = BLOCKED not regenerated. Dispatched as ONE sol lane (resume 01a0ae59…, brief impl-vis-field-land-03.md, log impl-land-03.log): Task A = LAND-01 fix r1, Task B = LAND-03. The uncommitted spec re-pin stays in the worktree, off-limits to the lane.
22983:On the detached checkout at ebfcacaa with the patch applied (port 4471): vtt-encounter.spec.ts 3/3 passed (1.0 m); reverse mutant (DM fog pin back to 0) → "Expected: 0 Received: 1", 1 failed; restored. Committed on claude/vis-field as 61dd454e while the LAND-03 lane runs (single-path add; the lane holds no git writes). Board-snapshot spec still red until LAND-03.
import { createHash } from 'node:crypto';
import { join } from 'node:path';
import { tmpdir } from 'node:os';
import { describe, expect, it } from 'vitest';
import { canonicalJson } from '../../../src/commands/canonical-json';
import { sha256 } from '../../../src/crypto/sha256';
import type { EncounterState } from '../../../src/combat/encounter';
import { applyRoomInitiativeProfile } from '../../../src/vtt/room-generator';
import {
  createEngineMcpRuntime,
  decodeArenaFixtureText,
  type EngineMcpLauncherManifest,
} from '../../../src/vtt/mcp/entrypoint';
import { buildOfferEnvironment } from '../../../src/vtt/offers/build-offer-environment';
import { BlindModelIngressRecorder, type BlindIngressRecord } from '../../../src/vtt/blind-model-ingress';
import {
  agentSessionIdFromCli,
  engineDispatchId,
  type AgentInvocation,
  type AgentSessionAdapter,
  type AgentSessionBinding,
  type AgentTurnResult,
} from '../../../src/vtt/agent-session';
import {
  classifyEngineCatalogEvidence,
  type EngineReadinessRecord,
} from '../../../src/vtt/engine-dispatch-evidence';
import {
  analyzeRegisteredPrimaryPair,
  D569_PRIMARY_JUDGES,
  type D569PairedAnalysisDocuments,
} from '../../../tools/d569-v5/analyze-primary-pair';
import { mergeRepairedHard } from '../../../tools/d569-v5/merge-repaired-hard';
import {
  d569ReconciliationSidecarSchema,
  sha256Text,
  type D569ReconciliationSidecar,
} from '../../../tools/d569-v5/reconciliation';
import {
  D569_HARD_REPLACEMENT_KEYS,
  validateD569FirstArm,
  validateD569RegisteredFirstArm,
} from '../../../tools/d569-v5/validate-first-arm';
import {
  D569_EXPERIMENT_MANIFEST_PATH,
  dryRunD569Experiment,
  validateD569ObservedRows,
  type D569DryRunCell,
  type D569ExperimentManifest,
  type D569ObservedRow,
} from '../../../tools/d569-blind-experiment';
import { canonicalD569SecondFamilyRegeneration } from '../../../tools/d569-second-family-manifest';
import { BRUTAL_10_PROTOCOL, buildRerunPacket } from '../../../tools/ai-dm-rerun-packet';
import { parseArenaArgs, runArena } from '../../../tools/ai-dm-arena';
import type { ConversationBoardSnapshotService } from '../../../tools/ai-dm-conversation';
import type { BoardImageArtifact, BoardSnapshotCapture } from '../../../tools/ai-dm-board-snapshot';
import { mkdirSync, mkdtempSync, readFileSync, writeFileSync } from '../../helpers/test-filesystem';

const PATCHED_COMMIT = 'd60a0571a4588879c65992a17ab95023911d17c1';
const MANIFEST = JSON.parse(readFileSync(D569_EXPERIMENT_MANIFEST_PATH, 'utf8')) as D569ExperimentManifest;
const access = {
  readText: (path: string): string => readFileSync(path, 'utf8'),
  secondFamilyAccess: {
    readFixture: (path: string): string => readFileSync(path, 'utf8'),
    regenerate: canonicalD569SecondFamilyRegeneration,
  },
};
const CELLS = dryRunD569Experiment(MANIFEST, access).filter((cell) =>
  cell.arm === 'gpt-5.6-luna-blind');
const FIRST_BRUTAL_STATE = applyRoomInitiativeProfile(
  decodeArenaFixtureText(readFileSync('tests/fixtures/arena-basis-brutal/seed-6203001.json', 'utf8')),
  'derived_v1',
);

function historical(room: number, rep: number, sessionId: string | null) {
  return { room, round: rep, outcome: 'authorized', sessionId };
}

function current(cell: D569DryRunCell, outcome: 'authorized' | 'refused' | 'service_null' | 'infrastructure_failed') {
  const room = cell.seed - (cell.basis === 'hard' ? 5_117_000 : 6_203_000);
  const scheduledCellKey = `${String(room)}:${String(cell.rep)}`;
  const dispatchId = `engine-dispatch:grid-${String(room)}-${String(cell.rep)}-0001`;
  const infrastructure = outcome === 'infrastructure_failed';
  const delivered = outcome === 'authorized' || outcome === 'refused';
  const boardSha = sha256(`board:${cell.basis}:${scheduledCellKey}`);
  const catalog = infrastructure
    ? { status: 'absent' as const, basis: 'required_engine_initialization_failed' as const, dispatchId, corroboration: ['startup failed'] }
    : { status: 'ready' as const, basis: 'required_cli_completed_with_valid_catalog' as const, dispatchId, advertisedInvocationCount: 0, resourceOperationCount: 0 };
  const delivery = infrastructure
    ? { status: 'infrastructure_absent' as const, dispatchId, measurement: null }
    : delivered
      ? { status: 'delivered' as const, dispatchId, contextSha256: 'a'.repeat(64), measurement: { baseBytes: 100, semanticBytes: 10 } }
      : { status: 'not_requested' as const, dispatchId, reason: 'catalog_ready_model_did_not_fetch' as const, measurement: null };
  const state = applyRoomInitiativeProfile(decodeArenaFixtureText(readFileSync(cell.fixturePath, 'utf8')), 'derived_v1');
  return {
    rowContractVersion: 'arena-row-v3', arm: 'single', basis: cell.basis, seed: cell.seed,
    room, round: cell.rep, scheduledCellKey, dispatchId, outcome,
    sessionId: infrastructure ? null : `agent-session:grid-${String(room)}-${String(cell.rep)}`,
    repoCommit: PATCHED_COMMIT, cli: 'codex', model: cell.model, effort: cell.effort,
    dmMode: 'blind', blindRepairArm: 'code_only', blindMaxAttempts: 3, blindFacts: false,
    decisionTransport: 'mcp_minimal', instructionSource: 'kb', escalated: false,
    escalationModel: null, escalationEffort: null, midRoundAdjustmentsEnabled: false, fallbackReason: null, planner: 'model',
    combatModel: 'initiative_segments_v1', initiativeOrder: [], plannedBy: { model: cell.model, effort: cell.effort },
    roundNarrative: null,
    authorizedPlan: outcome === 'authorized' ? [{
      actorId: 'monster:test', resolutionSummary: { actionSlots: [{ kind: 'wait', targetIds: [] }], movementFeet: 0 },
    }] : null,
    firstDecisionAccepted: outcome === 'authorized', decisionAttempts: 1,
    decisionRejectionCodes: [], normalizationCodes: [], skillName: null, skillHash: null,
    rationale: null, overridePolicy: 'strict',
    blindContextVersion: 'blind-turn-context-v1', blindIntentVersion: 'blind-round-intent-v1',
    blindIntentText: null, blindIntents: null, blindResolverOutcome: [], blindRejectionCodes: [],
    blindAttempts: [], blindResolverLatencyMs: 0, semanticBoardEvidence: null,
    creatureFactsEvidence: { sha256: 'c'.repeat(64), utf8Bytes: 1 },
    legalMovementEvidence: { sha256: 'd'.repeat(64), utf8Bytes: 1 },
    blindPrivateAnswerKey: {},
    startingRoomDigest: sha256(canonicalJson(state)), kbHash: cell.sharedKbHash,
    sharedKbComponentHashes: MANIFEST.sharedKb.componentHashes,
    boardImage: {
      mode: 'png', sha256: boardSha, bytes: 24, width: 1, height: 1, captureMs: 0,
      relativePath: `board-images/${boardSha}.png`,
    },
    uiFeedback: null,
    visualProfile: {
      primerVersion: MANIFEST.visualPins.blind.primerVersion,
      images: [{ role: MANIFEST.visualPins.blind.imageRole, sha256: boardSha, ordinal: 1 }],
      glyphMode: MANIFEST.visualPins.blind.glyphMode,
      captureTilePx: MANIFEST.visualPins.blind.captureTilePx,
    },
    engineCatalogEvidence: catalog,

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc 'npx vitest run --configLoader runner tests/unit/tools/ai-dm-conversation.test.ts --maxWorkers=1' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The D635.42 record confirms the supervisor’s “exactly 16” count came from the original gate’s ten brutal-b plus the six `d569-blind-experiment` failures. The two newly requested D569 files expose the same already-ruled frozen-family invalidity earlier in their own validation paths; they are not LAND-03 regressions. The conversation suite is now running and has not emitted failures.
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
`ai-dm-conversation` is still live after two minutes with no red output. I’m keeping it isolated as requested; this file exercises the real snapshot-service call path, so its completion is important evidence for the digest change.
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The conversation suite has passed three minutes without output; it remains active, not stalled at a reported assertion. I’ll continue polling in 30-second intervals and won’t overlap the browser service with it.
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
Still active at four minutes with no failure stream. The delivery suite’s 100-second runtime showed these tool tests are slow in this environment; I’m allowing the conversation file to finish normally rather than terminating an otherwise healthy run.
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The conversation file is now beyond five minutes but still has no assertion or timeout output. I’ll give it another minute; if it remains silent, I’ll inspect its child process state read-only before deciding whether this is a harness hang.
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "ps -eo pid,ppid,etime,stat,pcpu,pmem,args | rg 'vitest|ai-dm-conversation' | rg -v 'rg vitest|codex-linux-sandbox.*ps -eo'" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 0ms:
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc 'ps -eo pid,ppid,etime,stat,pcpu,pmem,comm,args --sort=-pcpu | head -25' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
    PID    PPID     ELAPSED STAT %CPU %MEM COMMAND         COMMAND
      1       0       00:00 Ss    0.0  0.0 codex-linux-san /home/vagrant/.codex/tmp/arg0/codex-arg0kHUXDj/codex-linux-sandbox --sandbox-policy-cwd /home/vagrant/PhpstormProjects/dnd-wt-vis-field --command-cwd /home/vagrant/PhpstormProjects/dnd-wt-vis-field --permission-profile {"type":"managed","file_system":{"type":"restricted","entries":[{"path":{"type":"special","value":{"kind":"root"}},"access":"read"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-vis-field"},"access":"write"},{"path":{"type":"special","value":{"kind":"slash_tmp"}},"access":"write"},{"path":{"type":"special","value":{"kind":"tmpdir"}},"access":"write"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-vis-field/.git"},"access":"read","missing_path_behavior":"skip"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-vis-field/.agents"},"access":"read","missing_path_behavior":"skip"},{"path":{"type":"path","path":"/home/vagrant/PhpstormProjects/dnd-wt-vis-field/.codex"},"access":"read","missing_path_behavior":"skip"}]},"network":"enabled"} --apply-seccomp-then-exec -- /bin/bash -c __CODEX_SNAPSHOT_OVERRIDE_SET_0="${CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS+x}" __CODEX_SNAPSHOT_OVERRIDE_0="${CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS-}" __CODEX_SNAPSHOT_OVERRIDE_SET_1="${CODEX_PERMISSION_PROFILE+x}" __CODEX_SNAPSHOT_OVERRIDE_1="${CODEX_PERMISSION_PROFILE-}" __CODEX_SNAPSHOT_OVERRIDE_SET_2="${CODEX_PLUGIN_METRICS_OUTPUT+x}" __CODEX_SNAPSHOT_OVERRIDE_2="${CODEX_PLUGIN_METRICS_OUTPUT-}" __CODEX_SNAPSHOT_OVERRIDE_SET_3="${CODEX_SESSION_ID+x}" __CODEX_SNAPSHOT_OVERRIDE_3="${CODEX_SESSION_ID-}" __CODEX_SNAPSHOT_OVERRIDE_SET_4="${CODEX_THREAD_ID+x}" __CODEX_SNAPSHOT_OVERRIDE_4="${CODEX_THREAD_ID-}" __CODEX_SNAPSHOT_OVERRIDE_SET_5="${CODEX_VERSION+x}" __CODEX_SNAPSHOT_OVERRIDE_5="${CODEX_VERSION-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_0="${ALL_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_0="${ALL_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_1="${BUNDLE_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_1="${BUNDLE_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_2="${BUNDLE_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_2="${BUNDLE_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_3="${BUNDLE_NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_3="${BUNDLE_NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_4="${BUNDLE_SSL_CA_CERT+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_4="${BUNDLE_SSL_CA_CERT-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_5="${CARGO_HTTP_CAINFO+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_5="${CARGO_HTTP_CAINFO-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_6="${CODEX_CA_CERTIFICATE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_6="${CODEX_CA_CERTIFICATE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_7="${CODEX_NETWORK_ALLOW_LOCAL_BINDING+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_7="${CODEX_NETWORK_ALLOW_LOCAL_BINDING-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_8="${CODEX_NETWORK_PROXY_ACTIVE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_8="${CODEX_NETWORK_PROXY_ACTIVE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_9="${CODEX_NETWORK_PROXY_ATTRIBUTION+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_9="${CODEX_NETWORK_PROXY_ATTRIBUTION-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_10="${CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_10="${CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_11="${CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_11="${CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_12="${CURL_CA_BUNDLE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_12="${CURL_CA_BUNDLE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_13="${DOCKER_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_13="${DOCKER_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_14="${DOCKER_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_14="${DOCKER_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_15="${ELECTRON_GET_USE_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_15="${ELECTRON_GET_USE_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_16="${FTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_16="${FTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_17="${GIT_SSL_CAINFO+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_17="${GIT_SSL_CAINFO-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_18="${HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_18="${HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_19="${HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_19="${HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_20="${NODE_EXTRA_CA_CERTS+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_20="${NODE_EXTRA_CA_CERTS-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_21="${NODE_USE_ENV_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_21="${NODE_USE_ENV_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_22="${NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_22="${NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_23="${NPM_CONFIG_CAFILE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_23="${NPM_CONFIG_CAFILE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_24="${NPM_CONFIG_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_24="${NPM_CONFIG_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_25="${NPM_CONFIG_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_25="${NPM_CONFIG_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_26="${NPM_CONFIG_NOPROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_26="${NPM_CONFIG_NOPROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_27="${NPM_CONFIG_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_27="${NPM_CONFIG_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_28="${PIP_CERT+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_28="${PIP_CERT-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_29="${PIP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_29="${PIP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_30="${REQUESTS_CA_BUNDLE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_30="${REQUESTS_CA_BUNDLE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_31="${SSL_CERT_FILE+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_31="${SSL_CERT_FILE-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_32="${WSS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_32="${WSS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_33="${WS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_33="${WS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_34="${YARN_HTTPS_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_34="${YARN_HTTPS_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_35="${YARN_HTTP_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_35="${YARN_HTTP_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_36="${YARN_NO_PROXY+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_36="${YARN_NO_PROXY-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_37="${all_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_37="${all_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_38="${ftp_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_38="${ftp_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_39="${http_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_39="${http_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_40="${https_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_40="${https_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_41="${no_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_41="${no_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_42="${npm_config_cafile+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_42="${npm_config_cafile-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_43="${npm_config_http_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_43="${npm_config_http_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_44="${npm_config_https_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_44="${npm_config_https_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_45="${npm_config_noproxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_45="${npm_config_noproxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_46="${npm_config_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_46="${npm_config_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_47="${ws_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_47="${ws_proxy-}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_48="${wss_proxy+x}" __CODEX_SNAPSHOT_PROXY_OVERRIDE_48="${wss_proxy-}" __CODEX_SNAPSHOT_PROXY_ENV_SET="${CODEX_NETWORK_PROXY_ACTIVE+x}"  if . '/home/vagrant/.codex/shell_snapshots/01a0ae59-86c2-7cb1-8b2e-bbf96f72fd73.1789638023249753372.sh' >/dev/null 2>&1; then :; fi  if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_0}" ]; then export CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS="${__CODEX_SNAPSHOT_OVERRIDE_0}"; else unset CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_1}" ]; then export CODEX_PERMISSION_PROFILE="${__CODEX_SNAPSHOT_OVERRIDE_1}"; else unset CODEX_PERMISSION_PROFILE; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_2}" ]; then export CODEX_PLUGIN_METRICS_OUTPUT="${__CODEX_SNAPSHOT_OVERRIDE_2}"; else unset CODEX_PLUGIN_METRICS_OUTPUT; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_3}" ]; then export CODEX_SESSION_ID="${__CODEX_SNAPSHOT_OVERRIDE_3}"; else unset CODEX_SESSION_ID; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_4}" ]; then export CODEX_THREAD_ID="${__CODEX_SNAPSHOT_OVERRIDE_4}"; else unset CODEX_THREAD_ID; fi if [ -n "${__CODEX_SNAPSHOT_OVERRIDE_SET_5}" ]; then export CODEX_VERSION="${__CODEX_SNAPSHOT_OVERRIDE_5}"; else unset CODEX_VERSION; fi if [ -n "$__CODEX_SNAPSHOT_PROXY_ENV_SET" ] || [ -n "${CODEX_NETWORK_PROXY_ACTIVE+x}" ]; then if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_0}" ]; then export ALL_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_0}"; else unset ALL_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_1}" ]; then export BUNDLE_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_1}"; else unset BUNDLE_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_2}" ]; then export BUNDLE_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_2}"; else unset BUNDLE_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_3}" ]; then export BUNDLE_NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_3}"; else unset BUNDLE_NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_4}" ]; then export BUNDLE_SSL_CA_CERT="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_4}"; else unset BUNDLE_SSL_CA_CERT; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_5}" ]; then export CARGO_HTTP_CAINFO="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_5}"; else unset CARGO_HTTP_CAINFO; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_6}" ]; then export CODEX_CA_CERTIFICATE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_6}"; else unset CODEX_CA_CERTIFICATE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_7}" ]; then export CODEX_NETWORK_ALLOW_LOCAL_BINDING="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_7}"; else unset CODEX_NETWORK_ALLOW_LOCAL_BINDING; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_8}" ]; then export CODEX_NETWORK_PROXY_ACTIVE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_8}"; else unset CODEX_NETWORK_PROXY_ACTIVE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_9}" ]; then export CODEX_NETWORK_PROXY_ATTRIBUTION="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_9}"; else unset CODEX_NETWORK_PROXY_ATTRIBUTION; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_10}" ]; then export CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_10}"; else unset CODEX_NETWORK_PROXY_BROKERED_CREDENTIALS; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_11}" ]; then export CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_11}"; else unset CODEX_NETWORK_PROXY_CREDENTIAL_BROKER_ACTIVE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_12}" ]; then export CURL_CA_BUNDLE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_12}"; else unset CURL_CA_BUNDLE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_13}" ]; then export DOCKER_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_13}"; else unset DOCKER_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_14}" ]; then export DOCKER_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_14}"; else unset DOCKER_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_15}" ]; then export ELECTRON_GET_USE_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_15}"; else unset ELECTRON_GET_USE_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_16}" ]; then export FTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_16}"; else unset FTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_17}" ]; then export GIT_SSL_CAINFO="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_17}"; else unset GIT_SSL_CAINFO; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_18}" ]; then export HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_18}"; else unset HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_19}" ]; then export HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_19}"; else unset HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_20}" ]; then export NODE_EXTRA_CA_CERTS="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_20}"; else unset NODE_EXTRA_CA_CERTS; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_21}" ]; then export NODE_USE_ENV_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_21}"; else unset NODE_USE_ENV_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_22}" ]; then export NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_22}"; else unset NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_23}" ]; then export NPM_CONFIG_CAFILE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_23}"; else unset NPM_CONFIG_CAFILE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_24}" ]; then export NPM_CONFIG_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_24}"; else unset NPM_CONFIG_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_25}" ]; then export NPM_CONFIG_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_25}"; else unset NPM_CONFIG_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_26}" ]; then export NPM_CONFIG_NOPROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_26}"; else unset NPM_CONFIG_NOPROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_27}" ]; then export NPM_CONFIG_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_27}"; else unset NPM_CONFIG_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_28}" ]; then export PIP_CERT="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_28}"; else unset PIP_CERT; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_29}" ]; then export PIP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_29}"; else unset PIP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_30}" ]; then export REQUESTS_CA_BUNDLE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_30}"; else unset REQUESTS_CA_BUNDLE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_31}" ]; then export SSL_CERT_FILE="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_31}"; else unset SSL_CERT_FILE; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_32}" ]; then export WSS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_32}"; else unset WSS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_33}" ]; then export WS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_33}"; else unset WS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_34}" ]; then export YARN_HTTPS_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_34}"; else unset YARN_HTTPS_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_35}" ]; then export YARN_HTTP_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_35}"; else unset YARN_HTTP_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_36}" ]; then export YARN_NO_PROXY="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_36}"; else unset YARN_NO_PROXY; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_37}" ]; then export all_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_37}"; else unset all_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_38}" ]; then export ftp_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_38}"; else unset ftp_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_39}" ]; then export http_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_39}"; else unset http_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_40}" ]; then export https_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_40}"; else unset https_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_41}" ]; then export no_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_41}"; else unset no_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_42}" ]; then export npm_config_cafile="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_42}"; else unset npm_config_cafile; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_43}" ]; then export npm_config_http_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_43}"; else unset npm_config_http_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_44}" ]; then export npm_config_https_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_44}"; else unset npm_config_https_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_45}" ]; then export npm_config_noproxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_45}"; else unset npm_config_noproxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_46}" ]; then export npm_config_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_46}"; else unset npm_config_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_47}" ]; then export ws_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_47}"; else unset ws_proxy; fi if [ -n "${__CODEX_SNAPSHOT_PROXY_OVERRIDE_SET_48}" ]; then export wss_proxy="${__CODEX_SNAPSHOT_PROXY_OVERRIDE_48}"; else unset wss_proxy; fi fi if [ -n "${PATH:-}" ]; then export PATH='/home/vagrant/.nvm/versions/node/v24.13.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/codex-path':"$PATH"; else export PATH='/home/vagrant/.nvm/versions/node/v24.13.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/codex-path'; fi  exec '/bin/bash' -c 'ps -eo pid,ppid,etime,stat,pcpu,pmem,comm,args --sort=-pcpu | head -25'
      2       1       00:00 S     0.0  0.0 bash            /bin/bash -c ps -eo pid,ppid,etime,stat,pcpu,pmem,comm,args --sort=-pcpu | head -25
      3       2       00:00 R     0.0  0.0 ps              ps -eo pid,ppid,etime,stat,pcpu,pmem,comm,args --sort=-pcpu
      4       2       00:00 S     0.0  0.0 head            head -25

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The read-only process check runs in a separate sandbox namespace and cannot see the active test session, so it was inconclusive. The original session still responds as running and has not produced failure output; I’m continuing the same run rather than duplicating it.
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The isolated conversation suite is still running at roughly eight minutes with no assertion stream. Its tests have several long arena simulations, so I’m preserving the requested full-file run and will report the actual duration once it exits.
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
No red output at nine minutes. I’ll allow the file through its likely 10-minute per-test ceiling; if the harness then reports a timeout, I’ll capture that as evidence rather than masking it with a narrower filter.
 succeeded in 605337ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json

 Test Files  1 passed (1)
      Tests  110 passed (110)
   Start at  05:53:49
   Duration  604.97s (transform 1.70s, setup 0ms, import 2.60s, tests 600.95s, environment 0ms)


diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
`ai-dm-conversation` completed cleanly: 110/110 passed in 604.97 seconds. Next is the required Chromium snapshot spec on the only authorized port (`4470`, one worker); this is the end-to-end proof that the service digest now matches the app-rendered digest.
exec
/bin/bash -lc 'PLAYWRIGHT_PORT=4470 PLAYWRIGHT_WORKERS=1 npx playwright test tests/browser/ai-dm-board-snapshot.spec.ts --project=chromium' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The Chromium snapshot spec is still active after one minute and has not emitted a failure. It is running with `PLAYWRIGHT_PORT=4470` and `PLAYWRIGHT_WORKERS=1` exactly as required.
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

 succeeded in 117425ms:
[WebServer] (node:38) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
[WebServer] (Use `node --trace-warnings ...` to show where the warning was created)

Running 2 tests using 1 worker

(node:245) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
  ✓  1 [chromium] › tests/browser/ai-dm-board-snapshot.spec.ts:258:1 › captures the full production DM board deterministically through durable save upload/load (15.9s)
  ✓  2 [chromium] › tests/browser/ai-dm-board-snapshot.spec.ts:284:1 › captures synchronized state-only DM, accessible-raster, and player evidence (22ms)

  2 passed (2.0m)

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The browser proof passed: 2/2 Chromium tests in 2.0 minutes, including the formerly timing-out production DM-board capture. I’ll now run the final query agreement suite and the brutal-b frozen-family baseline, then both TypeScript checks and repository hygiene checks.
exec
/bin/bash -lc 'npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts --maxWorkers=1' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 12261ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json

 Test Files  1 passed (1)
      Tests  20 passed (20)
   Start at  06:06:08
   Duration  11.94s (transform 1.35s, setup 0ms, import 2.11s, tests 8.36s, environment 0ms)


diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc 'npx vitest run --configLoader runner tests/unit/vtt/arena-basis-brutal-b.test.ts --maxWorkers=1' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 exited 1 in 27462ms:

 RUN  v4.1.10 /home/vagrant/PhpstormProjects/dnd-wt-vis-field

[spell-source-parse-cache] reused /tmp/dnd-srd-spell-source-parse-cache/c1e4d0ac346a65e4.json
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts (47 tests | 10 failed) 23760ms
     × pins seed 6206001 only after proving every brutal membership property from state 168ms
     × pins seed 6206002 only after proving every brutal membership property from state 30ms
     × pins seed 6206003 only after proving every brutal membership property from state 44ms
     × pins seed 6206004 only after proving every brutal membership property from state 67ms
     × pins seed 6206005 only after proving every brutal membership property from state 117ms
     × pins seed 6206006 only after proving every brutal membership property from state 148ms
     × pins seed 6206007 only after proving every brutal membership property from state 76ms
     × pins seed 6206008 only after proving every brutal membership property from state 39ms
     × pins seed 6206009 only after proving every brutal membership property from state 139ms
     × pins seed 6206010 only after proving every brutal membership property from state 372ms

⎯⎯⎯⎯⎯⎯ Failed Tests 10 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206001 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206001-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/10]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206002 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206002-monster-1 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/10]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206003 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206003-monster-1 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/10]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206004 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206004-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/10]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206005 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206005-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/10]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206006 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206006-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/10]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206007 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206007-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/10]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206008 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206008-monster-1 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/10]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206009 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206009-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[9/10]⎯

 FAIL  tests/unit/vtt/arena-basis-brutal-b.test.ts > second brutal arena basis family > pins seed 6206010 only after proving every brutal membership property from state
AssertionError: combatant:generated-6206010-monster-2 has no productive first-turn offer: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ assertBrutalMembership tests/unit/vtt/arena-basis-brutal-b.test.ts:392:84
    390|           slot.kind === 'use_world_object'));
    391|     });
    392|     expect(productive, `${monster.profile.id} has no productive first-…
       |                                                                                    ^
    393|   }
    394|
 ❯ tests/unit/vtt/arena-basis-brutal-b.test.ts:433:7

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[10/10]⎯


 Test Files  1 failed (1)
      Tests  10 failed | 37 passed (47)
   Start at  06:06:26
   Duration  27.13s (transform 1.34s, setup 0ms, import 1.91s, tests 23.76s, environment 0ms)


diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
The final agreement suite is green (20/20). Brutal-b is exactly the ruled baseline: 37 passed, ten productivity-pin failures for seeds `6206001` through `6206010`, with the same monster IDs as before. No helper or non-productivity row failed.
exec
/bin/bash -lc 'npx tsc -p tsconfig.app.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
exec
/bin/bash -lc 'npx tsc -p tsconfig.node.json --noEmit' in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 14426ms:
 succeeded in 27891ms:
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
Both compilers are clean: app TypeScript exited 0 in 14.43s; node TypeScript exited 0 in 27.89s. I’m doing the final hygiene pass now—diff whitespace, added-line width, exact status, fixture exclusion, and confirmation that the supervisor-owned LAND-02 file remains byte-identical.
exec
/bin/bash -lc "git diff --check && git diff --unified=0 | awk '/"'^'"\\+\\+\\+/{next} /"'^'"\\+/{if (length(substr("'$0,2)) > 120) print length(substr($0,2)) ":" substr($0,2)}'"' && git status --short && git diff --name-only && git diff --stat && sha256sum tests/browser/vtt-encounter.spec.ts tools/ai-dm-board-snapshot.ts && git rev-parse --short=8 HEAD && git diff --name-only -- tests/fixtures" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
 M src/vtt/mcp/entrypoint.ts
 M tests/unit/tools/ai-dm-board-delivery.test.ts
 M tests/unit/tools/ai-dm-board-snapshot.test.ts
 M tests/unit/vtt/engine-query-port.test.ts
 M tools/ai-dm-board-snapshot.ts
src/vtt/mcp/entrypoint.ts
tests/unit/tools/ai-dm-board-delivery.test.ts
tests/unit/tools/ai-dm-board-snapshot.test.ts
tests/unit/vtt/engine-query-port.test.ts
tools/ai-dm-board-snapshot.ts
 src/vtt/mcp/entrypoint.ts                     |   9 ++-
 tests/unit/tools/ai-dm-board-delivery.test.ts |   4 +-
 tests/unit/tools/ai-dm-board-snapshot.test.ts |  49 +++++++++++-
 tests/unit/vtt/engine-query-port.test.ts      | 111 ++++++++++++++++++++++----
 tools/ai-dm-board-snapshot.ts                 |   2 +-
 5 files changed, 154 insertions(+), 21 deletions(-)
cb89f2ddc5a8b7af2f564b496a88f2d568b456518ccda4623083162f9d079227  tests/browser/vtt-encounter.spec.ts
e9398ff7d97d62804920ce6a3ac017917ad932949574bc47415e0eaa4a48e55c  tools/ai-dm-board-snapshot.ts
61dd454e

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "rg -n \"export function traceCombatantLine|function traceCombatantLine\" src/combat/cover.ts && sed -n '730,820p' src/combat/cover.ts && rg -n \"function stateAtResolvedOrigin\" -A32 tests/unit/vtt/engine-query-port.test.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
570:export function traceCombatantLine(
601:export function traceCombatantLineToCells(
168:function stateAtResolvedOrigin(
169-  state: EncounterState,
170-  actorId: CombatantId,
171-  position: GridCell,
172-): EncounterState {
173-  return {
174-    ...state,
175-    activeCombatant: actorId,
176-    activeInitiativeIndex: 0,
177-    tokens: state.tokens.map((token) => token.combatantId === actorId
178-      ? { ...token, position }
179-      : token),
180-  };
181-}
182-
183-describe('canonical engine query port', () => {
184-  it('ENGINE_QUERY_VISIBILITY_EQUALS_CANONICAL_DETECTOR including located outcomes', () => {
185-    const observerBase = monsterProfile('query-visibility-observer');
186-    const observer = {
187-      ...observerBase,
188-      rules: {
189-        ...observerBase.rules,
190-        senses: [
191-          { kind: 'normal_sight' as const },
192-          { kind: 'tremorsense' as const, rangeFeet: 5 },
193-        ],
194-      },
195-    };
196-    const located = playerProfile('query-visibility-located');
197-    const seen = playerProfile('query-visibility-seen');
198-    const unseen = playerProfile('query-visibility-unseen');
199-    const hidden = playerProfile('query-visibility-hidden');
200-    const created = createEncounter({

diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

exec
/bin/bash -lc "sed -n '540,630p' src/combat/cover.ts" in /home/vagrant/PhpstormProjects/dnd-wt-vis-field
 succeeded in 0ms:
    ? traces.reduce(
        (best, candidate) => compareOpticalCornerTraces(candidate, best) < 0 ? candidate : best,
        first,
      )
    : null;
  const selected: TerrainLineTrace = {
    ...physicalSelected,
    blocksSight,
    firstBlockingCell: opticalSelected?.firstBlockingCell ?? null,
  };
  stateTraces.set(traceKey, selected);
  retainEquivalentTrace(equivalentTraceKey, selected);
  return selected;
}

/** The canonical 2014 corner-to-corner trace for two bare grid cells. */
export function traceTerrainLine(
  state: EncounterState,
  sourceCell: GridCell,
  targetCell: GridCell,
  options: TerrainLineOptions = {},
): TerrainLineTrace {
  return traceSpaces(state, [sourceCell], [targetCell], options);
}

export interface CreatureLineOptions {
  readonly sourceAnchor?: GridCell;
  readonly additionalSightBlockingCells?: readonly GridCell[];
}

export function traceCombatantLine(
  state: EncounterState,
  sourceId: CombatantId,
  targetId: CombatantId,
  options: CreatureLineOptions = {},
): TerrainLineTrace {
  const blockerSignature = sightBlockingSignature(
    normalizedSightBlockingCells(options.additionalSightBlockingCells),
  );
  const traceKey = `${String(sourceId)}->${String(targetId)}@${options.sourceAnchor === undefined
    ? 'current'
    : cellKey(options.sourceAnchor)}#sight:${blockerSignature}`;
  const cached = combatantLineTraces.get(state)?.get(traceKey);
  if (cached !== undefined) return cached;
  const source = options.sourceAnchor === undefined
    ? combatantSpace(state, sourceId)
    : combatantSpaceAt(state, sourceId, options.sourceAnchor);
  const trace = traceSpaces(state, source.cells, combatantSpace(state, targetId).cells, {
    sourceId,
    targetId,
    includeCreatures: true,
    ...(options.additionalSightBlockingCells === undefined
      ? {}
      : { additionalSightBlockingCells: options.additionalSightBlockingCells }),
  });
  const stateTraces = combatantLineTraces.get(state) ?? new Map<string, TerrainLineTrace>();
  stateTraces.set(traceKey, trace);
  if (!combatantLineTraces.has(state)) combatantLineTraces.set(state, stateTraces);
  return trace;
}

export function traceCombatantLineToCells(
  state: EncounterState,
  sourceId: CombatantId,
  targetCells: readonly GridCell[],
  options: CreatureLineOptions = {},
): TerrainLineTrace {
  const blockerSignature = sightBlockingSignature(
    normalizedSightBlockingCells(options.additionalSightBlockingCells),
  );
  const targetSignature = targetCells.map(cellKey).join(';');
  const anchorSignature = options.sourceAnchor === undefined ? 'current' : cellKey(options.sourceAnchor);
  const traceKey = `${String(sourceId)}->cells:${targetSignature}@${anchorSignature}#sight:${blockerSignature}`;
  const cached = combatantLineTraces.get(state)?.get(traceKey);
  if (cached !== undefined) return cached;
  const source = options.sourceAnchor === undefined
    ? combatantSpace(state, sourceId)
    : combatantSpaceAt(state, sourceId, options.sourceAnchor);
  const trace = traceSpaces(state, source.cells, targetCells, {
    sourceId,
    includeCreatures: true,
    ...(options.additionalSightBlockingCells === undefined
      ? {}
      : { additionalSightBlockingCells: options.additionalSightBlockingCells }),
  });
  const stateTraces = combatantLineTraces.get(state) ?? new Map<string, TerrainLineTrace>();
  stateTraces.set(traceKey, trace);
  if (!combatantLineTraces.has(state)) combatantLineTraces.set(state, stateTraces);
  return trace;
}


diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

codex
Implemented LAND-01-FIX-R1 and LAND-03.

Files changed:

- [engine-query-port.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/vtt/engine-query-port.test.ts)
- [ai-dm-board-snapshot.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tools/ai-dm-board-snapshot.ts)
- [ai-dm-board-snapshot.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/tools/ai-dm-board-snapshot.test.ts)
- [ai-dm-board-delivery.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/tools/ai-dm-board-delivery.test.ts)
- [entrypoint.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/mcp/entrypoint.ts) — both launcher validators independently recomputed the obsolete raw digest; aligned them after the delivery witness exposed the mismatch.

Witnesses:

- `SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed`
  - Giant Spider `(0,1)`, target `(5,1)`, blocking column `(3,0..2)`.
  - Blocked trace: `tier: half`, `blocksSight: true`; Web refused with `target_outside_line_of_sight`.
  - Column removed: `tier: none`, `blocksSight: false`; Web offered.
- `offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement`
  - Directly checks the query’s `traceCombatantLine` semantics cited at `engine-query-port.ts:1168-1170`.
  - Checked 214 offers, 316 attack/save components, including four save components.
  - Reducer remains an additional check: attacks enforce LOS; saves currently do not.
- `binds to the DM projection digest when a non-empty derived fog field separates it from raw state`
  - Five-cell row, observer at `0`, wall at `2`, darkness at `3–4`.
  - Hand-pinned derived fog: cells `3–4`.
  - Projected digest equals `boardStateDigest`; raw-state digest differs.

Mutants:

- Task A full-column save mutant:
  - Command: `npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts --maxWorkers=1 -t 'offered attacks and saves have query-clear traces'`
  - Mutant: 1 failed, 19 skipped; `seed-6203010.json` Web reported `blocksSight: true`.
  - Restored: 1 passed, 19 skipped.
- Task B reverse mutant restoring raw `canonicalJson(state)`:
  - New witness: 1 failed, 21 skipped.
  - Restored projected definition: 1 passed, 21 skipped.
  - Final production file hash restored to `e9398ff7d97d62804920ce6a3ac017917ad932949574bc47415e0eaa4a48e55c`.

Verification:

- `npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts --maxWorkers=1`
  - 20/20 passed.
- Board snapshot + screenshot probe:
  - 48/48 passed.
- Board delivery isolated:
  - 17/17 passed.
- `tests/unit/tools/ai-dm-conversation.test.ts`
  - 110/110 passed.
- `PLAYWRIGHT_PORT=4470 PLAYWRIGHT_WORKERS=1 npx playwright test tests/browser/ai-dm-board-snapshot.spec.ts --project=chromium`
  - 2/2 passed in 2.0 minutes.
- `npx tsc -p tsconfig.app.json --noEmit`
  - Exit 0.
- `npx tsc -p tsconfig.node.json --noEmit`
  - Exit 0.
- `git diff --check`
  - Clean.
- Added-line width check:
  - No added line over 120 columns.
- No fixture, `.claude`, or `docs` changes.

Expected red set remains unchanged:

- Brutal-b: 37 passed, exactly ten productivity failures—`pins seed 6206001...` through `pins seed 6206010...`.
- The canonical six D569 failures retain the same titles:
  - `validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment`
  - `dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows`
  - `adds exactly 180 separately labeled advice-assisted hint cells, 660 total`
  - `consumes the frozen second encounter family for all 480 core cells`
  - `records exact model effort cli and cliVersion for every observed row`
  - `rejects either decisive integrity field alone while preserving rows with neither field`

The additionally requested `d569-second-family-manifest` and `d569-v5` files expose the same already-ruled frozen-family invalidity earlier: the ledger pin plus seeds `6207002`, `6207004–6207009`, and `d569-v5` manifest bootstrap validation. No failure involves a `stateDigest`, embedded digest, or LAND-03 regression.

During execution the supervisor committed LAND-02, advancing HEAD from `ebfcacaa` to `61dd454e`. I did not touch that file; its hash remains `cb89f2ddc5a8b7af2f564b496a88f2d568b456518ccda4623083162f9d079227`. Final status contains exactly the five files listed above.

LAND-01-FIX-R1+LAND-03 DONE
diff --git a/src/vtt/mcp/entrypoint.ts b/src/vtt/mcp/entrypoint.ts
index da17bda8c5c696e29f416ffe06459449bfc4bac2..8bae2c98b3fec945b628e2b1ade652b3ba78ba84
--- a/src/vtt/mcp/entrypoint.ts
+++ b/src/vtt/mcp/entrypoint.ts
@@ -5,6 +5,7 @@
 import { dirname, isAbsolute, relative, resolve, sep } from 'node:path';
 import { canonicalJson } from '../../commands/canonical-json';
 import type { EncounterState } from '../../combat/encounter';
+import { projectDmView } from '../../combat/visibility';
 import {
   encounterBranchId,
   encounterSessionId,
@@ -880,7 +881,9 @@
     throw new TypeError('Board image binding is forbidden for non-round-plan launchers.');
   }
   const artifact = binding.artifact;
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (artifact.source.room !== manifest.room || artifact.source.round !== state.round ||
     artifact.source.revision !== state.revision || artifact.source.stateDigest !== sourceDigest) {
     throw new Error('Board image binding is stale for the launcher fixture.');
@@ -921,7 +924,9 @@
   if (manifest.phase === 'speculative' || manifest.requestKind === 'plan_adjustment') {
     throw new TypeError('Board HTML binding is forbidden for non-round-plan launchers.');
   }
-  const sourceDigest = createHash('sha256').update(canonicalJson(state), 'utf8').digest('hex');
+  const sourceDigest = createHash('sha256')
+    .update(canonicalJson(projectDmView(state).state), 'utf8')
+    .digest('hex');
   if (binding.artifact.source.room !== manifest.room ||
     binding.artifact.source.round !== state.round ||
     binding.artifact.source.revision !== state.revision ||
diff --git a/tests/unit/tools/ai-dm-board-delivery.test.ts b/tests/unit/tools/ai-dm-board-delivery.test.ts
index 421ea1ca306233b201330113af627696be9cdd21..58cffc59dd68ebfcc70244f3ac2f23d9316018d6
--- a/tests/unit/tools/ai-dm-board-delivery.test.ts
+++ b/tests/unit/tools/ai-dm-board-delivery.test.ts
@@ -12,6 +12,7 @@
 import { encounterBranchId, encounterSessionId, type AgentSessionId } from '../../../src/combat/values';
 import { canonicalJson } from '../../../src/commands/canonical-json';
 import { sha256 } from '../../../src/crypto/sha256';
+import { projectDmView } from '../../../src/combat/visibility';
 import { mcpRequestMeta, createMcpHandler } from '../../../src/vtt/mcp/handler';
 import {
   createDisabledEngineOfferFamilyPolicy,
@@ -767,7 +768,8 @@
     writeFileSync(join(root, htmlRelativePath), htmlBytes);
     const source: BoardImageSource = {
       room: 1, round: state.round, revision: state.revision,
-      stateDigest: sha256(canonicalJson(state)),
+      // The launcher artifact identifies the DM-view state rendered by the board app.
+      stateDigest: sha256(canonicalJson(projectDmView(state).state)),
     };
     const fixturePath = join(root, 'fixture.json');
     const proposalSpoolPath = join(root, 'proposals.jsonl');
diff --git a/tests/unit/tools/ai-dm-board-snapshot.test.ts b/tests/unit/tools/ai-dm-board-snapshot.test.ts
index 4ae4da3c155b16f53779d5819910b0b24aa1836c..384c5008901d1ad73190f6c073ef665aeeab0e48
--- a/tests/unit/tools/ai-dm-board-snapshot.test.ts
+++ b/tests/unit/tools/ai-dm-board-snapshot.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest';
 import { readFile } from '../../helpers/test-filesystem-promises';
 import { canonicalJson } from '../../../src/commands/canonical-json';
-import type { EncounterState } from '../../../src/combat/encounter';
+import { createEncounter, type EncounterState } from '../../../src/combat/encounter';
+import { sha256 } from '../../../src/crypto/sha256';
 import { terrainWallCells } from '../../../src/combat/terrain';
 import { projectDmView } from '../../../src/combat/visibility';
 import { projectDmBoard, projectStateOnlyDmBoard } from '../../../src/vtt/encounter-projections';
@@ -45,6 +46,7 @@
   type BoardSnapshotDomEvidence,
 } from '../../../tools/ai-dm-board-snapshot';
 import { installInteractiveDocument, interactiveElement } from '../../fixtures/interactive-dom';
+import { placedToken, playerProfile } from '../combat/fixtures';
 
 const CONTROL_FIXTURES = Array.from(
   { length: 10 },
@@ -214,6 +216,51 @@
     expect(stateOnly.stateDigest).toBe(projection.stateDigest);
   });
 
+  it('binds to the DM projection digest when a non-empty derived fog field separates it from raw state', () => {
+    const observer = playerProfile('snapshot-derived-fog-observer');
+    const state = createEncounter({
+      bounds: { columns: 5, rows: 1 },
+      combatants: [observer],
+      tokens: [placedToken(observer, 0, 0)],
+      blockedCells: [{ column: 2, row: 0 }],
+      environment: {
+        difficultTerrainRegions: [],
+        obscurementRegions: [],
+        lightRegions: [{
+          id: 'snapshot-derived-darkness',
+          level: 'darkness',
+          cells: [{ column: 3, row: 0 }, { column: 4, row: 0 }],
+        }],
+        movementRegions: [],
+        narrowOpeningRegions: [],
+      },
+    });
+    const view = projectDmView(state);
+    // The wall endpoint is visible; the two dark cells beyond it are the hand-derived fog field.
+    expect(view.state.foggedCells).toEqual([{ column: 3, row: 0 }, { column: 4, row: 0 }]);
+    const projection = projectDmBoard({
+      view,
+      coordinator: {
+        requestSequence: 1,
+        pendingRequest: null,
+        pendingCommand: null,
+        continuation: { kind: 'idle' },
+        pause: { kind: 'interrupted' },
+      },
+      controllers: [{
+        combatantId: observer.id,
+        controllerId: 'test:snapshot-derived-fog-observer',
+        kind: 'human',
+        generation: 0,
+      }],
+      history: [],
+      offerEnvironment: BOUND_OFFER_ENVIRONMENT,
+    });
+
+    expect(boardStateDigest(state)).toBe(projection.stateDigest);
+    expect(boardStateDigest(state)).not.toBe(sha256(canonicalJson(state)));
+  });
+
   it.each(CONTROL_FIXTURES)('renders generated bounds and every mechanical terrain cell for %s', async (path) => {
     const state = await loadArenaFixture(path);
     const projection = projectEncounterBoard(projectDmView(state));
diff --git a/tests/unit/vtt/engine-query-port.test.ts b/tests/unit/vtt/engine-query-port.test.ts
index 62acd4131b7a3469070f9f41ce9c246e4904c1e2..41e13bead1b505efa44d1beac1646626f034390e
--- a/tests/unit/vtt/engine-query-port.test.ts
+++ b/tests/unit/vtt/engine-query-port.test.ts
@@ -11,6 +11,7 @@
 import { monsterCombatantProfile } from '../../../src/combat/combatant';
 import { monsterAttackCommand, monsterSavingThrowCommand } from '../../../src/combat/monster-commands';
 import { GOBLIN_WARRIOR } from '../../../src/combat/statblocks/monsters';
+import { GIANT_SPIDER } from '../../../src/combat/statblocks/wild-beasts';
 import type { MonsterAttackAction, MonsterSavingThrowAction } from '../../../src/combat/statblock';
 import type { GridCell } from '../../../src/combat/grid';
 import { terrainBlocking, terrainKindOfWireBlocking } from '../../../src/combat/terrain';
@@ -136,6 +137,34 @@
   };
 }
 
+function blockedSaveState(blocked: boolean): {
+  readonly state: EncounterState;
+  readonly actorId: CombatantId;
+  readonly targetId: CombatantId;
+} {
+  const actorBase = monsterCombatantProfile(GIANT_SPIDER, {
+    combatantId: 'combatant:query-blocked-save-spider',
+    tokenId: 'token:query-blocked-save-spider',
+  });
+  const actor = {
+    ...actorBase,
+    rules: { ...actorBase.rules, speed: feet(0) },
+  };
+  const target = playerProfile('query-blocked-save-target');
+  return {
+    state: freshMonsterPlanningState(createEncounter({
+      bounds: { columns: 6, rows: 3 },
+      combatants: [actor, target],
+      tokens: [placedToken(actor, 0, 1), placedToken(target, 5, 1)],
+      blockedCells: blocked
+        ? Array.from({ length: 3 }, (_unused, row) => ({ column: 3, row }))
+        : [],
+    })),
+    actorId: actor.id,
+    targetId: target.id,
+  };
+}
+
 function stateAtResolvedOrigin(
   state: EncounterState,
   actorId: CombatantId,
@@ -526,9 +555,44 @@
         slot.use.target.kind === 'combatant' && slot.use.target.combatantId === opened.targetId))).toBe(true);
   });
 
-  it('offered attacks and saves in every frozen landing family pass the reducer LOS rule', () => {
+  it('SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed', () => {
+    const blocked = blockedSaveState(true);
+    const opened = blockedSaveState(false);
+    expect(traceCombatantLine(blocked.state, blocked.actorId, blocked.targetId)).toMatchObject({
+      // Boundary rays make physical cover Half, but every ray is optically blocked by the full column.
+      tier: 'half',
+      blocksSight: true,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(blocked.state, {
+      actorId: blocked.actorId,
+      targetId: blocked.targetId,
+      actionId: 'web',
+    })).toEqual({ legal: false, codes: ['target_outside_line_of_sight'] });
+    expect(availableEngineActorOptions(blocked.state, blocked.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === blocked.targetId))).toBe(false);
+
+    expect(traceCombatantLine(opened.state, opened.actorId, opened.targetId)).toMatchObject({
+      tier: 'none',
+      blocksSight: false,
+    });
+    expect(OFFER_ENVIRONMENT.queries.reach(opened.state, {
+      actorId: opened.actorId,
+      targetId: opened.targetId,
+      actionId: 'web',
+    })).toMatchObject({ legal: true });
+    expect(availableEngineActorOptions(opened.state, opened.actorId, OFFER_ENVIRONMENT).some((option) =>
+      option.actionSlots.some((slot) => slot.use.kind === 'saving_throw' &&
+        slot.use.actionId === 'web' && slot.use.target.kind === 'combatant' &&
+        slot.use.target.combatantId === opened.targetId))).toBe(true);
+  });
+
+  it('offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement', () => {
     const losFailures: string[] = [];
     let checkedOffers = 0;
+    let checkedComponents = 0;
+    let checkedSaveComponents = 0;
     for (const path of AGREEMENT_FIXTURE_PATHS) {
       const state = freshMonsterPlanningState(decodeArenaFixtureText(readFileSync(path, 'utf8')));
       const monsters = state.combatants.filter((combatant) =>
@@ -546,8 +610,9 @@
           if (uses.length === 0) continue;
           checkedOffers += 1;
           for (const use of uses) {
-            const targetId = use.targetIds[0];
-            if (targetId === undefined) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            if (use.targetIds.length === 0) throw new Error(`${path}: ${use.actionId} omitted its target.`);
+            checkedComponents += 1;
+            if (use.kind === 'saving_throw') checkedSaveComponents += 1;
             const executionState = stateAtResolvedOrigin(
               state,
               monster.profile.id,
@@ -563,15 +628,18 @@
                 candidate.id === use.actionId && candidate.kind === use.kind,
             );
             if (action === undefined) throw new Error(`${path}: resolved action ${use.actionId} is absent.`);
-            try {
-              if (action.kind === 'attack') {
-                reduceEncounter(
-                  executionState,
-                  monsterAttackCommand(action, monster.profile.id, targetId),
-                  () => 0.5,
-                );
-              } else {
-                reduceEncounter(
+            for (const targetId of use.targetIds) {
+              // This is the query's exact trace call at engine-query-port.ts:1168-1170: actor, target,
+              // resolved origin as sourceAnchor, canonical footprints/exclusions, and default optical blockers.
+              const trace = traceCombatantLine(executionState, monster.profile.id, targetId, {
+                sourceAnchor: resolution.mechanics.finalPosition,
+              });
+              expect(trace.blocksSight, `${path}:${option.optionId}:${use.actionId}:blocksSight`).toBe(false);
+              expect(trace.tier, `${path}:${option.optionId}:${use.actionId}:cover`).not.toBe('total');
+
+              if (action.kind === 'saving_throw') {
+                // force_save currently has no sight gate; this is an additional general execution check only.
+                expect(() => reduceEncounter(
                   executionState,
                   monsterSavingThrowCommand(
                     action,
@@ -580,12 +648,21 @@
                     use.multiattackComponent === true ? 'none' : use.slot === 'bonus' ? 'bonus_action' : 'action',
                   ),
                   () => 0.5,
+                )).not.toThrow();
+                continue;
+              }
+              try {
+                // Attack execution independently enforces the same sight/Total-Cover rule.
+                reduceEncounter(
+                  executionState,
+                  monsterAttackCommand(action, monster.profile.id, targetId),
+                  () => 0.5,
                 );
+              } catch (error) {
+                const message = error instanceof Error ? error.message : String(error);
+                if (!message.includes('Total Cover or is outside line of sight')) throw error;
+                losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
               }
-            } catch (error) {
-              const message = error instanceof Error ? error.message : String(error);
-              if (!message.includes('Total Cover or is outside line of sight')) throw error;
-              losFailures.push(`${path}:${monster.profile.id}:${option.optionId}:${use.actionId}`);
             }
           }
         }
@@ -593,6 +670,8 @@
     }
 
     expect(checkedOffers).toBe(214);
+    expect(checkedComponents).toBe(316);
+    expect(checkedSaveComponents).toBe(4);
     expect(losFailures).toEqual([]);
   }, 30_000);
 
diff --git a/tools/ai-dm-board-snapshot.ts b/tools/ai-dm-board-snapshot.ts
index 5bff8e143fbedea73f7792eb0bc364fe0586638c..35aa2cd17c300de66b229a75ec727c4ba1c8e912
--- a/tools/ai-dm-board-snapshot.ts
+++ b/tools/ai-dm-board-snapshot.ts
@@ -199,7 +199,7 @@
 }
 
 export function boardStateDigest(state: EncounterState): string {
-  return sha256Bytes(Buffer.from(canonicalJson(state), 'utf8'));
+  return sha256Bytes(Buffer.from(canonicalJson(projectDmView(state).state), 'utf8'));
 }
 
 function validateSource(state: EncounterState, source: BoardImageSource): void {

tokens used
780,967
Implemented LAND-01-FIX-R1 and LAND-03.

Files changed:

- [engine-query-port.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/vtt/engine-query-port.test.ts)
- [ai-dm-board-snapshot.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tools/ai-dm-board-snapshot.ts)
- [ai-dm-board-snapshot.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/tools/ai-dm-board-snapshot.test.ts)
- [ai-dm-board-delivery.test.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/tests/unit/tools/ai-dm-board-delivery.test.ts)
- [entrypoint.ts](/home/vagrant/PhpstormProjects/dnd-wt-vis-field/src/vtt/mcp/entrypoint.ts) — both launcher validators independently recomputed the obsolete raw digest; aligned them after the delivery witness exposed the mismatch.

Witnesses:

- `SAVE_OFFER_IGNORES_BLOCKS_SIGHT refuses Web behind a full column and opens when removed`
  - Giant Spider `(0,1)`, target `(5,1)`, blocking column `(3,0..2)`.
  - Blocked trace: `tier: half`, `blocksSight: true`; Web refused with `target_outside_line_of_sight`.
  - Column removed: `tier: none`, `blocksSight: false`; Web offered.
- `offered attacks and saves have query-clear traces; attacks also pass reducer LOS enforcement`
  - Directly checks the query’s `traceCombatantLine` semantics cited at `engine-query-port.ts:1168-1170`.
  - Checked 214 offers, 316 attack/save components, including four save components.
  - Reducer remains an additional check: attacks enforce LOS; saves currently do not.
- `binds to the DM projection digest when a non-empty derived fog field separates it from raw state`
  - Five-cell row, observer at `0`, wall at `2`, darkness at `3–4`.
  - Hand-pinned derived fog: cells `3–4`.
  - Projected digest equals `boardStateDigest`; raw-state digest differs.

Mutants:

- Task A full-column save mutant:
  - Command: `npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts --maxWorkers=1 -t 'offered attacks and saves have query-clear traces'`
  - Mutant: 1 failed, 19 skipped; `seed-6203010.json` Web reported `blocksSight: true`.
  - Restored: 1 passed, 19 skipped.
- Task B reverse mutant restoring raw `canonicalJson(state)`:
  - New witness: 1 failed, 21 skipped.
  - Restored projected definition: 1 passed, 21 skipped.
  - Final production file hash restored to `e9398ff7d97d62804920ce6a3ac017917ad932949574bc47415e0eaa4a48e55c`.

Verification:

- `npx vitest run --configLoader runner tests/unit/vtt/engine-query-port.test.ts --maxWorkers=1`
  - 20/20 passed.
- Board snapshot + screenshot probe:
  - 48/48 passed.
- Board delivery isolated:
  - 17/17 passed.
- `tests/unit/tools/ai-dm-conversation.test.ts`
  - 110/110 passed.
- `PLAYWRIGHT_PORT=4470 PLAYWRIGHT_WORKERS=1 npx playwright test tests/browser/ai-dm-board-snapshot.spec.ts --project=chromium`
  - 2/2 passed in 2.0 minutes.
- `npx tsc -p tsconfig.app.json --noEmit`
  - Exit 0.
- `npx tsc -p tsconfig.node.json --noEmit`
  - Exit 0.
- `git diff --check`
  - Clean.
- Added-line width check:
  - No added line over 120 columns.
- No fixture, `.claude`, or `docs` changes.

Expected red set remains unchanged:

- Brutal-b: 37 passed, exactly ten productivity failures—`pins seed 6206001...` through `pins seed 6206010...`.
- The canonical six D569 failures retain the same titles:
  - `validates the exact v5 Opus 4.8 arms and notes-only Fable Astra Sol panel amendment`
  - `dry-runs versioned Opus 4.8 cells without accepting historical Opus 5 rows`
  - `adds exactly 180 separately labeled advice-assisted hint cells, 660 total`
  - `consumes the frozen second encounter family for all 480 core cells`
  - `records exact model effort cli and cliVersion for every observed row`
  - `rejects either decisive integrity field alone while preserving rows with neither field`

The additionally requested `d569-second-family-manifest` and `d569-v5` files expose the same already-ruled frozen-family invalidity earlier: the ledger pin plus seeds `6207002`, `6207004–6207009`, and `d569-v5` manifest bootstrap validation. No failure involves a `stateDigest`, embedded digest, or LAND-03 regression.

During execution the supervisor committed LAND-02, advancing HEAD from `ebfcacaa` to `61dd454e`. I did not touch that file; its hash remains `cb89f2ddc5a8b7af2f564b496a88f2d568b456518ccda4623083162f9d079227`. Final status contains exactly the five files listed above.

LAND-01-FIX-R1+LAND-03 DONE
