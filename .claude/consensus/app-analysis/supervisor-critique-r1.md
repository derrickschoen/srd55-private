# APP-ANALYSIS-01 — supervisor critique of astra round 1

Every row below was verified by the supervisor on main a6228ba3 (code identical to 45f0bc33) by reading the cited lines or executing the cited utility; "confirmed" means the evidence holds as stated, not that the proposal is accepted as written.

| finding | verdict | supervisor evidence |
|---|---|---|
| A-F1 gate verdicts | **CONFIRMED** | tools/gate-vitest.mjs:58-62 computes `initialRunnerFailure` only when `initialFailedFiles.length === 0`; :95 pushes it; :106 `process.exitCode = failed.length === 0 ? 0 : 1`. `exitCode`, `signal`, `spawnError` are produced by gate-runner-lib.mjs:65-78 and consumed nowhere (grep: no reader in either gate). Same shape in gate-playwright.mjs:142/174/185. So (a) a child exit code ≠ 0 with a "successful" reporter is green, and (b) a global runner failure that coincides with ≥1 failed file is dropped when the serial retry passes. This is the runner that produced every gate verdict this window. |
| A-F2 build cache inputs | **CONFIRMED** | tools/dist-build-cache.mjs:26-40 lists src, public, index.html, four tools paths, licence texts; `?raw` imports from src resolve to 67 files under drizzle/ and 33 under docs/ (97 distinct outside src); vite.config.ts:114 embeds `git rev-parse HEAD`, not in the key. |
| A-F3 history cache miss | **CONFIRMED — finding against the supervisor's own verification** | src/vtt/dm-encounter-host.ts:592-604: identity check, then `isLinearAppend = suffix.length > 0 && …`, so an EMPTY suffix (unchanged history, fresh array) takes the full `detachedImmutable(history)` path. session-persistence.ts:2133 `history()` → `sessionHistory(...)` → :2437 `revisions.map(...)` allocates per call, so identity never matches. Consequence: every snapshot that does not append pays a full-history deep clone — the O(n) per read the F1 repair was meant to remove, on the read path. My instrumentation on 2026-09-12 saw "identity never hits" and "829 appends / 30 rebuilds" in an append-heavy test flow and I recorded identity-miss as a residual without following the empty-suffix branch to its consequence. Recorded in the handoff ledger. |
| A-F4 canonical JSON `__proto__` | **CONFIRMED by execution** | `npx tsx` probe of src/commands/canonical-json.ts `canonicalJson`: `{"__proto__":{"important":"retained?"},"x":1}` → `{"x":1}`; `{"nested":{"__proto__":7}}` → `{"nested":{}}`. Cause :56 `const result: Record<string, JsonValue> = {}` + indexed assignment. |
| A-F5 MCP execute(name: string, unknown) | plausible, not independently probed | engine-server.ts:363 signature as quoted. The compiler-fixture proposal is the right shape; the D569/D602 crash history makes it worth an M lane. |
| A-F6 spell level as number | plausible, spot-checked | domain/ids.ts:258-263 comment says the brand is unapplied; astra says the builder now narrows — the comment is stale either way. |
| A-F7 reverse edges combat→vtt | **CONFIRMED** at controllers.ts:13 (`DecisionProgram` from vtt/dm-bridge), coordinator.ts:36 and encounter.ts:16 are multi-line imports whose sources I did not expand; content-pack.ts:28 as cited. Matches my SCC result (75-file combat cycle). |
| A-F8 transport drift | **CONFIRMED** | worker-transport.ts:108-110: `initialSnapshot` returns `#initial` unconditionally; `subscribe` adds the listener with no terminal check and no replay. websocket-transport.ts:109-116 rejects when terminal and replays `#latestEvent`. |
| A-F9 inventory | **CONFIRMED**, independently found (my S-F5) | report.ts:35 placeholder, :42/:46 missing VTT_HANDOFF_ARTIFACT; hit today when the battery ran the strings literally. |
| A-F10 arena cost | consistent with my measurement | ai-dm-arena.test.ts 317.5 s / 57 tests; ai-dm-conversation.test.ts 691 s / 107 tests in today's gate report (the largest two of 638 files). |
| A-F11 docs | **CONFIRMED** | .claude/supervision.md:99 names `tsc -p tsconfig.app.json` as THE compile gate; BUILD-PLAN.md rows 2-5 `not-started`; ids.ts:260 comment. |

## Contests and merges
- A-F3 supersedes my S-F9 as the concrete first step; I withdraw S-F9's "freeze at construction" generalisation until A-F3's keyed projection exists and is measured. Keep the budgeted benchmark test from S-F9 as A-F3's verification.
- A-F7 is the executable first cut of my S-F1 (13 SCCs / 198 files, two of 77 and 75). Merge: A-F7 does the three extractions; S-F1's SCC-ratchet test is the acceptance for both and stops regrowth.
- A-F9 and my S-F5 are one item. Keep astra's broader framing (executable inventory) with my concrete failing case as the first test.
- A-F10 and my S-F3/S-F4 are one family: astra's seam (cell-execution port) is the mechanism; my tier split (`slow` project run once per landing) is the policy. Both are D613's lane.
- Ranking contest: I would place the test-cost family (A-F10/S-F3) above A-F6 (spell level types): 36% of unit-gate CPU is paid on every landing, the spell-level weakness has no observed wrong output. Astra's argument (broad read paths) is fair; I ask astra to re-rank with today's duration table in hand.
- Not covered by astra, please critique: S-F6 (three hand-rolled module resolvers, two with the same bug class this week), S-F7 (778 string-coded `throw new Error` vs 373 typed errors; sg rule at boundaries), S-F8 (casts concentrated at persistence/codec boundaries — my count is a regex estimate; an AST count is requested), S-F2 (encounter.ts split AFTER A-F7/S-F1, not before — astra's "do not split by size" and my "depends on S-F1" agree).
- A-F1's remark that "passed on retry" is not "load" applies to my own reporting today: the two homebrew browser failures were labelled load flakes by the runner at load ~2; the ledger already says cause not named.
