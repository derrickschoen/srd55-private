# QUIETSTONE-INTEGRATE-01 — residual ledger

## Implementation dc217f23 (final read impl-review-r3-astra.md, 2026-09-11 10:27 EDT; D607.1 rounds spent)
- Q-F11 RESIDUAL — Fable's image access is inferred indirectly: the probe compares model-reported PNG dimensions and coordinate-label count with expected values (tools/ai-dm-screenshot-probe.ts:2073) and never observes a Read tool-result event; matching numbers can pass without evidence of delivery, and a counting error after delivery reads as "image unverified"; missing structured_output is classified schema_rejected before the image check (:2069), permitting a scored zero without verified access. Disposition: the three-seat run may proceed, but Fable rows are reported as "indirect content check", and rows lacking transport evidence stay unverified and never support an art-regression conclusion. Closes with actual tool-result evidence of the PNG read.
- v2 re-delivery findings for the art assistant (plan §4): F-A props lack a top-left light bias; F-B token colours 21–49 (target ≤ 20); F-C seven palette near-twin pairs across 37 assets; F-D cleric/undead and cleric/beast silhouettes below 0.012; F-E skeleton/zombie, priest/acolyte, hobgoblin/bandit near-identical variants; F-F hazard misclassified as a prop; F-G floors/walls/doors below the 0.04 native-detail floor by design; F-H terrain-low-wall root copy differs from the package copy.
- Q-F11 consequence (2026-09-11, D615): with the Fable seat limit-blocked, the harness's failure classification matters. Every Fable row from the D611 panel run must be inspected at harvest: a `blocked` row with a null score is correct; a `schema_rejected` row with score 0 is the Q-F11 ordering defect and must NOT enter the comparison. The Fable seat is excluded from the owner's D610 decision either way and reported as unavailable.
