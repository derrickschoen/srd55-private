# D694-INTEL-01 B6 — astra MEDIUM review r2 (session 01a0bfa1-4fb4-7791-b71b-2c3bec9b1693, 64,373 tokens, final message only) on 0a209799

**ACCEPT — P1: 0 / P2: 0 / P3: 0.** Both round-one findings are resolved at `0a209799`.

- **Living 1/50 witness:** Both variants explicitly set and assert `life: 'living'` and the requested HP. Five-state equality covers complete reports, full dominance results, and public MCP actors/rows/opportunity costs. A hidden-HP threshold that changes these outputs between 1 and 50 now fails. Thresholds outside those endpoints remain untested; this is not universal HP coverage.

- **Fully perceived equivalence:** The new witness asserts whole participants, exact host participant membership, and both perceived player-side identities before comparing complete local/host reports and dominance. The corridor is meaningful: Fighter is adjacent to Guard, with Wizard farther along the constrained row. Dropping a perceived target or corrupting relevant position, occupancy, or combat inputs can affect offers or evaluation. It does not exercise effects, persistent areas, or world objects; a defect shared by the unchanged evaluation kernel could also pass. Those limits do not invalidate this positive projection witness.

- **Boundary:** All located model-facing opportunity/dominance producers route through `actorLocalOpportunityReport`, using cached offers and projected mechanics. Remaining production host calls are `engineDefaultPlanEntry` and `scriptedProposals`. The legacy symbol is absent. The authoritative-input compile negative remains meaningful against the privately branded local type.

- **Conversation:** Preserving exact `request.actors` is an appropriate in-scope correction. Count-based reconstruction selects a sorted candidate prefix and can substitute different actors; boundary lookup then throws `ACTOR_LOCAL_ACTOR_MISMATCH`. The change repairs reconstructed request membership. Host report evaluation remains unchanged, and conversation-test changes are exclusively renames.

- **Pins and scope:** Independently checked all five existing pin/host test files against `dd932e8b`: each is byte-identical after only the symbol substitution. Every literal is preserved. Exactly the nine manifest files changed; no fixture or documentation changes, regenerated expectations, or weakened assertions. The frozen plan hash matches; diff check passes and the worktree is clean.

- **Deletion evidence:** Ordered report IDs equal cached offer IDs; complete products match across relocation/removal, and Fighter ID/name and target references are rejected. These assertions catch report-level omissions, additions, and reordering. They do **not independently prove** the exact authoritative-minus-`Spear -> combatant:fighter` / `P25/T+0` ledger or Bandit→Wizard preservation; those remain recorded implementation evidence. The new positive witness additionally guards against uniform projection errors in its fully perceived scene.

Validation limitation: the requested wrapper run failed before tests with `EROFS` while Vite wrote its temporary config. I inspected the supervisor’s green verification logs and recorded reverse-order mutant result, but did not independently reproduce those runs or mutants.

D694-INTEL-01 B6 REVIEW R2 DONE
