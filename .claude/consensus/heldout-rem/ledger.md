# HELDOUT-RUNTIME-GUARD-01 — residual ledger (Astra final read review-runtime-guard-r3-astra.md, 2026-09-11 12:00 EDT; D607/D607.1 rounds spent; "fit to land after the full gate at D607.1's bounded level")

- RG-F4 residual (medium) — runtime import seeding recognises the named `createRequire` import but not the namespace/default forms (`import Module from 'node:module'; Module.createRequire(...)`, guard.ts:413) that retained static analysis explicitly permits (leak-check.ts:684); files reached outside the initial src/tools map still use the narrower worker parser without createRequire/alias propagation (worker:627, :142). Closes with equivalent symbol-backed value-edge discovery for every traversed source and all namespace import forms.
- RG-F6 residual (medium) — "actually loaded configurations" records only each server's configFile (worker:741); a root project config importing base-settings.mjs leaves the latter outside retained Rule N inspection although Vite's runner records the dependency (config.js:35285). Closes by including candidate-local configuration dependencies in the inspection set with a transitive imported-config regression.
- RG-F13 residual (HIGH) — canonical seed deduplication skips any resolved id whose realpath matches an existing seed (worker:601), so with preserveSymlinks a plugin that injects a protected import only when transforming the symlink path is never exercised. Closes by deduplicating transform work by load id + environment while keeping realpath identity for protection checks, with a transform-sensitive symlink control.
- Resolved in the final read: RG-F5, RG-F10, RG-F11, RG-F12 (and RG-F1/F2/F3/F7/F8/F9 earlier). The guard is NOT a complete fail-closed guarantee for the three forms above.
