
codex
A1. The audit worktree is the owner’s named repository checkout | **DISPROVED**: it is a linked worktree at `0f84e09f`, branch `claude/vtt-handoff`; the owner checkout’s `main` has advanced | `.git:1`; commands: `nl -ba .git`, `git worktree list --porcelain`, `git rev-parse HEAD` | Separate implementation location, publication location, and reported commit.

A2. This machine is Ubuntu 22.04.5 under WSL2, distribution name Ubuntu | **PROVED** | `/etc/os-release:1`; commands: `nl -ba /etc/os-release`, `cat /proc/sys/kernel/osrelease`, `printenv WSL_DISTRO_NAME` | Linux bootstrap and the distribution component of the UNC path.

A3. The owner repository exists and its handoff root already exists | **DISPROVED** for the handoff root: neither checkout has `.tmp/vtt-handoff` | Command: `ls -ld /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.tmp/vtt-handoff .tmp/vtt-handoff` | Bootstrap must create the exchange root; initial existence cannot be a prerequisite.

A4. UNC translation proves Windows can read and write the exchange | **UNPROVED**: translation succeeds, but access and interop were not exercised | Command: `wslpath -w /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.tmp/vtt-handoff`; `.git:1` establishes the owner path | Windows probe, permissions, and publication reliability remain separate checks.

A5. Node 24.13.0, npm 11.6.2, Python 3.10.12 and the named utilities are installed | **PROVED** for inspected executables | Commands: `command -v node npm python3 jq wslpath sg`, `node --version`, `npm --version`, `python3 --version` | Record actual tool versions; this does not prove dependencies or browser execution work.

A6. Node 24.13.0 is the project’s explicitly pinned required version | **UNPROVED**: package.json has no engine/package-manager pin; the launcher README expects Node 24 | `package.json:1`, `tools/discord-launcher/README.md:29`; command: `rg -n 'node-version|engines|packageManager|Node 24' .github package.json README.md docs/serving.md tools/discord-launcher/README.md` | Doctor must distinguish installed version from a documented project requirement.

A7. Dependency installation in this worktree is isolated | **DISPROVED**: `node_modules` symlinks into the owner checkout | Command: `ls -ld node_modules`; `.git:1` identifies the linked checkout | Installing/updating packages could disturb the running owner application or experiment.

A8. Standard Python venv bootstrapping has everything it needs | **DISPROVED** for ensurepip availability; the alternative pip/uv workflow remains **UNPROVED operationally** | `/usr/lib/python3.10/venv/__init__.py:330`; commands: `rg -n 'ensurepip|python3-venv|sys.exit' /usr/lib/python3.10/venv/__init__.py`, `ls -ld /usr/lib/python3.10/ensurepip /usr/lib/python3.10/ensurepip/__main__.py` | Do not equate missing ensurepip with mandatory sudo or repeat an untested workaround as proven.

A9. Cached Playwright builds and user-local Python tools prove readiness | **UNPROVED**: files exist, but executability, libraries, credentials and interop were not tested | Commands: `ls /home/vagrant/.cache/ms-playwright`, `ls -l /home/vagrant/.local/bin/uv /home/vagrant/.local/bin/virtualenv /home/vagrant/.local/bin/pip3` | Doctor and later permitted smoke tests must establish usability.

A10. Exchange-cache files will automatically be durable repository artifacts | **DISPROVED** | `.gitignore:28`, `:32`, `:41`; command: `nl -ba .gitignore | tail -85` | Schemas, scripts and reproducible fixtures need tracked homes outside ignored exchange directories.

A11. The current baseline passes and the working tree contains no unrelated changes | **UNPROVED** for passing tests; **PROVED** that inspected Git status was clean | Commands: `git status --short`, `git rev-parse HEAD`; `package.json:10` defines the unexecuted build | No baseline or READY test claim may be inferred from this audit.

A12. Everything called “VTT” shares one encounter model | **DISPROVED** | `src/vtt/model.ts:1` defines Yjs tokens/fog/dice; `src/combat/encounter.ts:750` defines EncounterState; commands: `nl -ba src/vtt/model.ts | sed -n '1,105p'`, `nl -ba src/combat/encounter.ts | sed -n '330,380p;750,830p'` | Avoid extracting the shared-tabletop model as the authoritative encounter engine.

A13. The existing VTT transport interface already implements the requested application transport | **DISPROVED**: it sends binary updates and exposes peer/status callbacks, without request correlation or snapshots | `src/vtt/transports/transport.ts:11`; command: `nl -ba src/vtt/transports/transport.ts | sed -n '1,135p'` | Reuse lifecycle patterns, not an assumed compatible contract.

A14. `src/main.ts` mounts the encounter application for every `/vtt` URL | **DISPROVED**: its direct shortcut additionally requires `encounter=reference` | `src/main.ts:47`; command: `nl -ba src/main.ts | sed -n '37,115p'` | Smoke tests must use a verified entry route and actual loading flow.

A15. An encounter-engine browser Worker already exists | **DISPROVED** in inspected source: the host is constructed in the DOM view; the identified Workers handle database/capability work | `src/vtt/encounter-app.ts:1415`, `src/main.ts:181`; commands: `rg -n 'new Worker|RpcClient' src/main.ts src/ui src/db`, `nl -ba src/vtt/encounter-app.ts` | Worker engine execution is new implementation, not preservation of an existing mode.

A16. `npm run serve` starts an authoritative Node encounter backend | **DISPROVED**: it prepares `dist` and serves GET/HEAD files; POST receives 405 | `package.json:11`, `tools/serve.mjs:53`, `:137`; command: `nl -ba tools/serve.mjs | sed -n '1,240p'` | Preserve static startup while explicitly adding or defining Node engine authority.

A17. No `/dm/*` server exists outside the dev-only Vite bridge | **DISPROVED** | `tools/discord-launcher/codex-dm-bridge.mjs:102` handles `/dm/session`, `/dm/exchange`, `/dm/mirror`, and `:115` binds localhost; command: `nl -ba tools/discord-launcher/codex-dm-bridge.mjs | sed -n '1,160p'` | The runtime inventory must include this existing companion.

A18. The existing `/dm/*` companion owns authoritative encounter state | **DISPROVED**: the browser remains authority; the companion exchanges AI requests and mirrors browser revisions | `tools/discord-launcher/README.md:11`, `codex-dm-bridge.mjs:103`; commands: `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'`, `nl -ba tools/discord-launcher/codex-dm-bridge.mjs` | Reusing its listener does not establish server-authoritative gameplay.

A19. The companion authenticates DM callers | **DISPROVED**: localhost HTTP origins are accepted, absent Origin is accepted, and no caller credential check appears in the handler | `tools/discord-launcher/codex-dm-bridge.mjs:44`, `:81`; command: `nl -ba tools/discord-launcher/codex-dm-bridge.mjs | sed -n '1,160p'` | A new authoritative API cannot inherit DM authorization from locality or requestedRole.

A20. No WebSocket server exists | **DISPROVED**: a hand-written upgrade/frame echo server exists, although it has no encounter authority or authenticated peers | `tools/discord-launcher/relay-server.mjs:35`, README.md:5; commands: `nl -ba tools/discord-launcher/relay-server.mjs | sed -n '1,130p'`, `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'` | Distinguish an existing spike from a reusable production game transport.

A21. Running the Discord launcher is a harmless localhost bootstrap step | **DISPROVED**: its documented workflow starts a Cloudflare/ngrok tunnel | `tools/discord-launcher/README.md:3`, `:37`; command: `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'` | The directive forbids deploying public connectivity; do not reuse the launcher wholesale.

A22. The engine MCP entrypoint speaks the bootstrap v1 protocol | **DISPROVED**: it uses stdin/stdout JSON-RPC/MCP | `tools/engine-mcp-server.ts:1`, `src/vtt/mcp/entrypoint.ts:145`, `:505`; commands: `nl -ba tools/engine-mcp-server.ts | sed -n '1,110p'`, `rg -n 'stdin|stdout|readline|jsonrpc|import|reduceSessionEncounter|reduceEncounter' src/vtt/mcp/entrypoint.ts` | Preserve MCP separately; an adapter is required.

A23. The Vite AI bridge and Codex DM companion are the same service | **DISPROVED**: Vite creates a development token and launches Claude/fake agents; the companion launches Codex | `tools/ai-bridge/plugin.ts:338`, `tools/discord-launcher/README.md:14`; commands: `nl -ba tools/ai-bridge/plugin.ts | sed -n '330,377p'`, `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'` | Preserve distinct security and build boundaries.

A24. `encounter-app.ts` directly mutates live EncounterState outside DmEncounterHost | **DISPROVED by source inspection**: no state/reducer mutation path found; gameplay handlers call `#host` | `src/vtt/encounter-app.ts:1563`, `:2071`, `:2211`, `:2645`, `:2896`, `:2932`, `:2974`; command: `rg -n 'encounter\\s*=|host\\.|reduceEncounter|reduceVane|coordinator\\.|registry\\.' src/vtt/encounter-app.ts` | Do not invent an old independent UI rules implementation as the refactor target.

A25. All session mutations in the UI already pass through the host | **DISPROVED**: browser save rename and deletion call the store directly; active-session deletion first closes the view | `src/vtt/encounter-app.ts:1620`, `:1632`; command: `nl -ba src/vtt/encounter-app.ts | sed -n '1066,1144p;1361,1455p;1470,1569p;1600,1760p;3066,3135p'` | Session lifecycle/persistence needs an explicit boundary beyond gameplay commands.

A26. Save restore/import already passes through the host | **DISPROVED**: `restoreStored`, folder-save `import`, and uploaded-file durable import bypass it; loading navigates or remounts afterward | `src/vtt/encounter-app.ts:1681`, `:1687`, `:1704`, `:1733`; command: `nl -ba src/vtt/encounter-app.ts` | Preserve conflict checks and prevent a Worker host retaining a stale session after restore.

A27. Folder operations are authoritative-engine commands today | **DISPROVED**: folder rename/delete/write/selection and downloads are UI-side adapter operations | `src/vtt/encounter-app.ts:1622`, `:1643`, `:1664`, `:1671`, `:1715`; command: `rg -n '#sessionFlow|this\\.#store\\.|this\\.#folder\\.|HumanController|\\.decide\\(|\\.submit\\(' src/vtt/encounter-app.ts` | Keep browser file handles and user gestures outside the rules core.

A28. UI-local assignments are additional encounter mutations | **DISPROVED** for inspected assignments: staged actions, selection, accessibility mode, projections and render counters are presentation state | `src/vtt/encounter-app.ts:1069`, `:1376`, `:1509`, `:1600`; command: `rg -n '^export |^class |#[A-Za-z]+\\s*=|this\\.#state|this\\.state|reduce|apply|Math.random|Date.now|set[A-Z]' src/vtt/encounter-app.ts` | Preserve the existing separation instead of moving selection into authority.

A29. Publishing a host snapshot already means browser persistence has completed | **DISPROVED**: the UI explicitly waits for store flush and coalesces pending snapshots before sending the player projection | `src/vtt/encounter-app.ts:1487`, `:1495`, `:1513`; command: `nl -ba src/vtt/encounter-app.ts` | Mutation-result and event acknowledgment must preserve durability ordering.

A30. The host exposes raw EncounterState directly to the DM UI | **DISPROVED for its encounter projection**: `snapshot()` projects, and `projectDmView()` clones state fields | `src/vtt/dm-encounter-host.ts:421`, `src/combat/visibility.ts:501`; command: `nl -ba src/combat/visibility.ts | sed -n '501,548p'` | Reuse existing projection ownership; complete transitive alias safety still requires verification.

A31. Existing host subscription already provides an initial snapshot and cleanup | **PROVED** | `src/vtt/dm-encounter-host.ts:456`; command: `nl -ba src/vtt/dm-encounter-host.ts | sed -n '1,100p;190,202p;421,467p'` | Reuse this application-service behavior in both runtime adapters.

A32. The player connection uses `window.postMessage` with origin/source checking | **DISPROVED**: both views instantiate BroadcastChannel; `local-window-channel.ts` contains message codecs | `src/vtt/encounter-app.ts:1082`, `:1432`, `:1551`; command: `nl -ba src/vtt/encounter-app.ts` | Document the actual same-origin channel and its lack of peer authentication.

A33. The player UI cannot obtain a DM seat | **DISPROVED** as a security claim: it explicitly opens a URL with `view=dm`, which the entrypoint accepts | `src/vtt/encounter-app.ts:1121`, `src/main.ts:55`; commands: `nl -ba src/vtt/encounter-app.ts`, `nl -ba src/main.ts | sed -n '37,115p'` | Current local presentation separation is not hostile-player secret protection.

A34. Local player decisions are wholly trusted | **DISPROVED**: session, pending request, encounter revision and offered action are checked | `src/vtt/local-window-channel.ts:26`; command: `nl -ba src/vtt/local-window-channel.ts | sed -n '1,215p'` | Preserve these validation checks while adding authenticated seat binding where needed.

A35. Local host messages receive equally deep validation | **DISPROVED**: acceptance checks session/kind, object projection and `audience === 'player'` only | `src/vtt/local-window-channel.ts:77`; same command as A34 | An external protocol decoder needs stronger validation than the existing trusted-host codec.

A36. The current host supplies a separately authenticated projection for each human player | **DISPROVED**: it creates one `seat:local-party` projection owning all configured player IDs | `src/vtt/dm-encounter-host.ts:425`; command: `nl -ba src/vtt/dm-encounter-host.ts | sed -n '1,100p;190,202p;421,467p'` | `playerId` mapping and per-client subscriptions are new policy.

A37. Fog rendering alone performs existing player filtering | **DISPROVED**: player projection removes concealed combatants, blocked cells and objects before transmission | `src/combat/visibility.ts:549`, `:641`; command: `nl -ba src/combat/visibility.ts | sed -n '24,72p;549,571p;635,661p'` | New snapshots must start from the player-safe projection, including removal transitions.

A38. Existing state provides the bootstrap `visible` and persistent `explored` sets directly | **DISPROVED**: it has fog cells and observer/subject last-seen history, not an explored-cell history | `src/combat/encounter.ts:765`, `:792`, `src/combat/visibility.ts:641`; commands: `nl -ba src/combat/encounter.ts`, `nl -ba src/combat/visibility.ts` | Define honest projection semantics; do not invent exploration history.

A39. EncounterState is already a SceneSnapshot-shaped record | **DISPROVED**: it contains rules, combatants, resources, decisions, effects, environment, journals and optional imported content | `src/combat/encounter.ts:750`; command: `nl -ba src/combat/encounter.ts | sed -n '330,380p;750,830p'` | The bootstrap is a lossy view, never a replacement save format.

A40. Token IDs and combatant IDs are interchangeable | **DISPROVED**: CombatToken stores both; the board’s art lookup uses combatant identity | `src/combat/combatant.ts:122`, `src/vtt/encounter-board.ts:815`; commands: `nl -ba src/combat/combatant.ts | sed -n '118,150p'`, `nl -ba src/vtt/encounter-board.ts | sed -n '810,850p'` | Preserve IDs and define the `token.move.tokenId` lookup explicitly.

A41. Existing coordinates are screen pixels | **DISPROVED**: mechanical GridCell uses integer column/row; grid distance multiplies cell distance by five | `src/combat/grid.ts:3`, `:25`, `:72`; command: `nl -ba src/combat/grid.ts | sed -n '1,95p'` | Conversion belongs at the projection/command adapter, not in saved coordinates.

A42. Existing token anchors already equal bootstrap ground-center anchors | **DISPROVED for multicell tokens**: footprints extend east/south from the stored anchor | `src/combat/creature-space.ts:396`; command: `nl -ba src/combat/creature-space.ts | sed -n '396,430p'` | Center conversion needs footprint-dependent offsets and a reversible move conversion.

A43. Existing tokens supply elevation, facing and art asset ID | **DISPROVED**: CombatToken has ID, combatant ID, position and placement mode only | `src/combat/combatant.ts:122`; command: `nl -ba src/combat/combatant.ts | sed -n '118,150p'` | Zero elevation/facing would be declared rendering conventions; arbitrary z movement is not existing support.

A44. A blocked cell is a wall segment | **DISPROVED**: it is an impassable opaque square; terrain derives cell-local wall classification and synthetic source identity | `src/combat/grid.ts:14`, `src/combat/terrain.ts:107`; commands: `nl -ba src/combat/grid.ts`, `nl -ba src/combat/terrain.ts | sed -n '1,170p'` | Cell boundaries can be rendered, but original segment orientation/height cannot be recovered.

A45. Every terrain object is one rectangular wall with canonical blocking flags | **DISPROVED**: footprints are arbitrary unique absolute cells; blocking accepts a transitional independent movement/sight/cover triple | `src/combat/world-objects.ts:49`, `:170`, `src/combat/terrain.ts:15`; commands: `nl -ba src/combat/world-objects.ts`, `nl -ba src/combat/terrain.ts` | Do not erase footprint shape or assume cover, movement and vision are equivalent.

A46. Visible perimeter-wall art proves there is a corresponding mechanical wall | **DISPROVED**: board wall pieces derive from bounds; comments distinguish decorative art from engine doors | `src/vtt/encounter-board.ts:768`, `:910`; command: `rg -n 'door|light|wall|facing|asset|height|footprint' src/vtt/d365-sample-dungeon.ts src/vtt/encounter-board.ts` | Do not derive authoritative geometry from the rendered wall band.

A47. Doors have explicit `open` and `wallId` fields | **DISPROVED**: they are cell-footprint world objects; board openness is inferred from movement blocking | `src/combat/world-objects.ts:50`, `src/vtt/encounter-board.ts:773`; commands: `nl -ba src/combat/world-objects.ts`, `rg -n 'door|light|wall|facing|asset|height|footprint' src/vtt/d365-sample-dungeon.ts src/vtt/encounter-board.ts` | A single door-controlled segment cannot be derived losslessly without additional authored data/policy.

A48. Light regions are toggleable point lights | **DISPROVED**: they contain cells and bright/dim/darkness level; later overlapping entries win | `src/combat/world-objects.ts:70`, `:230`; command: `nl -ba src/combat/world-objects.ts | sed -n '1,250p'` | Position, radius, color, intensity and enabled state require authored presentation data; arbitrary regions are not lossless circles.

A49. Presence of world modification operations proves `door.set` and `light.set` semantics | **DISPROVED**: existing operations modify objects or set a region’s light level, without the agreed door relationship or point-light identity | `src/combat/world-objects.ts:120`, `src/combat/encounter.ts:11682`, `:11769`; commands: `nl -ba src/combat/world-objects.ts`, `rg -n 'set_light_level|door|modify_object' src/combat/encounter.ts` | Expose only operations with an explicit correct mapping; otherwise UNSUPPORTED.

A50. Encounter revision, journal revision, event sequence and scene identity are interchangeable | **DISPROVED**: state and journal have separate counters, branch/head identities and room transitions; EncounterState has no sceneId | `src/combat/encounter.ts:756`, `src/vtt/session-persistence.ts:281`; commands: `nl -ba src/combat/encounter.ts`, `nl -ba src/vtt/session-persistence.ts` | Define monotonic protocol revisions and seq across undo, reconnect and room changes.

A51. A saved session is a v1 JSON serialization of EncounterState | **DISPROVED**: current export is schema 12 `vtt-session-journal-dag`, including revisions, RNG, coordinator, controllers and fingerprints | `src/vtt/session-persistence.ts:129`, `:281`, `:2419`, `:3369`; command: `nl -ba src/vtt/session-persistence.ts` | Preserve the journal, replay, undo branches and deterministic resume.

A52. `encounter-state-codec.ts` is the normal saved-session decoder | **DISPROVED**: its callers are arena-fixture decoders; saved sessions use their own revision/bundle migration and decoding path | `src/vtt/arena-fixture.ts:268`, `session-persistence.ts:921`, `:3389`; command: `rg -n 'decodeEncounterStateV1|encounter-state-codec' src/vtt src/worker` | Save compatibility cannot be proven by testing only the arena codec.

A53. Reading a save fingerprint is a cheap header-only operation | **DISPROVED**: `decodeSavedSessionFingerprint` migrates and replays the session | `src/vtt/session-persistence.ts:3385`; command: `nl -ba src/vtt/session-persistence.ts | sed -n '245,267p;3363,3420p'` | Doctor/import/listing design must account for real processing cost.

A54. Old saves can bypass existing migration checks during extraction | **DISPROVED**: version windows, adjacent migration registry, checksums and DAG expansion are explicit | `src/vtt/session-persistence.ts:3238`, `:3252`; command: `nl -ba src/vtt/session-persistence.ts | sed -n '915,975p;3238,3304p'` | Preserve originals and test accepted historical formats rather than guessing normalization.

A55. Encounter persistence is the character database Worker’s persistence | **DISPROVED**: live encounter saves use a separate IndexedDB store with legacy localStorage migration; a SQLite store implementation also exists | `src/vtt/local-session-store.ts:18`, `:23`, `session-persistence.ts:409`; commands: `nl -ba src/vtt/local-session-store.ts | sed -n '1,110p'`, `nl -ba src/vtt/session-persistence.ts` | Choose persistence adapters deliberately; moving the engine does not move browser-owned saves automatically.

A56. Replacing randomness with a fresh seeded generator preserves behavior | **DISPROVED**: RNG state includes algorithm, initial seed, cursor, draw count and stream ID, with transactional rollback adapters | `src/combat/random.ts:15`, `:119`; command: `nl -ba src/combat/random.ts | sed -n '1,145p'` | Worker/Node parity and save resume require preserved draw/rollback semantics.

A57. Calling only `reduceEncounter` preserves every persisted VTT mechanic | **DISPROVED**: the shared persisted-command reducer delegates through `reduceVaneWarrenEncounter` | `src/vtt/session-encounter-reducer.ts:4`, `src/vtt/vane-warren.ts:799`; commands: `nl -ba src/vtt/session-encounter-reducer.ts | sed -n '1,190p'`, `rg -n 'reduceEncounter|reduceSessionEncounter|reduceVaneWarrenEncounter' src/vtt/vane-warren.ts src/vtt/mcp/engine-server.ts src/vtt/engine-query-port.ts src/combat/coordinator.ts` | Both runtimes must reuse the actual application reducer chain.

A58. A source-directory split alone proves a portable, renderer-independent core | **UNPROVED**: host imports span bridge, session, projection and content-composition modules; current TS libraries permit browser globals | `src/vtt/dm-encounter-host.ts:1`, `tsconfig.app.json:5`; commands: `nl -ba src/vtt/dm-encounter-host.ts`, `nl -ba tsconfig.app.json | sed -n '1,90p'` | Require transitive import-boundary checks and actual runtime/bundle evidence.

A59. Existing RPC envelopes are bootstrap-v1 compatible | **DISPROVED**: numeric IDs, no `v`, unrestricted generic params/results, different error vocabulary and no snapshot-event union | `src/rpc/protocol.ts:3`, `:45`, `:68`; command: `nl -ba src/rpc/protocol.ts | sed -n '1,185p'` | Reuse mechanisms behind an explicit versioned adapter.

A60. Existing RpcClient already supplies useful Worker lifecycle machinery | **PROVED**: pending-ID correlation, transfer lists, error rejection, listener cleanup and optional termination | `src/rpc/client.ts:49`, `:85`, `:105`; command: `nl -ba src/rpc/client.ts | sed -n '1,260p'` | Avoid rebuilding basic request/response plumbing unnecessarily.

A61. RpcClient already handles subscriptions, reconnect, deduplication and unknown outcomes | **DISPROVED**: no event subscription/resume/dedup store; no timeout; error rejects pending calls without establishing mutation outcome | `src/rpc/client.ts:54`, `:74`, `:85`; same command as A60 | Add protocol lifecycle semantics; never turn transport rejection into blind mutation retry.

A62. RpcClient validates received responses and fully handles failed sends | **DISPROVED**: it trusts typed message data; a synchronous postMessage throw occurs after insertion into the pending map | `src/rpc/client.ts:54`, `:95`; same command as A60 | External/untrusted transport adaptation needs runtime decoding and explicit send-failure cleanup.

A63. Worker registry dispatch can be reused unchanged in plain Node | **DISPROVED**: handlers receive database/lifecycle context, and eager discovery uses Vite’s `import.meta.glob` | `src/worker/handler.ts:6`, `src/worker/registry.ts:109`; commands: `nl -ba src/worker/handler.ts | sed -n '1,220p'`, `nl -ba src/worker/registry.ts | sed -n '1,210p'` | Reuse duplicate-method checks, validation and dispatch patterns through a portable registry seam.

A64. Database Worker boot is a generic engine runtime | **DISPROVED**, but its serial queue and clone-failure fallback are reusable | `src/db/worker.ts:69`, `:104`, `:147`, `src/db/worker-post.ts:7`; commands: `nl -ba src/db/worker.ts | sed -n '1,190p'`, `nl -ba src/db/worker-post.ts | sed -n '1,110p'` | Keep SQLite/OPFS boot and recovery separate from encounter execution; ordinary message dispatch adds no SAB requirement.

A65. Existing command idempotency automatically covers RPC request IDs | **DISPROVED**: character commands use `operation_uuid` and `expected_revision`; generic RPC dispatch does not deduplicate | `src/worker/handlers/commands.ts:31`, `src/commands/character-command-executor.ts:716`; commands: `nl -ba src/worker/handlers/commands.ts | sed -n '1,150p'`, `rg -n 'operation_uuid|idempot|replay|expected_revision' src/commands/character-command-executor.ts` | Reuse the domain pattern, not its character-specific persistence or identity scope.

A66. Existing RPC tests prove encounter execution in a real Worker | **DISPROVED**: client unit tests use ErrorTransport; browser RPC tests exercise character commands | `tests/unit/rpc/client.test.ts:5`, `tests/browser/command-rpc.spec.ts:11`; command: `rg -n 'it\\(|test\\(|Worker|MessageChannel|new RpcClient' tests/unit/rpc/client.test.ts tests/unit/rpc/registry.test.ts tests/browser/command-rpc.spec.ts` | Add real encounter Worker/Node parity tests; naming a test “worker” is insufficient.

A67. `npm test` covers every test file in the repository | **DISPROVED**: Vitest includes `tests/**/*.test.ts`; launcher `.test.mjs` files and Playwright specs are outside that set | `vitest.config.ts:34`, `tools/discord-launcher/README.md:50`; commands: `nl -ba vitest.config.ts | sed -n '1,160p'`, `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'` | Report exact suites executed, especially companion-server tests.

A68. `npm run build`, plain root tsc and `typecheck` are equivalent checks | **DISPROVED**: build runs `tsc -b`, Vite and dist checks; typecheck adds command-outcome checking; root tsconfig has `files:[]` | `package.json:10`, `:13`, `tsconfig.json:2`; commands: `nl -ba package.json | sed -n '1,180p'`, `nl -ba tsconfig.json` | Do not claim compilation or build-boundary coverage from the wrong command.

A69. Existing test commands automatically serialize against other work | **DISPROVED**: ordinary test/browser scripts bypass the gate wrappers; wrappers acquire the shared lock and may retry failures | `package.json:16`, `:24`, `tools/gate-vitest.mjs:6`; commands: `nl -ba package.json`, `nl -ba tools/gate-vitest.mjs | sed -n '1,100p'` | Respect the present no-test instruction; later record retries separately from clean first-pass results.

A70. A unique Playwright base port completely isolates a checkout | **DISPROVED**: it reserves a worker port range; TS build-info paths and default Vitest cache paths are machine-global | `tests/browser/fixtures/worker-origin.ts:25`, `tsconfig.app.json:15`, `tsconfig.node.json:14`, `vitest.config.ts:27`; commands: `nl -ba tests/browser/fixtures/worker-origin.ts | sed -n '1,60p'`, `nl -ba tsconfig.app.json`, `nl -ba tsconfig.node.json`, `nl -ba vitest.config.ts` | Prevent port/cache/build-state collisions with the active experiment.

A71. Browser tests exercise production Node serving and real AI services by default | **DISPROVED**: Playwright starts Vite with `AI_BRIDGE_FAKE=1`; production serving has a separate check command | `playwright.config.ts:24`, `:35`, `package.json:12`; commands: `nl -ba playwright.config.ts | sed -n '1,170p'`, `nl -ba package.json` | A top-down smoke test alone cannot prove Node authority or production boundary correctness.

A72. Suggested logical asset IDs are valid existing AssetIds | **DISPROVED**: existing branded IDs require `art.<kind>.<name>.v<n>` | `src/assets/ids.ts:6`; command: `nl -ba src/assets/ids.ts | sed -n '1,100p'` | Use a deliberate logical-to-existing-asset mapping without renaming persisted engine IDs.

A73. Copying a PNG into public art changes the encounter UI’s displayed asset | **DISPROVED**: the resolver generates PNG bytes from registered recipes and caches data URIs | `src/assets/starter-art-resolver.ts:16`, `:27`, `:37`; command: `nl -ba src/assets/starter-art-resolver.ts | sed -n '1,90p'` | Staging requires a later serving/resolver integration and top-down fallback.

A74. Production art can be added or replaced without changing independent expectations | **DISPROVED**: tests pin 92 files, manifest coverage, generated bytes and independent hashes | `tests/unit/assets/starter-art.test.ts:54`, `:132`, `tests/unit/source-is-greppable.test.ts:125`; commands: `nl -ba tests/unit/assets/starter-art.test.ts | sed -n '1,175p'`, `rg -n 'raster|art|git ls-files|PIN|png' tests/unit/source-is-greppable.test.ts` | Keep staging separate; do not regenerate expected hashes merely to pass.

A75. Existing art requests satisfy the new outbox/inbox agreement | **DISPROVED**: they use `id`, slug, prompt, dimensions and one deliverable at `<uuid>-<slug>`, without the new request/result envelopes | `art/requests/new-request.mjs:37`, `request.schema.json:7`; commands: `nl -ba art/requests/new-request.mjs | sed -n '1,265p'`, `nl -ba art/requests/request.schema.json | sed -n '1,200p'` | Non-destructive mapping/migration is required.

A76. `new-request.mjs --check` validates its JSON Schema and delivered images | **DISPROVED**: it never loads the schema; it checks selected fields, naming, placeholders and file existence | `art/requests/new-request.mjs:66`; command: `nl -ba art/requests/new-request.mjs | sed -n '1,265p'` | New validation must cover actual JSON types, completion, image contents, hashes and identities.

A77. The current checker implements the formats allowed by its schema | **DISPROVED**: schema allows PNG/WebP/SVG, but checker requires a `.png` deliverable filename and does not inspect JPG/source/sidecar files | `art/requests/request.schema.json:148`, `new-request.mjs:75`, `:82`; commands: `nl -ba art/requests/request.schema.json`, `nl -ba art/requests/new-request.mjs` | Do not present its clean verdict as complete inbox validation.

A78. The existing UUID generator is a tested reusable UUIDv7 library | **DISPROVED** as a tested-library claim: it hand-packs timestamp/version/variant bits; no matching automated tests were found, and importing the module executes CLI dispatch | `art/requests/new-request.mjs:27`, `:91`; commands: `nl -ba art/requests/new-request.mjs`, `rg -n 'uuidv7|new-request' tests art/requests --glob '*test*' --glob '*spec*'` | Extract/test or replace it; bit-shape inspection does not prove collision, clock or input behavior.

A79. Current art retry/completion conventions preserve originals and identify finished bundles | **DISPROVED**: README instructs deleting rejected art and reusing its ID; completion is request status plus file presence | `art/requests/README.md:35`, `new-request.mjs:78`; commands: `nl -ba art/requests/README.md | sed -n '1,190p'`, `nl -ba art/requests/new-request.mjs` | Replace the destructive convention with immutable deliveries and result-last publication.

A80. Current art ingestion already checks traversal, symlinks, limits, hashes and pass alignment | **DISPROVED**: the checker uses joined paths and `existsSync`, with none of those checks | `art/requests/new-request.mjs:66`; command: `nl -ba art/requests/new-request.mjs | sed -n '1,265p'` | Treat inbox files as untrusted inputs; validate before staging and never execute delivered scripts.

A81. Existing `pngDimensions` proves that a file is a valid transparent PNG | **DISPROVED**: it checks eight signature bytes and reads offsets 16/20; it does not validate chunks, CRCs, decoding or alpha | `src/assets/png.ts:213`; command: `nl -ba src/assets/png.ts | tail -60` | A real bounded image validator is necessary; signature and dimensions alone are insufficient.

A82. Existing provenance automatically licenses externally generated replacement art | **DISPROVED**: the queue README explicitly excludes assistant-made art from the current procedural provenance statement | `art/requests/README.md:59`, `.claude/decisions.md:8557`; commands: `nl -ba art/requests/README.md | sed -n '1,190p'`, `nl -ba .claude/decisions.md` | Retain source/tool/request provenance and review authorization; metadata cannot silently relicense a delivery.

A83. A published bootstrap-v1 implementation/schema already resolves integration ambiguities | **DISPROVED within searched implementation/docs** | No matching implementation found with `rg -n 'scene.snapshot|session.open|token.move|door.set|light.set|SceneSnapshot|READY.json|art/outbox|art/inbox' src tools docs --glob '!*.json'`; existing RPC differs at `src/rpc/protocol.ts:5` | Both agents must pin an explicitly published interpretation, not infer one from existing RPC.

A84. Bootstrap v1 specifies how later requests select an opened session or resume it | **UNPROVED/UNSPECIFIED**: sessionId appears only in the open result; no request session field, resume method or binding rule is supplied | Directive bootstrap text; existing contrast: `src/vtt/local-window-channel.ts:12` (`nl -ba src/vtt/local-window-channel.ts`) | Define connection-bound sessions or a separately documented transport binding before HTTP/reconnect implementation.

A85. Bootstrap v1 specifies subscription and event sequencing sufficiently for independent implementations | **UNPROVED/UNSPECIFIED**: no subscribe method, seq scope/reset rule, response/event order or reconnect replay rule is supplied | Directive bootstrap and §4; existing initial-subscription behavior: `src/vtt/dm-encounter-host.ts:456` (`nl -ba src/vtt/dm-encounter-host.ts`) | Agree on transport lifecycle without silently adding wire methods.

A86. Duplicate mutation IDs have fully specified behavior | **UNPROVED/UNSPECIFIED**: the directive requires tests but does not define retention, same-ID/different-payload handling, in-flight duplicates or cross-reconnect identity | Directive §4; existing generic dispatch has no deduplication: `src/worker/registry.ts:67` (`nl -ba src/worker/registry.ts`) | Exactly-once claims, retry handling and session caches depend on these decisions.

A87. Every malformed request can receive the stated correlated failure envelope | **UNPROVED/UNSPECIFIED** when JSON is invalid or `id` is absent/nonstring | Directive failure requires `id:string`; existing numeric protocol uses fallback ID 0 at `src/db/worker.ts:109` (`nl -ba src/db/worker.ts`) | Specify transport-level malformed-envelope errors without inventing valid correlation IDs.

A88. JSON Schema alone settles all SceneSnapshot semantics | **DISPROVED as an assumption**: identity uniqueness, door references, authoritative permissions, geometry and revision transitions need semantic checks; dialect/unknown-field policy are not specified | Directive bootstrap; existing schema explicitly selects draft 2020-12 at `art/requests/request.schema.json:2` (`nl -ba art/requests/request.schema.json`) | Publish structural schema plus semantic contract tests.

A89. Bootstrap number requirements imply positive integer dimensions or bounded facings | **DISPROVED**: only finiteness is stated generally; revisions/seq alone are explicitly safe integers | Directive bootstrap; existing grid’s stronger integer/positive constraints: `src/combat/grid.ts:25` (`nl -ba src/combat/grid.ts`) | Do not silently narrow v1 acceptance; distinguish valid wire values from unsupported engine coordinates.

A90. `door.open` and referenced wall blocking have an unambiguous combined meaning | **UNPROVED/UNSPECIFIED**: the directive does not say whether wall booleans are closed-state properties or already reflect door state | Directive bootstrap; existing board derives openness directly from movement blocking at `src/vtt/encounter-board.ts:773` (`nl -ba src/vtt/encounter-board.ts`) | Both renderers must agree which geometry blocks when the door opens.

A91. The art agreement is already a complete cross-platform schema | **UNPROVED/UNSPECIFIED**: types/vocabularies for views, passes, footprint, provenance/errors, hash placement, frame identity and normal convention are incomplete | Directive art exchange; current incompatible schema: `art/requests/request.schema.json:61`, `:139` (`nl -ba art/requests/request.schema.json`) | Publish these definitions before claiming Windows/result-validator compatibility.

A92. “Relative to the result bundle” and filename-prefix rules uniquely determine every asset path | **UNPROVED/UNSPECIFIED**: root manifest sits beside the UUID directory; nested-path base, separators, drive/UNC forms and case collisions need explicit rules | Directive art exchange; existing queue hardcodes a different directory at `art/requests/new-request.mjs:18` (`nl -ba art/requests/new-request.mjs`) | Portable traversal rejection, hash lookup and Windows staging depend on normalization policy.

A93. Result-last publication and checksums alone prove immutable, authentic, completed delivery | **UNPROVED**: they do not establish publisher identity, prevent post-validation changes, or define partial/blocked acceptance and repeat-delivery behavior | Directive contract/art publication; current writes are ordinary `writeFileSync` at `art/requests/new-request.mjs:100` (`nl -ba art/requests/new-request.mjs`) | Define commit-marker contents, immutable destination checks and the trusted publisher boundary.

A94. Existing Windows art or a Godot build is required to finish backend handoff | **DISPROVED**: the directive permits synthetic exchange fixtures and provisional Windows contracts, forbids joining applications, and labels failures PARTIAL | Directive §§2, 5–6; current exchange root absence verified by A3 | Backend preparation can finish independently; real-art quality and later integration cannot be claimed as tested.

A95. General pre-alpha replacement guidance overrides this unit’s preservation requirement | **DISPROVED**: the new directive explicitly requires preserving the application, saves, IDs and features | `AGENTS.md:7`, `:18`, `.claude/decisions.md:8550`, `:8235`; commands: `cat AGENTS.md`, `nl -ba .claude/decisions.md` | Apply the unit’s narrower preservation scope; replacement permission is not permission to lose campaigns.

A96. Existing refusal semantics can be replaced globally with bootstrap failures | **DISPROVED**: D278 reserves RPC errors for defects/environment and returns expected refusals as Outcome values | `.claude/decisions.md:6333`, `src/rpc/protocol.ts:11`; commands: `nl -ba .claude/decisions.md | sed -n '6333,6353p;6609,6624p;8550,8579p;9888,9928p;19419,19448p;19585,19614p;21674,21689p'`, `nl -ba src/rpc/protocol.ts` | Translate at the renderer protocol boundary; retain richer application outcomes.

A97. D588’s existing queue naming remains the new exchange naming | **DISPROVED**: D588 mandates `<uuid>-<slug>`; this directive mandates `<uuid>.request.json`, UUID result directories and `__` prefixes | `.claude/decisions.md:19421`; same decisions read as A96 | Record the new agreement explicitly while preserving existing requests and deliveries.

A98. Earlier isometric work authorizes implementing or joining Godot in this unit | **DISPROVED**: D505/D508 concern a separate existing-state view lane; the new directive explicitly stops before Godot integration | `.claude/decisions.md:9888`, `:9912`; same decisions read as A96 | Do not import unrelated lane scope or treat its contract as bootstrap v1.

A99. The general three-round review cap governs a first-landing runtime/protocol subsystem | **DISPROVED by repository decisions**: D247 requires review until clean for first landings | `.claude/decisions.md:6609`; same decisions read as A96 | Resolve workflow precedence rather than declaring consensus merely because three rounds elapsed.

### Contradictions and unreachable steps

- **Audit facts corrected:** `/dm/*` exists outside Vite; a WebSocket echo server exists; player transport is BroadcastChannel; normal saved-session decoding is not the arena EncounterState codec.
- **Runtime premise corrected:** the encounter engine currently runs in the browser main thread. “Preserve both existing backend modes” cannot literally describe an existing encounter Worker; `npm run serve` is static serving.
- **Lossless derivation unavailable:** current state does not encode wall segments/heights, door-to-wall relationships, point-light photometry, token facing/elevation or explored-cell history. Synthetic authored geometry is possible; reconstructing absent facts is not.
- **Protocol agreement incomplete:** session binding/resume, subscriptions, sequencing, duplicate IDs, malformed-request correlation, door blocking semantics and several art fields require explicit shared definitions.
- **Queue conflict:** current filenames/schema/checker fail the new agreement; documented deletion/reuse of rejected assets contradicts preservation and immutable results.
- **Instruction tensions:** preserve-current behavior narrows AGENTS/D60 replacement freedom; D278 needs boundary translation; D588 naming is superseded for this exchange; D247 conflicts with the generic review cap. The supplied global stash instruction also conflicts with this directive’s explicit no-stash rule.
- **Environment gates:** named exchange directories are absent; Windows write/read, toolchain usability and tests remain unproved; shared `node_modules` makes dependency installation nonisolated.
- **Unreachable in this audit:** implementation, installation, publication, write probes, baseline tests, builds and READY verification are prohibited by the current read-only/no-execution task. No files were written and no tests or builds were run.
tokens used
176,008
A1. The audit worktree is the owner’s named repository checkout | **DISPROVED**: it is a linked worktree at `0f84e09f`, branch `claude/vtt-handoff`; the owner checkout’s `main` has advanced | `.git:1`; commands: `nl -ba .git`, `git worktree list --porcelain`, `git rev-parse HEAD` | Separate implementation location, publication location, and reported commit.

A2. This machine is Ubuntu 22.04.5 under WSL2, distribution name Ubuntu | **PROVED** | `/etc/os-release:1`; commands: `nl -ba /etc/os-release`, `cat /proc/sys/kernel/osrelease`, `printenv WSL_DISTRO_NAME` | Linux bootstrap and the distribution component of the UNC path.

A3. The owner repository exists and its handoff root already exists | **DISPROVED** for the handoff root: neither checkout has `.tmp/vtt-handoff` | Command: `ls -ld /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.tmp/vtt-handoff .tmp/vtt-handoff` | Bootstrap must create the exchange root; initial existence cannot be a prerequisite.

A4. UNC translation proves Windows can read and write the exchange | **UNPROVED**: translation succeeds, but access and interop were not exercised | Command: `wslpath -w /home/vagrant/PhpstormProjects/dnd-multiclass-spells-static/.tmp/vtt-handoff`; `.git:1` establishes the owner path | Windows probe, permissions, and publication reliability remain separate checks.

A5. Node 24.13.0, npm 11.6.2, Python 3.10.12 and the named utilities are installed | **PROVED** for inspected executables | Commands: `command -v node npm python3 jq wslpath sg`, `node --version`, `npm --version`, `python3 --version` | Record actual tool versions; this does not prove dependencies or browser execution work.

A6. Node 24.13.0 is the project’s explicitly pinned required version | **UNPROVED**: package.json has no engine/package-manager pin; the launcher README expects Node 24 | `package.json:1`, `tools/discord-launcher/README.md:29`; command: `rg -n 'node-version|engines|packageManager|Node 24' .github package.json README.md docs/serving.md tools/discord-launcher/README.md` | Doctor must distinguish installed version from a documented project requirement.

A7. Dependency installation in this worktree is isolated | **DISPROVED**: `node_modules` symlinks into the owner checkout | Command: `ls -ld node_modules`; `.git:1` identifies the linked checkout | Installing/updating packages could disturb the running owner application or experiment.

A8. Standard Python venv bootstrapping has everything it needs | **DISPROVED** for ensurepip availability; the alternative pip/uv workflow remains **UNPROVED operationally** | `/usr/lib/python3.10/venv/__init__.py:330`; commands: `rg -n 'ensurepip|python3-venv|sys.exit' /usr/lib/python3.10/venv/__init__.py`, `ls -ld /usr/lib/python3.10/ensurepip /usr/lib/python3.10/ensurepip/__main__.py` | Do not equate missing ensurepip with mandatory sudo or repeat an untested workaround as proven.

A9. Cached Playwright builds and user-local Python tools prove readiness | **UNPROVED**: files exist, but executability, libraries, credentials and interop were not tested | Commands: `ls /home/vagrant/.cache/ms-playwright`, `ls -l /home/vagrant/.local/bin/uv /home/vagrant/.local/bin/virtualenv /home/vagrant/.local/bin/pip3` | Doctor and later permitted smoke tests must establish usability.

A10. Exchange-cache files will automatically be durable repository artifacts | **DISPROVED** | `.gitignore:28`, `:32`, `:41`; command: `nl -ba .gitignore | tail -85` | Schemas, scripts and reproducible fixtures need tracked homes outside ignored exchange directories.

A11. The current baseline passes and the working tree contains no unrelated changes | **UNPROVED** for passing tests; **PROVED** that inspected Git status was clean | Commands: `git status --short`, `git rev-parse HEAD`; `package.json:10` defines the unexecuted build | No baseline or READY test claim may be inferred from this audit.

A12. Everything called “VTT” shares one encounter model | **DISPROVED** | `src/vtt/model.ts:1` defines Yjs tokens/fog/dice; `src/combat/encounter.ts:750` defines EncounterState; commands: `nl -ba src/vtt/model.ts | sed -n '1,105p'`, `nl -ba src/combat/encounter.ts | sed -n '330,380p;750,830p'` | Avoid extracting the shared-tabletop model as the authoritative encounter engine.

A13. The existing VTT transport interface already implements the requested application transport | **DISPROVED**: it sends binary updates and exposes peer/status callbacks, without request correlation or snapshots | `src/vtt/transports/transport.ts:11`; command: `nl -ba src/vtt/transports/transport.ts | sed -n '1,135p'` | Reuse lifecycle patterns, not an assumed compatible contract.

A14. `src/main.ts` mounts the encounter application for every `/vtt` URL | **DISPROVED**: its direct shortcut additionally requires `encounter=reference` | `src/main.ts:47`; command: `nl -ba src/main.ts | sed -n '37,115p'` | Smoke tests must use a verified entry route and actual loading flow.

A15. An encounter-engine browser Worker already exists | **DISPROVED** in inspected source: the host is constructed in the DOM view; the identified Workers handle database/capability work | `src/vtt/encounter-app.ts:1415`, `src/main.ts:181`; commands: `rg -n 'new Worker|RpcClient' src/main.ts src/ui src/db`, `nl -ba src/vtt/encounter-app.ts` | Worker engine execution is new implementation, not preservation of an existing mode.

A16. `npm run serve` starts an authoritative Node encounter backend | **DISPROVED**: it prepares `dist` and serves GET/HEAD files; POST receives 405 | `package.json:11`, `tools/serve.mjs:53`, `:137`; command: `nl -ba tools/serve.mjs | sed -n '1,240p'` | Preserve static startup while explicitly adding or defining Node engine authority.

A17. No `/dm/*` server exists outside the dev-only Vite bridge | **DISPROVED** | `tools/discord-launcher/codex-dm-bridge.mjs:102` handles `/dm/session`, `/dm/exchange`, `/dm/mirror`, and `:115` binds localhost; command: `nl -ba tools/discord-launcher/codex-dm-bridge.mjs | sed -n '1,160p'` | The runtime inventory must include this existing companion.

A18. The existing `/dm/*` companion owns authoritative encounter state | **DISPROVED**: the browser remains authority; the companion exchanges AI requests and mirrors browser revisions | `tools/discord-launcher/README.md:11`, `codex-dm-bridge.mjs:103`; commands: `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'`, `nl -ba tools/discord-launcher/codex-dm-bridge.mjs` | Reusing its listener does not establish server-authoritative gameplay.

A19. The companion authenticates DM callers | **DISPROVED**: localhost HTTP origins are accepted, absent Origin is accepted, and no caller credential check appears in the handler | `tools/discord-launcher/codex-dm-bridge.mjs:44`, `:81`; command: `nl -ba tools/discord-launcher/codex-dm-bridge.mjs | sed -n '1,160p'` | A new authoritative API cannot inherit DM authorization from locality or requestedRole.

A20. No WebSocket server exists | **DISPROVED**: a hand-written upgrade/frame echo server exists, although it has no encounter authority or authenticated peers | `tools/discord-launcher/relay-server.mjs:35`, README.md:5; commands: `nl -ba tools/discord-launcher/relay-server.mjs | sed -n '1,130p'`, `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'` | Distinguish an existing spike from a reusable production game transport.

A21. Running the Discord launcher is a harmless localhost bootstrap step | **DISPROVED**: its documented workflow starts a Cloudflare/ngrok tunnel | `tools/discord-launcher/README.md:3`, `:37`; command: `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'` | The directive forbids deploying public connectivity; do not reuse the launcher wholesale.

A22. The engine MCP entrypoint speaks the bootstrap v1 protocol | **DISPROVED**: it uses stdin/stdout JSON-RPC/MCP | `tools/engine-mcp-server.ts:1`, `src/vtt/mcp/entrypoint.ts:145`, `:505`; commands: `nl -ba tools/engine-mcp-server.ts | sed -n '1,110p'`, `rg -n 'stdin|stdout|readline|jsonrpc|import|reduceSessionEncounter|reduceEncounter' src/vtt/mcp/entrypoint.ts` | Preserve MCP separately; an adapter is required.

A23. The Vite AI bridge and Codex DM companion are the same service | **DISPROVED**: Vite creates a development token and launches Claude/fake agents; the companion launches Codex | `tools/ai-bridge/plugin.ts:338`, `tools/discord-launcher/README.md:14`; commands: `nl -ba tools/ai-bridge/plugin.ts | sed -n '330,377p'`, `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'` | Preserve distinct security and build boundaries.

A24. `encounter-app.ts` directly mutates live EncounterState outside DmEncounterHost | **DISPROVED by source inspection**: no state/reducer mutation path found; gameplay handlers call `#host` | `src/vtt/encounter-app.ts:1563`, `:2071`, `:2211`, `:2645`, `:2896`, `:2932`, `:2974`; command: `rg -n 'encounter\\s*=|host\\.|reduceEncounter|reduceVane|coordinator\\.|registry\\.' src/vtt/encounter-app.ts` | Do not invent an old independent UI rules implementation as the refactor target.

A25. All session mutations in the UI already pass through the host | **DISPROVED**: browser save rename and deletion call the store directly; active-session deletion first closes the view | `src/vtt/encounter-app.ts:1620`, `:1632`; command: `nl -ba src/vtt/encounter-app.ts | sed -n '1066,1144p;1361,1455p;1470,1569p;1600,1760p;3066,3135p'` | Session lifecycle/persistence needs an explicit boundary beyond gameplay commands.

A26. Save restore/import already passes through the host | **DISPROVED**: `restoreStored`, folder-save `import`, and uploaded-file durable import bypass it; loading navigates or remounts afterward | `src/vtt/encounter-app.ts:1681`, `:1687`, `:1704`, `:1733`; command: `nl -ba src/vtt/encounter-app.ts` | Preserve conflict checks and prevent a Worker host retaining a stale session after restore.

A27. Folder operations are authoritative-engine commands today | **DISPROVED**: folder rename/delete/write/selection and downloads are UI-side adapter operations | `src/vtt/encounter-app.ts:1622`, `:1643`, `:1664`, `:1671`, `:1715`; command: `rg -n '#sessionFlow|this\\.#store\\.|this\\.#folder\\.|HumanController|\\.decide\\(|\\.submit\\(' src/vtt/encounter-app.ts` | Keep browser file handles and user gestures outside the rules core.

A28. UI-local assignments are additional encounter mutations | **DISPROVED** for inspected assignments: staged actions, selection, accessibility mode, projections and render counters are presentation state | `src/vtt/encounter-app.ts:1069`, `:1376`, `:1509`, `:1600`; command: `rg -n '^export |^class |#[A-Za-z]+\\s*=|this\\.#state|this\\.state|reduce|apply|Math.random|Date.now|set[A-Z]' src/vtt/encounter-app.ts` | Preserve the existing separation instead of moving selection into authority.

A29. Publishing a host snapshot already means browser persistence has completed | **DISPROVED**: the UI explicitly waits for store flush and coalesces pending snapshots before sending the player projection | `src/vtt/encounter-app.ts:1487`, `:1495`, `:1513`; command: `nl -ba src/vtt/encounter-app.ts` | Mutation-result and event acknowledgment must preserve durability ordering.

A30. The host exposes raw EncounterState directly to the DM UI | **DISPROVED for its encounter projection**: `snapshot()` projects, and `projectDmView()` clones state fields | `src/vtt/dm-encounter-host.ts:421`, `src/combat/visibility.ts:501`; command: `nl -ba src/combat/visibility.ts | sed -n '501,548p'` | Reuse existing projection ownership; complete transitive alias safety still requires verification.

A31. Existing host subscription already provides an initial snapshot and cleanup | **PROVED** | `src/vtt/dm-encounter-host.ts:456`; command: `nl -ba src/vtt/dm-encounter-host.ts | sed -n '1,100p;190,202p;421,467p'` | Reuse this application-service behavior in both runtime adapters.

A32. The player connection uses `window.postMessage` with origin/source checking | **DISPROVED**: both views instantiate BroadcastChannel; `local-window-channel.ts` contains message codecs | `src/vtt/encounter-app.ts:1082`, `:1432`, `:1551`; command: `nl -ba src/vtt/encounter-app.ts` | Document the actual same-origin channel and its lack of peer authentication.

A33. The player UI cannot obtain a DM seat | **DISPROVED** as a security claim: it explicitly opens a URL with `view=dm`, which the entrypoint accepts | `src/vtt/encounter-app.ts:1121`, `src/main.ts:55`; commands: `nl -ba src/vtt/encounter-app.ts`, `nl -ba src/main.ts | sed -n '37,115p'` | Current local presentation separation is not hostile-player secret protection.

A34. Local player decisions are wholly trusted | **DISPROVED**: session, pending request, encounter revision and offered action are checked | `src/vtt/local-window-channel.ts:26`; command: `nl -ba src/vtt/local-window-channel.ts | sed -n '1,215p'` | Preserve these validation checks while adding authenticated seat binding where needed.

A35. Local host messages receive equally deep validation | **DISPROVED**: acceptance checks session/kind, object projection and `audience === 'player'` only | `src/vtt/local-window-channel.ts:77`; same command as A34 | An external protocol decoder needs stronger validation than the existing trusted-host codec.

A36. The current host supplies a separately authenticated projection for each human player | **DISPROVED**: it creates one `seat:local-party` projection owning all configured player IDs | `src/vtt/dm-encounter-host.ts:425`; command: `nl -ba src/vtt/dm-encounter-host.ts | sed -n '1,100p;190,202p;421,467p'` | `playerId` mapping and per-client subscriptions are new policy.

A37. Fog rendering alone performs existing player filtering | **DISPROVED**: player projection removes concealed combatants, blocked cells and objects before transmission | `src/combat/visibility.ts:549`, `:641`; command: `nl -ba src/combat/visibility.ts | sed -n '24,72p;549,571p;635,661p'` | New snapshots must start from the player-safe projection, including removal transitions.

A38. Existing state provides the bootstrap `visible` and persistent `explored` sets directly | **DISPROVED**: it has fog cells and observer/subject last-seen history, not an explored-cell history | `src/combat/encounter.ts:765`, `:792`, `src/combat/visibility.ts:641`; commands: `nl -ba src/combat/encounter.ts`, `nl -ba src/combat/visibility.ts` | Define honest projection semantics; do not invent exploration history.

A39. EncounterState is already a SceneSnapshot-shaped record | **DISPROVED**: it contains rules, combatants, resources, decisions, effects, environment, journals and optional imported content | `src/combat/encounter.ts:750`; command: `nl -ba src/combat/encounter.ts | sed -n '330,380p;750,830p'` | The bootstrap is a lossy view, never a replacement save format.

A40. Token IDs and combatant IDs are interchangeable | **DISPROVED**: CombatToken stores both; the board’s art lookup uses combatant identity | `src/combat/combatant.ts:122`, `src/vtt/encounter-board.ts:815`; commands: `nl -ba src/combat/combatant.ts | sed -n '118,150p'`, `nl -ba src/vtt/encounter-board.ts | sed -n '810,850p'` | Preserve IDs and define the `token.move.tokenId` lookup explicitly.

A41. Existing coordinates are screen pixels | **DISPROVED**: mechanical GridCell uses integer column/row; grid distance multiplies cell distance by five | `src/combat/grid.ts:3`, `:25`, `:72`; command: `nl -ba src/combat/grid.ts | sed -n '1,95p'` | Conversion belongs at the projection/command adapter, not in saved coordinates.

A42. Existing token anchors already equal bootstrap ground-center anchors | **DISPROVED for multicell tokens**: footprints extend east/south from the stored anchor | `src/combat/creature-space.ts:396`; command: `nl -ba src/combat/creature-space.ts | sed -n '396,430p'` | Center conversion needs footprint-dependent offsets and a reversible move conversion.

A43. Existing tokens supply elevation, facing and art asset ID | **DISPROVED**: CombatToken has ID, combatant ID, position and placement mode only | `src/combat/combatant.ts:122`; command: `nl -ba src/combat/combatant.ts | sed -n '118,150p'` | Zero elevation/facing would be declared rendering conventions; arbitrary z movement is not existing support.

A44. A blocked cell is a wall segment | **DISPROVED**: it is an impassable opaque square; terrain derives cell-local wall classification and synthetic source identity | `src/combat/grid.ts:14`, `src/combat/terrain.ts:107`; commands: `nl -ba src/combat/grid.ts`, `nl -ba src/combat/terrain.ts | sed -n '1,170p'` | Cell boundaries can be rendered, but original segment orientation/height cannot be recovered.

A45. Every terrain object is one rectangular wall with canonical blocking flags | **DISPROVED**: footprints are arbitrary unique absolute cells; blocking accepts a transitional independent movement/sight/cover triple | `src/combat/world-objects.ts:49`, `:170`, `src/combat/terrain.ts:15`; commands: `nl -ba src/combat/world-objects.ts`, `nl -ba src/combat/terrain.ts` | Do not erase footprint shape or assume cover, movement and vision are equivalent.

A46. Visible perimeter-wall art proves there is a corresponding mechanical wall | **DISPROVED**: board wall pieces derive from bounds; comments distinguish decorative art from engine doors | `src/vtt/encounter-board.ts:768`, `:910`; command: `rg -n 'door|light|wall|facing|asset|height|footprint' src/vtt/d365-sample-dungeon.ts src/vtt/encounter-board.ts` | Do not derive authoritative geometry from the rendered wall band.

A47. Doors have explicit `open` and `wallId` fields | **DISPROVED**: they are cell-footprint world objects; board openness is inferred from movement blocking | `src/combat/world-objects.ts:50`, `src/vtt/encounter-board.ts:773`; commands: `nl -ba src/combat/world-objects.ts`, `rg -n 'door|light|wall|facing|asset|height|footprint' src/vtt/d365-sample-dungeon.ts src/vtt/encounter-board.ts` | A single door-controlled segment cannot be derived losslessly without additional authored data/policy.

A48. Light regions are toggleable point lights | **DISPROVED**: they contain cells and bright/dim/darkness level; later overlapping entries win | `src/combat/world-objects.ts:70`, `:230`; command: `nl -ba src/combat/world-objects.ts | sed -n '1,250p'` | Position, radius, color, intensity and enabled state require authored presentation data; arbitrary regions are not lossless circles.

A49. Presence of world modification operations proves `door.set` and `light.set` semantics | **DISPROVED**: existing operations modify objects or set a region’s light level, without the agreed door relationship or point-light identity | `src/combat/world-objects.ts:120`, `src/combat/encounter.ts:11682`, `:11769`; commands: `nl -ba src/combat/world-objects.ts`, `rg -n 'set_light_level|door|modify_object' src/combat/encounter.ts` | Expose only operations with an explicit correct mapping; otherwise UNSUPPORTED.

A50. Encounter revision, journal revision, event sequence and scene identity are interchangeable | **DISPROVED**: state and journal have separate counters, branch/head identities and room transitions; EncounterState has no sceneId | `src/combat/encounter.ts:756`, `src/vtt/session-persistence.ts:281`; commands: `nl -ba src/combat/encounter.ts`, `nl -ba src/vtt/session-persistence.ts` | Define monotonic protocol revisions and seq across undo, reconnect and room changes.

A51. A saved session is a v1 JSON serialization of EncounterState | **DISPROVED**: current export is schema 12 `vtt-session-journal-dag`, including revisions, RNG, coordinator, controllers and fingerprints | `src/vtt/session-persistence.ts:129`, `:281`, `:2419`, `:3369`; command: `nl -ba src/vtt/session-persistence.ts` | Preserve the journal, replay, undo branches and deterministic resume.

A52. `encounter-state-codec.ts` is the normal saved-session decoder | **DISPROVED**: its callers are arena-fixture decoders; saved sessions use their own revision/bundle migration and decoding path | `src/vtt/arena-fixture.ts:268`, `session-persistence.ts:921`, `:3389`; command: `rg -n 'decodeEncounterStateV1|encounter-state-codec' src/vtt src/worker` | Save compatibility cannot be proven by testing only the arena codec.

A53. Reading a save fingerprint is a cheap header-only operation | **DISPROVED**: `decodeSavedSessionFingerprint` migrates and replays the session | `src/vtt/session-persistence.ts:3385`; command: `nl -ba src/vtt/session-persistence.ts | sed -n '245,267p;3363,3420p'` | Doctor/import/listing design must account for real processing cost.

A54. Old saves can bypass existing migration checks during extraction | **DISPROVED**: version windows, adjacent migration registry, checksums and DAG expansion are explicit | `src/vtt/session-persistence.ts:3238`, `:3252`; command: `nl -ba src/vtt/session-persistence.ts | sed -n '915,975p;3238,3304p'` | Preserve originals and test accepted historical formats rather than guessing normalization.

A55. Encounter persistence is the character database Worker’s persistence | **DISPROVED**: live encounter saves use a separate IndexedDB store with legacy localStorage migration; a SQLite store implementation also exists | `src/vtt/local-session-store.ts:18`, `:23`, `session-persistence.ts:409`; commands: `nl -ba src/vtt/local-session-store.ts | sed -n '1,110p'`, `nl -ba src/vtt/session-persistence.ts` | Choose persistence adapters deliberately; moving the engine does not move browser-owned saves automatically.

A56. Replacing randomness with a fresh seeded generator preserves behavior | **DISPROVED**: RNG state includes algorithm, initial seed, cursor, draw count and stream ID, with transactional rollback adapters | `src/combat/random.ts:15`, `:119`; command: `nl -ba src/combat/random.ts | sed -n '1,145p'` | Worker/Node parity and save resume require preserved draw/rollback semantics.

A57. Calling only `reduceEncounter` preserves every persisted VTT mechanic | **DISPROVED**: the shared persisted-command reducer delegates through `reduceVaneWarrenEncounter` | `src/vtt/session-encounter-reducer.ts:4`, `src/vtt/vane-warren.ts:799`; commands: `nl -ba src/vtt/session-encounter-reducer.ts | sed -n '1,190p'`, `rg -n 'reduceEncounter|reduceSessionEncounter|reduceVaneWarrenEncounter' src/vtt/vane-warren.ts src/vtt/mcp/engine-server.ts src/vtt/engine-query-port.ts src/combat/coordinator.ts` | Both runtimes must reuse the actual application reducer chain.

A58. A source-directory split alone proves a portable, renderer-independent core | **UNPROVED**: host imports span bridge, session, projection and content-composition modules; current TS libraries permit browser globals | `src/vtt/dm-encounter-host.ts:1`, `tsconfig.app.json:5`; commands: `nl -ba src/vtt/dm-encounter-host.ts`, `nl -ba tsconfig.app.json | sed -n '1,90p'` | Require transitive import-boundary checks and actual runtime/bundle evidence.

A59. Existing RPC envelopes are bootstrap-v1 compatible | **DISPROVED**: numeric IDs, no `v`, unrestricted generic params/results, different error vocabulary and no snapshot-event union | `src/rpc/protocol.ts:3`, `:45`, `:68`; command: `nl -ba src/rpc/protocol.ts | sed -n '1,185p'` | Reuse mechanisms behind an explicit versioned adapter.

A60. Existing RpcClient already supplies useful Worker lifecycle machinery | **PROVED**: pending-ID correlation, transfer lists, error rejection, listener cleanup and optional termination | `src/rpc/client.ts:49`, `:85`, `:105`; command: `nl -ba src/rpc/client.ts | sed -n '1,260p'` | Avoid rebuilding basic request/response plumbing unnecessarily.

A61. RpcClient already handles subscriptions, reconnect, deduplication and unknown outcomes | **DISPROVED**: no event subscription/resume/dedup store; no timeout; error rejects pending calls without establishing mutation outcome | `src/rpc/client.ts:54`, `:74`, `:85`; same command as A60 | Add protocol lifecycle semantics; never turn transport rejection into blind mutation retry.

A62. RpcClient validates received responses and fully handles failed sends | **DISPROVED**: it trusts typed message data; a synchronous postMessage throw occurs after insertion into the pending map | `src/rpc/client.ts:54`, `:95`; same command as A60 | External/untrusted transport adaptation needs runtime decoding and explicit send-failure cleanup.

A63. Worker registry dispatch can be reused unchanged in plain Node | **DISPROVED**: handlers receive database/lifecycle context, and eager discovery uses Vite’s `import.meta.glob` | `src/worker/handler.ts:6`, `src/worker/registry.ts:109`; commands: `nl -ba src/worker/handler.ts | sed -n '1,220p'`, `nl -ba src/worker/registry.ts | sed -n '1,210p'` | Reuse duplicate-method checks, validation and dispatch patterns through a portable registry seam.

A64. Database Worker boot is a generic engine runtime | **DISPROVED**, but its serial queue and clone-failure fallback are reusable | `src/db/worker.ts:69`, `:104`, `:147`, `src/db/worker-post.ts:7`; commands: `nl -ba src/db/worker.ts | sed -n '1,190p'`, `nl -ba src/db/worker-post.ts | sed -n '1,110p'` | Keep SQLite/OPFS boot and recovery separate from encounter execution; ordinary message dispatch adds no SAB requirement.

A65. Existing command idempotency automatically covers RPC request IDs | **DISPROVED**: character commands use `operation_uuid` and `expected_revision`; generic RPC dispatch does not deduplicate | `src/worker/handlers/commands.ts:31`, `src/commands/character-command-executor.ts:716`; commands: `nl -ba src/worker/handlers/commands.ts | sed -n '1,150p'`, `rg -n 'operation_uuid|idempot|replay|expected_revision' src/commands/character-command-executor.ts` | Reuse the domain pattern, not its character-specific persistence or identity scope.

A66. Existing RPC tests prove encounter execution in a real Worker | **DISPROVED**: client unit tests use ErrorTransport; browser RPC tests exercise character commands | `tests/unit/rpc/client.test.ts:5`, `tests/browser/command-rpc.spec.ts:11`; command: `rg -n 'it\\(|test\\(|Worker|MessageChannel|new RpcClient' tests/unit/rpc/client.test.ts tests/unit/rpc/registry.test.ts tests/browser/command-rpc.spec.ts` | Add real encounter Worker/Node parity tests; naming a test “worker” is insufficient.

A67. `npm test` covers every test file in the repository | **DISPROVED**: Vitest includes `tests/**/*.test.ts`; launcher `.test.mjs` files and Playwright specs are outside that set | `vitest.config.ts:34`, `tools/discord-launcher/README.md:50`; commands: `nl -ba vitest.config.ts | sed -n '1,160p'`, `nl -ba tools/discord-launcher/README.md | sed -n '1,55p'` | Report exact suites executed, especially companion-server tests.

A68. `npm run build`, plain root tsc and `typecheck` are equivalent checks | **DISPROVED**: build runs `tsc -b`, Vite and dist checks; typecheck adds command-outcome checking; root tsconfig has `files:[]` | `package.json:10`, `:13`, `tsconfig.json:2`; commands: `nl -ba package.json | sed -n '1,180p'`, `nl -ba tsconfig.json` | Do not claim compilation or build-boundary coverage from the wrong command.

A69. Existing test commands automatically serialize against other work | **DISPROVED**: ordinary test/browser scripts bypass the gate wrappers; wrappers acquire the shared lock and may retry failures | `package.json:16`, `:24`, `tools/gate-vitest.mjs:6`; commands: `nl -ba package.json`, `nl -ba tools/gate-vitest.mjs | sed -n '1,100p'` | Respect the present no-test instruction; later record retries separately from clean first-pass results.

A70. A unique Playwright base port completely isolates a checkout | **DISPROVED**: it reserves a worker port range; TS build-info paths and default Vitest cache paths are machine-global | `tests/browser/fixtures/worker-origin.ts:25`, `tsconfig.app.json:15`, `tsconfig.node.json:14`, `vitest.config.ts:27`; commands: `nl -ba tests/browser/fixtures/worker-origin.ts | sed -n '1,60p'`, `nl -ba tsconfig.app.json`, `nl -ba tsconfig.node.json`, `nl -ba vitest.config.ts` | Prevent port/cache/build-state collisions with the active experiment.

A71. Browser tests exercise production Node serving and real AI services by default | **DISPROVED**: Playwright starts Vite with `AI_BRIDGE_FAKE=1`; production serving has a separate check command | `playwright.config.ts:24`, `:35`, `package.json:12`; commands: `nl -ba playwright.config.ts | sed -n '1,170p'`, `nl -ba package.json` | A top-down smoke test alone cannot prove Node authority or production boundary correctness.

A72. Suggested logical asset IDs are valid existing AssetIds | **DISPROVED**: existing branded IDs require `art.<kind>.<name>.v<n>` | `src/assets/ids.ts:6`; command: `nl -ba src/assets/ids.ts | sed -n '1,100p'` | Use a deliberate logical-to-existing-asset mapping without renaming persisted engine IDs.

A73. Copying a PNG into public art changes the encounter UI’s displayed asset | **DISPROVED**: the resolver generates PNG bytes from registered recipes and caches data URIs | `src/assets/starter-art-resolver.ts:16`, `:27`, `:37`; command: `nl -ba src/assets/starter-art-resolver.ts | sed -n '1,90p'` | Staging requires a later serving/resolver integration and top-down fallback.

A74. Production art can be added or replaced without changing independent expectations | **DISPROVED**: tests pin 92 files, manifest coverage, generated bytes and independent hashes | `tests/unit/assets/starter-art.test.ts:54`, `:132`, `tests/unit/source-is-greppable.test.ts:125`; commands: `nl -ba tests/unit/assets/starter-art.test.ts | sed -n '1,175p'`, `rg -n 'raster|art|git ls-files|PIN|png' tests/unit/source-is-greppable.test.ts` | Keep staging separate; do not regenerate expected hashes merely to pass.

A75. Existing art requests satisfy the new outbox/inbox agreement | **DISPROVED**: they use `id`, slug, prompt, dimensions and one deliverable at `<uuid>-<slug>`, without the new request/result envelopes | `art/requests/new-request.mjs:37`, `request.schema.json:7`; commands: `nl -ba art/requests/new-request.mjs | sed -n '1,265p'`, `nl -ba art/requests/request.schema.json | sed -n '1,200p'` | Non-destructive mapping/migration is required.

A76. `new-request.mjs --check` validates its JSON Schema and delivered images | **DISPROVED**: it never loads the schema; it checks selected fields, naming, placeholders and file existence | `art/requests/new-request.mjs:66`; command: `nl -ba art/requests/new-request.mjs | sed -n '1,265p'` | New validation must cover actual JSON types, completion, image contents, hashes and identities.

A77. The current checker implements the formats allowed by its schema | **DISPROVED**: schema allows PNG/WebP/SVG, but checker requires a `.png` deliverable filename and does not inspect JPG/source/sidecar files | `art/requests/request.schema.json:148`, `new-request.mjs:75`, `:82`; commands: `nl -ba art/requests/request.schema.json`, `nl -ba art/requests/new-request.mjs` | Do not present its clean verdict as complete inbox validation.

A78. The existing UUID generator is a tested reusable UUIDv7 library | **DISPROVED** as a tested-library claim: it hand-packs timestamp/version/variant bits; no matching automated tests were found, and importing the module executes CLI dispatch | `art/requests/new-request.mjs:27`, `:91`; commands: `nl -ba art/requests/new-request.mjs`, `rg -n 'uuidv7|new-request' tests art/requests --glob '*test*' --glob '*spec*'` | Extract/test or replace it; bit-shape inspection does not prove collision, clock or input behavior.

A79. Current art retry/completion conventions preserve originals and identify finished bundles | **DISPROVED**: README instructs deleting rejected art and reusing its ID; completion is request status plus file presence | `art/requests/README.md:35`, `new-request.mjs:78`; commands: `nl -ba art/requests/README.md | sed -n '1,190p'`, `nl -ba art/requests/new-request.mjs` | Replace the destructive convention with immutable deliveries and result-last publication.

A80. Current art ingestion already checks traversal, symlinks, limits, hashes and pass alignment | **DISPROVED**: the checker uses joined paths and `existsSync`, with none of those checks | `art/requests/new-request.mjs:66`; command: `nl -ba art/requests/new-request.mjs | sed -n '1,265p'` | Treat inbox files as untrusted inputs; validate before staging and never execute delivered scripts.

A81. Existing `pngDimensions` proves that a file is a valid transparent PNG | **DISPROVED**: it checks eight signature bytes and reads offsets 16/20; it does not validate chunks, CRCs, decoding or alpha | `src/assets/png.ts:213`; command: `nl -ba src/assets/png.ts | tail -60` | A real bounded image validator is necessary; signature and dimensions alone are insufficient.

A82. Existing provenance automatically licenses externally generated replacement art | **DISPROVED**: the queue README explicitly excludes assistant-made art from the current procedural provenance statement | `art/requests/README.md:59`, `.claude/decisions.md:8557`; commands: `nl -ba art/requests/README.md | sed -n '1,190p'`, `nl -ba .claude/decisions.md` | Retain source/tool/request provenance and review authorization; metadata cannot silently relicense a delivery.

A83. A published bootstrap-v1 implementation/schema already resolves integration ambiguities | **DISPROVED within searched implementation/docs** | No matching implementation found with `rg -n 'scene.snapshot|session.open|token.move|door.set|light.set|SceneSnapshot|READY.json|art/outbox|art/inbox' src tools docs --glob '!*.json'`; existing RPC differs at `src/rpc/protocol.ts:5` | Both agents must pin an explicitly published interpretation, not infer one from existing RPC.

A84. Bootstrap v1 specifies how later requests select an opened session or resume it | **UNPROVED/UNSPECIFIED**: sessionId appears only in the open result; no request session field, resume method or binding rule is supplied | Directive bootstrap text; existing contrast: `src/vtt/local-window-channel.ts:12` (`nl -ba src/vtt/local-window-channel.ts`) | Define connection-bound sessions or a separately documented transport binding before HTTP/reconnect implementation.

A85. Bootstrap v1 specifies subscription and event sequencing sufficiently for independent implementations | **UNPROVED/UNSPECIFIED**: no subscribe method, seq scope/reset rule, response/event order or reconnect replay rule is supplied | Directive bootstrap and §4; existing initial-subscription behavior: `src/vtt/dm-encounter-host.ts:456` (`nl -ba src/vtt/dm-encounter-host.ts`) | Agree on transport lifecycle without silently adding wire methods.

A86. Duplicate mutation IDs have fully specified behavior | **UNPROVED/UNSPECIFIED**: the directive requires tests but does not define retention, same-ID/different-payload handling, in-flight duplicates or cross-reconnect identity | Directive §4; existing generic dispatch has no deduplication: `src/worker/registry.ts:67` (`nl -ba src/worker/registry.ts`) | Exactly-once claims, retry handling and session caches depend on these decisions.

A87. Every malformed request can receive the stated correlated failure envelope | **UNPROVED/UNSPECIFIED** when JSON is invalid or `id` is absent/nonstring | Directive failure requires `id:string`; existing numeric protocol uses fallback ID 0 at `src/db/worker.ts:109` (`nl -ba src/db/worker.ts`) | Specify transport-level malformed-envelope errors without inventing valid correlation IDs.

A88. JSON Schema alone settles all SceneSnapshot semantics | **DISPROVED as an assumption**: identity uniqueness, door references, authoritative permissions, geometry and revision transitions need semantic checks; dialect/unknown-field policy are not specified | Directive bootstrap; existing schema explicitly selects draft 2020-12 at `art/requests/request.schema.json:2` (`nl -ba art/requests/request.schema.json`) | Publish structural schema plus semantic contract tests.

A89. Bootstrap number requirements imply positive integer dimensions or bounded facings | **DISPROVED**: only finiteness is stated generally; revisions/seq alone are explicitly safe integers | Directive bootstrap; existing grid’s stronger integer/positive constraints: `src/combat/grid.ts:25` (`nl -ba src/combat/grid.ts`) | Do not silently narrow v1 acceptance; distinguish valid wire values from unsupported engine coordinates.

A90. `door.open` and referenced wall blocking have an unambiguous combined meaning | **UNPROVED/UNSPECIFIED**: the directive does not say whether wall booleans are closed-state properties or already reflect door state | Directive bootstrap; existing board derives openness directly from movement blocking at `src/vtt/encounter-board.ts:773` (`nl -ba src/vtt/encounter-board.ts`) | Both renderers must agree which geometry blocks when the door opens.

A91. The art agreement is already a complete cross-platform schema | **UNPROVED/UNSPECIFIED**: types/vocabularies for views, passes, footprint, provenance/errors, hash placement, frame identity and normal convention are incomplete | Directive art exchange; current incompatible schema: `art/requests/request.schema.json:61`, `:139` (`nl -ba art/requests/request.schema.json`) | Publish these definitions before claiming Windows/result-validator compatibility.

A92. “Relative to the result bundle” and filename-prefix rules uniquely determine every asset path | **UNPROVED/UNSPECIFIED**: root manifest sits beside the UUID directory; nested-path base, separators, drive/UNC forms and case collisions need explicit rules | Directive art exchange; existing queue hardcodes a different directory at `art/requests/new-request.mjs:18` (`nl -ba art/requests/new-request.mjs`) | Portable traversal rejection, hash lookup and Windows staging depend on normalization policy.

A93. Result-last publication and checksums alone prove immutable, authentic, completed delivery | **UNPROVED**: they do not establish publisher identity, prevent post-validation changes, or define partial/blocked acceptance and repeat-delivery behavior | Directive contract/art publication; current writes are ordinary `writeFileSync` at `art/requests/new-request.mjs:100` (`nl -ba art/requests/new-request.mjs`) | Define commit-marker contents, immutable destination checks and the trusted publisher boundary.

A94. Existing Windows art or a Godot build is required to finish backend handoff | **DISPROVED**: the directive permits synthetic exchange fixtures and provisional Windows contracts, forbids joining applications, and labels failures PARTIAL | Directive §§2, 5–6; current exchange root absence verified by A3 | Backend preparation can finish independently; real-art quality and later integration cannot be claimed as tested.

A95. General pre-alpha replacement guidance overrides this unit’s preservation requirement | **DISPROVED**: the new directive explicitly requires preserving the application, saves, IDs and features | `AGENTS.md:7`, `:18`, `.claude/decisions.md:8550`, `:8235`; commands: `cat AGENTS.md`, `nl -ba .claude/decisions.md` | Apply the unit’s narrower preservation scope; replacement permission is not permission to lose campaigns.

A96. Existing refusal semantics can be replaced globally with bootstrap failures | **DISPROVED**: D278 reserves RPC errors for defects/environment and returns expected refusals as Outcome values | `.claude/decisions.md:6333`, `src/rpc/protocol.ts:11`; commands: `nl -ba .claude/decisions.md | sed -n '6333,6353p;6609,6624p;8550,8579p;9888,9928p;19419,19448p;19585,19614p;21674,21689p'`, `nl -ba src/rpc/protocol.ts` | Translate at the renderer protocol boundary; retain richer application outcomes.

A97. D588’s existing queue naming remains the new exchange naming | **DISPROVED**: D588 mandates `<uuid>-<slug>`; this directive mandates `<uuid>.request.json`, UUID result directories and `__` prefixes | `.claude/decisions.md:19421`; same decisions read as A96 | Record the new agreement explicitly while preserving existing requests and deliveries.

A98. Earlier isometric work authorizes implementing or joining Godot in this unit | **DISPROVED**: D505/D508 concern a separate existing-state view lane; the new directive explicitly stops before Godot integration | `.claude/decisions.md:9888`, `:9912`; same decisions read as A96 | Do not import unrelated lane scope or treat its contract as bootstrap v1.

A99. The general three-round review cap governs a first-landing runtime/protocol subsystem | **DISPROVED by repository decisions**: D247 requires review until clean for first landings | `.claude/decisions.md:6609`; same decisions read as A96 | Resolve workflow precedence rather than declaring consensus merely because three rounds elapsed.

### Contradictions and unreachable steps

- **Audit facts corrected:** `/dm/*` exists outside Vite; a WebSocket echo server exists; player transport is BroadcastChannel; normal saved-session decoding is not the arena EncounterState codec.
- **Runtime premise corrected:** the encounter engine currently runs in the browser main thread. “Preserve both existing backend modes” cannot literally describe an existing encounter Worker; `npm run serve` is static serving.
- **Lossless derivation unavailable:** current state does not encode wall segments/heights, door-to-wall relationships, point-light photometry, token facing/elevation or explored-cell history. Synthetic authored geometry is possible; reconstructing absent facts is not.
- **Protocol agreement incomplete:** session binding/resume, subscriptions, sequencing, duplicate IDs, malformed-request correlation, door blocking semantics and several art fields require explicit shared definitions.
- **Queue conflict:** current filenames/schema/checker fail the new agreement; documented deletion/reuse of rejected assets contradicts preservation and immutable results.
- **Instruction tensions:** preserve-current behavior narrows AGENTS/D60 replacement freedom; D278 needs boundary translation; D588 naming is superseded for this exchange; D247 conflicts with the generic review cap. The supplied global stash instruction also conflicts with this directive’s explicit no-stash rule.
- **Environment gates:** named exchange directories are absent; Windows write/read, toolchain usability and tests remain unproved; shared `node_modules` makes dependency installation nonisolated.
- **Unreachable in this audit:** implementation, installation, publication, write probes, baseline tests, builds and READY verification are prohibited by the current read-only/no-execution task. No files were written and no tests or builds were run.
review exit:0
