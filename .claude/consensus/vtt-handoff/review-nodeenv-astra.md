1. **NE-F1 — Informational — `tools/dist-build-cache.mjs:106,177,246`: Existing cache keys remain valid, but no demonstrated stale-poisoning blocker.** The digest excludes the environment and helper source; the format remains v1. A pre-existing poisoned entry with matching hashes **would be restored**, bypassing the new environment override. However, the old helper throws on build failure before storing, and the existing `assert-dist-clean.mjs:71,269` rejects the probe marker. Both that guard and `package.json` are hashed inputs, so entries predating the guard cannot share this key. **No format bump is required for this demonstrated failure path.** Fresh-cache verification alone does not establish this; the old store ordering does.

2. **NE-F2 — Informational — `tools/dist-build-cache.mjs:132,195`; `tests/unit/tools/dist-build-cache.test.ts:23`: Environment handling and tests satisfy scope.** The function copies the parent and overrides only `NODE_ENV`; the actual build spawn uses it. Independent read-only assertions confirmed preservation of `PATH`, `HOME`, `npm_config_*`, `STATIC_APP_CACHE_DIR`, and `AI_BRIDGE_FAKE`, plus parent immutability and unset handling. The unit spec covers inherited test, unset, and preserved-variable cases. Only the helper and one unit test changed.

3. **NE-F3 — Low, non-blocking — `package.json:10`: Direct builds deserve a separate ticket.** `NODE_ENV=test npm run build` still fails outside the helper. Serve routing through `tools/serve.mjs:57` satisfies D612’s bounded scope, but the standalone build command remains environment-sensitive. Track production enforcement there separately; the existing guard prevents successful publication through that command.

**ACCEPT**