- **F79 — RESIDUAL.** Scalar `toJSON()` IDs and enumerable `__proto__` are corrected and tested. Identity capture still has edge cases described in F87 below. See [websocket-transport.ts:53](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/src/vtt/handoff/websocket-transport.ts:53).
- **F80 — RESOLVED.** The physical error sink is installed before session creation and retained until physical closure. The new raw-peer test exercises malformed frames during policy closure. See [node-runtime.ts:210](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tools/vtt-handoff/node-runtime.ts:210).
- **F81 — RESIDUAL.** Coverage materially improves: both unit adapters exercise five outcome mappings, projected-token removal, movement, mismatch, reconnect, and actual late subscription. However, the “reducer execution” counter increments at `service.setDoor`, not the reducer ([transport-conformance.ts:213](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tests/helpers/vtt-handoff/transport-conformance.ts:213)). Worker coverage still lacks successful movement, terminal-outcome and visibility-removal parity. The browser report still derives its scenario count from the manifest’s length rather than completed exercises ([runtime-parity.spec.ts:358](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tests/browser/vtt-handoff/runtime-parity.spec.ts:358)).
- **F82 — RESIDUAL.** The client now avoids Zod error-formatting serialization, and the extra-serialization mutant genuinely executes JSON work and changes measured counts. But the real Node factory is closed before measurement; a scripted server replaces it and uses saved, uninstrumented JSON functions. Thus `3 stringify / 4 parse` proves client behavior, not both production endpoints. See [node-websocket-transport.test.ts:52](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tests/unit/vtt/node-websocket-transport.test.ts:52).
- **F83 — RESIDUAL.** Constructor/service evidence now uses symbols, platform controls distinguish real globals from local names, and source mutants exercise extraction. These improvements should survive reconciliation. WebSocket platform coverage was dropped, however; see F89.
- **F84 — RESOLVED.** The runtime receives port `0`; its actual URL is read from the listening line. No bind-and-release preallocation remains. See [node-runtime-launch.launch-test.ts:88](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tests/integration-supervisor/vtt/node-runtime-launch.launch-test.ts:88).
- **F85 — RESOLVED.** Both streams are captured, drainage is tracked, and startup/cleanup failures include stdout and stderr. The now-visible build failure is separate. See [node-runtime-launch.launch-test.ts:36](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tests/integration-supervisor/vtt/node-runtime-launch.launch-test.ts:36).
- **F86 — RESOLVED.** `O_NONBLOCK` precedes descriptor inspection. The FIFO regression invokes the actual reader, while the blocking control must terminate with `SIGTERM`/`ETIMEDOUT`. See [token-claims.ts:34](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tools/vtt-handoff/token-claims.ts:34).

**F87 — High — Identity capture does not always match the serialized envelope.**  
At [websocket-transport.ts:56](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/src/vtt/handoff/websocket-transport.ts:56), every empty property name resets `rootSerializedValue`; empty names can occur inside caller data. A nested `"": {id: "other"}` can therefore replace the captured root ID with `"other"`.

Also, `id: new String("wire-id")` reaches the replacer as an object and is subsequently serialized as a string. Correlation reserves the synthetic uncorrelated key while the server responds with `"wire-id"`. These cases can fault the connection or misattribute a response to another pending request. Record both as F79 residual cases; identify the root only on the first replacer invocation and handle or reject boxed identity values consistently.

**F88 — Medium — Production serving inherits a nonproduction build environment.**  
The supervisor diagnosis matches source: the probe inherits `NODE_ENV` at [node-runtime-launch.launch-test.ts:86](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tests/integration-supervisor/vtt/node-runtime-launch.launch-test.ts:86); serve forwards it; [dist-build-cache.mjs:188](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tools/dist-build-cache.mjs:188) forwards it again to the build. Installed Vite derives `DEV` from whether `NODE_ENV` equals `"production"`, explaining retention of the seam at `src/main.ts:116`.

The durable fix belongs in the production build helper: explicitly use `NODE_ENV=production` for its build child, protecting every serve caller, including the owner’s workflow. A probe-only override would conceal the production-path defect. Keep an inherited-`NODE_ENV=test` regression to verify that boundary. The current failure is fail-closed and is a ledger residual, not a merge blocker; S7d remains unverified.

**F89 — Medium — The WebSocket browser dependency graph lost its platform assertion.**  
The former explicit Worker/WebSocket platform assertion was removed. The remaining platform check uses `CORE_ENTRYPOINTS`, which excludes `websocket-transport.ts` ([engine-boundary.test.ts:491](/home/vagrant/PhpstormProjects/dnd-wt-vtt-handoff-s7/tests/unit/vtt/engine-boundary.test.ts:491)). Its presence in the convergence graph does not check platform violations. Restore explicit browser-adapter coverage when reconciling with S6/S8, preserving the new symbol-resolved checks.

The sampled assertion replacements are legitimate: buffered late delivery now pins `[1,2]`; late-subscription checks inspect the latest sequence and revision; browser close-code expectations account for added sessions. The five in-process lines intentionally change late-subscriber behavior to the required buffering contract. The protocol guard rejects an invalid own `__proto__` field; valid-envelope dispatch is unchanged.

Reviewed clean HEAD `9a5767f9` without execution or edits. Merge under D605 with F79/F81/F82/F83 residuals and F87–F89 recorded. The supplied passing gates support integration, while complete three-runtime conformance and the launch proof remain outstanding.

ACCEPT S7