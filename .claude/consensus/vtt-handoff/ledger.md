# VTT-HANDOFF-IMPL-01 — residual ledger (D605: findings left open after the capped fix rounds)

Entries are Astra findings accepted as residuals at merge time. Each names the step, the finding, the file, and what closes it. Nothing here is a claim of correctness; it is the list of what the integrated branch does NOT yet prove.

## S7 (accepted 9a5767f9, final review impl-s7-review-r3-astra.md, 2026-09-10 20:49 EDT)
- F79 residual — identity capture edge cases (see F87).
- F81 residual — the "reducer execution" counter increments at service.setDoor, not the reducer (tests/helpers/vtt-handoff/transport-conformance.ts:213); Worker coverage lacks successful movement, terminal-outcome and visibility-removal parity; the browser report derives its scenario count from the manifest length, not completed exercises (runtime-parity.spec.ts:358).
- F82 residual — the JSON-operation measurement proves client behaviour only: the real Node factory is closed before measurement and a scripted server with uninstrumented JSON functions replaces it (node-websocket-transport.test.ts:52).
- F83 residual — WebSocket platform coverage dropped (see F89).
- F87 HIGH — websocket-transport.ts:56 resets rootSerializedValue on every empty property name (a nested "" key with an id replaces the captured root id); boxed `new String(id)` reaches the replacer as an object and serialises as a string, so correlation reserves the uncorrelated key while the server answers with the string. Close by identifying the root only on the first replacer invocation and handling/rejecting boxed identity values consistently, with both regressions.
- F88 MEDIUM — production serving inherits a non-production build environment: the launch probe inherits NODE_ENV=test (node-runtime-launch.launch-test.ts:86), serve.mjs forwards it, tools/dist-build-cache.mjs:188 forwards it to the build; Vite derives DEV from NODE_ENV !== 'production', so the dev-only seam at src/main.ts:116 survives and assert-dist-clean rejects the dist. Durable fix: the production build helper sets NODE_ENV=production for its build child (protects every serve caller including the owner's live 4173 workflow), plus an inherited-NODE_ENV=test regression. Until then the launch proof (S7d) is unverified; the failure is fail-closed.
- F89 MEDIUM — the WebSocket browser dependency graph lost its explicit platform assertion (engine-boundary.test.ts:491 uses CORE_ENTRYPOINTS, which excludes websocket-transport.ts). Restore explicit browser-adapter platform coverage when reconciling engine-boundary.test.ts with S6/S8, keeping the symbol-resolved checks.
