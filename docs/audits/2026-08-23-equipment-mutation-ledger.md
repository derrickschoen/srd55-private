# D339.4/D340.1 equipment mutation ledger

Each production mutation below was applied alone, killed by its named imported-pack test, and restored before the next mutation. No behavioral expectation was regenerated from production output. The restored focused suite reported `Test Files  1 passed (1)` and `Tests  6 passed (6)`.

| Mutation | Production change | Named killing test | Observed kill | Restoration |
|---|---|---|---|---|
| `worn_armor_drops` | Routed the failed-save worn relationship through a mutant forced-drop state change, removing the armor and placing it on the ground. | `worn_armor_drops: failed Heat Metal save drops a held sword but retained worn armor receives attack and ability-check Disadvantage` | exit 1; the retained worn set was empty instead of containing the armor. | Only held items enter forced drop; worn items execute both declared Disadvantage operations. |
| `dropped_item_vanishes` | Removed the ground-item insertion from forced drop while still removing the held item. | `dropped_item_vanishes and contact_damage_holder_only: cast drops onto the holder cell; pickup, re-equip, and later Bonus Action re-trigger damage every contact and drop it again` | exit 1; the expected sword and holder-cell position were absent from `groundItems`. | Forced drop again inserts the item at the holder token's current cell. |
| `second_interaction_free` | Changed the free-interaction boundary from `used >= 1` to `used > 1`. | `second_interaction_free: exactly one free interaction succeeds, a second free interaction is typed-refused, and Utilize pays the action` | exit 1; the second free Equip succeeded instead of producing `free_interaction_spent`. | The exact used boundary again refuses a second free interaction; Utilize remains available when the action is unspent. |
| `two_handed_in_one_hand` | Allowed a two-handed item id into the one-handed tuple alongside an already-held item. | `two_handed_in_one_hand: hand capacity succeeds with exactly one free unit, then refuses a two-handed item when both units are full` | exit 1; the expected typed hand-capacity refusal was not thrown. | Two-handed equip again requires the closed `empty` hand state and becomes the exclusive `two_handed` variant. |
| `contact_damage_holder_only` | Filtered Heat Metal's snapshotted contact set down to the held relationship. | `dropped_item_vanishes and contact_damage_holder_only: cast drops onto the holder cell; pickup, re-equip, and later Bonus Action re-trigger damage every contact and drop it again` | exit 1; the explicit non-holder contact remained at 40 HP instead of taking 2 Fire damage. | Damage again visits every physical-contact relationship before held/worn save handling. |
| `one_free_hand_refused` | Moved the one-handed capacity refusal boundary from exactly two occupied units to one or more. | `two_handed_in_one_hand: hand capacity succeeds with exactly one free unit, then refuses a two-handed item when both units are full` | exit 1; equipping the second one-handed item failed at the exactly-one-free boundary. | A second one-handed item again fills the remaining unit; only the exactly-full tuple refuses another held item. |

## Boundary and distinguishing-input coverage

- Hand capacity covers one occupied unit (equip succeeds) and exactly two occupied units (equip refuses); `one_free_hand_refused` was applied and killed.
- Interaction economy covers `objectInteractionsUsed` at zero (free Stow succeeds) and exactly one (second free Equip refuses); `second_interaction_free` was applied and killed, while the same Equip succeeds through a paid Utilize action.
- The Heat Metal pack fixture distinguishes held sword from worn armor: the sword becomes a ground item, while armor remains worn and receives both attack-roll and ability-check Disadvantage.
- The full sequence casts, damages all contacts, fails the holder's save, drops at the creature cell, picks up, re-equips through Utilize, and re-triggers through the bound Bonus Action lifecycle.
