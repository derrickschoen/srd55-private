# D391 mutation equivalence ledger

| Module | Mutant | One-line proof |
|---|---:|---|
| `src/vtt/session-record.ts` | 6497 | Removing the final switch arm's `break` cannot change control flow because no statement follows the switch and every event iteration proceeds directly to the loop boundary. |
| `src/vtt/stable-dom-render.ts` | 6567 | For every name returned by `getAttributeNames()` (or by keys of the coherent Map fallback), `getAttribute(name)` is non-null, so replacing its non-null guard with `true` cannot change the written attribute value. |
| `src/vtt/stable-dom-render.ts` | 6674 | In the error branch, an unkeyed interactive child stops the loop immediately, while a keyed child can be unstable only below an already-unkeyed ancestor that stops the loop before `root`, so replacing `unkeyed !== root` with `true` cannot change the reported ancestor. |
| `src/vtt/stable-dom-render.ts` | 6720 | The mutant only adds `syncElement(node, node)` for a new or tag-replaced node; attribute, text, child reconciliation, and mutable-state synchronization against the same object are all identity operations. |
| `src/vtt/survival-policy.ts` | 7077 | A selected casting guarantees a living target whose valid nonnegative HP gives `maximum - current >= average`; that target also satisfies `maximum + current >= average`, and every extra target admitted only by `+` has a smaller real deficit, so the unchanged deficit sort selects the same target. |
| `src/combat/controllers.ts` | 16 | While an abort listener is attached, `choose()` forbids replacing its pending request and both resolution paths clear it, so the pending request is either the captured request or absent and forcing the mismatch guard false cannot change the exported result. |
| `src/combat/controllers.ts` | 839 | Every repeated `stableSortKey` denotes the identical generated program and its score is a pure function of that program's target, so always replacing the prior entry writes the same exported candidate. |
| `src/combat/controllers.ts` | 844 | For an existing `stableSortKey`, the regenerated identical program necessarily has the same score, so forcing the strictly-greater comparison false preserves the same exported candidate. |
| `src/combat/controllers.ts` | 845 | For an existing `stableSortKey`, `>=` only replaces an identical program with its identical score, leaving every exported candidate field unchanged. |
| `src/combat/controllers.ts` | 846 | For an existing `stableSortKey`, `<=` only replaces an identical program with its identical score, leaving every exported candidate field unchanged. |
| `src/combat/controllers.ts` | 917 | `proposeRoundProgram()` always enumerates a living PC with score-40 Dash or a living monster with score-20 Dodge, so its exported `top` is never zero and forcing the zero-test false selects the original formula for every valid call. |
| `src/combat/controllers.ts` | 46 | `VisibleEncounterState` has a `viewer` property only on the DM projection, where its type is the literal `dm`; the player projection has `ownedCombatants` instead, so the removed `viewer === 'dm'` conjunct could not reject any exported input. |
| `src/combat/controllers.ts` | 137 | The strict-prefix guard proves `command.path.length < candidate.path.length`, and `every()` supplies only indices below `command.path.length`, so `candidate.path[index]` is always present and the removed undefined defense was unreachable through `isListedControllerAction()`. |
| `src/combat/controllers.ts` | 189 | As for mutant 46, a `viewer` property discriminates the DM projection and is typed as the literal `dm`, so removing the duplicate literal comparison leaves every `lastKnownHitPoints()` result unchanged. |
| `src/vtt/local-session-store.ts` | 1171 | Database version 1 runs `upgradeneeded` only for a new database whose object-store list is empty, so each unique required store is absent and replacing its absence guard with `true` creates the same three stores. |
| `src/vtt/local-session-store.ts` | 1125 | Every value reaching `isRecord()` from JSON or IndexedDB structured cloning is data-only: a JSON array cannot carry named fields and structured clone does not preserve array expandos, so admitting arrays cannot make any downstream record decoder accept one. |
| `src/vtt/local-session-store.ts` | 1187 | The encoded revision consumes only `Blob.stream()` as gzip input; replacing `{ type: 'application/json' }` with `{}` changes only the unused Blob MIME metadata and cannot change the compressed bytes or any exported store operation. |
| `src/vtt/local-session-store.ts` | 1188 | The `application/json` Blob type is never read and does not affect `Blob.stream()` bytes, gzip output, or decompression, so replacing that string with empty is observationally identical at every exported store boundary. |
| `src/combat/controllers.ts` | 52 | After the DM-only `viewer` branch returns, the `VisibleEncounterState` union leaves only the player projection, which always has `ownedCombatants`, so forcing that discriminant check true cannot change a result. |
| `src/combat/controllers.ts` | 133 | Final-report ID for the strict-prefix proof recorded at mutant 137: `command.path.length < candidate.path.length` makes every indexed listed cell present. |
| `src/combat/controllers.ts` | 219 | After the DM-only `viewer` branch returns, the remaining player projection necessarily has `ownedCombatants`, so forcing the second projection discriminant true preserves every lookup. |
| `src/combat/controllers.ts` | 857 | Final-report ID for the duplicate-program proof recorded at mutant 839: a repeated stable key regenerates the identical program with the identical pure target score. |
| `src/combat/controllers.ts` | 862 | Final-report ID for the equal-score proof recorded at mutant 844: the existing and regenerated candidates for one stable key have the same score. |
| `src/combat/controllers.ts` | 863 | Final-report ID for the equal-score proof recorded at mutant 845: `>=` can only replace an identical program and score. |
| `src/combat/controllers.ts` | 864 | Final-report ID for the equal-score proof recorded at mutant 846: `<=` can only replace an identical program and score. |
| `src/combat/controllers.ts` | 935 | Final-report ID for the nonzero-top proof recorded at mutant 917: every valid enumeration contains a positive-score Dash or Dodge candidate. |
| `src/vtt/local-session-store.ts` | 1148 | Final-report ID for the JSON/structured-clone array proof recorded at mutant 1125: admitting an array cannot make a downstream record decoder accept it. |
| `src/vtt/local-session-store.ts` | 1194 | Final-report ID for the version-1 upgrade proof recorded at mutant 1171: the new database has none of the three unique required stores. |
| `src/vtt/local-session-store.ts` | 1210 | Final-report ID for the unused Blob metadata proof recorded at mutant 1187. |
| `src/vtt/local-session-store.ts` | 1211 | Final-report ID for the unused Blob MIME string proof recorded at mutant 1188. |
| `src/vtt/local-session-store.ts` | 1577 | The loop visits both autosave pools with the same sort and cap; changing `snapshot.pool === pool` to `!==` merely swaps which iteration prunes each pool, leaving the union of removed snapshot IDs unchanged. |
| `src/vtt/local-session-store.ts` | 1252 | When `value.kind` is `autosave`, suppressing the `per_round` comparison either still admits `encounter_boundary` or falls through to the identical `{ kind: 'autosave', pool: 'per_round' }` default, so no decoded retention changes. |
| `src/vtt/local-session-store.ts` | 1306 | Suppressing the `typeof parsed.trigger === 'string'` rejection cannot admit a value: the immediately following closed trigger switch maps every non-string to `null`, which the next guard rejects. |
| `src/vtt/local-session-store.ts` | 1309 | Suppressing the `typeof parsed.pool === 'string'` rejection cannot admit a value: the later closed membership guard rejects every value other than the two pool string literals. |
| `src/vtt/local-session-store.ts` | 1434 | Killed by timeout: once a session has at least one revision, changing the deletion loop update from `revision += 1` to `revision -= 1` moves `1, 0, -1, ...` while the `revision <= revisionCount` guard remains permanently true. |
| `src/vtt/local-session-store.ts` | 1509 | Killed by timeout: when restore must delete a later revision, changing the loop update from `revision += 1` to `revision -= 1` moves away from `currentCount`, so its `revision <= currentCount` guard remains permanently true. |
| `src/vtt/local-session-store.ts` | 1664 | A queued write can remain when the publish promise settles only after a successful batch, where `#failure` is necessarily null; the failure handler acknowledges and removes every queued write, so forcing the null check true cannot schedule work after a failure. |
| `src/vtt/vane-warren.ts` | 7307 | Every roster entry reachable through the exported fight/session composers belongs to the fixed manifest and resolves to a bundled static statblock, so mutating the defensive unregistered-statblock error text cannot change an exported result. |
| `src/vtt/vane-warren.ts` | 7368 | Every fixed fight manifest lists its named leader as the first standing entry, so forcing the leader-search predicate true selects the same entry for every exported fight id. |
| `src/vtt/vane-warren.ts` | 7374 | Every fixed fight manifest contains its named leader and `createVaneWarrenFight` accepts only a closed fight id, so mutating the defensive missing-leader error text cannot change an exported result. |
| `src/vtt/vane-warren.ts` | 7562 | With an in-bounds preferred cell, radius `max(columns, rows) - 1` has already clamped and visited every in-bounds row and column; admitting radius `max(columns, rows)` repeats the same bounded cells and cannot find a new position. |
| `src/vtt/vane-warren.ts` | 7565 | Killed by timeout: after a blocked radius-zero candidate, changing `radius += 1` to `radius -= 1` produces `-1, -2, ...`, which always satisfies the positive upper-bound guard. |
| `src/vtt/vane-warren.ts` | 7575 | Killed by timeout: on a searched row, changing `row += 1` to `row -= 1` moves away from the inclusive upper row bound and leaves that loop permanently true. |
| `src/vtt/vane-warren.ts` | 7585 | Killed by timeout: on a searched column, changing `column += 1` to `column -= 1` moves away from the inclusive upper column bound and leaves that loop permanently true. |
| `src/vtt/vane-warren.ts` | 7664 | Each fixed alarm wave contains exactly one roster entry, so `every(already deployed)` and `some(already deployed)` evaluate identically for every wave reachable through the exported reducer. |
| `src/vtt/vane-warren.ts` | 7789 | The only fixed fight with conditional joiners is Last Muster, whose standing roster contains only its named leader, so forcing the leader-search predicate true selects the same entry. |
| `src/vtt/vane-warren.ts` | 7555 | Final-report ID for the exhaustive-radius proof recorded at mutant 7562: the last newly admitted radius only repeats already-clamped in-bounds cells. |
| `src/vtt/vane-warren.ts` | 7558 | Killed by timeout: after radius zero is blocked, decrementing radius produces an unbounded negative sequence that always satisfies the positive maximum-dimension guard. |
| `src/vtt/vane-warren.ts` | 7600 | When reinforcement initiative is appended, the existing Vane encounter has at least seven initiative slots including slots above 1; when initiative is empty the append branch returns `[]`, so `Math.max(-1, ...slots)` and `Math.max(+1, ...slots)` are identical on every reachable branch. |
| `src/vtt/vane-warren.ts` | 7612 | Each fixed alarm wave contains exactly one reinforcement, so its `profiles.map` index is always zero and `lastSlot + index + 1` equals `lastSlot - index + 1`. |
| `src/vtt/vane-warren.ts` | 7657 | Final-report ID for the single-member-wave proof recorded at mutant 7664: `every` and `some` are identical for each reachable one-entry wave. |
| `src/vtt/vane-warren.ts` | 7939 | Killed by timeout: forcing every member-id predicate true binds later PCs to the first party member's action inventory; the all-algorithm Vane chain then fails to make terminating progress and hits Stryker's 5-second bound. |
| `src/vtt/vane-warren.ts` | 7943 | Killed by timeout: forcing a found party member down the monster/regret action path corrupts PC turn selection; the all-algorithm Vane chain then fails to make terminating progress and hits Stryker's 5-second bound. |

The `shouldSpendHitDie` `>=`/`>` boundary mutant (6907) is not ledgered: the exported function accepts finite numeric deficits and modifiers, so `missingHitPoints: 6.5`, `sides: 8`, `constitutionModifier: 2` observably distinguishes `true` from `false`.
