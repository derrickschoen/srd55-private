# R10 — rules value objects, slot math, and class progression

Status: done (BUILD-PLAN increment 3)

## Delivered

- Added bounded ability-score and spell-level value objects, six-score hydration,
  modifiers, spell attack bonuses, and save DCs.
- Added every progression type's shared-caster rounding and independent preparation
  ceiling, validated per-class contributions, total-level proficiency, the complete
  level 1–20 shared slot table, epic capping, and the separate Pact Magic pool.
- Added transactional, idempotent persistence of all twelve 2024 base classes and
  their 240 progression rows, plus Eldritch Knight and Arcane Trickster and their 40
  subclass progression rows. Stable unique keys retain row IDs on reseed.
- Persisted the oracle's prepared/cantrip counts, class metadata, slot pools, Order
  options, Wizard spellbook/capability rules, and Warlock Mystic Arcanum rules.
- Added the persisted class-progression prepared-count lookup.

## Verification

- `npm test` — 7 files, 108 tests passed.
- Explicit integration run — 1 file, 60 persisted-state tests passed. The B00-owned
  Vitest config currently includes only `tests/unit`, so the integration file was run
  through Vitest's programmatic API without changing that config.
- `npm run typecheck` — passed.
- Property suite — six deterministic properties, 1,000 generated cases each.

## Sensitivity evidence

1. Ability modifier changed from floor to ceil and Spell Level 10 allowed: four
   intended value/bounds assertions failed; restored green.
2. Half-up rounding changed to down and Pact levels added to shared casting: five
   per-class, seed-build, separation, and generated-property assertions failed;
   restored green.
3. Level-20 shared slots, Pact level-1 count, and proficiency divisor mutated: nine
   exact table/boundary/property assertions failed; restored green.
4. Half-down preparation was allowed at class level 1: its exact preparation-vector
   assertion failed; restored green.
5. Persisted Wizard-20 preparation, Warlock-17 Arcanum, third-caster level-7 slots,
   and lookup output were mutated: four intended stored-row/lookup assertions failed;
   restored green.
6. Monotonicity, non-caster contribution, preparation bounds, and slot ordering were
   mutated together: the corresponding four generated properties failed with seeded
   counterexamples; restored green.
7. Both missing-class and missing-progression lookup errors were replaced with a
   fallback value: the two exact error assertions failed while their database-state
   assertions remained intact; restored green.

## Self-critique

- Rechecked the PHP rules, complete seeder, unit vectors, and owned feature cases.
  Full shared/Pact tables, all preparation boundaries, all class feature breakpoints,
  subclass breakpoints, Arcanum levels 11–20, and stable persisted identities are
  pinned.
- A sensitivity restore initially swapped the level-19/20 seventh-level slot counts;
  the full-table and persisted-row tests caught it before final verification, and the
  exact oracle rows were restored.
- Pact Apex report/command behavior belongs to later owned chunks; this slice provides
  and verifies its underlying Bard/Warlock progression, shared slots, Pact pool, and
  Arcanum data without touching those paths.
- Per instruction, review was self-contained: no sub-agent, Codex subprocess, or git
  command was used.
