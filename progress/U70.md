# U70 — application shell and character list

Status: done (BUILD-PLAN increment 20)

## Delivered

- Added the convention-discovered `/` screen with the oracle application
  shell, persisted theme, OPFS durability status, loading/empty/error states,
  deterministic character cards, warnings, responsive styling, and routing.
- Ported trimmed create/open and confirmed delete behavior through the typed
  query client.
- Added local catalog JSON import, complete SQLite download/confirmed restore,
  and portable character JSON download/import controls. Successful mutations
  refresh the list and transfer selector; failures remain recoverable alerts.
- Added explicit busy/disabled states and accessible names/status regions for
  every asynchronous or destructive action.

## Verification

- Owned Unit: 1 file, 7 tests passed.
- Owned Chromium: 1 file, 2 tests passed.
- `npm test`: 37 files, 304 tests passed.
- `npm run typecheck`: passed.
- Browser assertions read persisted `characters` and `spell_versions` rows
  after create, cancellation, delete, catalog import, database replacement,
  character import, rejected import, and reload.

## Sensitivity transitions

Every added test was made red at its intended assertion and restored:

1. Deleted immediately after create: Unit and Chromium observed no persisted
   character instead of Selene.
2. Bypassed confirmed deletion, changed class-card formatting, and forced
   durability unavailable: the three corresponding Unit tests failed.
3. Skipped catalog, database, and character import writes: all three transfer
   Unit tests failed on their fake persisted stores; Chromium retained the
   deliberately discarded database row instead of restoring the image.

## Self-critique

- Re-read all named PHP/Vue oracle files and checked exact labels, trimming,
  max length, singular warnings, empty-class copy, card ordering, navigation,
  and destructive confirmation text.
- Checked the typed client/backup contracts and chose raw SQLite for the
  browser database file while retaining the versioned envelope at RPC.
- Confirmed controls survive list refresh, update character export choices,
  preserve failed-import state, and use only owned files.
- No agent/subprocess, git command, registry/bootstrap/config edit, commit, or
  out-of-ownership write was used.
