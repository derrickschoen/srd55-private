# U71 — responsive spreadsheet planner

Status: done (BUILD-PLAN increment 21)

## Delivered

- Added the `/characters/:id` screen plugin and a persisted planner session
  backed only by the completed typed query/command clients.
- Ported class, subclass, ability, rules, class Order, Magic Initiate, standalone
  source, and removable-source editors with autosave, error, and revision state.
- Ported the filterable/sortable responsive spell-slot grid, eligible-spell
  combobox, locked/invalid/orphan/override/clear/replace flows, duplicate and
  invalid warnings, acknowledgement, live report, and casting math.
- Added in-session undo/redo, keyboard shortcuts, persisted operation history,
  named save points and restores, focus restoration, responsive table-to-card
  behavior, and reduced-motion handling.
- Ported the exact Sorcerous Burst, Chromatic Orb, d20/effect, defense-order,
  expected-value, and replayable seeded-roll dice oracle.

## Verification

- `npm run typecheck`: passed.
- Owned Vitest: 2 files, 4 tests passed.
- Owned Playwright: 1 file, 1 persisted browser workflow passed.
- `npm test`: 38 files, 301 tests passed.
- `npm run build`: passed; generated `dist/` output was removed afterward to
  preserve the exact owned-file boundary.

## Sensitivity transitions

Each owned test was driven red at its intended assertion, restored, and rerun
green:

1. Changed natural-critical recognition from 20 to 19; exact d20 composition
   failed.
2. Changed Burst explosion probability from 1/8 to 1/7; bounded expectation
   failed.
3. Changed live Burst explosion face from 8 to 7; seeded trace failed.
4. Misrouted the command inverse out of the undo stack; persisted undo state
   remained at Wisdom 18/revision 1.
5. Forced the legacy editor to persist `false`; Playwright observed
   `allow_legacy = 0` at revision 2.

## Self-critique

- Rechecked every listed oracle file and the existing TS workspace, command,
  eligibility, history, and save-point contracts.
- Verified the UI never treats a rendered projection as saved state: command
  completion always re-queries workspace/history, and browser assertions read
  SQLite rows before and after reload.
- Fixed review findings for focus loss across the disabled saving render,
  invalid selection-range calls, locked-slot Replace targeting, and late async
  rerenders after navigation.
- No registry, Worker, bootstrap, configuration, or other out-of-ownership
  source was changed. No sub-agent, Codex/Claude subprocess, git command, or
  commit was used.
