# T81 — PHP feature-to-browser parity

Status: done (BUILD-PLAN increment 24)

## Delivered

- Added exactly 28 Playwright workflows from the frozen manifest, translated
  from Laravel HTTP/Inertia entry points to typed Worker query, command,
  catalog, and backup RPCs.
- Added deterministic SQLite images for workspace mutations, nested sources,
  reports, printable data, catalog import, backup, concurrency, and the
  fresh-profile journey.
- Every mutation asserts OPFS-backed rows: characters/revisions, classes,
  sources, slots, spellbook, save points, acknowledgements, operations, and
  grouped audit differences. Read-only workflows compare complete exported
  SQLite images.
- Added the case map and production-owner sensitivity matrix.

## Verification

- `npm run typecheck`: passed.
- `npm test`: 40 files, 312 tests passed.
- Owned Chromium: 28/28 workflows passed in 2.8 minutes.
- Post-review workflow 5 (complete snapshot tables plus exact persisted diff)
  passed 1/1 after its final strengthening.
- Manifest count: exactly 28 `test(...)` declarations.

## Sensitivity transitions

- Green workflow 7 → bypassed its command RPC in the owned helper → persisted
  slot assertion failed (`current_spell_version_id` 1 instead of 8) → restored
  → filtered Chromium passed.
- Green workflow 24 → routed the test away from the report RPC → golden
  `report.character` failed as `undefined` → restored → final 28/28 passed.
- `tests/parity/PHP-FEATURE-SENSITIVITY.md` links every workflow to its
  production-owner red/restore/green evidence.

## Self-critique

- Re-read the frozen manifest and all selected PHP cases after implementation.
  Strengthened workflow 5 to assert the exact stored before/new audit values,
  not only portable-table capture.
- The initial complete run exposed two test-only mistakes: Set cardinality used
  an array matcher, and the warning fixture had legacy routes disabled. Both
  were corrected and rerun.
- Confirmed all 28 tests contain stored-row or complete-image assertions and
  reload checks cover CRUD, save points, import, both backups, and the
  fresh-profile journey.
- Only the five T81-owned files were written. No registry, Worker, bootstrap,
  build/config, external agent, Codex process, git command, or commit was used.
