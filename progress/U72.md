# U72 — reports and printable UI

Status: done (BUILD-PLAN increment 22)

## Delivered

- Added discovered, read-only routes for `/characters/:id/report` and
  `/characters/:id/print`, with unknown print variants falling back to the
  reference sheet.
- Ported the complete build-report presentation: character/caster/class data,
  preparation callout, source and provenance annotations, table math, Wizard
  states, duplicate versions/sources/slots/acknowledgements, and invalid or
  orphaned persisted selections.
- Ported both printable modes, exact unavailable/partial Tier 2 warnings,
  natural source ordering, spell facts and access annotations, Wizard states,
  and long-rest swap sections.
- Added responsive, keyboard-visible, semantic screen styling and print rules
  for hidden controls, stable one/two-column layouts, avoided card/heading
  splits, black print borders, and description widows/orphans.
- Renderers escape persisted text, sort collection copies only, and issue no
  command or mutation RPC.

## Persisted-state coverage

- Three SQLite-backed Unit tests assert exact stored character, source-slot,
  eligibility, orphan, override, free-cast, and description rows before
  presentation. Whole-database table hashes prove report building/rendering is
  read-only; serialized DTO equality proves rendering does not mutate inputs.
- Two Chromium tests install real SQLite fixture images, traverse the discovered
  routes and variant form, and compare the full exported database image plus
  stored character/slot rows before, after, and (for reports) after reload.

## Verification

- Focused UI Unit: 1 file, 3 tests passed.
- Focused Chromium: 1 file, 2 tests passed.
- `npm test`: 37 files, 300 tests passed.
- `npm run build`: TypeScript and Vite production build passed.
- `npm run test:browser`: 7 Chromium tests passed.

## Sensitivity evidence

Each production reversion was applied alone, failed at its intended assertion,
restored, and followed by green focused and full gates:

1. Removed capability provenance: the Unit route annotation assertion missed
   `Capability route`.
2. Removed natural source sorting: reversed input rendered `Gift 10` before
   `Gift 2`.
3. Changed the partial-data sentence: the exact notice assertion failed.
4. Disabled report route matching: Chromium could not find the report screen.
5. Printed controls instead of hiding them: Chromium computed `block`, not
   `none`, under print media.

## Self-critique

- Re-read all seven oracle files after implementation and checked every visible
  report/print branch, including legacy editions, null mechanics, inactive
  spellbook entries, empty collections, both incomplete-text states, and
  unknown variants.
- Confirmed every persisted string is escaped before `innerHTML`, headings and
  controls have semantic labels, table captions remain available to assistive
  technology, all generated IDs are deterministic, and Chromium canonicalizes
  `break-inside: avoid-page` to the asserted `avoid`.
- Removed production-build artifacts after verification. No git command,
  sub-agent, Codex/Claude subprocess, RPC/Worker/bootstrap/config edit, or
  out-of-ownership source change was used.
