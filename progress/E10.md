# E10 — Selection eligibility and eligible search

Status: done

## Files

- `src/eligibility/spell-selection-eligibility.ts`
  - Evaluates and refreshes persisted `valid`, `invalid`, and `unselected`
    state.
  - Enforces active catalog, legacy edition, level, list (including legacy
    identity membership), school, conjunctive tag, and unsupported collection
    rules in oracle order with exact public reasons.
- `src/eligibility/eligible-spell-search.ts`
  - Enforces character/slot ownership before candidate work.
  - Applies every predicate before a stable 50-row cap and returns the exact
    DTO with literal, case-insensitive substring search.
- `src/eligibility/spell-selection-service.ts`
  - Transactionally selects only into unlocked active slots owned by active
    sources and persists valid state.
- Added three owned test files covering evaluation, search, refresh, direct
  selection, persistence, rollback/no-write behavior, ownership, and guards.

## Oracle parity

- Retained selections are reclassified in place; refresh never clears their
  spell version ID.
- A legacy 2014 version can satisfy an allowed list through any list membership
  on the same identity, exactly as the PHP query does.
- Required tags use all-of semantics.
- Unsupported non-null selection collections throw only after the preceding
  predicates, matching the PHP evaluation order.
- Search predicates run in SQL before `LIMIT 50`; final evaluation remains a
  defense-in-depth filter.

## Tests

- `npm test`: 6 Unit files, 25 tests passed (6 E10 Unit tests).
- Explicit integration run (the frozen Vitest include targets Unit paths):
  1 file, 3 tests passed.
- `npm run typecheck`: passed.
- Every added test asserts stored rows: exact eligibility/reason/selection
  columns, retained IDs, owner IDs, rule inputs, and active/orphan/tombstoned
  states.

## Sensitivity transitions

Each of the nine added tests received its own production-behavior reversion:

- `unselected` → `valid` for empty slots failed the stored eligibility
  assertion.
- Changed required tags from all-of to any-of failed on stored `Missing Tag`
  state.
- Removed legacy identity-list fallback failed on stored legacy validity.
- Replaced substring search with exact matching failed the exact DTO assertion.
- Removed `LIMIT 50` failed with 51 persisted eligible candidates returned.
- Removed character ownership from slot lookup failed the cross-owner rejection.
- Bypassed service eligibility failed the inactive-candidate no-write guard.
- Removed active-source filtering failed the tombstoned-source guard.
- Disabled the legacy predicate failed retained-selection invalidation.

Every mutation was immediately restored; targeted tests returned green.

## Self-critique

- Rechecked predicate and reason ordering line-by-line against all three PHP
  services and the cited G1/G2/G3 and A3 cases.
- Tightened service row decoding to reject non-text JSON-list fields instead of
  coercing them.
- Confirmed SQL bindings are used for all user/catalog values and added an ID
  tie-breaker to make equal-level/equal-name ordering stable.
- No external agent was used, per the chunk instruction. No unresolved finding
  remains.
