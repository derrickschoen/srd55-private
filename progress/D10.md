# D10 — duplicate detection

Status: implemented

## Delivered

- Groups access routes by spell identity, preserving same-name distinct identities.
- Classifies unique, same-version wasteful/intentional, and cross-version conflicts.
- Returns the complete oracle contract with compact sources, selection slots, unique
  edition-sorted versions, exact explanations, and spell-name-sorted assessments.
- Produces browser-safe synchronous SHA-256 conflict fingerprints from the identity
  and edition-sorted version content keys.

## Verification

- `npm test` — 5 files, 23 tests passed (4 D10 detector tests).
- `npm run typecheck` — passed.
- The D10 scope is a pure route-DTO classifier and performs no persistence writes;
  acknowledgement persistence remains owned by C43.

## Sensitivity evidence

1. Raised the wasteful threshold from two counting selections to three: the complete
   contract test failed on the wasteful category and explanation.
2. Reverted grouping from identity ID to spell name: the same-name/different-identity
   test received one merged assessment instead of two.
3. Removed source compaction: the compact-source assertion received the duplicate
   `Wizard` source.
4. Removed edition sorting: the fingerprint test received `2024, 2014` and failed
   before accepting route-order-dependent fingerprint input.

Every mutation was restored, and the targeted 4-test suite returned green before the
final full-suite run.

## Self-critique

- Checked every emitted field against the oracle, including the distinct source and
  slot populations, selection-only version population, classification precedence,
  exact explanations, stable first-seen behavior for equal names, edition ordering,
  and fingerprint prefix/payload.
- Kept SHA-256 synchronous and browser-safe because report building is synchronous;
  the oracle fingerprint vector proves byte-for-byte parity.
- No external reviewer was used because the chunk explicitly requires in-session
  self-critique and prohibits spawning Codex.
