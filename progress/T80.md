# T80 — complete PHP Unit parity audit

Status: done (BUILD-PLAN increment 23)

## Delivered

- Audited all 57 top-level Pest cases in the six PHP Unit oracle files and
  recorded one explicit TypeScript evidence row per case.
- Reused the existing focused validator, value-object, rules/property,
  grant-rule, duplicate, assignment, command, access, report, and persisted
  class-progression coverage.
- Added only two missing cross-slice cases: exact assignment value-object
  hydration from SQLite references and the exact persisted six-class seed
  through the synchronous build-report boundary.
- Recorded direct T80 red/restored-green transitions plus the case ranges
  covered by each prior production owner's sensitivity evidence.

## Persisted-state coverage

- Assignment parity asserts all three stored exclusive-reference shapes,
  hydrates their exact value-object classes/IDs, and rereads identical rows
  afterward.
- Six-class parity asserts the six stored class/progression rows before
  deriving caster level 6, 4/3/3 shared slots, proficiency +3, and six
  first-level preparation ceilings, then rereads the unchanged rows.
- The map identifies the existing SQLite proof for every other case with a
  persisted representation; invalid contracts assert zero/residue-free writes.

## Verification

- Mechanical inventory: 57 PHP cases and 57 map rows.
- Focused parity: 1 file, 2 tests passed.
- `npm run typecheck`: passed.
- `npm test`: 41 files, 314 tests passed.

## Sensitivity transitions

Both T80 tests were driven red by an isolated production-behavior reversion in
a temporary worktree copy, restored, and rerun green:

1. Hydrated a selected stored reference as unassigned: the exact
   `UserSpellSelection` assertion failed while the selected ID remained stored.
2. Rounded `half_up` down: the exact stored six-class report assertion received
   caster level 5 / third-level slots 2 instead of 6 / 3.

The T80 production worktree was unchanged during both mutations.

## Self-critique

- Re-read every named PHP test and domain oracle, counted top-level Pest cases
  mechanically, and checked each mapping against executable TypeScript tests
  rather than file-level similarity.
- Checked dataset-bearing PHP cases against the consolidated TypeScript vectors,
  exact messages/contracts, deterministic property seeds, full shared/Pact
  tables, and stored JSON/row assertions.
- Confirmed the new tests are read-only after fixture setup, assert stored rows
  before and after derivation, and cover only seams not already pinned at the
  same boundary.
- No production, RPC, Worker, bootstrap, build, Playwright, or other
  out-of-ownership repository file was edited. No git command, commit,
  sub-agent, or Codex/Claude subprocess was used.

## Integrated review hardening

- Replaced the partial six-class character assertion with exact identity, name,
  ability, character-level, and proficiency equality.
- Review sensitivity changed that exact name expectation, observed the intended
  filtered failure, restored it, and returned the case green.
