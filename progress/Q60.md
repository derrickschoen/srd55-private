# Q60 — character, catalog, workspace, report, and history query RPC

Status: done (BUILD-PLAN increment 17)

## Delivered

- Ported the deterministic character-list card and complete workspace editing
  contracts, including class/subclass options, configured/order/removable
  sources, source catalogs, persisted slots, casting math, invalid selections,
  warning totals, and save points.
- Added transactional character create/get/delete and save-point capture, plus
  character-bound signed restore commands.
- Added deterministic decoded catalog reads for classes, subclasses, standalone
  sources, spells, memberships, and tags. Catalog text fields remain text and
  every JSON field is decoded before crossing RPC.
- Added parsed operation/change history DTOs with inverse commands, revisions,
  group/sequence metadata, reasons, reversible state, and before/after values.
- Exposed discovered typed `queries.*` handlers and one typed client for
  character/list/workspace, catalog, eligible spell search, save points, build
  reports, printable variants, and operation history. No sqlite-wasm row,
  statement, or connection object crosses the boundary.

## Verification

- `npm run typecheck`: passed.
- Owned query integration tests: 3 files, 9 tests passed.
- `npm test`: 36 files, 296 tests passed.
- Every owned test directly asserts persisted rows or a whole-table digest in
  addition to its returned DTO assertion. Read-only builders prove their
  character/catalog tables remain byte-equivalent.

## Sensitivity transitions

Each behavior was reverted alone, the named filtered test failed at its
intended assertion, the production behavior was restored, and the complete
owned suite returned green:

1. Removed list warning aggregation: persisted invalid/duplicate state reported
   `warning_count: 0` instead of `5`.
2. Removed workspace invalid-slot projection: persisted invalid slot IDs became
   an empty DTO list.
3. Replaced character deletion with a no-op update: the persisted character
   remained after direct CRUD.
4. Wrote the wrong save-point schema version: the stored row contained
   `reverted-version` instead of `a7-v1`.
5. Forced history changes non-reversible: the decoded persisted audit row
   reported `false`.
6. Removed catalog membership decoding: persisted Artificer/Wizard memberships
   returned as an empty list.
7. Replaced RPC character deletion with a no-op update: the persisted RPC
   character remained.
8. Replaced the RPC eligible query with a non-matching term: the exact Mage Hand
   DTO disappeared.
9. Skipped RPC save-point insertion: the workspace and persisted save-point
   table were empty.

## Self-critique

- Re-read every owned oracle builder/controller and the named feature tests,
  checking key order, stable ordering, wildcard-literal search, the 50-result
  cap, source configuration kinds, relevant attack/save math, save-point
  ownership, and report/print composition.
- Verified assumptions locally against the final schema and importer. This
  caught one mismatch before final verification: `spell_versions.components`
  is nullable catalog text, not JSON, so the catalog DTO now preserves it as
  text while decoding only actual JSON columns.
- Confirmed handler discovery needs no registry/Worker/bootstrap edit, every
  validator rejects extra keys, save-point signing uses the command executor's
  shared key, and all DTOs survive JSON serialization.
- No sub-agent, Codex/Claude subprocess, git command, out-of-ownership edit, or
  commit was used.
