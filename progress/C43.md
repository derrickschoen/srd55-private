# C43 — warnings, class updates, and snapshot restore

Status: done (BUILD-PLAN increment 14)

## Delivered

- `AcknowledgeWarningCommand` validates live conflicting-version fingerprints,
  trims canonical fingerprints and notes, inserts/updates acknowledgements,
  clears `invalidated_at`, and emits signed delete or prior-note inverses.
- `DeleteWarningAcknowledgementCommand` verifies integrity, scopes lookup and
  deletion to the character, preserves the prior note, and emits the
  acknowledgement inverse.
- `UpdateClassCommand` enforces class, level-20, and subclass ownership
  constraints; preserves custom config; creates/reactivates/tombstones class and
  subclass sources; regenerates grants; preserves stable selected slots; removes
  classes; and emits signed snapshot inverses inside an atomic outer transaction.
- `RestoreSnapshotCommand` verifies character-bound integrity, delegates active
  reference validation and atomic exact-row restore to `CharacterState`, and
  emits a signed redo snapshot.
- Added 7 persisted-state integration tests covering warning round trips and
  invalidation, cross-character deletion, class regeneration/removal/undo,
  repeated subclass switches, level constraints, rollback, exact snapshot
  restore/redo, inactive references, and cross-character signatures.

## Verification

- `npm run typecheck`: passed.
- `npm test`: 17 files, 144 tests passed.
- Programmatic owned integration run: 1 file, 7 tests passed. The shared Vitest
  config intentionally discovers only `tests/unit`.

## Sensitivity transitions

All 7 owned tests were driven red and then restored green:

1. Inverted live-warning detection and removed the character predicate from the
   delete lookup. The warning round trip rejected its active fingerprint and the
   ownership guard consumed the other character's row instead of rejecting.
2. Disabled level-dependent class generation and subclass synchronization. The
   stable-slot test found no persisted generated slot, and the switch test found
   no persisted subclass sources/configuration.
3. Removed the outer class transaction while retaining failing regeneration.
   The rollback test found the newly persisted class level and source instead of
   the exact pre-command snapshot.
4. Bypassed snapshot integrity and restore. The restore test retained the changed
   character/slot rows, and the foreign-signature test resolved instead of
   rejecting.

## Self-critique

- Rechecked every branch against the three PHP commands and the named workspace,
  guard, and abuse cases, including trimmed-input behavior, invalidation reset,
  first-class acquisition level, nullable configs, repeated subclass switches,
  source tombstoning, stable slot identity, inactive spell references, and
  character-scoped integrity.
- Confirmed all class/regeneration writes are enclosed by the outer transaction,
  restore validation precedes deletion, snapshot rows retain IDs/timestamps, and
  every owned assertion reads SQLite state.
- No registry, Worker, bootstrap, build/config, or out-of-ownership file was
  changed; no sub-agent, Codex/Claude subprocess, or git command was used.
