# G10 — grant-rule DSL

Status: done (BUILD-PLAN increment 4)

## Delivered

- Added the `FreeCast` value object with positive-use validation and the complete
  recovery/pool serialization contract.
- Added `GrantRule` object/JSON parsing, PHP-order validation, normalization,
  serialization, and typed properties for exactly six kinds: `fixed_spell`,
  `choice_from_list`, `choice_from_query`, `grant_source`, `capability`, and
  `spellbook_acquisition`.
- Preserved unrecognized extension fields while normalizing documented defaults,
  trimmed identifiers, choice levels, exact source-config activation, and
  free-cast metadata.
- Rejected the obsolete `choice_from_school` and `nested_source` names as unknown.

## Persisted-state coverage

All four G10 tests validate rules before inserting serialized rule arrays into
`feat_definitions.grant_rules`, then query and decode that stored column:

1. Exact normalized state for all six kinds, defaults, and extension preservation.
2. JSON round trips, source-config activation, four recoveries, and both pool scopes.
3. Every independent query predicate and all three granted-source reference forms.
4. Twenty-four malformed/obsolete contracts, invalid JSON, and invalid `FreeCast`
   uses; after every rejection, the stored definition count remains zero.

## Verification

- Focused: `npm test -- --run tests/unit/grants/grant-rule.test.ts` — 1 file,
  4 tests passed.
- Full: `npm test` — 5 files, 23 tests passed.
- `npm run typecheck` — passed.

## Sensitivity evidence

Each production mutation was isolated, run with the named test filter, restored,
and rerun green:

1. Defaulted `always_prepared` to true: the six-kind test failed on the reread
   stored JSON (`true` received versus `false` expected).
2. Serialized every pool as `per_spell`: the activation/free-cast test failed on
   the reread short-rest and at-will entries (`per_spell` versus `shared`).
3. Defaulted `level_max` to 8: the independent-predicate/source test failed on the
   reread minimum-only query (`8` versus `9`).
4. Bypassed fixed-spell count-one validation: the malformed-contract test failed
   at `fixed count should fail validation` because the invalid definition persisted
   instead of throwing.

The restored full suite produced the final 5-file / 23-test green result.

## Self-critique

- Compared validation and normalization order field-by-field with the PHP oracle.
- Fixed an initial nullish-default implementation so explicit `null` now fails
  boolean/integer/level validation instead of being mistaken for an absent field.
- Tightened integer checks to safe integers, matching PHP integer rather than float
  acceptance for JSON numbers.
- Isolated nested input/output data with defensive cloning and froze activation
  metadata so the readonly value object cannot silently change its serialized state.
- Confirmed the two obsolete kind names occur only in rejection tests, not production
  constants or branches. No unresolved findings remain.

No external agent was launched, as requested. No git command was run.
