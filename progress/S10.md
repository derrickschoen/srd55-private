# S10 — character state capture, restore, and diff

Status: done

## Delivered

- Added the `a7-v1` character snapshot service with the oracle's eleven character
  fields and all five restorable tables, captured in stable table/id order.
- Added complete pre-write validation for schema version, character shape, required
  fields, list/row shape, exact character ownership, positive spell IDs, and active
  spell-version references.
- Restores character metadata and every stored row in one nested-safe transaction.
  Child rows retain their original IDs, timestamps, and foreign-key identities;
  character `updated_at` is refreshed as in the PHP oracle.
- Added strict, deterministic character/table row diffs in table order and ascending
  numeric row-ID order.

## Verification

- Owned integration config (temporary `/tmp` config because the frozen project config
  currently collects only `tests/unit/**`): 1 file, 20 tests passed.
- `npm test`: 4 files, 19 tests passed.
- `npx tsc --noEmit --project tsconfig.app.json --incremental false`: passed.
- Persistence assertions query the six stored character/table surfaces directly,
  including exact restored IDs/timestamps and exact state after rejected restores.

## Sensitivity evidence

All mutations below were made in `character-state.ts`, observed red at the named
assertion, restored, and followed by the 20-test green run.

1. Omitted `warning_acknowledgements` from capture: the exact snapshot-key assertion
   failed. Reversed per-table diff sorting: the expected `2, 9` and `6, 12` row order
   assertions failed.
2. Replaced the missing-character message and treated a non-list diff table as empty:
   the two exact `toThrow` assertions failed independently.
3. Returned from `restore()` before validation: all 13 version/shape/ownership/spell
   boundary cases failed at their exact `toThrow(message)` assertions.
4. Skipped acknowledgement reinsertion: the direct stored-row equality assertion
   received `[]`. Skipped slot/spellbook reinsertion: the stored spell ID assertion
   received `null` instead of `1`.
5. Called restore writes outside `DatabaseContext.transaction()`: the injected
   primary-key failure left changed character metadata and deleted child rows, failing
   the exact persisted-state rollback assertion.

## Self-critique

- Cross-checked capture columns, table/delete/insert ordering, boundary messages, and
  ID-one behavior against the PHP oracle.
- Kept oracle-compatible row-shape validation rather than inventing a stricter portable
  format; database constraints remain the final guard and are transactionally safe.
- Quoted snapshot-derived column identifiers so malformed extra keys cannot become SQL
  injection. Such keys fail inside the transaction and leave live state unchanged.
- No shared contract/config/registry changes were needed. Per the chunk instruction,
  this review stayed within the session and used no external agent.
