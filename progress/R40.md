# R40 — deterministic build-report builder

Status: done (BUILD-PLAN increment 9)

## Delivered

- Ported the read-only character/caster summary with total character level,
  proficiency override/default, all abilities, alphabetized class sections,
  subclass overrides, independently rounded caster contributions, and exact
  prepared-count/preparation-ceiling data.
- Preserved the oracle's slot-pool distinction: one Spellcasting provider uses
  its persisted class/subclass table, multiple providers use the shared caster
  table, and Pact Magic is always reported separately.
- Added exact preparation callouts for shared-only, Pact-only, combined, and
  slotless builds, including correct ordinals.
- Returned deterministic access routes, duplicate classifications, active
  acknowledgement details, Wizard spellbook/prepared/ritual-only splits, and the
  complete explanation.
- Exposed the persisted invalid/orphaned/kept-override callout directly on the
  report using the PHP workspace DTO shape, including labels, casting ability,
  mechanically relevant attack/save values, duplicate state, and reasons.
- Centralized stable comparison functions with persisted IDs as final
  tie-breakers.

## Persisted-state coverage

`build-report.test.ts` has 5 integration tests. Every character-owned table
relevant to reporting is serialized before and after each build: character,
class levels, source instances, selection slots, spellbook entries, and warning
acknowledgements. Tests also directly assert the stored fraction metadata,
class/subclass rows, invalid-selection columns, acknowledgement invalidation,
and source provenance before asserting report values.

The fixture covers mixed half-casters (proving round-before-sum), Wizard and
Pact pools, same-version and cross-edition duplicates, preparation outside the
book, ritual-only access, active invalid selections, orphaned slots,
kept overrides, all supported subclass fractions, a persisted third-caster
table, and missing/unsupported diagnostics.

## Verification

- `npm run typecheck` — passed.
- Programmatic focused Vitest include — 1 file, 5 tests passed. The shared
  config intentionally includes only `tests/unit`.
- `npm test` — 17 files, 144 tests passed.

## Sensitivity evidence

Each owned test was filtered by name, driven red at its intended assertion with
a production-behavior reversion, immediately restored with `apply_patch`, and
rerun green:

1. Returned no invalid-selection rows: the exact three-row persisted warning
   callout failed (`[]` instead of orphaned/invalid/override rows).
2. Queried invalidated rather than active acknowledgements: the exact active
   acknowledgement assertion received `null`.
3. Disabled the lone-provider persisted slot-table branch: EK 7
   reported shared `{1: 3}` instead of its stored `{1: 4, 2: 2}` table.
4. Removed the explicit no-shared-slots wording from the Pact-only branch: the
   exact preparation callout assertion failed.
5. Mapped half-up subclasses to half-down: the stored `1/2`/`up` case reported
   `half_down` instead of `half_up`.

All five restored filtered runs and the final complete focused run were green.

## Self-critique

- Rechecked every branch of the PHP builder plus BuildReport,
  SubclassProgression, and PactApex cases against the final TypeScript.
- Verified report production contains no `exec`, transaction, INSERT, UPDATE,
  or DELETE call; persisted snapshots prove the composed access/eligibility
  readers also leave state byte-equivalent.
- Confirmed numeric slot order, class/source/spell order, assessment order, and
  ID tie-breakers are explicit; inactive acknowledgements and inactive
  spellbook routes remain excluded where the oracle excludes them.
- Fixed two review findings before final verification: empty persisted labels
  now use the oracle fallback, and invalid-row ability fallback matches the PHP
  workspace behavior.
- No sub-agent, Codex/Claude subprocess, git command, or out-of-scope file was
  used.
