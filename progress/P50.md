# P50 — deterministic printable spell-list builder

Status: done (BUILD-PLAN increment 10)

## Delivered

- Ported the PHP printable builder as a synchronous, read-only SQLite report:
  reference/full variants, character summary, source grouping, route
  annotations, Wizard state data, and text-completeness status.
- Added complete printable spell facts with persisted attack modes and save
  abilities, casting-action inference, tag-backed concentration/ritual flags,
  components, descriptions, and only the mechanically relevant attack bonus or
  save DC.
- Preserved `at_will`, `with_slots`, `slots_and_free_cast`,
  `free_cast_only`, `ritual_only`, and `available_on_long_rest` access wording.
- Added exact Cleric, Druid, and Wizard long-rest swap sections: active 2024
  list members from level 1 through each class ceiling, excluding identities
  currently prepared by that class and never presenting cantrips.
- Added locale-independent natural source ordering plus explicit
  level/name/mode/version/identity tie-breakers.

## Persisted-state coverage

`printable-list.test.ts` has 5 integration tests. Its dedicated fixture stores
three prepared-caster classes, naturally ambiguous source names (`Gift 2` and
`Gift 10`), both free-cast forms, attack/save fact rows, tag-only ritual and
concentration facts, current prepared identities, Wizard spellbook rows,
modern/legacy/inactive/list-level swap candidates, and mixed/complete/missing
description text.

Each test first asserts its controlling SQLite rows (slots and free-cast JSON,
list memberships, spellbook/tag state, or description columns). The
determinism test also hashes every persisted table before and after repeated
builds and proves the entire database remains byte-equivalent.

## Verification

- `npm run typecheck` — passed.
- Focused printable suite — 1 file, 5 tests passed.
- `npm test` — 31 files, 280 tests passed.

## Sensitivity evidence

Each owned test was filtered by name, driven red through one isolated
production reversal, restored with `apply_patch`, and rerun green:

1. Replaced natural comparison with lexical comparison: source order changed
   from `Gift 2, Gift 10` to `Gift 10, Gift 2`.
2. Allowed level-zero long-rest candidates: persisted `Guidance` appeared
   before `Bless` in the Cleric section.
3. Cleared the returned ritual-only split: persisted/tagged `Detect Magic`
   disappeared from the Wizard ritual-only state.
4. Disabled the zero-description branch: all-null persisted text reported
   `partial` instead of `unavailable`.
5. Returned stored descriptions in reference mode: the exact no-text assertion
   received description content.

All five restored filtered runs, the focused suite, typecheck, and the final
full suite were green.

## Self-critique

- Rechecked every PHP builder branch and the complete Vue printable DTO against
  the TypeScript output, including empty-text precedence, route deduplication,
  tag fallbacks, identity-based preparation exclusion, and fixed class-section
  order.
- Removed an initially exposed internal `source_instance_id` from source
  groups because the oracle DTO does not contain it.
- Confirmed production uses only `SELECT` reads and delegates to the already
  read-only build/access builders; no report path writes persisted state.
- No sub-agent, Codex/Claude subprocess, git command, commit, or out-of-scope
  file was used.
