# B00 — schema, contracts, storage, and composition substrate

Status: done (BUILD-PLAN increments 1–2)

## Delivered

- Preserved the oracle-verified 38-table, 59-index, two-trigger final schema.
- Added synchronous typed SQLite execution/query/scalar APIs, strict codecs,
  nested-safe savepoint transactions, and test factories.
- Added validated byte export, failure-safe image replacement/recovery, reset, and
  foreign-key-enabled reopen for memory and OPFS storage.
- Froze PHP-parity enums, row/read DTOs, the closed command payload union, open-ended
  RPC protocol/client/handler contracts, and discovery-only Worker/UI registries.
- Added non-production persisted-row inspection, serialized Worker dispatch,
  app/router/DOM skeletons, RPC documentation, and writable per-chunk tool caches.

## Verification

- `npm test` — 4 files, 18 tests passed.
- `npm run build` — typecheck and production Vite build passed.
- `npm run test:browser` — 2 Chromium tests passed.
- Schema parity evidence remains in `BUILD-PROGRESS.md`: all tables/columns/defaults,
  named and unnamed unique indexes, foreign keys/actions, CHECK, and triggers.
- Persistence evidence reads stored rows after transaction rollback, image
  replacement/recovery, and OPFS reload.

## Sensitivity evidence

1. `encodeBoolean()` forced true to `0`: stored codec assertion failed.
2. Transaction/savepoint wrappers bypassed: nested and outer rollback rows persisted,
   failing both exact row/count assertions.
3. Lifecycle replacement bypassed: discarded row remained; failure injection no
   longer rejected. Candidate prevalidation bypassed separately: live-connection
   identity assertion failed.
4. Registry sorting, duplicate detection, module dispatch, and production inspection
   gating bypassed: all four registry tests failed at their intended assertions.
5. SAH pool set to clear on initialization: reload returned `[]` instead of the stored
   character.
6. Worker lifecycle replacement bypassed: browser inspection retained the exact second
   character that replacement should remove.

Every mutation was restored before the final green Unit/build/browser runs. Earlier
increment-1 schema sensitivity transitions are recorded in `BUILD-PROGRESS.md`.

## Self-critique

The review caught the external read-only dependency symlink in Vite's serving/cache
paths, the need to validate candidates before closing the live connection, and the
need to serialize Worker requests while lifecycle handlers yield. It also removed the
RPC harness's indirect eager-registry import so parallel handler tests remain isolated.
These are resolved.
The user required self-review and prohibited spawning Codex, so no external reviewer
was used.
