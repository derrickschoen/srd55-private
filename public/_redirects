# Cloudflare Pages SPA fallback: serve index.html for client-side routes with a 200
# (not a 301). Cloudflare serves real static files first, so hashed assets are
# unaffected; this only catches app routes that have no matching file.
/*    /index.html    200
