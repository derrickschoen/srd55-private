node_modules/
node_modules
tmp/
.vite/
.vitest/
dist/
test-results/
playwright-report/
reports/*
!reports/module-state-census.json
.stryker-tmp*/
*.tsbuildinfo
.build-lock/
.plan-lock/
.worktrees/
.codex-locks/
orchestration/wave-state
orchestration/*.verdict
orchestration/logs/
.wave-lock/
# Scraped pages and everything derived from them. This line is the DEFAULT, not
# the guard: `git add -f` defeats it, and once a file is tracked `git
# check-ignore` stops reporting it at all, because a tracked file is never
# "ignored". The guard is
# tests/unit/tools/scraped-output-is-never-committed.test.ts, which asks
# `git ls-files` what is actually tracked.
scraped/
.tmp/
tools/discord-launcher/.env
__pycache__/
.vtt-soak-out/
.tmp-*
dnd-slim-runs/

# Licensing wall (D369 era): BG3-derived notes must never be committed to this public repo
docs/design/bg3-*.md

# Art delivered by the image-generation assistant stays untracked until it is
# promoted (art/requests/README.md); the greppable-source test would otherwise
# reject the raster, and the served art inventory is pinned.
art/incoming/*.png
art/incoming/*.webp
art/incoming/*.svg
